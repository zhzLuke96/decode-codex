const e = require(`./src-BZqs_tzA.js`),
  t = require(`./desktop-open-path-queue-CCElLwvI.js`),
  n = require(`./src-BAGkFo-J.js`),
  r = require(`./crash-reporter-env-CEsDRDdf.js`),
  i = require(`./file-based-logger-Uvi4O-N_.js`),
  a = require(`./sqlite-BqLffnB9.js`),
  o = require(`./feedback-desktop-log-archive-C-1tCT8H.js`);
let s = require(`node:url`),
  c = require(`electron`);
c = e.o(c);
let l = require(`node:os`);
l = e.o(l);
let u = require(`node:path`);
u = e.o(u);
let d = require(`node:util`),
  f = require(`node:crypto`),
  p = require(`node:fs`);
p = e.o(p);
let m = require(`node:fs/promises`);
m = e.o(m);
let h = require(`node:child_process`),
  g = require(`node:process`);
g = e.o(g);
let _ = require(`node:net`);
_ = e.o(_);
let v = require(`node:timers/promises`),
  y = require(`node:string_decoder`),
  b = require(`node:stream`),
  x = require(`node:stream/promises`),
  S = require(`node:readline`),
  C = require(`node:worker_threads`),
  w = require(`node:http`),
  T = require(`node:module`),
  E = require(`node:perf_hooks`),
  D = require(`node:buffer`),
  O = require(`node:inspector`);
O = e.o(O);
var k = /^[a-z0-9-_]+$/i;
function A(e) {
  let t = typeof e;
  if (t === `object`) {
    if (Array.isArray(e)) return `array`;
    if (e instanceof Date) return `date`;
  }
  return t;
}
function j(e) {
  for (let t = 0; t < e.length; t++) if (A(e[t]) !== `object`) return !1;
  return e.length != 0;
}
function M(e) {
  return JSON.stringify(e).replace(/\x7f/g, `\\u007f`);
}
function N(e, t, n, r) {
  if (n === 0)
    throw Error(
      `Could not stringify the object: maximum object depth exceeded`,
    );
  if (t === `number`)
    return isNaN(e)
      ? `nan`
      : e === 1 / 0
        ? `inf`
        : e === -1 / 0
          ? `-inf`
          : r && Number.isInteger(e)
            ? e.toFixed(1)
            : e.toString();
  if (t === `bigint` || t === `boolean`) return e.toString();
  if (t === `string`) return M(e);
  if (t === `date`) {
    if (isNaN(e.getTime())) throw TypeError(`cannot serialize invalid date`);
    return e.toISOString();
  }
  if (t === `object`) return P(e, n, r);
  if (t === `array`) return F(e, n, r);
}
function P(e, t, n) {
  let r = Object.keys(e);
  if (r.length === 0) return `{}`;
  let i = `{ `;
  for (let a = 0; a < r.length; a++) {
    let o = r[a];
    (a && (i += `, `),
      (i += k.test(o) ? o : M(o)),
      (i += ` = `),
      (i += N(e[o], A(e[o]), t - 1, n)));
  }
  return i + ` }`;
}
function F(e, t, n) {
  if (e.length === 0) return `[]`;
  let r = `[ `;
  for (let i = 0; i < e.length; i++) {
    if ((i && (r += `, `), e[i] === null || e[i] === void 0))
      throw TypeError(`arrays cannot contain null or undefined values`);
    r += N(e[i], A(e[i]), t - 1, n);
  }
  return r + ` ]`;
}
function I(e, t, n, r) {
  if (n === 0)
    throw Error(
      `Could not stringify the object: maximum object depth exceeded`,
    );
  let i = ``;
  for (let a = 0; a < e.length; a++)
    ((i += `${
      i &&
      `
`
    }[[${t}]]\n`),
      (i += ee(0, e[a], t, n, r)));
  return i;
}
function ee(e, t, n, r, i) {
  if (r === 0)
    throw Error(
      `Could not stringify the object: maximum object depth exceeded`,
    );
  let a = ``,
    o = ``,
    s = Object.keys(t);
  for (let e = 0; e < s.length; e++) {
    let c = s[e];
    if (t[c] !== null && t[c] !== void 0) {
      let e = A(t[c]);
      if (e === `symbol` || e === `function`)
        throw TypeError(`cannot serialize values of type '${e}'`);
      let s = k.test(c) ? c : M(c);
      if (e === `array` && j(t[c]))
        o +=
          (o &&
            `
`) + I(t[c], n ? `${n}.${s}` : s, r - 1, i);
      else if (e === `object`) {
        let e = n ? `${n}.${s}` : s;
        o +=
          (o &&
            `
`) + ee(e, t[c], e, r - 1, i);
      } else
        ((a += s),
          (a += ` = `),
          (a += N(t[c], e, r, i)),
          (a += `
`));
    }
  }
  return (
    e && (a || !o) && (a = a ? `[${e}]\n${a}` : `[${e}]`),
    a && o ? `${a}\n${o}` : a || o
  );
}
function L(e, { maxDepth: t = 1e3, numbersAsFloat: n = !1 } = {}) {
  if (A(e) !== `object`)
    throw TypeError(`stringify can only be called with an object`);
  let r = ee(0, e, ``, t, n);
  return r[r.length - 1] ===
    `
`
    ? r
    : r +
        `
`;
}
function R(e) {
  return z(e, n.li());
}
function z(e, t) {
  return t ? n.ri(e) : e;
}
function te(e, t) {
  return e.map((e) => z(e, t));
}
function ne(e) {
  return re(e, n.li());
}
function re(e, t) {
  return t ? n.ii(e, n.ai()) : e;
}
function ie(e, t) {
  return e.map((e) => re(e, t));
}
function ae(e) {
  return e
    ? {
        preferWslPaths: !0,
        convertWindowsPathToWslPath: n.ri,
        convertWslPathToWindowsPath: (e) => re(e, !0),
      }
    : { preferWslPaths: !1 };
}
function oe(e, t, r) {
  return n.eo(t) ? e : r ? z(e, !0) : he(e) ? re(e, !0) : e;
}
function se(e, t, n) {
  return pe(e, (e) => oe(e, t, n));
}
async function ce(e, t, n) {
  let r = new Map();
  for (let i of e) {
    let e = oe(i, t, n);
    r.has(e) || r.set(e, i);
  }
  let i = await Promise.all(
    Array.from(r, async ([e, r]) => {
      try {
        return [e, oe(await (0, m.realpath)(ue(r, t)), t, n)];
      } catch {
        return null;
      }
    }),
  );
  return Object.fromEntries(i.filter((e) => e != null));
}
function le(e, t, n) {
  return me(e, (e) => oe(e, t, n));
}
function ue(e, t) {
  return process.platform !== `win32` || n.eo(t) || !he(e) ? e : re(e, !0);
}
function de(e, t) {
  return pe(e, (e) => ue(e, t));
}
function fe(e, t) {
  return e ? me(e, (e) => ue(e, t)) : {};
}
function pe(e, t) {
  let n = new Set();
  return e.map(t).filter((e) => (n.has(e) ? !1 : (n.add(e), !0)));
}
function me(e, t) {
  let n = {};
  return (
    Object.entries(e).forEach(([e, r]) => {
      let i = t(e);
      n[i] ?? (n[i] = r);
    }),
    n
  );
}
function he(e) {
  return n.si(e) || e === `/` || /^\/(?!\/)/.test(e);
}
var ge = r.a(`devicecheck-attestation`),
  _e = `devicecheck.node`,
  ve = `v1`,
  ye = (0, T.createRequire)(__filename);
function be({ platform: e = process.platform } = {}) {
  return e === `darwin` || e === `win32`;
}
function xe({ arch: e = process.arch, platform: t = process.platform } = {}) {
  return t === `darwin` && e === `arm64`;
}
function Se({
  addon: e,
  arch: t = process.arch,
  bundleIdentifier: n,
  getSignals: r,
  platform: i = process.platform,
  resourcesPath: a = process.resourcesPath,
}) {
  return (o) =>
    o.method === `attestation/generate`
      ? be({ platform: i })
        ? Ce({
            addon: e,
            arch: t,
            bundleIdentifier: n,
            signals: r(),
            platform: i,
            resourcesPath: a,
          })
        : Promise.resolve(Ee(1, Date.now(), n))
      : null;
}
async function Ce({
  addon: e,
  arch: t = process.arch,
  bundleIdentifier: n,
  signals: r,
  platform: i = process.platform,
  resourcesPath: a = process.resourcesPath,
}) {
  let o = Date.now();
  if (!be({ platform: i })) return Ee(1, o, n);
  if (i !== `darwin`) return Ee(1, o, n, r);
  if (!xe({ arch: t, platform: i })) return Ee(2, o, n, r);
  let s;
  try {
    s = await (e ?? Te(a)).generateToken();
  } catch (e) {
    return Ee(4, o, n, r, { error: e });
  }
  return s.supported
    ? s.tokenBase64 == null
      ? Ee(4, o, n, r, { deviceCheckLatencyMs: s.latencyMs })
      : (ge().info(`DeviceCheck attestation generated`, {
          safe: {
            durationMs: Date.now() - o,
            deviceCheckLatencyMs: s.latencyMs ?? null,
          },
          sensitive: {},
        }),
        {
          token: De({
            bundleIdentifier: n,
            signals: r,
            token: s.tokenBase64,
            latencyMs: s.latencyMs,
          }),
        })
    : Ee(3, o, n, r, { deviceCheckLatencyMs: s.latencyMs });
}
async function we({
  addon: e,
  arch: t = process.arch,
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (!xe({ arch: t, platform: n })) return null;
  let i = await (e ?? Te(r)).generateToken();
  return i.supported ? (i.tokenBase64 ?? null) : null;
}
function Te(e) {
  if (e == null) throw Error(`DeviceCheck requires resourcesPath`);
  return ye((0, u.join)(e, `native`, _e));
}
function Ee(e, t, n, r, i = {}) {
  let a = {
    durationMs: Date.now() - t,
    errorCode: e,
    deviceCheckLatencyMs: i.deviceCheckLatencyMs ?? null,
  };
  return (
    i.error == null
      ? ge().info(`DeviceCheck attestation unavailable`, {
          safe: a,
          sensitive: {},
        })
      : ge().warning(`DeviceCheck attestation failed`, {
          safe: a,
          sensitive: { error: i.error },
        }),
    {
      token: De({
        bundleIdentifier: n,
        signals: r,
        errorCode: e,
        latencyMs: i.deviceCheckLatencyMs,
      }),
    }
  );
}
function De(e) {
  let t =
    `token` in e ? [Me(`token`, e.token)] : [Ne(`error_code`, e.errorCode)];
  return (
    t.push(Me(`bundle_id`, e.bundleIdentifier)),
    e.signals != null && t.push(Oe(e.signals)),
    e.latencyMs != null && t.push(Pe(`t`, e.latencyMs)),
    `${ve}.${ze(Buffer.concat([Buffer.from([160 + t.length]), ...t]))}`
  );
}
function Oe(e) {
  let t = ke([
    [0, Ie(e.schemaVersion)],
    [1, Ae(e.preferredLanguages.map(Fe))],
    [2, Fe(e.locale)],
    [3, Fe(e.timezone)],
    [4, Ie(e.screenSizeSum)],
    [5, je(e.screenScale)],
    [6, Fe(e.appSessionId)],
  ]);
  return Buffer.concat([Fe(`f`), Re(64, t.length), t]);
}
function ke(e) {
  return Buffer.concat([
    Re(160, e.length),
    ...e.flatMap(([e, t]) => [Ie(e), t]),
  ]);
}
function Ae(e) {
  return Buffer.concat([Re(128, e.length), ...e]);
}
function je(e) {
  return Number.isSafeInteger(e) && e >= 0 ? Ie(e) : Le(e);
}
function Me(e, t) {
  return Buffer.concat([Fe(e), Fe(t)]);
}
function Ne(e, t) {
  return Buffer.concat([Fe(e), Ie(t)]);
}
function Pe(e, t) {
  return Buffer.concat([Fe(e), Le(t)]);
}
function Fe(e) {
  let t = Buffer.from(e, `utf8`);
  return Buffer.concat([Re(96, t.length), t]);
}
function Ie(e) {
  if (!Number.isSafeInteger(e) || e < 0)
    throw Error(`Invalid CBOR unsigned integer: ${e}`);
  return Re(0, e);
}
function Le(e) {
  let t = Buffer.allocUnsafe(9);
  return ((t[0] = 251), t.writeDoubleBE(e, 1), t);
}
function Re(e, t) {
  if (t < 24) return Buffer.from([e + t]);
  if (t <= 255) return Buffer.from([e + 24, t]);
  if (t <= 65535) {
    let n = Buffer.allocUnsafe(3);
    return ((n[0] = e + 25), n.writeUInt16BE(t, 1), n);
  }
  if (t <= 4294967295) {
    let n = Buffer.allocUnsafe(5);
    return ((n[0] = e + 26), n.writeUInt32BE(t, 1), n);
  }
  throw Error(`CBOR length too large: ${t}`);
}
function ze(e) {
  return e
    .toString(`base64`)
    .replaceAll(`+`, `-`)
    .replaceAll(`/`, `_`)
    .replace(/=+$/u, ``);
}
var Be = n.xl({
    ambientSuggestions: n.ml(),
    appshotsEnabled: n.ml(),
    cuaPIP: n.ml(),
    browserPane: n.ml(),
    inAppBrowserUse: n.ml(),
    inAppBrowserUseAllowed: n.ml(),
    defaultLinkOpenTargetPreference: n.Vi.openLinkInTargetPreference.schema,
    linksDefaultInAppBrowser: n.ml(),
    localBackend: n.ml(),
    externalBrowserUse: n.ml(),
    externalBrowserUseAllowed: n.ml(),
    findShortcuts: n.ml().optional(),
    computerUse: n.ml(),
    computerUseNodeRepl: n.ml(),
    recordAndReplay: n.ml(),
    sites: n.ml(),
    control: n.ml(),
    deviceAttestation: n.ml(),
    dil: n.ml(),
    multiBrowserTabs: n.ml(),
    unifiedBrowserSkill: n.ml(),
    multiWindow: n.ml(),
    openAIMcpFormElicitations: n.ml(),
    processManager: n.ml(),
    quickChat: n.ml(),
    remoteControlClientScopedEnvironments: n.ml(),
    webMcp: n.ml(),
    deepResearch: n.ml(),
    visualize: n.ml(),
  }),
  Ve = Be.partial().strict(),
  He = {
    ambientSuggestions: !1,
    appshotsEnabled: !1,
    cuaPIP: !1,
    browserPane: !1,
    inAppBrowserUse: !1,
    inAppBrowserUseAllowed: !1,
    defaultLinkOpenTargetPreference: `external-browser`,
    linksDefaultInAppBrowser: !1,
    localBackend: !1,
    externalBrowserUse: !1,
    externalBrowserUseAllowed: !1,
    findShortcuts: !1,
    computerUse: !1,
    computerUseNodeRepl: !1,
    recordAndReplay: !1,
    sites: !1,
    control: !1,
    deviceAttestation: be(),
    dil: !1,
    multiBrowserTabs: !1,
    unifiedBrowserSkill: !1,
    multiWindow: !1,
    openAIMcpFormElicitations: !0,
    processManager: !1,
    quickChat: !1,
    remoteControlClientScopedEnvironments: !1,
    webMcp: !1,
    deepResearch: !1,
    visualize: !1,
  },
  Ue = Object.keys(He),
  We = { ...He },
  Ge = `CODEX_ELECTRON_DESKTOP_FEATURE_OVERRIDES`;
function Ke() {
  return We;
}
function qe(e) {
  return Be.safeParse(e).success;
}
function Je(e, t) {
  return Ue.some((n) => e[n] !== t[n]);
}
function Ye(e) {
  let t = { ...We, ...e, deviceAttestation: be() };
  return Ue.every((e) => t[e] === We[e]) ? !1 : ((We = t), !0);
}
function Xe(
  e,
  {
    buildFlavor: t = i.a.resolve(),
    env: n = g.default.env,
    platform: r = g.default.platform,
  } = {},
) {
  let a =
      r === `win32` && e.computerUse === !0
        ? { ...e, computerUseNodeRepl: !0 }
        : e,
    o =
      r === `win32` && n.CODEX_ELECTRON_ENABLE_WINDOWS_COMPUTER_USE === `1`
        ? { ...a, computerUse: !0, computerUseNodeRepl: !0 }
        : a,
    s = t === i.a.Dev ? Ze(n) : null;
  return s == null
    ? { ...o, deviceAttestation: be({ platform: r }) }
    : { ...o, ...s, deviceAttestation: be({ platform: r }) };
}
function Ze(e) {
  let t = e[Ge]?.trim();
  if (!t) return null;
  let n = JSON.parse(t);
  if (typeof n != `object` || !n || Array.isArray(n))
    throw Error(`${Ge} must be a JSON object`);
  let r = Ve.safeParse(n);
  if (!r.success) {
    let e = r.error.issues
      .map(
        (e) =>
          `${Ge}${e.path.length > 0 ? `.${e.path.join(`.`)}` : ``}: ${e.message}`,
      )
      .join(`; `);
    throw Error(e);
  }
  return r.data;
}
function Qe(e) {
  return Ke().ambientSuggestions && e.getEffective(n.Ki.enabled.key) === !0;
}
async function $e({ appServerConnection: e, settingsStore: t }) {
  if (!Qe(t)) return { enabled: !1, staleTimeMs: n.Qc(null) };
  let r = await e.getAccount();
  return { enabled: n.Zc(r.account), staleTimeMs: n.Qc(r.account) };
}
function et(e) {
  return Array.isArray(e) ? e.filter((e) => typeof e == `string`) : [];
}
function tt(e) {
  if (!e || typeof e != `object`) return {};
  let t = Object.entries(e).filter(([, e]) => typeof e == `string`);
  return Object.fromEntries(t);
}
function nt(e) {
  return {
    activeRoots: et(e.get(n.Gs.ACTIVE_WORKSPACE_ROOTS)),
    roots: et(e.get(n.Gs.WORKSPACE_ROOT_OPTIONS)),
    labels: tt(e.get(n.Gs.WORKSPACE_ROOT_LABELS)),
  };
}
function rt(e) {
  return nt(e).activeRoots;
}
function it(e) {
  return nt(e).roots;
}
function at(e) {
  return nt(e).labels;
}
function ot(e, t) {
  let r = e.get(n.Gs.REMOTE_PROJECTS) ?? [],
    i = [],
    a = {},
    o = new Set();
  for (let e of r) {
    if (e.hostId !== t) continue;
    let n = e.remotePath.trim();
    if (n.length === 0 || o.has(n)) continue;
    (i.push(n), o.add(n));
    let r = e.label.trim();
    r.length > 0 && (a[n] = r);
  }
  return { roots: i, labels: a };
}
function st(e, t) {
  e.set(n.Gs.ACTIVE_WORKSPACE_ROOTS, t);
}
function ct(e, t) {
  e.set(n.Gs.WORKSPACE_ROOT_OPTIONS, t);
}
function lt(e, t) {
  let r = [t, ...et(e.get(n.Gs.PROJECT_ORDER)).filter((e) => e !== t)];
  e.set(n.Gs.PROJECT_ORDER, r);
}
function ut(e, t) {
  let r = t(e.get(n.Gs.WORKSPACE_ROOT_OPTIONS) ?? null);
  return (e.set(n.Gs.WORKSPACE_ROOT_OPTIONS, r), r);
}
function dt(e, t) {
  let r = t(e.get(n.Gs.WORKSPACE_ROOT_LABELS) ?? null);
  return (e.set(n.Gs.WORKSPACE_ROOT_LABELS, r), r);
}
function ft(e, t) {
  if (rt(e).length > 0) return;
  let n = it(e);
  if (n.length > 0) {
    st(e, [n[0]]);
    return;
  }
  if (!t || t.length === 0) return;
  let r = new Set(),
    i = t
      .map((e) => e.path.trim())
      .filter((e) => e.length > 0)
      .filter((e) => (r.has(e) ? !1 : (r.add(e), !0)));
  i.length !== 0 &&
    (ct(e, i),
    st(e, [i[0]]),
    dt(e, (e) => {
      let n = { ...e };
      return (
        t.forEach((e) => {
          let t = e.name.trim();
          t && (n[e.path] ?? (n[e.path] = t));
        }),
        n
      );
    }));
}
var pt = r.a(`ambient-suggestions-background-refresh`);
function mt({
  appEvent: e,
  appServerClient: t,
  appServerConnectionRegistry: r,
  globalState: i,
  settingsStore: a,
}) {
  let o = new Map(),
    s = n.ui(t.hostConfig),
    c = async (e, r, i, a) => {
      let s = e.projectRoot,
        c = o.get(s);
      if (c != null) return c;
      let l = n.Gr(t.hostConfig.id, e.projectRoot, e.domain);
      if (l.generatedAtMs != null && Date.now() - l.generatedAtMs < a) return;
      let u = n
        .Kr({
          domain: e.domain,
          hostId: t.hostConfig.id,
          projectRoot: e.projectRoot,
          isProjectlessChat: e.isProjectlessChat,
          enabled: !0,
          appServerConnection: i,
          serviceTier: null,
          staleTimeMs: a,
        })
        .then(() => void 0)
        .catch((t) => {
          pt().warning(`Failed to refresh ambient suggestions`, {
            safe: { trigger: r },
            sensitive: { error: t, projectRoot: e.projectRoot },
          });
        })
        .finally(() => {
          o.delete(s);
        });
      return (o.set(s, u), u);
    },
    u = n.oo(z((0, l.homedir)(), s)),
    d = ht({ projectRoot: u, isProjectlessChat: !0 }),
    f = () => {
      let e = rt(i)[0] ?? null;
      if (e == null || e === `~`) return d;
      let t = n.oo(z(e, s));
      return u === t ? d : ht({ projectRoot: t, isProjectlessChat: !1 });
    },
    p = async (e) => {
      try {
        if (!Qe(a)) return;
        let n = r.getConnection(t.hostConfig.id),
          i = await $e({ appServerConnection: n, settingsStore: a });
        if (!i.enabled) return;
        await c(f(), e, n, i.staleTimeMs);
      } catch (t) {
        pt().warning(`Failed to refresh ambient suggestions`, {
          safe: { trigger: e },
          sensitive: { error: t },
        });
      }
    },
    m = e.subscribe((e) => {
      switch (e.type) {
        case `background`:
          p(e.type);
          return;
        case `foreground`:
        case `turnComplete`:
          return;
      }
    });
  return {
    dispose: () => {
      (m.dispose(), o.clear());
    },
  };
}
function ht({ projectRoot: e, isProjectlessChat: t }) {
  return { domain: null, projectRoot: e, isProjectlessChat: t };
}
function gt(e, t) {
  if (t.type === `projectless`) return null;
  let r = n.La(e.get(n.Gs.LOCAL_PROJECTS)),
    i = Object.hasOwn(r, t.projectId),
    a = it(e).includes(t.projectId);
  if (!i && !a) throw Error(`Automation project no longer exists`);
  return {
    projectId: t.projectId,
    roots: n
      .Aa({
        projectId: t.projectId,
        projectWritableRoots: n.ja(e.get(n.Gs.PROJECT_WRITABLE_ROOTS)),
        legacyRoot: i ? null : t.projectId,
      })
      .map(n.oo),
    isMetadataProject: i,
  };
}
function _t(e, t) {
  return t == null
    ? [n.oo(`~`)]
    : (gt(e, { type: `project`, projectId: t })?.roots ?? []);
}
function vt({ compatibilityCwds: e, executionEnvironment: t, projectId: n }) {
  if (t === `worktree` && (n == null || e.length !== 1))
    throw Error(`Worktree automations require a project with one folder`);
}
function yt({ setBrowserUseNativePipeEnabled: e }) {
  return {
    setDesktopFeatureAvailability: (t) => {
      t.inAppBrowserUse != null && e(t.inAppBrowserUse);
    },
    dispose: () => {
      e(!1);
    },
  };
}
var bt = [
    `1491199f46178eb038259aaea144492b8a8ade32bba5d2b4843c94936be551bb`,
    `6d25aa7656feac858f3a3bdaea5bcbab0dbfd426c9de8e6931ce90c399ee8e4f`,
  ],
  xt = `CODEX_ELECTRON_RESOURCES_PATH`;
function St({ env: e, resourcesPath: t }) {
  return e[xt]?.trim() || t;
}
var Ct = new Set([`about:blank`]);
function wt(e) {
  if (Ct.has(e)) return !0;
  let t;
  try {
    t = new URL(e);
  } catch {
    return !1;
  }
  return t.protocol === `http:` || t.protocol === `https:`;
}
var Tt = `browser/config.toml`,
  Et = `full_cdp_access_enabled`,
  Dt = `BROWSER_USE_AVAILABLE_BACKENDS`,
  Ot = `BROWSER_USE_CODEX_APP_BUILD_FLAVOR`,
  kt = `BROWSER_USE_CODEX_APP_VERSION`,
  At = `BROWSER_USE_DISABLE_AMBIENT_NETWORK`,
  jt = `BROWSER_USE_DISABLE_API_MEMBERS`,
  Mt = `BROWSER_USE_DISABLE_BROWSER_CAPABILITIES`,
  Nt = `BROWSER_USE_DISABLE_TAB_CAPABILITIES`,
  Pt = `BROWSER_USE_SECURITY_MODE`;
(function (e) {
  return (
    (e.Default = ``),
    (e.DisabledForLocalTesting = `disabled-for-local-testing`),
    (e.GaasBrowserEnvironment = `gaas-browser-environment`),
    e
  );
})({}).Default;
var Ft = class {
    nextId = 1;
    pendingRequests = new Map();
    requestHandlers = new Map();
    eventHandlers = new Map();
    constructor(e) {
      ((this.transport = e),
        this.transport.setMessageCallback((e) => {
          this.handleIncomingMessage(e);
        }),
        this.transport.addCloseListener?.((e) => {
          this.rejectPendingRequests(
            e?.message ?? `transport closed before response`,
          );
        }));
    }
    registerRequestHandlerObject(e) {
      [
        ...Object.getOwnPropertyNames(e),
        ...Object.getOwnPropertyNames(Object.getPrototypeOf(e)),
      ]
        .filter((t) => t !== `constructor` && typeof e[t] == `function`)
        .forEach((t) => {
          let n = e[t];
          n && this.registerRequestHandler(t, n.bind(e));
        });
    }
    registerRequestHandler(e, t) {
      this.requestHandlers.set(e, t);
    }
    addEventListener(e, t) {
      let n = e.toString(),
        r = this.eventHandlers.get(n) ?? [];
      (r.push(t), this.eventHandlers.set(n, r));
    }
    removeEventListener(e, t) {
      let n = e.toString(),
        r = this.eventHandlers.get(n) ?? [];
      this.eventHandlers.set(
        n,
        r.filter((e) => e !== t),
      );
    }
    sendNotification(e, t) {
      this.transport.sendMessage({ jsonrpc: `2.0`, method: e, params: t });
    }
    sendRequest(e, t) {
      let n = this.nextId++;
      return new Promise((r, i) => {
        this.pendingRequests.set(n, { resolve: r, reject: i });
        try {
          this.transport.sendMessage({
            jsonrpc: `2.0`,
            method: e.toString(),
            params: t,
            id: n,
          });
        } catch (e) {
          (this.pendingRequests.delete(n), i(e));
        }
      });
    }
    async handleIncomingMessage(e) {
      if (`method` in e) return this.handleIncomingRequest(e);
      if (e.id === void 0) return;
      let t = this.pendingRequests.get(e.id);
      if (t) {
        if ((this.pendingRequests.delete(e.id), `error` in e)) {
          t.reject(e.error?.message || `Something went wrong`);
          return;
        }
        t.resolve(e.result);
      }
    }
    rejectPendingRequests(e) {
      for (let t of this.pendingRequests.values()) t.reject(e);
      this.pendingRequests.clear();
    }
    async handleIncomingRequest(e) {
      if (e.id === void 0) {
        (this.eventHandlers.get(e.method ?? ``) ?? []).forEach((t) =>
          t(e.params),
        );
        return;
      }
      let t = this.requestHandlers.get(e.method ?? ``);
      if (!t) {
        this.transport.sendMessage({
          jsonrpc: `2.0`,
          id: e.id,
          error: {
            code: -1,
            message: `No handler registered for method: ${e.method}`,
          },
        });
        return;
      }
      try {
        let n = await t(e.params);
        this.transport.sendMessage({ jsonrpc: `2.0`, id: e.id, result: n });
      } catch (t) {
        this.transport.sendMessage({
          jsonrpc: `2.0`,
          id: e.id,
          error: {
            code: 1,
            message: t instanceof Error ? t.message : String(t),
          },
        });
      }
    }
  },
  It = class extends Ft {
    constructor(e, t, n = {}) {
      (super(e),
        (this.handler = t),
        (this.options = n),
        this.registerRequestHandlerObject(t),
        this.addEventListener(`moveMouse`, (e) => {
          Promise.resolve(this.handler.moveMouse(e)).catch((t) => {
            this.options.onMoveMouseError?.(t, e);
          });
        }));
    }
    ping() {
      return this.sendRequest(`ping`);
    }
    sendCdpEvent(e) {
      this.sendNotification(`onCDPEvent`, e);
    }
    sendCdpDetach(e) {
      this.sendNotification(`onCDPDetach`, e);
    }
    sendDownloadChange(e) {
      this.sendNotification(`onDownloadChange`, e);
    }
  };
async function Lt({ backendName: e, commandSets: t, handlers: n, payload: r }) {
  if (!t.some((e) => Object.prototype.hasOwnProperty.call(e.commands, r.type)))
    throw Rt(e, r);
  let i = n[r.type];
  if (i == null) throw Rt(e, r);
  return i(r);
}
function Rt(e, t) {
  return Error(`${e} does not support command "${t.type}".`);
}
var zt = (e) =>
    e === `win32` ? `\\\\.\\pipe\\codex-browser-use` : `/tmp/codex-browser-use`,
  Bt = `CODEX_CHROME_USER_DATA_DIR`,
  Vt = `NODE_REPL_DISABLE_ANALYTICS`,
  Ht = `NODE_REPL_INSTRUCTIONS_USE_CASE_BROWSER`,
  Ut = `NODE_REPL_INSTRUCTIONS_USE_CASE_CHROME`,
  Wt = `NODE_REPL_UNTRUSTED_ENV_ALLOWLIST`,
  Gt = `native_pipe`,
  Kt = n
    .xl({
      call_id: n.wl().trim().min(1).optional(),
      item_id: n.wl().trim().min(1).optional(),
      itemId: n.wl().trim().min(1).optional(),
      model: n.wl().trim().min(1).optional(),
      model_reasoning_effort: n.wl().trim().min(1).optional(),
      reasoningEffort: n.wl().trim().min(1).optional(),
      reasoning_effort: n.wl().trim().min(1).optional(),
      session_id: n.wl().trim().min(1).optional(),
      thread_id: n.wl().trim().min(1).optional(),
      threadId: n.wl().trim().min(1).optional(),
      turn_id: n.wl().trim().min(1).optional(),
    })
    .passthrough(),
  eee = n
    .xl({
      app: n
        .Tl([
          n.wl().trim().min(1),
          n
            .xl({
              bundleIdentifier: n.wl().trim().min(1).optional(),
              bundle_identifier: n.wl().trim().min(1).optional(),
            })
            .passthrough(),
        ])
        .optional(),
      bundleIdentifier: n.wl().trim().min(1).optional(),
      bundle_identifier: n.wl().trim().min(1).optional(),
      window: n
        .xl({ app: n.wl().trim().min(1).optional() })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  qt = n
    .xl({
      meta: n
        .xl({
          method: n.wl().trim().min(1).optional(),
          params: n.El().optional(),
          tool_params: n.El().optional(),
        })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  Jt = n
    .xl({
      action: n.dl([`accept`, `decline`, `cancel`]),
      _meta: n
        .xl({ persist: n.dl([`always`, `session`]).optional() })
        .passthrough()
        .optional(),
    })
    .passthrough();
function Yt(e, t, n) {
  let r = eee.safeParse(t),
    i = r.success ? en(r.data) : void 0,
    a = Kt.safeParse(n);
  return {
    toolName: e,
    transport: Gt,
    bundleIdentifier: i,
    model: a.success ? a.data.model : void 0,
    reasoningEffort: a.success ? tn(a.data) : void 0,
  };
}
function Xt(e) {
  let t = Kt.safeParse(e),
    n = t.success ? t.data : {},
    r = n.thread_id ?? n.threadId ?? n.session_id,
    i = n.item_id ?? n.itemId ?? n.call_id;
  return {
    pluginId: `computer-use@openai-bundled`,
    mcpServerName: `computer-use`,
    invocationSource: `model`,
    threadId: r,
    turnId: n.turn_id,
    itemId: i,
  };
}
function Zt(e) {
  let t = qt.safeParse(e);
  return t.success
    ? Yt(
        t.data.meta?.method ?? `unknown`,
        t.data.meta?.params ?? t.data.meta?.tool_params,
      )
    : { toolName: `unknown`, transport: Gt };
}
function Qt(e, t) {
  let n = Jt.safeParse(t);
  return n.success
    ? {
        type: `app-approval-resolved`,
        ...e,
        approvalResult: tee(n.data.action),
        approvalPersistence: n.data._meta?.persist,
      }
    : { type: `app-approval-resolved`, ...e, approvalResult: `unknown` };
}
function $t(e) {
  let t = { transport: e.transport };
  return (
    e.bundleIdentifier != null && (t.bundleIdentifier = e.bundleIdentifier),
    t
  );
}
function en(e) {
  return (
    e.bundleIdentifier ??
    e.bundle_identifier ??
    (typeof e.app == `string`
      ? e.app
      : (e.app?.bundleIdentifier ?? e.app?.bundle_identifier)) ??
    e.window?.app
  );
}
function tn(e) {
  return e.reasoning_effort ?? e.reasoningEffort ?? e.model_reasoning_effort;
}
function tee(e) {
  switch (e) {
    case `accept`:
      return `accepted`;
    case `decline`:
      return `declined`;
    case `cancel`:
      return `canceled`;
  }
}
function nn({ marketplaceName: e, marketplaces: t }) {
  return [e, n.As]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((e) => t.filter((t) => t.name === e));
}
function rn({ marketplaceName: e, marketplaces: t, pluginName: n }) {
  for (let r of nn({ marketplaceName: e, marketplaces: t })) {
    let e = r.plugins.find((e) => e.name === n);
    if (r.path != null && e != null)
      return { marketplace: { ...r, path: r.path }, plugin: e };
  }
  return null;
}
var an = `mcp_servers.${n.ws}`,
  on = `NODE_REPL_INSTRUCTIONS_USE_CASE_COMPUTER_USE`,
  sn = `SKY_CUA_SERVICE_PATH`,
  cn = `SKY_CUA_NATIVE_PIPE`,
  ln = `SKY_CUA_NATIVE_PIPE_DIRECTORY`,
  un = `Codex Computer Use.app`,
  dn = `\\\\.\\pipe\\codex-computer-use-${(0, f.randomUUID)()}`;
function fn({
  nativePipeDirectory: e,
  nativePipeEnabled: t = !1,
  serviceAppPath: n,
} = {}) {
  return {
    ...(n == null ? {} : { [sn]: n }),
    ...(t ? { [cn]: `1`, [ln]: e ?? dn } : {}),
  };
}
function pn({
  env: e = process.env,
  installedPluginServiceAppPath: t,
  pathExists: n = p.existsSync,
}) {
  let r = e[sn]?.trim();
  if (r) return r;
  let i = t?.trim();
  return i && n(i) ? i : null;
}
function mn({
  env: e = process.env,
  installedPluginRoot: t,
  pathExists: n = p.existsSync,
} = {}) {
  let r = t?.trim() || null;
  return {
    serviceAppPath: pn({
      env: e,
      installedPluginServiceAppPath: r == null ? null : u.default.join(r, un),
      pathExists: n,
    }),
  };
}
function hn({
  codexHome: e,
  env: t = process.env,
  marketplaceName: r = n.Ms(i.a.resolve()),
  marketplaces: a,
  pathExists: o = p.existsSync,
}) {
  for (let i of nn({ marketplaceName: r, marketplaces: a })) {
    let r = i.plugins.find(
      (e) =>
        e.name === `computer-use` &&
        e.installed &&
        e.enabled &&
        e.source.type === `local`,
    );
    if (r?.source.type === `local`)
      return mn({
        env: t,
        installedPluginRoot: n.Is({
          codexHome: e,
          localVersion: r.localVersion,
          marketplaceName: i.name,
          pluginName: r.name,
        }),
        pathExists: o,
      });
  }
  return mn({ env: t, pathExists: o });
}
var gn = n.Tl([
    n
      .xl({
        id: n.Tl([n.bl(), n.wl()]).optional(),
        jsonrpc: n.vl(`2.0`),
        method: n.wl(),
        params: n.El().optional(),
      })
      .passthrough(),
    n
      .xl({ id: n.Tl([n.bl(), n.wl()]), jsonrpc: n.vl(`2.0`), result: n.El() })
      .passthrough(),
    n
      .xl({
        error: n
          .xl({ code: n.bl(), data: n.El().optional(), message: n.wl() })
          .passthrough(),
        id: n.Tl([n.bl(), n.wl()]),
        jsonrpc: n.vl(`2.0`),
      })
      .passthrough(),
  ]),
  _n = n.xl({
    codexTurnMetadata: n.El().optional(),
    method: n.wl().trim().min(1),
    params: n.El().optional(),
  }),
  vn = n.xl({
    session_id: n.wl().trim().min(1),
    turn_id: n.wl().trim().min(1),
  }),
  yn = r.a(`computer-use-native-pipe-server`),
  bn = 4,
  xn = 8 * 1024 * 1024,
  Sn = `computer-use-approval:`,
  Cn = `requestComputerUseApproval`,
  wn = 300 * 1e3;
async function Tn({
  codexCliPath: e,
  nativePipeDirectory: t = dn,
  onAnalyticsEvent: r,
  windowsHelperPath: i,
  windowsHelperTransportModulePath: a,
}) {
  if (i == null || a == null)
    throw Error(`Windows Computer Use helper paths are unavailable`);
  let o = null,
    c = !1,
    l = new Map(),
    u = new Map(),
    d = (e) => {
      let t = u.get(e);
      t != null && (r?.({ type: `ended`, ...t }), u.delete(e));
    },
    f = async () => (
      (o ??= new (
        await import((0, s.pathToFileURL)(a).href)
      ).WindowsHelperTransport({
        helperArgs: [`--parent-pid`, String(process.pid)],
        helperCommand: i,
        helperEnv: e == null ? void 0 : { [n.Do]: e },
      })),
      o
    ),
    p = async () => {
      let e = o;
      e != null &&
        ((o = null),
        await e.close(),
        yn().debug(`computer-use native pipe helper transport closed`, {
          safe: {},
        }));
    },
    m = An(r),
    h = await Nn({
      onListening: (e) => {
        (yn().debug(`computer-use native pipe listening`, {
          safe: {},
          sensitive: { pipePath: e },
        }),
          r?.({ type: `server-launched`, transport: Gt }));
      },
      onMessage: (e, t, n) => {
        let i = gn.safeParse(e);
        if (!i.success || m.handleApprovalResponse(i.data)) return;
        let a = En(i.data);
        if (c && !a) {
          Dn(i.data, t);
          return;
        }
        (a || (c = !0),
          On({
            activeTurnBySocket: l,
            activeTurnAnalyticsBySocket: u,
            closeHelperTransport: p,
            getHelperTransport: f,
            approvalBridge: m,
            message: i.data,
            onAnalyticsEvent: r,
            sendResponse: t,
            socket: n,
          }).finally(() => {
            a || (c = !1);
          }));
      },
      onLastSocketClosed: () => {
        (yn().debug(`computer-use native pipe last socket closed`, {
          safe: {},
        }),
          m.rejectPendingApprovals(
            Error(`Computer Use native pipe client disconnected`),
          ),
          p().catch((e) => {
            yn().warning(`computer-use native pipe helper close failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }));
      },
      onSocketClose: (e) => {
        (d(e), l.delete(e));
      },
      onSocketError: (e) => {
        yn().warning(`computer-use native pipe socket error`, {
          safe: {},
          sensitive: { error: e },
        });
      },
      pipePath: t,
    });
  return {
    pipePath: h.pipePath,
    closeActiveTurn: async (e) => {
      let t = Fn(e);
      if (!Pn(l, e)) return !1;
      for (let [e, n] of l) n === t && l.delete(e);
      return (await p(), !0);
    },
    hasActiveTurn: (e) => Pn(l, e),
    dispose: async () => {
      m.rejectPendingApprovals(
        Error(`Computer Use native pipe is shutting down`),
      );
      for (let e of u.keys()) d(e);
      try {
        await p();
      } finally {
        ((o = null), (c = !1), await h.dispose(), l.clear(), u.clear());
      }
    },
  };
}
function En(e) {
  if (`method` in e && e.method === `close`) return !0;
  if (!(`method` in e) || e.method !== `request`) return !1;
  let t = _n.safeParse(e.params);
  return t.success && t.data.method === `end_turn`;
}
function Dn(e, t) {
  e.id != null &&
    t({
      error: {
        code: -32e3,
        message: `Computer Use helper already has an active request`,
      },
      id: e.id,
      jsonrpc: `2.0`,
    });
}
async function On({
  activeTurnBySocket: e,
  activeTurnAnalyticsBySocket: t,
  closeHelperTransport: n,
  getHelperTransport: r,
  approvalBridge: i,
  message: a,
  onAnalyticsEvent: o,
  sendResponse: s,
  socket: c,
}) {
  let l = a.id;
  if (l == null || !(`method` in a)) return;
  let u = null,
    d = null,
    f = null,
    p = null;
  try {
    if (a.method === `ping`) {
      s({ id: l, jsonrpc: `2.0`, result: `pong` });
      return;
    }
    if (a.method === `close`) {
      let r = t.get(c);
      (r != null && o?.({ type: `ended`, ...r }),
        t.delete(c),
        e.delete(c),
        yn().debug(`computer-use native pipe close requested`, { safe: {} }),
        await n(),
        s({ id: l, jsonrpc: `2.0`, result: null }));
      return;
    }
    if (a.method !== `request`)
      throw Error(`Unsupported Computer Use native pipe method`);
    let m = kn(a.params),
      h = Yt(m.method, m.params, m.codexTurnMetadata);
    ((d = Xt(m.codexTurnMetadata)),
      (f = h),
      jn({
        activeTurnAnalyticsBySocket: t,
        activeTurnBySocket: e,
        analyticsFields: h,
        onAnalyticsEvent: o,
        request: m,
        socket: c,
      }),
      (u = E.performance.now()));
    let g = await (
      await r()
    ).request(m.method, m.params, {
      codexTurnMetadata: m.codexTurnMetadata,
      createElicitation: (e) => i.requestApprovalForSender(s, e),
    });
    ((p = `completed`), s({ id: l, jsonrpc: `2.0`, result: g }));
  } catch (e) {
    (u != null && p == null && (p = `failed`),
      s({
        error: {
          code: -32e3,
          message: e instanceof Error ? e.message : String(e),
        },
        id: l,
        jsonrpc: `2.0`,
      }));
  } finally {
    u != null &&
      d != null &&
      f != null &&
      p != null &&
      o?.({
        type: `tool-called`,
        ...f,
        ...d,
        terminalStatus: p,
        mcpErrorPresent: p === `failed`,
        durationMs: Math.round(E.performance.now() - u),
      });
  }
}
function kn(e) {
  let t = _n.parse(e);
  return {
    codexTurnMetadata: t.codexTurnMetadata,
    method: t.method,
    params: t.params,
  };
}
function An(e) {
  let t = new Map();
  return {
    handleApprovalResponse: (n) => {
      let r = n.id;
      if (typeof r != `string` || !r.startsWith(Sn)) return !1;
      let i = t.get(r);
      if (i == null) return !0;
      if (
        (t.delete(r), clearTimeout(i.timeout), `error` in n && n.error != null)
      )
        return (e?.(Qt(i.tool, void 0)), i.reject(Error(n.error.message)), !0);
      let a = `result` in n ? n.result : void 0;
      return (e?.(Qt(i.tool, a)), i.resolve(a), !0);
    },
    rejectPendingApprovals: (e) => {
      for (let n of t.values()) (clearTimeout(n.timeout), n.reject(e));
      t.clear();
    },
    requestApprovalForSender: (n, r) =>
      new Promise((i, a) => {
        let o = `${Sn}${(0, f.randomUUID)()}`,
          s = Zt(r);
        e?.({ type: `app-approval-requested`, ...s });
        let c = setTimeout(() => {
          (t.delete(o),
            e?.({
              type: `app-approval-resolved`,
              ...s,
              approvalResult: `timeout`,
            }),
            a(Error(`Computer Use app approval timed out`)));
        }, wn);
        (c.unref(),
          t.set(o, { reject: a, resolve: i, timeout: c, tool: s }),
          n({ id: o, jsonrpc: `2.0`, method: Cn, params: r }));
      }),
  };
}
function jn({
  activeTurnAnalyticsBySocket: e,
  activeTurnBySocket: t,
  analyticsFields: n,
  onAnalyticsEvent: r,
  request: i,
  socket: a,
}) {
  let o = Mn(i);
  if (o == null || t.get(a) === o) return;
  let s = e.get(a);
  (s != null && r?.({ type: `ended`, ...s }), t.set(a, o));
  let c = $t(n);
  (e.set(a, c), r?.({ type: `started`, ...c }));
}
function Mn(e) {
  let t = vn.safeParse(e.codexTurnMetadata);
  return t.success
    ? Fn({ sessionId: t.data.session_id, turnId: t.data.turn_id })
    : null;
}
async function Nn({
  onListening: e,
  onLastSocketClosed: t,
  onMessage: n,
  onSocketClose: r,
  onSocketError: i,
  pipePath: a,
}) {
  await In(a);
  let o = new Set(),
    s = new Map(),
    c = _.default.createServer((e) => {
      (o.add(e), s.set(e, Buffer.alloc(0)));
      let a = (t) => {
        e.destroyed || e.write(nee(JSON.stringify(t)));
      };
      (e.on(`data`, (t) => {
        let r = Buffer.isBuffer(t) ? t : Buffer.from(t),
          i = s.get(e) ?? Buffer.alloc(0),
          o = ree(Buffer.concat([i, r]));
        if (o == null) {
          (s.delete(e), e.destroy());
          return;
        }
        s.set(e, o.remainingData);
        for (let t of o.messages)
          try {
            n(JSON.parse(t), a, e);
          } catch {
            continue;
          }
      }),
        e.on(`error`, i),
        e.on(`close`, () => {
          (r(e), o.delete(e), s.delete(e), o.size === 0 && t());
        }));
    });
  return (
    await new Promise((e, t) => {
      (c.once(`error`, t),
        c.listen(a, () => {
          (c.off(`error`, t), e());
        }));
    }),
    e(a),
    {
      pipePath: a,
      dispose: async () => {
        for (let e of o) e.destroy();
        (o.clear(),
          s.clear(),
          await new Promise((e) => {
            c.close(() => {
              e();
            });
          }),
          Ln(a));
      },
    }
  );
}
function Pn(e, t) {
  let n = Fn(t);
  for (let t of e.values()) if (t === n) return !0;
  return !1;
}
function Fn({ sessionId: e, turnId: t }) {
  return `${e}\0${t}`;
}
function nee(e) {
  let t = Buffer.from(e, `utf8`),
    n = Buffer.alloc(bn + t.length);
  return (
    (0, l.endianness)() === `LE`
      ? n.writeUInt32LE(t.length, 0)
      : n.writeUInt32BE(t.length, 0),
    t.copy(n, bn),
    n
  );
}
function ree(e) {
  let t = [],
    n = 0;
  for (; e.length - n >= bn; ) {
    let r =
      (0, l.endianness)() === `LE` ? e.readUInt32LE(n) : e.readUInt32BE(n);
    if (r > xn) return null;
    let i = bn + r;
    if (e.length - n < i) break;
    (t.push(e.subarray(n + bn, n + i).toString(`utf8`)), (n += i));
  }
  return { messages: t, remainingData: e.subarray(n) };
}
async function In(e) {
  (0, l.platform)() === `win32` ||
    !(0, p.existsSync)(e) ||
    (0, p.unlinkSync)(e);
}
function Ln(e) {
  (0, l.platform)() !== `win32` && (0, p.existsSync)(e) && (0, p.unlinkSync)(e);
}
var Rn = `--previous-notify`,
  zn = `turn-ended`,
  Bn = `codex-computer-use.exe`;
async function Vn({ codexHome: e, windowsHelperPath: t }) {
  let n = u.default.join(e, `config.toml`),
    r = await Un(n),
    i = Hn(r, t);
  return i.content === r
    ? { status: `already-present` }
    : (await (0, m.mkdir)(u.default.dirname(n), { recursive: !0 }),
      await (0, m.writeFile)(n, i.content, `utf8`),
      { status: i.hadNotify ? `repaired` : `installed` });
}
function Hn(e, t) {
  let n = Wn(e),
    r = Gn(n),
    i = [t, zn];
  return (
    r != null && i.push(Rn, JSON.stringify(r)),
    Xn(n, i)
      ? { content: e, hadNotify: !0 }
      : { content: Jn(e, L({ notify: i }).trimEnd()), hadNotify: n != null }
  );
}
async function Un(e) {
  try {
    return await (0, m.readFile)(e, `utf8`);
  } catch (e) {
    if (e instanceof Error && `code` in e && e.code === `ENOENT`) return ``;
    throw e;
  }
}
function Wn(e) {
  if (e.trim() === ``) return null;
  let t = n.Qr(e, { integersAsBigInt: !1 });
  if (t.notify == null) return null;
  if (!Yn(t.notify)) throw Error(`Codex notify config must be a string array`);
  return t.notify;
}
function Gn(e) {
  return e == null ? null : Kn(e) ? Gn(qn(e)) : e;
}
function Kn(e) {
  let [t, n] = e;
  return (
    n === zn && t?.replaceAll(`\\`, `/`).split(`/`).at(-1)?.toLowerCase() === Bn
  );
}
function qn(e) {
  let t = e[e.indexOf(Rn) + 1];
  if (t == null) return null;
  try {
    let e = JSON.parse(t);
    return Yn(e) ? e : null;
  } catch {
    return null;
  }
}
function Jn(e, t) {
  let n = [],
    r = !0,
    i = 0;
  for (let t of e.split(/\r?\n/)) {
    let e = t.trimStart(),
      a = t.split(`[`).length - t.split(`]`).length;
    (r && i === 0 && e.startsWith(`[`) && (r = !1),
      r
        ? i > 0
          ? (i += a)
          : /^notify\s*=/.test(e)
            ? (i = a)
            : n.push(t)
        : n.push(t));
  }
  let a = n.findIndex((e) => e.trimStart().startsWith(`[`)),
    o = a === -1 ? n : n.slice(0, a),
    s = a === -1 ? [] : n.slice(a);
  return `${[...o, t, ...s]
    .join(
      `
`,
    )
    .trim()}\n`;
}
function Yn(e) {
  return Array.isArray(e) && e.every((e) => typeof e == `string`);
}
function Xn(e, t) {
  return e != null && e.length === t.length && e.every((e, n) => e === t[n]);
}
var Zn = r.a(`computer-use-native-pipe`),
  Qn = null;
function $n() {
  return Qn?.ensureReadyPipePath() ?? Promise.resolve(null);
}
function er({
  codexHome: e = n.ni(),
  ensureNotifyConfig: t = Vn,
  codexCliPath: r,
  onAnalyticsEvent: i,
  platform: a = process.platform,
  resourcesPath: o = St({
    env: process.env,
    resourcesPath: process.resourcesPath,
  }),
  startServer: s = Tn,
}) {
  let c = !1,
    l = null,
    u = null,
    d = null,
    f = null,
    p = {},
    m = async () => {
      let e = l;
      ((l = null), (u = null), e != null && (await e.dispose()));
    },
    h = () => ({
      windowsHelperPath: n.lr(o),
      windowsHelperTransportModulePath: n.ur(o),
    }),
    g = (n) => {
      if (!c || a !== `win32` || l != null) return Promise.resolve();
      if (f != null) return f;
      let o = Promise.resolve()
        .then(async () => {
          try {
            let o = n ?? h(),
              { windowsHelperPath: d, windowsHelperTransportModulePath: f } = o;
            if (!c || a !== `win32`) return;
            if (d == null || f == null) {
              Zn().info(`computer-use native pipe helper paths unavailable`, {
                safe: {
                  missingHelperPath: d == null,
                  missingTransportModulePath: f == null,
                  platform: a,
                },
              });
              return;
            }
            try {
              let n = await t({ codexHome: e, windowsHelperPath: d });
              Zn().info(`computer-use notify config ensure finished`, {
                safe: { platform: a, status: n.status },
              });
            } catch (e) {
              throw (
                Zn().warning(`computer-use notify config ensure failed`, {
                  safe: { platform: a },
                  sensitive: { error: e },
                }),
                e
              );
            }
            let p = await s({
              codexCliPath: r,
              onAnalyticsEvent: i,
              windowsHelperPath: d,
              windowsHelperTransportModulePath: f,
            });
            if (!c || a !== `win32`) {
              await p.dispose();
              return;
            }
            ((l = p),
              (u = o),
              Zn().info(`computer-use native pipe startup ready`, {
                safe: { platform: a },
                sensitive: { pipePath: l.pipePath },
              }));
          } catch (e) {
            Zn().warning(`computer-use native pipe startup failed`, {
              safe: { platform: a },
              sensitive: { error: e },
            });
          }
        })
        .finally(() => {
          f === o && (f = null);
        });
      return ((f = o), o);
    },
    _ = () => {
      if (d != null) return d;
      let e = Promise.resolve()
        .then(async () => {
          try {
            if (
              !c ||
              a !== `win32` ||
              (f != null && (await f), !c || a !== `win32`)
            )
              return;
            if (l == null) {
              await g();
              return;
            }
            let e = h();
            if (
              u?.windowsHelperPath === e.windowsHelperPath &&
              u?.windowsHelperTransportModulePath ===
                e.windowsHelperTransportModulePath
            )
              return;
            (Zn().info(`computer-use native pipe helper paths changed`, {
              safe: { platform: a },
            }),
              await m(),
              await g(e));
          } catch (e) {
            Zn().warning(`computer-use native pipe refresh failed`, {
              safe: { platform: a },
              sensitive: { error: e },
            });
          }
        })
        .finally(() => {
          d === e && (d = null);
        });
      return ((d = e), e);
    },
    v = {
      closeActiveTurn: (e) => l?.closeActiveTurn(e) ?? Promise.resolve(!1),
      ensureReadyPipePath: async () => (await g(), l?.pipePath ?? null),
      hasActiveTurn: (e) => l?.hasActiveTurn(e) ?? !1,
      refresh: () => {
        _();
      },
      setDesktopFeatureAvailability: (e) => {
        if (!(e.computerUse == null && e.computerUseNodeRepl == null)) {
          if (
            ((p = { ...p, ...e }),
            (c = p.computerUse === !0 && p.computerUseNodeRepl === !0),
            !c)
          ) {
            m();
            return;
          }
          g();
        }
      },
      dispose: () => {
        ((c = !1), m(), Qn === v && (Qn = null));
      },
    };
  return ((Qn = v), v);
}
var tr = `NODE_REPL_HOST_SERVICES_PIPE_PATH`;
function nr() {
  return `/tmp/codex-host-services-${(0, f.randomUUID)()}.sock`;
}
function rr({
  env: e = process.env,
  isPackaged: t = !0,
  platform: r = process.platform,
  repoRoot: i = process.cwd(),
  resolveCodexPath: a = n.dr,
  resolveNodePath: o = n.fr,
  resolveNodeReplPath: s = n.pr,
  resolvePrimaryRuntimeNodePath: c = sr,
  resourcesPath: l,
}) {
  let u = l ?? St({ env: e, resourcesPath: process.resourcesPath }),
    d = c(r),
    f =
      or({
        platform: r,
        rawValue: e.CODEX_CLI_PATH,
        resolveWindowsAppsPath: a,
      }) ??
      ar({
        devRelativePathSegments: [`extension`, `bin`, `codex`],
        isPackaged: t,
        platform: r,
        repoRoot: i,
        resolveBundledPath: a,
        resourcesPath: u,
      }),
    p = ar({
      devRelativePathSegments: null,
      isPackaged: t,
      platform: r,
      repoRoot: i,
      resolveBundledPath: o,
      resourcesPath: u,
    }),
    m =
      or({
        platform: r,
        rawValue: e.CODEX_BROWSER_USE_NODE_PATH,
        resolveWindowsAppsPath: o,
      }) ??
      (p.path == null && d != null
        ? { path: d, source: `primary-runtime` }
        : p),
    h =
      or({
        platform: r,
        rawValue: e.CODEX_NODE_REPL_PATH,
        resolveWindowsAppsPath: s,
      }) ??
      ar({
        devRelativePathSegments: null,
        isPackaged: t,
        platform: r,
        repoRoot: i,
        resolveBundledPath: s,
        resourcesPath: u,
      });
  return {
    codexCliPath: f.path,
    codexCliPathSource: f.source,
    nodeModuleDirs: n.cr(u),
    nodePath: m.path,
    nodePathSource: m.source,
    nodeReplPath: h.path,
    nodeReplPathSource: h.source,
    platform: r,
  };
}
function ir({
  devRelativePathSegments: e,
  isPackaged: t,
  platform: n,
  repoRoot: r,
  resolveBundledPath: i,
  resourcesPath: a,
}) {
  let o = i(a);
  if (t || n === `win32` || o != null || e == null) return o;
  let s = u.default.join(r, ...e);
  return cr(s) ? s : null;
}
function ar(e) {
  let t = ir(e);
  return { path: t, source: t == null ? `missing` : `bundled-or-dev` };
}
function or({ platform: e, rawValue: t, resolveWindowsAppsPath: n }) {
  if (t == null) return null;
  let r = t.trim();
  if (r.length === 0) return null;
  if (e === `win32` && lr(r)) {
    let e = n(u.default.dirname(r));
    return e == null ? null : { path: e, source: `env-override-relocated` };
  }
  return { path: r, source: `env-override` };
}
function sr(e) {
  if (e !== `win32`) return null;
  let t = u.default.join(
    (0, l.homedir)(),
    `.cache`,
    `codex-runtimes`,
    n.Vn,
    `dependencies`,
    `node`,
    `bin`,
    `node.exe`,
  );
  return cr(t) ? t : null;
}
function cr(e) {
  if (!(0, p.existsSync)(e)) return !1;
  try {
    return (0, p.statSync)(e).isFile()
      ? (process.platform === `win32` || (0, p.accessSync)(e, p.constants.X_OK),
        !0)
      : !1;
  } catch {
    return !1;
  }
}
function lr(e) {
  return e
    .replaceAll(`/`, `\\`)
    .toLowerCase()
    .includes(`\\program files\\windowsapps\\`);
}
var ur = r.i(`BrowserUseThreadConfig`),
  dr = (0, d.promisify)(h.execFile),
  fr = { "features.js_repl": !1 },
  pr = {
    [an]: {
      command: `./Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient`,
      args: [`mcp`],
      cwd: `.`,
      enabled: !1,
    },
  },
  mr = { serviceAppPath: null },
  hr = [Bt, At, jt, Mt, Nt],
  gr = [Pt, Vt],
  _r = new WeakMap(),
  vr = `Control the in-app browser in conjunction with the Browser Plugin.`,
  yr = `Control the Chrome browser in conjunction with the Chrome Plugin. Prefer this method of controlling Chrome over alternatives (such as Computer Use) unless the user explicitly mentions an alternative.`,
  br = `Control desktop apps on macOS through Computer Use.`;
async function xr({
  appServerConnection: e,
  appVersion: t,
  desktopFeatureAvailability: r,
  includePrivateProcessEnv: i = !0,
  isPackaged: a,
  repoRoot: o,
  resourcesPath: s,
  resolveCodexPath: c,
  resolveNodePath: l,
  resolveNodeReplPath: u,
  resolvePrimaryRuntimeNodePath: d,
  shouldUseWslPaths: f,
  platform: p,
  hostServicesPipePath:
    m = g.default.env.NODE_REPL_HOST_SERVICES_PIPE_PATH?.trim() || null,
  trustedBrowserClientSha256s: h = bt,
}) {
  let _ = r.inAppBrowserUse || r.externalBrowserUse,
    v = r.computerUse && r.computerUseNodeRepl,
    y = Or(r);
  if (!_ && !v) return null;
  let b = rr({
      isPackaged: a,
      platform: p,
      repoRoot: o,
      resolveCodexPath: c,
      resolveNodePath: l,
      resolveNodeReplPath: u,
      resolvePrimaryRuntimeNodePath: d,
      resourcesPath: s,
    }),
    x = v && b.platform === `darwin` ? pr : null,
    S = v && b.platform === `darwin` ? await Cr(e, n.ni()) : mr,
    C = v && b.platform === `win32` ? await $n() : null,
    w = v && (b.platform === `darwin` || C != null);
  v &&
    b.platform === `win32` &&
    !w &&
    ur.info(`computer_use_native_pipe_thread_config_skipped`, {
      safe: { platform: b.platform, reason: `not-ready` },
      sensitive: {},
    });
  let T = _ || (v && (b.platform === `darwin` || w)) ? h : [],
    E = Dr({
      appVersion: t,
      computerUse: v,
      computerUsePaths: S,
      runtimePaths: b,
      sentryUserId: i
        ? (e.getCachedAuthenticatedPrincipal?.()?.userId ?? null)
        : null,
      shouldUseWslPaths: f,
      availableBrowserUseBackends: y,
      computerUseNativePipePath: C,
      hostServicesPipePath: m,
      trustedBrowserClientSha256s: T,
    });
  return E == null ? x : (wr(b), Ar([fr, E, x]));
}
function Sr(e) {
  _r.delete(e);
}
async function Cr(e, t) {
  let n = _r.get(e)?.get(t);
  if (n != null) return n;
  let r = _r.get(e) ?? new Map(),
    i = e
      .listPlugins(o.l)
      .then(({ marketplaces: e }) => hn({ codexHome: t, marketplaces: e }))
      .catch((n) => {
        let r = _r.get(e);
        return (
          r?.get(t) === i && (r.delete(t), r.size === 0 && _r.delete(e)),
          ur.warning(`computer_use_installed_plugin_path_resolve_failed`, {
            safe: {},
            sensitive: { error: n },
          }),
          mn()
        );
      });
  return (r.set(t, i), _r.set(e, r), i);
}
async function wr(e) {
  let t = await Tr(e.nodePath);
  ur.info(`browser_use_runtime_paths_selected`, {
    safe: {
      codexCliPathSource: e.codexCliPathSource,
      nodePathSource: e.nodePathSource,
      nodeReplPathSource: e.nodeReplPathSource,
      nodeVersionMajor: Er(t.value),
      nodeVersionStatus: t.status,
      platform: e.platform,
    },
    sensitive: {
      codexCliPath: e.codexCliPath,
      nodePath: e.nodePath,
      nodeReplPath: e.nodeReplPath,
      nodeVersionError: t.error,
      nodeVersionValue: t.value,
    },
  });
}
async function Tr(e) {
  if (e == null) return { status: `missing`, value: null };
  try {
    return {
      status: `ok`,
      value: (
        await dr(e, [`--version`], { timeout: 2e3, windowsHide: !0 })
      ).stdout.trim(),
    };
  } catch (e) {
    return { status: `failed`, value: null, error: e };
  }
}
function Er(e) {
  let t = e?.match(/^v?(\d+)/);
  return t?.[1] == null ? null : Number(t[1]);
}
function Dr({
  appVersion: e,
  availableBrowserUseBackends: t,
  computerUse: r,
  computerUseNativePipePath: a,
  computerUsePaths: o,
  hostServicesPipePath: s,
  runtimePaths: c,
  sentryUserId: l,
  shouldUseWslPaths: u,
  trustedBrowserClientSha256s: d,
}) {
  if (c.nodeReplPath == null)
    return (
      ur.info(`browser_use_setup_failed`, {
        safe: {
          backend: `node_repl`,
          phase: `runtime-paths`,
          platform: c.platform,
          reason: `node-repl-missing`,
        },
        sensitive: {},
      }),
      null
    );
  if (c.nodePath == null)
    return (
      ur.info(`browser_use_setup_failed`, {
        safe: {
          backend: `node_repl`,
          phase: `runtime-paths`,
          platform: c.platform,
          reason: `node-missing`,
        },
        sensitive: {},
      }),
      null
    );
  n.No(c.platform) &&
    c.codexCliPath == null &&
    ur.warning(`browser_use_codex_cli_missing_for_node_repl_sandbox`, {
      safe: {},
      sensitive: {},
    });
  let f = jr(c.nodeModuleDirs, c.platform),
    p = n.ni(),
    m = i.a.resolve(),
    h = m === i.a.Dev ? g.default.env[n.Oo] : void 0,
    _ = m === i.a.Dev ? g.default.env[n.jo] : void 0,
    v = i.a.isInternal(m) || g.default.env.NODE_REPL_TRACE_META === `1`,
    y = g.default.env[Wt]?.trim(),
    b = {
      [Dt]: t.join(`,`),
      ...(t.includes(`iab`) ? { [Ht]: vr } : {}),
      ...(t.includes(`chrome`) ? { [Ut]: yr } : {}),
      ...(r && c.platform === `darwin` ? { [on]: br } : {}),
      [Ot]: m,
      [kt]: e,
      ...kr(hr),
      ...(m === i.a.Dev ? kr(gr) : {}),
      ...(y ? { [Wt]: y } : {}),
      ...(r
        ? fn({
            nativePipeDirectory: a,
            nativePipeEnabled: a != null,
            serviceAppPath: c.platform === `darwin` ? o.serviceAppPath : null,
          })
        : {}),
      ...(r && c.platform === `darwin` && s != null ? { [tr]: s } : {}),
    };
  return n.Mo({
    codexCliPath: c.codexCliPath,
    codexHome: p,
    extraEnv: b,
    nodeModuleDirs: f,
    nodePath: c.nodePath,
    nodeReplPath: u ? n.ri(c.nodeReplPath) : c.nodeReplPath,
    platform: c.platform,
    requestMeta: h,
    sentryUserId: l,
    traceMeta: v,
    trustAllCode: _,
    trustedBrowserClientSha256s: d,
    shouldUseWslPaths: u,
  });
}
function Or(e) {
  let t = [];
  return (
    e.externalBrowserUse && t.push(`chrome`),
    e.inAppBrowserUse && t.push(`iab`),
    t
  );
}
function kr(e) {
  let t = {};
  for (let n of e) {
    let e = g.default.env[n];
    e != null && (t[n] = e);
  }
  return t;
}
function Ar(e) {
  let t = {};
  for (let n of e)
    if (n != null) for (let [e, r] of Object.entries(n)) t[e] = r;
  return Object.keys(t).length === 0 ? null : t;
}
function jr(e, t) {
  return e.join(t === `win32` ? `;` : `:`);
}
var Mr = `computerUseOverlay.usingComputer`,
  Nr = `ChatGPT is using your computer`,
  Pr = `computerUseOverlay.escToCancel`,
  Fr = `Esc to cancel`,
  Ir = `computer-use`,
  Lr = `config.json`,
  Rr = `#339cff`,
  zr = `en`,
  Br = new Map(),
  Vr = r.a(`computer-use-config`);
function Hr(e) {
  return (
    e === `appearanceTheme` ||
    e === `appearanceLightChromeTheme` ||
    e === `appearanceDarkChromeTheme` ||
    e === `localeOverride`
  );
}
async function Ur({
  settingsStore: e,
  isPackaged: t = c.app.isPackaged,
  repoRoot: r,
}) {
  await Yr({
    codexHome: n.ni(),
    config: Gr({
      settingsStore: e,
      locale: Kr(),
      localesDir: Jr({ isPackaged: t, repoRoot: r }),
      shouldUseDarkColors: qr(),
    }),
  });
}
function Wr({ settingsStore: e, isPackaged: t, repoRoot: n }) {
  Ur({ settingsStore: e, isPackaged: t, repoRoot: n }).catch((e) => {
    Vr().warning(`Failed to write Computer Use config`, {
      safe: {},
      sensitive: { error: e },
    });
  });
}
function Gr({
  settingsStore: e,
  locale: t,
  localesDir: r,
  shouldUseDarkColors: i,
}) {
  let a = ri(e.getEffective(n.Bi.localeOverride.key)) ?? ri(t) ?? zr,
    o = $r(r, a);
  return {
    accentColor: Zr({ settingsStore: e, shouldUseDarkColors: i }),
    direction: ti(a),
    locale: a,
    strings: { usingComputer: ei(o, Mr, Nr), escToCancel: ei(o, Pr, Fr) },
  };
}
function Kr() {
  return typeof c.app.getLocale == `function` ? c.app.getLocale() : `en`;
}
function qr() {
  return c.nativeTheme?.shouldUseDarkColors === !0;
}
function Jr({ isPackaged: e, repoRoot: t }) {
  return e
    ? (0, u.join)(c.app.getAppPath(), `native-menu-locales`)
    : (0, u.join)(t, `webview`, `src`, `locales`);
}
async function Yr({ codexHome: e, config: t }) {
  let n = (0, u.join)(e, Ir),
    r = (0, u.join)(n, Lr),
    i = (Br.get(r) ?? Promise.resolve())
      .catch(() => {})
      .then(() => Xr({ dir: n, configPath: r, config: t }));
  Br.set(r, i);
  try {
    await i;
  } finally {
    Br.get(r) === i && Br.delete(r);
  }
  return r;
}
async function Xr({ dir: e, configPath: t, config: n }) {
  let r = (0, u.join)(
    e,
    `${Lr}.${process.pid}.${Date.now()}.${(0, f.randomUUID)()}.tmp`,
  );
  await (0, m.mkdir)(e, { recursive: !0 });
  try {
    (await (0, m.writeFile)(r, `${JSON.stringify(n)}\n`, `utf8`),
      await (0, m.rename)(r, t));
  } catch (e) {
    throw (await (0, m.unlink)(r).catch(() => {}), e);
  }
}
function Zr({ settingsStore: e, shouldUseDarkColors: t }) {
  let r = (
    Qr(e.getEffective(n.Gi.theme.key) ?? `system`, t) === `light`
      ? e.getEffective(n.Gi.lightChromeTheme.key)
      : e.getEffective(n.Gi.darkChromeTheme.key)
  )?.accent;
  return ni(r) ? r : Rr;
}
function Qr(e, t) {
  return e === `system` ? (t ? `dark` : `light`) : e;
}
function $r(e, t) {
  let n = ri(e);
  if (n == null) return null;
  let r = t.replaceAll(`_`, `-`),
    i = r.split(`-`)[0],
    a = [
      (0, u.join)(n, `${r}.json`),
      i && i !== r ? (0, u.join)(n, `${i}.json`) : null,
    ];
  for (let e of a)
    if (e != null)
      try {
        let t = JSON.parse((0, p.readFileSync)(e, `utf8`));
        if (typeof t == `object` && t && !Array.isArray(t)) return t;
      } catch {}
  return null;
}
function ei(e, t, n) {
  let r = e?.[t];
  return ri(typeof r == `string` ? r : null) ?? n;
}
function ti(e) {
  try {
    return new Intl.Locale(e.replaceAll(`_`, `-`)).textInfo?.direction === `rtl`
      ? `rtl`
      : `ltr`;
  } catch {
    return `ltr`;
  }
}
function ni(e) {
  return /^#[0-9a-fA-F]{6}$/.test(e ?? ``);
}
function ri(e) {
  return e?.trim() || null;
}
async function ii({
  appServerClient: e,
  createSplitDirectories: t,
  directoryName: r,
  homeDirectory: i,
  prompt: a,
}) {
  let o = await e.platformPath();
  return n.Da({
    createSplitDirectories: t,
    directoryName: r,
    fileSystem: oi(e),
    homeDirectory: i,
    path: o,
    prompt: a,
  });
}
async function ai({ appServerClient: e, homeDirectory: t }) {
  let r = n.Oa({ homeDirectory: t, path: await e.platformPath() }),
    i = await n.ti
      .stat(r, e, { bypassCache: !0, followSymlinks: !1 })
      .catch((e) => {
        if (e instanceof Error && `code` in e && e.code === `ENOENT`)
          return null;
        throw e;
      });
  if (i != null && (!i.isDirectory() || i.isSymbolicLink()))
    throw Error(`Projectless thread directory must be a real directory`);
  return r;
}
function oi(e) {
  return {
    createDirectory: async ({ path: t, recursive: r }) => {
      await n.ti.mkdir(t, { recursive: r }, e);
    },
    getMetadata: async (t) => {
      let r = await n.ti.stat(t, e, { followSymlinks: !1 });
      return { isDirectory: r.isDirectory(), isSymlink: r.isSymbolicLink() };
    },
  };
}
var si = `Response MUST end with a remark-directive block.

## Responding

- Answer the user normally and concisely. Explain what you found, what you did, and what the user should focus on now.
- Automations: use the memory file at \`$CODEX_HOME/automations/<automation_id>/memory.md\` (create it if missing).
  - Read it first (if present) to avoid repeating recent work, especially for "changes since last run" tasks.
  - Memory is important: some tasks must build on prior work, and others must avoid duplicating prior focus.
  - Before returning the directive, write a concise summary of what you did/decided plus the current run time.
  - Use the \`Automation ID:\` value provided in the message to locate/update this file.
- REQUIRED: End with a valid remark-directive block on its own line (not inline).
  - Always include an inbox item directive:
    \`::inbox-item{title="Sample title" summary="Place description here"}\`

## Choosing return value

- For recurring/bg threads (e.g., "pull datadog logs and fix any new bugs", "address the PR comments"):
  - Always return \`::inbox-item{...}\` with the title/summary the user should see.

## Guidelines

- Directives MUST be on their own line.
- Output exactly ONE inbox-item directive.
- Do NOT use invalid remark-directive formatting.
- DO NOT place commas between arguments.
  - Valid: \`::inbox-item{title="Sample title" summary="Place description here"}\`
  - Invalid: \`::inbox-item{title="Sample title",summary="Place description here"}\`
- When referring to files, use full absolute filesystem links in Markdown (not relative paths).
  - Valid: [\`/Users/alice/project/src/main.ts\`](/Users/alice/project/src/main.ts)
  - Invalid: \`src/main.ts\` or \`[main](src/main.ts)\`
- Try not to ask the user for more input if possible to infer.
- If a PR is opened by the automation, add the \`codex-automation\` label when available alongside the normal \`codex\` label.
- Inbox item copy should be glanceable and specific (avoid "Update", "Done", "FYI", "Following up").
  - Title: what this thread now _is_ (state + object). Aim ~4-8 words.
  - Title should explain what was built or what happened.
- Summary: what the user should _do/know next_ (next step, blocker, or waiting-on). Aim ~6-14 words.
- Summary should usually match the general automation name or prompt summary.
- Both title and summary should be fairly short; usually avoid one-word titles/summaries.
  - Prefer concrete nouns + verbs; include a crisp status cue when helpful: "blocked", "needs decision", "ready for review".

## Examples (inbox-item)

- Work needed:
  - \`::inbox-item{title="Fix flaky checkout tests" summary="Repro isolated; needs CI run + patch"}\`
- Waiting on user decision:
  - \`::inbox-item{title="Choose API shape for filters" summary="Two options drafted; pick A vs B"}\`
- Status update with next step:
  - \`::inbox-item{title="PR comments addressed" summary="Ready for re-review; focus on auth edge case"}\`
`,
  ci = `<heartbeat>
  <automation_id>{{AUTOMATION_ID}}</automation_id>
  <current_time_iso>{{NOW_ISO}}</current_time_iso>
  <instructions>
{{AUTOMATION_PROMPT}}
  </instructions>
</heartbeat>
`,
  li = r.i(`worktree-cleanup-service`),
  ui = 7200 * 60 * 1e3;
async function di(e, t, r) {
  let i = Array.from(new Set(r)),
    a = await Promise.all(
      i.map(async (r) => {
        try {
          let i = await e.readThread(r, { includeTurns: !1 });
          if (i == null) return [r, null];
          let a = Number(i.updatedAt) * 1e3,
            o = null;
          if (i.path != null)
            try {
              let e = await n.ti.stat(i.path, t);
              o = Number.isFinite(e.mtimeMs) ? e.mtimeMs : null;
            } catch {}
          let s = [Number.isFinite(a) ? a : null, o].filter((e) => e != null);
          return [
            r,
            {
              updatedAtMs: s.length === 0 ? null : Math.max(...s),
              isInProgress: i.status.type === `active`,
              cwd: i.cwd ?? null,
              readFailed: !1,
            },
          ];
        } catch (e) {
          return (
            li.error(`Failed to get thread metadata`, {
              safe: { threadId: r },
              sensitive: { error: e },
            }),
            [
              r,
              {
                updatedAtMs: Date.now(),
                isInProgress: !0,
                cwd: null,
                readFailed: !0,
              },
            ]
          );
        }
      }),
    );
  return Object.fromEntries(a);
}
function fi(e) {
  let t = e.get(n.Gs.PINNED_THREAD_IDS);
  return Array.isArray(t) ? t.filter((e) => typeof e == `string`) : [];
}
function pi({ pinnedThreadIds: e, threadMetadataById: t, worktrees: r }) {
  let i = e
    .map((e) => t[e]?.cwd)
    .filter((e) => e != null)
    .map((e) => n.at(e).replace(/\/+$/, ``));
  return r
    .map((e) => e.dir)
    .filter((e) => {
      let t = n.at(e).replace(/\/+$/, ``);
      return i.some((e) => e === t || e.startsWith(`${t}/`));
    });
}
function mi({ pinnedThreadIds: e, threadMetadataById: t }) {
  return e.some((e) => {
    let n = t[e];
    return n == null || n.readFailed;
  });
}
function hi(e) {
  return e.getEffective(n.Fi.keepCount.key) ?? n.Fi.keepCount.default;
}
function gi(e) {
  return e.getEffective(n.zi.worktreeRoot.key) ?? n.zi.worktreeRoot.default;
}
function _i(e, t, r = !0) {
  return !r &&
    t.get(n.Gs.WORKTREE_AUTO_CLEANUP_UNPACKAGED_OVERRIDE_ENABLED) !== !0
    ? !1
    : e.getEffective(n.Fi.autoCleanupEnabled.key) !== !1;
}
function vi(e, t = Date.now()) {
  let n = e.get(a.E),
    r;
  return (
    typeof n == `number` && Number.isFinite(n) && n > 0
      ? (r = n)
      : ((r = t), e.set(a.E, r)),
    t < r + ui
  );
}
function yi({ globalState: e, worktrees: t }) {
  return t
    .map((e) => e.dir)
    .filter((t) => bi({ globalState: e, worktreeDir: t }));
}
function bi({ globalState: e, worktreeDir: t }) {
  let r = n.at(t).replace(/\/+$/, ``);
  return it(e).some((e) => {
    let t = n.at(e).replace(/\/+$/, ``);
    return t === r || t.startsWith(`${r}/`);
  });
}
var xi = 3e4,
  Si = 3,
  Ci = 2 * 6e4,
  wi = 6e4,
  Ti = 3e4,
  Ei = 10 * 6e4,
  Di = `set_thread_archived`,
  Oi = {
    type: `function`,
    name: Di,
    description: `Archive or unarchive a Codex thread.`,
    inputSchema: {
      type: `object`,
      additionalProperties: !1,
      properties: {
        threadId: {
          type: `string`,
          description: `Thread id to archive or unarchive. Omit to target the calling thread.`,
        },
        archived: {
          type: `boolean`,
          description: `Whether the thread should be archived.`,
        },
      },
      required: [`archived`],
    },
  },
  ki = {
    type: `namespace`,
    name: `codex_app`,
    description: `Tools provided by the Codex app.`,
    tools: [Oi],
  },
  iee = { model: n.Gc, reasoningEffort: n.Kc },
  Ai = r.a(`automations`),
  ji = async () => [],
  Mi = new Set([`response_item`, `event_msg`, `item`, `unknown`]);
function Ni({ lastRunAt: e, threadUpdatedAt: t }) {
  let n = [e, t].filter((e) => e != null && Number.isFinite(e));
  return n.length === 0 ? null : Math.max(...n);
}
function Pi({ automation: e, thread: t }) {
  let r = n.br(e.rrule);
  if (r == null) return null;
  let i = Ni({
    lastRunAt: e.lastRunAt,
    threadUpdatedAt: Number.isFinite(t.updatedAt) ? t.updatedAt * 1e3 : null,
  });
  return i == null ? null : i + r;
}
function Fi({ automation: e, now: t }) {
  let r = n.br(e.rrule);
  return r == null ? n.xr({ automation: e, now: t }) : t + r;
}
function Ii({ nextScheduledRunAt: e, now: t }) {
  return e == null ? t + wi : Math.min(e, t + wi);
}
function Li({ rrule: e, now: t }) {
  let r = n.br(e);
  return {
    scheduleKind: r == null ? `wall_clock` : `interval`,
    intervalMs: r,
    nextScheduledRunAt: r == null ? n.Sr({ rrule: e, now: t }) : t + r,
  };
}
function Ri(e) {
  return e == null
    ? null
    : {
        ...e,
        settings: {
          ...e.settings,
          developer_instructions: n.sn({
            baseInstructions: e.settings.developer_instructions,
          }),
        },
      };
}
async function zi(e, t, r) {
  let i = await t.getWorktreeRepository(e, r);
  return i
    ? { type: `branch`, branchName: (await n.lt(i))?.branch ?? `HEAD` }
    : { type: `branch`, branchName: `HEAD` };
}
async function Bi({
  sourceCwd: e,
  worktreeCwd: t,
  gitManager: r,
  appServerClient: i,
  automationId: a,
}) {
  let o = i,
    s = [t, (0, u.join)(n.ni(), `automations`, a)],
    c = await r.getWorktreeRepository(n.oo(t), o),
    l = c?.root;
  l && l !== t && s.push(l);
  let d = await r.getWorktreeRepository(n.oo(e), o),
    f = d?.root;
  f && f !== t && f !== l && s.push(f);
  let p = (await r.getRepoRepository(n.oo(c?.root ?? e), o))?.getCommonDir();
  if (p) {
    let t = (0, u.isAbsolute)(p) ? p : (0, u.resolve)(f ?? e, p);
    return (s.push(t), s);
  }
  let m = c ?? d,
    h = m == null ? null : await n.ft(m);
  return (
    h ? s.push((0, u.dirname)(h)) : f && s.push((0, u.join)(f, `.git`)),
    s
  );
}
async function Vi({
  sourceCwds: e,
  worktreeCwd: t,
  gitManager: r,
  appServerClient: i,
  automationId: a,
}) {
  let o = await Promise.all(
    (e.length > 0 ? e : [n.oo(t)]).map((e) =>
      Bi({
        sourceCwd: e,
        worktreeCwd: t,
        gitManager: r,
        appServerClient: i,
        automationId: a,
      }),
    ),
  );
  return [...new Set([...e, ...o.flat()])];
}
function Hi(e, t, r, i = null) {
  let a;
  a =
    t.sandbox_mode != null || t.approval_policy != null
      ? `custom`
      : n.al(n.il(t))
        ? `guardian-approvals`
        : `auto`;
  let o = i ?? a,
    s = n.ol(r, t),
    c =
      [a, `auto`, `granular`, `guardian-approvals`, `read-only`].find((e) =>
        s.includes(e),
      ) ??
      s.at(-1) ??
      `read-only`,
    l = s.includes(o) ? o : c,
    u = n.nl(l, e, t),
    d = u.sandboxPolicy,
    f = u.approvalsReviewer;
  return {
    approvalPolicy:
      l !== `granular` &&
      f === `user` &&
      (r?.allowedApprovalPolicies == null ||
        r.allowedApprovalPolicies.includes(`never`))
        ? `never`
        : u.approvalPolicy,
    approvalsReviewer: f,
    sandboxPolicy: d,
  };
}
function Ui(e, t) {
  let r = e.get(a.C)?.[n.el];
  if (typeof r != `object` || !r || Array.isArray(r)) return null;
  let i = Object.getOwnPropertyDescriptor(r, t)?.value,
    o = n.tl.safeParse(i);
  return o.success ? o.data : null;
}
function Wi(e) {
  return {
    approvalPolicy: e.approvalPolicy,
    approvalsReviewer: e.approvalsReviewer,
    sandboxPolicy: n.rl(e.approvalPolicy).sandboxPolicy,
  };
}
function Gi(e, t, r) {
  let i = n.ol(r, t),
    a = n.il(t),
    o =
      t.sandbox_mode != null || t.approval_policy != null
        ? `custom`
        : n.al(a)
          ? `guardian-approvals`
          : `auto`,
    s = o;
  return (
    i.includes(s) ||
      (s =
        o === `auto` && i.includes(`guardian-approvals`)
          ? `guardian-approvals`
          : o === `guardian-approvals` && i.includes(`auto`)
            ? `auto`
            : i.includes(`read-only`)
              ? `read-only`
              : (i[0] ?? `read-only`)),
    n.nl(s, e, t)
  );
}
function Ki(e, t) {
  return e.reduce((e, n, r) => e.then(() => t(n, r)), Promise.resolve());
}
function qi({
  appVersion: e,
  appServerConnectionRegistry: t,
  getDesktopFeatureAvailability: r = () => He,
  gitManager: i,
  globalState: a,
  threadProjectAssignments: o,
  settingsStore: s,
  appServerClient: c,
  worktreeLifecycle: l,
  requestDynamicToolsForThreadStart: u = ji,
  isPackaged: d = !0,
  repoRoot: f = process.cwd(),
  tickMs: p = xi,
  maxPerTick: m = Si,
}) {
  let h = !1,
    g = !1,
    _ = new Map(),
    v = new Map(),
    y = new Map(),
    b = async () => {
      if (!h) {
        h = !0;
        try {
          let p = t.getConnection(c.hostConfig.id),
            h = Date.now(),
            b = n.wr(h, m);
          await Promise.all(
            b.map(async (t) => {
              try {
                if (n.Jc(t)) {
                  await $i({
                    automation: t,
                    now: h,
                    appServerConnection: p,
                    heartbeatAutomationsEnabled: g,
                    heartbeatThreadRendererStates: _,
                    heartbeatThreadCollaborationModes: v,
                    heartbeatThreadPermissions: y,
                  });
                  return;
                }
                let m = n.xr({ automation: t, now: h });
                (n.Tr(t.id, m),
                  await Yi({
                    appVersion: e,
                    automation: t,
                    appServerConnection: p,
                    desktopFeatureAvailability: r(),
                    gitManager: i,
                    globalState: a,
                    threadProjectAssignments: o,
                    settingsStore: s,
                    appServerClient: c,
                    worktreeLifecycle: l,
                    requestDynamicToolsForThreadStart: u,
                    isPackaged: d,
                    repoRoot: f,
                  }));
              } catch (e) {
                Ai().warning(`Scheduled run failed`, {
                  safe: { automationId: t.id, error: e },
                  sensitive: {},
                });
              }
            }),
          );
        } finally {
          h = !1;
        }
      }
    },
    x = () => {
      b().catch((e) => {
        Ai().debug(`Automation scheduler tick threw`, {
          safe: { error: e },
          sensitive: {},
        });
      });
    };
  try {
    n.Rr() && t.getConnection(c.hostConfig.id).notifyAutomationRunsUpdated();
  } catch (e) {
    Ai().warning(`Failed to settle interrupted automation runs`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  (n.gr(Date.now()), x());
  let S = setInterval(x, p);
  return (
    S.unref(),
    {
      dispose: () => {
        (clearInterval(S), _.clear(), v.clear(), y.clear());
      },
      setHeartbeatAutomationsEnabled: (e) => {
        if (((g = e), !e)) {
          (_.clear(), v.clear(), y.clear());
          return;
        }
        x();
      },
      setHeartbeatThreadRendererState: (e) => {
        let t = e.threadId?.trim() ?? ``;
        t.length !== 0 &&
          (_.set(t, {
            isEligible: e.isEligible,
            reason: e.reason,
            updatedAtMs: Date.now(),
          }),
          e.collaborationMode != null && v.set(t, e.collaborationMode),
          e.permissions == null ? y.delete(t) : y.set(t, e.permissions));
      },
    }
  );
}
async function Ji({
  appVersion: e,
  automationId: t,
  appServerConnection: r,
  desktopFeatureAvailability: i = He,
  gitManager: a,
  globalState: o,
  threadProjectAssignments: s,
  settingsStore: c,
  appServerClient: l,
  worktreeLifecycle: u,
  requestDynamicToolsForThreadStart: d = ji,
  isPackaged: f = !0,
  repoRoot: p = process.cwd(),
  collaborationMode: m = null,
  permissions: h = null,
}) {
  let g = n.yr(t);
  if (!g) throw Error(`Automation not found.`);
  if (n.Jc(g)) {
    await ea({
      automation: g,
      appServerConnection: r,
      collaborationMode: m,
      permissions: h,
    });
    return;
  }
  let _ = n.xr({ automation: g, now: Date.now() });
  (n.Tr(g.id, _),
    await Yi({
      appVersion: e,
      automation: g,
      appServerConnection: r,
      desktopFeatureAvailability: i,
      gitManager: a,
      globalState: o,
      threadProjectAssignments: s,
      settingsStore: c,
      appServerClient: l,
      worktreeLifecycle: u,
      requestDynamicToolsForThreadStart: d,
      isPackaged: f,
      repoRoot: p,
    }));
}
async function Yi({
  appVersion: e,
  automation: t,
  appServerConnection: r,
  desktopFeatureAvailability: i,
  gitManager: a,
  globalState: o,
  threadProjectAssignments: s,
  settingsStore: c,
  appServerClient: l,
  worktreeLifecycle: u,
  requestDynamicToolsForThreadStart: d,
  isPackaged: f,
  repoRoot: p,
}) {
  let m;
  if (t.target == null) m = t.cwds.map((e) => ({ type: `legacy`, cwd: e }));
  else if (t.target.type === `projectless`) m = [{ type: `projectless` }];
  else {
    let e = gt(o, t.target);
    m = e == null ? [] : [{ type: `project`, project: e }];
  }
  if (m.length === 0) {
    Ai().warning(`Scheduled run skipped: no folders configured`, {
      safe: { automationId: t.id },
      sensitive: {},
    });
    return;
  }
  let h = (await r.getConfigRequirements()).requirements,
    g = iee;
  try {
    ((g = n.qc({
      automation: t,
      models: (
        await r.listModels({ includeHidden: !0, cursor: null, limit: 100 })
      ).data,
    })),
      t.model != null &&
        t.model !== g.model &&
        Ai().warning(`Automation model unavailable; falling back`, {
          safe: {
            automationId: t.id,
            requestedModel: t.model,
            fallbackModel: g.model,
          },
          sensitive: {},
        }));
  } catch (e) {
    Ai().warning(`Failed to load models for automation run`, {
      safe: { automationId: t.id, error: e },
      sensitive: {},
    });
  }
  let _ = await Xi(d),
    v = [];
  if (
    (await Ki(m, (n) =>
      Zi({
        appVersion: e,
        automation: t,
        appServerConnection: r,
        desktopFeatureAvailability: i,
        gitManager: a,
        globalState: o,
        threadProjectAssignments: s,
        settingsStore: c,
        appServerClient: l,
        worktreeLifecycle: u,
        configRequirements: h,
        target: n,
        automationModelSettings: g,
        dynamicTools: _,
        isPackaged: f,
        repoRoot: p,
      }).catch((e) => {
        (v.push(e),
          Ai().warning(`Scheduled run failed`, {
            safe: { automationId: t.id, error: e },
            sensitive: {},
          }));
      }),
    ),
    v.length > 0)
  )
    throw v[0];
}
async function Xi(e) {
  try {
    let t = await e();
    if (t.some((e) => e.type === `function`))
      return t.some((e) => e.type === `function` && e.name === Di)
        ? t
        : [...t, Oi];
    let n = t.find((e) => e.type === `namespace` && e.name === `codex_app`);
    return n?.type === `namespace`
      ? n.tools.some((e) => e.name === Di)
        ? t
        : t.map((e) => (e === n ? { ...n, tools: [...n.tools, Oi] } : e))
      : [...t, ki];
  } catch (e) {
    return (
      Ai().warning(`Failed to load dynamic tools for automation run`, {
        safe: { error: e },
        sensitive: {},
      }),
      [ki]
    );
  }
}
async function Zi({
  appVersion: e,
  automation: t,
  appServerConnection: r,
  desktopFeatureAvailability: i,
  gitManager: a,
  globalState: o,
  threadProjectAssignments: s,
  settingsStore: c,
  appServerClient: u,
  worktreeLifecycle: d,
  configRequirements: p,
  target: m,
  automationModelSettings: h,
  dynamicTools: g,
  isPackaged: _,
  repoRoot: v,
}) {
  let y = u,
    b = `Automation: ${t.name}`,
    x = si.trim(),
    S = m.type === `projectless`,
    C = m.type === `project` ? m.project : null,
    w = C?.roots ?? [],
    T = m.type === `legacy` ? m.cwd : w.length === 1 ? (w[0] ?? null) : null,
    E = T ?? n.oo(`~`),
    D = T == null ? [] : [T],
    O = null,
    k = null;
  if (S || T == null) {
    let e = await ii({
        appServerClient: u,
        createSplitDirectories: !0,
        homeDirectory: (0, l.homedir)(),
        prompt: t.prompt,
      }),
      r = n.ui(u.hostConfig);
    ((E = n.oo(z(e.cwd, r))),
      (O = z(e.outputDirectory, r)),
      (k = z(e.workspaceRoot, r)),
      (D = S ? [] : [n.oo(k), ...w]),
      S || (O = null));
  }
  let A = await r.getUserSavedConfiguration(E),
    j = Ui(o, u.hostConfig.id),
    M =
      O == null
        ? null
        : n.ka({
            cwd: E,
            projectlessOutputDirectory: O,
            projectlessWorkspaceBrowserRoot: k,
          }),
    N = [
      n.on({
        baseInstructions: x,
        isNonGitWorkspace: !0,
        threadToolsEnabled: !0,
      }),
      M,
    ].filter((e) => !!e).join(`

`),
    P = Hi(D, A, p, j),
    F = S ? Wi(P) : P,
    I = t.executionEnvironment === `worktree` && !S;
  if (I && T == null)
    throw Error(`Worktree automations require a project with one folder`);
  let ee = I && (await a.getWorktreeRepository(E, y))?.root != null,
    L = async (t) => {
      let a = u.hostConfig;
      Wr({ settingsStore: c, isPackaged: _, repoRoot: v });
      let o = await xr({
          appServerConnection: r,
          appVersion: e,
          desktopFeatureAvailability: i,
          cwd: t,
          isPackaged: _,
          repoRoot: v,
          shouldUseWslPaths: n.ui(a),
        }),
        s = {
          model: h.model,
          modelProvider: null,
          cwd: t,
          approvalPolicy: F.approvalPolicy,
          approvalsReviewer: F.approvalsReviewer,
          sandbox:
            F.sandboxPolicy.type === `dangerFullAccess`
              ? `danger-full-access`
              : F.sandboxPolicy.type === `readOnly`
                ? `read-only`
                : `workspace-write`,
          config: o,
          developerInstructions: N,
          personality: null,
          ephemeral: null,
          threadSource: `automation`,
          dynamicTools: g,
          mockExperimentalField: null,
          experimentalRawEvents: !1,
          serviceTier: null,
        };
      return r.startThread(s);
    },
    R = null,
    te = null,
    ne = null,
    re = `pending:${(0, f.randomUUID)()}`;
  (n.Mr(t.id, re, t.name, T), r.notifyAutomationRunsUpdated());
  try {
    if (ee) {
      let e = await zi(E, a, u),
        r = await n.st({
          gitManager: a,
          workspaceRoot: E,
          startingState: e,
          localEnvironmentConfigPath: t.localEnvironmentConfigPath,
          host: y,
          worktreesRoot: gi(c),
        });
      if (!r.success) throw r.error;
      ((ne = r.worktreeGitRoot), (R = await L(r.worktreeWorkspaceRoot)));
    } else R = await L(E);
    let e = R.thread.id;
    if (((te = e), ne != null))
      try {
        (await d.setOwnerThread({ conversationId: n.uc(e), worktree: ne }),
          d.requestCleanup());
      } catch (e) {
        Ai().warning(
          `Automation worktree thread started, but owner metadata could not be updated`,
          { safe: { automationId: t.id }, sensitive: { error: e } },
        );
      }
    (S
      ? (o.update(n.Gs.PROJECTLESS_THREAD_IDS, (t) =>
          t?.includes(e) ? t : [...(t ?? []), e],
        ),
        k != null &&
          o.update(n.Gs.THREAD_WORKSPACE_ROOT_HINTS, (t) => ({ ...t, [e]: k })),
        O != null &&
          o.update(n.Gs.THREAD_PROJECTLESS_OUTPUT_DIRECTORIES, (t) => ({
            ...t,
            [e]: O,
          })))
      : C != null &&
        (await s.setAssignment({
          threadId: n.uc(e),
          assignment: {
            projectKind: `local`,
            projectId: C.projectId,
            ...(C.isMetadataProject ? {} : { path: C.projectId }),
            cwd: E,
            pendingCoreUpdate: !1,
          },
        })),
      n.Ir(re, te) || n.Mr(t.id, te, t.name, T),
      r.notifyAutomationRunsUpdated(),
      Qi({ automation: t, threadId: te, appServerConnection: r }));
  } catch (e) {
    throw (n.Fr(re, `auto`), r.notifyAutomationRunsUpdated(), e);
  }
  if (R == null || te == null) throw Error(`Automation thread start failed`);
  let ie = Hi(
      S
        ? []
        : await Vi({
            sourceCwds: m.type === `project` ? m.project.roots : [E],
            worktreeCwd: R.cwd,
            gitManager: a,
            appServerClient: u,
            automationId: t.id,
          }),
      A,
      p,
      j,
    ),
    ae = S ? Wi(ie) : ie,
    oe =
      t.lastRunAt == null
        ? `never`
        : `${new Date(t.lastRunAt).toISOString()} (${t.lastRunAt})`,
    se = `${b}\nAutomation ID: ${t.id}\nAutomation memory: $CODEX_HOME/automations/${t.id}/memory.md\nLast run: ${oe}\n\n${t.prompt}`,
    ce = {
      threadId: te,
      input: [{ type: `text`, text: se, text_elements: [] }],
      cwd: R.cwd,
      approvalPolicy: ae.approvalPolicy,
      approvalsReviewer: ae.approvalsReviewer,
      sandboxPolicy: ae.sandboxPolicy,
      model: h.model,
      effort: h.reasoningEffort,
      serviceTier: null,
      summary: n.mi,
      personality: null,
      outputSchema: null,
      collaborationMode: null,
    };
  await r.startTurn(ce, {
    automationKind: t.kind,
    sessionId: R.thread.sessionId,
  });
}
async function Qi({ automation: e, threadId: t, appServerConnection: n }) {
  try {
    await n.updateThreadTitle(t, e.name, null);
  } catch (n) {
    Ai().warning(`Failed to set automation thread title`, {
      safe: { automationId: e.id, threadId: t, error: n },
      sensitive: {},
    });
  }
}
async function $i({
  automation: e,
  now: t,
  appServerConnection: r,
  heartbeatAutomationsEnabled: i,
  heartbeatThreadRendererStates: a,
  heartbeatThreadCollaborationModes: o,
  heartbeatThreadPermissions: s,
}) {
  let c = Li({ rrule: e.rrule, now: t });
  if (!i) {
    (n.Dr(e.id, c.nextScheduledRunAt),
      Ai().debug(`Heartbeat automation skipped: feature disabled`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `feature_disabled`,
          nextRunAt: c.nextScheduledRunAt,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let l = await r.readThread(e.targetThreadId).catch(() => null);
  if (l == null) {
    let r = Ii({ nextScheduledRunAt: c.nextScheduledRunAt, now: t });
    (n.Dr(e.id, r),
      Ai().warning(`Heartbeat automation skipped: thread missing`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `thread_missing`,
          nextRunAt: r,
          nextCooldownAt: null,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let u = Pi({ automation: e, thread: l }),
    d = sa({
      expectedThreadId: e.targetThreadId,
      heartbeatThreadRendererStates: a,
    });
  if (d != null) {
    let r = Ii({ nextScheduledRunAt: c.nextScheduledRunAt, now: t });
    (n.Dr(e.id, r),
      Ai().debug(`Heartbeat automation blocked by renderer state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: d,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let f = Ri(oa(o, e.targetThreadId));
  if (f == null) {
    let r = Ii({ nextScheduledRunAt: c.nextScheduledRunAt, now: t });
    (n.Dr(e.id, r),
      Ai().debug(`Heartbeat automation waiting for renderer mode state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `missing_collaboration_mode`,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let p = await ca({ status: l.status, rolloutPath: l.path });
  if (p != null) {
    let r = Ii({ nextScheduledRunAt: c.nextScheduledRunAt, now: t });
    (n.Dr(e.id, r),
      Ai().debug(`Heartbeat automation blocked by thread state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: p,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  if (u != null && u > t) {
    (n.Dr(e.id, u),
      Ai().debug(`Heartbeat automation skipped: not due yet`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `cooldown_not_elapsed`,
          nextRunAt: u,
          nextCooldownAt: u,
          remainingMs: u - t,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let m = Fi({ automation: e, now: t });
  (n.Tr(e.id, m),
    await ta({
      automation: e,
      thread: l,
      appServerConnection: r,
      collaborationMode: f,
      permissions: s.get(e.targetThreadId) ?? null,
      waitForCompletion: !0,
    }));
}
async function ea({
  automation: e,
  appServerConnection: t,
  collaborationMode: r,
  permissions: i,
}) {
  let a = await t.readThread(e.targetThreadId).catch(() => null);
  if (a == null) throw Error(`Heartbeat thread not found.`);
  if ((await ca({ status: a.status, rolloutPath: a.path })) != null)
    throw Error(`Heartbeat thread is busy right now.`);
  let o = Fi({ automation: e, now: Date.now() });
  n.Tr(e.id, o);
  let s = Ri(r);
  if (s == null) throw Error(`Heartbeat thread mode is still loading.`);
  await ta({
    automation: e,
    thread: a,
    appServerConnection: t,
    collaborationMode: s,
    permissions: i,
    waitForCompletion: !1,
  });
}
async function ta({
  automation: e,
  thread: t,
  appServerConnection: n,
  collaborationMode: r,
  permissions: i,
  waitForCompletion: a,
}) {
  let o = await na({ thread: t, appServerConnection: n }),
    s = i ?? (await ra({ appServerConnection: n, cwd: o.cwd })),
    c = ia(e);
  await ua({
    appServerConnection: n,
    sessionId: o.sessionId,
    threadId: o.id,
    cwd: o.cwd,
    prompt: c,
    collaborationMode: r,
    approvalPolicy: s.approvalPolicy,
    approvalsReviewer: s.approvalsReviewer,
    sandboxPolicy: s.sandboxPolicy,
    model: null,
    effort: null,
    serviceTier: null,
    waitForCompletion: a,
  });
}
async function na({ thread: e, appServerConnection: t }) {
  let n = await t.resumeThread(e.id, { cwd: e.cwd, path: e.path });
  return {
    id: n.thread.id,
    cwd: n.thread.cwd || n.cwd || e.cwd,
    path: n.thread.path ?? e.path,
    sessionId: n.thread.sessionId,
  };
}
async function ra({ appServerConnection: e, cwd: t }) {
  let [n, r] = await Promise.all([
    e.getUserSavedConfiguration(t),
    e.getConfigRequirements().catch(() => ({ requirements: null })),
  ]);
  return Gi([t], n, r.requirements);
}
function ia(e) {
  return ci
    .replaceAll(`{{AUTOMATION_ID}}`, e.id)
    .replaceAll(`{{NOW_ISO}}`, new Date().toISOString())
    .replaceAll(`{{AUTOMATION_PROMPT}}`, e.prompt);
}
function aa(e, t) {
  let n = e.get(t);
  return n == null
    ? null
    : Date.now() - n.updatedAtMs > Ci
      ? (e.delete(t), null)
      : n;
}
function oa(e, t) {
  return e.get(t) ?? null;
}
function sa({ expectedThreadId: e, heartbeatThreadRendererStates: t }) {
  let n = aa(t, e);
  return n == null || n.isEligible ? null : (n.reason ?? `renderer_ineligible`);
}
async function ca({ status: e, rolloutPath: t }) {
  if (e.type !== `active`) return null;
  if (e.activeFlags.includes(`waitingOnUserInput`))
    return `waiting_on_user_input`;
  if (e.activeFlags.includes(`waitingOnApproval`)) return `waiting_on_approval`;
  if (e.activeFlags.length > 0) return `active_with_flags`;
  if (t == null || t.trim().length === 0) return `active_without_rollout_path`;
  let n = await la(t);
  return n == null
    ? `active_without_terminal_event`
    : n === `task_complete`
      ? null
      : Mi.has(n)
        ? `active_recent_rollout_activity`
        : `active_without_terminal_event`;
}
async function la(e) {
  let { open: t } = await import(`node:fs/promises`),
    n;
  try {
    n = await t(e, `r`);
    let r = await n.stat();
    if (r.size === 0) return null;
    let i = Math.min(r.size, 256 * 1024),
      a = Buffer.alloc(i);
    await n.read(a, 0, i, r.size - i);
    let o = a.toString(`utf8`).split(`
`);
    r.size > i && o.shift();
    let s = null;
    for (let e of o) {
      let t = e.trim();
      if (t.length !== 0)
        try {
          let e = JSON.parse(t);
          (e.event === `task_complete` ||
            e.event === `response_item` ||
            e.event === `event_msg`) &&
            (s = e.event);
        } catch {
          continue;
        }
    }
    return s;
  } catch {
    return null;
  } finally {
    await n?.close().catch(() => void 0);
  }
}
async function ua({
  appServerConnection: e,
  sessionId: t,
  threadId: r,
  cwd: i,
  prompt: a,
  collaborationMode: o,
  approvalPolicy: s,
  approvalsReviewer: c,
  sandboxPolicy: l,
  model: u,
  effort: d,
  serviceTier: f,
  waitForCompletion: p,
}) {
  let m = {
    threadId: r,
    input: [{ type: `text`, text: a, text_elements: [] }],
    cwd: i,
    approvalPolicy: s,
    approvalsReviewer: c,
    sandboxPolicy: l,
    model: u,
    effort: d,
    serviceTier: f,
    summary: n.mi,
    personality: null,
    outputSchema: null,
    collaborationMode: o,
  };
  if (p) {
    await da({
      appServerConnection: e,
      sessionId: t,
      threadId: r,
      turnStartParams: m,
    });
    return;
  }
  let h = fa({ appServerConnection: e, threadId: r });
  try {
    let n = await e.startTurn(m, { automationKind: `heartbeat`, sessionId: t });
    await h.waitForTurn(n.turn.id);
  } catch (e) {
    throw (h.dispose(), e);
  }
}
async function da({
  appServerConnection: e,
  sessionId: t,
  threadId: n,
  turnStartParams: r,
}) {
  let i = null,
    a = () => {},
    o = new Promise((t, r) => {
      let o = setTimeout(() => {
          (a(), t());
        }, Ei),
        s = e.registerInternalNotificationHandler((e) => {
          pa({
            notification: e,
            threadId: n,
            turnId: i,
            onTurnStarted: (e) => {
              i = e;
            },
            onCompleted: (e, n) => {
              if (((i = e), a(), n !== `completed`)) {
                r(Error(`Heartbeat automation did not complete.`));
                return;
              }
              t();
            },
          });
        });
      a = () => {
        (clearTimeout(o), s(), (a = () => {}));
      };
    });
  try {
    ((i = (await e.startTurn(r, { automationKind: `heartbeat`, sessionId: t }))
      .turn.id),
      await o);
  } catch (e) {
    throw (a(), e);
  }
}
function fa({ appServerConnection: e, threadId: t }) {
  let n = new Set(),
    r = null,
    i = null,
    a = null,
    o = null,
    s = !1,
    c = () => {
      r == null || !n.has(r) || a?.();
    },
    l = e.registerInternalNotificationHandler((e) => {
      pa({
        notification: e,
        threadId: t,
        turnId: null,
        onTurnStarted: (e) => {
          (n.add(e), c());
        },
        onCompleted: (e) => {
          (n.add(e), c());
        },
      });
    }),
    u = new Promise((e, t) => {
      ((a = e), (o = t));
    }),
    d = () => {
      s || ((s = !0), i != null && (clearTimeout(i), (i = null)), l());
    };
  return {
    dispose: d,
    waitForTurn: async (e) => {
      ((r = e),
        (i = setTimeout(() => {
          o?.(Error(`Timed out waiting for heartbeat automation to start.`));
        }, Ti)),
        c());
      try {
        await u;
      } finally {
        d();
      }
    },
  };
}
function pa({
  notification: e,
  threadId: t,
  turnId: n,
  onTurnStarted: r,
  onCompleted: i,
}) {
  switch (e.method) {
    case `turn/started`: {
      let n = e.params;
      if (n.threadId !== t) return;
      r(n.turn.id);
      return;
    }
    case `turn/completed`: {
      let r = e.params;
      if (r.threadId !== t || (n != null && r.turn.id !== n)) return;
      i(r.turn.id, r.turn.status);
      return;
    }
    default:
      return;
  }
}
var ma = (0, d.promisify)(h.execFile),
  ha = 100,
  ga = new Map(),
  _a = `
$ErrorActionPreference = "Stop"
$ProgressPreference = "SilentlyContinue"

$code = @"
using System;
using System.Runtime.InteropServices;

[StructLayout(LayoutKind.Sequential)]
public struct SIZE {
  public int cx;
  public int cy;
  public SIZE(int x, int y) { cx = x; cy = y; }
}

[StructLayout(LayoutKind.Sequential)]
public struct BITMAPINFOHEADER {
  public uint biSize;
  public int biWidth;
  public int biHeight;
  public ushort biPlanes;
  public ushort biBitCount;
  public uint biCompression;
  public uint biSizeImage;
  public int biXPelsPerMeter;
  public int biYPelsPerMeter;
  public uint biClrUsed;
  public uint biClrImportant;
}

[StructLayout(LayoutKind.Sequential)]
public struct BITMAPINFO {
  public BITMAPINFOHEADER bmiHeader;
  public uint bmiColors;
}

[ComImport]
[InterfaceType(ComInterfaceType.InterfaceIsIUnknown)]
[Guid("bcc18b79-ba16-442f-80c4-8a59c30c463b")]
public interface IShellItemImageFactory {
  void GetImage(SIZE size, int flags, out IntPtr phbm);
}

public static class CodexShellIconNative {
  const int BI_RGB = 0;
  const int DIB_RGB_COLORS = 0;
  const int ICON_ONLY = 4;

  [DllImport("shell32.dll", CharSet = CharSet.Unicode, PreserveSig = false)]
  public static extern void SHCreateItemFromParsingName(
    string pszPath,
    IntPtr pbc,
    ref Guid riid,
    [MarshalAs(UnmanagedType.Interface)] out IShellItemImageFactory ppv
  );

  [DllImport("gdi32.dll")]
  public static extern bool DeleteObject(IntPtr hObject);

  [DllImport("gdi32.dll")]
  static extern int GetDIBits(
    IntPtr hdc,
    IntPtr hbmp,
    uint uStartScan,
    uint cScanLines,
    byte[] lpvBits,
    ref BITMAPINFO lpbi,
    uint uUsage
  );

  [DllImport("user32.dll")]
  static extern IntPtr GetDC(IntPtr hWnd);

  [DllImport("user32.dll")]
  static extern int ReleaseDC(IntPtr hWnd, IntPtr hDC);

  public static string GetAppsFolderIconDataUrl(string appId) {
    Guid iid = new Guid("bcc18b79-ba16-442f-80c4-8a59c30c463b");
    IShellItemImageFactory factory;
    SHCreateItemFromParsingName("shell:AppsFolder\\\\" + appId, IntPtr.Zero, ref iid, out factory);

    IntPtr hbitmap;
    factory.GetImage(new SIZE(32, 32), ICON_ONLY, out hbitmap);
    try {
      using (var bitmap = BitmapFromHBitmapPreserveAlpha(hbitmap, 32, 32))
      using (var stream = new System.IO.MemoryStream()) {
        bitmap.Save(stream, System.Drawing.Imaging.ImageFormat.Png);
        return "data:image/png;base64," + Convert.ToBase64String(stream.ToArray());
      }
    } finally {
      DeleteObject(hbitmap);
    }
  }

  static System.Drawing.Bitmap BitmapFromHBitmapPreserveAlpha(IntPtr hbitmap, int width, int height) {
    var info = new BITMAPINFO();
    info.bmiHeader.biSize = (uint)Marshal.SizeOf(typeof(BITMAPINFOHEADER));
    info.bmiHeader.biWidth = width;
    info.bmiHeader.biHeight = -height;
    info.bmiHeader.biPlanes = 1;
    info.bmiHeader.biBitCount = 32;
    info.bmiHeader.biCompression = BI_RGB;
    info.bmiHeader.biSizeImage = (uint)(width * height * 4);

    byte[] pixels = new byte[width * height * 4];
    IntPtr hdc = GetDC(IntPtr.Zero);
    try {
      int scanLines = GetDIBits(hdc, hbitmap, 0, (uint)height, pixels, ref info, DIB_RGB_COLORS);
      if (scanLines == 0) {
        throw new InvalidOperationException("GetDIBits failed.");
      }
    } finally {
      ReleaseDC(IntPtr.Zero, hdc);
    }

    var bitmap = new System.Drawing.Bitmap(width, height, System.Drawing.Imaging.PixelFormat.Format32bppArgb);
    var data = bitmap.LockBits(
      new System.Drawing.Rectangle(0, 0, width, height),
      System.Drawing.Imaging.ImageLockMode.WriteOnly,
      System.Drawing.Imaging.PixelFormat.Format32bppArgb
    );
    try {
      Marshal.Copy(pixels, 0, data.Scan0, pixels.Length);
    } finally {
      bitmap.UnlockBits(data);
    }
    return bitmap;
  }
}
"@

Add-Type -AssemblyName System.Drawing
if (-not ("CodexShellIconNative" -as [type])) {
  Add-Type -TypeDefinition $code -ReferencedAssemblies System.Drawing
}

function Convert-ImageFileToDataUrl([string]$Path) {
  if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
    return $null
  }

  $extension = [System.IO.Path]::GetExtension($Path).ToLowerInvariant()
  $mimeType = switch ($extension) {
    ".png" { "image/png" }
    ".jpg" { "image/jpeg" }
    ".jpeg" { "image/jpeg" }
    ".ico" { "image/x-icon" }
    ".bmp" { "image/bmp" }
    default { $null }
  }
  if ($null -eq $mimeType) {
    return $null
  }

  $bytes = [System.IO.File]::ReadAllBytes($Path)
  return "data:$mimeType;base64,$([Convert]::ToBase64String($bytes))"
}

function Convert-ExeIconToDataUrl([string]$Path) {
  if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
    return $null
  }

  Add-Type -AssemblyName System.Drawing
  $icon = [System.Drawing.Icon]::ExtractAssociatedIcon($Path)
  if ($null -eq $icon) {
    return $null
  }

  $bitmap = $null
  $stream = New-Object System.IO.MemoryStream
  try {
    $bitmap = $icon.ToBitmap()
    $bitmap.Save($stream, [System.Drawing.Imaging.ImageFormat]::Png)
    return "data:image/png;base64,$([Convert]::ToBase64String($stream.ToArray()))"
  } finally {
    if ($null -ne $bitmap) { $bitmap.Dispose() }
    $icon.Dispose()
    $stream.Dispose()
  }
}

$AppPath = $env:CODEX_NATIVE_DESKTOP_APP_ICON_PATH
if ([string]::IsNullOrWhiteSpace($AppPath)) {
  exit 0
}

$trimmedAppPath = $AppPath.Trim()
if (Test-Path -LiteralPath $trimmedAppPath -PathType Leaf) {
  $extension = [System.IO.Path]::GetExtension($trimmedAppPath).ToLowerInvariant()
  $dataUrl = if ($extension -eq ".exe" -or $extension -eq ".dll") {
    Convert-ExeIconToDataUrl $trimmedAppPath
  } else {
    Convert-ImageFileToDataUrl $trimmedAppPath
  }
  if ($null -ne $dataUrl) {
    Write-Output $dataUrl
  }
  exit 0
}

try {
  Write-Output ([CodexShellIconNative]::GetAppsFolderIconDataUrl($trimmedAppPath))
} catch {
  exit 0
}
`;
async function va(e) {
  let t = ya(e);
  if (t === ``) return null;
  let n = ba(t),
    r = ga.get(n);
  if (r != null) return (ga.delete(n), ga.set(n, r), r);
  let i = Sa(t).then((e) => (e ?? ga.delete(n), e));
  return (ga.set(n, i), xa(), i);
}
function ya(e) {
  let t = e.trim();
  if (t.toLowerCase().startsWith(`process:`)) {
    let e = t.slice(8).trim();
    if (e.includes(`\\`) || e.includes(`/`)) return e;
  }
  return t;
}
function ba(e) {
  return e.trim().toLowerCase();
}
function xa() {
  for (; ga.size > ha; ) {
    let e = ga.keys().next().value;
    if (e == null) return;
    ga.delete(e);
  }
}
async function Sa(e) {
  try {
    let { stdout: t } = await ma(
      `powershell.exe`,
      [
        `-NoProfile`,
        `-NonInteractive`,
        `-ExecutionPolicy`,
        `Bypass`,
        `-EncodedCommand`,
        Buffer.from(_a, `utf16le`).toString(`base64`),
      ],
      {
        encoding: `utf8`,
        env: { ...r.t(process.env), CODEX_NATIVE_DESKTOP_APP_ICON_PATH: e },
        timeout: 1e4,
        windowsHide: !0,
      },
    );
    return Ca(t);
  } catch {
    return null;
  }
}
function Ca(e) {
  return (
    e.match(/data:image\/[a-z0-9.+-]+;base64,[A-Za-z0-9+/=]+/gi)?.at(-1) ?? null
  );
}
var wa = `sky.node`,
  Ta = (0, T.createRequire)(__filename);
function Ea({ electronAppPath: e, resourcesPath: t }) {
  let n = [
    ...(t == null ? [] : [u.default.join(t, `native`, wa)]),
    u.default.join(
      e,
      `native`,
      `sky-addon`,
      `build`,
      `Release-${process.arch}`,
      wa,
    ),
  ];
  for (let e of n) if ((0, p.existsSync)(e)) return Ta(e);
  throw Error(`Sky native addon is unavailable`);
}
var Da = n.xl({ iconSmallForAppPath: n.hl((e) => typeof e == `function`) }),
  Oa = `computer-use-app-icons.node`,
  ka = 100,
  Aa = 1e4,
  ja = {
    cdp: null,
    chrome: {
      fileName: `google-chrome.png`,
      pluginNames: [`chrome-internal`, `chrome-dev`, `chrome`],
    },
    iab: null,
  },
  Ma = (0, T.createRequire)(__filename),
  Na = new Map(),
  Pa = n.xl({
    bundleIdentifier: n.wl(),
    iconSmallDataURL: n
      .wl()
      .nullish()
      .transform((e) => e ?? null),
    name: n.wl(),
    windowTitle: n
      .wl()
      .nullish()
      .transform((e) => e ?? null),
  });
async function Fa({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return null;
  try {
    let n = null,
      i = await Promise.race([
        (e ?? Ea({ electronAppPath: t, resourcesPath: r })).frontmostWindow(),
        new Promise((e) => {
          ((n = setTimeout(() => {
            e(null);
          }, Aa)),
            n.unref());
        }),
      ]).finally(() => {
        n != null && clearTimeout(n);
      });
    return i == null ? null : Pa.parse(JSON.parse(i));
  } catch {
    return null;
  }
}
async function Ia(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = St({
      env: process.env,
      resourcesPath: process.resourcesPath,
    }),
  } = {},
) {
  let a = Ra(e, r);
  if (a === ``) return null;
  let o = Ba(a, r),
    s = Na.get(o);
  if (s != null) return (Na.delete(o), Na.set(o, s), s);
  let l = za(a, {
    addon: t,
    electronAppPath: n,
    platform: r,
    resourcesPath: i,
  }).then((e) => (e ?? Na.delete(o), e));
  return (Na.set(o, l), Va(), l);
}
async function La(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = St({
      env: process.env,
      resourcesPath: process.resourcesPath,
    }),
  } = {},
) {
  if (r !== `darwin` || !e.endsWith(`.app`)) return null;
  try {
    return await (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).iconMediumForAppPath(e);
  } catch {
    return null;
  }
}
function Ra(e, t) {
  return t === `win32` ? ya(e) : t === `darwin` ? e.trim() : ``;
}
async function za(
  e,
  { addon: t, electronAppPath: n, platform: r, resourcesPath: i },
) {
  try {
    return r === `darwin`
      ? await (
          t ?? Ea({ electronAppPath: n, resourcesPath: i })
        ).iconSmallForAppPath(e)
      : r === `win32`
        ? await (
            t ?? Ha({ electronAppPath: n, resourcesPath: i })
          ).iconSmallForAppPath(e)
        : null;
  } catch {
    return null;
  }
}
function Ba(e, t) {
  return `${t}:${t === `win32` ? e.toLowerCase() : e}`;
}
function Va() {
  for (; Na.size > ka; ) {
    let e = Na.keys().next().value;
    if (e == null) return;
    Na.delete(e);
  }
}
function Ha({ electronAppPath: e, resourcesPath: t }) {
  for (let n of Ua({ electronAppPath: e, resourcesPath: t }))
    if ((0, p.existsSync)(n)) return Da.parse(Ma(n));
  throw Error(`Computer Use app icons native addon is unavailable`);
}
function Ua({ electronAppPath: e, resourcesPath: t }) {
  return [
    ...(t == null ? [] : [u.default.join(t, `native`, Oa)]),
    u.default.join(
      e,
      `native`,
      `computer-use-app-icons-addon`,
      `build`,
      `Release-${process.arch}`,
      `computer_use_app_icons.node`,
    ),
  ];
}
async function Wa(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  return r !== `darwin` || e.trim() === ``
    ? null
    : (
        t ?? Ea({ electronAppPath: n, resourcesPath: i })
      ).spawnComputerUseService(e);
}
function Ga(
  e,
  t,
  {
    addon: n,
    electronAppPath: r = c.app.getAppPath(),
    platform: i = process.platform,
    resourcesPath: a = process.resourcesPath,
  } = {},
) {
  if (i !== `darwin` || !Number.isSafeInteger(e) || e <= 0 || t.trim() === ``)
    return !1;
  try {
    return (
      n ?? Ea({ electronAppPath: r, resourcesPath: a })
    ).computerUseServiceProcessMatchesExecutablePath(e, t);
  } catch {
    return !1;
  }
}
function Ka({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ea({ electronAppPath: t, resourcesPath: r })
    ).isPrivacySettingsTerminationRequest();
  } catch {
    return !1;
  }
}
function qa({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ea({ electronAppPath: t, resourcesPath: r })
    ).startRemoteHostedPIPContentHost();
  } catch {
    return !1;
  }
}
function Ja({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ea({ electronAppPath: t, resourcesPath: r })
    ).stopRemoteHostedPIPContentHost();
  } catch {
    return !1;
  }
}
function Ya(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentActiveThreadID(e);
  } catch {
    return !1;
  }
}
function Xa(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentSuppressedThreadIDs(e);
  } catch {
    return !1;
  }
}
function Za(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin` || !Number.isFinite(e)) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentMaxDisplaySize(e);
  } catch {
    return !1;
  }
}
function Qa(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentMaxDisplaySizeChangedHandler(e);
  } catch {
    return !1;
  }
}
function $a({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ea({ electronAppPath: t, resourcesPath: r })
    ).hasRemoteHostedPIPContentActivePresentation();
  } catch {
    return !1;
  }
}
function eo({
  addon: e,
  electronAppPath: t = c.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ea({ electronAppPath: t, resourcesPath: r })
    ).hasRemoteHostedPIPContentAnyPresentation();
  } catch {
    return !1;
  }
}
function to(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentVisible(e);
  } catch {
    return !1;
  }
}
function no(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentPetWakeRequestHandler(e);
  } catch {
    return !1;
  }
}
function ro(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).setRemoteHostedPIPContentVisibilityRequestHandler(e);
  } catch {
    return !1;
  }
}
function io(
  e,
  {
    addon: t,
    electronAppPath: n,
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin`) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n ?? c.app.getAppPath(), resourcesPath: i })
    ).setRemoteHostedPIPContentComputerUseCursorLocationHandler(e);
  } catch {
    return !1;
  }
}
function ao(
  e,
  t,
  {
    addon: n,
    anchors: r = null,
    anchorRect: i = null,
    animated: a = !1,
    electronAppPath: o = c.app.getAppPath(),
    platform: s = process.platform,
    presentationScope: l = `thread`,
    resourcesPath: u = process.resourcesPath,
  } = {},
) {
  if (s !== `darwin` || e.trim() === ``) return !1;
  try {
    return (
      n ?? Ea({ electronAppPath: o, resourcesPath: u })
    ).registerRemoteHostedPIPContentHost({
      anchors: r,
      anchorRect: i,
      animated: a,
      contentBounds: t.getContentBounds(),
      id: e,
      nativeWindowHandle:
        typeof t.getNativeWindowHandle == `function`
          ? t.getNativeWindowHandle()
          : null,
      presentationScope: l,
      title: t.getTitle(),
    });
  } catch {
    return !1;
  }
}
function oo(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin` || e.trim() === ``) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).unregisterRemoteHostedPIPContentHost(e);
  } catch {
    return !1;
  }
}
function so(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin` || e.trim() === ``) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).completeRemoteHostedPIPContentThread(e);
  } catch {
    return !1;
  }
}
function co(
  e,
  t,
  {
    addon: n,
    electronAppPath: r = c.app.getAppPath(),
    platform: i = process.platform,
    resourcesPath: a = process.resourcesPath,
  } = {},
) {
  if (i !== `darwin` || e.trim() === `` || t.trim() === ``) return !1;
  try {
    return (
      n ?? Ea({ electronAppPath: r, resourcesPath: a })
    ).invalidateRemoteHostedPIPContentTurn(e, t);
  } catch {
    return !1;
  }
}
function lo(
  e,
  t,
  n,
  r,
  {
    addon: i,
    electronAppPath: a = c.app.getAppPath(),
    platform: o = process.platform,
    resourcesPath: s = St({
      env: process.env,
      resourcesPath: process.resourcesPath,
    }),
  } = {},
) {
  if (
    o !== `darwin` ||
    e.trim() === `` ||
    t.trim() === `` ||
    !n.startsWith(`data:image/`)
  )
    return !1;
  try {
    let o = ja[r],
      c =
        s == null || o == null
          ? null
          : (o.pluginNames
              .flatMap((e) =>
                [``, `app.asar.unpacked`].map((t) =>
                  u.default.join(
                    s,
                    t,
                    `plugins`,
                    `openai-bundled`,
                    `plugins`,
                    e,
                    `assets`,
                    o.fileName,
                  ),
                ),
              )
              .find((e) => (0, p.existsSync)(e)) ?? null);
    return (
      i ?? Ea({ electronAppPath: a, resourcesPath: s })
    ).upsertBrowserUsePIPContent(e, t, n, c);
  } catch {
    return !1;
  }
}
function uo(
  e,
  {
    addon: t,
    electronAppPath: n = c.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  if (r !== `darwin` || e.trim() === ``) return !1;
  try {
    return (
      t ?? Ea({ electronAppPath: n, resourcesPath: i })
    ).invalidateBrowserUsePIPContent(e);
  } catch {
    return !1;
  }
}
var fo = n.xl({
    kind: n.vl(`browserUse`),
    backend: n.dl([`chrome`, `iab`, `cdp`]),
    browserId: n.wl().trim().min(1),
    openTabIds: n.pl(n.wl().trim().min(1)).optional(),
    sessionEnded: n.vl(!0).optional(),
    screenshot: n
      .xl({
        tabId: n.wl().trim().min(1),
        url: n.wl().startsWith(`data:image/`),
      })
      .optional(),
  }),
  po = n.xl({ "codex/toolSurface": fo }),
  mo = r.a(`browser-use-pip`);
function ho({
  appServerConnection: e,
  isEnabled: t = () => !0,
  invalidatePresentation: n = uo,
  upsertPresentation: r = lo,
}) {
  let i = new Map(),
    a = (e) => {
      let t = i.get(e);
      if (t != null) {
        for (let e of t.presentationIDsByTabID.values()) n(e);
        i.delete(e);
      }
    },
    o = (e) => {
      for (let [t, n] of i) n.threadID === e && a(t);
    },
    s = e.registerInternalNotificationHandler((e) => {
      let s = go(e);
      if (s != null) {
        if (!t()) return;
        let { surface: e, threadID: o } = s,
          c = JSON.stringify([o, e.browserId]);
        if (e.sessionEnded === !0) {
          a(c);
          return;
        }
        let l = i.get(c);
        if (e.screenshot != null) {
          let t = `browser:${JSON.stringify([o, e.browserId, e.screenshot.tabId])}`;
          r(t, o, e.screenshot.url, e.backend)
            ? ((l ??= { presentationIDsByTabID: new Map(), threadID: o }),
              l.presentationIDsByTabID.set(e.screenshot.tabId, t),
              i.set(c, l))
            : mo().warning(`Failed to upsert Browser Use PiP content`, {
                safe: { backend: e.backend, tabID: e.screenshot.tabId },
              });
        }
        if (e.openTabIds != null && l != null) {
          let t = new Set(e.openTabIds);
          for (let [e, r] of l.presentationIDsByTabID)
            t.has(e) || (n(r), l.presentationIDsByTabID.delete(e));
          l.presentationIDsByTabID.size === 0 && i.delete(c);
        }
        return;
      }
      (e.method === `thread/archived` || e.method === `thread/closed`) &&
        typeof e.params?.threadId == `string` &&
        o(e.params.threadId);
    });
  return () => {
    s();
    for (let e of i.keys()) a(e);
  };
}
function go(e) {
  if (
    e.method !== `item/completed` ||
    typeof e.params?.threadId != `string` ||
    typeof e.params.item != `object` ||
    e.params.item == null ||
    !(`type` in e.params.item) ||
    e.params.item.type !== `mcpToolCall` ||
    !(`server` in e.params.item) ||
    e.params.item.server !== `node_repl` ||
    !(`result` in e.params.item) ||
    typeof e.params.item.result != `object` ||
    e.params.item.result == null ||
    !(`_meta` in e.params.item.result)
  )
    return null;
  let t = po.safeParse(e.params.item.result._meta);
  return t.success
    ? (mo().info(`Received Browser Use PiP metadata`, {
        safe: {
          backend: t.data[`codex/toolSurface`].backend,
          browserID: t.data[`codex/toolSurface`].browserId,
          tabID: t.data[`codex/toolSurface`].screenshot?.tabId,
          threadID: e.params.threadId,
        },
      }),
      { surface: t.data[`codex/toolSurface`], threadID: e.params.threadId })
    : null;
}
var _o = [`features.js_repl`, `mcp_servers.${n.ko}`, an];
async function vo({
  appServerConnection: e,
  appVersion: t,
  desktopFeatureAvailability: r,
  isPackaged: i,
  platform: a,
  repoRoot: o,
  resolveCodexPath: s,
  resolveNodePath: c,
  resolveNodeReplPath: l,
  resolvePrimaryRuntimeNodePath: u,
  resourcesPath: d,
}) {
  Sr(e);
  let f = await xr({
    appServerConnection: e,
    appVersion: t,
    desktopFeatureAvailability: r,
    cwd: null,
    hostServicesPipePath: null,
    includePrivateProcessEnv: !1,
    isPackaged: i,
    platform: a,
    repoRoot: o,
    resolveCodexPath: s,
    resolveNodePath: c,
    resolveNodeReplPath: l,
    resolvePrimaryRuntimeNodePath: u,
    resourcesPath: d,
    shouldUseWslPaths: n.ui(e.hostConfig),
  });
  await e.sendAppServerRequest(`config/batchWrite`, {
    edits: yo(f),
    expectedVersion: null,
    filePath: null,
    reloadUserConfig: !0,
  });
}
function yo(e) {
  return _o.map((t) => ({
    keyPath: t,
    mergeStrategy: `replace`,
    value: e?.[t] ?? null,
  }));
}
var bo = [`.codex-plugin`, `unified-skill.md`],
  xo = [`skills`, `control-in-app-browser`, `SKILL.md`],
  So = [`skills`, `control-chrome`, `SKILL.md`],
  Co = new Map([
    [n.Ss, xo],
    [o.s, So],
    [n.Cs, So],
    [o.c, So],
  ]),
  wo = [
    {
      destination: [`skills`, `computer-use`, `SKILL.md`],
      source: [`.codex-plugin`, `computer-use-node-repl.md`],
    },
  ],
  To = n.yl({});
function Eo(e) {
  return Co.has(e);
}
async function Do(e) {
  let t,
    n = [],
    r = Co.get(e.pluginName);
  if (
    (r == null
      ? e.pluginName === `computer-use` &&
        ((t = e.computerUseSkillVariant), t === `node-repl` && (n = wo))
      : ((t = e.browserSkillVariant),
        t === `unified` && (n = [{ destination: r, source: bo }])),
    t == null)
  )
    return;
  await Promise.all(
    n.map(({ destination: t, source: n }) =>
      m.default.copyFile(
        (0, u.join)(e.pluginRoot, ...n),
        (0, u.join)(e.pluginRoot, ...t),
      ),
    ),
  );
  let i = (0, u.join)(e.pluginRoot, `.codex-plugin`, `plugin.json`),
    a = await To.parseAsync(JSON.parse(await m.default.readFile(i, `utf8`)));
  await m.default.writeFile(
    i,
    `${JSON.stringify({ ...a, bundledContentVariant: t }, null, 2)}\n`,
    `utf8`,
  );
}
var Oo = r.i(`BrowserUseLocalToBundledMigration`),
  ko = n.yl({
    name: n.wl(),
    source: n.yl({ path: n.wl().trim().min(1), source: n.wl() }).optional(),
  }),
  Ao = n.yl({ plugins: n.pl(ko) });
async function jo(e) {
  try {
    await Mo(e);
  } catch (e) {
    Oo.warning(`browser_use_local_to_bundled_migration_failed`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
async function Mo(e) {
  let t = e.marketplaceName ?? `openai-bundled`,
    { marketplaces: r } = await e.appServerConnection.listPlugins(o.l),
    i = rn({ marketplaces: r, marketplaceName: t, pluginName: n.Ps });
  if (i == null) {
    Oo.debug(`bundled_browser_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let a = No({ codexHome: e.codexHome, marketplaces: r });
  if (a == null) {
    Oo.debug(`local_browser_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  (await e.appServerConnection.uninstallPlugin({ pluginId: a.plugin.id }),
    Oo.info(`browser_use_local_to_bundled_migration_uninstalled_local`, {
      safe: {},
    }),
    await e.trashItem(a.plugin.source.path),
    Oo.info(`browser_use_local_to_bundled_migration_moved_local_to_trash`, {
      safe: {},
      sensitive: { localPluginPath: a.plugin.source.path },
    }),
    await Po({
      codexHome: e.codexHome,
      localPluginPath: a.plugin.source.path,
      marketplacePath: a.marketplace.path,
    }),
    !i.plugin.installed &&
      (await e.appServerConnection.installPlugin({
        marketplacePath: i.marketplace.path,
        pluginName: n.Ps,
      }),
      Oo.info(`browser_use_local_to_bundled_migration_installed_bundled`, {
        safe: {},
        sensitive: { marketplacePath: i.marketplace.path },
      })));
}
function No(e) {
  let t = (0, u.join)(e.codexHome, `plugins`, n.Fs);
  for (let n of e.marketplaces)
    for (let e of n.plugins)
      if (
        e.name === `browser-use` &&
        e.installed &&
        n.path != null &&
        e.source.type === `local` &&
        Ro(e.source.path, t)
      )
        return {
          marketplace: { ...n, path: n.path },
          plugin: { ...e, source: e.source },
        };
  return null;
}
async function Po(e) {
  try {
    let t = await Fo(e.marketplacePath),
      n = t.plugins.filter(
        (t) =>
          !Io({
            codexHome: e.codexHome,
            localPluginPath: e.localPluginPath,
            plugin: t,
          }),
      );
    if (n.length === t.plugins.length) {
      Oo.debug(
        `browser_use_local_to_bundled_migration_marketplace_entry_missing`,
        { safe: {}, sensitive: { marketplacePath: e.marketplacePath } },
      );
      return;
    }
    (await m.default.writeFile(
      e.marketplacePath,
      JSON.stringify({ ...t, plugins: n }, null, 2) +
        `
`,
      `utf8`,
    ),
      Oo.info(
        `browser_use_local_to_bundled_migration_removed_marketplace_entry`,
        { safe: {}, sensitive: { marketplacePath: e.marketplacePath } },
      ));
  } catch (t) {
    Oo.warning(
      `browser_use_local_to_bundled_migration_marketplace_cleanup_failed`,
      { safe: {}, sensitive: { error: t, marketplacePath: e.marketplacePath } },
    );
  }
}
async function Fo(e) {
  return Ao.parseAsync(JSON.parse(await m.default.readFile(e, `utf8`)));
}
function Io(e) {
  return (
    e.plugin.name === `browser-use` &&
    e.plugin.source?.source === `local` &&
    Lo({
      codexHome: e.codexHome,
      localPluginPath: e.localPluginPath,
      sourcePath: e.plugin.source.path,
    })
  );
}
function Lo(e) {
  return [
    (0, u.resolve)(e.sourcePath),
    (0, u.resolve)(e.codexHome, e.sourcePath),
    (0, u.resolve)((0, u.dirname)(e.codexHome), e.sourcePath),
  ].some((t) => Ro(t, e.localPluginPath));
}
function Ro(e, t) {
  return (0, u.resolve)(e) === (0, u.resolve)(t);
}
var zo = (0, d.promisify)(h.execFile),
  Bo = `Codex Computer Use.app`,
  Vo = `--previous-notify`,
  Ho = r.i(`ComputerUseLocalToBundledMigration`),
  Uo = n.yl({ name: n.vl(n.ws), version: n.wl().trim().min(1) }),
  Wo = n.yl({
    name: n.wl(),
    source: n.yl({ path: n.wl().trim().min(1), source: n.wl() }).optional(),
  }),
  Go = n.yl({ plugins: n.pl(Wo) });
async function Ko(e) {
  try {
    await qo(e);
  } catch (e) {
    Ho.warning(`computer_use_local_to_bundled_migration_failed`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
async function qo(e) {
  let t = e.marketplaceName ?? `openai-bundled`,
    { marketplaces: r } = await e.appServerConnection.listPlugins(o.l),
    i = rn({ marketplaces: r, marketplaceName: t, pluginName: n.ws });
  if (i == null) {
    Ho.debug(`bundled_computer_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let a = Jo({ codexHome: e.codexHome, marketplaces: r });
  if (a == null) {
    Ho.debug(`local_computer_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let s = await ps(a.plugin.source.path),
    c = s != null && s > 0,
    l = c
      ? !1
      : await (e.isMacAppNotarized ?? gs)(
          (0, u.join)(a.plugin.source.path, Bo),
        );
  if (!l && !c) {
    Ho.info(`local_computer_use_plugin_kept_for_development`, {
      safe: { localBuildNumber: s },
      sensitive: { localPluginPath: a.plugin.source.path },
    });
    return;
  }
  (await e.appServerConnection.uninstallPlugin({ pluginId: a.plugin.id }),
    Ho.info(`computer_use_local_to_bundled_migration_uninstalled_local`, {
      safe: {
        localBuildNumber: s,
        reason: l ? `gatekeeper_accepted` : `versioned_build`,
      },
    }),
    await e.trashItem(a.plugin.source.path),
    Ho.info(`computer_use_local_to_bundled_migration_moved_local_to_trash`, {
      safe: {
        localBuildNumber: s,
        reason: l ? `gatekeeper_accepted` : `versioned_build`,
      },
      sensitive: { localPluginPath: a.plugin.source.path },
    }),
    await Yo({
      codexHome: e.codexHome,
      localPluginPath: a.plugin.source.path,
      marketplacePath: a.marketplace.path,
    }),
    await Zo({ codexHome: e.codexHome, localPluginPath: a.plugin.source.path }),
    !i.plugin.installed &&
      (await e.appServerConnection.installPlugin({
        marketplacePath: i.marketplace.path,
        pluginName: n.ws,
      }),
      Ho.info(`computer_use_local_to_bundled_migration_installed_bundled`, {
        safe: {},
        sensitive: { marketplacePath: i.marketplace.path },
      })));
}
function Jo(e) {
  let t = (0, u.join)(e.codexHome, `plugins`, n.ws);
  for (let n of e.marketplaces)
    for (let e of n.plugins)
      if (
        e.name === `computer-use` &&
        e.installed &&
        n.path != null &&
        e.source.type === `local` &&
        _s(e.source.path, t)
      )
        return {
          marketplace: { ...n, path: n.path },
          plugin: { ...e, source: e.source },
        };
  return null;
}
async function Yo(e) {
  try {
    let t = await Xo(e.marketplacePath),
      n = t.plugins.filter(
        (t) =>
          !ds({
            codexHome: e.codexHome,
            localPluginPath: e.localPluginPath,
            plugin: t,
          }),
      );
    if (n.length === t.plugins.length) {
      Ho.debug(
        `computer_use_local_to_bundled_migration_marketplace_entry_missing`,
        { safe: {}, sensitive: { marketplacePath: e.marketplacePath } },
      );
      return;
    }
    (await m.default.writeFile(
      e.marketplacePath,
      JSON.stringify({ ...t, plugins: n }, null, 2) +
        `
`,
      `utf8`,
    ),
      Ho.info(
        `computer_use_local_to_bundled_migration_removed_marketplace_entry`,
        { safe: {}, sensitive: { marketplacePath: e.marketplacePath } },
      ));
  } catch (t) {
    Ho.warning(
      `computer_use_local_to_bundled_migration_marketplace_cleanup_failed`,
      { safe: {}, sensitive: { error: t, marketplacePath: e.marketplacePath } },
    );
  }
}
async function Xo(e) {
  return Go.parseAsync(JSON.parse(await m.default.readFile(e, `utf8`)));
}
async function Zo(e) {
  let t = (0, u.join)(e.codexHome, `config.toml`);
  try {
    let n = await m.default.readFile(t, `utf8`).catch((e) => {
      if (ys(e, `ENOENT`)) return null;
      throw e;
    });
    if (n == null) return;
    let r = Qo({ content: n, localPluginPath: e.localPluginPath });
    if (r === n) return;
    (await m.default.writeFile(t, r, `utf8`),
      Ho.info(`computer_use_local_to_bundled_migration_removed_config`, {
        safe: {},
        sensitive: { configPath: t },
      }));
  } catch (e) {
    Ho.warning(
      `computer_use_local_to_bundled_migration_config_cleanup_failed`,
      { safe: {}, sensitive: { configPath: t, error: e } },
    );
  }
}
function Qo(e) {
  let t = n.Qr(e.content),
    r = e.content;
  $o({ config: t, localPluginPath: e.localPluginPath }) && (r = is(r, ss));
  let i = es({ config: t, localPluginPath: e.localPluginPath });
  return (i.changed && (r = as(r, i.notify)), us(r));
}
function $o(e) {
  let t = e.config.mcp_servers?.[n.ws]?.command;
  return typeof t == `string` && vs(t, e.localPluginPath);
}
function es(e) {
  return rs(e.config.notify)
    ? ts({ localPluginPath: e.localPluginPath, notify: e.config.notify })
    : { changed: !1 };
}
function ts(e) {
  let t = e.notify[0];
  if (t == null || !vs(t, e.localPluginPath)) return { changed: !1 };
  let n = ns(e.notify);
  if (n == null) return { changed: !0, notify: null };
  let r = ts({ localPluginPath: e.localPluginPath, notify: n });
  return { changed: !0, notify: r.changed ? r.notify : n };
}
function ns(e) {
  let t = e.indexOf(Vo);
  if (t < 0 || t + 1 >= e.length) return null;
  try {
    let n = JSON.parse(e[t + 1] ?? ``);
    return rs(n) ? n : null;
  } catch {
    return null;
  }
}
function rs(e) {
  return Array.isArray(e) && e.every((e) => typeof e == `string`);
}
function is(e, t) {
  let n = e.split(/\r?\n/),
    r = [],
    i = !1;
  for (let e of n) (cs(e) && ((i = t(e)), i)) || i || r.push(e);
  return r.join(`
`);
}
function as(e, t) {
  let n = os(e.split(/\r?\n/));
  if (t == null)
    return n.join(`
`);
  let r = `notify = ${JSON.stringify(t)}`,
    i = n.findIndex(cs);
  if (i < 0)
    return [...n, r].join(`
`);
  let a = [...n];
  return (
    a.splice(i, 0, r),
    i + 1 < a.length && a[i + 1]?.trim() !== `` && a.splice(i + 1, 0, ``),
    a.join(`
`)
  );
}
function os(e) {
  let t = [],
    n = !1;
  for (let r = 0; r < e.length; r += 1) {
    let i = e[r] ?? ``;
    if (cs(i)) return (t.push(...e.slice(r)), t);
    if (n) {
      i.includes(`]`) && (n = !1);
      continue;
    }
    if (ls(i)) {
      i.includes(`[`) && !i.includes(`]`) && (n = !0);
      continue;
    }
    t.push(i);
  }
  return t;
}
function ss(e) {
  return /^\s*\[\s*mcp_servers\s*\.\s*(?:"computer-use"|computer-use)\s*\]\s*$/.test(
    e,
  );
}
function cs(e) {
  return /^\s*\[/.test(e);
}
function ls(e) {
  let t = e.trimStart();
  return t.startsWith(`notify `) || t.startsWith(`notify=`);
}
function us(e) {
  let t = e.trimEnd();
  return t === `` ? `` : `${t}\n`;
}
function ds(e) {
  return (
    e.plugin.name === `computer-use` &&
    e.plugin.source?.source === `local` &&
    fs({
      codexHome: e.codexHome,
      localPluginPath: e.localPluginPath,
      sourcePath: e.plugin.source.path,
    })
  );
}
function fs(e) {
  return [
    (0, u.resolve)(e.sourcePath),
    (0, u.resolve)(e.codexHome, e.sourcePath),
    (0, u.resolve)((0, u.dirname)(e.codexHome), e.sourcePath),
  ].some((t) => _s(t, e.localPluginPath));
}
async function ps(e) {
  try {
    return hs((await ms(e)).version);
  } catch (t) {
    return (
      Ho.warning(`computer_use_plugin_manifest_read_failed`, {
        safe: {},
        sensitive: { error: t, pluginPath: e },
      }),
      null
    );
  }
}
async function ms(e) {
  let t = (0, u.join)(e, `.codex-plugin`, `plugin.json`);
  return Uo.parseAsync(JSON.parse(await m.default.readFile(t, `utf8`)));
}
function hs(e) {
  let t = e.match(/^\d+\.\d+\.(\d+)(?:[-+].*)?$/);
  return t == null ? null : Number(t[1]);
}
async function gs(e) {
  if (process.platform !== `darwin`) return !1;
  try {
    return (
      await zo(
        `/usr/sbin/spctl`,
        [`--assess`, `--type`, `execute`, `--verbose=4`, e],
        { env: r.t(process.env), timeout: 5e3 },
      ),
      !0
    );
  } catch {
    return !1;
  }
}
function _s(e, t) {
  return (0, u.resolve)(e) === (0, u.resolve)(t);
}
function vs(e, t) {
  let n = (0, u.resolve)(e),
    r = (0, u.resolve)(t);
  return n === r || n.startsWith(`${r}${u.sep}`);
}
function ys(e, t) {
  return typeof e == `object` && !!e && `code` in e && e.code === t;
}
var bs = [
  {
    autoInstallOptOutKey: n.js(n.Os),
    installWhenMissing: !0,
    name: n.Os,
    isAvailable: ({ features: e }) => e.sites,
  },
  {
    autoInstallOptOutKey: n.js(n.Ss),
    installWhenMissing: !0,
    name: n.Ss,
    isAvailable: ({ features: e }) => e.inAppBrowserUseAllowed,
    migrate: jo,
  },
  {
    name: o.s,
    syncInstallStateWithChromeExtension: !0,
    isAvailable: ({ buildFlavor: e, env: t, features: n }) =>
      o.i(e, t) && n.externalBrowserUseAllowed,
  },
  {
    name: n.Cs,
    syncInstallStateWithChromeExtension: !0,
    isAvailable: ({ buildFlavor: e, env: t, features: n }) =>
      o.a(e, t) && n.externalBrowserUseAllowed,
  },
  {
    name: o.c,
    syncInstallStateWithChromeExtension: !0,
    isAvailable: ({ buildFlavor: e, features: t }) =>
      t.externalBrowserUseAllowed && o.o(e),
  },
  {
    autoInstallOptOutKey: n.js(n.ws),
    installWhenMissing: !0,
    installWhenMissingRequiresOptIn: !0,
    name: n.ws,
    isAvailable: ({ features: e, platform: t }) =>
      t === `darwin` && e.computerUse,
    migrate: Ko,
  },
  {
    autoInstallOptOutKey: n.js(n.ws),
    installWhenMissing: !0,
    installWhenMissingRequiresOptIn: !0,
    name: n.ws,
    isAvailable: ({ features: e, platform: t }) =>
      t === `win32` && e.computerUse,
  },
  {
    name: n.Ds,
    isAvailable: ({ features: e, platform: t }) =>
      t === `darwin` && e.recordAndReplay,
  },
  { name: n.Es, isAvailable: () => !0 },
  {
    installWhenMissing: !0,
    name: n.Ts,
    isAvailable: ({ features: e }) => e.deepResearch,
  },
  {
    installWhenMissing: !0,
    name: n.ks,
    isAvailable: ({ features: e }) => e.visualize,
  },
];
function xs(e) {
  let t;
  return (
    (t =
      e.chromeExtensionInstalled === !1 && e.chromeExtensionSyncManaged
        ? `chrome_extension_removed`
        : e.installedBefore
          ? `installed_before`
          : e.chromeExtensionInstalled === !0
            ? `chrome_extension_installed`
            : e.autoInstallWhenMissing
              ? `auto_install`
              : `not_installed`),
    {
      eligibilityReason: e.eligibilityReason,
      expectationReason: t,
      expectedInstalled:
        t === `installed_before` ||
        t === `chrome_extension_installed` ||
        t === `auto_install`,
      installedAfter: e.installedAfter,
      installedBefore: e.installedBefore,
      pluginName: e.pluginName,
      reconcileReason: e.reconcileReason,
    }
  );
}
var Ss = e.n((e) => {
    (Object.defineProperty(e, `__esModule`, { value: !0 }),
      (e.sync = e.isexe = void 0));
    var t = require(`fs`),
      n = require(`fs/promises`);
    ((e.isexe = async (e, t = {}) => {
      let { ignoreErrors: i = !1 } = t;
      try {
        return r(await (0, n.stat)(e), t);
      } catch (e) {
        let t = e;
        if (i || t.code === `EACCES`) return !1;
        throw t;
      }
    }),
      (e.sync = (e, n = {}) => {
        let { ignoreErrors: i = !1 } = n;
        try {
          return r((0, t.statSync)(e), n);
        } catch (e) {
          let t = e;
          if (i || t.code === `EACCES`) return !1;
          throw t;
        }
      }));
    var r = (e, t) => e.isFile() && i(e, t),
      i = (e, t) => {
        let n = t.uid ?? process.getuid?.(),
          r = t.groups ?? process.getgroups?.() ?? [],
          i = t.gid ?? process.getgid?.() ?? r[0];
        if (n === void 0 || i === void 0) throw Error(`cannot get uid or gid`);
        let a = new Set([i, ...r]),
          o = e.mode,
          s = e.uid,
          c = e.gid;
        return !!(
          o & 1 ||
          (o & 8 && a.has(c)) ||
          (o & 64 && s === n) ||
          (o & 72 && n === 0)
        );
      };
  }),
  Cs = e.n((e) => {
    (Object.defineProperty(e, `__esModule`, { value: !0 }),
      (e.sync = e.isexe = void 0));
    var t = require(`fs`),
      n = require(`fs/promises`);
    ((e.isexe = async (e, t = {}) => {
      let { ignoreErrors: r = !1 } = t;
      try {
        return i(await (0, n.stat)(e), e, t);
      } catch (e) {
        let t = e;
        if (r || t.code === `EACCES`) return !1;
        throw t;
      }
    }),
      (e.sync = (e, n = {}) => {
        let { ignoreErrors: r = !1 } = n;
        try {
          return i((0, t.statSync)(e), e, n);
        } catch (e) {
          let t = e;
          if (r || t.code === `EACCES`) return !1;
          throw t;
        }
      }));
    var r = (e, t) => {
        let { pathExt: n = process.env.PATHEXT || `` } = t,
          r = n.split(`;`);
        if (r.indexOf(``) !== -1) return !0;
        for (let t = 0; t < r.length; t++) {
          let n = r[t].toLowerCase(),
            i = e.substring(e.length - n.length).toLowerCase();
          if (n && i === n) return !0;
        }
        return !1;
      },
      i = (e, t, n) => e.isFile() && r(t, n);
  }),
  ws = e.n((e) => {
    Object.defineProperty(e, `__esModule`, { value: !0 });
  }),
  Ts = e.n((e) => {
    var t =
        (e && e.__createBinding) ||
        (Object.create
          ? function (e, t, n, r) {
              r === void 0 && (r = n);
              var i = Object.getOwnPropertyDescriptor(t, n);
              ((!i ||
                (`get` in i ? !t.__esModule : i.writable || i.configurable)) &&
                (i = {
                  enumerable: !0,
                  get: function () {
                    return t[n];
                  },
                }),
                Object.defineProperty(e, r, i));
            }
          : function (e, t, n, r) {
              (r === void 0 && (r = n), (e[r] = t[n]));
            }),
      n =
        (e && e.__setModuleDefault) ||
        (Object.create
          ? function (e, t) {
              Object.defineProperty(e, `default`, { enumerable: !0, value: t });
            }
          : function (e, t) {
              e.default = t;
            }),
      r =
        (e && e.__importStar) ||
        function (e) {
          if (e && e.__esModule) return e;
          var r = {};
          if (e != null)
            for (var i in e)
              i !== `default` &&
                Object.prototype.hasOwnProperty.call(e, i) &&
                t(r, e, i);
          return (n(r, e), r);
        },
      i =
        (e && e.__exportStar) ||
        function (e, n) {
          for (var r in e)
            r !== `default` &&
              !Object.prototype.hasOwnProperty.call(n, r) &&
              t(n, e, r);
        };
    (Object.defineProperty(e, `__esModule`, { value: !0 }),
      (e.sync = e.isexe = e.posix = e.win32 = void 0));
    var a = r(Ss());
    e.posix = a;
    var o = r(Cs());
    ((e.win32 = o), i(ws(), e));
    var s =
      (process.env._ISEXE_TEST_PLATFORM_ || process.platform) === `win32`
        ? o
        : a;
    ((e.isexe = s.isexe), (e.sync = s.sync));
  }),
  Es = e.n((e, t) => {
    var { isexe: n, sync: r } = Ts(),
      { join: i, delimiter: a, sep: o, posix: s } = require(`path`),
      c = process.platform === `win32`,
      l = new RegExp(
        `[${s.sep}${o === s.sep ? `` : o}]`.replace(/(\\)/g, `\\$1`),
      ),
      u = RegExp(`^\\.${l.source}`),
      d = (e) => Object.assign(Error(`not found: ${e}`), { code: `ENOENT` }),
      f = (
        e,
        {
          path: t = process.env.PATH,
          pathExt: n = process.env.PATHEXT,
          delimiter: r = a,
        },
      ) => {
        let i = e.match(l)
          ? [``]
          : [...(c ? [process.cwd()] : []), ...(t || ``).split(r)];
        if (c) {
          let t = n || [`.EXE`, `.CMD`, `.BAT`, `.COM`].join(r),
            a = t.split(r).flatMap((e) => [e, e.toLowerCase()]);
          return (
            e.includes(`.`) && a[0] !== `` && a.unshift(``),
            { pathEnv: i, pathExt: a, pathExtExe: t }
          );
        }
        return { pathEnv: i, pathExt: [``] };
      },
      p = (e, t) => {
        let n = /^".*"$/.test(e) ? e.slice(1, -1) : e;
        return (!n && u.test(t) ? t.slice(0, 2) : ``) + i(n, t);
      },
      m = async (e, t = {}) => {
        let { pathEnv: r, pathExt: i, pathExtExe: a } = f(e, t),
          o = [];
        for (let s of r) {
          let r = p(s, e);
          for (let e of i) {
            let i = r + e;
            if (await n(i, { pathExt: a, ignoreErrors: !0 })) {
              if (!t.all) return i;
              o.push(i);
            }
          }
        }
        if (t.all && o.length) return o;
        if (t.nothrow) return null;
        throw d(e);
      };
    ((t.exports = m),
      (m.sync = (e, t = {}) => {
        let { pathEnv: n, pathExt: i, pathExtExe: a } = f(e, t),
          o = [];
        for (let s of n) {
          let n = p(s, e);
          for (let e of i) {
            let i = n + e;
            if (r(i, { pathExt: a, ignoreErrors: !0 })) {
              if (!t.all) return i;
              o.push(i);
            }
          }
        }
        if (t.all && o.length) return o;
        if (t.nothrow) return null;
        throw d(e);
      }));
  }),
  Ds = e.o(Es()),
  Os = process.platform === `darwin`,
  ks = process.platform === `win32`;
function As(e) {
  let t = Ds.default.sync(e, { nothrow: !0 });
  return typeof t == `string` && (0, p.existsSync)(t) ? t : ks ? Fs(e) : null;
}
function js(e) {
  let t = e;
  for (;;) {
    if ((0, p.existsSync)(t)) return t;
    let e = (0, u.dirname)(t);
    if (e === t) return null;
    t = e;
  }
}
function Ms(e) {
  if (Os) return [`-R`, e];
  if (ks) {
    let t = js(e);
    return t ? ((0, p.statSync)(t).isFile() ? [`/select,`, t] : [t]) : [e];
  }
  return [e];
}
function Ns(e, t, n, r) {
  return e === `win32` && /\.(cmd|bat)$/i.test(t)
    ? { command: r ?? `cmd.exe`, args: [`/d`, `/s`, `/c`, t, ...n] }
    : { command: t, args: n };
}
function Ps(e, t, n) {
  return new Promise((i, a) => {
    let o = Ns(process.platform, e, t, process.env.ComSpec),
      s = (0, h.spawn)(o.command, o.args, {
        stdio: n?.stdin == null ? `ignore` : [`pipe`, `ignore`, `ignore`],
        env: r.t(n?.env ?? process.env),
      });
    (s.on(`error`, a),
      s.stdin?.on(`error`, a),
      s.stdin?.end(n?.stdin),
      s.on(`close`, (n) => {
        if (!n) {
          i();
          return;
        }
        a(Error(`${e} exited with code ${n ?? `unknown`} (${t.join(` `)})`));
      }));
  });
}
function Fs(e) {
  let t = (0, h.spawnSync)(`where.exe`, [e], {
    windowsHide: !0,
    encoding: `utf8`,
    env: r.t(process.env),
  });
  return t.status !== 0 || typeof t.stdout != `string`
    ? null
    : (t.stdout
        .split(/\r?\n/)
        .map((e) => e.trim())
        .find((e) => e.length > 0) ?? null);
}
var Is =
    process.env.LOCALAPPDATA ??
    (0, u.join)((0, l.homedir)(), `AppData`, `Local`),
  Ls = [
    (0, u.join)(Is, `Programs`),
    process.env.ProgramFiles,
    process.env[`ProgramFiles(x86)`],
  ].flatMap((e) => (e ? [e] : [])),
  Rs = (0, u.join)(Is, `Microsoft`, `WindowsApps`),
  zs = [
    process.env.APPDATA
      ? (0, u.join)(
          process.env.APPDATA,
          `Microsoft`,
          `Windows`,
          `Start Menu`,
          `Programs`,
        )
      : null,
    process.env.ProgramData
      ? (0, u.join)(
          process.env.ProgramData,
          `Microsoft`,
          `Windows`,
          `Start Menu`,
          `Programs`,
        )
      : null,
  ].flatMap((e) => (e ? [e] : [])),
  Bs = (e) =>
    process.env[e] == null
      ? null
      : (0, u.join)(
          process.env[e],
          `Microsoft Visual Studio`,
          `Installer`,
          `vswhere.exe`,
        ),
  Vs = [Bs(`ProgramFiles(x86)`), Bs(`ProgramFiles`)].flatMap((e) =>
    e ? [e] : [],
  );
function Hs(e) {
  for (let t of Ls)
    for (let n of e) {
      let e = (0, u.join)(t, ...n);
      if ((0, p.existsSync)(e)) return e;
    }
  return null;
}
function Us(e) {
  if (!(0, p.existsSync)(e)) return null;
  if ((0, u.extname)(e)) return e;
  for (let t of [`.cmd`, `.bat`, `.exe`]) {
    let n = `${e}${t}`;
    if ((0, p.existsSync)(n)) return n;
  }
  return e;
}
function Ws(e, t) {
  let n = (0, u.dirname)(e);
  if ((0, u.basename)(n).toLowerCase() === `bin`) {
    let e = (0, u.join)((0, u.dirname)(n), t);
    if ((0, p.existsSync)(e)) return e;
  }
  let r = (0, u.join)(n, t);
  return (0, p.existsSync)(r) ? r : null;
}
function Gs(e) {
  return Us((0, u.join)(Rs, e));
}
function Ks() {
  return Rs;
}
function qs({ pathCommand: e, executableName: t, installDirName: n }) {
  if (e) {
    let n = Us(e);
    if (n)
      return (
        Ws(n, t) ??
        ((0, u.basename)(n).toLowerCase() === t.toLowerCase() ? n : null)
      );
  }
  return Hs([[n, t]]);
}
function Js(e) {
  let t = Vs.find((e) => (0, p.existsSync)(e));
  if (t == null) return null;
  let n = (0, h.spawnSync)(
    t,
    [`-products`, `*`, `-all`, `-find`, e, `-utf8`, `-nologo`, `-prerelease`],
    { windowsHide: !0, encoding: `utf8`, env: r.t(process.env) },
  );
  return n.status !== 0 || typeof n.stdout != `string`
    ? null
    : (n.stdout
        .split(/\r?\n/)
        .map((e) => e.trim())
        .find((e) => e.length > 0 && (0, p.existsSync)(e)) ?? null);
}
function Ys(e) {
  let t = new Set(e.map((e) => e.toLowerCase()));
  for (let e of zs) {
    let n = Xs(e, t);
    if (n) return n;
  }
  return null;
}
function Xs(e, t) {
  if (!(0, p.existsSync)(e)) return null;
  for (let n of (0, p.readdirSync)(e, { withFileTypes: !0 })) {
    let r = (0, u.join)(e, n.name);
    if (n.isFile() && t.has(n.name.toLowerCase())) return r;
    if (!n.isDirectory()) continue;
    let i = Xs(r, t);
    if (i) return i;
  }
  return null;
}
var Zs = `com.google.Chrome`,
  Qs = `/usr/bin/open`,
  $s = /^[a-p]{32}$/;
function ec(e) {
  return `chrome://extensions/?id=${ac(e)}`;
}
function tc({
  extensionId: e,
  homeDir: t = (0, l.homedir)(),
  localAppDataDir: n = process.env.LOCALAPPDATA,
  platform: r = process.platform,
}) {
  let i = ac(e),
    a = oc({ homeDir: t, localAppDataDir: n, platform: r });
  return a == null || !(0, p.existsSync)(a)
    ? !1
    : (0, p.readdirSync)(a, { withFileTypes: !0 }).some(
        (e) =>
          e.isDirectory() &&
          (0, p.existsSync)((0, u.join)(a, e.name, `Extensions`, i)),
      );
}
async function nc({
  extensionId: e,
  platform: t = process.platform,
  detectChromeCommand: n = rc,
  runCommand: r = Ps,
}) {
  if (t === `darwin`) {
    await r(Qs, [`-b`, Zs, ec(e)]);
    return;
  }
  if (t === `win32`) {
    let t = n();
    if (t == null) throw Error(`Google Chrome is not installed`);
    await r(t, [ec(e)]);
    return;
  }
  throw Error(
    `Opening Chrome extension settings is only supported on macOS and Windows`,
  );
}
function rc() {
  return (
    As(`chrome.exe`) ??
    As(`chrome`) ??
    Hs([[`Google`, `Chrome`, `Application`, `chrome.exe`]]) ??
    ic()
  );
}
function ic() {
  let e = (0, u.join)(
    process.env.LOCALAPPDATA ??
      (0, u.join)((0, l.homedir)(), `AppData`, `Local`),
    `Google`,
    `Chrome`,
    `Application`,
    `chrome.exe`,
  );
  return (0, p.existsSync)(e) ? e : null;
}
function ac(e) {
  let t = e.trim();
  if (!$s.test(t)) throw Error(`Invalid Chrome extension id`);
  return t;
}
function oc({ homeDir: e, localAppDataDir: t, platform: n }) {
  return n === `darwin`
    ? (0, u.join)(e, `Library`, `Application Support`, `Google`, `Chrome`)
    : n === `win32`
      ? (0, u.join)(
          t ?? (0, u.join)(e, `AppData`, `Local`),
          `Google`,
          `Chrome`,
          `User Data`,
        )
      : null;
}
var sc = [`plugins`],
  cc = [`.agents`, `plugins`, `marketplace.json`],
  lc = [`scripts`, `extension-id.json`],
  uc = new Set(o.u),
  dc = n
    .wl()
    .trim()
    .min(1)
    .refine(
      (e) => e !== `.` && e !== `..` && !e.includes(`/`) && !e.includes(`\\`),
      `Expected a single marketplace name.`,
    ),
  fc = n.yl({
    name: n.wl().trim().min(1),
    source: n.xl({ source: n.vl(`local`), path: n.wl().trim().min(1) }),
  }),
  pc = n.yl({
    name: dc.refine(n.Ns, `Expected an OpenAI bundled marketplace name.`),
    interface: n.El().optional(),
    plugins: n.pl(fc),
  }),
  mc = n.yl({ name: dc, interface: n.El().optional(), plugins: n.pl(fc) }),
  hc = n.yl({
    bundledContentVariant: n.wl().trim().min(1).optional(),
    name: n.wl(),
    version: n.wl().trim().min(1),
  }),
  gc = n.xl({ extensionId: n.wl().trim().min(1) }),
  _c = r.i(`BundledPluginsMarketplace`),
  vc = (0, d.promisify)(h.execFile),
  yc = (function (e) {
    return (
      (e.LocalPluginSource = `local_plugin_source`),
      (e.Other = `other`),
      (e.PluginCacheWindowsFileLock = `plugin_cache_windows_file_lock`),
      (e.PluginsDisabled = `plugins_disabled`),
      e
    );
  })({});
async function bc(e) {
  let t = e.marketplaceName ?? `openai-bundled`,
    n = e.marketplacePluginNames,
    r = new Set(e.reconcilePluginNames ?? n),
    i = new Set(e.syncInstallStateWithChromeExtensionPluginNames ?? []),
    a = e.throwOnReconcileFailure === !0,
    o = await Dc({
      appServerConnection: e.appServerConnection,
      failOnError: a,
      marketplaceName: t,
      marketplacePluginNames: n,
    }),
    s = o.marketplace,
    c = new Set(n),
    l = s?.plugins.filter((e) => e.installed && !c.has(e.name)) ?? [],
    u,
    d,
    f;
  try {
    (([d, u] = await Promise.all([
      e.appServerConnection.codexHome(),
      e.appServerConnection.platformFamily(),
    ])),
      (f = await Oc({
        browserSkillVariant: e.browserSkillVariant,
        codexHome: d,
        computerUseSkillVariant: e.computerUseSkillVariant,
        marketplacePluginNames: n,
        marketplaceName: t,
        platformFamily: u,
        resourcesPath: e.resourcesPath,
        runtimeMarketplaceRoot: e.runtimeMarketplaceRoot,
      })));
  } catch (n) {
    return (
      _c.warning(`bundled_plugins_marketplace_resolve_failed`, {
        safe: {},
        sensitive: { error: n },
      }),
      await Xc({
        appServerConnection: e.appServerConnection,
        chromeExtensionSyncManagedPluginStore:
          e.chromeExtensionSyncManagedPluginStore,
        disabledInstalledPlugins: l,
        marketplaceName: t,
      }),
      { hadReconcileFailure: !0 }
    );
  }
  if (f == null)
    return (
      _c.warning(`bundled_plugins_marketplace_missing`, {
        safe: { marketplacePluginNames: n },
        sensitive: {},
      }),
      await Xc({
        appServerConnection: e.appServerConnection,
        chromeExtensionSyncManagedPluginStore:
          e.chromeExtensionSyncManagedPluginStore,
        disabledInstalledPlugins: l,
        marketplaceName: t,
      }),
      { hadReconcileFailure: !0 }
    );
  let p = await wc({
    appServerConnection: e.appServerConnection,
    chromeExtensionSyncManagedPluginStore:
      e.chromeExtensionSyncManagedPluginStore,
    devRuntimeRepoRoot: e.devRuntimeRepoRoot,
    throwOnReconcileFailure: a,
    forceInstallPluginNames: new Set(e.forceInstallPluginNames ?? []),
    installWhenMissingPluginNames: new Set(
      e.installWhenMissingPluginNames ?? [],
    ),
    isChromeExtensionInstalled: e.isChromeExtensionInstalled ?? tc,
    installedBundledMarketplace: s,
    materializedMarketplace: f,
    platformFamily: u,
    reconcilePluginNames: r,
    resourcesPath: e.resourcesPath,
    syncInstallStateWithChromeExtensionPluginNames: i,
  });
  return (
    await Xc({
      appServerConnection: e.appServerConnection,
      chromeExtensionSyncManagedPluginStore:
        e.chromeExtensionSyncManagedPluginStore,
      disabledInstalledPlugins: l,
      marketplaceName: t,
    }),
    p.installedPluginCountAfterWrite != null &&
      Hc({
        installedBundledMarketplace: s,
        installedPluginCountAfterWrite: p.installedPluginCountAfterWrite,
        marketplaceName: t,
        materializedMarketplace: f,
      }),
    { hadReconcileFailure: o.hadReadFailure || p.hadReconcileFailure }
  );
}
async function xc(e) {
  if (e.marketplacePluginNames.length === 0) return null;
  let t = await Sc(e);
  return t == null ? null : JSON.stringify(t);
}
async function Sc(e) {
  if (e.marketplacePluginNames.length === 0) return [];
  let t = e.marketplaceName ?? `openai-bundled`;
  try {
    let n = await kc({ marketplaceName: t, resourcesPath: e.resourcesPath });
    if (n == null)
      return (
        _c.warning(`bundled_plugins_marketplace_missing`, {
          safe: { marketplacePluginNames: e.marketplacePluginNames },
          sensitive: {},
        }),
        null
      );
    let r = Gc({
        marketplacePluginNames: e.marketplacePluginNames,
        marketplace: await Ic(n),
      }),
      i = e.isChromeExtensionInstalled ?? tc;
    return await Promise.all(
      r.plugins.map(async (e) => {
        let t = await Fc(Nc({ bundledPlugin: e, localMarketplaceRoot: n }));
        return {
          extensionId: t,
          isExtensionInstalled: i({ extensionId: t }),
          pluginName: e.name,
        };
      }),
    );
  } catch (t) {
    return (
      _c.warning(`bundled_plugins_chrome_extension_sync_state_read_failed`, {
        safe: { marketplacePluginNames: e.marketplacePluginNames },
        sensitive: { error: t },
      }),
      null
    );
  }
}
async function Cc(e) {
  let t, n;
  try {
    ((t = await e.appServerConnection.platformFamily()),
      (n = await Ac({
        marketplaceRoot: e.marketplaceRoot,
        platformFamily: t,
      })));
  } catch (t) {
    return (
      _c.warning(`bundled_plugins_marketplace_resolve_failed`, {
        safe: {},
        sensitive: { error: t, marketplaceRoot: e.marketplaceRoot },
      }),
      !1
    );
  }
  if (n == null)
    return (
      _c.warning(`bundled_plugins_marketplace_missing`, {
        safe: {},
        sensitive: { marketplaceRoot: e.marketplaceRoot },
      }),
      !1
    );
  let r = (
      await Dc({
        appServerConnection: e.appServerConnection,
        failOnError: !1,
        marketplaceName: n.marketplace.name,
        marketplacePluginNames: n.marketplace.plugins.map((e) => e.name),
      })
    ).marketplace,
    i = await wc({
      appServerConnection: e.appServerConnection,
      devRuntimeRepoRoot: e.devRuntimeRepoRoot,
      throwOnReconcileFailure: !1,
      forceInstallPluginNames: new Set(),
      installWhenMissingPluginNames: new Set(
        n.marketplace.plugins.map((e) => e.name),
      ),
      isChromeExtensionInstalled: tc,
      installedBundledMarketplace: r,
      materializedMarketplace: n,
      platformFamily: t,
      reconcilePluginNames: new Set(n.marketplace.plugins.map((e) => e.name)),
      resourcesPath: e.resourcesPath,
      syncInstallStateWithChromeExtensionPluginNames: new Set(),
    });
  return i.installedPluginCountAfterWrite == null
    ? !1
    : (Hc({
        installedBundledMarketplace: r,
        installedPluginCountAfterWrite: i.installedPluginCountAfterWrite,
        marketplaceName: n.marketplace.name,
        materializedMarketplace: n,
      }),
      !0);
}
async function wc(e) {
  let t;
  try {
    t = (
      await e.appServerConnection.addMarketplace({
        source: e.materializedMarketplace.marketplaceRoot,
      })
    ).marketplaceName;
  } catch (t) {
    if (
      (_c.warning(`bundled_plugins_marketplace_add_failed`, {
        safe: { marketplaceName: e.materializedMarketplace.marketplace.name },
        sensitive: {
          error: t,
          marketplaceRoot: e.materializedMarketplace.marketplaceRoot,
        },
      }),
      e.throwOnReconcileFailure)
    )
      throw t;
    return { hadReconcileFailure: !0, installedPluginCountAfterWrite: null };
  }
  _c.info(`bundled_plugins_marketplace_added`, {
    safe: {
      marketplacePluginNames: e.materializedMarketplace.marketplace.plugins.map(
        (e) => e.name,
      ),
      marketplaceName: t,
    },
    sensitive: { marketplaceRoot: e.materializedMarketplace.marketplaceRoot },
  });
  let n =
      e.syncInstallStateWithChromeExtensionPluginNames.size > 0
        ? await Dc({
            appServerConnection: e.appServerConnection,
            failOnError: e.throwOnReconcileFailure,
            marketplaceName: t,
            marketplacePluginNames: [...e.reconcilePluginNames],
          })
        : { hadReadFailure: !1, marketplace: e.installedBundledMarketplace },
    r = n.marketplace,
    i;
  try {
    i = await Kc({
      appServerConnection: e.appServerConnection,
      chromeExtensionSyncManagedPluginStore:
        e.chromeExtensionSyncManagedPluginStore,
      devRuntimeRepoRoot: e.devRuntimeRepoRoot,
      forceInstallPluginNames: e.forceInstallPluginNames,
      installWhenMissingPluginNames: e.installWhenMissingPluginNames,
      isChromeExtensionInstalled: e.isChromeExtensionInstalled,
      installedBundledMarketplace: r,
      localMarketplaceRoot: e.materializedMarketplace.localMarketplaceRoot,
      marketplace: e.materializedMarketplace.marketplace,
      marketplaceName: t,
      marketplaceRoot: e.materializedMarketplace.marketplaceRoot,
      platformFamily: e.platformFamily,
      reconcilePluginNames: e.reconcilePluginNames,
      resourcesPath: e.resourcesPath,
      syncInstallStateWithChromeExtensionPluginNames:
        e.syncInstallStateWithChromeExtensionPluginNames,
    });
  } catch (n) {
    if (
      (_c.warning(`bundled_plugins_marketplace_install_failed`, {
        safe: {
          errorCategory: Tc({ error: n, platformFamily: e.platformFamily }),
          marketplaceName: t,
          platformFamily: e.platformFamily,
        },
        sensitive: {
          error: n,
          marketplaceRoot: e.materializedMarketplace.marketplaceRoot,
        },
      }),
      e.throwOnReconcileFailure)
    )
      throw n;
    return { hadReconcileFailure: !0, installedPluginCountAfterWrite: null };
  }
  let { firstFailure: a } = i;
  if (a != null) {
    if (e.throwOnReconcileFailure) throw a.error;
    return { hadReconcileFailure: !0, installedPluginCountAfterWrite: null };
  }
  return {
    hadReconcileFailure: n.hadReadFailure,
    installedPluginCountAfterWrite: i.installedPluginCountAfterWrite,
  };
}
function Tc(e) {
  let t = Ec(e.error).toLowerCase();
  return t.includes(`codex plugins are disabled for this workspace`)
    ? yc.PluginsDisabled
    : e.platformFamily === `windows` &&
        (n.Ls(e.error) === `cache_overwrite_failure` ||
          t.includes(`access is denied`) ||
          t.includes(`being used by another process`) ||
          t.includes(`os error 32`) ||
          t.includes(`os error 5`))
      ? yc.PluginCacheWindowsFileLock
      : t.includes(`plugin source path is not a directory`) ||
          t.includes(`missing plugin.json`) ||
          t.includes(`missing or invalid plugin.json`) ||
          t.includes(`local plugin source path`) ||
          t.includes(`failed to read plugin source directory`) ||
          t.includes(`failed to resolve local marketplace source path`) ||
          t.includes(
            `marketplace root does not contain a supported manifest`,
          ) ||
          t.includes(`marketplace file is not in a supported location`)
        ? yc.LocalPluginSource
        : yc.Other;
}
function Ec(e) {
  return e instanceof Error ? e.message : String(e);
}
async function Dc(e) {
  try {
    let { marketplaces: t } = await e.appServerConnection.listPlugins(o.l);
    return {
      hadReadFailure: !1,
      marketplace: t.find((t) => t.name === e.marketplaceName),
    };
  } catch (t) {
    if (
      (_c.warning(`bundled_plugins_list_failed`, {
        safe: {
          marketplaceName: e.marketplaceName,
          marketplacePluginNames: e.marketplacePluginNames,
        },
        sensitive: { error: t },
      }),
      e.failOnError)
    )
      throw t;
    return { hadReadFailure: !0, marketplace: void 0 };
  }
}
async function Oc(e) {
  let t = await kc({
    marketplaceName: e.marketplaceName,
    resourcesPath: e.resourcesPath,
  });
  if (t == null) return null;
  let r = await jc({
      browserSkillVariant: e.browserSkillVariant,
      codexHome: e.codexHome,
      computerUseSkillVariant: e.computerUseSkillVariant,
      marketplacePluginNames: e.marketplacePluginNames,
      marketplaceName: e.marketplaceName,
      sourceMarketplaceRoot: t,
      targetMarketplaceRoot: e.runtimeMarketplaceRoot,
    }),
    i = await Wc(r.marketplaceRoot);
  return {
    localMarketplaceRoot: i,
    marketplace: r.marketplace,
    marketplaceWriteCounts: r.marketplaceWriteCounts,
    marketplaceRoot: n.or({ localPath: i, platformFamily: e.platformFamily }),
  };
}
async function kc(e) {
  let t = [e.marketplaceName, n.As]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((t) => [
      {
        marketplaceName: t,
        marketplaceRoot: (0, u.join)(e.resourcesPath, ...sc, t),
      },
      {
        marketplaceName: t,
        marketplaceRoot: (0, u.join)(
          e.resourcesPath,
          `app.asar.unpacked`,
          ...sc,
          t,
        ),
      },
    ]);
  return (
    (
      await Promise.all(
        t.map(async ({ marketplaceName: e, marketplaceRoot: t }) =>
          (await pl({ marketplaceName: e, marketplaceRoot: t })) ? t : null,
        ),
      )
    ).find((e) => e != null) ?? null
  );
}
async function Ac(e) {
  if (!(await ml({ marketplaceRoot: e.marketplaceRoot }))) return null;
  let t = await Wc(e.marketplaceRoot);
  return {
    localMarketplaceRoot: t,
    marketplace: await Lc(e.marketplaceRoot),
    marketplaceWriteCounts: null,
    marketplaceRoot: n.or({ localPath: t, platformFamily: e.platformFamily }),
  };
}
async function jc(e) {
  let t;
  try {
    t = await Ic(e.sourceMarketplaceRoot);
  } catch (t) {
    throw (
      Bc({
        error: t,
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.sourceMarketplaceRoot,
        marketplaceRootKind: `bundled_resource`,
      }),
      t
    );
  }
  let r = {
      ...Gc({
        marketplacePluginNames: e.marketplacePluginNames,
        marketplace: t,
      }),
      name: e.marketplaceName,
    },
    i = {
      pluginCountAfterWrite: r.plugins.length,
      pluginCountBeforeWrite: await Rc({
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.targetMarketplaceRoot,
        marketplaceRootKind: `runtime_marketplace`,
      }),
    },
    a = `${e.targetMarketplaceRoot}.staging-${(0, f.randomUUID)()}`,
    o = `cleanup_staging_before_write`;
  try {
    (await zc({
      directory: a,
      directoryKind: `staging_marketplace`,
      marketplaceName: e.marketplaceName,
      marketplaceWriteCounts: i,
      removePhase: `cleanup_staging_before_write`,
    }),
      (o = `create_staging`),
      await m.default.mkdir((0, u.join)(a, ...cc.slice(0, -1)), {
        recursive: !0,
      }),
      (o = `write_manifest`));
    try {
      await m.default.writeFile(
        (0, u.join)(a, ...cc),
        `${JSON.stringify(r, null, 2)}\n`,
        `utf8`,
      );
    } catch (t) {
      throw (
        _c.warning(`plugin_marketplace_manifest_write_failed`, {
          safe: { marketplaceName: e.marketplaceName, ...i },
          sensitive: { error: t, marketplaceRoot: a },
        }),
        t
      );
    }
    ((o = `copy_plugins`),
      await Promise.all(
        r.plugins.map(async (t) => {
          let n = (0, u.resolve)(e.sourceMarketplaceRoot, t.source.path),
            r = (0, u.resolve)(a, t.source.path);
          (_l({ label: `bundled plugin target`, path: r, root: a }),
            await m.default.mkdir((0, u.dirname)(r), { recursive: !0 }),
            await fl(n, r),
            await Do({
              browserSkillVariant: e.browserSkillVariant,
              computerUseSkillVariant: e.computerUseSkillVariant,
              pluginName: t.name,
              pluginRoot: r,
            }));
        }),
      ),
      (o = `stop_chrome_native_host`),
      await n.yn({
        force: !0,
        repointManifestsToPluginCache: {
          codexHome: e.codexHome,
          marketplaceName: e.marketplaceName,
        },
        root: e.targetMarketplaceRoot,
      }),
      (o = `replace_target`),
      await zc({
        directory: e.targetMarketplaceRoot,
        directoryKind: `runtime_marketplace`,
        marketplaceName: e.marketplaceName,
        marketplaceWriteCounts: i,
        removePhase: `replace_target`,
      }),
      (o = `create_target_parent`),
      await m.default.mkdir((0, u.resolve)(e.targetMarketplaceRoot, `..`), {
        recursive: !0,
      }),
      (o = `rename_staging`),
      await m.default.rename(a, e.targetMarketplaceRoot));
  } catch (t) {
    throw (
      _c.warning(`plugin_marketplace_folder_write_failed`, {
        safe: { folderWritePhase: o, marketplaceName: e.marketplaceName, ...i },
        sensitive: {
          error: t,
          sourceMarketplaceRoot: e.sourceMarketplaceRoot,
          targetMarketplaceRoot: e.targetMarketplaceRoot,
        },
      }),
      t
    );
  } finally {
    await zc({
      directory: a,
      directoryKind: `staging_marketplace`,
      marketplaceName: e.marketplaceName,
      marketplaceWriteCounts: i,
      removePhase: `cleanup_staging_after_write`,
    });
  }
  return (
    _c.info(`plugin_marketplace_folder_write_succeeded`, {
      safe: { marketplaceName: e.marketplaceName, ...i },
      sensitive: {
        sourceMarketplaceRoot: e.sourceMarketplaceRoot,
        targetMarketplaceRoot: e.targetMarketplaceRoot,
      },
    }),
    _c.info(`bundled_plugins_runtime_marketplace_written`, {
      safe: {
        pluginCount: r.plugins.length,
        pluginNames: r.plugins.map((e) => e.name),
      },
      sensitive: { targetMarketplaceRoot: e.targetMarketplaceRoot },
    }),
    {
      localMarketplaceRoot: e.targetMarketplaceRoot,
      marketplace: r,
      marketplaceWriteCounts: i,
      marketplaceRoot: e.targetMarketplaceRoot,
    }
  );
}
async function Mc(e) {
  let t = n.mn(e.bundledPlugin.name);
  if (t == null) return;
  let r = Nc({
      bundledPlugin: e.bundledPlugin,
      localMarketplaceRoot: e.localMarketplaceRoot,
    }),
    i = ol({
      bundledPluginName: e.bundledPlugin.name,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      executionHostPath: e.executionHostPath,
    }),
    [a, o] = await Promise.all([Fc(r), Pc(r)]);
  (await n._n({
    latestRoot: i,
    versionRoot: al({
      bundledPluginName: e.bundledPlugin.name,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      executionHostPath: e.executionHostPath,
      version: o.version,
    }),
    waitForVersionRoot: !0,
  }),
    await n.bn({
      codexHome: e.codexHome,
      devRuntimeRepoRoot: e.devRuntimeRepoRoot,
      extensionId: a,
      nativeHostName: t,
      pluginRoot: i,
      pluginVersion: o.version,
      resourcesPath: e.resourcesPath,
    }));
}
function Nc(e) {
  let t = (0, u.resolve)(e.localMarketplaceRoot, e.bundledPlugin.source.path);
  return (
    _l({
      label: `bundled plugin source`,
      path: t,
      root: e.localMarketplaceRoot,
    }),
    t
  );
}
async function Pc(e) {
  return hc.parseAsync(
    JSON.parse(
      await m.default.readFile(
        (0, u.join)(e, `.codex-plugin`, `plugin.json`),
        `utf8`,
      ),
    ),
  );
}
async function Fc(e) {
  return gc.parse(
    JSON.parse(await m.default.readFile((0, u.join)(e, ...lc), `utf8`)),
  ).extensionId;
}
async function Ic(e) {
  return pc.parseAsync(
    JSON.parse(await m.default.readFile((0, u.join)(e, ...cc), `utf8`)),
  );
}
async function Lc(e) {
  return mc.parseAsync(
    JSON.parse(await m.default.readFile((0, u.join)(e, ...cc), `utf8`)),
  );
}
async function Rc(e) {
  if (!(0, p.existsSync)((0, u.join)(e.marketplaceRoot, ...cc))) return null;
  try {
    return (await Lc(e.marketplaceRoot)).plugins.length;
  } catch (t) {
    return (
      Bc({
        error: t,
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.marketplaceRoot,
        marketplaceRootKind: e.marketplaceRootKind,
      }),
      null
    );
  }
}
async function zc(e) {
  try {
    await m.default.rm(e.directory, { force: !0, recursive: !0 });
  } catch (t) {
    throw (
      _c.warning(`plugin_marketplace_folder_remove_failed`, {
        safe: {
          directoryKind: e.directoryKind,
          marketplaceName: e.marketplaceName,
          removePhase: e.removePhase,
          ...e.marketplaceWriteCounts,
        },
        sensitive: { directory: e.directory, error: t },
      }),
      t
    );
  }
}
function Bc(e) {
  _c.warning(`plugin_marketplace_manifest_read_failed`, {
    safe: {
      errorCategory: Vc(e.error),
      marketplaceName: e.marketplaceName,
      marketplaceRootKind: e.marketplaceRootKind,
    },
    sensitive: { error: e.error, marketplaceRoot: e.marketplaceRoot },
  });
}
function Vc(e) {
  return e instanceof SyntaxError
    ? `corrupt_json`
    : e instanceof n.Dl
      ? `invalid_schema`
      : `read_failed`;
}
function Hc(e) {
  let t = new Set(
    e.materializedMarketplace.marketplace.plugins.map((e) => e.name),
  );
  _c.info(`plugin_marketplace_sync_completed`, {
    safe: {
      marketplaceName: e.marketplaceName,
      pluginCount: e.materializedMarketplace.marketplace.plugins.length,
      pluginsInstalledAfterWrite: e.installedPluginCountAfterWrite,
      pluginsInstalledBeforeWrite: Uc(e.installedBundledMarketplace, t),
      ...e.materializedMarketplace.marketplaceWriteCounts,
    },
    sensitive: {},
  });
}
function Uc(e, t) {
  return e?.plugins.filter((e) => e.installed && t.has(e.name)).length ?? 0;
}
async function Wc(e) {
  try {
    return await m.default.realpath(e);
  } catch {
    return e;
  }
}
function Gc(e) {
  let t = new Set(e.marketplacePluginNames);
  return {
    ...e.marketplace,
    plugins: e.marketplace.plugins.filter((e) => t.has(e.name)),
  };
}
async function Kc(e) {
  let t = await e.appServerConnection.codexHome(),
    r = n.ar(e.platformFamily),
    i = ll({ marketplaceRoot: e.marketplaceRoot, executionHostPath: r }),
    a =
      e.syncInstallStateWithChromeExtensionPluginNames.size === 0
        ? new Set()
        : await tl({
            chromeExtensionSyncManagedPluginStore:
              e.chromeExtensionSyncManagedPluginStore,
          }),
    o = e.marketplace.plugins.filter((t) => e.reconcilePluginNames.has(t.name)),
    s = new Set(e.marketplace.plugins.map((e) => e.name)),
    c = new Set(
      e.installedBundledMarketplace?.plugins
        .filter((e) => e.installed && s.has(e.name))
        .map((e) => e.name) ?? [],
    ),
    l = o.length,
    u = null,
    d = (
      await Promise.all(
        o.map(async (r, a) => {
          let o = n.mn(r.name);
          if (o == null) return null;
          try {
            return (
              await n.xn({
                codexHome: t,
                devRuntimeRepoRoot: e.devRuntimeRepoRoot,
                nativeHostName: o,
                resourcesPath: e.resourcesPath,
              }),
              null
            );
          } catch (e) {
            return {
              error: e,
              safe: {
                ...qc(r.source.path),
                installPhase: `copy_chrome_plugin_app_server_codex_runtime`,
                pluginCount: l,
                pluginIndex: a,
                pluginName: r.name,
              },
              sensitive: {
                bundledPluginSourcePath: r.source.path,
                marketplacePath: i,
              },
            };
          }
        }),
      )
    ).find((e) => e != null);
  if (d != null)
    return { firstFailure: d, installedPluginCountAfterWrite: c.size };
  for (let [s, d] of o.entries()) {
    let o = `read_bundled_manifest`,
      f,
      p,
      m,
      h = null,
      g = async () => {
        if (h != null) return h;
        let t = vl({
          label: `bundled plugin source`,
          path: d.source.path,
          executionHostPath: r,
          root: e.marketplaceRoot,
        });
        return (
          (h = await ul({
            appServerConnection: e.appServerConnection,
            executionHostPath: r,
            pluginRoot: t,
          })),
          h
        );
      };
    try {
      let s = !1,
        l = e.installedBundledMarketplace?.plugins.find(
          (e) => e.name === d.name,
        ),
        u = e.forceInstallPluginNames.has(d.name),
        h = e.syncInstallStateWithChromeExtensionPluginNames.has(d.name),
        _ = n.mn(d.name) != null,
        v = async () => {
          o = `reconcile_chrome_native_host`;
          let n = Yc(e.resourcesPath, d.name);
          await Mc({
            bundledPlugin: d,
            codexHome: t,
            devRuntimeRepoRoot: e.devRuntimeRepoRoot,
            localMarketplaceRoot: e.localMarketplaceRoot,
            marketplaceName: e.marketplaceName,
            executionHostPath: r,
            resourcesPath: n,
          });
        },
        y = e.installWhenMissingPluginNames.has(d.name);
      if (l != null && l.installed) {
        o = `read_bundled_manifest`;
        let n = await g();
        ((o = `read_installed_manifest`),
          (f = await rl({
            appServerConnection: e.appServerConnection,
            bundledManifest: n,
            bundledPlugin: d,
            codexHome: t,
            installedPlugin: l,
            marketplaceName: e.marketplaceName,
            marketplaceRoot: e.marketplaceRoot,
            executionHostPath: r,
          })));
      } else f = `missing`;
      if (h) {
        o = `read_chrome_extension_config`;
        let r = await Fc(
            Nc({
              bundledPlugin: d,
              localMarketplaceRoot: e.localMarketplaceRoot,
            }),
          ),
          i = e.isChromeExtensionInstalled({ extensionId: r }),
          u = l?.installed === !0,
          p = l != null && a.has(l.id),
          m = i
            ? u
              ? `keep_installed`
              : `install_missing`
            : u
              ? p
                ? `uninstall_managed`
                : `keep_user_managed`
              : `keep_missing`;
        if (
          (_c.info(`bundled_plugin_chrome_extension_sync_decision`, {
            safe: {
              chromeExtensionSyncAction: m,
              extensionId: r,
              installedPluginId: l?.id ?? null,
              installedPluginStatus: f,
              isExtensionInstalled: i,
              isPluginInstalled: u,
              isPluginSyncManaged: p,
              pluginName: d.name,
            },
            sensitive: {},
          }),
          (y = i),
          (s = i && (!u || p)),
          !i && u && p)
        ) {
          (_c.info(`bundled_plugin_uninstall_requested`, {
            safe: {
              pluginId: l.id,
              pluginName: l.name,
              reason: `chrome_extension_uninstalled`,
            },
            sensitive: {},
          }),
            (o = `sync_chrome_extension_state`),
            await e.appServerConnection.uninstallPlugin({ pluginId: l.id }),
            c.delete(d.name),
            _ &&
              ((o = `reconcile_chrome_native_host`),
              await n.vn({
                codexHome: t,
                marketplaceName: e.marketplaceName,
                pluginName: d.name,
              })),
            (o = `write_chrome_extension_sync_marker`),
            await nl({
              chromeExtensionSyncManagedPluginStore:
                e.chromeExtensionSyncManagedPluginStore,
              managed: !1,
              pluginId: l.id,
            }));
          continue;
        }
        !i &&
          u &&
          _c.info(`bundled_plugin_uninstall_skipped_user_managed`, {
            safe: {
              pluginId: l.id,
              pluginName: l.name,
              reason: `chrome_extension_uninstalled`,
            },
            sensitive: {},
          });
      }
      if (
        ((p = Jc({
          forceInstall: u,
          installWhenMissing: y,
          installedPluginStatus: f,
        })),
        p == null)
      ) {
        (_c.info(
          f === `missing`
            ? `bundled_plugin_install_skipped_missing`
            : `bundled_plugin_install_skipped_current`,
          { safe: { pluginName: d.name }, sensitive: {} },
        ),
          f === `missing` &&
            _ &&
            ((o = `reconcile_chrome_native_host`),
            await n.vn({
              codexHome: t,
              marketplaceName: e.marketplaceName,
              pluginName: d.name,
            })),
          f === `current` &&
            _ &&
            (await v(),
            await sl({
              codexHome: t,
              getBundledManifest: g,
              marketplaceName: e.marketplaceName,
              executionHostPath: r,
              pluginName: d.name,
            })));
        continue;
      }
      if (
        ((m = il({
          bundledPluginName: d.name,
          codexHome: t,
          marketplaceName: e.marketplaceName,
          executionHostPath: r,
        })),
        _)
      ) {
        o = `stop_chrome_native_host`;
        try {
          await n.yn({ root: m });
        } catch (e) {
          _c.warning(`bundled_plugin_chrome_native_host_stop_failed`, {
            safe: { pluginName: d.name },
            sensitive: { error: e },
          });
        }
      }
      (_c.info(`bundled_plugin_install_requested`, {
        safe: { ...qc(d.source.path), pluginName: d.name, reason: p },
        sensitive: {
          bundledPluginSourcePath: d.source.path,
          installedPluginCacheRoot: m,
          marketplacePath: i,
        },
      }),
        (o = `install_plugin`),
        await e.appServerConnection.installPlugin({
          marketplacePath: i,
          pluginName: d.name,
        }),
        c.add(d.name),
        _ && (await v()),
        (o = `read_bundled_manifest`),
        await g(),
        _ &&
          ((o = `reconcile_chrome_native_host`),
          await sl({
            codexHome: t,
            getBundledManifest: g,
            marketplaceName: e.marketplaceName,
            executionHostPath: r,
            pluginName: d.name,
          })),
        s &&
          ((o = `write_chrome_extension_sync_marker`),
          await nl({
            chromeExtensionSyncManagedPluginStore:
              e.chromeExtensionSyncManagedPluginStore,
            managed: !0,
            pluginId: `${d.name}@${e.marketplaceName}`,
          })),
        f !== `missing` &&
          l?.enabled === !1 &&
          (_c.info(`bundled_plugin_enabled_config_write_requested`, {
            safe: {
              enabled: !1,
              pluginId: `${d.name}@${e.marketplaceName}`,
              pluginName: d.name,
            },
            sensitive: {},
          }),
          (o = `restore_disabled_config`),
          await el({
            appServerConnection: e.appServerConnection,
            enabled: !1,
            pluginId: `${d.name}@${e.marketplaceName}`,
          })));
    } catch (t) {
      let n = {
        ...qc(d.source.path),
        installPhase: o,
        pluginCount: l,
        pluginIndex: s,
        pluginName: d.name,
      };
      (p != null && (n.installReason = p),
        f != null && (n.installedPluginStatus = f));
      let r = {
        error: t,
        safe: n,
        sensitive: {
          bundledPluginSourcePath: d.source.path,
          installedPluginCacheRoot: m,
          marketplacePath: i,
        },
      };
      (_c.warning(`bundled_plugins_marketplace_install_failed`, {
        safe: {
          errorCategory: Tc({ error: t, platformFamily: e.platformFamily }),
          marketplaceName: e.marketplaceName,
          platformFamily: e.platformFamily,
          ...n,
        },
        sensitive: {
          error: t,
          marketplaceRoot: e.marketplaceRoot,
          ...r.sensitive,
        },
      }),
        (u ??= r));
    }
  }
  return { firstFailure: u, installedPluginCountAfterWrite: c.size };
}
function qc(e) {
  let t = e.split(/[\\/]+/).filter(Boolean);
  return !e.startsWith(`/`) &&
    !e.startsWith(`\\`) &&
    !/^[a-zA-Z]:/.test(e) &&
    !t.includes(`..`)
    ? { bundledPluginSourcePath: e, bundledPluginSourcePathKind: `relative` }
    : { bundledPluginSourcePathKind: `redacted` };
}
function Jc(e) {
  return e.forceInstall && e.installedPluginStatus === `current`
    ? `forced`
    : e.installedPluginStatus === `missing`
      ? e.installWhenMissing
        ? `missing`
        : null
      : e.installedPluginStatus === `current`
        ? null
        : e.installedPluginStatus;
}
function Yc(e, t) {
  if (e == null)
    throw Error(
      `Missing resourcesPath required to sync Chrome native host resources for ${t}.`,
    );
  return e;
}
async function Xc(e) {
  if (e.disabledInstalledPlugins.length === 0) return;
  let t = await e.appServerConnection.codexHome();
  for (let r of e.disabledInstalledPlugins)
    (await n.vn({
      codexHome: t,
      marketplaceName: e.marketplaceName,
      pluginName: r.name,
    }),
      _c.info(`bundled_plugin_uninstall_requested`, {
        safe: {
          pluginId: r.id,
          pluginName: r.name,
          reason: `not_in_bundled_marketplace_plugin_names`,
        },
        sensitive: {},
      }),
      await e.appServerConnection.uninstallPlugin({ pluginId: r.id }),
      e.chromeExtensionSyncManagedPluginStore?.setManagedPluginId(r.id, !1));
}
async function Zc(e) {
  if (e.marketplaceName !== `openai-bundled`) return;
  let t = [];
  try {
    ({ marketplaces: t } = await e.appServerConnection.listPlugins(o.l));
  } catch (e) {
    _c.warning(`stale_bundled_marketplaces_list_failed`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  let n = t.filter((e) => uc.has(e.name));
  for (let t of n)
    try {
      (await e.appServerConnection.sendAppServerRequest(`marketplace/remove`, {
        marketplaceName: t.name,
      }),
        _c.info(`stale_bundled_marketplace_remove_requested`, {
          safe: { marketplaceName: t.name },
          sensitive: {},
        }));
    } catch (e) {
      _c.warning(`stale_bundled_marketplace_remove_failed`, {
        safe: { marketplaceName: t.name },
        sensitive: { error: e },
      });
    }
  try {
    await Qc({ codexHome: await e.appServerConnection.codexHome() });
  } catch (e) {
    _c.warning(`stale_bundled_marketplace_directory_cleanup_failed`, {
      safe: { reason: `codex_home_unavailable` },
      sensitive: { error: e },
    });
  }
}
async function Qc(e) {
  for (let t of o.u)
    (await $c({
      codexHome: e.codexHome,
      directoryKind: `plugin_cache`,
      marketplaceName: t,
      pathSegments: [`plugins`, `cache`, t],
    }),
      await $c({
        codexHome: e.codexHome,
        directoryKind: `runtime_marketplace`,
        marketplaceName: t,
        pathSegments: [`.tmp`, `bundled-marketplaces`, t],
      }));
}
async function $c(e) {
  let t = (0, u.join)(e.codexHome, ...e.pathSegments);
  if ((0, p.existsSync)(t))
    try {
      (await m.default.rm(t, { recursive: !0, force: !0 }),
        _c.info(`stale_bundled_marketplace_directory_removed`, {
          safe: {
            directoryKind: e.directoryKind,
            marketplaceName: e.marketplaceName,
          },
          sensitive: {},
        }));
    } catch (n) {
      (_c.warning(`stale_bundled_marketplace_directory_remove_failed`, {
        safe: {
          directoryKind: e.directoryKind,
          marketplaceName: e.marketplaceName,
        },
        sensitive: { error: n },
      }),
        _c.warning(`plugin_marketplace_folder_remove_failed`, {
          safe: {
            directoryKind: e.directoryKind,
            marketplaceName: e.marketplaceName,
            removePhase: `stale_cleanup`,
          },
          sensitive: { directory: t, error: n },
        }));
    }
}
async function el(e) {
  await e.appServerConnection.sendAppServerRequest(`config/batchWrite`, {
    edits: [
      {
        keyPath: `plugins.${e.pluginId}.enabled`,
        mergeStrategy: `upsert`,
        value: e.enabled,
      },
    ],
    reloadUserConfig: !0,
  });
}
async function tl(e) {
  if (e.chromeExtensionSyncManagedPluginStore == null)
    throw Error(`Missing Chrome extension sync managed plugin store.`);
  let t = e.chromeExtensionSyncManagedPluginStore.getManagedPluginIds();
  return (
    _c.info(`bundled_plugins_chrome_extension_sync_marker_read`, {
      safe: { managedPluginCount: t.size },
      sensitive: {},
    }),
    t
  );
}
async function nl(e) {
  if (e.chromeExtensionSyncManagedPluginStore == null)
    throw Error(`Missing Chrome extension sync managed plugin store.`);
  e.chromeExtensionSyncManagedPluginStore.setManagedPluginId(
    e.pluginId,
    e.managed,
  );
}
async function rl(e) {
  try {
    if (e.installedPlugin.source.type !== `local`) return `unknown`;
    let t = il({
      bundledPluginName: e.bundledPlugin.name,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      executionHostPath: e.executionHostPath,
    });
    if (
      yl({
        path: e.installedPlugin.source.path,
        executionHostPath: e.executionHostPath,
        root: t,
      })
    ) {
      let t = await ul({
        appServerConnection: e.appServerConnection,
        executionHostPath: e.executionHostPath,
        pluginRoot: e.installedPlugin.source.path,
      });
      return cl({ bundledManifest: e.bundledManifest, installedManifest: t });
    }
    if (
      yl({
        path: e.installedPlugin.source.path,
        executionHostPath: e.executionHostPath,
        root: e.marketplaceRoot,
      })
    ) {
      let t = await ul({
        appServerConnection: e.appServerConnection,
        executionHostPath: e.executionHostPath,
        pluginRoot: al({
          bundledPluginName: e.bundledPlugin.name,
          codexHome: e.codexHome,
          marketplaceName: e.marketplaceName,
          executionHostPath: e.executionHostPath,
          version: e.bundledManifest.version,
        }),
      });
      return cl({ bundledManifest: e.bundledManifest, installedManifest: t });
    }
    let n = await ul({
      appServerConnection: e.appServerConnection,
      executionHostPath: e.executionHostPath,
      pluginRoot: e.installedPlugin.source.path,
    });
    return cl({ bundledManifest: e.bundledManifest, installedManifest: n });
  } catch {
    return `unknown`;
  }
}
function il(e) {
  return vl({
    label: `bundled plugin cache`,
    path: e.bundledPluginName,
    executionHostPath: e.executionHostPath,
    root: e.executionHostPath.join(
      e.codexHome,
      `plugins`,
      `cache`,
      e.marketplaceName,
    ),
  });
}
function al(e) {
  return vl({
    label: `bundled plugin cache version`,
    path: e.version,
    executionHostPath: e.executionHostPath,
    root: il(e),
  });
}
function ol(e) {
  return vl({
    label: `bundled plugin latest cache`,
    path: `latest`,
    executionHostPath: e.executionHostPath,
    root: il(e),
  });
}
async function sl(e) {
  if (n.mn(e.pluginName) == null) return;
  let t = await e.getBundledManifest();
  await n._n({
    latestRoot: ol({
      bundledPluginName: e.pluginName,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      executionHostPath: e.executionHostPath,
    }),
    versionRoot: al({
      bundledPluginName: e.pluginName,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      executionHostPath: e.executionHostPath,
      version: t.version,
    }),
    waitForVersionRoot: !1,
  });
}
function cl(e) {
  return e.bundledManifest.name === e.installedManifest.name &&
    e.bundledManifest.bundledContentVariant ===
      e.installedManifest.bundledContentVariant &&
    e.bundledManifest.version === e.installedManifest.version
    ? `current`
    : `outdated`;
}
function ll(e) {
  return e.executionHostPath.join(e.marketplaceRoot, ...cc);
}
async function ul(e) {
  return hc.parseAsync(
    JSON.parse(
      await dl({
        appServerConnection: e.appServerConnection,
        path: e.executionHostPath.join(
          e.pluginRoot,
          `.codex-plugin`,
          `plugin.json`,
        ),
      }),
    ),
  );
}
async function dl(e) {
  return new Response(await e.appServerConnection.readFile(e.path)).text();
}
async function fl(e, t) {
  if (g.default.platform === `darwin`) {
    await vc(`ditto`, [`--noqtn`, e, t]);
    return;
  }
  if (g.default.platform !== `win32`) {
    await m.default.cp(e, t, { recursive: !0, verbatimSymlinks: !0 });
    return;
  }
  let { copyDirectoryAllowDecryptedDestinationOnEncryptionFailure: n } =
    await Promise.resolve().then(() =>
      require(`./windows-file-copy-BOzaG6X5.js`),
    );
  await n({
    copy: () => m.default.cp(e, t, { recursive: !0, verbatimSymlinks: !0 }),
    destination: t,
    source: e,
  });
}
async function pl(e) {
  return hl({
    marketplaceName: e.marketplaceName,
    marketplaceRoot: e.marketplaceRoot,
    marketplaceRootKind: `bundled_resource`,
    marketplaceSchema: pc,
  });
}
async function ml(e) {
  return hl({ marketplaceRoot: e.marketplaceRoot, marketplaceSchema: mc });
}
async function hl(e) {
  let t = (0, u.join)(e.marketplaceRoot, ...cc);
  if (!(0, p.existsSync)(t)) return !1;
  try {
    let n = await e.marketplaceSchema.parseAsync(
      JSON.parse(await m.default.readFile(t, `utf8`)),
    );
    return (
      await Promise.all(
        n.plugins.map(async (t) =>
          gl({
            pluginName: t.name,
            pluginRoot: (0, u.resolve)(e.marketplaceRoot, t.source.path),
          }),
        ),
      )
    ).every((e) => e);
  } catch (t) {
    return (
      e.marketplaceName != null &&
        e.marketplaceRootKind != null &&
        Bc({
          error: t,
          marketplaceName: e.marketplaceName,
          marketplaceRoot: e.marketplaceRoot,
          marketplaceRootKind: e.marketplaceRootKind,
        }),
      !1
    );
  }
}
async function gl(e) {
  let t = (0, u.join)(e.pluginRoot, `.codex-plugin`, `plugin.json`);
  if (!(0, p.existsSync)(t)) return !1;
  try {
    return (
      (await hc.parseAsync(JSON.parse(await m.default.readFile(t, `utf8`))))
        .name === e.pluginName
    );
  } catch {
    return !1;
  }
}
function _l(e) {
  let t = (0, u.resolve)(e.root),
    n = (0, u.resolve)(e.path);
  if (n !== t && !n.startsWith(`${t}${u.sep}`))
    throw Error(`${e.label} path escapes marketplace root: ${n}`);
}
function vl(e) {
  let t = e.executionHostPath.resolve(e.root),
    n = e.executionHostPath.resolve(t, e.path);
  if (!xl({ path: n, executionHostPath: e.executionHostPath, root: t }))
    throw Error(`${e.label} path escapes marketplace root: ${n}`);
  return n;
}
function yl(e) {
  return xl({
    path: bl(e.path, e.executionHostPath),
    executionHostPath: e.executionHostPath,
    root: bl(e.root, e.executionHostPath),
  });
}
function bl(e, t) {
  let n = t.resolve(e);
  return t === u.win32 ? t.toNamespacedPath(n).toLowerCase() : n;
}
function xl(e) {
  let t = e.root.endsWith(e.executionHostPath.sep)
    ? e.root
    : `${e.root}${e.executionHostPath.sep}`;
  return e.path === e.root || e.path.startsWith(t);
}
var Sl = `CODEX_ELECTRON_BUNDLED_PLUGINS_RESOURCES_PATH`,
  Cl = `CODEX_ELECTRON_BUNDLED_PLUGINS_FORCE_RELOAD`;
function wl(e) {
  return (
    (e?.env ?? process.env)[Sl]?.trim() ||
    e?.resourcesPath ||
    process.resourcesPath
  );
}
function Tl(e) {
  return (0, u.join)(
    e.codexHome,
    `.tmp`,
    `bundled-marketplaces`,
    e.marketplaceName ?? `openai-bundled`,
  );
}
function El(e) {
  let t = (e?.env ?? process.env)[Cl]?.trim();
  return t
    ? new Set(
        t
          .split(/[,\s]+/)
          .map((e) => e.trim())
          .filter((e) => e.length > 0),
      )
    : new Set();
}
var Dl = r.a(`bundled-plugins`),
  Ol = `CODEX_ENABLE_DEV_BUNDLED_PLUGINS`,
  kl = new Set([n.Ss, o.s, n.Cs, o.c, n.ws]);
function Al(e) {
  let t = e.env ?? process.env,
    r = e.resourcesPath ?? wl({ env: t }),
    s = n.Ms(e.buildFlavor),
    c =
      e.runtimeMarketplaceRoot ??
      Tl({ codexHome: e.codexHome, marketplaceName: s }),
    l = t.NODE_ENV === `development` ? e.repoRoot : void 0,
    u = El({ env: t }),
    d = {
      getManagedPluginIds: () =>
        new Set(
          e.globalState.get(
            `electron-chrome-extension-sync-managed-plugin-ids`,
          ) ?? [],
        ),
      setManagedPluginId: (t, n) => {
        let r = new Set(
          e.globalState.get(
            `electron-chrome-extension-sync-managed-plugin-ids`,
          ) ?? [],
        );
        (n ? r.add(t) : r.delete(t),
          e.globalState.set(a._, r.size === 0 ? void 0 : [...r].sort()));
      },
    },
    f = e.platform ?? process.platform;
  if (e.buildFlavor === i.a.Dev && t[Ol]?.trim() !== `1`) {
    let t = null,
      n = Promise.resolve(),
      i = null;
    return {
      reconcileExternalPluginState: async () => {},
      reloadBundledPlugins: async () => {},
      setDesktopFeatureAvailability: (a) => {
        if (t == null) {
          if (!qe(a)) return n;
          t = a;
        } else t = { ...t, ...a };
        let o = t,
          s = JSON.stringify({
            computerUse: o.computerUse,
            computerUseNodeRepl: o.computerUseNodeRepl,
            externalBrowserUse: o.externalBrowserUse,
            inAppBrowserUse: o.inAppBrowserUse,
          });
        return i === s
          ? n
          : ((i = s),
            (n = n
              .catch(() => {})
              .then(async () => {
                try {
                  await vo({
                    appServerConnection: e.getLocalAppServerConnection(),
                    appVersion: e.appVersion,
                    desktopFeatureAvailability: o,
                    isPackaged: e.isPackaged,
                    platform: f,
                    repoRoot: e.repoRoot,
                    resourcesPath: r,
                  });
                } catch (e) {
                  throw (i === s && (i = null), e);
                }
              })
              .catch(() => {})),
            n);
      },
      waitForPendingReconcile: () => n,
    };
  }
  let p = null,
    m = { browser: `unknown`, chrome: `unknown`, computerUse: `unknown` },
    h = null,
    g = null,
    _ = null,
    v = Promise.resolve(),
    y = !1,
    b = ``,
    x,
    S,
    C = new Set(),
    w = (n) =>
      bs.filter((r) =>
        r.isAvailable({
          buildFlavor: e.buildFlavor,
          env: t,
          features: n,
          platform: f,
        }),
      ),
    T = async (t) => {
      if (e.onInstallStateObserved == null) return async () => {};
      let i = e.getLocalAppServerConnection(),
        a,
        o;
      try {
        [a, o] = await Promise.all([
          Nl({ appServerConnection: i, marketplaceName: s }),
          Sc({
            marketplacePluginNames: D(t.marketplacePluginDescriptors),
            marketplaceName: s,
            resourcesPath: r,
          }),
        ]);
      } catch (e) {
        return (
          Dl().warning(`bundled_plugin_install_state_observation_failed`, {
            safe: { phase: `before`, reason: t.reason },
            sensitive: { error: e },
          }),
          async () => {}
        );
      }
      let c = d.getManagedPluginIds();
      return async () => {
        try {
          let r = await Nl({ appServerConnection: i, marketplaceName: s }),
            l = t.marketplacePluginDescriptors
              .map((e) => e.name)
              .filter((e) => kl.has(e)),
            u = new Set([
              ...l,
              ...[...a].filter(([, e]) => e.installed).map(([e]) => e),
            ]),
            d = new Map(o?.map((e) => [e.pluginName, e.isExtensionInstalled]));
          for (let i of u) {
            let o = a.get(i);
            e.onInstallStateObserved?.(
              xs({
                autoInstallWhenMissing:
                  t.installWhenMissingPluginNames.includes(i),
                chromeExtensionInstalled: d.get(i) ?? null,
                chromeExtensionSyncManaged: o != null && c.has(o.id),
                eligibilityReason:
                  i === n.Ss
                    ? t.eligibilityReasons.browser
                    : i === `computer-use`
                      ? t.eligibilityReasons.computerUse
                      : t.eligibilityReasons.chrome,
                installedAfter: r.get(i)?.installed === !0,
                installedBefore: o?.installed === !0,
                pluginName: i,
                reconcileReason: t.reason,
              }),
            );
          }
        } catch (e) {
          Dl().warning(`bundled_plugin_install_state_observation_failed`, {
            safe: { phase: `after`, reason: t.reason },
            sensitive: { error: e },
          });
        }
      };
    },
    E = ({ force: e, reason: t }) => {
      if (p == null)
        return (
          Dl().info(`bundled_plugins_reconcile_skipped_features_unavailable`, {
            safe: { reason: t },
            sensitive: {},
          }),
          v
        );
      let n = p,
        r = m,
        i = w(n),
        a = i.map((e) => e.name),
        o = jl({ desktopFeatureAvailability: n, marketplacePluginNames: a }),
        s = Ml({
          desktopFeatureAvailability: n,
          marketplacePluginNames: a,
          platform: f,
        }),
        c = JSON.stringify({
          browserSkillVariant: o,
          computerUseSkillVariant: s,
          browserUseConfig: {
            computerUse: n.computerUse,
            computerUseNodeRepl: n.computerUseNodeRepl,
            externalBrowserUse: n.externalBrowserUse,
            inAppBrowserUse: n.inAppBrowserUse,
          },
          marketplacePluginNames: a,
        });
      if (!e && h === c) return v;
      h = c;
      let l = i.some((e) => e.syncInstallStateWithChromeExtension === !0),
        u = y;
      return (
        (v = v
          .catch(() => {})
          .then(async () => {
            ((y = !0),
              Dl().info(`bundled_plugins_reconcile_started`, {
                safe: { hasExternalPluginStateSync: l, queued: u, reason: t },
                sensitive: {},
              }));
            try {
              (await j({
                desktopFeatureAvailability: n,
                eligibilityReasons: r,
                forceInstall: e,
                marketplacePluginDescriptors: i,
                marketplacePluginNames: a,
                reason: t,
              }),
                Dl().info(`bundled_plugins_reconcile_completed`, {
                  safe: { hasExternalPluginStateSync: l, reason: t },
                  sensitive: {},
                }));
            } catch (e) {
              throw (
                Dl().warning(`bundled_plugins_reconcile_failed`, {
                  safe: { hasExternalPluginStateSync: l, reason: t },
                  sensitive: { error: e },
                }),
                e
              );
            } finally {
              y = !1;
            }
          })
          .catch(() => {})),
        v
      );
    },
    D = (e) =>
      e
        .filter((e) => e.syncInstallStateWithChromeExtension === !0)
        .map((e) => e.name),
    O = async (e) =>
      xc({
        marketplacePluginNames: D(e),
        marketplaceName: s,
        resourcesPath: r,
      }),
    k = async (t) => {
      if (p == null)
        return (
          Dl().info(`bundled_plugins_reconcile_skipped_features_unavailable`, {
            safe: { reason: t },
            sensitive: {},
          }),
          v
        );
      let n = w(p),
        i = m,
        a = n.map((e) => e.name),
        o = jl({ desktopFeatureAvailability: p, marketplacePluginNames: a }),
        u = Ml({
          desktopFeatureAvailability: p,
          marketplacePluginNames: a,
          platform: f,
        }),
        h = D(n),
        b = n.filter((e) => e.syncInstallStateWithChromeExtension === !0),
        x = await O(n);
      if (x == null || x === g || x === _) return v;
      _ = x;
      let S = y;
      return (
        (v = v
          .catch(() => {})
          .then(async () => {
            ((y = !0),
              Dl().info(`bundled_plugins_reconcile_started`, {
                safe: { hasExternalPluginStateSync: !0, queued: S, reason: t },
                sensitive: {},
              }));
            try {
              let n = await T({
                eligibilityReasons: i,
                installWhenMissingPluginNames: [],
                marketplacePluginDescriptors: b,
                reason: t,
              });
              try {
                await bc({
                  appServerConnection: e.getLocalAppServerConnection(),
                  browserSkillVariant: o,
                  computerUseSkillVariant: u,
                  chromeExtensionSyncManagedPluginStore: d,
                  devRuntimeRepoRoot: l,
                  marketplacePluginNames: a,
                  throwOnReconcileFailure: !0,
                  forceInstallPluginNames: [],
                  installWhenMissingPluginNames: [],
                  reconcilePluginNames: h,
                  syncInstallStateWithChromeExtensionPluginNames: h,
                  marketplaceName: s,
                  resourcesPath: r,
                  runtimeMarketplaceRoot: c,
                });
              } finally {
                await n();
              }
              ((g = x),
                e.onReconcileComplete?.(),
                Dl().info(`bundled_plugins_reconcile_completed`, {
                  safe: { hasExternalPluginStateSync: !0, reason: t },
                  sensitive: {},
                }));
            } catch (e) {
              throw (
                Dl().warning(`bundled_plugins_reconcile_failed`, {
                  safe: { hasExternalPluginStateSync: !0, reason: t },
                  sensitive: { error: e },
                }),
                e
              );
            } finally {
              (_ === x && (_ = null), (y = !1));
            }
          })
          .catch(() => {})),
        v
      );
    },
    A = async (t, n) => {
      let r = n.flatMap((e) => {
        let t = e.autoInstallOptOutKey;
        return e.installWhenMissingRequiresOptIn === !0 &&
          t != null &&
          !C.has(t)
          ? [[e.name, t]]
          : [];
      });
      try {
        let n = r.some(([, t]) => e.globalState.get(t) == null)
          ? (await t.listPlugins(o.l)).marketplaces
          : [];
        for (let [t, i] of r)
          (e.globalState.get(i) == null &&
            rn({ marketplaceName: s, marketplaces: n, pluginName: t })?.plugin
              .installed === !0 &&
            e.globalState.set(i, !1),
            C.add(i));
      } catch (e) {
        Dl().warning(
          `bundled_plugins_auto_install_preference_initialize_failed`,
          { safe: {}, sensitive: { error: e } },
        );
      }
    },
    j = async (t) => {
      let n = e.getLocalAppServerConnection();
      await A(n, t.marketplacePluginDescriptors);
      let a = t.marketplacePluginDescriptors
          .filter((e) => t.forceInstall || u.has(e.name))
          .map((e) => e.name),
        o = t.marketplacePluginDescriptors
          .filter((e) => e.installWhenMissing === !0)
          .filter((t) => {
            if (t.autoInstallOptOutKey == null) return !0;
            let n = e.globalState.get(t.autoInstallOptOutKey);
            return t.installWhenMissingRequiresOptIn === !0
              ? n === !1
              : n !== !0;
          })
          .map((e) => e.name),
        p = t.marketplacePluginDescriptors
          .filter((e) => e.syncInstallStateWithChromeExtension === !0)
          .map((e) => e.name),
        m = jl({
          desktopFeatureAvailability: t.desktopFeatureAvailability,
          marketplacePluginNames: t.marketplacePluginNames,
        }),
        h = t.marketplacePluginNames.filter(Eo).join(`,`),
        _ = Ml({
          desktopFeatureAvailability: t.desktopFeatureAvailability,
          marketplacePluginNames: t.marketplacePluginNames,
          platform: f,
        });
      switch (e.buildFlavor) {
        case i.a.Agent:
        case i.a.Dev:
        case i.a.InternalAlpha:
        case i.a.Nightly:
        case i.a.PublicBeta:
          await Zc({ appServerConnection: n, marketplaceName: s });
          break;
        case i.a.Prod:
          break;
      }
      let v = await T({
          eligibilityReasons: t.eligibilityReasons,
          installWhenMissingPluginNames: o,
          marketplacePluginDescriptors: t.marketplacePluginDescriptors,
          reason: t.reason,
        }),
        y;
      try {
        ((y = await bc({
          appServerConnection: n,
          browserSkillVariant: m,
          computerUseSkillVariant: _,
          chromeExtensionSyncManagedPluginStore: d,
          devRuntimeRepoRoot: l,
          marketplacePluginNames: t.marketplacePluginNames,
          forceInstallPluginNames: a,
          installWhenMissingPluginNames: o,
          syncInstallStateWithChromeExtensionPluginNames: p,
          marketplaceName: s,
          resourcesPath: r,
          runtimeMarketplaceRoot: c,
        })),
          await Promise.all(
            t.marketplacePluginDescriptors.map(async (t) => {
              t.migrate != null &&
                (await t.migrate({
                  appServerConnection: n,
                  codexHome: e.codexHome,
                  marketplaceName: s,
                  trashItem: e.trashItem,
                }));
            }),
          ));
      } finally {
        await v();
      }
      (await vo({
        appServerConnection: n,
        appVersion: e.appVersion,
        desktopFeatureAvailability: t.desktopFeatureAvailability,
        isPackaged: e.isPackaged,
        platform: f,
        repoRoot: e.repoRoot,
        resourcesPath: r,
      }),
        y.hadReconcileFailure || (g = await O(t.marketplacePluginDescriptors)),
        (h !== b || m !== x || _ !== S) && e.onSkillsMayHaveChanged?.(),
        (b = h),
        (x = m),
        (S = _),
        e.onReconcileComplete?.());
    };
  return {
    reconcileExternalPluginState: (e = `focus`) => k(e),
    reloadBundledPlugins: () => E({ force: !0, reason: `debug-reload` }),
    setDesktopFeatureAvailability: (e, t) => {
      if ((t != null && (m = t), p == null)) {
        if (!qe(e)) return v;
        p = e;
      } else p = { ...p, ...e };
      return E({ force: !1, reason: `startup` });
    },
    waitForPendingReconcile: () => v,
  };
}
function jl(e) {
  if (e.marketplacePluginNames.some(Eo))
    return e.desktopFeatureAvailability.multiBrowserTabs &&
      e.desktopFeatureAvailability.unifiedBrowserSkill
      ? `unified`
      : `backend-specific`;
}
function Ml(e) {
  if (
    !(
      e.platform !== `darwin` ||
      !e.marketplacePluginNames.includes(`computer-use`)
    )
  )
    return e.desktopFeatureAvailability.computerUseNodeRepl
      ? `node-repl`
      : `legacy-mcp`;
}
async function Nl(e) {
  let { marketplaces: t } = await e.appServerConnection.listPlugins(o.l);
  return new Map(
    [...kl].flatMap((n) => {
      let r = rn({
        marketplaceName: e.marketplaceName,
        marketplaces: t,
        pluginName: n,
      })?.plugin;
      return r == null ? [] : [[n, r]];
    }),
  );
}
var Pl = r.a(`codex-app-config`),
  aee = `codex-app`,
  oee = `config.json`,
  Fl = 1,
  see = n.xl({ version: n.bl().int().optional() }),
  cee = n
    .xl({ remotePath: n.wl().trim().min(1), label: n.wl().optional() })
    .strict(),
  lee = n
    .xl({ sshAlias: n.wl().trim().min(1), projects: n.pl(cee).default([]) })
    .strict(),
  uee = n
    .xl({
      version: n.vl(Fl).optional().default(1),
      remoteConnectionMaxRetryAttempts: n.bl().int().nonnegative().optional(),
      sshConnectTimeoutSeconds: n.bl().int().nonnegative().optional(),
      remoteConnections: n.pl(lee).default([]),
    })
    .strict();
function Il(e) {
  return (0, u.join)(e, aee, oee);
}
async function dee(e) {
  let t = Il(e);
  if (!(0, p.existsSync)(t)) return { status: `missing`, configPath: t };
  let n;
  try {
    n = await (0, m.readFile)(t, `utf8`);
  } catch (e) {
    return (
      Pl().warning(`failed_to_read_config`, {
        safe: {},
        sensitive: { configPath: t, error: e },
      }),
      { status: `read-error`, configPath: t, error: e }
    );
  }
  let r;
  try {
    r = JSON.parse(n);
  } catch (e) {
    return (
      Pl().warning(`failed_to_parse_config`, {
        safe: {},
        sensitive: { configPath: t, error: e },
      }),
      { status: `invalid-json`, configPath: t, error: e }
    );
  }
  let i = see.safeParse(r);
  if (i.success && i.data.version != null && i.data.version !== Fl)
    return (
      Pl().warning(`unsupported_config_version`, {
        safe: { version: i.data.version, supportedVersion: Fl },
        sensitive: { configPath: t },
      }),
      {
        status: `unsupported-version`,
        configPath: t,
        version: i.data.version,
        supportedVersion: Fl,
      }
    );
  let a = uee.safeParse(r);
  return a.success
    ? { status: `loaded`, configPath: t, config: a.data }
    : (Pl().warning(`invalid_config`, {
        safe: {},
        sensitive: { configPath: t, error: a.error },
      }),
      { status: `invalid-config`, configPath: t, error: a.error });
}
async function Ll(e, t) {
  let n = fee(t);
  n != null &&
    (
      await c.dialog.showMessageBox(e, {
        type: `error`,
        buttons: [`Copy details`, `OK`],
        defaultId: 1,
        cancelId: 1,
        noLink: !0,
        message: n.message,
        detail: n.detail,
      })
    ).response === 0 &&
    c.clipboard.writeText(n.detail);
}
function fee(e) {
  switch (e.status) {
    case `applied`: {
      let { missingSshAliases: t } = e.result;
      return t.length === 0
        ? null
        : {
            message: `ChatGPT couldn't finish remote setup`,
            detail: [
              `Your remote workspace was not set up completely.`,
              ``,
              `Please copy these details and send them to your administrator.`,
              ``,
              `Details:`,
              `ChatGPT could not find these SSH aliases after checking SSH config:`,
              ``,
              ...t.map((e) => `- ${e}`),
              ``,
              `Config file: ${e.configPath}`,
              ``,
              `Ask your administrator to confirm that the setup command completed and that these SSH aliases exist.`,
            ].join(`
`),
          };
    }
    case `missing`:
      return {
        message: `ChatGPT couldn't find remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `ChatGPT could not find the setup config file.`,
          ``,
          `Expected file: ${e.configPath}`,
        ].join(`
`),
      };
    case `read-error`:
      return {
        message: `ChatGPT couldn't read remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `ChatGPT could not read the setup config file.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
    case `invalid-json`:
      return {
        message: `ChatGPT couldn't read remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `The setup config file is not valid JSON.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
    case `unsupported-version`:
      return {
        message: `ChatGPT couldn't apply remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `Setup config version ${e.version} is not supported by this version of ChatGPT.`,
          ``,
          `Supported version: ${e.supportedVersion}`,
          `Config file: ${e.configPath}`,
        ].join(`
`),
      };
    case `invalid-config`:
      return {
        message: `ChatGPT couldn't apply remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `The setup config file does not match the format ChatGPT expects.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error.message}`,
        ].join(`
`),
      };
    case `unexpected-error`:
      return {
        message: `ChatGPT couldn't finish remote setup`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `ChatGPT hit an unexpected error while applying the setup config.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
  }
}
var pee = `CODEX_ELECTRON_COMPUTER_USE_APP_PATH`,
  Rl = `Codex Computer Use.app`;
function mee() {
  return hee() ?? gee(_ee());
}
function hee() {
  let e = process.env[pee]?.trim();
  return e ? zl(e) : null;
}
function gee(e) {
  for (let t of e) if ((0, p.existsSync)(t)) return zl(t);
  return null;
}
function _ee() {
  return [n.Ms(i.a.resolve()), n.As]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((e) => [
      u.default.join(wl(), `plugins`, e, `plugins`, n.ws, Rl),
      u.default.join(
        c.app.getAppPath(),
        `.generated`,
        `dev-resources`,
        `plugins`,
        e,
        `plugins`,
        n.ws,
        Rl,
      ),
    ]);
}
function zl(e) {
  try {
    return p.realpathSync.native(e);
  } catch {
    return e;
  }
}
var vee = `computer-use`,
  Bl = `Codex Computer Use.app`,
  Vl = `CODEX_ELECTRON_SKIP_COMPUTER_USE_CANONICAL_REFRESH`,
  Hl = (0, d.promisify)(h.execFile);
function Ul(e) {
  return u.default.join(e, vee, Bl);
}
async function Wl({ sourceServiceAppPath: e, codexHome: t }) {
  if (
    process.platform !== `darwin` ||
    process.env[Vl]?.trim() === `1` ||
    e == null
  )
    return;
  let n = Ul(t);
  (await m.default.mkdir(u.default.dirname(n), { recursive: !0 }),
    await m.default.rm(n, { force: !0, recursive: !0 }),
    await Hl(`ditto`, [`--noqtn`, e, n]));
}
var Gl = class {
  appshotsEnabled = !1;
  nodeReplEnabled = !1;
  disposed = !1;
  servicePid = null;
  pendingOperation = Promise.resolve();
  isExpectedServiceProcess;
  isProcessRunning;
  serviceExecutablePath;
  spawnService;
  constructor(
    e,
    {
      isExpectedServiceProcess: t = Ga,
      isProcessRunning: n = Kl,
      spawnService: r = Wa,
    } = {},
  ) {
    ((this.isExpectedServiceProcess = t),
      (this.isProcessRunning = n),
      (this.serviceExecutablePath = u.default.join(
        e,
        `Contents`,
        `MacOS`,
        `SkyComputerUseService`,
      )),
      (this.spawnService = r));
  }
  setAppshotsEnabled(e) {
    return this.setEnabledFeatures({
      appshotsEnabled: e,
      nodeReplEnabled: this.nodeReplEnabled,
    });
  }
  setEnabledFeatures({ appshotsEnabled: e, nodeReplEnabled: t }) {
    return this.appshotsEnabled === e && this.nodeReplEnabled === t
      ? Promise.resolve()
      : ((this.appshotsEnabled = e),
        (this.nodeReplEnabled = t),
        this.runSerialized(async () => {
          this.disposed ||
            (this.enabled && (await this.ensureServicePidForEnabledFeatures()));
        }));
  }
  ensureServicePid() {
    return this.runSerialized(async () => {
      if (this.disposed || !this.enabled)
        throw Error(`Managed Computer Use service is not enabled`);
      return this.ensureServicePidForEnabledFeatures();
    });
  }
  invalidateServicePid(e) {
    return this.runSerialized(async () => {
      this.disposed ||
        !this.appshotsEnabled ||
        (this.servicePid === e &&
          ((this.isProcessRunning(e) &&
            this.isExpectedServiceProcess(e, this.serviceExecutablePath)) ||
            (this.servicePid = null)));
    });
  }
  dispose() {
    ((this.disposed = !0),
      (this.appshotsEnabled = !1),
      (this.nodeReplEnabled = !1));
  }
  runSerialized(e) {
    let t = this.pendingOperation.then(e, e);
    return (
      (this.pendingOperation = t.then(
        () => {},
        () => {},
      )),
      t
    );
  }
  get enabled() {
    return this.appshotsEnabled || this.nodeReplEnabled;
  }
  async ensureServicePidForEnabledFeatures() {
    let e = this.servicePid;
    if (
      e != null &&
      this.isProcessRunning(e) &&
      this.isExpectedServiceProcess(e, this.serviceExecutablePath)
    )
      return e;
    this.servicePid = null;
    let t = await this.spawnService(this.serviceExecutablePath);
    if (t == null || !Number.isSafeInteger(t) || t <= 0)
      throw Error(`Failed to spawn managed Computer Use service`);
    return ((this.servicePid = t), t);
  }
};
function Kl(e, t = process.platform) {
  try {
    process.kill(e, 0);
  } catch (e) {
    return !(typeof e == `object` && e && `code` in e && e.code === `ESRCH`);
  }
  if (t !== `darwin`) return !0;
  try {
    let t = (0, h.execFileSync)(`ps`, [`-o`, `state=`, `-p`, String(e)], {
      encoding: `utf8`,
      stdio: [`ignore`, `pipe`, `ignore`],
    }).trim();
    return t !== `` && !t.startsWith(`Z`);
  } catch {
    return !0;
  }
}
function ql({
  appServerConnection: e,
  closeActiveTurn: t,
  completePIPThread: n = so,
  invalidatePIPTurn: r = co,
}) {
  return e.registerInternalNotificationHandler((e) => {
    Jl(e) &&
      (e.params.turn.status === `completed`
        ? n(e.params.threadId)
        : r(e.params.threadId, e.params.turn.id),
      t({ sessionId: e.params.threadId, turnId: e.params.turn.id }));
  });
}
function Jl(e) {
  return (
    e.method === `turn/completed` &&
    typeof e.params?.threadId == `string` &&
    typeof e.params.turn == `object` &&
    e.params.turn != null &&
    `id` in e.params.turn &&
    typeof e.params.turn.id == `string`
  );
}
var Yl = `codex_desktop:message-from-view`,
  B = `codex_desktop:message-for-view`,
  Xl = `codex_desktop:browser-sidebar-runtime-message`,
  Zl = `codex_desktop:mcp-app-sandbox-guest-message`,
  Ql = `codex_desktop:mcp-app-sandbox-host-message`,
  $l = `codex_desktop:show-context-menu`,
  eu = `codex_desktop:show-application-menu`,
  tu = `codex_desktop:get-sentry-init-options`,
  nu = `codex_desktop:get-build-flavor`,
  ru = `codex_desktop:get-uses-owl-app-shell`,
  iu = `codex_desktop:get-system-theme-variant`,
  au = `codex_desktop:get-shared-object-snapshot`,
  ou = `codex_desktop:get-fast-mode-rollout-metrics`,
  su = `codex_desktop:system-theme-variant-updated`,
  cu = `codex_desktop:trigger-sentry-test`,
  lu = `codex_desktop:connect-app-host`,
  uu = `codex_desktop:start-file-drag`,
  du = `icons`,
  fu = `https://chatgpt.com/backend-api`,
  pu = `http://localhost:8000/api`,
  mu = `Codex Desktop`,
  hu = `codex_desktop`;
function gu(e) {
  return `codex_desktop:worker:${e}:from-view`;
}
function _u(e) {
  return `codex_desktop:worker:${e}:for-view`;
}
var vu = [
    {
      displayName: `CSV`,
      extension: `csv`,
      mimeType: `text/csv`,
      macOSContentType: `public.comma-separated-values-text`,
      windowsAssociation: `open-with-only`,
    },
    {
      displayName: `TSV`,
      extension: `tsv`,
      mimeType: `text/tab-separated-values`,
      macOSContentType: `public.tab-separated-values-text`,
      windowsAssociation: `open-with-only`,
    },
    {
      displayName: `XLS`,
      extension: `xls`,
      mimeType: `application/vnd.ms-excel`,
      windowsAssociation: `open-with-only`,
    },
    {
      displayName: `XLSM`,
      extension: `xlsm`,
      mimeType: `application/vnd.ms-excel.sheet.macroEnabled.12`,
      windowsAssociation: `open-with-only`,
    },
    {
      displayName: `XLSX`,
      extension: `xlsx`,
      mimeType: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`,
      windowsAssociation: `open-with-only`,
    },
    {
      advertiseOnLinux: !1,
      displayName: `Codex Skill`,
      extension: `skill`,
      macOSContentType: `com.openai.codex.skill`,
      macOSHandlerRank: `Owner`,
      mimeType: `application/vnd.openai.codex.skill`,
      windowsAssociation: `default-eligible`,
    },
  ],
  yu = vu.map(({ extension: e }) => `.${e}`);
vu.flatMap(({ advertiseOnLinux: e, mimeType: t }) => (e === !1 ? [] : [t]));
var bu = new Set(yu);
function xu(e) {
  return bu.has(u.extname(e).toLowerCase());
}
function Su(e, t = process.platform) {
  let n = [];
  for (let r = 1; r < e.length; r += 1) {
    let i = e[r];
    if (i == null || i.length === 0) continue;
    if (i === `--open-project`) {
      r += 1;
      continue;
    }
    if (i.startsWith(`--open-project=`) || i.startsWith(`-`) || wu(i)) continue;
    let a = Tu(i);
    a == null || !Eu(a, t) || !xu(a) || n.push(a);
  }
  return n;
}
function Cu(e) {
  let t = [],
    n = new Set();
  for (let r of e)
    n.has(r) ||
      (n.add(r), t.push({ label: u.basename(r), path: r, fsPath: r }));
  return t;
}
function wu(e) {
  return e.toLowerCase().startsWith(`codex://`);
}
function Tu(e) {
  if (!e.toLowerCase().startsWith(`file://`)) return e;
  try {
    return (0, s.fileURLToPath)(e);
  } catch {
    return null;
  }
}
function Eu(e, t) {
  return t === `win32` ? u.win32.isAbsolute(e) : u.posix.isAbsolute(e);
}
var Du = `CODEX_ELECTRON_DEV_PARENT_PID`,
  Ou = `CODEX_ELECTRON_DEV_WEBVIEW_PID`,
  ku = 1e3;
function Au(e) {
  let t = e?.trim();
  if (!t) return null;
  let n = Number(t);
  return !Number.isInteger(n) || n <= 0 ? null : n;
}
function ju(e) {
  return Au(e);
}
function Mu(e) {
  return Au(e);
}
function Nu(e) {
  return typeof e == `object` && !!e && `code` in e && e.code === `ESRCH`;
}
function Pu(e, t = (e, t) => process.kill(e, t)) {
  try {
    return (t(e, 0), !0);
  } catch (e) {
    return !Nu(e);
  }
}
function Fu({
  app: e,
  parentPid: t = ju(process.env[Du]),
  pollIntervalMs: n = ku,
  isParentProcessRunning: r = Pu,
  setIntervalImpl: i = setInterval,
  clearIntervalImpl: a = clearInterval,
}) {
  if (t == null) return () => {};
  let o = !1,
    s = () => {
      o || ((o = !0), a(c));
    },
    c = i(() => {
      r(t) || (s(), e.quit());
    }, n);
  return (e.once(`will-quit`, s), s);
}
function Iu({
  app: e,
  webviewPid: t = Mu(process.env[Ou]),
  killImpl: n = (e, t) => process.kill(e, t),
}) {
  if (t == null) return () => {};
  let r = () => {
    try {
      n(-t, `SIGKILL`);
      return;
    } catch {}
    try {
      n(t, `SIGKILL`);
    } catch {}
  };
  return (
    e.once(`will-quit`, r),
    () => {
      e.off(`will-quit`, r);
    }
  );
}
var Lu = `trayMenu.openApp`,
  Ru = `Open {appName}`,
  zu = `trayMenu.newChat`,
  Bu = `New Task`,
  Vu = `trayMenu.pinnedThreads`,
  Hu = `Pinned`,
  Uu = `trayMenu.runningThreads`,
  Wu = `Running`,
  Gu = `trayMenu.recentThreads`,
  Ku = `Recent`,
  qu = `trayMenu.unreadThreads`,
  Ju = `Unread`,
  Yu = `trayMenu.usage`,
  Xu = `Usage`,
  Zu = `trayMenu.more`,
  Qu = `More`,
  $u = `trayMenu.projectlessThreads`,
  ed = `Tasks`,
  td = 3,
  nd = 35,
  rd = class {
    completedThreads = [];
    unreadThreads = [];
    recentThreads = [];
    constructor(e, t) {
      ((this.onOpenThread = e), (this.nativeIntl = t), this.refreshMenu());
    }
    async handleMessage(e) {
      switch (e.type) {
        case `tray-menu-threads-changed`:
          ((this.unreadThreads = e.trayMenuThreads.unreadThreads),
            (this.recentThreads = e.trayMenuThreads.recentThreads),
            this.refreshMenu());
          return;
      }
    }
    setCompletedThreads(e) {
      ((this.completedThreads = e), this.refreshMenu());
    }
    refreshMenu() {
      let e = this.nativeIntl.formatMessage({
          messageId: Zu,
          defaultMessage: Qu,
        }),
        t = this.nativeIntl.formatMessage({
          messageId: $u,
          defaultMessage: ed,
        }),
        n = id(this.unreadThreads, this.completedThreads),
        r = new Set(n.map((e) => e.path)),
        i = [
          ad({
            label: this.nativeIntl.formatMessage({
              messageId: qu,
              defaultMessage: Ju,
            }),
            moreLabel: e,
            threads: n,
            projectlessLabel: t,
            onOpenThread: this.onOpenThread,
          }),
          ad({
            label: this.nativeIntl.formatMessage({
              messageId: Gu,
              defaultMessage: Ku,
            }),
            moreLabel: e,
            threads: this.recentThreads.filter((e) => !r.has(e.path)),
            projectlessLabel: t,
            onOpenThread: this.onOpenThread,
          }),
        ]
          .filter((e) => e.length > 0)
          .flatMap((e, t) => (t === 0 ? e : [{ type: `separator` }, ...e]));
      this.setDockMenu(i);
    }
    setDockMenu(e) {
      process.platform === `darwin` &&
        c.app.dock?.setMenu(c.Menu.buildFromTemplate(e));
    }
  };
function id(e, t) {
  let n = new Set(e.map((e) => e.path));
  return [...e, ...t.filter((e) => !n.has(e.path))];
}
function ad({
  label: e,
  moreLabel: t,
  threads: n,
  projectlessLabel: r,
  onOpenThread: i,
}) {
  if (n.length === 0) return [];
  let a = n.slice(0, td),
    o = n.slice(td);
  return [
    { label: e, enabled: !1 },
    ...a.map((e) => od({ thread: e, projectlessLabel: r, onOpenThread: i })),
    ...(o.length > 0
      ? [
          {
            label: t,
            submenu: o.map((e) =>
              od({ thread: e, projectlessLabel: r, onOpenThread: i }),
            ),
          },
        ]
      : []),
  ];
}
function od({ thread: e, projectlessLabel: t, onOpenThread: n }) {
  let r = Array.from(e.title),
    i = e.isProjectless ? t : e.projectLabel;
  return {
    label:
      r.length <= nd
        ? e.title
        : `${r
            .slice(0, nd - 1)
            .join(``)
            .trimEnd()}…`,
    ...(i.length > 0 ? { sublabel: i } : {}),
    click: () => {
      n(e.path);
    },
  };
}
var V = `local`,
  sd = { id: V, display_name: `Local`, kind: `local` },
  cd = r.a(`automation-project-migration`);
async function ld(e) {
  let t = n.La(e.get(n.Gs.LOCAL_PROJECTS)),
    r = n.ja(e.get(n.Gs.PROJECT_WRITABLE_ROOTS)),
    i = e.get(n.Gs.PROJECT_ORDER) ?? [],
    a = it(e),
    o = 0,
    s = await n.Er(async (s) => {
      if (s === `~`) return { type: `projectless` };
      if (!n.Bs(s))
        return (
          cd().warning(`Skipping automation cwd that cannot become a project`, {
            safe: {},
            sensitive: { cwd: s },
          }),
          null
        );
      let c = ud({
        cwd: s,
        legacyProjectIds: a,
        localProjects: t,
        projectWritableRoots: r,
      });
      if (c != null) return { type: `project`, projectId: c };
      let l = (0, f.randomUUID)(),
        d = Date.now();
      return (
        (t = n.Ia({
          localProjects: t,
          projectId: l,
          project: {
            id: l,
            name: u.default.posix.basename(n.Us(s)) || s,
            createdAt: d,
            updatedAt: d,
          },
        })),
        (r = { ...r, [l]: [{ kind: `local`, path: s }] }),
        (i = [...i, l]),
        e.set(n.Gs.LOCAL_PROJECTS, t),
        e.set(n.Gs.PROJECT_WRITABLE_ROOTS, r),
        e.set(n.Gs.PROJECT_ORDER, i),
        await e.flush(),
        (o += 1),
        { type: `project`, projectId: l }
      );
    });
  return (
    (s > 0 || o > 0) &&
      cd().info(`Migrated automations to project targets`, {
        safe: { createdProjectCount: o, migratedAutomationCount: s },
        sensitive: {},
      }),
    s
  );
}
function ud({
  cwd: e,
  legacyProjectIds: t,
  localProjects: r,
  projectWritableRoots: i,
}) {
  let a = n.zs(e);
  return (
    Object.keys(r).find((e) => {
      let t = i[e] ?? [];
      return t.length === 1 && t[0] != null && n.zs(t[0].path) === a;
    }) ??
    t.find((e) => n.zs(e) === a) ??
    null
  );
}
var dd = r.a(`remote-project-migration`);
function fd({
  remoteCwdsByHostAndWorkspace: e,
  remoteProjects: t,
  workspaceRootOptions: n,
}) {
  if (e == null) return;
  let r = new Set(t.map((e) => `${e.hostId}::${e.remotePath}`)),
    i = new Set(n),
    a = {};
  for (let [t, n] of Object.entries(e)) {
    let e = {};
    for (let [a, o] of Object.entries(n))
      a === o && (i.has(a) || r.has(`${t}::${o}`)) && (e[a] = o);
    Object.keys(e).length !== 0 && (a[t] = e);
  }
  return Object.keys(a).length > 0 ? a : void 0;
}
function pd(e) {
  let t = it(e),
    r = e.get(n.Gs.REMOTE_CWDS_BY_HOST_AND_WORKSPACE) ?? void 0;
  if (t.length === 0 || r == null) return;
  let i = e.get(n.Gs.SELECTED_REMOTE_HOST_ID) ?? null,
    a = at(e),
    o = rt(e),
    s = e.get(n.Gs.PROJECT_ORDER) ?? null,
    c = e.get(n.Gs.ACTIVE_REMOTE_PROJECT_ID) ?? null,
    l = e.get(n.Gs.REMOTE_PROJECTS) ?? [],
    d = new Set(l.map((e) => `${e.hostId}::${e.remotePath}`)),
    p = [],
    m = [],
    h = 0;
  for (let [e, n] of Object.entries(r))
    for (let [r, i] of Object.entries(n)) {
      if (r !== i) {
        h += 1;
        continue;
      }
      if (!t.includes(r)) {
        m.push({ hostId: e, workspaceRoot: r });
        continue;
      }
      let n = a[r]?.trim() || u.default.posix.basename(i) || i;
      p.push({ hostId: e, workspaceRoot: r, remotePath: i, label: n });
    }
  if (p.length === 0) {
    let i = fd({
      remoteCwdsByHostAndWorkspace: r,
      remoteProjects: l,
      workspaceRootOptions: t,
    });
    (JSON.stringify(i) !== JSON.stringify(r) &&
      e.set(n.Gs.REMOTE_CWDS_BY_HOST_AND_WORKSPACE, i),
      dd().info(
        `[remote-project-migration] no string-based remote projects found`,
        {
          safe: {
            ignoredLocalProjectMappings: h,
            selfMappingsMissingWorkspaceRootCount: m.length,
          },
          sensitive: { selfMappingsMissingWorkspaceRoot: m },
        },
      ));
    return;
  }
  dd().info(`[remote-project-migration] found string-based remote projects`, {
    safe: {
      candidateCount: p.length,
      ignoredLocalProjectMappings: h,
      selfMappingsMissingWorkspaceRootCount: m.length,
    },
    sensitive: {
      stringBasedRemoteProjects: p,
      selfMappingsMissingWorkspaceRoot: m,
    },
  });
  let g = new Set(p.map((e) => e.workspaceRoot)),
    _ = [],
    v = [];
  for (let e of p) {
    let t = `${e.hostId}::${e.remotePath}`;
    if (d.has(t)) {
      v.push({ hostId: e.hostId, remotePath: e.remotePath });
      continue;
    }
    (_.push({
      id: (0, f.randomUUID)(),
      hostId: e.hostId,
      remotePath: e.remotePath,
      label: e.label,
    }),
      d.add(t));
  }
  dd().info(`[remote-project-migration] prepared remote project objects`, {
    safe: {
      createdRemoteProjectsCount: _.length,
      reusedRemoteProjectsCount: v.length,
      migratedWorkspaceRootCount: g.size,
    },
    sensitive: { newRemoteProjects: _, reusedRemoteProjects: v },
  });
  let y = _.length > 0 ? [...l, ..._] : l,
    b = new Map();
  for (let e of y) {
    let t = b.get(e.remotePath) ?? [];
    (t.push(e), b.set(e.remotePath, t));
  }
  let x = t.filter((e) => g.has(e)),
    S = new Map();
  for (let e of x) {
    let t = [...(b.get(e) ?? [])];
    (t.sort((e, t) => {
      let n = e.hostId === i ? 0 : 1,
        r = t.hostId === i ? 0 : 1;
      return n === r ? e.hostId.localeCompare(t.hostId) : n - r;
    }),
      S.set(
        e,
        t.map((e) => e.id),
      ));
  }
  let C = t.filter((e) => !g.has(e)),
    w = o.filter((e) => !g.has(e)),
    T = o.find((e) => g.has(e)),
    E = s ?? t,
    D = [],
    O = new Set();
  for (let e of E) {
    if (!g.has(e)) {
      O.has(e) || (D.push(e), O.add(e));
      continue;
    }
    let t = S.get(e) ?? [];
    for (let e of t) O.has(e) || (D.push(e), O.add(e));
  }
  for (let e of x) {
    let t = S.get(e) ?? [];
    for (let e of t) O.has(e) || (D.push(e), O.add(e));
  }
  let k = T == null ? c : (S.get(T)?.[0] ?? c);
  (dd().info(`[remote-project-migration] rewriting project state`, {
    safe: {
      nextWorkspaceRootOptionsCount: C.length,
      nextActiveWorkspaceRootsCount: w.length,
      nextProjectOrderCount: D.length,
      hasNextActiveRemoteProjectId: k != null,
    },
    sensitive: {
      migratedWorkspaceRoots: [...g],
      replacementRemoteProjectIdsByWorkspaceRoot: Object.fromEntries(S),
      nextWorkspaceRootOptions: C,
      nextActiveWorkspaceRoots: w,
      nextProjectOrder: D,
      nextActiveRemoteProjectId: k,
    },
  }),
    _.length > 0 && e.set(n.Gs.REMOTE_PROJECTS, y),
    C.length > 0 ? ct(e, C) : e.set(n.Gs.WORKSPACE_ROOT_OPTIONS, void 0),
    dt(e, (e) => {
      if (e == null) return;
      let t = Object.fromEntries(Object.entries(e).filter(([e]) => !g.has(e)));
      return Object.keys(t).length > 0 ? t : void 0;
    }),
    w.length > 0
      ? st(e, w)
      : C.length > 0
        ? st(e, [C[0]])
        : e.set(n.Gs.ACTIVE_WORKSPACE_ROOTS, void 0),
    e.set(n.Gs.PROJECT_ORDER, D.length > 0 ? D : void 0),
    e.set(n.Gs.ACTIVE_REMOTE_PROJECT_ID, k ?? void 0),
    e.set(
      n.Gs.REMOTE_CWDS_BY_HOST_AND_WORKSPACE,
      fd({
        remoteCwdsByHostAndWorkspace: r,
        remoteProjects: y,
        workspaceRootOptions: C,
      }),
    ));
}
var md = [
  {
    id: 1,
    runAsync: async (e) => {
      pd(e);
    },
  },
  {
    id: 2,
    runAsync: async (e) => {
      await ld(e);
    },
  },
  {
    id: 3,
    runAsync: async (e) => {
      _d(e);
    },
  },
];
async function hd(e) {
  _d(e) && (await e.flushOrThrow());
}
async function gd(e, t) {
  let n = e.get(`electron-local-data-migration-id`) ?? 0;
  for (let r of t)
    r.id <= n ||
      (await r.runAsync(e), e.set(a.y, r.id), await e.flushOrThrow());
}
function _d(e) {
  return Array.isArray(e.getStored(`electron-browser-download-history`))
    ? (e.delete(a.g), !0)
    : !1;
}
var vd = 4,
  yd = 8 * 1024 * 1024,
  bd = 200,
  xd = `__native_pipe_request_`;
async function Sd(e = {}) {
  let t = new Cd(
    e.pipePath ?? (await Nd(e.nativePipeDirectory)),
    e.socketPeerAuthorizer ?? (() => ({ authorized: !0 })),
    e.events ?? {},
  );
  try {
    return (await t.start(), t);
  } catch (e) {
    throw (await t.close(), e);
  }
}
var Cd = class {
  sockets = new Set();
  pendingDataBySocket = new Map();
  socketsByClientId = new Map();
  clientIdsBySocket = new Map();
  server = _.default.createServer((e) => {
    this.handleSocketConnection(e);
  });
  messageCallback = null;
  nextClientId = 1;
  started = !1;
  ownsPipePath = !1;
  constructor(e, t, n) {
    ((this.pipePath = e), (this.socketPeerAuthorizer = t), (this.events = n));
  }
  async start() {
    (await Fd(this.pipePath),
      await new Promise((e, t) => {
        (this.server.once(`error`, t),
          this.server.listen(this.pipePath, () => {
            (this.server.off(`error`, t), e());
          }));
      }),
      (this.started = !0),
      (this.ownsPipePath = !0),
      this.events.onListening?.(this.pipePath));
  }
  sendMessage(e) {
    if (Od(e)) {
      let t = jd(JSON.stringify(e));
      for (let e of this.sockets) e.destroyed || e.write(t);
      return;
    }
    let t = Td(e);
    if (t == null) return;
    let n = this.socketsByClientId.get(t.clientId);
    if (n == null) return;
    e.id = t.messageId;
    let r = jd(JSON.stringify(e));
    n.destroyed || n.write(r);
  }
  setMessageCallback(e) {
    this.messageCallback = e;
  }
  async close() {
    let e = this.ownsPipePath;
    for (let e of this.sockets) e.destroy();
    (this.sockets.clear(),
      this.pendingDataBySocket.clear(),
      this.clientIdsBySocket.clear(),
      this.socketsByClientId.clear(),
      (this.started &&=
        (await new Promise((e) => {
          this.server.close(() => {
            e();
          });
        }),
        !1)),
      (this.ownsPipePath = !1),
      e && Id(this.pipePath));
  }
  handleSocketConnection(e) {
    let t;
    try {
      t = this.socketPeerAuthorizer(e);
    } catch (t) {
      (this.events.onAuthorizationError?.(t), e.destroy());
      return;
    }
    if (!t.authorized) {
      (this.events.onRejectedSocket?.(t), e.destroy());
      return;
    }
    (this.sockets.add(e), this.pendingDataBySocket.set(e, Buffer.alloc(0)));
    let n = String(this.nextClientId);
    ((this.nextClientId += 1),
      this.clientIdsBySocket.set(e, n),
      this.socketsByClientId.set(n, e),
      e.on(`data`, (t) => {
        this.handleSocketData(e, t);
      }),
      e.on(`error`, (e) => {
        this.events.onSocketError?.(e);
      }),
      e.on(`close`, () => {
        (this.sockets.delete(e), this.pendingDataBySocket.delete(e));
        let t = this.clientIdsBySocket.get(e);
        t != null &&
          (this.clientIdsBySocket.delete(e), this.socketsByClientId.delete(t));
      }));
  }
  handleSocketData(e, t) {
    let n = Buffer.isBuffer(t) ? t : Buffer.from(t),
      r = this.pendingDataBySocket.get(e) ?? Buffer.alloc(0),
      i = Md(Buffer.concat([r, n]));
    if (i == null) {
      (this.events.onInvalidFrame?.(),
        this.pendingDataBySocket.delete(e),
        e.destroy());
      return;
    }
    this.pendingDataBySocket.set(e, i.remainingData);
    for (let t of i.messages) this.handleIncomingMessage(e, t);
  }
  handleIncomingMessage(e, t) {
    let n;
    try {
      n = Ed(t);
    } catch {
      return;
    }
    if (Dd(n)) {
      let t = this.clientIdsBySocket.get(e);
      if (t == null) return;
      n.id = wd(t, n.id);
    }
    this.messageCallback?.(n);
  }
};
function wd(e, t) {
  return `${xd}${e}:${JSON.stringify(t)}`;
}
function Td(e) {
  if (typeof e.id != `string`) return;
  let t = e.id;
  if (!t.startsWith(xd)) return;
  let n = t.slice(22),
    r = n.indexOf(`:`);
  if (r < 1) return;
  let i = n.slice(0, r),
    a;
  try {
    a = JSON.parse(n.slice(r + 1));
  } catch {
    return;
  }
  if (Ad(a)) return { clientId: i, messageId: a };
}
function Ed(e) {
  let t = JSON.parse(e);
  if (!kd(t)) throw Error(`invalid transport message`);
  return t;
}
function Dd(e) {
  return Od(e) && Ad(e.id);
}
function Od(e) {
  return `method` in e && typeof e.method == `string`;
}
function kd(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function Ad(e) {
  return typeof e == `string` || typeof e == `number`;
}
function jd(e) {
  let t = Buffer.from(e, `utf8`),
    n = Buffer.alloc(vd + t.length);
  return (
    (0, l.endianness)() === `LE`
      ? n.writeUInt32LE(t.length, 0)
      : n.writeUInt32BE(t.length, 0),
    t.copy(n, vd),
    n
  );
}
function Md(e) {
  let t = [],
    n = 0;
  for (; e.length - n >= vd; ) {
    let r =
      (0, l.endianness)() === `LE` ? e.readUInt32LE(n) : e.readUInt32BE(n);
    if (r > yd) return null;
    let i = vd + r;
    if (e.length - n < i) break;
    (t.push(e.subarray(n + vd, n + i).toString(`utf8`)), (n += i));
  }
  return { messages: t, remainingData: e.subarray(n) };
}
async function Nd(e) {
  return (
    (e ??= zt((0, l.platform)())),
    (0, l.platform)() === `win32`
      ? `${e}-${(0, f.randomUUID)()}`
      : (await Pd(e), u.default.join(e, `${(0, f.randomUUID)()}.sock`))
  );
}
async function Pd(e) {
  try {
    await (0, m.mkdir)(e, { recursive: !0 });
    return;
  } catch (e) {
    if (!(e instanceof Error && `code` in e && e.code === `EEXIST`)) throw e;
  }
  let t = await (0, m.lstat)(e);
  if (!t.isDirectory()) {
    if (!t.isSocket())
      throw Error(
        `Browser-use native pipe directory path exists and is not a directory`,
      );
    if (await Ld(e))
      throw Error(`Browser-use native pipe directory path is already in use`);
    (await (0, m.unlink)(e), await (0, m.mkdir)(e, { recursive: !0 }));
  }
}
async function Fd(e) {
  if (!((0, l.platform)() === `win32` || !(0, p.existsSync)(e))) {
    if (await Ld(e)) throw Error(`Browser-use native pipe is already in use`);
    (0, p.unlinkSync)(e);
  }
}
function Id(e) {
  (0, l.platform)() === `win32` ||
    !(0, p.existsSync)(e) ||
    (0, p.unlinkSync)(e);
}
async function Ld(e) {
  return new Promise((t, n) => {
    let r = _.default.createConnection(e),
      i = !1,
      a = setTimeout(() => {
        o(!0);
      }, bd);
    a.unref();
    let o = (e) => {
      if (!i) {
        if (((i = !0), clearTimeout(a), r.destroy(), e instanceof Error)) {
          n(e);
          return;
        }
        t(e);
      }
    };
    (r.once(`connect`, () => {
      o(!0);
    }),
      r.once(`error`, (e) => {
        if (e.code === `ECONNREFUSED` || e.code === `ENOENT`) {
          o(!1);
          return;
        }
        o(e);
      }));
  });
}
var Rd = `CODEX_BROWSER_USE_PEER_AUTHORIZATION`,
  zd = `browser-use-peer-authorization.node`;
function Bd(e) {
  return e[Rd] === `1`;
}
var Vd = r.a(`browser-use-native-pipe-peer-authorizer`),
  Hd = (0, T.createRequire)(__filename);
function Ud() {
  if (process.platform !== `darwin`) return () => ({ authorized: !0 });
  let e = i.a.readFromPackageMetadata(),
    t =
      e != null &&
      i.a.shouldIncludeBrowserUsePeerAuthorization(e, process.platform),
    n = !t && Bd(process.env);
  if (c.app?.isPackaged === !0 && e == null)
    return (
      Vd().warning(`browser-use native pipe peer authorization disabled`, {
        safe: { reason: `missing-package-build-flavor` },
        sensitive: {},
      }),
      () => ({ authorized: !1, reason: `missing-package-build-flavor` })
    );
  if (!t && !n) return () => ({ authorized: !0 });
  let r = n
    ? St({ env: process.env, resourcesPath: process.resourcesPath })
    : process.resourcesPath;
  if (r == null)
    throw Error(`Browser-use peer authorization requires resourcesPath`);
  let a;
  try {
    a = Hd((0, u.join)(r, `native`, zd));
  } catch (e) {
    throw Error(`Failed to load browser-use peer authorization addon`, {
      cause: e,
    });
  }
  return (
    Vd().info(`browser-use native pipe peer authorization enabled`, {
      safe: { mode: n ? `dev` : `packaged` },
      sensitive: {},
    }),
    (e) => {
      let t = Wd(e);
      return t == null
        ? { authorized: !1, reason: `missing-socket-file-descriptor` }
        : a.authorizeSocketPeer(t, n);
    }
  );
}
function Wd(e) {
  let t = e._handle?.fd;
  return typeof t == `number` && Number.isInteger(t) && t >= 0 ? t : null;
}
var Gd = r.a(`node-repl-host-services-native-pipe`),
  Kd = n.xl({
    id: n.Tl([n.bl(), n.wl()]),
    jsonrpc: n.vl(`2.0`),
    method: n.vl(`ensureService`),
    params: n.xl({ service: n.wl().trim().min(1).max(128) }),
  });
async function qd({
  services: e,
  pipePath: t = nr(),
  socketPeerAuthorizer: n = Ud(),
}) {
  let r = await Sd({
    pipePath: t,
    socketPeerAuthorizer: n,
    events: {
      onListening: (e) => {
        ((0, p.chmodSync)(e, 384),
          Gd().info(`node_repl_host_services_listening`, {
            safe: {},
            sensitive: { pipePath: e },
          }));
      },
      onAuthorizationError: (e) => {
        Gd().warning(`node_repl_host_services_peer_authorization_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      },
      onRejectedSocket: (e) => {
        Gd().warning(`node_repl_host_services_peer_rejected`, {
          safe: { reason: e.reason ?? `unauthorized` },
          sensitive: {},
        });
      },
      onSocketError: (e) => {
        Gd().warning(`node_repl_host_services_socket_error`, {
          safe: {},
          sensitive: { error: e },
        });
      },
    },
  });
  return (
    r.setMessageCallback((t) => {
      Jd(t, e, (e) => {
        r.sendMessage(e);
      });
    }),
    { pipePath: r.pipePath, dispose: () => r.close() }
  );
}
async function Jd(e, t, n) {
  let r = Kd.safeParse(e);
  if (!r.success) {
    let t = Yd(e);
    t != null &&
      n({
        error: { code: -32602, message: `Invalid host service request` },
        id: t,
        jsonrpc: `2.0`,
      });
    return;
  }
  let i = t.get(r.data.params.service);
  if (i == null) {
    n({
      error: {
        code: -32602,
        message: `Unsupported host service: ${r.data.params.service}`,
      },
      id: r.data.id,
      jsonrpc: `2.0`,
    });
    return;
  }
  try {
    let e = await i();
    n({ id: r.data.id, jsonrpc: `2.0`, result: e ?? {} });
  } catch (e) {
    (Gd().warning(`node_repl_host_service_ensure_failed`, {
      safe: { service: r.data.params.service },
      sensitive: { error: e },
    }),
      n({
        error: { code: -32e3, message: `Host service ensure failed` },
        id: r.data.id,
        jsonrpc: `2.0`,
      }));
  }
}
function Yd(e) {
  if (typeof e != `object` || !e || !(`id` in e)) return null;
  let t = e.id;
  return typeof t == `string` || typeof t == `number` ? t : null;
}
var Xd = r.a(`react-devtools-extension`),
  Zd = `fmkadmapgofadopljbjfkapdkoienihi`;
async function Qd(e = {}) {
  let t = $d(e),
    n =
      e.loadExtension ??
      ((e, t) => c.session.defaultSession.loadExtension(e, t));
  if (t == null)
    return (
      Xd().trace(`React DevTools extension not found`, {
        safe: { extensionId: Zd, platform: process.platform },
      }),
      !1
    );
  try {
    let e = await n(t);
    return (
      Xd().info(`Loaded React DevTools extension`, {
        safe: { extensionId: e.id, name: e.name, version: e.version },
      }),
      !0
    );
  } catch (e) {
    return (
      Xd().error(`Failed to load React DevTools extension`, {
        safe: {},
        sensitive: { error: String(e) },
      }),
      !1
    );
  }
}
function $d(e = {}) {
  let {
    platform: t = process.platform,
    homeDirectory: n = (0, l.homedir)(),
    localAppData: r = process.env.LOCALAPPDATA,
    userDataDirectory: i = c.app.getPath(`userData`),
  } = e;
  for (let e of ef({
    platform: t,
    homeDirectory: n,
    localAppData: r,
    userDataDirectory: i,
  })) {
    let t = rf((0, u.join)(e, Zd));
    if (t != null) return t;
  }
  return null;
}
function ef({
  platform: e,
  homeDirectory: t,
  localAppData: n,
  userDataDirectory: r,
}) {
  return [
    (0, u.join)(r, `extensions`),
    ...tf({ platform: e, homeDirectory: t, localAppData: n }).flatMap(nf),
  ];
}
function tf({ platform: e, homeDirectory: t, localAppData: n }) {
  return e === `darwin`
    ? [(0, u.join)(t, `Library`, `Application Support`, `Google`, `Chrome`)]
    : e === `win32`
      ? n == null
        ? []
        : [(0, u.join)(n, `Google`, `Chrome`, `User Data`)]
      : e === `linux`
        ? [
            (0, u.join)(t, `.config`, `google-chrome`),
            (0, u.join)(t, `.config`, `google-chrome-beta`),
            (0, u.join)(t, `.config`, `google-chrome-canary`),
            (0, u.join)(t, `.config`, `chromium`),
          ]
        : [];
}
function nf(e) {
  return (0, p.existsSync)(e)
    ? (0, p.readdirSync)(e, { withFileTypes: !0 })
        .filter((e) => e.isDirectory())
        .map((t) => (0, u.join)(e, t.name, `Extensions`))
    : [];
}
function rf(e) {
  if (!(0, p.existsSync)(e)) return null;
  let t = (0, p.readdirSync)(e, { withFileTypes: !0 })
    .filter((e) => e.isDirectory())
    .map((e) => e.name)
    .sort((e, t) =>
      e.localeCompare(t, void 0, { numeric: !0, sensitivity: `base` }),
    )
    .at(-1);
  return t == null ? null : (0, u.join)(e, t);
}
var af = e.o(n.sl()),
  of = { width: 356, height: 320 },
  sf = { width: 384, height: 400 },
  cf = { top: 8, right: 28, bottom: 8, left: 0 },
  lf = 24,
  uf = 4,
  df = 12,
  ff = 96;
function pf({
  anchor: e,
  displayBounds: t,
  mode: n = `native`,
  mascotSize: r,
  nativeMaterialAttached: i = !1,
  previousPlacement: a,
  traySize: o,
  viewportSize: s = n === `native` ? sf : of,
}) {
  let c = {
      width: Math.min(s.width, t.width),
      height: Math.min(s.height, t.height),
    },
    l = n === `native`,
    u = yf(
      {
        ...e,
        width: Math.min(r.width, t.width),
        height: Math.min(r.height, t.height),
      },
      t,
    ),
    d =
      o == null
        ? null
        : {
            width: Math.min(
              o.width,
              Math.max(0, c.width - (l ? cf.left + cf.right : 0)),
            ),
            height: Math.min(
              o.height,
              Math.max(
                0,
                c.height -
                  u.height -
                  cf.bottom -
                  uf -
                  (l ? cf.top + df * 2 : 0),
              ),
            ),
          },
    f = mf(u, t),
    p =
      d == null
        ? a
        : hf({
            anchor: u,
            displayBounds: t,
            mode: n,
            preferredPlacement: f,
            previousPlacement: a,
            traySize: d,
          }),
    m = d == null ? null : _f(gf(u, d, p, n), t, n),
    h = l
      ? bf({ anchor: u, displayBounds: t, placement: p, viewport: c })
      : xf({
          contentBounds: Cf([vf(u), ...(m == null ? [] : [m])]),
          displayBounds: t,
          viewport: c,
        }),
    g = c,
    _ = h.x,
    v = h.y;
  if (l && i) v = u.y;
  else if (l && m != null) {
    let e = lf - df,
      n = c.width + e * 2;
    ((g = { width: Math.min(n, t.width), height: c.height }),
      (_ =
        g.width === n
          ? h.x - e
          : Df(h.x, m.x + m.width + lf - g.width, m.x - lf)));
  }
  let y =
    l && i
      ? { x: _, y: v, width: g.width, height: u.height }
      : { ...h, x: _, y: v, width: g.width, height: g.height };
  return {
    anchor: u,
    mascot: wf(u, y),
    placement: p,
    tray: m == null ? null : wf(m, y),
    viewport: g,
    windowBounds: y,
  };
}
function mf(e, t) {
  return `${Ef(e) < Ef(t) ? `bottom` : `top`}-${Tf(e) < Tf(t) ? `start` : `end`}`;
}
function hf({
  anchor: e,
  displayBounds: t,
  mode: n,
  preferredPlacement: r,
  previousPlacement: i,
  traySize: a,
}) {
  let o;
  o =
    n === `legacy`
      ? [`top-start`, `top-end`, `bottom-start`, `bottom-end`]
      : r.endsWith(`start`)
        ? [`top-start`, `bottom-start`]
        : [`top-end`, `bottom-end`];
  let s = o
    .map((o) => {
      let s =
        n === `native` ? o.startsWith(`top`) === i.startsWith(`top`) : o === i;
      return {
        placement: o,
        score: Sf(gf(e, a, o, n), t) + (o === r ? 0 : 32) + (s ? -ff : 0),
      };
    })
    .sort((e, t) => e.score - t.score)[0];
  if (s == null) throw Error(`Expected avatar overlay placement`);
  return s.placement;
}
function gf(e, t, n, r) {
  let i = n.startsWith(`top`);
  return r === `legacy`
    ? {
        x: n.endsWith(`end`) ? e.x + e.width + cf.right - t.width : e.x,
        y: i ? e.y - t.height - uf : e.y + e.height + uf,
        width: t.width,
        height: t.height,
      }
    : {
        x: e.x + (e.width - t.width) / 2,
        y: i ? e.y - cf.top - t.height - uf : e.y + e.height + cf.bottom + uf,
        width: t.width,
        height: t.height,
      };
}
function _f(e, t, n) {
  let r = n === `native` ? df : 0;
  return {
    ...e,
    x: Df(e.x, t.x + r, t.x + t.width - e.width - r),
    y: Df(e.y, t.y + r, t.y + t.height - e.height - r),
  };
}
function vf(e) {
  return {
    x: e.x - cf.left,
    y: e.y - cf.top,
    width: e.width + cf.left + cf.right,
    height: e.height + cf.top + cf.bottom,
  };
}
function yf(e, t) {
  return {
    ...e,
    x: Df(e.x, t.x, t.x + t.width - e.width),
    y: Df(e.y, t.y, t.y + t.height - e.height - cf.bottom),
  };
}
function bf({ anchor: e, displayBounds: t, placement: n, viewport: r }) {
  return {
    x: Df(e.x + e.width / 2 - r.width / 2, t.x, t.x + t.width - r.width),
    y: Df(
      n.startsWith(`top`)
        ? e.y + e.height + cf.bottom - r.height
        : e.y - cf.top,
      t.y,
      t.y + t.height - r.height,
    ),
    width: r.width,
    height: r.height,
  };
}
function xf({ contentBounds: e, displayBounds: t, viewport: n }) {
  return {
    x: Df(e.x + e.width - n.width, t.x, t.x + t.width - n.width),
    y: Df(e.y + e.height - n.height, t.y, t.y + t.height - n.height),
    width: n.width,
    height: n.height,
  };
}
function Sf(e, t) {
  let n = Math.max(0, t.x - e.x),
    r = Math.max(0, t.y - e.y),
    i = Math.max(0, e.x + e.width - t.x - t.width),
    a = Math.max(0, e.y + e.height - t.y - t.height);
  return n + r + i + a + (n + i) * e.height + (r + a) * e.width;
}
function Cf(e) {
  let t = Math.min(...e.map((e) => e.x)),
    n = Math.min(...e.map((e) => e.y)),
    r = Math.max(...e.map((e) => e.x + e.width)),
    i = Math.max(...e.map((e) => e.y + e.height));
  return { x: t, y: n, width: r - t, height: i - n };
}
function wf(e, t) {
  return {
    left: Math.round(e.x - t.x),
    top: Math.round(e.y - t.y),
    width: Math.round(e.width),
    height: Math.round(e.height),
  };
}
function Tf(e) {
  return e.x + e.width / 2;
}
function Ef(e) {
  return e.y + e.height / 2;
}
function Df(e, t, n) {
  return t > n
    ? Math.round((t + n) / 2)
    : Math.min(Math.max(Math.round(e), t), n);
}
var Of = sf.height + 256 * 2,
  kf = sf.width + 64,
  Af = 180,
  jf = 0.5,
  Mf = 1e4;
function Nf({
  createWindow: e,
  getCursorScreenPoint: t,
  getSurfaceContentState: r,
  getOverlayBounds: i,
  nativeBridge: a,
  onBeforeSurfaceFocus: o,
  onNativeAttachmentFailed: s,
  onNativePresentationOwned: c,
  onPreparationUpdated: l,
  onSurfaceLifecycleFailed: u,
  onSurfaceLifecycleTransition: d,
  onSurfaceFailed: f,
  onSurfaceFocus: p,
  onSurfaceReadyRejected: m,
  onSurfaceReadyTimeout: h,
  onActivityStackScrollOffsetPublished: g,
  presentsNotificationStackExpansionImmediately: _ = !1,
}) {
  let v = new Map(),
    y = new Map(),
    b = null,
    x = null,
    S = 0,
    C = null,
    w = null,
    T = !1,
    E = null,
    D = null,
    O = null,
    k = a == null,
    A = null,
    j = [],
    M = 0,
    N = !1,
    P = !1,
    F = null;
  return {
    consumePointerDownSurfaceId() {
      let e = a?.consumePointerDownSurfaceId() ?? null;
      return e === a?.getPointerSurfaceId() ? e : null;
    },
    consumePointerUpSurfaceId() {
      return a?.consumePointerUpSurfaceId() ?? null;
    },
    detach() {
      ((k = !0), ce(), (w = null), ue(), a?.detach(), fe());
    },
    getPreparation(e) {
      return v.get(e)?.preparation ?? null;
    },
    getPointerSurfaceId() {
      return a?.getPointerSurfaceId() ?? null;
    },
    getSurfaceId(e) {
      return e == null
        ? null
        : ([...v].find(([, t]) => t.window.webContents.id === e.id)?.[0] ??
            null);
    },
    hideSurfacesImmediately() {
      ((b = null), ue());
      for (let e of v.values()) e.visible && (ie(e, 0), (e.hasPresented = !1));
    },
    moveBackingCanvases() {
      for (let e of v.values()) e.window.setBounds(Ff(i()));
    },
    publishActivityStackScrollOffset() {
      if (!k) {
        if (E != null) {
          T = !0;
          return;
        }
        ((T = !1), g(M));
      }
    },
    setNotificationStackExpanded(e) {
      (!e && O != null && E == null && ((T = !1), g(M)),
        (N = e),
        e || se(),
        !(k || !_ || P === e) &&
          ((P = e), (F = e), a?.setNotificationStackExpanded(e)));
    },
    setPointerInteractionSurface: ge,
    setSurfaceKeyboardInteraction: he,
    surfaceReady: L,
    scrollActivityStack: I,
    prepareSurfaces(t, o) {
      if (
        k ||
        (o.isNotificationStackExpanded &&
          O != null &&
          o.activityStackScrollOffset !== O)
      )
        return;
      O = null;
      let s = F === o.isNotificationStackExpanded;
      (s && (F = null),
        (N = o.isNotificationStackExpanded),
        N || ce(),
        (A = o.activityStackPresentation),
        (j = o.activityStackItems),
        (M = o.activityStackScrollOffset));
      let c =
        o.isNotificationStackExpanded &&
        (E === M || b?.presentsActivityStackScrollImmediately === !0);
      E = null;
      let u = new Set(),
        d = !1,
        f = new Set(),
        p = new Set(),
        m = new Set(),
        h = new Map(),
        g = new Set(t.map(({ id: e }) => e));
      for (let e of y.keys()) g.has(e) || oe(e);
      for (let g of t) {
        let t = g.id;
        m.add(t);
        let _ = v.get(t);
        if (_ == null) {
          if (!n.Xs.includes(t))
            throw Error(`Missing avatar overlay composition surface: ${t}`);
          let a = e(t);
          (a.setBounds(Ff(i())),
            a.setFocusable(!1),
            a.setIgnoreMouseEvents(!0, { forward: !0 }),
            a.setOpacity(0),
            a.showInactive(),
            (_ = {
              activityStackPresentation: o.activityStackPresentation,
              attached: !1,
              diagnostic: { lastStage: `created` },
              hasPresented: !1,
              keyboardInteractive: !1,
              preparation: {
                acceptsPointerEvents: !1,
                backingRect: { height: 0, left: 0, top: 0, width: 0 },
                chromiumPresentationRect: {
                  height: 0,
                  left: 0,
                  top: 0,
                  width: 0,
                },
                contentState: r(t, o.contentState),
                cornerRadius: 0,
                edgeZone: null,
                id: t,
                nativeAttached: !1,
                opacity: 0,
                platterRect: { height: 0, left: 0, top: 0, width: 0 },
                presentationOffset: { x: 0, y: 0 },
                presentationRect: { height: 0, left: 0, top: 0, width: 0 },
                revision: 0,
              },
              presentedPreparation: null,
              visible: !1,
              window: a,
            }),
            v.set(t, _));
        }
        let x = r(t, o.contentState),
          S = !(0, af.default)(_.preparation.contentState, x),
          C = !1,
          w = y.get(t);
        if (w != null) {
          let e = _.activityStackPresentation.slots.find(
              ({ slotId: e }) => e === t,
            ),
            n = o.activityStackPresentation.slots.find(
              ({ slotId: e }) => e === t,
            ),
            r = e != null && n != null ? If(_.preparation, e, n) : null;
          if (
            w.revision === _.preparation.revision &&
            w.itemId === e?.itemId &&
            w.itemId === n?.itemId &&
            !S &&
            r != null &&
            qf(r, { ...g, acceptsPointerEvents: r.acceptsPointerEvents })
          )
            continue;
          (oe(t, w.revision), (C = !0));
        }
        if (
          !C &&
          c &&
          _.visible &&
          !S &&
          !Wf(t, _.activityStackPresentation, o.activityStackPresentation)
        ) {
          let e = _.activityStackPresentation.slots.find(
              ({ slotId: e }) => e === t,
            ),
            n = o.activityStackPresentation.slots.find(
              ({ slotId: e }) => e === t,
            );
          if (e != null && n != null) {
            if (
              ((_.activityStackPresentation = o.activityStackPresentation),
              zf(e.contentRect, n.contentRect) &&
                zf(e.presentationRect, n.presentationRect) &&
                zf(e.visibleRect, n.visibleRect))
            )
              continue;
            ((_.preparation = If(_.preparation, e, n)), u.add(t));
            continue;
          }
        }
        let T = qf(_.preparation, g);
        if (!C && _.visible && !S && T) continue;
        if (
          !C &&
          _.visible &&
          _.attached &&
          !b?.waitingRevisions.has(t) &&
          !S
        ) {
          ((_.activityStackPresentation = o.activityStackPresentation),
            (_.preparation = {
              ..._.preparation,
              acceptsPointerEvents: g.acceptsPointerEvents,
              backingRect: g.rect,
              chromiumOverflowCornerRadius: g.chromiumOverflowCornerRadius,
              chromiumOverflowRect: g.chromiumOverflowRect,
              chromiumPresentationRect: g.chromiumPresentationRect,
              cornerRadius: g.cornerRadius,
              edgeZone: g.edgeZone,
              opacity: g.opacity,
              platterRect: g.platterRect,
              presentationOffset: g.presentationOffset,
              presentationRect: g.presentationRect,
              revision: _.preparation.revision + 1,
              trailingAccessory: g.trailingAccessory,
            }),
            u.add(t));
          continue;
        }
        let E =
          _.visible &&
          Wf(t, _.activityStackPresentation, o.activityStackPresentation);
        E &&
          (c
            ? (ie(_, 0), (d = !0))
            : _.attached &&
              _.presentedPreparation != null &&
              (a?.animateSurface({
                ...Pf(_.presentedPreparation, 0),
                acceptsPointerEvents: !1,
              }),
              (d = !0)));
        let D = _.preparation.revision + 1;
        s && t.startsWith(`activity-slot-`) && !T && Gf(_.preparation, g)
          ? (f.add(t), E && p.add(t))
          : (!_.visible || !T) && u.add(t);
        let O = {
          attached: _.attached,
          activityStackPresentation: o.activityStackPresentation,
          diagnostic: { lastStage: `preparation-sent` },
          hasPresented: _.hasPresented,
          keyboardInteractive: _.keyboardInteractive,
          preparation: {
            acceptsPointerEvents: g.acceptsPointerEvents,
            backingRect: g.rect,
            chromiumOverflowCornerRadius: g.chromiumOverflowCornerRadius,
            chromiumOverflowRect: g.chromiumOverflowRect,
            chromiumPresentationRect: g.chromiumPresentationRect,
            contentState: x,
            cornerRadius: g.cornerRadius,
            edgeZone: g.edgeZone,
            id: t,
            nativeAttached: _.attached,
            opacity: g.opacity,
            platterRect: g.platterRect,
            presentationOffset: g.presentationOffset,
            presentationRect: g.presentationRect,
            revision: D,
            trailingAccessory: g.trailingAccessory,
          },
          presentedPreparation: _.presentedPreparation,
          visible: !0,
          window: _.window,
        };
        (v.set(t, O), h.set(t, D), l(_.window, O.preparation));
      }
      d && a?.presentNotificationStack(0);
      let _ = new Set();
      for (let [e, t] of v) !m.has(e) && t.visible && _.add(e);
      if (b != null) {
        for (let [e, t] of b.waitingRevisions)
          m.has(e) && !h.has(e) && h.set(e, t);
        for (let e of b.animatedSurfaceIds) m.has(e) && u.add(e);
        for (let e of b.retargetedActivitySurfaceIds) m.has(e) && f.add(e);
        for (let e of b.retargetedReassignedActivitySurfaceIds)
          m.has(e) && p.add(e);
      }
      for (let e of u) (f.delete(e), p.delete(e));
      if (u.size === 0 && _.size === 0 && f.size === 0 && P === N) {
        for (let [e, t] of h)
          b?.waitingRevisions.has(e) && b.waitingRevisions.set(e, t);
        return;
      }
      (h.size === 0 &&
        b != null &&
        Yf(m, b.visibleSurfaceIds) &&
        Yf(_, b.hiddenSurfaceIds) &&
        N === b.notificationStackExpanded) ||
        ((S += 1),
        (b = {
          animationDurationMs: o.animationDurationMs,
          animatedSurfaceIds: u,
          hiddenSurfaceIds: _,
          id: S,
          notificationStackExpanded: N,
          presentsActivityStackScrollImmediately: c,
          retargetedActivitySurfaceIds: f,
          retargetedReassignedActivitySurfaceIds: p,
          visibleSurfaceIds: m,
          waitingRevisions: h,
        }),
        le(),
        R());
    },
    surfaceFailed(e) {
      v.has(e) && (u?.(e), f?.(e), de());
    },
  };
  function I(e) {
    if (
      k ||
      !N ||
      A == null ||
      !A.slots.some(({ visibleRect: e }) => e.height > 0)
    )
      return !1;
    let t = n.nc({
      contentHeight: A.contentHeight,
      deltaY: e,
      scrollOffset: D ?? M,
      viewportHeight: A.viewportRect.height,
    });
    return ((D = t), t === M || ee(), !0);
  }
  function ee() {
    if (k || !N || A == null || b != null || E != null || D == null) return;
    if (D === M) {
      ((D = null), T && ((T = !1), g(M)));
      return;
    }
    let e = Bf({
      currentPresentation: A,
      items: j,
      scrollOffset: M,
      targetScrollOffset: D,
    });
    if (
      ((e = Vf({
        items: j,
        scrollOffset: M,
        awaitingPaints: y,
        targetScrollOffset: e,
        viewportRect: A.viewportRect,
      })),
      e === M)
    )
      return;
    let t = n.tc({
      expanded: !0,
      items: j,
      scrollOffset: e,
      viewportRect: A.viewportRect,
    });
    if (((M = e), (O = e), !Uf(A, t))) {
      ((E = e), g(e));
      return;
    }
    (ne(A, t), (A = t), ee());
  }
  function L(e) {
    let t = v.get(e.id);
    if (t == null) return `surface-missing`;
    if (!t.visible) return pe(t, e, `surface-hidden`);
    if (e.revision !== t.preparation.revision)
      return pe(t, e, `revision-mismatch:expected=${t.preparation.revision}`);
    switch (e.phase) {
      case `preparation-received`: {
        t.diagnostic.lastStage !== `painted` &&
          t.diagnostic.lastStage !== `presented` &&
          (t.diagnostic = { lastStage: e.phase });
        let n = t.activityStackPresentation.slots.find(
          ({ slotId: t }) => t === e.id,
        );
        n != null &&
          t.attached &&
          t.preparation.opacity <= 0.01 &&
          b?.presentsActivityStackScrollImmediately === !0 &&
          b.waitingRevisions.get(e.id) === e.revision &&
          (ae(e.id, n.itemId, e.revision),
          b.waitingRevisions.delete(e.id),
          R());
        return;
      }
      case `mounted`:
        ((t.diagnostic = { lastStage: e.phase }), d?.(e.id, e.phase));
        return;
      case `painted-before-attach`: {
        if (t.attached) return pe(t, e, `surface-already-attached`);
        if (b == null) return pe(t, e, `transition-missing`);
        let n = b.waitingRevisions.get(e.id);
        if (n !== e.revision)
          return pe(
            t,
            e,
            `transition-revision-mismatch:expected=${n ?? `none`}`,
          );
        if (
          ((t.diagnostic = { lastStage: e.phase }),
          d?.(e.id, e.phase),
          (t.attached =
            a?.attachSurface(e.id, t.window, t.preparation.cornerRadius) ?? !1),
          !t.attached)
        )
          return (
            (t.diagnostic = { lastStage: `native-attach-failed` }),
            u?.(e.id, `native-attached`),
            de(),
            `native-attach-failed`
          );
        ((t.diagnostic = { lastStage: `native-attached` }),
          d?.(e.id, `native-attached`),
          (t.preparation = {
            ...t.preparation,
            nativeAttached: !0,
            revision: t.preparation.revision + 1,
          }),
          l(t.window, t.preparation),
          b?.waitingRevisions.set(e.id, t.preparation.revision),
          R());
        return;
      }
      case `painted`: {
        if (!t.attached) return pe(t, e, `painted-before-native-attach`);
        let n = y.get(e.id);
        if (n?.revision === e.revision) {
          t.diagnostic = { lastStage: e.phase };
          let r = A,
            i = t.activityStackPresentation.slots.find(
              ({ slotId: t }) => t === e.id,
            ),
            o = r?.slots.find(({ slotId: t }) => t === e.id);
          if (r == null || i?.itemId !== n.itemId || o?.itemId !== n.itemId)
            return (
              u?.(e.id, `painted`),
              de(),
              `activity-stack-paint-assignment-mismatch`
            );
          (oe(e.id, e.revision),
            (t.preparation = If(t.preparation, i, o)),
            (t.activityStackPresentation = r),
            a?.animateSurface(Pf(t.preparation, 0)),
            (t.presentedPreparation = t.preparation),
            a?.presentNotificationStack(0),
            ee());
          return;
        }
        if (b == null) return pe(t, e, `transition-missing`);
        let r = b.waitingRevisions.get(e.id);
        if (r !== e.revision)
          return pe(
            t,
            e,
            `transition-revision-mismatch:expected=${r ?? `none`}`,
          );
        ((t.diagnostic = { lastStage: e.phase }),
          b.waitingRevisions.delete(e.id),
          R());
        return;
      }
    }
  }
  function R() {
    if (b == null || b.waitingRevisions.size > 0) return;
    let e = b;
    ((b = null), ue(), z(e));
  }
  function z(e) {
    a?.orderSurfaces(Hf(A?.slots ?? []));
    let t = P !== e.notificationStackExpanded,
      n = !1,
      r = 0;
    for (let t of e.retargetedActivitySurfaceIds) {
      let r = v.get(t);
      r != null &&
        (a?.animateSurface(Pf(r.preparation, 0)),
        e.retargetedReassignedActivitySurfaceIds.has(t) && (n = !0));
    }
    for (let t of e.hiddenSurfaceIds) {
      let i = v.get(t);
      if (i != null) {
        let a = ie(
          i,
          e.presentsActivityStackScrollImmediately ? 0 : e.animationDurationMs,
        );
        t.startsWith(`activity-slot-`) && ((n = !0), (r = Math.max(r, a)));
      }
    }
    for (let t of e.animatedSurfaceIds) {
      let i = v.get(t);
      if (i == null) continue;
      let a = te(
        i,
        e.presentsActivityStackScrollImmediately &&
          i.preparation.id !== `composer`
          ? 0
          : e.animationDurationMs,
      );
      t.startsWith(`activity-slot-`) && ((n = !0), (r = Math.max(r, a)));
    }
    if (
      (t &&
        ((P = e.notificationStackExpanded), a?.setNotificationStackExpanded(P)),
      !t && n)
    ) {
      a?.presentNotificationStack(r);
      for (let t of e.retargetedActivitySurfaceIds) {
        let e = v.get(t);
        e != null && (e.presentedPreparation = e.preparation);
      }
    }
    for (let t of e.visibleSurfaceIds) {
      let e = v.get(t);
      e != null &&
        (e.window.setOpacity(1),
        e.keyboardInteractive
          ? (e.window.setFocusable(!0), e.window.setIgnoreMouseEvents(!1))
          : e.window.showInactive(),
        re(e));
    }
    (e.visibleSurfaceIds.size > 0 && c(), ee());
  }
  function te(e, t) {
    let n = Pf(
      e.preparation,
      t ?? (e.preparation.id === `composer` || e.hasPresented ? Af : 0),
    );
    return e.attached
      ? (a?.animateSurface(n),
        (e.presentedPreparation = e.preparation),
        e.hasPresented || d?.(e.preparation.id, `presented`),
        (e.diagnostic = { lastStage: `presented` }),
        (e.hasPresented = !0),
        n.durationMs)
      : (e.window.setIgnoreMouseEvents(!0, { forward: !0 }),
        e.window.setOpacity(0),
        0);
  }
  function ne(e, t) {
    for (let n of t.slots) {
      let r = e.slots.find(({ slotId: e }) => e === n.slotId),
        i = v.get(n.slotId);
      if (r == null || i == null || !i.attached || y.has(n.slotId)) continue;
      ((i.preparation = If(i.preparation, r, n)),
        (i.activityStackPresentation = t));
      let o = Pf(i.preparation, 0);
      (a?.animateSurface(o), (i.presentedPreparation = i.preparation));
    }
    a?.presentNotificationStack(0);
  }
  function re(e) {
    if (e == null || !e.visible || !e.attached || e.preparation.id !== C)
      return;
    (e.window.show(),
      e.window.setFocusable(!0),
      e.window.setIgnoreMouseEvents(!1),
      (e.keyboardInteractive = !0));
    let t = !1,
      n = () => {
        t || !e.keyboardInteractive || ((t = !0), p?.(e.window));
      };
    (e.window.isFocused() || e.window.once(`focus`, n),
      o(),
      e.window.focus(),
      e.window.webContents.focus(),
      e.window.isFocused() && n(),
      (C = null));
  }
  function ie(e, t = Af) {
    (oe(e.preparation.id),
      (e.visible = !1),
      he(e.preparation.id, !1),
      (e.preparation = {
        ...e.preparation,
        revision: e.preparation.revision + 1,
      }));
    let n = { ...Pf(e.preparation, t), acceptsPointerEvents: !1, opacity: 0 };
    return e.attached
      ? (a?.animateSurface(n), n.durationMs)
      : (e.window.setOpacity(0), 0);
  }
  function ae(e, t, n) {
    oe(e);
    let r = setTimeout(() => {
      if (y.get(e)?.revision !== n) return;
      oe(e, n);
      let t = v.get(e);
      (t != null && h?.([me(t)]), u?.(e, `painted`), de());
    }, Mf);
    y.set(e, { itemId: t, revision: n, timeout: r });
  }
  function oe(e, t) {
    let n = y.get(e);
    n == null ||
      (t != null && n.revision !== t) ||
      (clearTimeout(n.timeout), y.delete(e));
  }
  function se() {
    for (let e of y.keys()) oe(e);
  }
  function ce() {
    (se(), (T = !1), (E = null), (D = null), (O = null));
  }
  function le() {
    (ue(),
      b?.waitingRevisions.size !== 0 &&
        (x = setTimeout(() => {
          if (b != null) {
            h?.(
              [...b.waitingRevisions.keys()].flatMap((e) => {
                let t = v.get(e);
                return t == null ? [] : [me(t)];
              }),
            );
            for (let e of b.waitingRevisions.keys())
              u?.(
                e,
                v.get(e)?.attached === !0 ? `painted` : `painted-before-attach`,
              );
          }
          de();
        }, Mf)));
  }
  function ue() {
    x != null && (clearTimeout(x), (x = null));
  }
  function de() {
    ((k = !0), ce(), ue(), (b = null), a?.detach(), fe(), s());
  }
  function fe() {
    let e = [...v.values()];
    v.clear();
    for (let t of e) t.window.isDestroyed() || t.window.close();
  }
  function pe(e, t, n) {
    return (
      (e.diagnostic = { ...e.diagnostic, lastRejectionReason: n }),
      m?.(t, n, me(e)),
      n
    );
  }
  function me(e) {
    return {
      attached: e.attached,
      hasPresented: e.hasPresented,
      id: e.preparation.id,
      lastRejectionReason: e.diagnostic.lastRejectionReason,
      lastStage: e.diagnostic.lastStage,
      revision: e.preparation.revision,
      transitionId:
        b?.waitingRevisions.has(e.preparation.id) === !0 ? b.id : null,
      visible: e.visible,
      webContentsId: e.window.webContents.id,
    };
  }
  function he(e, t) {
    let n = v.get(e);
    if (!t) {
      (C === e && (C = null), n != null && (n.keyboardInteractive = !1), _e());
      return;
    }
    n?.keyboardInteractive || C === e || ((C = e), re(n));
  }
  function ge(e) {
    if (w === e) {
      let t = e == null ? null : v.get(e);
      t != null && ve(t);
      return;
    }
    ((w = e), _e());
  }
  function _e() {
    for (let [e, t] of v) {
      let n = e === w;
      (t.window.setFocusable(t.keyboardInteractive),
        n
          ? (t.window.setIgnoreMouseEvents(!1), ve(t))
          : t.window.setIgnoreMouseEvents(!0, { forward: !0 }));
    }
  }
  function ve(e) {
    let t = ye(e);
    t != null &&
      e.window.webContents.sendInputEvent({
        type: `mouseMove`,
        ...t,
        movementX: 0,
        movementY: 0,
      });
  }
  function ye(e) {
    let n = t(),
      r = e.window.getContentBounds(),
      i =
        n.x -
        r.x -
        e.preparation.backingRect.left -
        e.preparation.presentationOffset.x,
      a =
        n.y -
        r.y -
        e.preparation.backingRect.top -
        e.preparation.presentationOffset.y;
    return i >= 0 && a >= 0 && i <= r.width && a <= r.height
      ? { x: i, y: a }
      : null;
  }
}
function Pf(e, t) {
  let n = e.presentationRect.height < e.platterRect.height;
  return {
    acceptsPointerEvents: e.acceptsPointerEvents,
    backingRect: e.backingRect,
    chromiumOverflowCornerRadius: n ? void 0 : e.chromiumOverflowCornerRadius,
    chromiumOverflowRect:
      n || e.chromiumOverflowRect == null ? void 0 : Jf(e.chromiumOverflowRect),
    chromiumPresentationRect: Jf(e.chromiumPresentationRect),
    durationMs: t,
    edgeZone: e.edgeZone,
    id: e.id,
    opacity: e.opacity,
    platterRect: Jf(e.platterRect),
    presentationOffset: e.presentationOffset,
    presentationRect: Jf(e.presentationRect),
    revision: e.revision,
    trailingAccessory:
      e.trailingAccessory == null
        ? void 0
        : { ...e.trailingAccessory, rect: Jf(e.trailingAccessory.rect) },
  };
}
function Ff(e) {
  return { height: Of, width: kf, x: e.x - 32, y: e.y - 256 };
}
function If(e, t, n) {
  let r = n.presentationRect.left - t.presentationRect.left,
    i = n.presentationRect.top - t.presentationRect.top,
    a = Rf(e.backingRect, { x: r, y: i });
  return {
    ...e,
    acceptsPointerEvents: n.visibleRect.height > 0,
    backingRect: a,
    chromiumOverflowRect:
      e.chromiumOverflowRect == null
        ? void 0
        : Rf(e.chromiumOverflowRect, { x: r, y: i }),
    chromiumPresentationRect: a,
    edgeZone: n.edgeZone,
    opacity: n.visibleRect.height > 0 ? 1 : 0,
    platterRect: a,
    presentationOffset: { x: 0, y: 0 },
    presentationRect: Lf(a, n.visibleRect, n.presentationRect),
    revision: e.revision + 1,
    trailingAccessory:
      e.trailingAccessory == null
        ? void 0
        : {
            ...e.trailingAccessory,
            rect: Rf(e.trailingAccessory.rect, { x: r, y: i }),
          },
  };
}
function Lf(e, t, n) {
  return {
    height: t.height,
    left: e.left + t.left - n.left,
    top: e.top + t.top - n.top,
    width: t.width,
  };
}
function Rf(e, t) {
  return { ...e, left: e.left + t.x, top: e.top + t.y };
}
function zf(e, t) {
  return (
    e.height === t.height &&
    e.left === t.left &&
    e.top === t.top &&
    e.width === t.width
  );
}
function Bf({
  currentPresentation: e,
  items: t,
  scrollOffset: r,
  targetScrollOffset: i,
}) {
  let a = Math.sign(i - r),
    o = Math.abs(i - r);
  if (
    Uf(
      e,
      n.tc({
        expanded: !0,
        items: t,
        scrollOffset: i,
        viewportRect: e.viewportRect,
      }),
    )
  )
    return i;
  let s = 0,
    c = Math.ceil(o);
  for (; s + 1 < c; ) {
    let i = Math.floor((s + c) / 2);
    Uf(
      e,
      n.tc({
        expanded: !0,
        items: t,
        scrollOffset: r + a * i,
        viewportRect: e.viewportRect,
      }),
    )
      ? (s = i)
      : (c = i);
  }
  return r + a * Math.min(o, Math.max(1, s));
}
function Vf({
  awaitingPaints: e,
  items: t,
  scrollOffset: r,
  targetScrollOffset: i,
  viewportRect: a,
}) {
  if (e.size === 0) return i;
  let o = (r) =>
    !n
      .tc({ expanded: !0, items: t, scrollOffset: r, viewportRect: a })
      .slots.some(({ slotId: t, visibleRect: n }) => e.has(t) && n.height > 0);
  if (o(i)) return i;
  let s = Math.sign(i - r),
    c = 0,
    l = Math.ceil(Math.abs(i - r));
  for (; c + 1 < l; ) {
    let e = Math.floor((c + l) / 2);
    o(r + s * e) ? (c = e) : (l = e);
  }
  return r + s * c;
}
function Hf(e) {
  let t = e.toSorted((e, t) => e.zIndex - t.zIndex).map(({ slotId: e }) => e),
    r = new Set(t);
  return [
    ...n.Xs.filter((e) => e !== `mascot-badge` && !r.has(e)),
    ...t,
    `mascot-badge`,
  ];
}
function Uf(e, t) {
  return (
    e.slots.length === t.slots.length &&
    e.slots.every(({ itemId: e, slotId: n }) =>
      t.slots.some((t) => t.itemId === e && t.slotId === n),
    )
  );
}
function Wf(e, t, n) {
  let r = t.slots.find(({ slotId: t }) => t === e)?.itemId;
  return r != null && r !== n.slots.find(({ slotId: t }) => t === e)?.itemId;
}
function Gf(e, t) {
  return (
    e.edgeZone === t.edgeZone &&
    e.opacity === t.opacity &&
    e.presentationOffset.x === t.presentationOffset.x &&
    e.presentationOffset.y === t.presentationOffset.y &&
    Kf(e.backingRect, t.rect) &&
    Kf(e.chromiumPresentationRect, t.chromiumPresentationRect) &&
    Kf(e.platterRect, t.platterRect) &&
    Kf(e.presentationRect, t.presentationRect)
  );
}
function Kf(e, t) {
  return (
    Math.abs(e.height - t.height) < jf &&
    Math.abs(e.left - t.left) < jf &&
    Math.abs(e.top - t.top) < jf &&
    Math.abs(e.width - t.width) < jf
  );
}
function qf(e, t) {
  return (
    e.acceptsPointerEvents === t.acceptsPointerEvents &&
    e.chromiumOverflowCornerRadius === t.chromiumOverflowCornerRadius &&
    ((e.chromiumOverflowRect == null && t.chromiumOverflowRect == null) ||
      (e.chromiumOverflowRect != null &&
        t.chromiumOverflowRect != null &&
        zf(e.chromiumOverflowRect, t.chromiumOverflowRect))) &&
    e.cornerRadius === t.cornerRadius &&
    e.edgeZone === t.edgeZone &&
    e.opacity === t.opacity &&
    e.platterRect.height === t.platterRect.height &&
    e.platterRect.left === t.platterRect.left &&
    e.platterRect.top === t.platterRect.top &&
    e.platterRect.width === t.platterRect.width &&
    e.chromiumPresentationRect.height === t.chromiumPresentationRect.height &&
    e.chromiumPresentationRect.left === t.chromiumPresentationRect.left &&
    e.chromiumPresentationRect.top === t.chromiumPresentationRect.top &&
    e.chromiumPresentationRect.width === t.chromiumPresentationRect.width &&
    e.presentationOffset.x === t.presentationOffset.x &&
    e.presentationOffset.y === t.presentationOffset.y &&
    e.presentationRect.height === t.presentationRect.height &&
    e.presentationRect.left === t.presentationRect.left &&
    e.presentationRect.top === t.presentationRect.top &&
    e.presentationRect.width === t.presentationRect.width &&
    e.backingRect.height === t.rect.height &&
    e.backingRect.left === t.rect.left &&
    e.backingRect.top === t.rect.top &&
    e.backingRect.width === t.rect.width &&
    (0, af.default)(e.trailingAccessory, t.trailingAccessory)
  );
}
function Jf(e) {
  return { ...e, left: 32 + e.left, top: 256 + e.top };
}
function Yf(e, t) {
  return e.size === t.size && [...e].every((e) => t.has(e));
}
var Xf = 180;
function Zf({
  createWindow: e,
  getCursorScreenPoint: t,
  getOverlayBounds: n,
  nativeBridge: r,
  onBeforeSurfaceFocus: i,
  onNativeAttachmentFailed: a,
  onNativePresentationOwned: o,
  onPreparationUpdated: s,
  onSurfaceLifecycleFailed: c,
  onSurfaceLifecycleTransition: l,
  onSurfaceFailed: u,
  onSurfaceFocus: d,
  onSurfaceReadyRejected: f,
  onSurfaceReadyTimeout: p,
  onActivityStackScrollOffsetPublished: m,
  overlayWindow: h,
}) {
  let g = null,
    _ = !1,
    v = null,
    y = null,
    b = !1,
    x,
    S = () => {
      b || ((b = !0), o());
    };
  return (
    (x = Nf({
      createWindow: e,
      getCursorScreenPoint: t,
      getOverlayBounds: n,
      getSurfaceContentState: $f,
      nativeBridge:
        r == null
          ? null
          : {
              animateSurface: (e) => {
                r.animateSurface(e);
              },
              attachSurface: (e, t, n) =>
                !_ || y == null
                  ? !1
                  : ((_ = r.attachMaterial({
                      ...y,
                      rootWindow: h,
                      surfaces: [{ cornerRadius: n, id: e, window: t }],
                    })),
                    _),
              consumePointerDownSurfaceId: () =>
                r.consumePointerDownSurfaceId(),
              consumePointerUpSurfaceId: () => r.consumePointerUpSurfaceId(),
              detach: () => {
                ((_ = !1), (v = null), (y = null), r.detach());
              },
              getPointerSurfaceId: () => r.getPointerSurfaceId(),
              orderSurfaces: (e) => {
                r.orderSurfaces(e);
              },
              presentNotificationStack: (e) => {
                r.presentNotificationStack(e);
              },
              setNotificationStackExpanded: (e) => {
                r.setNotificationStackExpanded(e);
              },
            },
      onBeforeSurfaceFocus: i,
      onNativeAttachmentFailed: a,
      onNativePresentationOwned: () => {
        if (v != null && r != null) {
          if (((_ = r.updateMaterial(v)), !_)) {
            (x.detach(), a());
            return;
          }
          ((y = v), (v = null));
        }
        S();
      },
      onPreparationUpdated: s,
      onSurfaceLifecycleFailed: c,
      onSurfaceLifecycleTransition: l,
      onSurfaceFailed: u,
      onSurfaceFocus: d,
      onSurfaceReadyRejected: f,
      onSurfaceReadyTimeout: p,
      onActivityStackScrollOffsetPublished: m,
      presentsNotificationStackExpansionImmediately: !0,
    })),
    {
      ...x,
      prepareSurfaces(e, t) {
        if (r == null) return;
        let n = Qf(e, t, 0),
          i =
            y != null &&
            (y.activityStackRect == null) != (n.activityStackRect == null),
          o = {
            ...n,
            activityStackDurationMs:
              (g != null && g !== t.isNotificationStackExpanded) || i ? Xf : 0,
          };
        g = t.isNotificationStackExpanded;
        let s = _,
          c = s && y?.activityStackRect == null && o.activityStackRect != null,
          l =
            s &&
            t.isWindowDragActive &&
            e.surfaces.length > 0 &&
            !(0, af.default)(y?.activityStackRect, o.activityStackRect),
          u = !c && !l && (!s || !(0, af.default)(y, o));
        if (
          ((v = c || l ? o : null),
          u &&
            ((_ = s
              ? r.updateMaterial(o)
              : r.attachMaterial({ ...o, rootWindow: h, surfaces: [] })),
            _ && (y = o)),
          !_)
        ) {
          (x.detach(), a());
          return;
        }
        (x.prepareSurfaces(e.surfaces, {
          animationDurationMs: t.isWindowDragActive ? 0 : void 0,
          activityStackItems: e.activityStackItems,
          activityStackPresentation: e.activityStackPresentation,
          activityStackScrollOffset: e.activityStackScrollOffset,
          contentState: t,
          isNotificationStackExpanded: t.isNotificationStackExpanded,
        }),
          e.surfaces.length === 0 && S());
      },
      updateMascotRect(e) {
        if (r == null || !_ || y == null) return;
        v != null && (v = { ...v, mascotRect: e });
        let t = { ...y, activityStackDurationMs: 0, mascotRect: e };
        if (((_ = r.updateMaterial(t)), !_)) {
          (x.detach(), a());
          return;
        }
        y = t;
      },
    }
  );
}
function Qf(e, t, r) {
  let i = e.surfaces.filter(
      ({ id: e, opacity: t, presentationRect: r }) =>
        n.$s.some((t) => t === e) && t > 0.01 && r.width > 0 && r.height > 0,
    ),
    a = null;
  if (t.isNotificationStackExpanded) {
    let t = i[0],
      n = e.activityStackPresentation.slots.find(
        ({ slotId: e }) => e === t?.id,
      );
    if (t != null && n != null) {
      let r = e.activityStackPresentation.viewportRect;
      a = {
        ...r,
        left: t.platterRect.left + r.left - n.presentationRect.left,
        top: t.platterRect.top + r.top - n.presentationRect.top,
      };
    }
  } else {
    let e = t.layout.placement.startsWith(`bottom`),
      r = null,
      o = null,
      s = 0;
    for (let { platterRect: t, presentationRect: n } of i) {
      let i = e ? n.top : n.top + n.height - t.height;
      ((r == null || (e ? i < r.top : i > r.top)) && (r = { ...t, top: i }),
        (o == null || t.top < o.top) && (o = t),
        (s = Math.max(s, t.width)));
    }
    if (r != null && o != null) {
      let t = n.rc({
        expandsTowardBottom: e,
        frontRect: o,
        nearEdgeRect: r,
        notificationCount: i.length,
      });
      a = {
        height: n.ic(i.length, t.height),
        left: t.left,
        top: t.top,
        width: s,
      };
    }
  }
  let o = 0;
  for (let e of i) e.opacity > o && (o = e.opacity);
  return {
    activityStackDurationMs: r,
    activityStackOpacity: a == null ? 0 : o,
    activityStackRect: a,
    mascotBadgeTint: n.Qs(t.activityStackItems.length === 0, t.activities),
    mascotRect: t.layout.mascot,
  };
}
function $f(e, t) {
  if (e === `composer` || e === `mascot-badge`) return t;
  let n = t.activityStackPresentation.slots.find(({ slotId: t }) => t === e),
    r = n?.itemId;
  return ep({
    ...t,
    activities: t.activities.filter(({ notification: e }) => e.id === r),
    activityStackPresentation: {
      ...t.activityStackPresentation,
      slots: n == null ? [] : [n],
    },
    expandedNotificationIds:
      r != null && t.expandedNotificationIds.includes(r) ? [r] : [],
    isQuickChatVisible: !1,
    pointerSurfaceId: t.pointerSurfaceId === e ? e : null,
    quickChatDraft: ``,
    quickChatResetRevision: 0,
  });
}
function ep(e) {
  let t =
    e.activityStackBackingLayoutHeight - e.activityStackVisibleLayoutHeight;
  return {
    ...e,
    activityStackVisibleLayoutHeight: e.activityStackBackingLayoutHeight,
    layout:
      e.layout.tray == null
        ? e.layout
        : {
            ...e.layout,
            tray: {
              ...e.layout.tray,
              height: e.layout.tray.height + t,
              top: e.layout.placement.startsWith(`top`)
                ? e.layout.tray.top - t
                : e.layout.tray.top,
            },
          },
  };
}
function tp(e) {
  let t = new Map();
  return {
    failed(n, r) {
      let i = t.get(n) ?? [],
        a = r ?? i.at(-1) ?? `created`;
      e({
        action: `detach-native-and-use-css-fallback`,
        failedPhase: a,
        id: n,
        trace: `${i.join(` -> `)} -> failed phase=${a} action=detach-native-and-use-css-fallback`,
      });
    },
    transition(n, r) {
      let i = r === `created` ? [] : (t.get(n) ?? []);
      i.includes(r) ||
        (i.push(r), t.set(n, i), e({ id: n, trace: i.join(` -> `) }));
    },
  };
}
var np = `http://localhost:5175/`;
function rp(e) {
  let t = c.app.isPackaged
    ? new URL(`${a.F}/avatar-overlay-composition-surface.html`)
    : new URL(
        `/avatar-overlay-composition-surface.html`,
        process.env.ELECTRON_RENDERER_URL || np,
      );
  return (t.searchParams.set(`surfaceId`, e), t.toString());
}
function ip({
  onFailure: e,
  onLifecycleTransition: t,
  parentWindow: n,
  platform: r = process.platform,
  preloadPath: i,
  registerWindow: a,
  surfaceId: o,
  surfaceUrl: s,
}) {
  let l = new c.BrowserWindow({
    acceptFirstMouse: !0,
    backgroundColor: `#00000000`,
    ...(r === `darwin` ? { enableLargerThanScreen: !0 } : {}),
    focusable: !1,
    frame: !1,
    hasShadow: !1,
    height: sf.height,
    parent: n,
    resizable: !1,
    show: !1,
    skipTaskbar: !0,
    title: `Pet Surface ${o}`,
    transparent: !0,
    webPreferences: {
      backgroundThrottling: !1,
      contextIsolation: !0,
      nodeIntegration: !1,
      preload: i,
    },
    width: sf.width,
  });
  return (
    l.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    l.webContents.on(`will-navigate`, (e, t) => {
      t !== s && e.preventDefault();
    }),
    l.webContents.on(`will-frame-navigate`, (e) => {
      (!e.isMainFrame || e.url !== s) && e.preventDefault();
    }),
    l.webContents.on(`will-redirect`, (e) => {
      e.preventDefault();
    }),
    a(l),
    t?.(`created`),
    l.setAlwaysOnTop(!0, `floating`),
    l.webContents.setBackgroundThrottling(!1),
    l.webContents.on(`did-finish-load`, () => {
      t?.(`did-finish-load`);
    }),
    l.webContents.on(`did-fail-load`, e),
    l.webContents.on(`render-process-gone`, e),
    l.on(`closed`, e),
    t?.(`load-url`),
    l.loadURL(s).catch(e),
    l
  );
}
var ap = 300,
  op = 32,
  sp = 100,
  cp = 8,
  lp = `avatar-overlay`,
  up = 0.5,
  dp = class {
    coordinator = null;
    lifecycle = tp((e) => {
      if (e.failedPhase == null) {
        r.r().info(`Native pet composition lifecycle`, {
          safe: { id: e.id, trace: e.trace },
        });
        return;
      }
      r.r().warning(`Native pet composition lifecycle`, {
        safe: {
          action: e.action,
          id: e.id,
          phase: e.failedPhase,
          trace: e.trace,
        },
      });
    });
    latestState = null;
    nativeMaterialAttached = !1;
    overlayWindow = null;
    pointerSurfaceExitTimer = null;
    pointerSurfaceHovered = !1;
    remoteHostedPIPContentHostRegistered = !1;
    pointerSurfaceId = null;
    pointerSyncTimer = null;
    remoteHostedPIPMascotRect = null;
    scrollSettleTimer = null;
    scrollSyncTimer = null;
    nextCompositionSessionId = 0;
    pendingRecoveryAttempt = !1;
    recoveryAttemptSessionId = null;
    tucked = !1;
    unavailable = !1;
    constructor(e, t, n, r = () => !0) {
      ((this.windowManager = e),
        (this.nativeBridge = t),
        (this.onNativeMaterialStateChanged = n),
        (this.isRemoteHostedPIPEnabled = r));
    }
    setOverlayWindow(e) {
      e !== this.overlayWindow &&
        (this.detach(),
        (this.overlayWindow = e),
        this.publishRemoteHostedPIPContentHost());
    }
    isEnabled() {
      return this.latestState != null;
    }
    isNativeMaterialAttached() {
      return this.nativeMaterialAttached;
    }
    getDevelopmentDiagnostics(e = `include`) {
      let t = this.latestState,
        r =
          t?.measurements.surfaces.map(
            ({ id: e, opacity: t, presentationRect: n }) => ({
              id: e,
              opacity: t,
              presentationRect: n,
            }),
          ) ?? [];
      if (t != null && !t.contentState.isNotificationStackExpanded) {
        let e = t.contentState.layout.placement.startsWith(`bottom`),
          i = t.measurements.surfaces
            .filter(
              ({ id: e, opacity: t, presentationRect: r }) =>
                n.$s.some((t) => t === e) && t > 0.01 && r.height > 0,
            )
            .map(({ id: t, platterRect: n, presentationRect: r }) => ({
              frontRect: n,
              id: t,
              nearEdgeRect: {
                ...n,
                top: e ? r.top : r.top + r.height - n.height,
              },
            }))
            .sort((e, t) => e.frontRect.top - t.frontRect.top),
          a = i[0]?.frontRect,
          o = i
            .map((e) => e.nearEdgeRect)
            .sort((t, n) => (e ? t.top - n.top : n.top - t.top))[0],
          s =
            a == null || o == null
              ? null
              : n.rc({
                  expandsTowardBottom: e,
                  frontRect: a,
                  nearEdgeRect: o,
                  notificationCount: i.length,
                });
        s != null &&
          (r = r.flatMap((e) => {
            let t = i.findIndex(({ id: t }) => t === e.id);
            if (t < 0) return [e];
            let r = n.ec[t];
            return r == null ? [] : [{ ...e, presentationRect: n.ac(s, r) }];
          }));
      }
      return {
        hierarchy:
          e === `include` ? (this.nativeBridge?.dumpHierarchy() ?? null) : null,
        diagnosticEvents: this.nativeBridge?.getDiagnosticEvents() ?? [],
        desiredPresentations: r,
        mascotSize: this.latestState?.measurements.mascot ?? null,
        nativeAnimating: this.nativeBridge?.isAnimating() ?? !1,
        nativeMaterialAttached: this.nativeMaterialAttached,
        nativePresentations: this.nativeBridge?.getPresentations() ?? [],
      };
    }
    getCursorPosition() {
      return this.nativeBridge?.getCursorPosition() ?? null;
    }
    isPrimaryMouseButtonPressed() {
      return this.nativeBridge?.isPrimaryMouseButtonPressed() ?? !1;
    }
    moveBackingCanvases() {
      (this.coordinator?.moveBackingCanvases(),
        this.publishRemoteHostedPIPContentHost());
    }
    performWindowDrag() {
      return this.overlayWindow == null || this.overlayWindow.isDestroyed()
        ? !1
        : (this.nativeBridge?.performWindowDrag(this.overlayWindow) ?? !1);
    }
    updateMascotRect(e) {
      ((this.remoteHostedPIPMascotRect = e),
        this.coordinator?.updateMascotRect(e),
        this.publishRemoteHostedPIPContentHost());
    }
    setOverlayWindowPosition(e) {
      return this.overlayWindow == null || this.overlayWindow.isDestroyed()
        ? !1
        : (this.nativeBridge?.setOverlayWindowPosition(this.overlayWindow, e) ??
            !1);
    }
    setFeedbackDiagnosticsEnabled(e) {
      this.nativeBridge?.setFeedbackDiagnosticsEnabled(e);
    }
    tuck() {
      this.tucked ||
        ((this.tucked = !0),
        this.unavailable &&
          ((this.pendingRecoveryAttempt = !0),
          r.r().info(`Native pet composition recovery boundary`, {
            safe: { sessionId: this.nextCompositionSessionId },
          })),
        (this.unavailable = !1),
        this.detachCoordinator());
    }
    wake() {
      this.tucked &&
        ((this.tucked = !1),
        this.nativeBridge?.consumeScreensDidWake(),
        this.latestState != null && this.ensureCoordinator());
    }
    setState(e, t) {
      if (this.overlayWindow?.webContents.id === e) {
        if (t == null) {
          ((this.latestState = null), this.detachCoordinator());
          return;
        }
        if (
          ((this.latestState = {
            contentState: t.contentState,
            measurements: t.measurements,
          }),
          this.publishRemoteHostedPIPContentHost(),
          !(this.nativeBridge == null || this.tucked || this.unavailable))
        ) {
          if (this.coordinator == null) {
            this.ensureCoordinator();
            return;
          }
          this.prepareLatestState();
        }
      }
    }
    handleSurfaceReady(e, t) {
      let n = this.coordinator?.getSurfaceId({ id: e });
      if (n !== t.id) {
        let i = `surface-id-mismatch:expected=${n ?? `none`}`;
        return (
          r.r().warning(`Native pet composition surface ready rejected`, {
            safe: {
              id: t.id,
              phase: t.phase,
              reason: i,
              revision: t.revision,
              surfaceId: n ?? `none`,
              webContentsId: e,
            },
          }),
          i
        );
      }
      return this.coordinator?.surfaceReady(t) ?? `coordinator-missing`;
    }
    handleSurfaceAction(e, t) {
      let n = this.overlayWindow?.webContents.id === e,
        r = n ? null : (this.coordinator?.getSurfaceId({ id: e }) ?? null);
      if (!(!n && r == null)) {
        if (
          t.type === `scroll-activity-stack` &&
          this.coordinator?.scrollActivityStack(t.deltaY) === !0
        ) {
          this.scheduleActivityStackScrollOffsetPublication();
          return;
        }
        switch (t.type) {
          case `activate-notification`:
            break;
          case `close-notification-stack`:
            this.coordinator?.setNotificationStackExpanded(!1);
            break;
          case `open-notification-stack`:
            this.coordinator?.setNotificationStackExpanded(!0);
            break;
          case `quick-chat-active-changed`:
            this.coordinator?.setSurfaceKeyboardInteraction(
              `composer`,
              t.isActive,
            );
            break;
          case `run-notification-control`:
            switch (t.action.type) {
              case `open-follow-up`:
                r != null &&
                  this.coordinator?.setSurfaceKeyboardInteraction(r, !0);
                break;
              case `close-follow-up`:
              case `stop`:
                r != null &&
                  this.coordinator?.setSurfaceKeyboardInteraction(r, !1);
                break;
              case `submit-follow-up`:
                break;
            }
            break;
          case `activity-stack-scroll-offset-changed`:
          case `composition-pointer-surface-changed`:
          case `dismiss-notification`:
          case `notification-expansion-changed`:
          case `quick-chat-draft-changed`:
          case `quick-chat-surface-hover-changed`:
          case `quick-chat-visibility-changed`:
          case `run-notification-action`:
          case `scroll-activity-stack`:
          case `submit-question-option`:
          case `submit-quick-chat`:
            break;
        }
        n || this.sendActionToRoot(t);
      }
    }
    getSurfaceHost(e) {
      let t = this.coordinator?.getSurfaceId(e);
      return (
        t != null && this.lifecycle.transition(t, `connected`),
        t == null ? null : new fp(t, e.id, this)
      );
    }
    getPreparation(e) {
      return this.coordinator?.getPreparation(e) ?? null;
    }
    detach() {
      ((this.latestState = null),
        (this.pendingRecoveryAttempt = !1),
        (this.recoveryAttemptSessionId = null),
        (this.tucked = !1),
        (this.unavailable = !1),
        (this.remoteHostedPIPMascotRect = null),
        this.detachCoordinator(),
        this.unregisterRemoteHostedPIPContentHost());
    }
    ensureCoordinator() {
      let e = this.overlayWindow;
      e == null ||
        e.isDestroyed() ||
        this.nativeBridge == null ||
        this.createCoordinator(e, this.nativeBridge);
    }
    createCoordinator(e, t) {
      let i = this.nextCompositionSessionId + 1;
      ((this.nextCompositionSessionId = i),
        this.pendingRecoveryAttempt &&
          ((this.pendingRecoveryAttempt = !1),
          (this.recoveryAttemptSessionId = i),
          r.r().info(`Native pet composition recovery attempt`, {
            safe: { sessionId: i },
          })));
      let a = new Map(),
        o = null;
      ((o = Zf({
        createWindow: (t) => {
          let n = ip({
            onFailure: () => {
              o?.surfaceFailed(t);
            },
            onLifecycleTransition: (e) => {
              this.lifecycle.transition(t, e);
            },
            parentWindow: e,
            preloadPath: (0, u.join)(
              __dirname,
              `avatar-overlay-composition-surface-preload.js`,
            ),
            registerWindow: (e) =>
              this.windowManager.registerAuxiliaryWindow(
                e,
                `avatarOverlay`,
                `exclude`,
              ),
            surfaceId: t,
            surfaceUrl: rp(t),
          });
          return (a.set(t, n), n);
        },
        getCursorScreenPoint: () => c.screen.getCursorScreenPoint(),
        getOverlayBounds: () =>
          this.overlayWindow?.getContentBounds() ?? { x: 0, y: 0 },
        nativeBridge: t,
        onBeforeSurfaceFocus: () => {
          c.app.focus({ steal: !0 });
        },
        onNativeAttachmentFailed: () => {
          (r.r().warning(`Native pet composition attachment failed`, {
            safe: { sessionId: i },
          }),
            this.recoveryAttemptSessionId === i &&
              ((this.recoveryAttemptSessionId = null),
              r.r().warning(`Native pet composition recovery failed`, {
                safe: { sessionId: i },
              })),
            (this.unavailable = !0),
            (this.nativeMaterialAttached = !1),
            this.onNativeMaterialStateChanged());
        },
        onNativePresentationOwned: () => {
          (r.r().info(`Native pet composition presentation owned`, {
            safe: { sessionId: i },
          }),
            this.recoveryAttemptSessionId === i &&
              ((this.recoveryAttemptSessionId = null),
              r.r().info(`Native pet composition recovery succeeded`, {
                safe: { sessionId: i },
              })),
            this.setNativeMaterialAttached(!0));
        },
        onPreparationUpdated: (e, t) => {
          let o = a.get(t.id);
          o != null &&
            (r.r().info(`Native pet composition preparation sent`, {
              safe: {
                backingRect: t.backingRect,
                backingWindowBounds: o.getContentBounds(),
                chromiumPresentationRect: t.chromiumPresentationRect,
                id: t.id,
                nativeAttached: t.nativeAttached,
                overlayBounds: this.overlayWindow?.getContentBounds() ?? null,
                platterRect: t.platterRect,
                presentationOffset: t.presentationOffset,
                presentationRect: t.presentationRect,
                revision: t.revision,
                sessionId: i,
                webContentsId: o.webContents.id,
              },
            }),
            o.webContents.send(n.Zs, t));
        },
        onSurfaceLifecycleFailed: (e, t) => {
          this.lifecycle.failed(e, t);
        },
        onSurfaceLifecycleTransition: (e, t) => {
          this.lifecycle.transition(e, t);
        },
        onSurfaceFocus: (e) => {
          e.webContents.send(n.Js, void 0);
        },
        overlayWindow: e,
        onSurfaceFailed: (e) => {
          r.r().warning(`Native pet composition surface failed`, {
            safe: { id: e },
          });
        },
        onSurfaceReadyRejected: (e, t, n) => {
          t === `surface-hidden` ||
            t === `transition-missing` ||
            t.startsWith(`revision-mismatch:`) ||
            t.startsWith(`transition-revision-mismatch:`) ||
            r.r().warning(`Native pet composition readiness rejected`, {
              safe: {
                ...n,
                phase: e.phase,
                readyRevision: e.revision,
                reason: t,
                sessionId: i,
              },
            });
        },
        onSurfaceReadyTimeout: (e) => {
          r.r().warning(`Native pet composition surface timed out`, {
            safe: {
              sessionId: i,
              surfaces: e
                .map(({ id: e, revision: t }) => `${e}:${t}`)
                .join(`,`),
            },
          });
          for (let t of e) {
            let e = a.get(t.id);
            e != null &&
              r.r().warning(`Native pet composition pending window state`, {
                safe: {
                  backgroundThrottling: e.webContents.getBackgroundThrottling(),
                  destroyed: e.isDestroyed(),
                  attached: t.attached,
                  hasPresented: t.hasPresented,
                  id: t.id,
                  lastRejectionReason: t.lastRejectionReason ?? null,
                  lastStage: t.lastStage,
                  loading: e.webContents.isLoading(),
                  opacity: e.getOpacity(),
                  overlayOpacity:
                    this.overlayWindow == null ||
                    this.overlayWindow.isDestroyed()
                      ? null
                      : this.overlayWindow.getOpacity(),
                  overlayVisible:
                    this.overlayWindow != null &&
                    !this.overlayWindow.isDestroyed() &&
                    this.overlayWindow.isVisible(),
                  revision: t.revision,
                  sessionId: i,
                  transitionId: t.transitionId,
                  visible: e.isVisible(),
                  webContentsId: e.webContents.id,
                },
              });
          }
        },
        onActivityStackScrollOffsetPublished: (e) => {
          this.sendActionToRoot({
            type: `activity-stack-scroll-offset-changed`,
            offset: e,
          });
        },
      })),
        (this.coordinator = o),
        (this.scrollSyncTimer = setInterval(() => {
          let e = t.consumeActivityStackScrollDeltaY();
          e !== 0 &&
            o.scrollActivityStack(e) &&
            this.scheduleActivityStackScrollOffsetPublication();
        }, cp)),
        this.scrollSyncTimer.unref());
      let s = (e, t, n = !1) => {
          let r = a.get(e),
            i = o.getPreparation(e);
          if (r == null || i == null) return;
          let s = r.getContentBounds(),
            l = c.screen.getCursorScreenPoint(),
            u = l.x - s.x - i.backingRect.left - i.presentationOffset.x,
            d = l.y - s.y - i.backingRect.top - i.presentationOffset.y,
            f;
          switch (t) {
            case `mouseDown`:
            case `mouseUp`:
              f = { button: `left`, clickCount: 1, type: t, x: u, y: d };
              break;
            case `mouseLeave`:
              f = { type: t, x: u, y: d };
              break;
            case `mouseMove`:
              f = { movementX: 0, movementY: 0, type: t, x: u, y: d };
              break;
          }
          (n &&
            (t === `mouseLeave` || t === `mouseMove`) &&
            (f.modifiers = [`leftbuttondown`]),
            r.webContents.sendInputEvent(f));
        },
        l = null,
        d = null;
      ((this.pointerSyncTimer = setInterval(() => {
        if (t.consumeScreensDidWake()) {
          (r.r().info(`Rebuilding native pet composition after screen wake`),
            this.tuck(),
            this.wake());
          return;
        }
        let e = o.getPointerSurfaceId();
        (l?.startsWith(`activity-slot-`) &&
          l !== e &&
          s(l, `mouseLeave`, d === l),
          (l = e),
          e !== this.pointerSurfaceId &&
            ((this.pointerSurfaceId = e),
            this.sendActionToRoot({
              type: `composition-pointer-surface-changed`,
              surfaceId: e,
            })),
          o.setPointerInteractionSurface(e),
          e == null
            ? this.pointerSurfaceHovered &&
              this.pointerSurfaceExitTimer == null &&
              (this.pointerSurfaceExitTimer = setTimeout(() => {
                ((this.pointerSurfaceExitTimer = null),
                  o.getPointerSurfaceId() ??
                    ((this.pointerSurfaceHovered = !1),
                    this.sendActionToRoot({
                      type: `quick-chat-surface-hover-changed`,
                      isHovered: !1,
                    })));
              }, ap))
            : (e.startsWith(`activity-slot-`) && s(e, `mouseMove`, d === e),
              this.pointerSurfaceExitTimer != null &&
                (clearTimeout(this.pointerSurfaceExitTimer),
                (this.pointerSurfaceExitTimer = null)),
              this.pointerSurfaceHovered ||
                ((this.pointerSurfaceHovered = !0),
                this.sendActionToRoot({
                  type: `quick-chat-surface-hover-changed`,
                  isHovered: !0,
                }))));
        let n = o.consumePointerDownSurfaceId();
        n === `composer`
          ? o.setSurfaceKeyboardInteraction(`composer`, !0)
          : n?.startsWith(`activity-slot-`) &&
              this.latestState != null &&
              !this.latestState.contentState.isNotificationStackExpanded &&
              this.latestState.contentState.activities.length > 1
            ? (o.setNotificationStackExpanded(!0),
              this.sendActionToRoot({ type: `open-notification-stack` }))
            : n?.startsWith(`activity-slot-`) && ((d = n), s(n, `mouseDown`));
        let i = o.consumePointerUpSurfaceId();
        d != null && i === d && (s(d, `mouseUp`), (d = null));
      }, op)),
        this.pointerSyncTimer.unref(),
        this.prepareLatestState());
    }
    prepareLatestState() {
      this.latestState == null ||
        this.coordinator == null ||
        this.coordinator.prepareSurfaces(
          this.latestState.measurements,
          this.latestState.contentState,
        );
    }
    scheduleActivityStackScrollOffsetPublication() {
      (this.scrollSettleTimer != null && clearTimeout(this.scrollSettleTimer),
        (this.scrollSettleTimer = setTimeout(() => {
          ((this.scrollSettleTimer = null),
            this.coordinator?.publishActivityStackScrollOffset());
        }, sp)),
        this.scrollSettleTimer.unref());
    }
    sendActionToRoot(e) {
      let t = this.overlayWindow;
      t == null ||
        t.isDestroyed() ||
        this.windowManager.sendMessageToWebContents(t.webContents, {
          type: `avatar-overlay-composition-action`,
          action: e,
        });
    }
    detachCoordinator() {
      (this.pointerSurfaceExitTimer != null &&
        (clearTimeout(this.pointerSurfaceExitTimer),
        (this.pointerSurfaceExitTimer = null)),
        this.pointerSurfaceHovered &&
          ((this.pointerSurfaceHovered = !1),
          this.sendActionToRoot({
            type: `quick-chat-surface-hover-changed`,
            isHovered: !1,
          })),
        this.pointerSurfaceId != null &&
          ((this.pointerSurfaceId = null),
          this.sendActionToRoot({
            type: `composition-pointer-surface-changed`,
            surfaceId: null,
          })),
        this.pointerSyncTimer != null &&
          (clearInterval(this.pointerSyncTimer),
          (this.pointerSyncTimer = null)),
        this.scrollSyncTimer != null &&
          (clearInterval(this.scrollSyncTimer), (this.scrollSyncTimer = null)),
        this.scrollSettleTimer != null &&
          (clearTimeout(this.scrollSettleTimer),
          (this.scrollSettleTimer = null)),
        this.coordinator?.detach(),
        (this.coordinator = null),
        this.setNativeMaterialAttached(!1));
    }
    setNativeMaterialAttached(e) {
      this.nativeMaterialAttached !== e &&
        ((this.nativeMaterialAttached = e),
        e && this.publishRemoteHostedPIPContentHost(),
        this.onNativeMaterialStateChanged());
    }
    publishRemoteHostedPIPContentHost() {
      let e = this.overlayWindow,
        t =
          this.remoteHostedPIPMascotRect ??
          this.latestState?.contentState.layout.mascot;
      if (!this.isRemoteHostedPIPEnabled() || e == null || e.isDestroyed()) {
        this.unregisterRemoteHostedPIPContentHost();
        return;
      }
      if (t == null) return;
      let n = t.width * up,
        r = t.height * up,
        i = { x: t.left + t.width / 2, y: t.top + t.height / 2 },
        a = { x: i.x - n / 2, y: i.y - r / 2, width: n, height: r },
        o = pp(e, t);
      ao(`avatar-overlay`, e, {
        anchorRect: a,
        anchors: [
          {
            alignment: o,
            point:
              o === `top-left`
                ? { x: t.left + t.width, y: t.top + t.height }
                : i,
          },
        ],
        presentationScope: `all`,
      }) && (this.remoteHostedPIPContentHostRegistered = !0);
    }
    unregisterRemoteHostedPIPContentHost() {
      this.remoteHostedPIPContentHostRegistered &&
        ((this.remoteHostedPIPContentHostRegistered = !1), oo(lp));
    }
  },
  fp = class extends a.P {
    constructor(e, t, n) {
      (super(), (this.id = e), (this.webContentsId = t), (this.host = n));
    }
    dispatchAction(e) {
      this.host.handleSurfaceAction(this.webContentsId, e);
    }
    getPreparation() {
      let e = this.host.getPreparation(this.id);
      if (e == null)
        throw Error(`Missing avatar overlay composition surface: ${this.id}`);
      return e;
    }
    surfaceReady(e) {
      if (e.id !== this.id)
        throw Error(`Unexpected avatar overlay composition surface: ${e.id}`);
      return this.host.handleSurfaceReady(this.webContentsId, e);
    }
  };
function pp(e, t) {
  let n = e.getContentBounds(),
    r = { x: n.x + t.left, y: n.y + t.top, width: t.width, height: t.height },
    i = c.screen.getDisplayNearestPoint({
      x: r.x + r.width / 2,
      y: r.y + r.height / 2,
    }).workArea;
  switch (mf(r, i)) {
    case `top-start`:
      return `bottom-left`;
    case `top-end`:
      return `bottom-right`;
    case `bottom-start`:
      return `top-left`;
    case `bottom-end`:
      return `top-right`;
  }
}
var mp = 500;
function hp({ isEnabled: e, isMacOS: t, preferenceStore: n }) {
  let r = new Map(),
    i = new Map(),
    o = new Map(),
    s = new Set(),
    l = new Map(),
    u = !1,
    d = e(),
    f,
    p = [],
    m,
    h = !0,
    g = null,
    _ = null,
    v = null,
    y = !1,
    b = (e, t) => !1,
    x = new Set(),
    S = !1,
    C = !1,
    w = !1,
    T = () => {
      let n = f ?? null,
        r = n != null && t && e() && u && $a(),
        i = m;
      if (
        (i != null &&
          i.conversationId !== n &&
          i.isActive &&
          se(i.conversationId, !1),
        n == null)
      ) {
        m = void 0;
        return;
      }
      (i?.conversationId === n && i.isActive === r) ||
        ((m = { conversationId: n, isActive: r }), se(n, r));
    },
    E = () => {
      _ != null || !t || !e() || ((_ = setInterval(T, mp)), _.unref(), T());
    },
    D = () => {
      _ != null && (clearInterval(_), (_ = null));
    },
    O = () => {
      !t || !e() || ((u ||= qa()), u && (F(), P(), I(), ee(), E()));
    },
    k = (n) => {
      let r = e(),
        i = r ? n : null;
      if (!(!t || f === i)) {
        if (r) {
          if ((O(), !u)) return;
        } else if (f === void 0) {
          f = null;
          return;
        }
        ((f = i), Ya(i), T());
      }
    },
    A = (n) => {
      let r = e(),
        i = r ? Array.from(n).sort() : [];
      !t ||
        (p.length === i.length && p.every((e, t) => e === i[t])) ||
        (r && (O(), !u)) ||
        ((p = i), Xa(i));
    },
    j = (e) => {
      ((x = new Set(e)), ne());
    },
    M = (e) => {
      (j(e),
        v?.({
          hiddenThreadIds: Array.from(x).sort(),
          type: `remote-hosted-pip-hidden-thread-ids-requested`,
        }));
    },
    N = (n) => {
      ((h = n),
        v?.({
          conversationIds: [],
          isVisible: n,
          type: `remote-hosted-pip-visibility-requested`,
        }),
        !(!t || !e()) && (O(), u && to(n)));
    };
  function P() {
    C ||
      !t ||
      (C = ro((e, t) => {
        M(_p(x, t, e));
      }));
  }
  function F() {
    w ||
      !t ||
      g == null ||
      (w = no(() => {
        g?.();
      }));
  }
  function I() {
    S ||
      !t ||
      (S = Qa((e) => {
        n.set(a.T, e);
      }));
  }
  function ee() {
    if (y || !t) return;
    y = !0;
    let e = n.getStored(a.T);
    e != null && Za(e);
  }
  let L = (e) => {
      let t = o.get(e);
      t != null && l.get(t) === e && (oo(t), l.delete(t));
    },
    R = (e) => {
      (L(e), o.delete(e), i.delete(e), s.delete(e));
    },
    z = () => {
      (A(new Set()), k(null), D());
      for (let e of new Set(o.values())) oo(e);
      (o.clear(),
        s.clear(),
        l.clear(),
        oo(lp),
        (w &&= (no(null), !1)),
        (u &&= (Ja(), !1)));
    },
    te = (t, n) => {
      if (!e() || n.anchorRect == null) {
        R(t.id);
        return;
      }
      let r = o.get(t.id);
      if ((r != null && r !== n.hostId && (L(t.id), o.delete(t.id)), O(), !u))
        return;
      o.set(t.id, n.hostId);
      let i = n.animated && eo();
      ao(n.hostId, t, {
        anchors: n.anchors,
        anchorRect: n.anchorRect,
        animated: i,
        presentationScope: n.presentationScope,
      }) && l.set(n.hostId, t.id);
    },
    ne = () => {
      let e = new Set(),
        t = new Set();
      for (let t of x) e.add(t);
      for (let n of c.BrowserWindow.getAllWindows()) {
        let i = r.get(n.id);
        i != null && b(n, i) && (e.add(i), t.add(i));
      }
      A(e);
      let n = c.BrowserWindow.getFocusedWindow();
      if (n == null) return;
      let a = r.get(n.id);
      if (a === void 0) return;
      let o = i.get(n.id);
      o != null && s.has(n.id) && l.get(o.hostId) !== n.id && te(n, o);
      let u = a != null && t.has(a);
      k(s.has(n.id) && !u ? a : null);
    },
    re = (e, n) => {
      let i = c.BrowserWindow.fromWebContents(e);
      if (!(i == null || !t)) {
        if (vp(e.getURL())) {
          r.delete(i.id);
          return;
        }
        (r.set(i.id, n), i.isFocused() && ne());
      }
    },
    ie = (t, n) => {
      if ((i.set(t.id, n), !e())) {
        (L(t.id), o.delete(t.id), s.delete(t.id), t.isFocused() && ne());
        return;
      }
      if (n.anchorRect == null) {
        (R(t.id), t.isFocused() && ne());
        return;
      }
      (s.add(t.id),
        (t.isFocused() || l.get(n.hostId) === t.id) && te(t, n),
        t.isFocused() && ne());
    },
    ae = (e, n) => {
      let r = c.BrowserWindow.fromWebContents(e);
      r == null || !t || ie(r, n);
    },
    oe = (e) => {
      if (!t) return;
      let n = (t) => {
          if (vp(t)) {
            r.delete(e.id);
            return;
          }
          let n = gp(t);
          (r.set(e.id, n), e.isFocused() && ne());
        },
        i = (e, t) => {
          n(t);
        },
        a = (e, t, r) => {
          r && n(t);
        },
        { webContents: o } = e;
      (o.on(`did-navigate`, i),
        o.on(`did-navigate-in-page`, a),
        e.on(`focus`, ne),
        e.on(`closed`, () => {
          (r.delete(e.id),
            R(e.id),
            o.off(`did-navigate`, i),
            o.off(`did-navigate-in-page`, a),
            ne());
        }),
        n(o.getURL()));
    },
    se = (e, t) => {
      v?.({
        conversationId: e,
        isActive: t,
        type: `remote-hosted-pip-stream-state-changed`,
      });
    };
  return {
    dispose: () => {
      (z(), i.clear(), (S &&= (Qa(null), !1)), (C &&= (ro(null), !1)));
    },
    ensureContentHostStarted: O,
    reconcileEnabledState: () => {
      let t = e();
      if (t) {
        if ((O(), !d))
          for (let e of c.BrowserWindow.getAllWindows()) {
            let t = i.get(e.id);
            t != null && ie(e, t);
          }
        (N(h), ne());
      } else d && z();
      d = t;
    },
    registerWindow: oe,
    syncActiveThreadForFocusedWindow: ne,
    setThreadSuppressionPredicate: (e) => {
      ((b = e), ne());
    },
    setHiddenThreadIDs: j,
    setActiveThreadForWebContents: re,
    setHostLayoutForWebContents: ae,
    setPetWakeRequestHandler: (e) => {
      ((g = e), e == null && w ? (no(null), (w = !1)) : e != null && u && F());
    },
    setVisible: N,
    setWindowMessageSender: (e) => {
      ((v = e), m == null ? T() : se(m.conversationId, m.isActive));
    },
  };
}
function gp(e) {
  try {
    let t = new URL(e);
    return yp(t.pathname) ?? yp(t.searchParams.get(`initialRoute`));
  } catch {
    return null;
  }
}
function _p(e, t, n) {
  let r = new Set(e);
  for (let e of t) n ? r.delete(e) : r.add(e);
  return Array.from(r).sort();
}
function vp(e) {
  try {
    let t = new URL(e),
      n = t.searchParams.get(`initialRoute`);
    return (
      t.pathname === `/avatar-overlay` ||
      t.pathname === `/avatar-overlay-composition-surface.html` ||
      n === `/avatar-overlay`
    );
  } catch {
    return !1;
  }
}
function yp(e) {
  let t = e?.match(/^\/local\/([^/?#]+)/);
  if (t == null) return null;
  try {
    return decodeURIComponent(t[1]);
  } catch {
    return t[1];
  }
}
function bp({ ensureWindow: e, showPrimaryWindow: t }) {
  let n = [],
    i = new Set(),
    a = !1,
    o = null,
    s = () => {
      if (!a) return Promise.resolve();
      if (o != null) return o;
      let r = n.splice(0);
      return (
        (o = (async () => {
          for (let n of r)
            await xp(n, { ensureWindow: e, showPrimaryWindow: t });
        })().finally(() => {
          ((o = null), n.length > 0 && s());
        })),
        o
      );
    };
  return {
    onRecovery: (e) => {
      let t = `${e.databasePath}\0${e.backupFolder}`;
      i.has(t) ||
        (i.add(t),
        n.push(e),
        r.r().info(`Queued app-server SQLite recovery confirmation`, {
          safe: { pendingNotifications: n.length },
          sensitive: {
            backupFolder: e.backupFolder,
            databasePath: e.databasePath,
          },
        }),
        s());
    },
    enable: async () => {
      ((a = !0), await s());
    },
  };
}
async function xp(e, { ensureWindow: t, showPrimaryWindow: n }) {
  r.r().info(`Showing app-server SQLite recovery confirmation`, {
    safe: {},
    sensitive: { backupFolder: e.backupFolder, databasePath: e.databasePath },
  });
  let i = await t();
  (n({ stealFocus: !0 }),
    await a.n(e.databasePath, e.backupFolder, i).catch((e) => {
      r.r().warning(`Failed to show app-server SQLite recovery confirmation`, {
        safe: {},
        sensitive: { error: e },
      });
    }));
}
var Sp = `state_5.sqlite`,
  Cp = `sqlite`,
  wp = `.app-server-state-reconciled-v1`,
  Tp = 200,
  Ep = n.xl({
    type: n.vl(`session_meta`),
    payload: n.xl({
      id: n.wl(),
      cwd: n.wl().refine((e) => e.trim().length > 0),
    }),
  });
async function Dp(e) {
  if (!(process.env.CODEX_SQLITE_HOME != null || n.li())) {
    try {
      let t = await jp(e);
      t != null &&
        r.r().info(`Reconciled temporary app-server state`, { safe: t });
    } catch (e) {
      r.r().warning(`Failed to reconcile temporary app-server state`, {
        safe: { errorName: e instanceof Error ? e.name : `UnknownError` },
      });
    }
    try {
      let t = await Op(e);
      t > 0 &&
        r.r().info(`Repaired missing thread cwd metadata`, {
          safe: { repaired: t },
        });
    } catch (e) {
      r.r().warning(`Failed to repair missing thread cwd metadata`, {
        safe: { errorName: e instanceof Error ? e.name : `UnknownError` },
      });
    }
  }
}
async function Op(e) {
  let t = (0, u.join)(e, Sp);
  if (!(await Rp(t))) return 0;
  let n = new (zp())(t, { fileMustExist: !0 });
  try {
    return await kp(n);
  } finally {
    n.close();
  }
}
async function kp(e, t = Ap) {
  let n = Pp(e, `threads`).includes(`updated_at_ms`)
      ? `updated_at_ms`
      : `updated_at`,
    r = e
      .prepare(
        `SELECT id, rollout_path FROM threads
       WHERE (cwd = '' OR cwd IS NULL)
         AND rollout_path IS NOT NULL
         AND rollout_path <> ''
       ORDER BY ${Lp(n)} DESC
       LIMIT ?`,
      )
      .all(Tp),
    i = (
      await Promise.all(
        r.map(async (e) => {
          let n = e.id,
            r = e.rollout_path;
          if (typeof n != `string` || typeof r != `string`) return null;
          let i = await t(r, n);
          return i == null || i.trim().length === 0 ? null : { cwd: i, id: n };
        }),
      )
    ).filter((e) => e != null);
  if (i.length === 0) return 0;
  let a = e.prepare(
      `UPDATE threads SET cwd = ? WHERE id = ? AND (cwd = '' OR cwd IS NULL)`,
    ),
    o = 0;
  e.exec(`BEGIN IMMEDIATE`);
  try {
    for (let e of i) o += Number(a.run(e.cwd, e.id).changes);
    e.exec(`COMMIT`);
  } catch (t) {
    throw (e.exec(`ROLLBACK`), t);
  }
  return o;
}
async function Ap(e, t) {
  let n = (0, p.createReadStream)(e, { encoding: `utf8` }),
    r = (0, S.createInterface)({ crlfDelay: 1 / 0, input: n });
  try {
    for await (let e of r) {
      let n;
      try {
        n = JSON.parse(e);
      } catch {
        return null;
      }
      let r = Ep.safeParse(n);
      return !r.success || r.data.payload.id !== t ? null : r.data.payload.cwd;
    }
    return null;
  } catch {
    return null;
  } finally {
    (r.close(), n.destroy());
  }
}
async function jp(e) {
  let t = (0, u.join)(e, Cp, Sp),
    n = (0, u.join)(e, Sp),
    r = (0, u.join)(e, wp);
  if ((await Rp(r)) || !(await Rp(t))) return null;
  let i = zp();
  if (!(await Rp(n))) {
    let e = new i(t, { fileMustExist: !0, readonly: !0 });
    try {
      return (
        await e.backup(n),
        await (0, m.writeFile)(r, ``),
        {
          dynamicToolsInserted: 0,
          inserted: 0,
          repaired: 0,
          spawnEdgesInserted: 0,
        }
      );
    } finally {
      e.close();
    }
  }
  let a = new i(n, { fileMustExist: !0 }),
    o = new i(t, { fileMustExist: !0, readonly: !0 });
  try {
    let e = Mp(a, o);
    return (await (0, m.writeFile)(r, ``), e);
  } finally {
    (o.close(), a.close());
  }
}
function Mp(e, t) {
  let n = Np(e, t, `threads`),
    r = n.includes(`updated_at_ms`) ? `updated_at_ms` : `updated_at`,
    i = n.map(Lp).join(`, `),
    a = n.map(() => `?`).join(`, `),
    o = e.prepare(`SELECT ${i} FROM threads WHERE id = ?`),
    s = e.prepare(`INSERT INTO threads (${i}) VALUES (${a})`),
    c = 0,
    l = 0,
    u = new Set();
  e.exec(`BEGIN IMMEDIATE`);
  try {
    for (let a of t.prepare(`SELECT ${i} FROM threads`).all()) {
      let t = a.id;
      if (typeof t != `string`) continue;
      let i = o.get(t);
      if (i == null) {
        (s.run(...n.map((e) => a[e] ?? null)), (c += 1), u.add(t));
        continue;
      }
      let d = Ip(a[r]) > Ip(i[r]),
        f = n.filter(
          (e) => e !== `id` && !Fp(a[e]) && (Fp(i[e]) || (d && a[e] !== i[e])),
        );
      f.length !== 0 &&
        (e
          .prepare(
            `UPDATE threads SET ${f.map((e) => `${Lp(e)} = ?`).join(`, `)} WHERE id = ?`,
          )
          .run(...f.map((e) => a[e] ?? null), t),
        (l += 1),
        u.add(t));
    }
    let a = Np(e, t, `thread_spawn_edges`),
      d = 0;
    if (a.length > 0) {
      let n = a.map(Lp).join(`, `),
        r = e.prepare(
          `INSERT OR IGNORE INTO thread_spawn_edges (${n}) VALUES (${a.map(() => `?`).join(`, `)})`,
        );
      for (let e of t.prepare(`SELECT ${n} FROM thread_spawn_edges`).all())
        d += Number(r.run(...a.map((t) => e[t] ?? null)).changes);
    }
    let f = Np(e, t, `thread_dynamic_tools`),
      p = 0;
    if (f.length > 0) {
      let n = f.map(Lp).join(`, `),
        r = t.prepare(
          `SELECT ${n} FROM thread_dynamic_tools WHERE thread_id = ?`,
        ),
        i = e.prepare(
          `INSERT OR IGNORE INTO thread_dynamic_tools (${n}) VALUES (${f.map(() => `?`).join(`, `)})`,
        );
      for (let e of u)
        for (let t of r.all(e))
          p += Number(i.run(...f.map((e) => t[e] ?? null)).changes);
    }
    return (
      e.exec(`COMMIT`),
      {
        dynamicToolsInserted: p,
        inserted: c,
        repaired: l,
        spawnEdgesInserted: d,
      }
    );
  } catch (t) {
    throw (e.exec(`ROLLBACK`), t);
  }
}
function Np(e, t, n) {
  let r = new Set(Pp(t, n));
  return Pp(e, n).filter((e) => r.has(e));
}
function Pp(e, t) {
  return e
    .prepare(`PRAGMA table_info(${Lp(t)})`)
    .all()
    .flatMap((e) => (typeof e.name == `string` ? [e.name] : []));
}
function Fp(e) {
  return e == null || e === ``;
}
function Ip(e) {
  return typeof e == `bigint` || typeof e == `number` ? e : -1;
}
function Lp(e) {
  return `"${e.replaceAll(`"`, `""`)}"`;
}
async function Rp(e) {
  try {
    return (await (0, m.access)(e), !0);
  } catch {
    return !1;
  }
}
function zp() {
  try {
    return require(`better-sqlite3`);
  } catch {
    throw Error(`better-sqlite3 is only bundled with the Electron app`);
  }
}
var Bp = 6e4,
  Vp = class {
    lastRecoverableLogByKey = new Map();
    constructor(e, t, n) {
      ((this.app = e), (this.errorReporter = t), (this.logger = n));
    }
    register() {
      this.app.on(`child-process-gone`, this.onChildProcessGone);
    }
    onChildProcessGone = (e, t) => {
      if (t.reason !== `clean-exit`) {
        if (Hp(t)) {
          this.logRecoverableChildProcessGone(t);
          return;
        }
        this.errorReporter.reportFatal(
          Error(`Child process gone (${t.type})`),
          {
            tags: {
              errorType: `child-process-gone`,
              processType: t.type,
              reason: t.reason,
            },
            extra: {
              exitCode: t.exitCode,
              name: t.name,
              serviceName: t.serviceName,
            },
          },
        );
      }
    };
    logRecoverableChildProcessGone(e) {
      let t = `${e.type}:${e.serviceName ?? e.name ?? ``}:${e.reason}`,
        n = Date.now(),
        r = this.lastRecoverableLogByKey.get(t);
      (r != null && n - r < Bp) ||
        (this.lastRecoverableLogByKey.set(t, n),
        this.logger.warning(`Recoverable Chromium child process gone`, {
          safe: {
            exitCode: e.exitCode,
            name: e.name ?? null,
            processType: e.type,
            reason: e.reason,
            serviceName: e.serviceName ?? null,
          },
        }));
    }
  };
function Hp(e) {
  return e.reason === `launch-failed` || e.type === `GPU`
    ? !0
    : e.type === `Utility` && e.serviceName === `audio.mojom.AudioService`;
}
var Up = {};
function Wp(e, t) {
  let n = t || Up;
  return Gp(
    e,
    typeof n.includeImageAlt == `boolean` ? n.includeImageAlt : !0,
    typeof n.includeHtml == `boolean` ? n.includeHtml : !0,
  );
}
function Gp(e, t, n) {
  if (qp(e)) {
    if (`value` in e) return e.type === `html` && !n ? `` : e.value;
    if (t && `alt` in e && e.alt) return e.alt;
    if (`children` in e) return Kp(e.children, t, n);
  }
  return Array.isArray(e) ? Kp(e, t, n) : ``;
}
function Kp(e, t, n) {
  let r = [],
    i = -1;
  for (; ++i < e.length; ) r[i] = Gp(e[i], t, n);
  return r.join(``);
}
function qp(e) {
  return !!(e && typeof e == `object`);
}
var Jp = {
    AElig: `Æ`,
    AMP: `&`,
    Aacute: `Á`,
    Abreve: `Ă`,
    Acirc: `Â`,
    Acy: `А`,
    Afr: `𝔄`,
    Agrave: `À`,
    Alpha: `Α`,
    Amacr: `Ā`,
    And: `⩓`,
    Aogon: `Ą`,
    Aopf: `𝔸`,
    ApplyFunction: `⁡`,
    Aring: `Å`,
    Ascr: `𝒜`,
    Assign: `≔`,
    Atilde: `Ã`,
    Auml: `Ä`,
    Backslash: `∖`,
    Barv: `⫧`,
    Barwed: `⌆`,
    Bcy: `Б`,
    Because: `∵`,
    Bernoullis: `ℬ`,
    Beta: `Β`,
    Bfr: `𝔅`,
    Bopf: `𝔹`,
    Breve: `˘`,
    Bscr: `ℬ`,
    Bumpeq: `≎`,
    CHcy: `Ч`,
    COPY: `©`,
    Cacute: `Ć`,
    Cap: `⋒`,
    CapitalDifferentialD: `ⅅ`,
    Cayleys: `ℭ`,
    Ccaron: `Č`,
    Ccedil: `Ç`,
    Ccirc: `Ĉ`,
    Cconint: `∰`,
    Cdot: `Ċ`,
    Cedilla: `¸`,
    CenterDot: `·`,
    Cfr: `ℭ`,
    Chi: `Χ`,
    CircleDot: `⊙`,
    CircleMinus: `⊖`,
    CirclePlus: `⊕`,
    CircleTimes: `⊗`,
    ClockwiseContourIntegral: `∲`,
    CloseCurlyDoubleQuote: `”`,
    CloseCurlyQuote: `’`,
    Colon: `∷`,
    Colone: `⩴`,
    Congruent: `≡`,
    Conint: `∯`,
    ContourIntegral: `∮`,
    Copf: `ℂ`,
    Coproduct: `∐`,
    CounterClockwiseContourIntegral: `∳`,
    Cross: `⨯`,
    Cscr: `𝒞`,
    Cup: `⋓`,
    CupCap: `≍`,
    DD: `ⅅ`,
    DDotrahd: `⤑`,
    DJcy: `Ђ`,
    DScy: `Ѕ`,
    DZcy: `Џ`,
    Dagger: `‡`,
    Darr: `↡`,
    Dashv: `⫤`,
    Dcaron: `Ď`,
    Dcy: `Д`,
    Del: `∇`,
    Delta: `Δ`,
    Dfr: `𝔇`,
    DiacriticalAcute: `´`,
    DiacriticalDot: `˙`,
    DiacriticalDoubleAcute: `˝`,
    DiacriticalGrave: "`",
    DiacriticalTilde: `˜`,
    Diamond: `⋄`,
    DifferentialD: `ⅆ`,
    Dopf: `𝔻`,
    Dot: `¨`,
    DotDot: `⃜`,
    DotEqual: `≐`,
    DoubleContourIntegral: `∯`,
    DoubleDot: `¨`,
    DoubleDownArrow: `⇓`,
    DoubleLeftArrow: `⇐`,
    DoubleLeftRightArrow: `⇔`,
    DoubleLeftTee: `⫤`,
    DoubleLongLeftArrow: `⟸`,
    DoubleLongLeftRightArrow: `⟺`,
    DoubleLongRightArrow: `⟹`,
    DoubleRightArrow: `⇒`,
    DoubleRightTee: `⊨`,
    DoubleUpArrow: `⇑`,
    DoubleUpDownArrow: `⇕`,
    DoubleVerticalBar: `∥`,
    DownArrow: `↓`,
    DownArrowBar: `⤓`,
    DownArrowUpArrow: `⇵`,
    DownBreve: `̑`,
    DownLeftRightVector: `⥐`,
    DownLeftTeeVector: `⥞`,
    DownLeftVector: `↽`,
    DownLeftVectorBar: `⥖`,
    DownRightTeeVector: `⥟`,
    DownRightVector: `⇁`,
    DownRightVectorBar: `⥗`,
    DownTee: `⊤`,
    DownTeeArrow: `↧`,
    Downarrow: `⇓`,
    Dscr: `𝒟`,
    Dstrok: `Đ`,
    ENG: `Ŋ`,
    ETH: `Ð`,
    Eacute: `É`,
    Ecaron: `Ě`,
    Ecirc: `Ê`,
    Ecy: `Э`,
    Edot: `Ė`,
    Efr: `𝔈`,
    Egrave: `È`,
    Element: `∈`,
    Emacr: `Ē`,
    EmptySmallSquare: `◻`,
    EmptyVerySmallSquare: `▫`,
    Eogon: `Ę`,
    Eopf: `𝔼`,
    Epsilon: `Ε`,
    Equal: `⩵`,
    EqualTilde: `≂`,
    Equilibrium: `⇌`,
    Escr: `ℰ`,
    Esim: `⩳`,
    Eta: `Η`,
    Euml: `Ë`,
    Exists: `∃`,
    ExponentialE: `ⅇ`,
    Fcy: `Ф`,
    Ffr: `𝔉`,
    FilledSmallSquare: `◼`,
    FilledVerySmallSquare: `▪`,
    Fopf: `𝔽`,
    ForAll: `∀`,
    Fouriertrf: `ℱ`,
    Fscr: `ℱ`,
    GJcy: `Ѓ`,
    GT: `>`,
    Gamma: `Γ`,
    Gammad: `Ϝ`,
    Gbreve: `Ğ`,
    Gcedil: `Ģ`,
    Gcirc: `Ĝ`,
    Gcy: `Г`,
    Gdot: `Ġ`,
    Gfr: `𝔊`,
    Gg: `⋙`,
    Gopf: `𝔾`,
    GreaterEqual: `≥`,
    GreaterEqualLess: `⋛`,
    GreaterFullEqual: `≧`,
    GreaterGreater: `⪢`,
    GreaterLess: `≷`,
    GreaterSlantEqual: `⩾`,
    GreaterTilde: `≳`,
    Gscr: `𝒢`,
    Gt: `≫`,
    HARDcy: `Ъ`,
    Hacek: `ˇ`,
    Hat: `^`,
    Hcirc: `Ĥ`,
    Hfr: `ℌ`,
    HilbertSpace: `ℋ`,
    Hopf: `ℍ`,
    HorizontalLine: `─`,
    Hscr: `ℋ`,
    Hstrok: `Ħ`,
    HumpDownHump: `≎`,
    HumpEqual: `≏`,
    IEcy: `Е`,
    IJlig: `Ĳ`,
    IOcy: `Ё`,
    Iacute: `Í`,
    Icirc: `Î`,
    Icy: `И`,
    Idot: `İ`,
    Ifr: `ℑ`,
    Igrave: `Ì`,
    Im: `ℑ`,
    Imacr: `Ī`,
    ImaginaryI: `ⅈ`,
    Implies: `⇒`,
    Int: `∬`,
    Integral: `∫`,
    Intersection: `⋂`,
    InvisibleComma: `⁣`,
    InvisibleTimes: `⁢`,
    Iogon: `Į`,
    Iopf: `𝕀`,
    Iota: `Ι`,
    Iscr: `ℐ`,
    Itilde: `Ĩ`,
    Iukcy: `І`,
    Iuml: `Ï`,
    Jcirc: `Ĵ`,
    Jcy: `Й`,
    Jfr: `𝔍`,
    Jopf: `𝕁`,
    Jscr: `𝒥`,
    Jsercy: `Ј`,
    Jukcy: `Є`,
    KHcy: `Х`,
    KJcy: `Ќ`,
    Kappa: `Κ`,
    Kcedil: `Ķ`,
    Kcy: `К`,
    Kfr: `𝔎`,
    Kopf: `𝕂`,
    Kscr: `𝒦`,
    LJcy: `Љ`,
    LT: `<`,
    Lacute: `Ĺ`,
    Lambda: `Λ`,
    Lang: `⟪`,
    Laplacetrf: `ℒ`,
    Larr: `↞`,
    Lcaron: `Ľ`,
    Lcedil: `Ļ`,
    Lcy: `Л`,
    LeftAngleBracket: `⟨`,
    LeftArrow: `←`,
    LeftArrowBar: `⇤`,
    LeftArrowRightArrow: `⇆`,
    LeftCeiling: `⌈`,
    LeftDoubleBracket: `⟦`,
    LeftDownTeeVector: `⥡`,
    LeftDownVector: `⇃`,
    LeftDownVectorBar: `⥙`,
    LeftFloor: `⌊`,
    LeftRightArrow: `↔`,
    LeftRightVector: `⥎`,
    LeftTee: `⊣`,
    LeftTeeArrow: `↤`,
    LeftTeeVector: `⥚`,
    LeftTriangle: `⊲`,
    LeftTriangleBar: `⧏`,
    LeftTriangleEqual: `⊴`,
    LeftUpDownVector: `⥑`,
    LeftUpTeeVector: `⥠`,
    LeftUpVector: `↿`,
    LeftUpVectorBar: `⥘`,
    LeftVector: `↼`,
    LeftVectorBar: `⥒`,
    Leftarrow: `⇐`,
    Leftrightarrow: `⇔`,
    LessEqualGreater: `⋚`,
    LessFullEqual: `≦`,
    LessGreater: `≶`,
    LessLess: `⪡`,
    LessSlantEqual: `⩽`,
    LessTilde: `≲`,
    Lfr: `𝔏`,
    Ll: `⋘`,
    Lleftarrow: `⇚`,
    Lmidot: `Ŀ`,
    LongLeftArrow: `⟵`,
    LongLeftRightArrow: `⟷`,
    LongRightArrow: `⟶`,
    Longleftarrow: `⟸`,
    Longleftrightarrow: `⟺`,
    Longrightarrow: `⟹`,
    Lopf: `𝕃`,
    LowerLeftArrow: `↙`,
    LowerRightArrow: `↘`,
    Lscr: `ℒ`,
    Lsh: `↰`,
    Lstrok: `Ł`,
    Lt: `≪`,
    Map: `⤅`,
    Mcy: `М`,
    MediumSpace: ` `,
    Mellintrf: `ℳ`,
    Mfr: `𝔐`,
    MinusPlus: `∓`,
    Mopf: `𝕄`,
    Mscr: `ℳ`,
    Mu: `Μ`,
    NJcy: `Њ`,
    Nacute: `Ń`,
    Ncaron: `Ň`,
    Ncedil: `Ņ`,
    Ncy: `Н`,
    NegativeMediumSpace: `​`,
    NegativeThickSpace: `​`,
    NegativeThinSpace: `​`,
    NegativeVeryThinSpace: `​`,
    NestedGreaterGreater: `≫`,
    NestedLessLess: `≪`,
    NewLine: `
`,
    Nfr: `𝔑`,
    NoBreak: `⁠`,
    NonBreakingSpace: `\xA0`,
    Nopf: `ℕ`,
    Not: `⫬`,
    NotCongruent: `≢`,
    NotCupCap: `≭`,
    NotDoubleVerticalBar: `∦`,
    NotElement: `∉`,
    NotEqual: `≠`,
    NotEqualTilde: `≂̸`,
    NotExists: `∄`,
    NotGreater: `≯`,
    NotGreaterEqual: `≱`,
    NotGreaterFullEqual: `≧̸`,
    NotGreaterGreater: `≫̸`,
    NotGreaterLess: `≹`,
    NotGreaterSlantEqual: `⩾̸`,
    NotGreaterTilde: `≵`,
    NotHumpDownHump: `≎̸`,
    NotHumpEqual: `≏̸`,
    NotLeftTriangle: `⋪`,
    NotLeftTriangleBar: `⧏̸`,
    NotLeftTriangleEqual: `⋬`,
    NotLess: `≮`,
    NotLessEqual: `≰`,
    NotLessGreater: `≸`,
    NotLessLess: `≪̸`,
    NotLessSlantEqual: `⩽̸`,
    NotLessTilde: `≴`,
    NotNestedGreaterGreater: `⪢̸`,
    NotNestedLessLess: `⪡̸`,
    NotPrecedes: `⊀`,
    NotPrecedesEqual: `⪯̸`,
    NotPrecedesSlantEqual: `⋠`,
    NotReverseElement: `∌`,
    NotRightTriangle: `⋫`,
    NotRightTriangleBar: `⧐̸`,
    NotRightTriangleEqual: `⋭`,
    NotSquareSubset: `⊏̸`,
    NotSquareSubsetEqual: `⋢`,
    NotSquareSuperset: `⊐̸`,
    NotSquareSupersetEqual: `⋣`,
    NotSubset: `⊂⃒`,
    NotSubsetEqual: `⊈`,
    NotSucceeds: `⊁`,
    NotSucceedsEqual: `⪰̸`,
    NotSucceedsSlantEqual: `⋡`,
    NotSucceedsTilde: `≿̸`,
    NotSuperset: `⊃⃒`,
    NotSupersetEqual: `⊉`,
    NotTilde: `≁`,
    NotTildeEqual: `≄`,
    NotTildeFullEqual: `≇`,
    NotTildeTilde: `≉`,
    NotVerticalBar: `∤`,
    Nscr: `𝒩`,
    Ntilde: `Ñ`,
    Nu: `Ν`,
    OElig: `Œ`,
    Oacute: `Ó`,
    Ocirc: `Ô`,
    Ocy: `О`,
    Odblac: `Ő`,
    Ofr: `𝔒`,
    Ograve: `Ò`,
    Omacr: `Ō`,
    Omega: `Ω`,
    Omicron: `Ο`,
    Oopf: `𝕆`,
    OpenCurlyDoubleQuote: `“`,
    OpenCurlyQuote: `‘`,
    Or: `⩔`,
    Oscr: `𝒪`,
    Oslash: `Ø`,
    Otilde: `Õ`,
    Otimes: `⨷`,
    Ouml: `Ö`,
    OverBar: `‾`,
    OverBrace: `⏞`,
    OverBracket: `⎴`,
    OverParenthesis: `⏜`,
    PartialD: `∂`,
    Pcy: `П`,
    Pfr: `𝔓`,
    Phi: `Φ`,
    Pi: `Π`,
    PlusMinus: `±`,
    Poincareplane: `ℌ`,
    Popf: `ℙ`,
    Pr: `⪻`,
    Precedes: `≺`,
    PrecedesEqual: `⪯`,
    PrecedesSlantEqual: `≼`,
    PrecedesTilde: `≾`,
    Prime: `″`,
    Product: `∏`,
    Proportion: `∷`,
    Proportional: `∝`,
    Pscr: `𝒫`,
    Psi: `Ψ`,
    QUOT: `"`,
    Qfr: `𝔔`,
    Qopf: `ℚ`,
    Qscr: `𝒬`,
    RBarr: `⤐`,
    REG: `®`,
    Racute: `Ŕ`,
    Rang: `⟫`,
    Rarr: `↠`,
    Rarrtl: `⤖`,
    Rcaron: `Ř`,
    Rcedil: `Ŗ`,
    Rcy: `Р`,
    Re: `ℜ`,
    ReverseElement: `∋`,
    ReverseEquilibrium: `⇋`,
    ReverseUpEquilibrium: `⥯`,
    Rfr: `ℜ`,
    Rho: `Ρ`,
    RightAngleBracket: `⟩`,
    RightArrow: `→`,
    RightArrowBar: `⇥`,
    RightArrowLeftArrow: `⇄`,
    RightCeiling: `⌉`,
    RightDoubleBracket: `⟧`,
    RightDownTeeVector: `⥝`,
    RightDownVector: `⇂`,
    RightDownVectorBar: `⥕`,
    RightFloor: `⌋`,
    RightTee: `⊢`,
    RightTeeArrow: `↦`,
    RightTeeVector: `⥛`,
    RightTriangle: `⊳`,
    RightTriangleBar: `⧐`,
    RightTriangleEqual: `⊵`,
    RightUpDownVector: `⥏`,
    RightUpTeeVector: `⥜`,
    RightUpVector: `↾`,
    RightUpVectorBar: `⥔`,
    RightVector: `⇀`,
    RightVectorBar: `⥓`,
    Rightarrow: `⇒`,
    Ropf: `ℝ`,
    RoundImplies: `⥰`,
    Rrightarrow: `⇛`,
    Rscr: `ℛ`,
    Rsh: `↱`,
    RuleDelayed: `⧴`,
    SHCHcy: `Щ`,
    SHcy: `Ш`,
    SOFTcy: `Ь`,
    Sacute: `Ś`,
    Sc: `⪼`,
    Scaron: `Š`,
    Scedil: `Ş`,
    Scirc: `Ŝ`,
    Scy: `С`,
    Sfr: `𝔖`,
    ShortDownArrow: `↓`,
    ShortLeftArrow: `←`,
    ShortRightArrow: `→`,
    ShortUpArrow: `↑`,
    Sigma: `Σ`,
    SmallCircle: `∘`,
    Sopf: `𝕊`,
    Sqrt: `√`,
    Square: `□`,
    SquareIntersection: `⊓`,
    SquareSubset: `⊏`,
    SquareSubsetEqual: `⊑`,
    SquareSuperset: `⊐`,
    SquareSupersetEqual: `⊒`,
    SquareUnion: `⊔`,
    Sscr: `𝒮`,
    Star: `⋆`,
    Sub: `⋐`,
    Subset: `⋐`,
    SubsetEqual: `⊆`,
    Succeeds: `≻`,
    SucceedsEqual: `⪰`,
    SucceedsSlantEqual: `≽`,
    SucceedsTilde: `≿`,
    SuchThat: `∋`,
    Sum: `∑`,
    Sup: `⋑`,
    Superset: `⊃`,
    SupersetEqual: `⊇`,
    Supset: `⋑`,
    THORN: `Þ`,
    TRADE: `™`,
    TSHcy: `Ћ`,
    TScy: `Ц`,
    Tab: `	`,
    Tau: `Τ`,
    Tcaron: `Ť`,
    Tcedil: `Ţ`,
    Tcy: `Т`,
    Tfr: `𝔗`,
    Therefore: `∴`,
    Theta: `Θ`,
    ThickSpace: `  `,
    ThinSpace: ` `,
    Tilde: `∼`,
    TildeEqual: `≃`,
    TildeFullEqual: `≅`,
    TildeTilde: `≈`,
    Topf: `𝕋`,
    TripleDot: `⃛`,
    Tscr: `𝒯`,
    Tstrok: `Ŧ`,
    Uacute: `Ú`,
    Uarr: `↟`,
    Uarrocir: `⥉`,
    Ubrcy: `Ў`,
    Ubreve: `Ŭ`,
    Ucirc: `Û`,
    Ucy: `У`,
    Udblac: `Ű`,
    Ufr: `𝔘`,
    Ugrave: `Ù`,
    Umacr: `Ū`,
    UnderBar: `_`,
    UnderBrace: `⏟`,
    UnderBracket: `⎵`,
    UnderParenthesis: `⏝`,
    Union: `⋃`,
    UnionPlus: `⊎`,
    Uogon: `Ų`,
    Uopf: `𝕌`,
    UpArrow: `↑`,
    UpArrowBar: `⤒`,
    UpArrowDownArrow: `⇅`,
    UpDownArrow: `↕`,
    UpEquilibrium: `⥮`,
    UpTee: `⊥`,
    UpTeeArrow: `↥`,
    Uparrow: `⇑`,
    Updownarrow: `⇕`,
    UpperLeftArrow: `↖`,
    UpperRightArrow: `↗`,
    Upsi: `ϒ`,
    Upsilon: `Υ`,
    Uring: `Ů`,
    Uscr: `𝒰`,
    Utilde: `Ũ`,
    Uuml: `Ü`,
    VDash: `⊫`,
    Vbar: `⫫`,
    Vcy: `В`,
    Vdash: `⊩`,
    Vdashl: `⫦`,
    Vee: `⋁`,
    Verbar: `‖`,
    Vert: `‖`,
    VerticalBar: `∣`,
    VerticalLine: `|`,
    VerticalSeparator: `❘`,
    VerticalTilde: `≀`,
    VeryThinSpace: ` `,
    Vfr: `𝔙`,
    Vopf: `𝕍`,
    Vscr: `𝒱`,
    Vvdash: `⊪`,
    Wcirc: `Ŵ`,
    Wedge: `⋀`,
    Wfr: `𝔚`,
    Wopf: `𝕎`,
    Wscr: `𝒲`,
    Xfr: `𝔛`,
    Xi: `Ξ`,
    Xopf: `𝕏`,
    Xscr: `𝒳`,
    YAcy: `Я`,
    YIcy: `Ї`,
    YUcy: `Ю`,
    Yacute: `Ý`,
    Ycirc: `Ŷ`,
    Ycy: `Ы`,
    Yfr: `𝔜`,
    Yopf: `𝕐`,
    Yscr: `𝒴`,
    Yuml: `Ÿ`,
    ZHcy: `Ж`,
    Zacute: `Ź`,
    Zcaron: `Ž`,
    Zcy: `З`,
    Zdot: `Ż`,
    ZeroWidthSpace: `​`,
    Zeta: `Ζ`,
    Zfr: `ℨ`,
    Zopf: `ℤ`,
    Zscr: `𝒵`,
    aacute: `á`,
    abreve: `ă`,
    ac: `∾`,
    acE: `∾̳`,
    acd: `∿`,
    acirc: `â`,
    acute: `´`,
    acy: `а`,
    aelig: `æ`,
    af: `⁡`,
    afr: `𝔞`,
    agrave: `à`,
    alefsym: `ℵ`,
    aleph: `ℵ`,
    alpha: `α`,
    amacr: `ā`,
    amalg: `⨿`,
    amp: `&`,
    and: `∧`,
    andand: `⩕`,
    andd: `⩜`,
    andslope: `⩘`,
    andv: `⩚`,
    ang: `∠`,
    ange: `⦤`,
    angle: `∠`,
    angmsd: `∡`,
    angmsdaa: `⦨`,
    angmsdab: `⦩`,
    angmsdac: `⦪`,
    angmsdad: `⦫`,
    angmsdae: `⦬`,
    angmsdaf: `⦭`,
    angmsdag: `⦮`,
    angmsdah: `⦯`,
    angrt: `∟`,
    angrtvb: `⊾`,
    angrtvbd: `⦝`,
    angsph: `∢`,
    angst: `Å`,
    angzarr: `⍼`,
    aogon: `ą`,
    aopf: `𝕒`,
    ap: `≈`,
    apE: `⩰`,
    apacir: `⩯`,
    ape: `≊`,
    apid: `≋`,
    apos: `'`,
    approx: `≈`,
    approxeq: `≊`,
    aring: `å`,
    ascr: `𝒶`,
    ast: `*`,
    asymp: `≈`,
    asympeq: `≍`,
    atilde: `ã`,
    auml: `ä`,
    awconint: `∳`,
    awint: `⨑`,
    bNot: `⫭`,
    backcong: `≌`,
    backepsilon: `϶`,
    backprime: `‵`,
    backsim: `∽`,
    backsimeq: `⋍`,
    barvee: `⊽`,
    barwed: `⌅`,
    barwedge: `⌅`,
    bbrk: `⎵`,
    bbrktbrk: `⎶`,
    bcong: `≌`,
    bcy: `б`,
    bdquo: `„`,
    becaus: `∵`,
    because: `∵`,
    bemptyv: `⦰`,
    bepsi: `϶`,
    bernou: `ℬ`,
    beta: `β`,
    beth: `ℶ`,
    between: `≬`,
    bfr: `𝔟`,
    bigcap: `⋂`,
    bigcirc: `◯`,
    bigcup: `⋃`,
    bigodot: `⨀`,
    bigoplus: `⨁`,
    bigotimes: `⨂`,
    bigsqcup: `⨆`,
    bigstar: `★`,
    bigtriangledown: `▽`,
    bigtriangleup: `△`,
    biguplus: `⨄`,
    bigvee: `⋁`,
    bigwedge: `⋀`,
    bkarow: `⤍`,
    blacklozenge: `⧫`,
    blacksquare: `▪`,
    blacktriangle: `▴`,
    blacktriangledown: `▾`,
    blacktriangleleft: `◂`,
    blacktriangleright: `▸`,
    blank: `␣`,
    blk12: `▒`,
    blk14: `░`,
    blk34: `▓`,
    block: `█`,
    bne: `=⃥`,
    bnequiv: `≡⃥`,
    bnot: `⌐`,
    bopf: `𝕓`,
    bot: `⊥`,
    bottom: `⊥`,
    bowtie: `⋈`,
    boxDL: `╗`,
    boxDR: `╔`,
    boxDl: `╖`,
    boxDr: `╓`,
    boxH: `═`,
    boxHD: `╦`,
    boxHU: `╩`,
    boxHd: `╤`,
    boxHu: `╧`,
    boxUL: `╝`,
    boxUR: `╚`,
    boxUl: `╜`,
    boxUr: `╙`,
    boxV: `║`,
    boxVH: `╬`,
    boxVL: `╣`,
    boxVR: `╠`,
    boxVh: `╫`,
    boxVl: `╢`,
    boxVr: `╟`,
    boxbox: `⧉`,
    boxdL: `╕`,
    boxdR: `╒`,
    boxdl: `┐`,
    boxdr: `┌`,
    boxh: `─`,
    boxhD: `╥`,
    boxhU: `╨`,
    boxhd: `┬`,
    boxhu: `┴`,
    boxminus: `⊟`,
    boxplus: `⊞`,
    boxtimes: `⊠`,
    boxuL: `╛`,
    boxuR: `╘`,
    boxul: `┘`,
    boxur: `└`,
    boxv: `│`,
    boxvH: `╪`,
    boxvL: `╡`,
    boxvR: `╞`,
    boxvh: `┼`,
    boxvl: `┤`,
    boxvr: `├`,
    bprime: `‵`,
    breve: `˘`,
    brvbar: `¦`,
    bscr: `𝒷`,
    bsemi: `⁏`,
    bsim: `∽`,
    bsime: `⋍`,
    bsol: `\\`,
    bsolb: `⧅`,
    bsolhsub: `⟈`,
    bull: `•`,
    bullet: `•`,
    bump: `≎`,
    bumpE: `⪮`,
    bumpe: `≏`,
    bumpeq: `≏`,
    cacute: `ć`,
    cap: `∩`,
    capand: `⩄`,
    capbrcup: `⩉`,
    capcap: `⩋`,
    capcup: `⩇`,
    capdot: `⩀`,
    caps: `∩︀`,
    caret: `⁁`,
    caron: `ˇ`,
    ccaps: `⩍`,
    ccaron: `č`,
    ccedil: `ç`,
    ccirc: `ĉ`,
    ccups: `⩌`,
    ccupssm: `⩐`,
    cdot: `ċ`,
    cedil: `¸`,
    cemptyv: `⦲`,
    cent: `¢`,
    centerdot: `·`,
    cfr: `𝔠`,
    chcy: `ч`,
    check: `✓`,
    checkmark: `✓`,
    chi: `χ`,
    cir: `○`,
    cirE: `⧃`,
    circ: `ˆ`,
    circeq: `≗`,
    circlearrowleft: `↺`,
    circlearrowright: `↻`,
    circledR: `®`,
    circledS: `Ⓢ`,
    circledast: `⊛`,
    circledcirc: `⊚`,
    circleddash: `⊝`,
    cire: `≗`,
    cirfnint: `⨐`,
    cirmid: `⫯`,
    cirscir: `⧂`,
    clubs: `♣`,
    clubsuit: `♣`,
    colon: `:`,
    colone: `≔`,
    coloneq: `≔`,
    comma: `,`,
    commat: `@`,
    comp: `∁`,
    compfn: `∘`,
    complement: `∁`,
    complexes: `ℂ`,
    cong: `≅`,
    congdot: `⩭`,
    conint: `∮`,
    copf: `𝕔`,
    coprod: `∐`,
    copy: `©`,
    copysr: `℗`,
    crarr: `↵`,
    cross: `✗`,
    cscr: `𝒸`,
    csub: `⫏`,
    csube: `⫑`,
    csup: `⫐`,
    csupe: `⫒`,
    ctdot: `⋯`,
    cudarrl: `⤸`,
    cudarrr: `⤵`,
    cuepr: `⋞`,
    cuesc: `⋟`,
    cularr: `↶`,
    cularrp: `⤽`,
    cup: `∪`,
    cupbrcap: `⩈`,
    cupcap: `⩆`,
    cupcup: `⩊`,
    cupdot: `⊍`,
    cupor: `⩅`,
    cups: `∪︀`,
    curarr: `↷`,
    curarrm: `⤼`,
    curlyeqprec: `⋞`,
    curlyeqsucc: `⋟`,
    curlyvee: `⋎`,
    curlywedge: `⋏`,
    curren: `¤`,
    curvearrowleft: `↶`,
    curvearrowright: `↷`,
    cuvee: `⋎`,
    cuwed: `⋏`,
    cwconint: `∲`,
    cwint: `∱`,
    cylcty: `⌭`,
    dArr: `⇓`,
    dHar: `⥥`,
    dagger: `†`,
    daleth: `ℸ`,
    darr: `↓`,
    dash: `‐`,
    dashv: `⊣`,
    dbkarow: `⤏`,
    dblac: `˝`,
    dcaron: `ď`,
    dcy: `д`,
    dd: `ⅆ`,
    ddagger: `‡`,
    ddarr: `⇊`,
    ddotseq: `⩷`,
    deg: `°`,
    delta: `δ`,
    demptyv: `⦱`,
    dfisht: `⥿`,
    dfr: `𝔡`,
    dharl: `⇃`,
    dharr: `⇂`,
    diam: `⋄`,
    diamond: `⋄`,
    diamondsuit: `♦`,
    diams: `♦`,
    die: `¨`,
    digamma: `ϝ`,
    disin: `⋲`,
    div: `÷`,
    divide: `÷`,
    divideontimes: `⋇`,
    divonx: `⋇`,
    djcy: `ђ`,
    dlcorn: `⌞`,
    dlcrop: `⌍`,
    dollar: `$`,
    dopf: `𝕕`,
    dot: `˙`,
    doteq: `≐`,
    doteqdot: `≑`,
    dotminus: `∸`,
    dotplus: `∔`,
    dotsquare: `⊡`,
    doublebarwedge: `⌆`,
    downarrow: `↓`,
    downdownarrows: `⇊`,
    downharpoonleft: `⇃`,
    downharpoonright: `⇂`,
    drbkarow: `⤐`,
    drcorn: `⌟`,
    drcrop: `⌌`,
    dscr: `𝒹`,
    dscy: `ѕ`,
    dsol: `⧶`,
    dstrok: `đ`,
    dtdot: `⋱`,
    dtri: `▿`,
    dtrif: `▾`,
    duarr: `⇵`,
    duhar: `⥯`,
    dwangle: `⦦`,
    dzcy: `џ`,
    dzigrarr: `⟿`,
    eDDot: `⩷`,
    eDot: `≑`,
    eacute: `é`,
    easter: `⩮`,
    ecaron: `ě`,
    ecir: `≖`,
    ecirc: `ê`,
    ecolon: `≕`,
    ecy: `э`,
    edot: `ė`,
    ee: `ⅇ`,
    efDot: `≒`,
    efr: `𝔢`,
    eg: `⪚`,
    egrave: `è`,
    egs: `⪖`,
    egsdot: `⪘`,
    el: `⪙`,
    elinters: `⏧`,
    ell: `ℓ`,
    els: `⪕`,
    elsdot: `⪗`,
    emacr: `ē`,
    empty: `∅`,
    emptyset: `∅`,
    emptyv: `∅`,
    emsp13: ` `,
    emsp14: ` `,
    emsp: ` `,
    eng: `ŋ`,
    ensp: ` `,
    eogon: `ę`,
    eopf: `𝕖`,
    epar: `⋕`,
    eparsl: `⧣`,
    eplus: `⩱`,
    epsi: `ε`,
    epsilon: `ε`,
    epsiv: `ϵ`,
    eqcirc: `≖`,
    eqcolon: `≕`,
    eqsim: `≂`,
    eqslantgtr: `⪖`,
    eqslantless: `⪕`,
    equals: `=`,
    equest: `≟`,
    equiv: `≡`,
    equivDD: `⩸`,
    eqvparsl: `⧥`,
    erDot: `≓`,
    erarr: `⥱`,
    escr: `ℯ`,
    esdot: `≐`,
    esim: `≂`,
    eta: `η`,
    eth: `ð`,
    euml: `ë`,
    euro: `€`,
    excl: `!`,
    exist: `∃`,
    expectation: `ℰ`,
    exponentiale: `ⅇ`,
    fallingdotseq: `≒`,
    fcy: `ф`,
    female: `♀`,
    ffilig: `ﬃ`,
    fflig: `ﬀ`,
    ffllig: `ﬄ`,
    ffr: `𝔣`,
    filig: `ﬁ`,
    fjlig: `fj`,
    flat: `♭`,
    fllig: `ﬂ`,
    fltns: `▱`,
    fnof: `ƒ`,
    fopf: `𝕗`,
    forall: `∀`,
    fork: `⋔`,
    forkv: `⫙`,
    fpartint: `⨍`,
    frac12: `½`,
    frac13: `⅓`,
    frac14: `¼`,
    frac15: `⅕`,
    frac16: `⅙`,
    frac18: `⅛`,
    frac23: `⅔`,
    frac25: `⅖`,
    frac34: `¾`,
    frac35: `⅗`,
    frac38: `⅜`,
    frac45: `⅘`,
    frac56: `⅚`,
    frac58: `⅝`,
    frac78: `⅞`,
    frasl: `⁄`,
    frown: `⌢`,
    fscr: `𝒻`,
    gE: `≧`,
    gEl: `⪌`,
    gacute: `ǵ`,
    gamma: `γ`,
    gammad: `ϝ`,
    gap: `⪆`,
    gbreve: `ğ`,
    gcirc: `ĝ`,
    gcy: `г`,
    gdot: `ġ`,
    ge: `≥`,
    gel: `⋛`,
    geq: `≥`,
    geqq: `≧`,
    geqslant: `⩾`,
    ges: `⩾`,
    gescc: `⪩`,
    gesdot: `⪀`,
    gesdoto: `⪂`,
    gesdotol: `⪄`,
    gesl: `⋛︀`,
    gesles: `⪔`,
    gfr: `𝔤`,
    gg: `≫`,
    ggg: `⋙`,
    gimel: `ℷ`,
    gjcy: `ѓ`,
    gl: `≷`,
    glE: `⪒`,
    gla: `⪥`,
    glj: `⪤`,
    gnE: `≩`,
    gnap: `⪊`,
    gnapprox: `⪊`,
    gne: `⪈`,
    gneq: `⪈`,
    gneqq: `≩`,
    gnsim: `⋧`,
    gopf: `𝕘`,
    grave: "`",
    gscr: `ℊ`,
    gsim: `≳`,
    gsime: `⪎`,
    gsiml: `⪐`,
    gt: `>`,
    gtcc: `⪧`,
    gtcir: `⩺`,
    gtdot: `⋗`,
    gtlPar: `⦕`,
    gtquest: `⩼`,
    gtrapprox: `⪆`,
    gtrarr: `⥸`,
    gtrdot: `⋗`,
    gtreqless: `⋛`,
    gtreqqless: `⪌`,
    gtrless: `≷`,
    gtrsim: `≳`,
    gvertneqq: `≩︀`,
    gvnE: `≩︀`,
    hArr: `⇔`,
    hairsp: ` `,
    half: `½`,
    hamilt: `ℋ`,
    hardcy: `ъ`,
    harr: `↔`,
    harrcir: `⥈`,
    harrw: `↭`,
    hbar: `ℏ`,
    hcirc: `ĥ`,
    hearts: `♥`,
    heartsuit: `♥`,
    hellip: `…`,
    hercon: `⊹`,
    hfr: `𝔥`,
    hksearow: `⤥`,
    hkswarow: `⤦`,
    hoarr: `⇿`,
    homtht: `∻`,
    hookleftarrow: `↩`,
    hookrightarrow: `↪`,
    hopf: `𝕙`,
    horbar: `―`,
    hscr: `𝒽`,
    hslash: `ℏ`,
    hstrok: `ħ`,
    hybull: `⁃`,
    hyphen: `‐`,
    iacute: `í`,
    ic: `⁣`,
    icirc: `î`,
    icy: `и`,
    iecy: `е`,
    iexcl: `¡`,
    iff: `⇔`,
    ifr: `𝔦`,
    igrave: `ì`,
    ii: `ⅈ`,
    iiiint: `⨌`,
    iiint: `∭`,
    iinfin: `⧜`,
    iiota: `℩`,
    ijlig: `ĳ`,
    imacr: `ī`,
    image: `ℑ`,
    imagline: `ℐ`,
    imagpart: `ℑ`,
    imath: `ı`,
    imof: `⊷`,
    imped: `Ƶ`,
    in: `∈`,
    incare: `℅`,
    infin: `∞`,
    infintie: `⧝`,
    inodot: `ı`,
    int: `∫`,
    intcal: `⊺`,
    integers: `ℤ`,
    intercal: `⊺`,
    intlarhk: `⨗`,
    intprod: `⨼`,
    iocy: `ё`,
    iogon: `į`,
    iopf: `𝕚`,
    iota: `ι`,
    iprod: `⨼`,
    iquest: `¿`,
    iscr: `𝒾`,
    isin: `∈`,
    isinE: `⋹`,
    isindot: `⋵`,
    isins: `⋴`,
    isinsv: `⋳`,
    isinv: `∈`,
    it: `⁢`,
    itilde: `ĩ`,
    iukcy: `і`,
    iuml: `ï`,
    jcirc: `ĵ`,
    jcy: `й`,
    jfr: `𝔧`,
    jmath: `ȷ`,
    jopf: `𝕛`,
    jscr: `𝒿`,
    jsercy: `ј`,
    jukcy: `є`,
    kappa: `κ`,
    kappav: `ϰ`,
    kcedil: `ķ`,
    kcy: `к`,
    kfr: `𝔨`,
    kgreen: `ĸ`,
    khcy: `х`,
    kjcy: `ќ`,
    kopf: `𝕜`,
    kscr: `𝓀`,
    lAarr: `⇚`,
    lArr: `⇐`,
    lAtail: `⤛`,
    lBarr: `⤎`,
    lE: `≦`,
    lEg: `⪋`,
    lHar: `⥢`,
    lacute: `ĺ`,
    laemptyv: `⦴`,
    lagran: `ℒ`,
    lambda: `λ`,
    lang: `⟨`,
    langd: `⦑`,
    langle: `⟨`,
    lap: `⪅`,
    laquo: `«`,
    larr: `←`,
    larrb: `⇤`,
    larrbfs: `⤟`,
    larrfs: `⤝`,
    larrhk: `↩`,
    larrlp: `↫`,
    larrpl: `⤹`,
    larrsim: `⥳`,
    larrtl: `↢`,
    lat: `⪫`,
    latail: `⤙`,
    late: `⪭`,
    lates: `⪭︀`,
    lbarr: `⤌`,
    lbbrk: `❲`,
    lbrace: `{`,
    lbrack: `[`,
    lbrke: `⦋`,
    lbrksld: `⦏`,
    lbrkslu: `⦍`,
    lcaron: `ľ`,
    lcedil: `ļ`,
    lceil: `⌈`,
    lcub: `{`,
    lcy: `л`,
    ldca: `⤶`,
    ldquo: `“`,
    ldquor: `„`,
    ldrdhar: `⥧`,
    ldrushar: `⥋`,
    ldsh: `↲`,
    le: `≤`,
    leftarrow: `←`,
    leftarrowtail: `↢`,
    leftharpoondown: `↽`,
    leftharpoonup: `↼`,
    leftleftarrows: `⇇`,
    leftrightarrow: `↔`,
    leftrightarrows: `⇆`,
    leftrightharpoons: `⇋`,
    leftrightsquigarrow: `↭`,
    leftthreetimes: `⋋`,
    leg: `⋚`,
    leq: `≤`,
    leqq: `≦`,
    leqslant: `⩽`,
    les: `⩽`,
    lescc: `⪨`,
    lesdot: `⩿`,
    lesdoto: `⪁`,
    lesdotor: `⪃`,
    lesg: `⋚︀`,
    lesges: `⪓`,
    lessapprox: `⪅`,
    lessdot: `⋖`,
    lesseqgtr: `⋚`,
    lesseqqgtr: `⪋`,
    lessgtr: `≶`,
    lesssim: `≲`,
    lfisht: `⥼`,
    lfloor: `⌊`,
    lfr: `𝔩`,
    lg: `≶`,
    lgE: `⪑`,
    lhard: `↽`,
    lharu: `↼`,
    lharul: `⥪`,
    lhblk: `▄`,
    ljcy: `љ`,
    ll: `≪`,
    llarr: `⇇`,
    llcorner: `⌞`,
    llhard: `⥫`,
    lltri: `◺`,
    lmidot: `ŀ`,
    lmoust: `⎰`,
    lmoustache: `⎰`,
    lnE: `≨`,
    lnap: `⪉`,
    lnapprox: `⪉`,
    lne: `⪇`,
    lneq: `⪇`,
    lneqq: `≨`,
    lnsim: `⋦`,
    loang: `⟬`,
    loarr: `⇽`,
    lobrk: `⟦`,
    longleftarrow: `⟵`,
    longleftrightarrow: `⟷`,
    longmapsto: `⟼`,
    longrightarrow: `⟶`,
    looparrowleft: `↫`,
    looparrowright: `↬`,
    lopar: `⦅`,
    lopf: `𝕝`,
    loplus: `⨭`,
    lotimes: `⨴`,
    lowast: `∗`,
    lowbar: `_`,
    loz: `◊`,
    lozenge: `◊`,
    lozf: `⧫`,
    lpar: `(`,
    lparlt: `⦓`,
    lrarr: `⇆`,
    lrcorner: `⌟`,
    lrhar: `⇋`,
    lrhard: `⥭`,
    lrm: `‎`,
    lrtri: `⊿`,
    lsaquo: `‹`,
    lscr: `𝓁`,
    lsh: `↰`,
    lsim: `≲`,
    lsime: `⪍`,
    lsimg: `⪏`,
    lsqb: `[`,
    lsquo: `‘`,
    lsquor: `‚`,
    lstrok: `ł`,
    lt: `<`,
    ltcc: `⪦`,
    ltcir: `⩹`,
    ltdot: `⋖`,
    lthree: `⋋`,
    ltimes: `⋉`,
    ltlarr: `⥶`,
    ltquest: `⩻`,
    ltrPar: `⦖`,
    ltri: `◃`,
    ltrie: `⊴`,
    ltrif: `◂`,
    lurdshar: `⥊`,
    luruhar: `⥦`,
    lvertneqq: `≨︀`,
    lvnE: `≨︀`,
    mDDot: `∺`,
    macr: `¯`,
    male: `♂`,
    malt: `✠`,
    maltese: `✠`,
    map: `↦`,
    mapsto: `↦`,
    mapstodown: `↧`,
    mapstoleft: `↤`,
    mapstoup: `↥`,
    marker: `▮`,
    mcomma: `⨩`,
    mcy: `м`,
    mdash: `—`,
    measuredangle: `∡`,
    mfr: `𝔪`,
    mho: `℧`,
    micro: `µ`,
    mid: `∣`,
    midast: `*`,
    midcir: `⫰`,
    middot: `·`,
    minus: `−`,
    minusb: `⊟`,
    minusd: `∸`,
    minusdu: `⨪`,
    mlcp: `⫛`,
    mldr: `…`,
    mnplus: `∓`,
    models: `⊧`,
    mopf: `𝕞`,
    mp: `∓`,
    mscr: `𝓂`,
    mstpos: `∾`,
    mu: `μ`,
    multimap: `⊸`,
    mumap: `⊸`,
    nGg: `⋙̸`,
    nGt: `≫⃒`,
    nGtv: `≫̸`,
    nLeftarrow: `⇍`,
    nLeftrightarrow: `⇎`,
    nLl: `⋘̸`,
    nLt: `≪⃒`,
    nLtv: `≪̸`,
    nRightarrow: `⇏`,
    nVDash: `⊯`,
    nVdash: `⊮`,
    nabla: `∇`,
    nacute: `ń`,
    nang: `∠⃒`,
    nap: `≉`,
    napE: `⩰̸`,
    napid: `≋̸`,
    napos: `ŉ`,
    napprox: `≉`,
    natur: `♮`,
    natural: `♮`,
    naturals: `ℕ`,
    nbsp: `\xA0`,
    nbump: `≎̸`,
    nbumpe: `≏̸`,
    ncap: `⩃`,
    ncaron: `ň`,
    ncedil: `ņ`,
    ncong: `≇`,
    ncongdot: `⩭̸`,
    ncup: `⩂`,
    ncy: `н`,
    ndash: `–`,
    ne: `≠`,
    neArr: `⇗`,
    nearhk: `⤤`,
    nearr: `↗`,
    nearrow: `↗`,
    nedot: `≐̸`,
    nequiv: `≢`,
    nesear: `⤨`,
    nesim: `≂̸`,
    nexist: `∄`,
    nexists: `∄`,
    nfr: `𝔫`,
    ngE: `≧̸`,
    nge: `≱`,
    ngeq: `≱`,
    ngeqq: `≧̸`,
    ngeqslant: `⩾̸`,
    nges: `⩾̸`,
    ngsim: `≵`,
    ngt: `≯`,
    ngtr: `≯`,
    nhArr: `⇎`,
    nharr: `↮`,
    nhpar: `⫲`,
    ni: `∋`,
    nis: `⋼`,
    nisd: `⋺`,
    niv: `∋`,
    njcy: `њ`,
    nlArr: `⇍`,
    nlE: `≦̸`,
    nlarr: `↚`,
    nldr: `‥`,
    nle: `≰`,
    nleftarrow: `↚`,
    nleftrightarrow: `↮`,
    nleq: `≰`,
    nleqq: `≦̸`,
    nleqslant: `⩽̸`,
    nles: `⩽̸`,
    nless: `≮`,
    nlsim: `≴`,
    nlt: `≮`,
    nltri: `⋪`,
    nltrie: `⋬`,
    nmid: `∤`,
    nopf: `𝕟`,
    not: `¬`,
    notin: `∉`,
    notinE: `⋹̸`,
    notindot: `⋵̸`,
    notinva: `∉`,
    notinvb: `⋷`,
    notinvc: `⋶`,
    notni: `∌`,
    notniva: `∌`,
    notnivb: `⋾`,
    notnivc: `⋽`,
    npar: `∦`,
    nparallel: `∦`,
    nparsl: `⫽⃥`,
    npart: `∂̸`,
    npolint: `⨔`,
    npr: `⊀`,
    nprcue: `⋠`,
    npre: `⪯̸`,
    nprec: `⊀`,
    npreceq: `⪯̸`,
    nrArr: `⇏`,
    nrarr: `↛`,
    nrarrc: `⤳̸`,
    nrarrw: `↝̸`,
    nrightarrow: `↛`,
    nrtri: `⋫`,
    nrtrie: `⋭`,
    nsc: `⊁`,
    nsccue: `⋡`,
    nsce: `⪰̸`,
    nscr: `𝓃`,
    nshortmid: `∤`,
    nshortparallel: `∦`,
    nsim: `≁`,
    nsime: `≄`,
    nsimeq: `≄`,
    nsmid: `∤`,
    nspar: `∦`,
    nsqsube: `⋢`,
    nsqsupe: `⋣`,
    nsub: `⊄`,
    nsubE: `⫅̸`,
    nsube: `⊈`,
    nsubset: `⊂⃒`,
    nsubseteq: `⊈`,
    nsubseteqq: `⫅̸`,
    nsucc: `⊁`,
    nsucceq: `⪰̸`,
    nsup: `⊅`,
    nsupE: `⫆̸`,
    nsupe: `⊉`,
    nsupset: `⊃⃒`,
    nsupseteq: `⊉`,
    nsupseteqq: `⫆̸`,
    ntgl: `≹`,
    ntilde: `ñ`,
    ntlg: `≸`,
    ntriangleleft: `⋪`,
    ntrianglelefteq: `⋬`,
    ntriangleright: `⋫`,
    ntrianglerighteq: `⋭`,
    nu: `ν`,
    num: `#`,
    numero: `№`,
    numsp: ` `,
    nvDash: `⊭`,
    nvHarr: `⤄`,
    nvap: `≍⃒`,
    nvdash: `⊬`,
    nvge: `≥⃒`,
    nvgt: `>⃒`,
    nvinfin: `⧞`,
    nvlArr: `⤂`,
    nvle: `≤⃒`,
    nvlt: `<⃒`,
    nvltrie: `⊴⃒`,
    nvrArr: `⤃`,
    nvrtrie: `⊵⃒`,
    nvsim: `∼⃒`,
    nwArr: `⇖`,
    nwarhk: `⤣`,
    nwarr: `↖`,
    nwarrow: `↖`,
    nwnear: `⤧`,
    oS: `Ⓢ`,
    oacute: `ó`,
    oast: `⊛`,
    ocir: `⊚`,
    ocirc: `ô`,
    ocy: `о`,
    odash: `⊝`,
    odblac: `ő`,
    odiv: `⨸`,
    odot: `⊙`,
    odsold: `⦼`,
    oelig: `œ`,
    ofcir: `⦿`,
    ofr: `𝔬`,
    ogon: `˛`,
    ograve: `ò`,
    ogt: `⧁`,
    ohbar: `⦵`,
    ohm: `Ω`,
    oint: `∮`,
    olarr: `↺`,
    olcir: `⦾`,
    olcross: `⦻`,
    oline: `‾`,
    olt: `⧀`,
    omacr: `ō`,
    omega: `ω`,
    omicron: `ο`,
    omid: `⦶`,
    ominus: `⊖`,
    oopf: `𝕠`,
    opar: `⦷`,
    operp: `⦹`,
    oplus: `⊕`,
    or: `∨`,
    orarr: `↻`,
    ord: `⩝`,
    order: `ℴ`,
    orderof: `ℴ`,
    ordf: `ª`,
    ordm: `º`,
    origof: `⊶`,
    oror: `⩖`,
    orslope: `⩗`,
    orv: `⩛`,
    oscr: `ℴ`,
    oslash: `ø`,
    osol: `⊘`,
    otilde: `õ`,
    otimes: `⊗`,
    otimesas: `⨶`,
    ouml: `ö`,
    ovbar: `⌽`,
    par: `∥`,
    para: `¶`,
    parallel: `∥`,
    parsim: `⫳`,
    parsl: `⫽`,
    part: `∂`,
    pcy: `п`,
    percnt: `%`,
    period: `.`,
    permil: `‰`,
    perp: `⊥`,
    pertenk: `‱`,
    pfr: `𝔭`,
    phi: `φ`,
    phiv: `ϕ`,
    phmmat: `ℳ`,
    phone: `☎`,
    pi: `π`,
    pitchfork: `⋔`,
    piv: `ϖ`,
    planck: `ℏ`,
    planckh: `ℎ`,
    plankv: `ℏ`,
    plus: `+`,
    plusacir: `⨣`,
    plusb: `⊞`,
    pluscir: `⨢`,
    plusdo: `∔`,
    plusdu: `⨥`,
    pluse: `⩲`,
    plusmn: `±`,
    plussim: `⨦`,
    plustwo: `⨧`,
    pm: `±`,
    pointint: `⨕`,
    popf: `𝕡`,
    pound: `£`,
    pr: `≺`,
    prE: `⪳`,
    prap: `⪷`,
    prcue: `≼`,
    pre: `⪯`,
    prec: `≺`,
    precapprox: `⪷`,
    preccurlyeq: `≼`,
    preceq: `⪯`,
    precnapprox: `⪹`,
    precneqq: `⪵`,
    precnsim: `⋨`,
    precsim: `≾`,
    prime: `′`,
    primes: `ℙ`,
    prnE: `⪵`,
    prnap: `⪹`,
    prnsim: `⋨`,
    prod: `∏`,
    profalar: `⌮`,
    profline: `⌒`,
    profsurf: `⌓`,
    prop: `∝`,
    propto: `∝`,
    prsim: `≾`,
    prurel: `⊰`,
    pscr: `𝓅`,
    psi: `ψ`,
    puncsp: ` `,
    qfr: `𝔮`,
    qint: `⨌`,
    qopf: `𝕢`,
    qprime: `⁗`,
    qscr: `𝓆`,
    quaternions: `ℍ`,
    quatint: `⨖`,
    quest: `?`,
    questeq: `≟`,
    quot: `"`,
    rAarr: `⇛`,
    rArr: `⇒`,
    rAtail: `⤜`,
    rBarr: `⤏`,
    rHar: `⥤`,
    race: `∽̱`,
    racute: `ŕ`,
    radic: `√`,
    raemptyv: `⦳`,
    rang: `⟩`,
    rangd: `⦒`,
    range: `⦥`,
    rangle: `⟩`,
    raquo: `»`,
    rarr: `→`,
    rarrap: `⥵`,
    rarrb: `⇥`,
    rarrbfs: `⤠`,
    rarrc: `⤳`,
    rarrfs: `⤞`,
    rarrhk: `↪`,
    rarrlp: `↬`,
    rarrpl: `⥅`,
    rarrsim: `⥴`,
    rarrtl: `↣`,
    rarrw: `↝`,
    ratail: `⤚`,
    ratio: `∶`,
    rationals: `ℚ`,
    rbarr: `⤍`,
    rbbrk: `❳`,
    rbrace: `}`,
    rbrack: `]`,
    rbrke: `⦌`,
    rbrksld: `⦎`,
    rbrkslu: `⦐`,
    rcaron: `ř`,
    rcedil: `ŗ`,
    rceil: `⌉`,
    rcub: `}`,
    rcy: `р`,
    rdca: `⤷`,
    rdldhar: `⥩`,
    rdquo: `”`,
    rdquor: `”`,
    rdsh: `↳`,
    real: `ℜ`,
    realine: `ℛ`,
    realpart: `ℜ`,
    reals: `ℝ`,
    rect: `▭`,
    reg: `®`,
    rfisht: `⥽`,
    rfloor: `⌋`,
    rfr: `𝔯`,
    rhard: `⇁`,
    rharu: `⇀`,
    rharul: `⥬`,
    rho: `ρ`,
    rhov: `ϱ`,
    rightarrow: `→`,
    rightarrowtail: `↣`,
    rightharpoondown: `⇁`,
    rightharpoonup: `⇀`,
    rightleftarrows: `⇄`,
    rightleftharpoons: `⇌`,
    rightrightarrows: `⇉`,
    rightsquigarrow: `↝`,
    rightthreetimes: `⋌`,
    ring: `˚`,
    risingdotseq: `≓`,
    rlarr: `⇄`,
    rlhar: `⇌`,
    rlm: `‏`,
    rmoust: `⎱`,
    rmoustache: `⎱`,
    rnmid: `⫮`,
    roang: `⟭`,
    roarr: `⇾`,
    robrk: `⟧`,
    ropar: `⦆`,
    ropf: `𝕣`,
    roplus: `⨮`,
    rotimes: `⨵`,
    rpar: `)`,
    rpargt: `⦔`,
    rppolint: `⨒`,
    rrarr: `⇉`,
    rsaquo: `›`,
    rscr: `𝓇`,
    rsh: `↱`,
    rsqb: `]`,
    rsquo: `’`,
    rsquor: `’`,
    rthree: `⋌`,
    rtimes: `⋊`,
    rtri: `▹`,
    rtrie: `⊵`,
    rtrif: `▸`,
    rtriltri: `⧎`,
    ruluhar: `⥨`,
    rx: `℞`,
    sacute: `ś`,
    sbquo: `‚`,
    sc: `≻`,
    scE: `⪴`,
    scap: `⪸`,
    scaron: `š`,
    sccue: `≽`,
    sce: `⪰`,
    scedil: `ş`,
    scirc: `ŝ`,
    scnE: `⪶`,
    scnap: `⪺`,
    scnsim: `⋩`,
    scpolint: `⨓`,
    scsim: `≿`,
    scy: `с`,
    sdot: `⋅`,
    sdotb: `⊡`,
    sdote: `⩦`,
    seArr: `⇘`,
    searhk: `⤥`,
    searr: `↘`,
    searrow: `↘`,
    sect: `§`,
    semi: `;`,
    seswar: `⤩`,
    setminus: `∖`,
    setmn: `∖`,
    sext: `✶`,
    sfr: `𝔰`,
    sfrown: `⌢`,
    sharp: `♯`,
    shchcy: `щ`,
    shcy: `ш`,
    shortmid: `∣`,
    shortparallel: `∥`,
    shy: `­`,
    sigma: `σ`,
    sigmaf: `ς`,
    sigmav: `ς`,
    sim: `∼`,
    simdot: `⩪`,
    sime: `≃`,
    simeq: `≃`,
    simg: `⪞`,
    simgE: `⪠`,
    siml: `⪝`,
    simlE: `⪟`,
    simne: `≆`,
    simplus: `⨤`,
    simrarr: `⥲`,
    slarr: `←`,
    smallsetminus: `∖`,
    smashp: `⨳`,
    smeparsl: `⧤`,
    smid: `∣`,
    smile: `⌣`,
    smt: `⪪`,
    smte: `⪬`,
    smtes: `⪬︀`,
    softcy: `ь`,
    sol: `/`,
    solb: `⧄`,
    solbar: `⌿`,
    sopf: `𝕤`,
    spades: `♠`,
    spadesuit: `♠`,
    spar: `∥`,
    sqcap: `⊓`,
    sqcaps: `⊓︀`,
    sqcup: `⊔`,
    sqcups: `⊔︀`,
    sqsub: `⊏`,
    sqsube: `⊑`,
    sqsubset: `⊏`,
    sqsubseteq: `⊑`,
    sqsup: `⊐`,
    sqsupe: `⊒`,
    sqsupset: `⊐`,
    sqsupseteq: `⊒`,
    squ: `□`,
    square: `□`,
    squarf: `▪`,
    squf: `▪`,
    srarr: `→`,
    sscr: `𝓈`,
    ssetmn: `∖`,
    ssmile: `⌣`,
    sstarf: `⋆`,
    star: `☆`,
    starf: `★`,
    straightepsilon: `ϵ`,
    straightphi: `ϕ`,
    strns: `¯`,
    sub: `⊂`,
    subE: `⫅`,
    subdot: `⪽`,
    sube: `⊆`,
    subedot: `⫃`,
    submult: `⫁`,
    subnE: `⫋`,
    subne: `⊊`,
    subplus: `⪿`,
    subrarr: `⥹`,
    subset: `⊂`,
    subseteq: `⊆`,
    subseteqq: `⫅`,
    subsetneq: `⊊`,
    subsetneqq: `⫋`,
    subsim: `⫇`,
    subsub: `⫕`,
    subsup: `⫓`,
    succ: `≻`,
    succapprox: `⪸`,
    succcurlyeq: `≽`,
    succeq: `⪰`,
    succnapprox: `⪺`,
    succneqq: `⪶`,
    succnsim: `⋩`,
    succsim: `≿`,
    sum: `∑`,
    sung: `♪`,
    sup1: `¹`,
    sup2: `²`,
    sup3: `³`,
    sup: `⊃`,
    supE: `⫆`,
    supdot: `⪾`,
    supdsub: `⫘`,
    supe: `⊇`,
    supedot: `⫄`,
    suphsol: `⟉`,
    suphsub: `⫗`,
    suplarr: `⥻`,
    supmult: `⫂`,
    supnE: `⫌`,
    supne: `⊋`,
    supplus: `⫀`,
    supset: `⊃`,
    supseteq: `⊇`,
    supseteqq: `⫆`,
    supsetneq: `⊋`,
    supsetneqq: `⫌`,
    supsim: `⫈`,
    supsub: `⫔`,
    supsup: `⫖`,
    swArr: `⇙`,
    swarhk: `⤦`,
    swarr: `↙`,
    swarrow: `↙`,
    swnwar: `⤪`,
    szlig: `ß`,
    target: `⌖`,
    tau: `τ`,
    tbrk: `⎴`,
    tcaron: `ť`,
    tcedil: `ţ`,
    tcy: `т`,
    tdot: `⃛`,
    telrec: `⌕`,
    tfr: `𝔱`,
    there4: `∴`,
    therefore: `∴`,
    theta: `θ`,
    thetasym: `ϑ`,
    thetav: `ϑ`,
    thickapprox: `≈`,
    thicksim: `∼`,
    thinsp: ` `,
    thkap: `≈`,
    thksim: `∼`,
    thorn: `þ`,
    tilde: `˜`,
    times: `×`,
    timesb: `⊠`,
    timesbar: `⨱`,
    timesd: `⨰`,
    tint: `∭`,
    toea: `⤨`,
    top: `⊤`,
    topbot: `⌶`,
    topcir: `⫱`,
    topf: `𝕥`,
    topfork: `⫚`,
    tosa: `⤩`,
    tprime: `‴`,
    trade: `™`,
    triangle: `▵`,
    triangledown: `▿`,
    triangleleft: `◃`,
    trianglelefteq: `⊴`,
    triangleq: `≜`,
    triangleright: `▹`,
    trianglerighteq: `⊵`,
    tridot: `◬`,
    trie: `≜`,
    triminus: `⨺`,
    triplus: `⨹`,
    trisb: `⧍`,
    tritime: `⨻`,
    trpezium: `⏢`,
    tscr: `𝓉`,
    tscy: `ц`,
    tshcy: `ћ`,
    tstrok: `ŧ`,
    twixt: `≬`,
    twoheadleftarrow: `↞`,
    twoheadrightarrow: `↠`,
    uArr: `⇑`,
    uHar: `⥣`,
    uacute: `ú`,
    uarr: `↑`,
    ubrcy: `ў`,
    ubreve: `ŭ`,
    ucirc: `û`,
    ucy: `у`,
    udarr: `⇅`,
    udblac: `ű`,
    udhar: `⥮`,
    ufisht: `⥾`,
    ufr: `𝔲`,
    ugrave: `ù`,
    uharl: `↿`,
    uharr: `↾`,
    uhblk: `▀`,
    ulcorn: `⌜`,
    ulcorner: `⌜`,
    ulcrop: `⌏`,
    ultri: `◸`,
    umacr: `ū`,
    uml: `¨`,
    uogon: `ų`,
    uopf: `𝕦`,
    uparrow: `↑`,
    updownarrow: `↕`,
    upharpoonleft: `↿`,
    upharpoonright: `↾`,
    uplus: `⊎`,
    upsi: `υ`,
    upsih: `ϒ`,
    upsilon: `υ`,
    upuparrows: `⇈`,
    urcorn: `⌝`,
    urcorner: `⌝`,
    urcrop: `⌎`,
    uring: `ů`,
    urtri: `◹`,
    uscr: `𝓊`,
    utdot: `⋰`,
    utilde: `ũ`,
    utri: `▵`,
    utrif: `▴`,
    uuarr: `⇈`,
    uuml: `ü`,
    uwangle: `⦧`,
    vArr: `⇕`,
    vBar: `⫨`,
    vBarv: `⫩`,
    vDash: `⊨`,
    vangrt: `⦜`,
    varepsilon: `ϵ`,
    varkappa: `ϰ`,
    varnothing: `∅`,
    varphi: `ϕ`,
    varpi: `ϖ`,
    varpropto: `∝`,
    varr: `↕`,
    varrho: `ϱ`,
    varsigma: `ς`,
    varsubsetneq: `⊊︀`,
    varsubsetneqq: `⫋︀`,
    varsupsetneq: `⊋︀`,
    varsupsetneqq: `⫌︀`,
    vartheta: `ϑ`,
    vartriangleleft: `⊲`,
    vartriangleright: `⊳`,
    vcy: `в`,
    vdash: `⊢`,
    vee: `∨`,
    veebar: `⊻`,
    veeeq: `≚`,
    vellip: `⋮`,
    verbar: `|`,
    vert: `|`,
    vfr: `𝔳`,
    vltri: `⊲`,
    vnsub: `⊂⃒`,
    vnsup: `⊃⃒`,
    vopf: `𝕧`,
    vprop: `∝`,
    vrtri: `⊳`,
    vscr: `𝓋`,
    vsubnE: `⫋︀`,
    vsubne: `⊊︀`,
    vsupnE: `⫌︀`,
    vsupne: `⊋︀`,
    vzigzag: `⦚`,
    wcirc: `ŵ`,
    wedbar: `⩟`,
    wedge: `∧`,
    wedgeq: `≙`,
    weierp: `℘`,
    wfr: `𝔴`,
    wopf: `𝕨`,
    wp: `℘`,
    wr: `≀`,
    wreath: `≀`,
    wscr: `𝓌`,
    xcap: `⋂`,
    xcirc: `◯`,
    xcup: `⋃`,
    xdtri: `▽`,
    xfr: `𝔵`,
    xhArr: `⟺`,
    xharr: `⟷`,
    xi: `ξ`,
    xlArr: `⟸`,
    xlarr: `⟵`,
    xmap: `⟼`,
    xnis: `⋻`,
    xodot: `⨀`,
    xopf: `𝕩`,
    xoplus: `⨁`,
    xotime: `⨂`,
    xrArr: `⟹`,
    xrarr: `⟶`,
    xscr: `𝓍`,
    xsqcup: `⨆`,
    xuplus: `⨄`,
    xutri: `△`,
    xvee: `⋁`,
    xwedge: `⋀`,
    yacute: `ý`,
    yacy: `я`,
    ycirc: `ŷ`,
    ycy: `ы`,
    yen: `¥`,
    yfr: `𝔶`,
    yicy: `ї`,
    yopf: `𝕪`,
    yscr: `𝓎`,
    yucy: `ю`,
    yuml: `ÿ`,
    zacute: `ź`,
    zcaron: `ž`,
    zcy: `з`,
    zdot: `ż`,
    zeetrf: `ℨ`,
    zeta: `ζ`,
    zfr: `𝔷`,
    zhcy: `ж`,
    zigrarr: `⇝`,
    zopf: `𝕫`,
    zscr: `𝓏`,
    zwj: `‍`,
    zwnj: `‌`,
  },
  Yp = {}.hasOwnProperty;
function Xp(e) {
  return Yp.call(Jp, e) ? Jp[e] : !1;
}
function Zp(e, t, n, r) {
  let i = e.length,
    a = 0,
    o;
  if (
    ((t = t < 0 ? (-t > i ? 0 : i + t) : t > i ? i : t),
    (n = n > 0 ? n : 0),
    r.length < 1e4)
  )
    ((o = Array.from(r)), o.unshift(t, n), e.splice(...o));
  else
    for (n && e.splice(t, n); a < r.length; )
      ((o = r.slice(a, a + 1e4)),
        o.unshift(t, 0),
        e.splice(...o),
        (a += 1e4),
        (t += 1e4));
}
function Qp(e, t) {
  return e.length > 0 ? (Zp(e, e.length, 0, t), e) : t;
}
var $p = {}.hasOwnProperty;
function em(e) {
  let t = {},
    n = -1;
  for (; ++n < e.length; ) tm(t, e[n]);
  return t;
}
function tm(e, t) {
  let n;
  for (n in t) {
    let r = ($p.call(e, n) ? e[n] : void 0) || (e[n] = {}),
      i = t[n],
      a;
    if (i)
      for (a in i) {
        $p.call(r, a) || (r[a] = []);
        let e = i[a];
        nm(r[a], Array.isArray(e) ? e : e ? [e] : []);
      }
  }
}
function nm(e, t) {
  let n = -1,
    r = [];
  for (; ++n < t.length; ) (t[n].add === `after` ? e : r).push(t[n]);
  Zp(e, 0, 0, r);
}
function rm(e, t) {
  let n = Number.parseInt(e, t);
  return n < 9 ||
    n === 11 ||
    (n > 13 && n < 32) ||
    (n > 126 && n < 160) ||
    (n > 55295 && n < 57344) ||
    (n > 64975 && n < 65008) ||
    (n & 65535) == 65535 ||
    (n & 65535) == 65534 ||
    n > 1114111
    ? `�`
    : String.fromCodePoint(n);
}
function im(e) {
  return e
    .replace(/[\t\n\r ]+/g, ` `)
    .replace(/^ | $/g, ``)
    .toLowerCase()
    .toUpperCase();
}
var am = gm(/[A-Za-z]/),
  om = gm(/[\dA-Za-z]/),
  sm = gm(/[#-'*+\--9=?A-Z^-~]/);
function cm(e) {
  return e !== null && (e < 32 || e === 127);
}
var lm = gm(/\d/),
  um = gm(/[\dA-Fa-f]/),
  dm = gm(/[!-/:-@[-`{-~]/);
function H(e) {
  return e !== null && e < -2;
}
function fm(e) {
  return e !== null && (e < 0 || e === 32);
}
function pm(e) {
  return e === -2 || e === -1 || e === 32;
}
var mm = gm(/\p{P}|\p{S}/u),
  hm = gm(/\s/);
function gm(e) {
  return t;
  function t(t) {
    return t !== null && t > -1 && e.test(String.fromCharCode(t));
  }
}
function _m(e, t, n, r) {
  let i = r ? r - 1 : 1 / 0,
    a = 0;
  return o;
  function o(r) {
    return pm(r) ? (e.enter(n), s(r)) : t(r);
  }
  function s(r) {
    return pm(r) && a++ < i ? (e.consume(r), s) : (e.exit(n), t(r));
  }
}
var vm = { tokenize: ym };
function ym(e) {
  let t = e.attempt(this.parser.constructs.contentInitial, r, i),
    n;
  return t;
  function r(n) {
    if (n === null) {
      e.consume(n);
      return;
    }
    return (
      e.enter(`lineEnding`),
      e.consume(n),
      e.exit(`lineEnding`),
      _m(e, t, `linePrefix`)
    );
  }
  function i(t) {
    return (e.enter(`paragraph`), a(t));
  }
  function a(t) {
    let r = e.enter(`chunkText`, { contentType: `text`, previous: n });
    return (n && (n.next = r), (n = r), o(t));
  }
  function o(t) {
    if (t === null) {
      (e.exit(`chunkText`), e.exit(`paragraph`), e.consume(t));
      return;
    }
    return H(t) ? (e.consume(t), e.exit(`chunkText`), a) : (e.consume(t), o);
  }
}
var bm = { tokenize: Sm },
  xm = { tokenize: Cm };
function Sm(e) {
  let t = this,
    n = [],
    r = 0,
    i,
    a,
    o;
  return s;
  function s(i) {
    if (r < n.length) {
      let a = n[r];
      return ((t.containerState = a[1]), e.attempt(a[0].continuation, c, l)(i));
    }
    return l(i);
  }
  function c(e) {
    if ((r++, t.containerState._closeFlow)) {
      ((t.containerState._closeFlow = void 0), i && v());
      let n = t.events.length,
        a = n,
        o;
      for (; a--; )
        if (t.events[a][0] === `exit` && t.events[a][1].type === `chunkFlow`) {
          o = t.events[a][1].end;
          break;
        }
      _(r);
      let s = n;
      for (; s < t.events.length; ) ((t.events[s][1].end = { ...o }), s++);
      return (
        Zp(t.events, a + 1, 0, t.events.slice(n)),
        (t.events.length = s),
        l(e)
      );
    }
    return s(e);
  }
  function l(a) {
    if (r === n.length) {
      if (!i) return f(a);
      if (i.currentConstruct && i.currentConstruct.concrete) return m(a);
      t.interrupt = !!(i.currentConstruct && !i._gfmTableDynamicInterruptHack);
    }
    return ((t.containerState = {}), e.check(xm, u, d)(a));
  }
  function u(e) {
    return (i && v(), _(r), f(e));
  }
  function d(e) {
    return (
      (t.parser.lazy[t.now().line] = r !== n.length),
      (o = t.now().offset),
      m(e)
    );
  }
  function f(n) {
    return ((t.containerState = {}), e.attempt(xm, p, m)(n));
  }
  function p(e) {
    return (r++, n.push([t.currentConstruct, t.containerState]), f(e));
  }
  function m(n) {
    if (n === null) {
      (i && v(), _(0), e.consume(n));
      return;
    }
    return (
      (i ||= t.parser.flow(t.now())),
      e.enter(`chunkFlow`, { _tokenizer: i, contentType: `flow`, previous: a }),
      h(n)
    );
  }
  function h(n) {
    if (n === null) {
      (g(e.exit(`chunkFlow`), !0), _(0), e.consume(n));
      return;
    }
    return H(n)
      ? (e.consume(n),
        g(e.exit(`chunkFlow`)),
        (r = 0),
        (t.interrupt = void 0),
        s)
      : (e.consume(n), h);
  }
  function g(e, n) {
    let s = t.sliceStream(e);
    if (
      (n && s.push(null),
      (e.previous = a),
      a && (a.next = e),
      (a = e),
      i.defineSkip(e.start),
      i.write(s),
      t.parser.lazy[e.start.line])
    ) {
      let e = i.events.length;
      for (; e--; )
        if (
          i.events[e][1].start.offset < o &&
          (!i.events[e][1].end || i.events[e][1].end.offset > o)
        )
          return;
      let n = t.events.length,
        a = n,
        s,
        c;
      for (; a--; )
        if (t.events[a][0] === `exit` && t.events[a][1].type === `chunkFlow`) {
          if (s) {
            c = t.events[a][1].end;
            break;
          }
          s = !0;
        }
      for (_(r), e = n; e < t.events.length; )
        ((t.events[e][1].end = { ...c }), e++);
      (Zp(t.events, a + 1, 0, t.events.slice(n)), (t.events.length = e));
    }
  }
  function _(r) {
    let i = n.length;
    for (; i-- > r; ) {
      let r = n[i];
      ((t.containerState = r[1]), r[0].exit.call(t, e));
    }
    n.length = r;
  }
  function v() {
    (i.write([null]),
      (a = void 0),
      (i = void 0),
      (t.containerState._closeFlow = void 0));
  }
}
function Cm(e, t, n) {
  return _m(
    e,
    e.attempt(this.parser.constructs.document, t, n),
    `linePrefix`,
    this.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
  );
}
function wm(e) {
  if (e === null || fm(e) || hm(e)) return 1;
  if (mm(e)) return 2;
}
function Tm(e, t, n) {
  let r = [],
    i = -1;
  for (; ++i < e.length; ) {
    let a = e[i].resolveAll;
    a && !r.includes(a) && ((t = a(t, n)), r.push(a));
  }
  return t;
}
var Em = { name: `attention`, resolveAll: Dm, tokenize: Om };
function Dm(e, t) {
  let n = -1,
    r,
    i,
    a,
    o,
    s,
    c,
    l,
    u;
  for (; ++n < e.length; )
    if (
      e[n][0] === `enter` &&
      e[n][1].type === `attentionSequence` &&
      e[n][1]._close
    ) {
      for (r = n; r--; )
        if (
          e[r][0] === `exit` &&
          e[r][1].type === `attentionSequence` &&
          e[r][1]._open &&
          t.sliceSerialize(e[r][1]).charCodeAt(0) ===
            t.sliceSerialize(e[n][1]).charCodeAt(0)
        ) {
          if (
            (e[r][1]._close || e[n][1]._open) &&
            (e[n][1].end.offset - e[n][1].start.offset) % 3 &&
            !(
              (e[r][1].end.offset -
                e[r][1].start.offset +
                e[n][1].end.offset -
                e[n][1].start.offset) %
              3
            )
          )
            continue;
          c =
            e[r][1].end.offset - e[r][1].start.offset > 1 &&
            e[n][1].end.offset - e[n][1].start.offset > 1
              ? 2
              : 1;
          let d = { ...e[r][1].end },
            f = { ...e[n][1].start };
          (km(d, -c),
            km(f, c),
            (o = {
              type: c > 1 ? `strongSequence` : `emphasisSequence`,
              start: d,
              end: { ...e[r][1].end },
            }),
            (s = {
              type: c > 1 ? `strongSequence` : `emphasisSequence`,
              start: { ...e[n][1].start },
              end: f,
            }),
            (a = {
              type: c > 1 ? `strongText` : `emphasisText`,
              start: { ...e[r][1].end },
              end: { ...e[n][1].start },
            }),
            (i = {
              type: c > 1 ? `strong` : `emphasis`,
              start: { ...o.start },
              end: { ...s.end },
            }),
            (e[r][1].end = { ...o.start }),
            (e[n][1].start = { ...s.end }),
            (l = []),
            e[r][1].end.offset - e[r][1].start.offset &&
              (l = Qp(l, [
                [`enter`, e[r][1], t],
                [`exit`, e[r][1], t],
              ])),
            (l = Qp(l, [
              [`enter`, i, t],
              [`enter`, o, t],
              [`exit`, o, t],
              [`enter`, a, t],
            ])),
            (l = Qp(
              l,
              Tm(t.parser.constructs.insideSpan.null, e.slice(r + 1, n), t),
            )),
            (l = Qp(l, [
              [`exit`, a, t],
              [`enter`, s, t],
              [`exit`, s, t],
              [`exit`, i, t],
            ])),
            e[n][1].end.offset - e[n][1].start.offset
              ? ((u = 2),
                (l = Qp(l, [
                  [`enter`, e[n][1], t],
                  [`exit`, e[n][1], t],
                ])))
              : (u = 0),
            Zp(e, r - 1, n - r + 3, l),
            (n = r + l.length - u - 2));
          break;
        }
    }
  for (n = -1; ++n < e.length; )
    e[n][1].type === `attentionSequence` && (e[n][1].type = `data`);
  return e;
}
function Om(e, t) {
  let n = this.parser.constructs.attentionMarkers.null,
    r = this.previous,
    i = wm(r),
    a;
  return o;
  function o(t) {
    return ((a = t), e.enter(`attentionSequence`), s(t));
  }
  function s(o) {
    if (o === a) return (e.consume(o), s);
    let c = e.exit(`attentionSequence`),
      l = wm(o),
      u = !l || (l === 2 && i) || n.includes(o),
      d = !i || (i === 2 && l) || n.includes(r);
    return (
      (c._open = !!(a === 42 ? u : u && (i || !d))),
      (c._close = !!(a === 42 ? d : d && (l || !u))),
      t(o)
    );
  }
}
function km(e, t) {
  ((e.column += t), (e.offset += t), (e._bufferIndex += t));
}
var Am = { name: `autolink`, tokenize: jm };
function jm(e, t, n) {
  let r = 0;
  return i;
  function i(t) {
    return (
      e.enter(`autolink`),
      e.enter(`autolinkMarker`),
      e.consume(t),
      e.exit(`autolinkMarker`),
      e.enter(`autolinkProtocol`),
      a
    );
  }
  function a(t) {
    return am(t) ? (e.consume(t), o) : t === 64 ? n(t) : l(t);
  }
  function o(e) {
    return e === 43 || e === 45 || e === 46 || om(e) ? ((r = 1), s(e)) : l(e);
  }
  function s(t) {
    return t === 58
      ? (e.consume(t), (r = 0), c)
      : (t === 43 || t === 45 || t === 46 || om(t)) && r++ < 32
        ? (e.consume(t), s)
        : ((r = 0), l(t));
  }
  function c(r) {
    return r === 62
      ? (e.exit(`autolinkProtocol`),
        e.enter(`autolinkMarker`),
        e.consume(r),
        e.exit(`autolinkMarker`),
        e.exit(`autolink`),
        t)
      : r === null || r === 32 || r === 60 || cm(r)
        ? n(r)
        : (e.consume(r), c);
  }
  function l(t) {
    return t === 64 ? (e.consume(t), u) : sm(t) ? (e.consume(t), l) : n(t);
  }
  function u(e) {
    return om(e) ? d(e) : n(e);
  }
  function d(n) {
    return n === 46
      ? (e.consume(n), (r = 0), u)
      : n === 62
        ? ((e.exit(`autolinkProtocol`).type = `autolinkEmail`),
          e.enter(`autolinkMarker`),
          e.consume(n),
          e.exit(`autolinkMarker`),
          e.exit(`autolink`),
          t)
        : f(n);
  }
  function f(t) {
    if ((t === 45 || om(t)) && r++ < 63) {
      let n = t === 45 ? f : d;
      return (e.consume(t), n);
    }
    return n(t);
  }
}
var Mm = { partial: !0, tokenize: Nm };
function Nm(e, t, n) {
  return r;
  function r(t) {
    return pm(t) ? _m(e, i, `linePrefix`)(t) : i(t);
  }
  function i(e) {
    return e === null || H(e) ? t(e) : n(e);
  }
}
var Pm = {
  continuation: { tokenize: Im },
  exit: Lm,
  name: `blockQuote`,
  tokenize: Fm,
};
function Fm(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    if (t === 62) {
      let n = r.containerState;
      return (
        (n.open ||= (e.enter(`blockQuote`, { _container: !0 }), !0)),
        e.enter(`blockQuotePrefix`),
        e.enter(`blockQuoteMarker`),
        e.consume(t),
        e.exit(`blockQuoteMarker`),
        a
      );
    }
    return n(t);
  }
  function a(n) {
    return pm(n)
      ? (e.enter(`blockQuotePrefixWhitespace`),
        e.consume(n),
        e.exit(`blockQuotePrefixWhitespace`),
        e.exit(`blockQuotePrefix`),
        t)
      : (e.exit(`blockQuotePrefix`), t(n));
  }
}
function Im(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return pm(t)
      ? _m(
          e,
          a,
          `linePrefix`,
          r.parser.constructs.disable.null.includes(`codeIndented`)
            ? void 0
            : 4,
        )(t)
      : a(t);
  }
  function a(r) {
    return e.attempt(Pm, t, n)(r);
  }
}
function Lm(e) {
  e.exit(`blockQuote`);
}
var Rm = { name: `characterEscape`, tokenize: zm };
function zm(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`characterEscape`),
      e.enter(`escapeMarker`),
      e.consume(t),
      e.exit(`escapeMarker`),
      i
    );
  }
  function i(r) {
    return dm(r)
      ? (e.enter(`characterEscapeValue`),
        e.consume(r),
        e.exit(`characterEscapeValue`),
        e.exit(`characterEscape`),
        t)
      : n(r);
  }
}
var Bm = { name: `characterReference`, tokenize: Vm };
function Vm(e, t, n) {
  let r = this,
    i = 0,
    a,
    o;
  return s;
  function s(t) {
    return (
      e.enter(`characterReference`),
      e.enter(`characterReferenceMarker`),
      e.consume(t),
      e.exit(`characterReferenceMarker`),
      c
    );
  }
  function c(t) {
    return t === 35
      ? (e.enter(`characterReferenceMarkerNumeric`),
        e.consume(t),
        e.exit(`characterReferenceMarkerNumeric`),
        l)
      : (e.enter(`characterReferenceValue`), (a = 31), (o = om), u(t));
  }
  function l(t) {
    return t === 88 || t === 120
      ? (e.enter(`characterReferenceMarkerHexadecimal`),
        e.consume(t),
        e.exit(`characterReferenceMarkerHexadecimal`),
        e.enter(`characterReferenceValue`),
        (a = 6),
        (o = um),
        u)
      : (e.enter(`characterReferenceValue`), (a = 7), (o = lm), u(t));
  }
  function u(s) {
    if (s === 59 && i) {
      let i = e.exit(`characterReferenceValue`);
      return o === om && !Xp(r.sliceSerialize(i))
        ? n(s)
        : (e.enter(`characterReferenceMarker`),
          e.consume(s),
          e.exit(`characterReferenceMarker`),
          e.exit(`characterReference`),
          t);
    }
    return o(s) && i++ < a ? (e.consume(s), u) : n(s);
  }
}
var Hm = { partial: !0, tokenize: Gm },
  Um = { concrete: !0, name: `codeFenced`, tokenize: Wm };
function Wm(e, t, n) {
  let r = this,
    i = { partial: !0, tokenize: x },
    a = 0,
    o = 0,
    s;
  return c;
  function c(e) {
    return l(e);
  }
  function l(t) {
    let n = r.events[r.events.length - 1];
    return (
      (a =
        n && n[1].type === `linePrefix`
          ? n[2].sliceSerialize(n[1], !0).length
          : 0),
      (s = t),
      e.enter(`codeFenced`),
      e.enter(`codeFencedFence`),
      e.enter(`codeFencedFenceSequence`),
      u(t)
    );
  }
  function u(t) {
    return t === s
      ? (o++, e.consume(t), u)
      : o < 3
        ? n(t)
        : (e.exit(`codeFencedFenceSequence`),
          pm(t) ? _m(e, d, `whitespace`)(t) : d(t));
  }
  function d(n) {
    return n === null || H(n)
      ? (e.exit(`codeFencedFence`), r.interrupt ? t(n) : e.check(Hm, h, b)(n))
      : (e.enter(`codeFencedFenceInfo`),
        e.enter(`chunkString`, { contentType: `string` }),
        f(n));
  }
  function f(t) {
    return t === null || H(t)
      ? (e.exit(`chunkString`), e.exit(`codeFencedFenceInfo`), d(t))
      : pm(t)
        ? (e.exit(`chunkString`),
          e.exit(`codeFencedFenceInfo`),
          _m(e, p, `whitespace`)(t))
        : t === 96 && t === s
          ? n(t)
          : (e.consume(t), f);
  }
  function p(t) {
    return t === null || H(t)
      ? d(t)
      : (e.enter(`codeFencedFenceMeta`),
        e.enter(`chunkString`, { contentType: `string` }),
        m(t));
  }
  function m(t) {
    return t === null || H(t)
      ? (e.exit(`chunkString`), e.exit(`codeFencedFenceMeta`), d(t))
      : t === 96 && t === s
        ? n(t)
        : (e.consume(t), m);
  }
  function h(t) {
    return e.attempt(i, b, g)(t);
  }
  function g(t) {
    return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), _);
  }
  function _(t) {
    return a > 0 && pm(t) ? _m(e, v, `linePrefix`, a + 1)(t) : v(t);
  }
  function v(t) {
    return t === null || H(t)
      ? e.check(Hm, h, b)(t)
      : (e.enter(`codeFlowValue`), y(t));
  }
  function y(t) {
    return t === null || H(t)
      ? (e.exit(`codeFlowValue`), v(t))
      : (e.consume(t), y);
  }
  function b(n) {
    return (e.exit(`codeFenced`), t(n));
  }
  function x(e, t, n) {
    let i = 0;
    return a;
    function a(t) {
      return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), c);
    }
    function c(t) {
      return (
        e.enter(`codeFencedFence`),
        pm(t)
          ? _m(
              e,
              l,
              `linePrefix`,
              r.parser.constructs.disable.null.includes(`codeIndented`)
                ? void 0
                : 4,
            )(t)
          : l(t)
      );
    }
    function l(t) {
      return t === s ? (e.enter(`codeFencedFenceSequence`), u(t)) : n(t);
    }
    function u(t) {
      return t === s
        ? (i++, e.consume(t), u)
        : i >= o
          ? (e.exit(`codeFencedFenceSequence`),
            pm(t) ? _m(e, d, `whitespace`)(t) : d(t))
          : n(t);
    }
    function d(r) {
      return r === null || H(r) ? (e.exit(`codeFencedFence`), t(r)) : n(r);
    }
  }
}
function Gm(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return t === null
      ? n(t)
      : (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), a);
  }
  function a(e) {
    return r.parser.lazy[r.now().line] ? n(e) : t(e);
  }
}
var Km = { name: `codeIndented`, tokenize: Jm },
  qm = { partial: !0, tokenize: Ym };
function Jm(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (e.enter(`codeIndented`), _m(e, a, `linePrefix`, 5)(t));
  }
  function a(e) {
    let t = r.events[r.events.length - 1];
    return t &&
      t[1].type === `linePrefix` &&
      t[2].sliceSerialize(t[1], !0).length >= 4
      ? o(e)
      : n(e);
  }
  function o(t) {
    return t === null
      ? c(t)
      : H(t)
        ? e.attempt(qm, o, c)(t)
        : (e.enter(`codeFlowValue`), s(t));
  }
  function s(t) {
    return t === null || H(t)
      ? (e.exit(`codeFlowValue`), o(t))
      : (e.consume(t), s);
  }
  function c(n) {
    return (e.exit(`codeIndented`), t(n));
  }
}
function Ym(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return r.parser.lazy[r.now().line]
      ? n(t)
      : H(t)
        ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), i)
        : _m(e, a, `linePrefix`, 5)(t);
  }
  function a(e) {
    let a = r.events[r.events.length - 1];
    return a &&
      a[1].type === `linePrefix` &&
      a[2].sliceSerialize(a[1], !0).length >= 4
      ? t(e)
      : H(e)
        ? i(e)
        : n(e);
  }
}
var Xm = { name: `codeText`, previous: Qm, resolve: Zm, tokenize: $m };
function Zm(e) {
  let t = e.length - 4,
    n = 3,
    r,
    i;
  if (
    (e[n][1].type === `lineEnding` || e[n][1].type === `space`) &&
    (e[t][1].type === `lineEnding` || e[t][1].type === `space`)
  ) {
    for (r = n; ++r < t; )
      if (e[r][1].type === `codeTextData`) {
        ((e[n][1].type = `codeTextPadding`),
          (e[t][1].type = `codeTextPadding`),
          (n += 2),
          (t -= 2));
        break;
      }
  }
  for (r = n - 1, t++; ++r <= t; )
    i === void 0
      ? r !== t && e[r][1].type !== `lineEnding` && (i = r)
      : (r === t || e[r][1].type === `lineEnding`) &&
        ((e[i][1].type = `codeTextData`),
        r !== i + 2 &&
          ((e[i][1].end = e[r - 1][1].end),
          e.splice(i + 2, r - i - 2),
          (t -= r - i - 2),
          (r = i + 2)),
        (i = void 0));
  return e;
}
function Qm(e) {
  return (
    e !== 96 ||
    this.events[this.events.length - 1][1].type === `characterEscape`
  );
}
function $m(e, t, n) {
  let r = 0,
    i,
    a;
  return o;
  function o(t) {
    return (e.enter(`codeText`), e.enter(`codeTextSequence`), s(t));
  }
  function s(t) {
    return t === 96
      ? (e.consume(t), r++, s)
      : (e.exit(`codeTextSequence`), c(t));
  }
  function c(t) {
    return t === null
      ? n(t)
      : t === 32
        ? (e.enter(`space`), e.consume(t), e.exit(`space`), c)
        : t === 96
          ? ((a = e.enter(`codeTextSequence`)), (i = 0), u(t))
          : H(t)
            ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), c)
            : (e.enter(`codeTextData`), l(t));
  }
  function l(t) {
    return t === null || t === 32 || t === 96 || H(t)
      ? (e.exit(`codeTextData`), c(t))
      : (e.consume(t), l);
  }
  function u(n) {
    return n === 96
      ? (e.consume(n), i++, u)
      : i === r
        ? (e.exit(`codeTextSequence`), e.exit(`codeText`), t(n))
        : ((a.type = `codeTextData`), l(n));
  }
}
var eh = class {
  constructor(e) {
    ((this.left = e ? [...e] : []), (this.right = []));
  }
  get(e) {
    if (e < 0 || e >= this.left.length + this.right.length)
      throw RangeError(
        "Cannot access index `" +
          e +
          "` in a splice buffer of size `" +
          (this.left.length + this.right.length) +
          "`",
      );
    return e < this.left.length
      ? this.left[e]
      : this.right[this.right.length - e + this.left.length - 1];
  }
  get length() {
    return this.left.length + this.right.length;
  }
  shift() {
    return (this.setCursor(0), this.right.pop());
  }
  slice(e, t) {
    let n = t ?? 1 / 0;
    return n < this.left.length
      ? this.left.slice(e, n)
      : e > this.left.length
        ? this.right
            .slice(
              this.right.length - n + this.left.length,
              this.right.length - e + this.left.length,
            )
            .reverse()
        : this.left
            .slice(e)
            .concat(
              this.right
                .slice(this.right.length - n + this.left.length)
                .reverse(),
            );
  }
  splice(e, t, n) {
    let r = t || 0;
    this.setCursor(Math.trunc(e));
    let i = this.right.splice(this.right.length - r, 1 / 0);
    return (n && th(this.left, n), i.reverse());
  }
  pop() {
    return (this.setCursor(1 / 0), this.left.pop());
  }
  push(e) {
    (this.setCursor(1 / 0), this.left.push(e));
  }
  pushMany(e) {
    (this.setCursor(1 / 0), th(this.left, e));
  }
  unshift(e) {
    (this.setCursor(0), this.right.push(e));
  }
  unshiftMany(e) {
    (this.setCursor(0), th(this.right, e.reverse()));
  }
  setCursor(e) {
    if (
      !(
        e === this.left.length ||
        (e > this.left.length && this.right.length === 0) ||
        (e < 0 && this.left.length === 0)
      )
    )
      if (e < this.left.length) {
        let t = this.left.splice(e, 1 / 0);
        th(this.right, t.reverse());
      } else {
        let t = this.right.splice(
          this.left.length + this.right.length - e,
          1 / 0,
        );
        th(this.left, t.reverse());
      }
  }
};
function th(e, t) {
  let n = 0;
  if (t.length < 1e4) e.push(...t);
  else for (; n < t.length; ) (e.push(...t.slice(n, n + 1e4)), (n += 1e4));
}
function nh(e) {
  let t = {},
    n = -1,
    r,
    i,
    a,
    o,
    s,
    c,
    l,
    u = new eh(e);
  for (; ++n < u.length; ) {
    for (; n in t; ) n = t[n];
    if (
      ((r = u.get(n)),
      n &&
        r[1].type === `chunkFlow` &&
        u.get(n - 1)[1].type === `listItemPrefix` &&
        ((c = r[1]._tokenizer.events),
        (a = 0),
        a < c.length && c[a][1].type === `lineEndingBlank` && (a += 2),
        a < c.length && c[a][1].type === `content`))
    )
      for (; ++a < c.length && c[a][1].type !== `content`; )
        c[a][1].type === `chunkText` &&
          ((c[a][1]._isInFirstContentOfListItem = !0), a++);
    if (r[0] === `enter`)
      r[1].contentType && (Object.assign(t, rh(u, n)), (n = t[n]), (l = !0));
    else if (r[1]._container) {
      for (a = n, i = void 0; a--; )
        if (
          ((o = u.get(a)),
          o[1].type === `lineEnding` || o[1].type === `lineEndingBlank`)
        )
          o[0] === `enter` &&
            (i && (u.get(i)[1].type = `lineEndingBlank`),
            (o[1].type = `lineEnding`),
            (i = a));
        else if (
          !(o[1].type === `linePrefix` || o[1].type === `listItemIndent`)
        )
          break;
      i &&
        ((r[1].end = { ...u.get(i)[1].start }),
        (s = u.slice(i, n)),
        s.unshift(r),
        u.splice(i, n - i + 1, s));
    }
  }
  return (Zp(e, 0, 1 / 0, u.slice(0)), !l);
}
function rh(e, t) {
  let n = e.get(t)[1],
    r = e.get(t)[2],
    i = t - 1,
    a = [],
    o = n._tokenizer;
  o ||
    ((o = r.parser[n.contentType](n.start)),
    n._contentTypeTextTrailing && (o._contentTypeTextTrailing = !0));
  let s = o.events,
    c = [],
    l = {},
    u,
    d,
    f = -1,
    p = n,
    m = 0,
    h = 0,
    g = [h];
  for (; p; ) {
    for (; e.get(++i)[1] !== p; );
    (a.push(i),
      p._tokenizer ||
        ((u = r.sliceStream(p)),
        p.next || u.push(null),
        d && o.defineSkip(p.start),
        p._isInFirstContentOfListItem &&
          (o._gfmTasklistFirstContentOfListItem = !0),
        o.write(u),
        p._isInFirstContentOfListItem &&
          (o._gfmTasklistFirstContentOfListItem = void 0)),
      (d = p),
      (p = p.next));
  }
  for (p = n; ++f < s.length; )
    s[f][0] === `exit` &&
      s[f - 1][0] === `enter` &&
      s[f][1].type === s[f - 1][1].type &&
      s[f][1].start.line !== s[f][1].end.line &&
      ((h = f + 1),
      g.push(h),
      (p._tokenizer = void 0),
      (p.previous = void 0),
      (p = p.next));
  for (
    o.events = [],
      p ? ((p._tokenizer = void 0), (p.previous = void 0)) : g.pop(),
      f = g.length;
    f--;

  ) {
    let t = s.slice(g[f], g[f + 1]),
      n = a.pop();
    (c.push([n, n + t.length - 1]), e.splice(n, 2, t));
  }
  for (c.reverse(), f = -1; ++f < c.length; )
    ((l[m + c[f][0]] = m + c[f][1]), (m += c[f][1] - c[f][0] - 1));
  return l;
}
var ih = { resolve: oh, tokenize: sh },
  ah = { partial: !0, tokenize: ch };
function oh(e) {
  return (nh(e), e);
}
function sh(e, t) {
  let n;
  return r;
  function r(t) {
    return (
      e.enter(`content`),
      (n = e.enter(`chunkContent`, { contentType: `content` })),
      i(t)
    );
  }
  function i(t) {
    return t === null ? a(t) : H(t) ? e.check(ah, o, a)(t) : (e.consume(t), i);
  }
  function a(n) {
    return (e.exit(`chunkContent`), e.exit(`content`), t(n));
  }
  function o(t) {
    return (
      e.consume(t),
      e.exit(`chunkContent`),
      (n.next = e.enter(`chunkContent`, {
        contentType: `content`,
        previous: n,
      })),
      (n = n.next),
      i
    );
  }
}
function ch(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.exit(`chunkContent`),
      e.enter(`lineEnding`),
      e.consume(t),
      e.exit(`lineEnding`),
      _m(e, a, `linePrefix`)
    );
  }
  function a(i) {
    if (i === null || H(i)) return n(i);
    let a = r.events[r.events.length - 1];
    return !r.parser.constructs.disable.null.includes(`codeIndented`) &&
      a &&
      a[1].type === `linePrefix` &&
      a[2].sliceSerialize(a[1], !0).length >= 4
      ? t(i)
      : e.interrupt(r.parser.constructs.flow, n, t)(i);
  }
}
function lh(e, t, n, r, i, a, o, s, c) {
  let l = c || 1 / 0,
    u = 0;
  return d;
  function d(t) {
    return t === 60
      ? (e.enter(r), e.enter(i), e.enter(a), e.consume(t), e.exit(a), f)
      : t === null || t === 32 || t === 41 || cm(t)
        ? n(t)
        : (e.enter(r),
          e.enter(o),
          e.enter(s),
          e.enter(`chunkString`, { contentType: `string` }),
          h(t));
  }
  function f(n) {
    return n === 62
      ? (e.enter(a), e.consume(n), e.exit(a), e.exit(i), e.exit(r), t)
      : (e.enter(s), e.enter(`chunkString`, { contentType: `string` }), p(n));
  }
  function p(t) {
    return t === 62
      ? (e.exit(`chunkString`), e.exit(s), f(t))
      : t === null || t === 60 || H(t)
        ? n(t)
        : (e.consume(t), t === 92 ? m : p);
  }
  function m(t) {
    return t === 60 || t === 62 || t === 92 ? (e.consume(t), p) : p(t);
  }
  function h(i) {
    return !u && (i === null || i === 41 || fm(i))
      ? (e.exit(`chunkString`), e.exit(s), e.exit(o), e.exit(r), t(i))
      : u < l && i === 40
        ? (e.consume(i), u++, h)
        : i === 41
          ? (e.consume(i), u--, h)
          : i === null || i === 32 || i === 40 || cm(i)
            ? n(i)
            : (e.consume(i), i === 92 ? g : h);
  }
  function g(t) {
    return t === 40 || t === 41 || t === 92 ? (e.consume(t), h) : h(t);
  }
}
function uh(e, t, n, r, i, a) {
  let o = this,
    s = 0,
    c;
  return l;
  function l(t) {
    return (e.enter(r), e.enter(i), e.consume(t), e.exit(i), e.enter(a), u);
  }
  function u(l) {
    return s > 999 ||
      l === null ||
      l === 91 ||
      (l === 93 && !c) ||
      (l === 94 && !s && `_hiddenFootnoteSupport` in o.parser.constructs)
      ? n(l)
      : l === 93
        ? (e.exit(a), e.enter(i), e.consume(l), e.exit(i), e.exit(r), t)
        : H(l)
          ? (e.enter(`lineEnding`), e.consume(l), e.exit(`lineEnding`), u)
          : (e.enter(`chunkString`, { contentType: `string` }), d(l));
  }
  function d(t) {
    return t === null || t === 91 || t === 93 || H(t) || s++ > 999
      ? (e.exit(`chunkString`), u(t))
      : (e.consume(t), (c ||= !pm(t)), t === 92 ? f : d);
  }
  function f(t) {
    return t === 91 || t === 92 || t === 93 ? (e.consume(t), s++, d) : d(t);
  }
}
function dh(e, t, n, r, i, a) {
  let o;
  return s;
  function s(t) {
    return t === 34 || t === 39 || t === 40
      ? (e.enter(r),
        e.enter(i),
        e.consume(t),
        e.exit(i),
        (o = t === 40 ? 41 : t),
        c)
      : n(t);
  }
  function c(n) {
    return n === o
      ? (e.enter(i), e.consume(n), e.exit(i), e.exit(r), t)
      : (e.enter(a), l(n));
  }
  function l(t) {
    return t === o
      ? (e.exit(a), c(o))
      : t === null
        ? n(t)
        : H(t)
          ? (e.enter(`lineEnding`),
            e.consume(t),
            e.exit(`lineEnding`),
            _m(e, l, `linePrefix`))
          : (e.enter(`chunkString`, { contentType: `string` }), u(t));
  }
  function u(t) {
    return t === o || t === null || H(t)
      ? (e.exit(`chunkString`), l(t))
      : (e.consume(t), t === 92 ? d : u);
  }
  function d(t) {
    return t === o || t === 92 ? (e.consume(t), u) : u(t);
  }
}
function fh(e, t) {
  let n;
  return r;
  function r(i) {
    return H(i)
      ? (e.enter(`lineEnding`), e.consume(i), e.exit(`lineEnding`), (n = !0), r)
      : pm(i)
        ? _m(e, r, n ? `linePrefix` : `lineSuffix`)(i)
        : t(i);
  }
}
var ph = { name: `definition`, tokenize: hh },
  mh = { partial: !0, tokenize: gh };
function hh(e, t, n) {
  let r = this,
    i;
  return a;
  function a(t) {
    return (e.enter(`definition`), o(t));
  }
  function o(t) {
    return uh.call(
      r,
      e,
      s,
      n,
      `definitionLabel`,
      `definitionLabelMarker`,
      `definitionLabelString`,
    )(t);
  }
  function s(t) {
    return (
      (i = im(r.sliceSerialize(r.events[r.events.length - 1][1]).slice(1, -1))),
      t === 58
        ? (e.enter(`definitionMarker`),
          e.consume(t),
          e.exit(`definitionMarker`),
          c)
        : n(t)
    );
  }
  function c(t) {
    return fm(t) ? fh(e, l)(t) : l(t);
  }
  function l(t) {
    return lh(
      e,
      u,
      n,
      `definitionDestination`,
      `definitionDestinationLiteral`,
      `definitionDestinationLiteralMarker`,
      `definitionDestinationRaw`,
      `definitionDestinationString`,
    )(t);
  }
  function u(t) {
    return e.attempt(mh, d, d)(t);
  }
  function d(t) {
    return pm(t) ? _m(e, f, `whitespace`)(t) : f(t);
  }
  function f(a) {
    return a === null || H(a)
      ? (e.exit(`definition`), r.parser.defined.push(i), t(a))
      : n(a);
  }
}
function gh(e, t, n) {
  return r;
  function r(t) {
    return fm(t) ? fh(e, i)(t) : n(t);
  }
  function i(t) {
    return dh(
      e,
      a,
      n,
      `definitionTitle`,
      `definitionTitleMarker`,
      `definitionTitleString`,
    )(t);
  }
  function a(t) {
    return pm(t) ? _m(e, o, `whitespace`)(t) : o(t);
  }
  function o(e) {
    return e === null || H(e) ? t(e) : n(e);
  }
}
var _h = { name: `hardBreakEscape`, tokenize: vh };
function vh(e, t, n) {
  return r;
  function r(t) {
    return (e.enter(`hardBreakEscape`), e.consume(t), i);
  }
  function i(r) {
    return H(r) ? (e.exit(`hardBreakEscape`), t(r)) : n(r);
  }
}
var yh = { name: `headingAtx`, resolve: bh, tokenize: xh };
function bh(e, t) {
  let n = e.length - 2,
    r = 3,
    i,
    a;
  return (
    e[r][1].type === `whitespace` && (r += 2),
    n - 2 > r && e[n][1].type === `whitespace` && (n -= 2),
    e[n][1].type === `atxHeadingSequence` &&
      (r === n - 1 || (n - 4 > r && e[n - 2][1].type === `whitespace`)) &&
      (n -= r + 1 === n ? 2 : 4),
    n > r &&
      ((i = { type: `atxHeadingText`, start: e[r][1].start, end: e[n][1].end }),
      (a = {
        type: `chunkText`,
        start: e[r][1].start,
        end: e[n][1].end,
        contentType: `text`,
      }),
      Zp(e, r, n - r + 1, [
        [`enter`, i, t],
        [`enter`, a, t],
        [`exit`, a, t],
        [`exit`, i, t],
      ])),
    e
  );
}
function xh(e, t, n) {
  let r = 0;
  return i;
  function i(t) {
    return (e.enter(`atxHeading`), a(t));
  }
  function a(t) {
    return (e.enter(`atxHeadingSequence`), o(t));
  }
  function o(t) {
    return t === 35 && r++ < 6
      ? (e.consume(t), o)
      : t === null || fm(t)
        ? (e.exit(`atxHeadingSequence`), s(t))
        : n(t);
  }
  function s(n) {
    return n === 35
      ? (e.enter(`atxHeadingSequence`), c(n))
      : n === null || H(n)
        ? (e.exit(`atxHeading`), t(n))
        : pm(n)
          ? _m(e, s, `whitespace`)(n)
          : (e.enter(`atxHeadingText`), l(n));
  }
  function c(t) {
    return t === 35 ? (e.consume(t), c) : (e.exit(`atxHeadingSequence`), s(t));
  }
  function l(t) {
    return t === null || t === 35 || fm(t)
      ? (e.exit(`atxHeadingText`), s(t))
      : (e.consume(t), l);
  }
}
var Sh =
    `address.article.aside.base.basefont.blockquote.body.caption.center.col.colgroup.dd.details.dialog.dir.div.dl.dt.fieldset.figcaption.figure.footer.form.frame.frameset.h1.h2.h3.h4.h5.h6.head.header.hr.html.iframe.legend.li.link.main.menu.menuitem.nav.noframes.ol.optgroup.option.p.param.search.section.summary.table.tbody.td.tfoot.th.thead.title.tr.track.ul`.split(
      `.`,
    ),
  Ch = [`pre`, `script`, `style`, `textarea`],
  wh = { concrete: !0, name: `htmlFlow`, resolveTo: Dh, tokenize: Oh },
  Th = { partial: !0, tokenize: Ah },
  Eh = { partial: !0, tokenize: kh };
function Dh(e) {
  let t = e.length;
  for (; t-- && !(e[t][0] === `enter` && e[t][1].type === `htmlFlow`); );
  return (
    t > 1 &&
      e[t - 2][1].type === `linePrefix` &&
      ((e[t][1].start = e[t - 2][1].start),
      (e[t + 1][1].start = e[t - 2][1].start),
      e.splice(t - 2, 2)),
    e
  );
}
function Oh(e, t, n) {
  let r = this,
    i,
    a,
    o,
    s,
    c;
  return l;
  function l(e) {
    return u(e);
  }
  function u(t) {
    return (e.enter(`htmlFlow`), e.enter(`htmlFlowData`), e.consume(t), d);
  }
  function d(s) {
    return s === 33
      ? (e.consume(s), f)
      : s === 47
        ? (e.consume(s), (a = !0), h)
        : s === 63
          ? (e.consume(s), (i = 3), r.interrupt ? t : I)
          : am(s)
            ? (e.consume(s), (o = String.fromCharCode(s)), g)
            : n(s);
  }
  function f(a) {
    return a === 45
      ? (e.consume(a), (i = 2), p)
      : a === 91
        ? (e.consume(a), (i = 5), (s = 0), m)
        : am(a)
          ? (e.consume(a), (i = 4), r.interrupt ? t : I)
          : n(a);
  }
  function p(i) {
    return i === 45 ? (e.consume(i), r.interrupt ? t : I) : n(i);
  }
  function m(i) {
    return i === `CDATA[`.charCodeAt(s++)
      ? (e.consume(i), s === 6 ? (r.interrupt ? t : O) : m)
      : n(i);
  }
  function h(t) {
    return am(t) ? (e.consume(t), (o = String.fromCharCode(t)), g) : n(t);
  }
  function g(s) {
    if (s === null || s === 47 || s === 62 || fm(s)) {
      let c = s === 47,
        l = o.toLowerCase();
      return !c && !a && Ch.includes(l)
        ? ((i = 1), r.interrupt ? t(s) : O(s))
        : Sh.includes(o.toLowerCase())
          ? ((i = 6), c ? (e.consume(s), _) : r.interrupt ? t(s) : O(s))
          : ((i = 7),
            r.interrupt && !r.parser.lazy[r.now().line]
              ? n(s)
              : a
                ? v(s)
                : y(s));
    }
    return s === 45 || om(s)
      ? (e.consume(s), (o += String.fromCharCode(s)), g)
      : n(s);
  }
  function _(i) {
    return i === 62 ? (e.consume(i), r.interrupt ? t : O) : n(i);
  }
  function v(t) {
    return pm(t) ? (e.consume(t), v) : E(t);
  }
  function y(t) {
    return t === 47
      ? (e.consume(t), E)
      : t === 58 || t === 95 || am(t)
        ? (e.consume(t), b)
        : pm(t)
          ? (e.consume(t), y)
          : E(t);
  }
  function b(t) {
    return t === 45 || t === 46 || t === 58 || t === 95 || om(t)
      ? (e.consume(t), b)
      : x(t);
  }
  function x(t) {
    return t === 61 ? (e.consume(t), S) : pm(t) ? (e.consume(t), x) : y(t);
  }
  function S(t) {
    return t === null || t === 60 || t === 61 || t === 62 || t === 96
      ? n(t)
      : t === 34 || t === 39
        ? (e.consume(t), (c = t), C)
        : pm(t)
          ? (e.consume(t), S)
          : w(t);
  }
  function C(t) {
    return t === c
      ? (e.consume(t), (c = null), T)
      : t === null || H(t)
        ? n(t)
        : (e.consume(t), C);
  }
  function w(t) {
    return t === null ||
      t === 34 ||
      t === 39 ||
      t === 47 ||
      t === 60 ||
      t === 61 ||
      t === 62 ||
      t === 96 ||
      fm(t)
      ? x(t)
      : (e.consume(t), w);
  }
  function T(e) {
    return e === 47 || e === 62 || pm(e) ? y(e) : n(e);
  }
  function E(t) {
    return t === 62 ? (e.consume(t), D) : n(t);
  }
  function D(t) {
    return t === null || H(t) ? O(t) : pm(t) ? (e.consume(t), D) : n(t);
  }
  function O(t) {
    return t === 45 && i === 2
      ? (e.consume(t), M)
      : t === 60 && i === 1
        ? (e.consume(t), N)
        : t === 62 && i === 4
          ? (e.consume(t), ee)
          : t === 63 && i === 3
            ? (e.consume(t), I)
            : t === 93 && i === 5
              ? (e.consume(t), F)
              : H(t) && (i === 6 || i === 7)
                ? (e.exit(`htmlFlowData`), e.check(Th, L, k)(t))
                : t === null || H(t)
                  ? (e.exit(`htmlFlowData`), k(t))
                  : (e.consume(t), O);
  }
  function k(t) {
    return e.check(Eh, A, L)(t);
  }
  function A(t) {
    return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), j);
  }
  function j(t) {
    return t === null || H(t) ? k(t) : (e.enter(`htmlFlowData`), O(t));
  }
  function M(t) {
    return t === 45 ? (e.consume(t), I) : O(t);
  }
  function N(t) {
    return t === 47 ? (e.consume(t), (o = ``), P) : O(t);
  }
  function P(t) {
    if (t === 62) {
      let n = o.toLowerCase();
      return Ch.includes(n) ? (e.consume(t), ee) : O(t);
    }
    return am(t) && o.length < 8
      ? (e.consume(t), (o += String.fromCharCode(t)), P)
      : O(t);
  }
  function F(t) {
    return t === 93 ? (e.consume(t), I) : O(t);
  }
  function I(t) {
    return t === 62
      ? (e.consume(t), ee)
      : t === 45 && i === 2
        ? (e.consume(t), I)
        : O(t);
  }
  function ee(t) {
    return t === null || H(t)
      ? (e.exit(`htmlFlowData`), L(t))
      : (e.consume(t), ee);
  }
  function L(n) {
    return (e.exit(`htmlFlow`), t(n));
  }
}
function kh(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return H(t)
      ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), a)
      : n(t);
  }
  function a(e) {
    return r.parser.lazy[r.now().line] ? n(e) : t(e);
  }
}
function Ah(e, t, n) {
  return r;
  function r(r) {
    return (
      e.enter(`lineEnding`),
      e.consume(r),
      e.exit(`lineEnding`),
      e.attempt(Mm, t, n)
    );
  }
}
var jh = { name: `htmlText`, tokenize: Mh };
function Mh(e, t, n) {
  let r = this,
    i,
    a,
    o;
  return s;
  function s(t) {
    return (e.enter(`htmlText`), e.enter(`htmlTextData`), e.consume(t), c);
  }
  function c(t) {
    return t === 33
      ? (e.consume(t), l)
      : t === 47
        ? (e.consume(t), x)
        : t === 63
          ? (e.consume(t), y)
          : am(t)
            ? (e.consume(t), w)
            : n(t);
  }
  function l(t) {
    return t === 45
      ? (e.consume(t), u)
      : t === 91
        ? (e.consume(t), (a = 0), m)
        : am(t)
          ? (e.consume(t), v)
          : n(t);
  }
  function u(t) {
    return t === 45 ? (e.consume(t), p) : n(t);
  }
  function d(t) {
    return t === null
      ? n(t)
      : t === 45
        ? (e.consume(t), f)
        : H(t)
          ? ((o = d), N(t))
          : (e.consume(t), d);
  }
  function f(t) {
    return t === 45 ? (e.consume(t), p) : d(t);
  }
  function p(e) {
    return e === 62 ? M(e) : e === 45 ? f(e) : d(e);
  }
  function m(t) {
    return t === `CDATA[`.charCodeAt(a++)
      ? (e.consume(t), a === 6 ? h : m)
      : n(t);
  }
  function h(t) {
    return t === null
      ? n(t)
      : t === 93
        ? (e.consume(t), g)
        : H(t)
          ? ((o = h), N(t))
          : (e.consume(t), h);
  }
  function g(t) {
    return t === 93 ? (e.consume(t), _) : h(t);
  }
  function _(t) {
    return t === 62 ? M(t) : t === 93 ? (e.consume(t), _) : h(t);
  }
  function v(t) {
    return t === null || t === 62
      ? M(t)
      : H(t)
        ? ((o = v), N(t))
        : (e.consume(t), v);
  }
  function y(t) {
    return t === null
      ? n(t)
      : t === 63
        ? (e.consume(t), b)
        : H(t)
          ? ((o = y), N(t))
          : (e.consume(t), y);
  }
  function b(e) {
    return e === 62 ? M(e) : y(e);
  }
  function x(t) {
    return am(t) ? (e.consume(t), S) : n(t);
  }
  function S(t) {
    return t === 45 || om(t) ? (e.consume(t), S) : C(t);
  }
  function C(t) {
    return H(t) ? ((o = C), N(t)) : pm(t) ? (e.consume(t), C) : M(t);
  }
  function w(t) {
    return t === 45 || om(t)
      ? (e.consume(t), w)
      : t === 47 || t === 62 || fm(t)
        ? T(t)
        : n(t);
  }
  function T(t) {
    return t === 47
      ? (e.consume(t), M)
      : t === 58 || t === 95 || am(t)
        ? (e.consume(t), E)
        : H(t)
          ? ((o = T), N(t))
          : pm(t)
            ? (e.consume(t), T)
            : M(t);
  }
  function E(t) {
    return t === 45 || t === 46 || t === 58 || t === 95 || om(t)
      ? (e.consume(t), E)
      : D(t);
  }
  function D(t) {
    return t === 61
      ? (e.consume(t), O)
      : H(t)
        ? ((o = D), N(t))
        : pm(t)
          ? (e.consume(t), D)
          : T(t);
  }
  function O(t) {
    return t === null || t === 60 || t === 61 || t === 62 || t === 96
      ? n(t)
      : t === 34 || t === 39
        ? (e.consume(t), (i = t), k)
        : H(t)
          ? ((o = O), N(t))
          : pm(t)
            ? (e.consume(t), O)
            : (e.consume(t), A);
  }
  function k(t) {
    return t === i
      ? (e.consume(t), (i = void 0), j)
      : t === null
        ? n(t)
        : H(t)
          ? ((o = k), N(t))
          : (e.consume(t), k);
  }
  function A(t) {
    return t === null ||
      t === 34 ||
      t === 39 ||
      t === 60 ||
      t === 61 ||
      t === 96
      ? n(t)
      : t === 47 || t === 62 || fm(t)
        ? T(t)
        : (e.consume(t), A);
  }
  function j(e) {
    return e === 47 || e === 62 || fm(e) ? T(e) : n(e);
  }
  function M(r) {
    return r === 62
      ? (e.consume(r), e.exit(`htmlTextData`), e.exit(`htmlText`), t)
      : n(r);
  }
  function N(t) {
    return (
      e.exit(`htmlTextData`),
      e.enter(`lineEnding`),
      e.consume(t),
      e.exit(`lineEnding`),
      P
    );
  }
  function P(t) {
    return pm(t)
      ? _m(
          e,
          F,
          `linePrefix`,
          r.parser.constructs.disable.null.includes(`codeIndented`)
            ? void 0
            : 4,
        )(t)
      : F(t);
  }
  function F(t) {
    return (e.enter(`htmlTextData`), o(t));
  }
}
var Nh = { name: `labelEnd`, resolveAll: Lh, resolveTo: Rh, tokenize: zh },
  Ph = { tokenize: Bh },
  Fh = { tokenize: Vh },
  Ih = { tokenize: Hh };
function Lh(e) {
  let t = -1,
    n = [];
  for (; ++t < e.length; ) {
    let r = e[t][1];
    if (
      (n.push(e[t]),
      r.type === `labelImage` ||
        r.type === `labelLink` ||
        r.type === `labelEnd`)
    ) {
      let e = r.type === `labelImage` ? 4 : 2;
      ((r.type = `data`), (t += e));
    }
  }
  return (e.length !== n.length && Zp(e, 0, e.length, n), e);
}
function Rh(e, t) {
  let n = e.length,
    r = 0,
    i,
    a,
    o,
    s;
  for (; n--; )
    if (((i = e[n][1]), a)) {
      if (i.type === `link` || (i.type === `labelLink` && i._inactive)) break;
      e[n][0] === `enter` && i.type === `labelLink` && (i._inactive = !0);
    } else if (o) {
      if (
        e[n][0] === `enter` &&
        (i.type === `labelImage` || i.type === `labelLink`) &&
        !i._balanced &&
        ((a = n), i.type !== `labelLink`)
      ) {
        r = 2;
        break;
      }
    } else i.type === `labelEnd` && (o = n);
  let c = {
      type: e[a][1].type === `labelLink` ? `link` : `image`,
      start: { ...e[a][1].start },
      end: { ...e[e.length - 1][1].end },
    },
    l = { type: `label`, start: { ...e[a][1].start }, end: { ...e[o][1].end } },
    u = {
      type: `labelText`,
      start: { ...e[a + r + 2][1].end },
      end: { ...e[o - 2][1].start },
    };
  return (
    (s = [
      [`enter`, c, t],
      [`enter`, l, t],
    ]),
    (s = Qp(s, e.slice(a + 1, a + r + 3))),
    (s = Qp(s, [[`enter`, u, t]])),
    (s = Qp(
      s,
      Tm(t.parser.constructs.insideSpan.null, e.slice(a + r + 4, o - 3), t),
    )),
    (s = Qp(s, [[`exit`, u, t], e[o - 2], e[o - 1], [`exit`, l, t]])),
    (s = Qp(s, e.slice(o + 1))),
    (s = Qp(s, [[`exit`, c, t]])),
    Zp(e, a, e.length, s),
    e
  );
}
function zh(e, t, n) {
  let r = this,
    i = r.events.length,
    a,
    o;
  for (; i--; )
    if (
      (r.events[i][1].type === `labelImage` ||
        r.events[i][1].type === `labelLink`) &&
      !r.events[i][1]._balanced
    ) {
      a = r.events[i][1];
      break;
    }
  return s;
  function s(t) {
    return a
      ? a._inactive
        ? d(t)
        : ((o = r.parser.defined.includes(
            im(r.sliceSerialize({ start: a.end, end: r.now() })),
          )),
          e.enter(`labelEnd`),
          e.enter(`labelMarker`),
          e.consume(t),
          e.exit(`labelMarker`),
          e.exit(`labelEnd`),
          c)
      : n(t);
  }
  function c(t) {
    return t === 40
      ? e.attempt(Ph, u, o ? u : d)(t)
      : t === 91
        ? e.attempt(Fh, u, o ? l : d)(t)
        : o
          ? u(t)
          : d(t);
  }
  function l(t) {
    return e.attempt(Ih, u, d)(t);
  }
  function u(e) {
    return t(e);
  }
  function d(e) {
    return ((a._balanced = !0), n(e));
  }
}
function Bh(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`resource`),
      e.enter(`resourceMarker`),
      e.consume(t),
      e.exit(`resourceMarker`),
      i
    );
  }
  function i(t) {
    return fm(t) ? fh(e, a)(t) : a(t);
  }
  function a(t) {
    return t === 41
      ? u(t)
      : lh(
          e,
          o,
          s,
          `resourceDestination`,
          `resourceDestinationLiteral`,
          `resourceDestinationLiteralMarker`,
          `resourceDestinationRaw`,
          `resourceDestinationString`,
          32,
        )(t);
  }
  function o(t) {
    return fm(t) ? fh(e, c)(t) : u(t);
  }
  function s(e) {
    return n(e);
  }
  function c(t) {
    return t === 34 || t === 39 || t === 40
      ? dh(
          e,
          l,
          n,
          `resourceTitle`,
          `resourceTitleMarker`,
          `resourceTitleString`,
        )(t)
      : u(t);
  }
  function l(t) {
    return fm(t) ? fh(e, u)(t) : u(t);
  }
  function u(r) {
    return r === 41
      ? (e.enter(`resourceMarker`),
        e.consume(r),
        e.exit(`resourceMarker`),
        e.exit(`resource`),
        t)
      : n(r);
  }
}
function Vh(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return uh.call(
      r,
      e,
      a,
      o,
      `reference`,
      `referenceMarker`,
      `referenceString`,
    )(t);
  }
  function a(e) {
    return r.parser.defined.includes(
      im(r.sliceSerialize(r.events[r.events.length - 1][1]).slice(1, -1)),
    )
      ? t(e)
      : n(e);
  }
  function o(e) {
    return n(e);
  }
}
function Hh(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`reference`),
      e.enter(`referenceMarker`),
      e.consume(t),
      e.exit(`referenceMarker`),
      i
    );
  }
  function i(r) {
    return r === 93
      ? (e.enter(`referenceMarker`),
        e.consume(r),
        e.exit(`referenceMarker`),
        e.exit(`reference`),
        t)
      : n(r);
  }
}
var Uh = { name: `labelStartImage`, resolveAll: Nh.resolveAll, tokenize: Wh };
function Wh(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.enter(`labelImage`),
      e.enter(`labelImageMarker`),
      e.consume(t),
      e.exit(`labelImageMarker`),
      a
    );
  }
  function a(t) {
    return t === 91
      ? (e.enter(`labelMarker`),
        e.consume(t),
        e.exit(`labelMarker`),
        e.exit(`labelImage`),
        o)
      : n(t);
  }
  function o(e) {
    return e === 94 && `_hiddenFootnoteSupport` in r.parser.constructs
      ? n(e)
      : t(e);
  }
}
var Gh = { name: `labelStartLink`, resolveAll: Nh.resolveAll, tokenize: Kh };
function Kh(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.enter(`labelLink`),
      e.enter(`labelMarker`),
      e.consume(t),
      e.exit(`labelMarker`),
      e.exit(`labelLink`),
      a
    );
  }
  function a(e) {
    return e === 94 && `_hiddenFootnoteSupport` in r.parser.constructs
      ? n(e)
      : t(e);
  }
}
var qh = { name: `lineEnding`, tokenize: Jh };
function Jh(e, t) {
  return n;
  function n(n) {
    return (
      e.enter(`lineEnding`),
      e.consume(n),
      e.exit(`lineEnding`),
      _m(e, t, `linePrefix`)
    );
  }
}
var Yh = { name: `thematicBreak`, tokenize: Xh };
function Xh(e, t, n) {
  let r = 0,
    i;
  return a;
  function a(t) {
    return (e.enter(`thematicBreak`), o(t));
  }
  function o(e) {
    return ((i = e), s(e));
  }
  function s(a) {
    return a === i
      ? (e.enter(`thematicBreakSequence`), c(a))
      : r >= 3 && (a === null || H(a))
        ? (e.exit(`thematicBreak`), t(a))
        : n(a);
  }
  function c(t) {
    return t === i
      ? (e.consume(t), r++, c)
      : (e.exit(`thematicBreakSequence`),
        pm(t) ? _m(e, s, `whitespace`)(t) : s(t));
  }
}
var Zh = {
    continuation: { tokenize: tg },
    exit: rg,
    name: `list`,
    tokenize: eg,
  },
  Qh = { partial: !0, tokenize: ig },
  $h = { partial: !0, tokenize: ng };
function eg(e, t, n) {
  let r = this,
    i = r.events[r.events.length - 1],
    a =
      i && i[1].type === `linePrefix`
        ? i[2].sliceSerialize(i[1], !0).length
        : 0,
    o = 0;
  return s;
  function s(t) {
    let i =
      r.containerState.type ||
      (t === 42 || t === 43 || t === 45 ? `listUnordered` : `listOrdered`);
    if (
      i === `listUnordered`
        ? !r.containerState.marker || t === r.containerState.marker
        : lm(t)
    ) {
      if (
        (r.containerState.type ||
          ((r.containerState.type = i), e.enter(i, { _container: !0 })),
        i === `listUnordered`)
      )
        return (
          e.enter(`listItemPrefix`),
          t === 42 || t === 45 ? e.check(Yh, n, l)(t) : l(t)
        );
      if (!r.interrupt || t === 49)
        return (e.enter(`listItemPrefix`), e.enter(`listItemValue`), c(t));
    }
    return n(t);
  }
  function c(t) {
    return lm(t) && ++o < 10
      ? (e.consume(t), c)
      : (!r.interrupt || o < 2) &&
          (r.containerState.marker
            ? t === r.containerState.marker
            : t === 41 || t === 46)
        ? (e.exit(`listItemValue`), l(t))
        : n(t);
  }
  function l(t) {
    return (
      e.enter(`listItemMarker`),
      e.consume(t),
      e.exit(`listItemMarker`),
      (r.containerState.marker = r.containerState.marker || t),
      e.check(Mm, r.interrupt ? n : u, e.attempt(Qh, f, d))
    );
  }
  function u(e) {
    return ((r.containerState.initialBlankLine = !0), a++, f(e));
  }
  function d(t) {
    return pm(t)
      ? (e.enter(`listItemPrefixWhitespace`),
        e.consume(t),
        e.exit(`listItemPrefixWhitespace`),
        f)
      : n(t);
  }
  function f(n) {
    return (
      (r.containerState.size =
        a + r.sliceSerialize(e.exit(`listItemPrefix`), !0).length),
      t(n)
    );
  }
}
function tg(e, t, n) {
  let r = this;
  return ((r.containerState._closeFlow = void 0), e.check(Mm, i, a));
  function i(n) {
    return (
      (r.containerState.furtherBlankLines =
        r.containerState.furtherBlankLines ||
        r.containerState.initialBlankLine),
      _m(e, t, `listItemIndent`, r.containerState.size + 1)(n)
    );
  }
  function a(n) {
    return r.containerState.furtherBlankLines || !pm(n)
      ? ((r.containerState.furtherBlankLines = void 0),
        (r.containerState.initialBlankLine = void 0),
        o(n))
      : ((r.containerState.furtherBlankLines = void 0),
        (r.containerState.initialBlankLine = void 0),
        e.attempt($h, t, o)(n));
  }
  function o(i) {
    return (
      (r.containerState._closeFlow = !0),
      (r.interrupt = void 0),
      _m(
        e,
        e.attempt(Zh, t, n),
        `linePrefix`,
        r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
      )(i)
    );
  }
}
function ng(e, t, n) {
  let r = this;
  return _m(e, i, `listItemIndent`, r.containerState.size + 1);
  function i(e) {
    let i = r.events[r.events.length - 1];
    return i &&
      i[1].type === `listItemIndent` &&
      i[2].sliceSerialize(i[1], !0).length === r.containerState.size
      ? t(e)
      : n(e);
  }
}
function rg(e) {
  e.exit(this.containerState.type);
}
function ig(e, t, n) {
  let r = this;
  return _m(
    e,
    i,
    `listItemPrefixWhitespace`,
    r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 5,
  );
  function i(e) {
    let i = r.events[r.events.length - 1];
    return !pm(e) && i && i[1].type === `listItemPrefixWhitespace`
      ? t(e)
      : n(e);
  }
}
var ag = { name: `setextUnderline`, resolveTo: og, tokenize: sg };
function og(e, t) {
  let n = e.length,
    r,
    i,
    a;
  for (; n--; )
    if (e[n][0] === `enter`) {
      if (e[n][1].type === `content`) {
        r = n;
        break;
      }
      e[n][1].type === `paragraph` && (i = n);
    } else
      (e[n][1].type === `content` && e.splice(n, 1),
        !a && e[n][1].type === `definition` && (a = n));
  let o = {
    type: `setextHeading`,
    start: { ...e[r][1].start },
    end: { ...e[e.length - 1][1].end },
  };
  return (
    (e[i][1].type = `setextHeadingText`),
    a
      ? (e.splice(i, 0, [`enter`, o, t]),
        e.splice(a + 1, 0, [`exit`, e[r][1], t]),
        (e[r][1].end = { ...e[a][1].end }))
      : (e[r][1] = o),
    e.push([`exit`, o, t]),
    e
  );
}
function sg(e, t, n) {
  let r = this,
    i;
  return a;
  function a(t) {
    let a = r.events.length,
      s;
    for (; a--; )
      if (
        r.events[a][1].type !== `lineEnding` &&
        r.events[a][1].type !== `linePrefix` &&
        r.events[a][1].type !== `content`
      ) {
        s = r.events[a][1].type === `paragraph`;
        break;
      }
    return !r.parser.lazy[r.now().line] && (r.interrupt || s)
      ? (e.enter(`setextHeadingLine`), (i = t), o(t))
      : n(t);
  }
  function o(t) {
    return (e.enter(`setextHeadingLineSequence`), s(t));
  }
  function s(t) {
    return t === i
      ? (e.consume(t), s)
      : (e.exit(`setextHeadingLineSequence`),
        pm(t) ? _m(e, c, `lineSuffix`)(t) : c(t));
  }
  function c(r) {
    return r === null || H(r) ? (e.exit(`setextHeadingLine`), t(r)) : n(r);
  }
}
var cg = { tokenize: lg };
function lg(e) {
  let t = this,
    n = e.attempt(
      Mm,
      r,
      e.attempt(
        this.parser.constructs.flowInitial,
        i,
        _m(
          e,
          e.attempt(this.parser.constructs.flow, i, e.attempt(ih, i)),
          `linePrefix`,
        ),
      ),
    );
  return n;
  function r(r) {
    if (r === null) {
      e.consume(r);
      return;
    }
    return (
      e.enter(`lineEndingBlank`),
      e.consume(r),
      e.exit(`lineEndingBlank`),
      (t.currentConstruct = void 0),
      n
    );
  }
  function i(r) {
    if (r === null) {
      e.consume(r);
      return;
    }
    return (
      e.enter(`lineEnding`),
      e.consume(r),
      e.exit(`lineEnding`),
      (t.currentConstruct = void 0),
      n
    );
  }
}
var ug = { resolveAll: mg() },
  dg = pg(`string`),
  fg = pg(`text`);
function pg(e) {
  return { resolveAll: mg(e === `text` ? hg : void 0), tokenize: t };
  function t(t) {
    let n = this,
      r = this.parser.constructs[e],
      i = t.attempt(r, a, o);
    return a;
    function a(e) {
      return c(e) ? i(e) : o(e);
    }
    function o(e) {
      if (e === null) {
        t.consume(e);
        return;
      }
      return (t.enter(`data`), t.consume(e), s);
    }
    function s(e) {
      return c(e) ? (t.exit(`data`), i(e)) : (t.consume(e), s);
    }
    function c(e) {
      if (e === null) return !0;
      let t = r[e],
        i = -1;
      if (t)
        for (; ++i < t.length; ) {
          let e = t[i];
          if (!e.previous || e.previous.call(n, n.previous)) return !0;
        }
      return !1;
    }
  }
}
function mg(e) {
  return t;
  function t(t, n) {
    let r = -1,
      i;
    for (; ++r <= t.length; )
      i === void 0
        ? t[r] && t[r][1].type === `data` && ((i = r), r++)
        : (!t[r] || t[r][1].type !== `data`) &&
          (r !== i + 2 &&
            ((t[i][1].end = t[r - 1][1].end),
            t.splice(i + 2, r - i - 2),
            (r = i + 2)),
          (i = void 0));
    return e ? e(t, n) : t;
  }
}
function hg(e, t) {
  let n = 0;
  for (; ++n <= e.length; )
    if (
      (n === e.length || e[n][1].type === `lineEnding`) &&
      e[n - 1][1].type === `data`
    ) {
      let r = e[n - 1][1],
        i = t.sliceStream(r),
        a = i.length,
        o = -1,
        s = 0,
        c;
      for (; a--; ) {
        let e = i[a];
        if (typeof e == `string`) {
          for (o = e.length; e.charCodeAt(o - 1) === 32; ) (s++, o--);
          if (o) break;
          o = -1;
        } else if (e === -2) ((c = !0), s++);
        else if (e !== -1) {
          a++;
          break;
        }
      }
      if ((t._contentTypeTextTrailing && n === e.length && (s = 0), s)) {
        let i = {
          type:
            n === e.length || c || s < 2 ? `lineSuffix` : `hardBreakTrailing`,
          start: {
            _bufferIndex: a ? o : r.start._bufferIndex + o,
            _index: r.start._index + a,
            line: r.end.line,
            column: r.end.column - s,
            offset: r.end.offset - s,
          },
          end: { ...r.end },
        };
        ((r.end = { ...i.start }),
          r.start.offset === r.end.offset
            ? Object.assign(r, i)
            : (e.splice(n, 0, [`enter`, i, t], [`exit`, i, t]), (n += 2)));
      }
      n++;
    }
  return e;
}
var gg = e.i({
    attentionMarkers: () => wg,
    contentInitial: () => vg,
    disable: () => Tg,
    document: () => _g,
    flow: () => bg,
    flowInitial: () => yg,
    insideSpan: () => Cg,
    string: () => xg,
    text: () => Sg,
  }),
  _g = {
    42: Zh,
    43: Zh,
    45: Zh,
    48: Zh,
    49: Zh,
    50: Zh,
    51: Zh,
    52: Zh,
    53: Zh,
    54: Zh,
    55: Zh,
    56: Zh,
    57: Zh,
    62: Pm,
  },
  vg = { 91: ph },
  yg = { [-2]: Km, [-1]: Km, 32: Km },
  bg = {
    35: yh,
    42: Yh,
    45: [ag, Yh],
    60: wh,
    61: ag,
    95: Yh,
    96: Um,
    126: Um,
  },
  xg = { 38: Bm, 92: Rm },
  Sg = {
    [-5]: qh,
    [-4]: qh,
    [-3]: qh,
    33: Uh,
    38: Bm,
    42: Em,
    60: [Am, jh],
    91: Gh,
    92: [_h, Rm],
    93: Nh,
    95: Em,
    96: Xm,
  },
  Cg = { null: [Em, ug] },
  wg = { null: [42, 95] },
  Tg = { null: [] };
function Eg(e, t, n) {
  let r = {
      _bufferIndex: -1,
      _index: 0,
      line: (n && n.line) || 1,
      column: (n && n.column) || 1,
      offset: (n && n.offset) || 0,
    },
    i = {},
    a = [],
    o = [],
    s = [],
    c = {
      attempt: C(x),
      check: C(S),
      consume: v,
      enter: y,
      exit: b,
      interrupt: C(S, { interrupt: !0 }),
    },
    l = {
      code: null,
      containerState: {},
      defineSkip: h,
      events: [],
      now: m,
      parser: e,
      previous: null,
      sliceSerialize: f,
      sliceStream: p,
      write: d,
    },
    u = t.tokenize.call(l, c);
  return (t.resolveAll && a.push(t), l);
  function d(e) {
    return (
      (o = Qp(o, e)),
      g(),
      o[o.length - 1] === null
        ? (w(t, 0), (l.events = Tm(a, l.events, l)), l.events)
        : []
    );
  }
  function f(e, t) {
    return Og(p(e), t);
  }
  function p(e) {
    return Dg(o, e);
  }
  function m() {
    let { _bufferIndex: e, _index: t, line: n, column: i, offset: a } = r;
    return { _bufferIndex: e, _index: t, line: n, column: i, offset: a };
  }
  function h(e) {
    ((i[e.line] = e.column), E());
  }
  function g() {
    let e;
    for (; r._index < o.length; ) {
      let t = o[r._index];
      if (typeof t == `string`)
        for (
          e = r._index, r._bufferIndex < 0 && (r._bufferIndex = 0);
          r._index === e && r._bufferIndex < t.length;

        )
          _(t.charCodeAt(r._bufferIndex));
      else _(t);
    }
  }
  function _(e) {
    u = u(e);
  }
  function v(e) {
    (H(e)
      ? (r.line++, (r.column = 1), (r.offset += e === -3 ? 2 : 1), E())
      : e !== -1 && (r.column++, r.offset++),
      r._bufferIndex < 0
        ? r._index++
        : (r._bufferIndex++,
          r._bufferIndex === o[r._index].length &&
            ((r._bufferIndex = -1), r._index++)),
      (l.previous = e));
  }
  function y(e, t) {
    let n = t || {};
    return (
      (n.type = e),
      (n.start = m()),
      l.events.push([`enter`, n, l]),
      s.push(n),
      n
    );
  }
  function b(e) {
    let t = s.pop();
    return ((t.end = m()), l.events.push([`exit`, t, l]), t);
  }
  function x(e, t) {
    w(e, t.from);
  }
  function S(e, t) {
    t.restore();
  }
  function C(e, t) {
    return n;
    function n(n, r, i) {
      let a, o, s, u;
      return Array.isArray(n) ? f(n) : `tokenize` in n ? f([n]) : d(n);
      function d(e) {
        return t;
        function t(t) {
          let n = t !== null && e[t],
            r = t !== null && e.null;
          return f([
            ...(Array.isArray(n) ? n : n ? [n] : []),
            ...(Array.isArray(r) ? r : r ? [r] : []),
          ])(t);
        }
      }
      function f(e) {
        return ((a = e), (o = 0), e.length === 0 ? i : p(e[o]));
      }
      function p(e) {
        return n;
        function n(n) {
          return (
            (u = T()),
            (s = e),
            e.partial || (l.currentConstruct = e),
            e.name && l.parser.constructs.disable.null.includes(e.name)
              ? h(n)
              : e.tokenize.call(
                  t ? Object.assign(Object.create(l), t) : l,
                  c,
                  m,
                  h,
                )(n)
          );
        }
      }
      function m(t) {
        return (e(s, u), r);
      }
      function h(e) {
        return (u.restore(), ++o < a.length ? p(a[o]) : i);
      }
    }
  }
  function w(e, t) {
    (e.resolveAll && !a.includes(e) && a.push(e),
      e.resolve &&
        Zp(l.events, t, l.events.length - t, e.resolve(l.events.slice(t), l)),
      e.resolveTo && (l.events = e.resolveTo(l.events, l)));
  }
  function T() {
    let e = m(),
      t = l.previous,
      n = l.currentConstruct,
      i = l.events.length,
      a = Array.from(s);
    return { from: i, restore: o };
    function o() {
      ((r = e),
        (l.previous = t),
        (l.currentConstruct = n),
        (l.events.length = i),
        (s = a),
        E());
    }
  }
  function E() {
    r.line in i &&
      r.column < 2 &&
      ((r.column = i[r.line]), (r.offset += i[r.line] - 1));
  }
}
function Dg(e, t) {
  let n = t.start._index,
    r = t.start._bufferIndex,
    i = t.end._index,
    a = t.end._bufferIndex,
    o;
  if (n === i) o = [e[n].slice(r, a)];
  else {
    if (((o = e.slice(n, i)), r > -1)) {
      let e = o[0];
      typeof e == `string` ? (o[0] = e.slice(r)) : o.shift();
    }
    a > 0 && o.push(e[i].slice(0, a));
  }
  return o;
}
function Og(e, t) {
  let n = -1,
    r = [],
    i;
  for (; ++n < e.length; ) {
    let a = e[n],
      o;
    if (typeof a == `string`) o = a;
    else
      switch (a) {
        case -5:
          o = `\r`;
          break;
        case -4:
          o = `
`;
          break;
        case -3:
          o = `\r
`;
          break;
        case -2:
          o = t ? ` ` : `	`;
          break;
        case -1:
          if (!t && i) continue;
          o = ` `;
          break;
        default:
          o = String.fromCharCode(a);
      }
    ((i = a === -2), r.push(o));
  }
  return r.join(``);
}
function kg(e) {
  let t = {
    constructs: em([gg, ...((e || {}).extensions || [])]),
    content: n(vm),
    defined: [],
    document: n(bm),
    flow: n(cg),
    lazy: {},
    string: n(dg),
    text: n(fg),
  };
  return t;
  function n(e) {
    return n;
    function n(n) {
      return Eg(t, e, n);
    }
  }
}
function Ag(e) {
  for (; !nh(e); );
  return e;
}
var jg = /[\0\t\n\r]/g;
function Mg() {
  let e = 1,
    t = ``,
    n = !0,
    r;
  return i;
  function i(i, a, o) {
    let s = [],
      c,
      l,
      u,
      d,
      f;
    for (
      i =
        t +
        (typeof i == `string`
          ? i.toString()
          : new TextDecoder(a || void 0).decode(i)),
        u = 0,
        t = ``,
        n &&= (i.charCodeAt(0) === 65279 && u++, void 0);
      u < i.length;

    ) {
      if (
        ((jg.lastIndex = u),
        (c = jg.exec(i)),
        (d = c && c.index !== void 0 ? c.index : i.length),
        (f = i.charCodeAt(d)),
        !c)
      ) {
        t = i.slice(u);
        break;
      }
      if (f === 10 && u === d && r) (s.push(-3), (r = void 0));
      else
        switch (
          ((r &&= (s.push(-5), void 0)),
          u < d && (s.push(i.slice(u, d)), (e += d - u)),
          f)
        ) {
          case 0:
            (s.push(65533), e++);
            break;
          case 9:
            for (l = Math.ceil(e / 4) * 4, s.push(-2); e++ < l; ) s.push(-1);
            break;
          case 10:
            (s.push(-4), (e = 1));
            break;
          default:
            ((r = !0), (e = 1));
        }
      u = d + 1;
    }
    return (o && (r && s.push(-5), t && s.push(t), s.push(null)), s);
  }
}
var Ng = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function Pg(e) {
  return e.replace(Ng, Fg);
}
function Fg(e, t, n) {
  if (t) return t;
  if (n.charCodeAt(0) === 35) {
    let e = n.charCodeAt(1),
      t = e === 120 || e === 88;
    return rm(n.slice(t ? 2 : 1), t ? 16 : 10);
  }
  return Xp(n) || e;
}
function Ig(e) {
  return !e || typeof e != `object`
    ? ``
    : `position` in e || `type` in e
      ? Rg(e.position)
      : `start` in e || `end` in e
        ? Rg(e)
        : `line` in e || `column` in e
          ? Lg(e)
          : ``;
}
function Lg(e) {
  return zg(e && e.line) + `:` + zg(e && e.column);
}
function Rg(e) {
  return Lg(e && e.start) + `-` + Lg(e && e.end);
}
function zg(e) {
  return e && typeof e == `number` ? e : 1;
}
var Bg = {}.hasOwnProperty;
function Vg(e, t, n) {
  return (
    t && typeof t == `object` && ((n = t), (t = void 0)),
    Hg(n)(
      Ag(
        kg(n)
          .document()
          .write(Mg()(e, t, !0)),
      ),
    )
  );
}
function Hg(e) {
  let t = {
    transforms: [],
    canContainEols: [`emphasis`, `fragment`, `heading`, `paragraph`, `strong`],
    enter: {
      autolink: a(he),
      autolinkProtocol: T,
      autolinkEmail: T,
      atxHeading: a(de),
      blockQuote: a(oe),
      characterEscape: T,
      characterReference: T,
      codeFenced: a(se),
      codeFencedFenceInfo: o,
      codeFencedFenceMeta: o,
      codeIndented: a(se, o),
      codeText: a(ce, o),
      codeTextData: T,
      data: T,
      codeFlowValue: T,
      definition: a(le),
      definitionDestinationString: o,
      definitionLabelString: o,
      definitionTitleString: o,
      emphasis: a(ue),
      hardBreakEscape: a(fe),
      hardBreakTrailing: a(fe),
      htmlFlow: a(pe, o),
      htmlFlowData: T,
      htmlText: a(pe, o),
      htmlTextData: T,
      image: a(me),
      label: o,
      link: a(he),
      listItem: a(_e),
      listItemValue: f,
      listOrdered: a(ge, d),
      listUnordered: a(ge),
      paragraph: a(ve),
      reference: R,
      referenceString: o,
      resourceDestinationString: o,
      resourceTitleString: o,
      setextHeading: a(de),
      strong: a(ye),
      thematicBreak: a(xe),
    },
    exit: {
      atxHeading: c(),
      atxHeadingSequence: x,
      autolink: c(),
      autolinkEmail: ae,
      autolinkProtocol: ie,
      blockQuote: c(),
      characterEscapeValue: E,
      characterReferenceMarkerHexadecimal: te,
      characterReferenceMarkerNumeric: te,
      characterReferenceValue: ne,
      characterReference: re,
      codeFenced: c(g),
      codeFencedFence: h,
      codeFencedFenceInfo: p,
      codeFencedFenceMeta: m,
      codeFlowValue: E,
      codeIndented: c(_),
      codeText: c(j),
      codeTextData: E,
      data: E,
      definition: c(),
      definitionDestinationString: b,
      definitionLabelString: v,
      definitionTitleString: y,
      emphasis: c(),
      hardBreakEscape: c(O),
      hardBreakTrailing: c(O),
      htmlFlow: c(k),
      htmlFlowData: E,
      htmlText: c(A),
      htmlTextData: E,
      image: c(N),
      label: F,
      labelText: P,
      lineEnding: D,
      link: c(M),
      listItem: c(),
      listOrdered: c(),
      listUnordered: c(),
      paragraph: c(),
      referenceString: z,
      resourceDestinationString: I,
      resourceTitleString: ee,
      resource: L,
      setextHeading: c(w),
      setextHeadingLineSequence: C,
      setextHeadingText: S,
      strong: c(),
      thematicBreak: c(),
    },
  };
  Wg(t, (e || {}).mdastExtensions || []);
  let n = {};
  return r;
  function r(e) {
    let r = { type: `root`, children: [] },
      a = {
        stack: [r],
        tokenStack: [],
        config: t,
        enter: s,
        exit: l,
        buffer: o,
        resume: u,
        data: n,
      },
      c = [],
      d = -1;
    for (; ++d < e.length; )
      (e[d][1].type === `listOrdered` || e[d][1].type === `listUnordered`) &&
        (e[d][0] === `enter` ? c.push(d) : (d = i(e, c.pop(), d)));
    for (d = -1; ++d < e.length; ) {
      let n = t[e[d][0]];
      Bg.call(n, e[d][1].type) &&
        n[e[d][1].type].call(
          Object.assign({ sliceSerialize: e[d][2].sliceSerialize }, a),
          e[d][1],
        );
    }
    if (a.tokenStack.length > 0) {
      let e = a.tokenStack[a.tokenStack.length - 1];
      (e[1] || Kg).call(a, void 0, e[0]);
    }
    for (
      r.position = {
        start: Ug(
          e.length > 0 ? e[0][1].start : { line: 1, column: 1, offset: 0 },
        ),
        end: Ug(
          e.length > 0
            ? e[e.length - 2][1].end
            : { line: 1, column: 1, offset: 0 },
        ),
      },
        d = -1;
      ++d < t.transforms.length;

    )
      r = t.transforms[d](r) || r;
    return r;
  }
  function i(e, t, n) {
    let r = t - 1,
      i = -1,
      a = !1,
      o,
      s,
      c,
      l;
    for (; ++r <= n; ) {
      let t = e[r];
      switch (t[1].type) {
        case `listUnordered`:
        case `listOrdered`:
        case `blockQuote`:
          (t[0] === `enter` ? i++ : i--, (l = void 0));
          break;
        case `lineEndingBlank`:
          t[0] === `enter` && (o && !l && !i && !c && (c = r), (l = void 0));
          break;
        case `linePrefix`:
        case `listItemValue`:
        case `listItemMarker`:
        case `listItemPrefix`:
        case `listItemPrefixWhitespace`:
          break;
        default:
          l = void 0;
      }
      if (
        (!i && t[0] === `enter` && t[1].type === `listItemPrefix`) ||
        (i === -1 &&
          t[0] === `exit` &&
          (t[1].type === `listUnordered` || t[1].type === `listOrdered`))
      ) {
        if (o) {
          let i = r;
          for (s = void 0; i--; ) {
            let t = e[i];
            if (t[1].type === `lineEnding` || t[1].type === `lineEndingBlank`) {
              if (t[0] === `exit`) continue;
              (s && ((e[s][1].type = `lineEndingBlank`), (a = !0)),
                (t[1].type = `lineEnding`),
                (s = i));
            } else if (
              !(
                t[1].type === `linePrefix` ||
                t[1].type === `blockQuotePrefix` ||
                t[1].type === `blockQuotePrefixWhitespace` ||
                t[1].type === `blockQuoteMarker` ||
                t[1].type === `listItemIndent`
              )
            )
              break;
          }
          (c && (!s || c < s) && (o._spread = !0),
            (o.end = Object.assign({}, s ? e[s][1].start : t[1].end)),
            e.splice(s || r, 0, [`exit`, o, t[2]]),
            r++,
            n++);
        }
        if (t[1].type === `listItemPrefix`) {
          let i = {
            type: `listItem`,
            _spread: !1,
            start: Object.assign({}, t[1].start),
            end: void 0,
          };
          ((o = i),
            e.splice(r, 0, [`enter`, i, t[2]]),
            r++,
            n++,
            (c = void 0),
            (l = !0));
        }
      }
    }
    return ((e[t][1]._spread = a), n);
  }
  function a(e, t) {
    return n;
    function n(n) {
      (s.call(this, e(n), n), t && t.call(this, n));
    }
  }
  function o() {
    this.stack.push({ type: `fragment`, children: [] });
  }
  function s(e, t, n) {
    (this.stack[this.stack.length - 1].children.push(e),
      this.stack.push(e),
      this.tokenStack.push([t, n || void 0]),
      (e.position = { start: Ug(t.start), end: void 0 }));
  }
  function c(e) {
    return t;
    function t(t) {
      (e && e.call(this, t), l.call(this, t));
    }
  }
  function l(e, t) {
    let n = this.stack.pop(),
      r = this.tokenStack.pop();
    if (r)
      r[0].type !== e.type &&
        (t ? t.call(this, e, r[0]) : (r[1] || Kg).call(this, e, r[0]));
    else
      throw Error(
        "Cannot close `" +
          e.type +
          "` (" +
          Ig({ start: e.start, end: e.end }) +
          `): it’s not open`,
      );
    n.position.end = Ug(e.end);
  }
  function u() {
    return Wp(this.stack.pop());
  }
  function d() {
    this.data.expectingFirstListItemValue = !0;
  }
  function f(e) {
    if (this.data.expectingFirstListItemValue) {
      let t = this.stack[this.stack.length - 2];
      ((t.start = Number.parseInt(this.sliceSerialize(e), 10)),
        (this.data.expectingFirstListItemValue = void 0));
    }
  }
  function p() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.lang = e;
  }
  function m() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.meta = e;
  }
  function h() {
    this.data.flowCodeInside ||
      (this.buffer(), (this.data.flowCodeInside = !0));
  }
  function g() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    ((t.value = e.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, ``)),
      (this.data.flowCodeInside = void 0));
  }
  function _() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e.replace(/(\r?\n|\r)$/g, ``);
  }
  function v(e) {
    let t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((n.label = t), (n.identifier = im(this.sliceSerialize(e)).toLowerCase()));
  }
  function y() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.title = e;
  }
  function b() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.url = e;
  }
  function x(e) {
    let t = this.stack[this.stack.length - 1];
    t.depth ||= this.sliceSerialize(e).length;
  }
  function S() {
    this.data.setextHeadingSlurpLineEnding = !0;
  }
  function C(e) {
    let t = this.stack[this.stack.length - 1];
    t.depth = this.sliceSerialize(e).codePointAt(0) === 61 ? 1 : 2;
  }
  function w() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function T(e) {
    let t = this.stack[this.stack.length - 1].children,
      n = t[t.length - 1];
    ((!n || n.type !== `text`) &&
      ((n = be()),
      (n.position = { start: Ug(e.start), end: void 0 }),
      t.push(n)),
      this.stack.push(n));
  }
  function E(e) {
    let t = this.stack.pop();
    ((t.value += this.sliceSerialize(e)), (t.position.end = Ug(e.end)));
  }
  function D(e) {
    let n = this.stack[this.stack.length - 1];
    if (this.data.atHardBreak) {
      let t = n.children[n.children.length - 1];
      ((t.position.end = Ug(e.end)), (this.data.atHardBreak = void 0));
      return;
    }
    !this.data.setextHeadingSlurpLineEnding &&
      t.canContainEols.includes(n.type) &&
      (T.call(this, e), E.call(this, e));
  }
  function O() {
    this.data.atHardBreak = !0;
  }
  function k() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function A() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function j() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function M() {
    let e = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      let t = this.data.referenceType || `shortcut`;
      ((e.type += `Reference`),
        (e.referenceType = t),
        delete e.url,
        delete e.title);
    } else (delete e.identifier, delete e.label);
    this.data.referenceType = void 0;
  }
  function N() {
    let e = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      let t = this.data.referenceType || `shortcut`;
      ((e.type += `Reference`),
        (e.referenceType = t),
        delete e.url,
        delete e.title);
    } else (delete e.identifier, delete e.label);
    this.data.referenceType = void 0;
  }
  function P(e) {
    let t = this.sliceSerialize(e),
      n = this.stack[this.stack.length - 2];
    ((n.label = Pg(t)), (n.identifier = im(t).toLowerCase()));
  }
  function F() {
    let e = this.stack[this.stack.length - 1],
      t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((this.data.inReference = !0),
      n.type === `link` ? (n.children = e.children) : (n.alt = t));
  }
  function I() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.url = e;
  }
  function ee() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.title = e;
  }
  function L() {
    this.data.inReference = void 0;
  }
  function R() {
    this.data.referenceType = `collapsed`;
  }
  function z(e) {
    let t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((n.label = t),
      (n.identifier = im(this.sliceSerialize(e)).toLowerCase()),
      (this.data.referenceType = `full`));
  }
  function te(e) {
    this.data.characterReferenceType = e.type;
  }
  function ne(e) {
    let t = this.sliceSerialize(e),
      n = this.data.characterReferenceType,
      r;
    n
      ? ((r = rm(t, n === `characterReferenceMarkerNumeric` ? 10 : 16)),
        (this.data.characterReferenceType = void 0))
      : (r = Xp(t));
    let i = this.stack[this.stack.length - 1];
    i.value += r;
  }
  function re(e) {
    let t = this.stack.pop();
    t.position.end = Ug(e.end);
  }
  function ie(e) {
    E.call(this, e);
    let t = this.stack[this.stack.length - 1];
    t.url = this.sliceSerialize(e);
  }
  function ae(e) {
    E.call(this, e);
    let t = this.stack[this.stack.length - 1];
    t.url = `mailto:` + this.sliceSerialize(e);
  }
  function oe() {
    return { type: `blockquote`, children: [] };
  }
  function se() {
    return { type: `code`, lang: null, meta: null, value: `` };
  }
  function ce() {
    return { type: `inlineCode`, value: `` };
  }
  function le() {
    return {
      type: `definition`,
      identifier: ``,
      label: null,
      title: null,
      url: ``,
    };
  }
  function ue() {
    return { type: `emphasis`, children: [] };
  }
  function de() {
    return { type: `heading`, depth: 0, children: [] };
  }
  function fe() {
    return { type: `break` };
  }
  function pe() {
    return { type: `html`, value: `` };
  }
  function me() {
    return { type: `image`, title: null, url: ``, alt: null };
  }
  function he() {
    return { type: `link`, title: null, url: ``, children: [] };
  }
  function ge(e) {
    return {
      type: `list`,
      ordered: e.type === `listOrdered`,
      start: null,
      spread: e._spread,
      children: [],
    };
  }
  function _e(e) {
    return { type: `listItem`, spread: e._spread, checked: null, children: [] };
  }
  function ve() {
    return { type: `paragraph`, children: [] };
  }
  function ye() {
    return { type: `strong`, children: [] };
  }
  function be() {
    return { type: `text`, value: `` };
  }
  function xe() {
    return { type: `thematicBreak` };
  }
}
function Ug(e) {
  return { line: e.line, column: e.column, offset: e.offset };
}
function Wg(e, t) {
  let n = -1;
  for (; ++n < t.length; ) {
    let r = t[n];
    Array.isArray(r) ? Wg(e, r) : Gg(e, r);
  }
}
function Gg(e, t) {
  let n;
  for (n in t)
    if (Bg.call(t, n))
      switch (n) {
        case `canContainEols`: {
          let r = t[n];
          r && e[n].push(...r);
          break;
        }
        case `transforms`: {
          let r = t[n];
          r && e[n].push(...r);
          break;
        }
        case `enter`:
        case `exit`: {
          let r = t[n];
          r && Object.assign(e[n], r);
          break;
        }
      }
}
function Kg(e, t) {
  throw Error(
    e
      ? "Cannot close `" +
          e.type +
          "` (" +
          Ig({ start: e.start, end: e.end }) +
          "): a different token (`" +
          t.type +
          "`, " +
          Ig({ start: t.start, end: t.end }) +
          `) is open`
      : "Cannot close document, a token (`" +
          t.type +
          "`, " +
          Ig({ start: t.start, end: t.end }) +
          `) is still open`,
  );
}
var qg = new Set([
  `root`,
  `blockquote`,
  `list`,
  `listItem`,
  `table`,
  `tableRow`,
  `tableCell`,
]);
function Jg(e) {
  let t = e.trim();
  return t.length === 0 ? `` : Yg(Vg(t)).replace(/\s+/g, ` `).trim();
}
function Yg(e) {
  return e.children == null
    ? Wp(e, { includeHtml: !1 })
    : e.children
        .map(Yg)
        .filter((e) => e.length > 0)
        .join(qg.has(e.type) ? ` ` : ``);
}
var Xg = `codex-notification`,
  Zg = `${Xg}.wav`,
  Qg = `2DC432GLL2.com.openai.codex.notifications`,
  $g = r.a(`desktop-notifications`);
function e_(e) {
  return u.default.join(
    e,
    `Library`,
    `Group Containers`,
    Qg,
    `Library`,
    `Sounds`,
  );
}
var t_ = class {
    isSupported;
    createNotification;
    logger = $g();
    notificationSoundStagingStarted = !1;
    notifications = new Map();
    constructor(e) {
      ((this.options = e),
        (this.isSupported =
          e.isSupported ?? (() => c.Notification.isSupported())),
        e.createNotification
          ? (this.createNotification = e.createNotification)
          : (this.createNotification = (e) => {
              let t = new c.Notification(e);
              return {
                show: () => t.show(),
                on: (e, n) => {
                  switch (e) {
                    case `action`:
                      return t.on(`action`, (e, t) => {
                        n(e, t);
                      });
                    case `click`:
                      return t.on(`click`, () => {
                        n(void 0);
                      });
                    case `close`:
                      return t.on(`close`, () => {
                        n(void 0);
                      });
                  }
                },
                close: () => t.close(),
              };
            }));
    }
    showNotification(e, t, n, r) {
      if ((this.stageNotificationSoundIfNeeded(), !this.isSupported())) {
        (this.logger.warning(`Notification API not supported on this host`),
          r?.());
        return;
      }
      if (t.isDestroyed()) {
        (this.logger.warning(
          `target webContents destroyed; dropping notification`,
          { safe: { notificationId: e.id }, sensitive: {} },
        ),
          r?.());
        return;
      }
      let i = (e.actions ?? []).slice(0, 4);
      this.logger.info(`show notification`, {
        safe: { notificationId: e.id, kind: e.kind, actionCount: i.length },
        sensitive: {},
      });
      let a =
          e.kind === `permission` || e.kind === `question` ? `never` : void 0,
        o = e.kind === `turn-complete` && typeof e.replyPlaceholder == `string`,
        s = this.notifications.get(e.id);
      s != null && (this.removeNotification(e.id), s.notification.close?.());
      let c = Jg(e.title),
        l = this.createNotification({
          title: c,
          body: Jg(e.body),
          silent: !1,
          sound: this.options.platform === `darwin` ? Xg : void 0,
          timeoutType: a,
          hasReply: o,
          replyPlaceholder: o ? Jg(e.replyPlaceholder ?? ``) : void 0,
          actions: i.map((e) => ({ type: `button`, text: Jg(e.title) })),
        });
      (l.on(`click`, () => {
        (n({ notificationId: e.id, actionId: null, actionType: `open` }),
          this.logger.info(`notification click open`, {
            safe: { notificationId: e.id },
            sensitive: {},
          }));
      }),
        l.on(`action`, (t, r) => {
          let a = i[r ?? -1];
          a &&
            (this.logger.info(`notification action`, {
              safe: {
                notificationId: e.id,
                actionId: a.id,
                actionType: a.actionType,
              },
              sensitive: {},
            }),
            n({
              notificationId: e.id,
              actionId: a.id,
              actionType: a.actionType,
            }));
        }),
        l.on(`reply`, (t, r) => {
          n({
            notificationId: e.id,
            actionId: null,
            actionType: `reply`,
            reply:
              typeof r == `string`
                ? r
                : r == null
                  ? ``
                  : (JSON.stringify(r) ?? ``),
          });
        }),
        l.on(`close`, () => {
          this.removeNotification(e.id);
        }),
        this.notifications.set(e.id, {
          notification: l,
          conversationId: e.conversationId ?? null,
          navigationPath: e.navigationPath ?? null,
          completedThread:
            e.kind === `turn-complete` && e.navigationPath != null
              ? {
                  title: c,
                  path: e.navigationPath,
                  projectLabel: ``,
                  isProjectless: !1,
                }
              : null,
          onNotificationRemoved: r,
        }),
        this.emitCompletedThreadsChanged(),
        l.show());
    }
    stageNotificationSoundIfNeeded() {
      if (
        this.notificationSoundStagingStarted ||
        ((this.notificationSoundStagingStarted = !0),
        this.options.platform !== `darwin`) ||
        typeof process.resourcesPath != `string`
      )
        return;
      let e = u.default.join(process.resourcesPath, Zg),
        t = u.default.join(__dirname, `..`, `assets`, `sounds`, Zg),
        n = (0, p.existsSync)(e) ? e : t;
      if (!(0, p.existsSync)(n)) return;
      let r = e_(l.default.homedir());
      (0, m.mkdir)(r, { recursive: !0 })
        .then(() =>
          (0, m.copyFile)(n, u.default.join(r, Zg), p.constants.COPYFILE_EXCL),
        )
        .catch((e) => {
          e.code !== `EEXIST` &&
            this.logger.warning(`failed to stage notification sound`, {
              safe: {},
              sensitive: { error: e },
            });
        });
    }
    dismissByConversationId(e) {
      for (let [t, n] of this.notifications.entries())
        n.conversationId === e &&
          (n.notification.close?.(), this.removeNotification(t));
    }
    dismissByNotificationId(e) {
      (this.notifications.get(e)?.notification.close?.(),
        this.removeNotification(e));
    }
    dismissByNavigationPath(e) {
      for (let [t, n] of this.notifications.entries())
        n.navigationPath === e &&
          (n.notification.close?.(), this.removeNotification(t));
    }
    removeNotification(e) {
      let t = this.notifications.get(e);
      t != null &&
        (this.notifications.delete(e),
        t.onNotificationRemoved?.(),
        this.emitCompletedThreadsChanged());
    }
    emitCompletedThreadsChanged() {
      if (this.options.onCompletedThreadsChanged == null) return;
      let e = new Set(),
        t = [];
      for (let n of [...this.notifications.values()].reverse())
        n.completedThread == null ||
          e.has(n.completedThread.path) ||
          (e.add(n.completedThread.path), t.push(n.completedThread));
      this.options.onCompletedThreadsChanged(t);
    }
  },
  n_ = class {
    constructor(e) {
      this.app = e;
    }
    getVersion() {
      return this.app.getVersion();
    }
    onShutdown(e) {
      return (
        this.app.on(`before-quit`, e),
        {
          dispose: () => {
            this.app.off(`before-quit`, e);
          },
        }
      );
    }
  },
  r_ = class {
    constructor(e) {
      this.broadcaster = e;
    }
    persistAndNotify(e) {
      let t = n.C(e);
      return t.length <= 0
        ? 0
        : (this.broadcaster.inboxItemsChanged(n.lc.CHANGED), t.length);
    }
    markRead(e, t) {
      n.b(e, t) && this.broadcaster.inboxItemsChanged(n.lc.CHANGED);
    }
    markUnread(e) {
      n.b(e, null) && this.broadcaster.inboxItemsChanged(n.lc.CHANGED);
    }
    markAllAutomationRunsRead(e) {
      n.y(e) > 0 && this.broadcaster.inboxItemsChanged(n.lc.CHANGED);
    }
  },
  i_ = 2,
  a_ = null;
function o_() {
  return process.versions.electron
    ? ((a_ ??= n.Hr(n.Br({ databaseFileName: s_() }), c_)), a_)
    : null;
}
function s_() {
  let e =
    process.env.BUILD_FLAVOR ??
    (process.env.NODE_ENV === `production` ? `prod` : `dev`);
  return e === `prod`
    ? `codex-history-snapshots.db`
    : `codex-history-snapshots-${e}.db`;
}
function c_(e) {
  let t = e.prepare(`PRAGMA user_version`).get()?.user_version ?? 0;
  if (t > i_) throw Error(`History cache schema ${t} is newer than supported`);
  if (t !== i_) {
    e.prepare(`BEGIN`).run();
    try {
      (e.exec(`
      DROP TABLE IF EXISTS app_server_history_snapshots;
      CREATE TABLE app_server_history_snapshots (
        principal_key TEXT NOT NULL,
        host_id TEXT NOT NULL,
        thread_id TEXT NOT NULL,
        accessed_at INTEGER NOT NULL,
        payload_bytes INTEGER NOT NULL CHECK (payload_bytes >= 0),
        payload_json TEXT NOT NULL,
        PRIMARY KEY (principal_key, host_id, thread_id)
      );
      CREATE INDEX app_server_history_snapshots_access_idx
        ON app_server_history_snapshots (accessed_at);
    `),
        e.prepare(`PRAGMA user_version = ${i_}`).run(),
        e.prepare(`COMMIT`).run());
    } catch (t) {
      throw (e.prepare(`ROLLBACK`).run(), t);
    }
  }
}
var l_ = n.bl().nullable(),
  u_ = n.gl(`type`, [
    n.xl({ type: n.vl(`notLoaded`) }),
    n.xl({ type: n.vl(`idle`) }),
    n.xl({ type: n.vl(`systemError`) }),
    n.xl({
      type: n.vl(`active`),
      activeFlags: n.pl(n.dl([`waitingOnApproval`, `waitingOnUserInput`])),
    }),
  ]),
  d_ = n.Tl([
    n.dl([`cli`, `vscode`, `exec`, `appServer`, `unknown`]),
    n.xl({ custom: n.wl() }),
    n.xl({ subAgent: n._l() }),
  ]),
  f_ = n
    .xl({ id: n.wl(), type: n.wl() })
    .passthrough()
    .superRefine((e, t) => {
      let n = !0;
      switch (e.type) {
        case `agentMessage`:
          n = typeof e.text == `string`;
          break;
        case `userMessage`:
          n = Array.isArray(e.content);
          break;
        case `imageGeneration`:
          n = typeof e.result == `string`;
          break;
        case `collabAgentToolCall`:
          n = Array.isArray(e.receiverThreadIds);
          break;
        default:
          break;
      }
      n ||
        t.addIssue({
          code: `custom`,
          message: `Malformed ${e.type} history item`,
        });
    }),
  p_ = n.xl({
    version: n.vl(1),
    threadId: n.wl(),
    threadSummary: n
      .xl({
        createdAt: n.bl(),
        cwd: n.wl().nullable(),
        forkedFromId: n.wl().nullable().optional(),
        gitInfo: n
          .xl({
            branch: n.wl().nullable(),
            originUrl: n.wl().nullable(),
            sha: n.wl().nullable(),
          })
          .nullable(),
        hasUnreadTurn: n.ml(),
        mode: n.dl([`default`, `durable`]).optional(),
        modelProvider: n.wl(),
        parentThreadId: n.wl().nullable(),
        threadRuntimeStatus: u_,
        threadSource: n.wl().nullable(),
        threadStartKind: n.wl().optional(),
        source: d_.nullable(),
        title: n.wl().nullable(),
        updatedAt: n.bl(),
        workspaceKind: n.dl([`project`, `projectless`]),
      })
      .passthrough(),
    truncatedBefore: n.ml(),
    turns: n.pl(
      n.xl({
        omittedItemCount: n.bl().int().nonnegative(),
        turn: n
          .xl({
            id: n.wl(),
            items: n.pl(f_),
            itemsView: n.dl([`notLoaded`, `summary`, `full`]),
            status: n.dl([`completed`, `interrupted`, `failed`, `inProgress`]),
            error: n._l().nullable(),
            startedAt: l_,
            completedAt: l_,
            durationMs: l_,
          })
          .passthrough(),
      }),
    ),
  }),
  m_ = n.hl();
function h_(e, t) {
  try {
    let n = m_.parse(p_.parse(JSON.parse(e)));
    return t == null || n.threadId === t ? n : null;
  } catch {
    return null;
  }
}
var g_ = 1024 * 1024,
  __ = 720 * 60 * 60 * 1e3,
  v_ = class {
    evictionTargetBytes;
    maxBytes;
    maxThreadBytes;
    now;
    ttlMs;
    constructor(e, t = {}) {
      if (
        ((this.db = e),
        (this.evictionTargetBytes = t.evictionTargetBytes ?? 160 * g_),
        (this.maxBytes = t.maxBytes ?? 200 * g_),
        (this.maxThreadBytes = t.maxThreadBytes ?? 128 * 1024),
        (this.now = t.now ?? Date.now),
        (this.ttlMs = t.ttlMs ?? __),
        this.evictionTargetBytes > this.maxBytes)
      )
        throw Error(`History cache eviction target exceeds its maximum`);
    }
    write(e, t, n) {
      (y_(e, `principal`), y_(t, `host`));
      let r = h_(n);
      if (r == null) throw Error(`History snapshot payload is invalid`);
      let { threadId: i } = r,
        a = D.Buffer.byteLength(n);
      if (a > this.maxThreadBytes)
        throw Error(`History snapshot exceeds the per-thread limit`);
      let o = this.now();
      this.transaction(() => {
        (this.db
          .prepare(
            `INSERT INTO app_server_history_snapshots (
             principal_key, host_id, thread_id, accessed_at,
             payload_bytes, payload_json
           ) VALUES (?, ?, ?, ?, ?, ?)
           ON CONFLICT(principal_key, host_id, thread_id) DO UPDATE SET
             accessed_at = excluded.accessed_at,
             payload_bytes = excluded.payload_bytes,
             payload_json = excluded.payload_json`,
          )
          .run(e, t, i, o, a, n),
          this.runMaintenance(o));
      });
    }
    read(e, t, n) {
      let r = this.db
        .prepare(
          `SELECT accessed_at, payload_json
         FROM app_server_history_snapshots
         WHERE principal_key = ? AND host_id = ? AND thread_id = ?`,
        )
        .get(e, t, n);
      if (r == null) return null;
      let i = this.now();
      return r.accessed_at < i - this.ttlMs || h_(r.payload_json, n) == null
        ? (this.delete(e, t, n), null)
        : (this.db
            .prepare(
              `UPDATE app_server_history_snapshots SET accessed_at = ?
         WHERE principal_key = ? AND host_id = ? AND thread_id = ?`,
            )
            .run(i, e, t, n),
          r.payload_json);
    }
    delete(e, t, n) {
      this.db
        .prepare(
          `DELETE FROM app_server_history_snapshots
         WHERE principal_key = ? AND host_id = ? AND thread_id = ?`,
        )
        .run(e, t, n);
    }
    runMaintenance(e) {
      this.db
        .prepare(
          `DELETE FROM app_server_history_snapshots WHERE accessed_at < ?`,
        )
        .run(e - this.ttlMs);
      let t =
        this.db
          .prepare(
            `SELECT COALESCE(SUM(payload_bytes), 0) AS total_bytes
           FROM app_server_history_snapshots`,
          )
          .get()?.total_bytes ?? 0;
      if (t <= this.maxBytes) return;
      let n = this.db
        .prepare(
          `SELECT principal_key, host_id, thread_id, payload_bytes
         FROM app_server_history_snapshots ORDER BY accessed_at`,
        )
        .all();
      for (let e of n)
        if (
          (this.delete(e.principal_key, e.host_id, e.thread_id),
          (t -= e.payload_bytes),
          t <= this.evictionTargetBytes)
        )
          return;
    }
    transaction(e) {
      this.db.prepare(`BEGIN IMMEDIATE`).run();
      try {
        let t = e();
        return (this.db.prepare(`COMMIT`).run(), t);
      } catch (e) {
        throw (this.db.prepare(`ROLLBACK`).run(), e);
      }
    }
  };
function y_(e, t) {
  if (e.length === 0) throw Error(`History cache ${t} key is empty`);
}
var b_ = class extends a.P {
  #e;
  get services() {
    return this.#e;
  }
  constructor(e) {
    (super(), (this.#e = e));
  }
};
function x_(e, t) {
  if (e === i.a.Agent && t === n.Vc.ChatGPT)
    return { dark: `icon-agent.ico`, light: `icon-agent.ico` };
  switch (t) {
    case n.Vc.Codex:
      return { dark: `codex-tray.ico`, light: `codex-tray.ico` };
    case n.Vc.ChatGPT:
      return { dark: `chatgpt-tray-dark.ico`, light: `chatgpt-tray-light.ico` };
  }
}
function S_(e) {
  switch (e) {
    case n.Vc.Codex:
      return [`codexTemplate.png`, `codexTemplate@2x.png`];
    case n.Vc.ChatGPT:
      return [`chatgptTemplate.png`, `chatgptTemplate@2x.png`];
  }
}
function C_(e, t, r = n.Vc.Codex) {
  if (r === n.Vc.ChatGPT && (t === `darwin` || t === `win32`))
    switch (e) {
      case i.a.Nightly:
        return `icon-chatgpt-nightly`;
      case i.a.InternalAlpha:
        return `icon-chatgpt-alpha`;
      case i.a.PublicBeta:
        return `icon-chatgpt-beta`;
      case i.a.Dev:
      case i.a.Prod:
        return `icon-chatgpt`;
      case i.a.Agent:
        return `icon-agent`;
    }
  switch (e) {
    case i.a.Agent:
      return `icon-agent`;
    case i.a.Nightly:
      return `icon-nightly`;
    case i.a.InternalAlpha:
      return `icon-alpha`;
    case i.a.PublicBeta:
      return `icon-beta`;
    case i.a.Dev:
    case i.a.Prod:
      return `icon`;
  }
}
function w_(e) {
  let t;
  switch (e) {
    case i.a.Dev:
    case i.a.Prod:
    case i.a.Agent:
      t = ``;
      break;
    case i.a.Nightly:
      t = `-nightly`;
      break;
    case i.a.InternalAlpha:
      t = `-alpha`;
      break;
    case i.a.PublicBeta:
      t = `-beta`;
      break;
  }
  return {
    dark: `icon-codex-dark-color${t}.png`,
    light: `icon-codex-light${t}.png`,
  };
}
var T_ = class extends a.P {
  async get() {
    let e = i.a.resolve(),
      t = a.U();
    return {
      version: n.Fc(c.app.getVersion()).version,
      buildNumber: a.k.value,
      buildFlavor: e,
      osName: n.Ra(process.platform) ?? null,
      systemVersion:
        typeof process.getSystemVersion == `function`
          ? process.getSystemVersion()
          : null,
      appName: c.app.getName(),
      appBrand: t,
      appIconMedium: await O_(e),
      dockIconPreviews: E_(e, t),
    };
  }
};
function E_(e, t) {
  if (process.platform !== `darwin` || t == null) return null;
  let n = w_(e),
    r = D_(`${C_(e, `darwin`, t)}.png`),
    i = D_(n.dark),
    a = D_(n.light);
  return r == null || i == null || a == null
    ? null
    : { appDefault: r, codexDark: i, codexLight: a };
}
function D_(e) {
  if (e == null) return null;
  let t = c.app.isPackaged ? (0, u.join)(process.resourcesPath, e) : null,
    n =
      t != null && (0, p.existsSync)(t)
        ? t
        : (0, u.join)(c.app.getAppPath(), `src`, `icons`, e),
    r = c.nativeImage.createFromPath(n);
  return r.isEmpty()
    ? null
    : r.resize({ width: 128, height: 128, quality: `best` }).toDataURL();
}
async function O_(e) {
  if (process.platform !== `darwin`) return null;
  if (e === i.a.Dev) {
    let e = c.nativeImage.createFromPath(
      (0, u.join)(c.app.getAppPath(), `src`, `icons`, `icon-dev-outline.png`),
    );
    return e.isEmpty()
      ? null
      : e.resize({ width: 128, height: 128, quality: `best` }).toDataURL();
  }
  return c.app.isPackaged ? La(a.s()) : null;
}
var k_ = `ssh_websocket_v0`;
function A_() {
  var e =
      typeof SuppressedError == `function`
        ? SuppressedError
        : function (e, t) {
            var n = Error();
            return (
              (n.name = `SuppressedError`),
              (n.error = e),
              (n.suppressed = t),
              n
            );
          },
    t = {},
    n = [];
  function r(e, t) {
    if (t != null) {
      if (Object(t) !== t)
        throw TypeError(
          `using declarations can only be used with objects, functions, null, or undefined.`,
        );
      if (e)
        var r = t[Symbol.asyncDispose || Symbol.for(`Symbol.asyncDispose`)];
      if (
        r === void 0 &&
        ((r = t[Symbol.dispose || Symbol.for(`Symbol.dispose`)]), e)
      )
        var i = r;
      if (typeof r != `function`) throw TypeError(`Object is not disposable.`);
      (i &&
        (r = function () {
          try {
            i.call(t);
          } catch (e) {
            return Promise.reject(e);
          }
        }),
        n.push({ v: t, d: r, a: e }));
    } else e && n.push({ d: t, a: e });
    return t;
  }
  return {
    e: t,
    u: r.bind(null, !1),
    a: r.bind(null, !0),
    d: function () {
      var r,
        i = this.e,
        a = 0;
      function o() {
        for (; (r = n.pop()); )
          try {
            if (!r.a && a === 1)
              return ((a = 0), n.push(r), Promise.resolve().then(o));
            if (r.d) {
              var e = r.d.call(r.v);
              if (r.a) return ((a |= 2), Promise.resolve(e).then(o, s));
            } else a |= 1;
          } catch (e) {
            return s(e);
          }
        if (a === 1) return i === t ? Promise.resolve() : Promise.reject(i);
        if (i !== t) throw i;
      }
      function s(n) {
        return ((i = i === t ? n : new e(n, i)), o());
      }
      return o();
    },
  };
}
var j_ = class extends a.P {
    authorizationLeases = new Map();
    invalidationListeners = new Map();
    constructor(e, t, n, r, i) {
      (super(),
        (this.getPrincipal = e),
        (this.getStore = t),
        (this.getHostKey = n),
        i((e) => {
          if (e.previous != null) {
            this.authorizationLeases.clear();
            for (let e of this.invalidationListeners.keys())
              this.invalidateHost(e);
          }
        }),
        r((e) => this.invalidateHost(e)));
    }
    acquireAuthorizationLease(e) {
      return this.runAuthorized(e, null, () => this.getAuthorizationLease(e));
    }
    subscribeAuthorizationLeaseInvalidation(e, t) {
      let n = t.dup(),
        r = this.invalidationListeners.get(e) ?? new Set();
      (r.add(n), this.invalidationListeners.set(e, r));
      let i = () => {
        (r.delete(n) && n[Symbol.dispose](),
          r.size === 0 && this.invalidationListeners.delete(e));
      };
      return (n.onRpcBroken(i), new M_(i));
    }
    read(e, t, n) {
      return this.runAuthorized(
        e,
        t,
        (e, t) => this.getStore()?.read(e, t, n) ?? null,
      );
    }
    write(e, t, n) {
      return this.runAuthorized(e, t, (e, t) => {
        let r = this.getStore();
        if (r == null) throw Error(`History snapshot store is unavailable`);
        r.write(e, t, n);
      });
    }
    delete(e, t, n) {
      return this.runAuthorized(e, t, (e, t) => {
        this.getStore()?.delete(e, t, n);
      });
    }
    getAuthorizationLease(e) {
      let t = this.authorizationLeases.get(e);
      if (t != null) return t;
      let n = (0, f.randomUUID)();
      return (this.authorizationLeases.set(e, n), n);
    }
    invalidateHost(e) {
      this.authorizationLeases.delete(e);
      for (let n of this.invalidationListeners.get(e) ?? [])
        try {
          var t = A_();
          t.u(n());
        } catch (e) {
          t.e = e;
        } finally {
          t.d();
        }
    }
    async runAuthorized(e, t, n) {
      let r = this.getHostKey(e);
      if (r == null) return { status: `unavailable` };
      let i = this.getAuthorizationLease(e);
      if (t != null && t !== i) return { status: `unavailable` };
      try {
        let t = await this.getPrincipal();
        return t == null ||
          this.getHostKey(e) !== r ||
          i !== this.getAuthorizationLease(e)
          ? { status: `unavailable` }
          : { status: `ok`, value: n(N_(t), r) };
      } catch {
        return { status: `unavailable` };
      }
    }
  },
  M_ = class extends a.P {
    constructor(e) {
      (super(), (this.unsubscribeOnce = e));
    }
    unsubscribe() {
      (this.unsubscribeOnce?.(), (this.unsubscribeOnce = null));
    }
  };
function N_(e) {
  return (0, f.createHash)(`sha256`)
    .update(`${e.userId}\0${e.accountId}`)
    .digest(`hex`);
}
function P_(e, t = e.kind === `local` ? (e.websocket_url ?? null) : null) {
  let n;
  switch (e.kind) {
    case `local`:
      n = [e.kind, e.id, t];
      break;
    case `ssh`:
      n = [e.kind, e.id, e[k_]];
      break;
    case `wsl`:
      n = [e.kind, e.id, e.distro];
      break;
    case `remote-control`:
      n = [e.kind, e.id, e.env_id];
      break;
  }
  let r = (0, f.createHash)(`sha256`).update(JSON.stringify(n)).digest(`hex`);
  return `${e.id}:${r}`;
}
function F_() {
  let e = process.env.CODEX_APP_VERSION?.trim();
  return !c.app.isPackaged && e ? e : c.app.getVersion();
}
var I_ = { darwin: `Mac OS`, linux: `X11; Linux`, win32: `Windows NT 10.0` };
function L_(e) {
  let t = process.env.CODEX_API_BASE_URL;
  return t && t.trim().length > 0
    ? t.replace(/\/+$/, ``)
    : (process.env.CODEX_API_ENDPOINT ?? ``).toLowerCase() === `localhost`
      ? e.devApiBaseUrl
      : e.prodApiBaseUrl;
}
function R_(e, t) {
  return `${L_(e)}/${t.replace(/^\/+/, ``)}`;
}
async function z_({
  action: e,
  appServerClient: t,
  desktopOriginator: n,
  headers: r = {},
  refreshToken: i = !1,
}) {
  let o = await t.getAuthToken({ refreshToken: i });
  if (!o) {
    let t = a.W();
    throw Error(
      t === `ChatGPT`
        ? `Sign in to ChatGPT to ${e}.`
        : `Sign in to ChatGPT in ${t} to ${e}.`,
    );
  }
  let s = { ...r };
  return (B_(s, o, { desktopOriginator: n }), s);
}
function B_(e, t, { desktopOriginator: n, includeSurfaceHeaders: r = !0 }) {
  G_(e, `Authorization`, `Bearer ${t}`);
  let i = H_(t);
  (i && G_(e, `ChatGPT-Account-Id`, i), r && V_(e, { desktopOriginator: n }));
}
function V_(e, { desktopOriginator: t }) {
  (W_(e, `originator`) || G_(e, `originator`, t),
    W_(e, `User-Agent`) || G_(e, `User-Agent`, U_()));
}
function H_(e) {
  let t = e.split(`.`);
  if (t.length < 2) return null;
  try {
    let e = JSON.parse(Buffer.from(t[1], `base64url`).toString(`utf8`))[
      `https://api.openai.com/auth`
    ];
    if (e && typeof e == `object` && e) {
      let t = e.chatgpt_account_id;
      return typeof t == `string` ? t : null;
    }
  } catch {}
  return null;
}
function U_() {
  let e = I_[process.platform] ?? process.platform;
  return `Codex Desktop/${F_()} (${e}; ${process.arch})`;
}
function W_(e, t) {
  return K_(e, t) != null;
}
function G_(e, t, n) {
  let r = K_(e, t);
  (r && delete e[r], (e[t] = n));
}
function K_(e, t) {
  let n = t.toLowerCase();
  return Object.keys(e).find((e) => e.toLowerCase() === n);
}
var q_ = 100,
  J_ = n.xl({ detail: n.wl().optional() }),
  Y_ = class extends Error {},
  X_ = class extends Error {},
  Z_ = class extends Error {};
async function Q_({ appServerClient: e, clientId: t, desktopApiOptions: n }) {
  return ov({
    appServerClient: e,
    clientId: t,
    desktopApiOptions: n,
    cursor: null,
  });
}
async function $_({
  appServerClient: e,
  desktopApiOptions: t,
  envId: n,
  name: r,
}) {
  return sv(
    await (
      await tv({
        action: `rename remote control environment`,
        appServerClient: e,
        desktopApiOptions: t,
        method: `PATCH`,
        path: av(n),
        headers: { "Content-Type": `application/json` },
        body: JSON.stringify({ name: r }),
        mapNotFoundToFeatureUnavailable: !1,
      })
    ).json(),
  );
}
async function ev({ appServerClient: e, desktopApiOptions: t, envId: n }) {
  await tv({
    action: `delete remote control environment`,
    appServerClient: e,
    desktopApiOptions: t,
    method: `DELETE`,
    path: av(n),
    mapNotFoundToFeatureUnavailable: !1,
  });
}
async function tv({
  action: e,
  appServerClient: t,
  desktopApiOptions: n,
  path: r,
  method: i,
  headers: a = {},
  body: o,
  mapNotFoundToFeatureUnavailable: s = !0,
}) {
  let l = R_(n, r),
    u = await nv({
      action: e,
      appServerClient: t,
      desktopApiOptions: n,
      headers: a,
    }),
    d = await c.net.fetch(l, { method: i, headers: u, body: o });
  if (
    (d.status === 401 &&
      ((u = await nv({
        action: e,
        appServerClient: t,
        desktopApiOptions: n,
        headers: a,
        refreshToken: !0,
      })),
      (d = await c.net.fetch(l, { method: i, headers: u, body: o }))),
    d.status === 404 && s)
  )
    throw new Y_();
  if (d.status === 403) throw new Z_(await lv(d));
  if (d.status === 401) throw new X_(rv(e));
  if (!d.ok)
    throw Error(`Remote control request failed (${d.status}): ${await lv(d)}`);
  return d;
}
async function nv({
  action: e,
  appServerClient: t,
  desktopApiOptions: n,
  headers: r = {},
  refreshToken: i = !1,
}) {
  try {
    return await z_({
      action: e,
      appServerClient: t,
      desktopOriginator: n.desktopOriginator,
      headers: r,
      refreshToken: i,
    });
  } catch (t) {
    throw t instanceof Error && t.message === rv(e) ? new X_(t.message) : t;
  }
}
function rv(e) {
  return `Sign in to ChatGPT to ${e}.`;
}
function iv(e, t) {
  let n = new URLSearchParams({ limit: String(q_) });
  return (
    t != null && n.set(`cursor`, t),
    e == null
      ? `/codex/remote/control/environments?${n.toString()}`
      : `/codex/remote/control/clients/${encodeURIComponent(e)}/environments?${n.toString()}`
  );
}
function av(e) {
  return `/codex/remote/control/environments/${encodeURIComponent(e)}`;
}
async function ov({
  appServerClient: e,
  clientId: t,
  desktopApiOptions: n,
  cursor: r,
}) {
  let i = await (
      await tv({
        action: `load remote control environments`,
        appServerClient: e,
        desktopApiOptions: n,
        mapNotFoundToFeatureUnavailable: t == null,
        method: `GET`,
        path: iv(t, r),
      })
    ).json(),
    a = i.items.map(sv);
  return i.cursor == null
    ? a
    : a.concat(
        await ov({
          appServerClient: e,
          clientId: t,
          desktopApiOptions: n,
          cursor: i.cursor,
        }),
      );
}
function sv(e) {
  let t = e.env_id,
    r = cv(e, `display_name`),
    i = cv(e, `host_name`),
    a = (r ?? e.name ?? i ?? ``).trim(),
    o = (i ?? e.name ?? r ?? ``).trim(),
    s = a || o || t,
    c = o || s;
  return {
    hostId: n.Ya(t),
    displayName: s,
    hostName: c,
    autoConnect: !1,
    source: `remote-control`,
    envId: t,
    environmentKind: e.kind,
    clientType: e.client_type ?? `CODEX_UNKNOWN`,
    online: e.online,
    busy: e.busy,
    os: e.os,
    arch: e.arch,
    appServerVersion: e.app_server_version,
    installationId: e.installation_id ?? null,
    lastSeenAt: e.last_seen_at ?? null,
  };
}
function cv(e, t) {
  let n = e[t];
  return typeof n == `string` ? n : null;
}
async function lv(e) {
  try {
    let t = await e.text();
    if (!t) return e.statusText || `Unknown error`;
    try {
      let e = J_.safeParse(JSON.parse(t));
      if (e.success && e.data.detail != null) return e.data.detail;
    } catch {}
    return t;
  } catch {
    return e.statusText || `Unknown error`;
  }
}
var uv = r.a(`remote-control-websocket`),
  dv = [`remote_control_controller_websocket`],
  fv = class extends Error {
    errorCode = n.sa;
    constructor() {
      super(
        `Remote control enrollment step-up token does not match current account.`,
      );
    }
  },
  pv = `codex.remote_control.enroll`,
  mv = 300,
  hv = `after-first-unlock-device-key-v1`,
  gv = n
    .xl({
      sub: n.wl().optional(),
      "https://api.openai.com/auth": n
        .xl({
          account_id: n.wl().optional(),
          account_user_id: n.wl().optional(),
          chatgpt_account_id: n.wl().optional(),
          chatgpt_account_user_id: n.wl().optional(),
          user_id: n.wl().optional(),
        })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  _v = n.Cl(n.wl(), n.El()),
  vv = class extends n.dn {
    constructor(e) {
      let t = xv(e.desktopApiOptions),
        r = e.globalState,
        i = e.deviceKeyClient;
      super({
        envId: e.hostConfig.env_id,
        connectionGroup: e.appServerClient,
        connectionKey: t,
        websocketUrl: n.fn(
          R_(e.desktopApiOptions, `/codex/remote/control/client`),
        ),
        getAuthHeaders: ({ headers: t } = {}) =>
          Cv({
            appServerClient: e.appServerClient,
            desktopApiOptions: e.desktopApiOptions,
            headers: t,
          }),
        enrollClient: ({ headers: n }) =>
          wv({
            appServerClient: e.appServerClient,
            deviceKeyClient: i,
            desktopApiOptions: e.desktopApiOptions,
            enrollmentKey: t,
            globalState: r,
            headers: n,
            requestRemoteControlEnrollmentStepUpToken:
              e.requestRemoteControlEnrollmentStepUpToken,
          }),
        authorizeDeviceKeyChallenge: (e) =>
          ry({
            challenge: e,
            deviceKeyClient: i,
            enrollmentKey: t,
            globalState: r,
          }),
      });
    }
  };
async function yv({
  appServerClient: e,
  desktopApiOptions: t,
  deviceKeyClient: n,
  globalState: r,
  requestRemoteControlEnrollmentStepUpToken: i,
}) {
  await wv({
    appServerClient: e,
    deviceKeyClient: n,
    desktopApiOptions: t,
    enrollmentKey: xv(t),
    globalState: r,
    headers: await Cv({
      action: `authorize remote control environments`,
      appServerClient: e,
      desktopApiOptions: t,
    }),
    requestRemoteControlEnrollmentStepUpToken: i,
  });
}
async function bv({
  appServerClient: e,
  desktopApiOptions: t,
  deviceKeyClient: n,
  globalState: r,
}) {
  let i = jv(
    await Cv({
      action: `check remote control authorization`,
      appServerClient: e,
      desktopApiOptions: t,
    }),
  );
  if (i.tokenAccountUserId == null)
    return { clientAuthorized: !1, clientId: null };
  let a = await Gv({
    authIdentity: i,
    connectionKey: xv(t),
    deviceKeyClient: n,
    globalState: r,
  });
  return { clientAuthorized: a != null, clientId: a?.record.clientId ?? null };
}
function xv(e) {
  return [e.desktopOriginator, e.devApiBaseUrl, e.prodApiBaseUrl, hv].join(`
`);
}
function Sv(e, t) {
  return `${e}\n${t}`;
}
async function Cv({
  action: e = `connect remote control environments`,
  appServerClient: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return z_({
    action: e,
    appServerClient: t,
    desktopOriginator: n.desktopOriginator,
    headers: r,
  });
}
async function wv({
  appServerClient: e,
  deviceKeyClient: t,
  desktopApiOptions: n,
  enrollmentKey: r,
  globalState: i,
  headers: a,
  requestRemoteControlEnrollmentStepUpToken: o,
}) {
  let s = jv(a),
    c = s.tokenAccountUserId;
  if (c == null)
    throw Error(
      `Remote control enrollment requires the current ChatGPT account user id.`,
    );
  let l = s.tokenAccountId ?? s.headerChatGptAccountId,
    u = await Gv({
      authIdentity: s,
      connectionKey: r,
      deviceKeyClient: t,
      globalState: i,
    }),
    d = u?.record ?? null,
    f = u?.key ?? Sv(r, c),
    p = d,
    m;
  if (p == null) {
    if (o == null)
      throw Error(
        `Remote control enrollment requires explicit authorization in settings.`,
      );
    uv().info(`remote_control_client_enrollment_start_request`, {
      ...Mv({ authIdentity: s, hasExistingEnrollment: !1 }),
    });
    let u = await Bv({
      appServerClient: e,
      body: {},
      desktopApiOptions: n,
      headers: a,
    });
    if (
      (uv().info(`remote_control_client_enrollment_start_response`, {
        ...Mv({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseClientId: u.client_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
        }),
      }),
      u.account_user_id !== c &&
        !(
          s.tokenAccountId != null &&
          s.headerChatGptAccountId === s.tokenAccountId &&
          s.tokenAuthUserId === u.account_user_id
        ))
    )
      throw (
        uv().warning(
          `remote_control_client_enrollment_start_account_mismatch`,
          {
            ...Mv({
              authIdentity: s,
              hasExistingEnrollment: !1,
              responseAccountUserId: u.account_user_id,
              responseClientId: u.client_id,
              responseChallengeId: u.device_key_challenge.challenge_id,
            }),
          },
        ),
        Error(`Remote control enrollment start does not match current account.`)
      );
    p = await Qv({
      accountUserId: u.account_user_id,
      clientId: u.client_id,
      deviceKeyClient: t,
    });
    try {
      if (
        (uv().info(`remote_control_client_enrollment_key_created`, {
          safe: { algorithm: p.algorithm, protectionClass: p.protectionClass },
          sensitive: {
            accountUserId: p.accountUserId,
            clientId: p.clientId,
            keyId: p.keyId,
          },
        }),
        o == null)
      )
        throw Error(
          `Remote control enrollment requires a step-up authorization flow.`,
        );
      uv().info(`remote_control_client_enrollment_step_up_requested`, {
        ...Mv({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
          responseClientId: u.client_id,
        }),
      });
      let d = await o({ accountId: l }),
        f = Pv({ accountUserId: c, stepUpToken: d }),
        h = Mv({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
          responseClientId: u.client_id,
        });
      (uv().info(`remote_control_client_enrollment_step_up_validated`, {
        safe: { ...h.safe, stepUpTokenScopes: f.scopes },
        sensitive: {
          ...h.sensitive,
          stepUpIssuedAt: f.issuedAt,
          stepUpPasswordAuthTime: f.passwordAuthTime,
          stepUpTokenAccountUserId: f.accountUserId,
        },
      }),
        (m = await Vv({
          appServerClient: e,
          body: {
            client_id: p.clientId,
            step_up_token: d,
            device_identity: ey(p),
            device_key_proof: await ny({
              challenge: u.device_key_challenge,
              deviceKeyClient: t,
              desktopApiOptions: n,
              enrollment: p,
              expectedPath: `/codex/remote/control/client/enroll/finish`,
              requireDeviceIdentityHash: !1,
            }),
          },
          desktopApiOptions: n,
          headers: a,
        })),
        uv().info(`remote_control_client_enrollment_finish_response`, {
          ...Nv(m),
        }),
        Ev(m, p),
        Yv(i, Sv(r, p.accountUserId), p));
    } catch (e) {
      throw (await $v({ deviceKeyClient: t, enrollment: p }), e);
    }
  } else {
    uv().info(`remote_control_client_refresh_start_request`, {
      ...Mv({
        authIdentity: s,
        existingEnrollment: d,
        hasExistingEnrollment: !0,
      }),
    });
    let c;
    try {
      c = await Hv({
        appServerClient: e,
        body: { client_id: p.clientId },
        desktopApiOptions: n,
        headers: a,
      });
    } catch (s) {
      if (!Zv(s)) throw s;
      return (
        await $v({ deviceKeyClient: t, enrollment: p }),
        Xv(i, f),
        wv({
          appServerClient: e,
          deviceKeyClient: t,
          desktopApiOptions: n,
          enrollmentKey: r,
          globalState: i,
          headers: a,
          requestRemoteControlEnrollmentStepUpToken: o,
        })
      );
    }
    if (
      (uv().info(`remote_control_client_refresh_start_response`, {
        ...Mv({
          authIdentity: s,
          existingEnrollment: d,
          hasExistingEnrollment: !0,
          responseAccountUserId: c.account_user_id,
          responseClientId: c.client_id,
          responseChallengeId: c.device_key_challenge.challenge_id,
        }),
      }),
      c.client_id !== p.clientId || c.account_user_id !== p.accountUserId)
    )
      throw (
        uv().warning(`remote_control_client_refresh_start_mismatch`, {
          ...Mv({
            authIdentity: s,
            existingEnrollment: d,
            hasExistingEnrollment: !0,
            responseAccountUserId: c.account_user_id,
            responseClientId: c.client_id,
            responseChallengeId: c.device_key_challenge.challenge_id,
          }),
        }),
        Error(
          `Remote control refresh challenge does not match local enrollment.`,
        )
      );
    ((m = await Uv({
      appServerClient: e,
      body: {
        client_id: p.clientId,
        device_key_proof: await ny({
          challenge: c.device_key_challenge,
          deviceKeyClient: t,
          desktopApiOptions: n,
          enrollment: p,
          expectedPath: `/codex/remote/control/client/refresh/finish`,
          requireDeviceIdentityHash: !0,
        }),
      },
      desktopApiOptions: n,
      headers: a,
    })),
      uv().info(`remote_control_client_refresh_finish_response`, { ...Nv(m) }));
  }
  let h = Ev(m, p);
  return {
    clientId: m.client_id,
    headers: {
      "x-codex-client-session-token": `Bearer ${m.remote_control_token}`,
    },
    tokenExpiresAt: h.tokenExpiresAt,
    scopes: h.scopes,
    requiresDeviceKeyProof: !0,
  };
}
function Tv(e) {
  if (e == null) return;
  let t = Date.parse(e);
  if (Number.isFinite(t)) return Math.floor(t / 1e3);
}
function Ev(e, t) {
  if (e.client_id !== t.clientId || e.account_user_id !== t.accountUserId)
    throw Error(
      `Remote control token response does not match local enrollment.`,
    );
  let n = Tv(e.expires_at);
  if (n == null || n <= Math.floor(Date.now() / 1e3))
    throw Error(`Remote control token response has invalid expiration.`);
  if (e.scopes.length !== dv.length || !dv.every((t, n) => e.scopes[n] === t))
    throw Error(`Remote control token response has unexpected scopes.`);
  return { tokenExpiresAt: n, scopes: [...dv] };
}
function Dv(e) {
  return e[`ChatGPT-Account-Id`] ?? e[`chatgpt-account-id`] ?? null;
}
function Ov(e, t) {
  let n = t.toLowerCase();
  for (let [t, r] of Object.entries(e)) if (t.toLowerCase() === n) return r;
  return null;
}
function kv(e) {
  let t = Ov(e, `authorization`);
  if (t == null) return null;
  let [n, r] = t.split(/\s+/, 2);
  return n?.toLowerCase() !== `bearer` || !r ? null : r;
}
function Av(e) {
  try {
    let t = gv.safeParse(Lv(e));
    return t.success ? t.data : null;
  } catch {
    return null;
  }
}
function jv(e) {
  let t = kv(e),
    n = t == null ? null : Av(t),
    r = n?.[`https://api.openai.com/auth`];
  return {
    headerChatGptAccountId: Dv(e),
    tokenAccountId: r?.chatgpt_account_id ?? r?.account_id ?? null,
    tokenAccountUserId:
      r?.chatgpt_account_user_id ?? r?.account_user_id ?? null,
    tokenAuthUserId: r?.user_id ?? null,
    tokenSubject: n?.sub ?? null,
  };
}
function Mv({
  authIdentity: e,
  existingEnrollment: t,
  hasExistingEnrollment: n,
  responseAccountUserId: r,
  responseChallengeId: i,
  responseClientId: a,
}) {
  return {
    safe: { hasExistingEnrollment: n },
    sensitive: {
      headerChatGptAccountId: e.headerChatGptAccountId,
      localEnrollmentAccountUserId: t?.accountUserId ?? null,
      localEnrollmentClientId: t?.clientId ?? null,
      responseAccountUserId: r ?? null,
      responseChallengeId: i ?? null,
      responseClientId: a ?? null,
      selectedAccountUserId: e.tokenAccountUserId,
      tokenAccountId: e.tokenAccountId,
      tokenAccountUserId: e.tokenAccountUserId,
      tokenAuthUserId: e.tokenAuthUserId,
      tokenSubject: e.tokenSubject,
    },
  };
}
function Nv(e) {
  return {
    safe: { expiresAt: e.expires_at, scopes: e.scopes },
    sensitive: { accountUserId: e.account_user_id, clientId: e.client_id },
  };
}
function Pv({ accountUserId: e, stepUpToken: t }) {
  let r = Lv(t);
  Iv({ payload: r });
  let i = n.la.parse(r),
    a = i[`https://api.openai.com/auth`],
    o = a.chatgpt_account_user_id ?? a.account_user_id,
    s = Fv(i);
  if (o !== e) throw new fv();
  if (Math.floor(Date.now() / 1e3) - i.iat > mv)
    throw Error(`Remote control enrollment step-up token is not fresh.`);
  if (Date.now() - i.pwd_auth_time > mv * 1e3)
    throw Error(
      `Remote control enrollment step-up token does not have fresh password auth.`,
    );
  if (s.length !== 1 || s[0] !== pv)
    throw Error(
      `Remote control enrollment step-up token is missing required authorization.`,
    );
  return {
    accountUserId: o ?? null,
    issuedAt: i.iat,
    passwordAuthTime: i.pwd_auth_time,
    scopes: s,
  };
}
function Fv({ scope: e, scp: t }) {
  let n = new Set();
  for (let t of e?.split(/\s+/) ?? []) t.length > 0 && n.add(t);
  for (let e of t ?? []) e.length > 0 && n.add(e);
  return [...n];
}
function Iv({ payload: e }) {
  let t = _v.safeParse(e);
  if (!t.success) {
    uv().warning(`remote_control_step_up_token_payload_unexpected`, {
      safe: { payloadType: typeof e },
      sensitive: {},
    });
    return;
  }
  let n = t.data,
    r = _v.safeParse(n[`https://api.openai.com/auth`]),
    i = r.success ? r.data : null,
    a = _v.safeParse(n[`https://api.openai.com/mfa`]),
    o = a.success ? a.data : null;
  uv().info(`remote_control_step_up_token_payload_decoded`, {
    safe: {
      claimKeys: Object.keys(n).sort(),
      hasScope: typeof n.scope == `string`,
      hasScopeArray: Array.isArray(n.scp),
      hasIssuedAt: typeof n.iat == `number`,
      hasPasswordAuthTime: typeof n.pwd_auth_time == `number`,
      authClaimKeys: i == null ? null : Object.keys(i).sort(),
      hasAuthAmr: Array.isArray(i?.amr),
      hasAuthAccountUserId: typeof i?.chatgpt_account_user_id == `string`,
      hasLegacyAuthAccountUserId: typeof i?.account_user_id == `string`,
      mfaClaimKeys: o == null ? null : Object.keys(o).sort(),
      hasMfaRequired: typeof o?.required == `string`,
    },
    sensitive: {},
  });
}
function Lv(e) {
  let t = e.split(`.`);
  if (t.length < 2 || !t[1])
    throw Error(`Remote control enrollment step-up token is malformed.`);
  return JSON.parse(Buffer.from(t[1], `base64url`).toString(`utf8`));
}
function Rv(e) {
  let t = process.env.CODEX_API_BASE_URL;
  return t && t.trim().length > 0
    ? t.replace(/\/+$/, ``)
    : (process.env.CODEX_API_ENDPOINT ?? ``).toLowerCase() === `localhost`
      ? e.devApiBaseUrl
      : e.prodApiBaseUrl;
}
function zv(e, t) {
  let n = new URL(t.replace(/^\/+/, ``), `${Rv(e).replace(/\/+$/, ``)}/`);
  return { targetOrigin: n.origin, targetPath: n.pathname };
}
async function Bv({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return Wv({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/enroll/start`,
  });
}
async function Vv({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return Wv({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/enroll/finish`,
  });
}
async function Hv({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return Wv({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    mapNotFoundToFeatureUnavailable: !1,
    path: `/codex/remote/control/client/refresh/start`,
  });
}
async function Uv({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return Wv({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/refresh/finish`,
  });
}
async function Wv({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
  mapNotFoundToFeatureUnavailable: i,
  path: a,
}) {
  return await (
    await tv({
      action: `connect remote control environments`,
      appServerClient: e,
      desktopApiOptions: n,
      headers: { ...r, "content-type": `application/json` },
      method: `POST`,
      path: a,
      body: JSON.stringify(t),
      mapNotFoundToFeatureUnavailable: i,
    })
  ).json();
}
async function Gv({
  authIdentity: e,
  connectionKey: t,
  deviceKeyClient: n,
  globalState: r,
}) {
  let i = (
    await Promise.all(
      Kv(e).map(async (e) => {
        let i = Sv(t, e);
        return {
          key: i,
          record: await Jv({
            deviceKeyClient: n,
            enrollmentKey: i,
            globalState: r,
          }),
        };
      }),
    )
  ).find((e) => e.record != null);
  return i?.record == null ? null : { key: i.key, record: i.record };
}
function Kv(e) {
  if (e.tokenAccountUserId == null) return [];
  let t = [e.tokenAccountUserId];
  return (
    e.tokenAccountId != null &&
      e.headerChatGptAccountId === e.tokenAccountId &&
      e.tokenAuthUserId != null &&
      e.tokenAuthUserId !== e.tokenAccountUserId &&
      t.push(e.tokenAuthUserId),
    t
  );
}
function qv(e, t) {
  let r = e.get(`electron-remote-control-client-enrollments`) ?? {},
    i = n.ca.safeParse(r[t]);
  return i.success ? i.data : null;
}
async function Jv({ deviceKeyClient: e, enrollmentKey: t, globalState: n }) {
  let r = qv(n, t);
  if (r == null) return null;
  try {
    let t = await e.getDeviceKeyPublic(r.keyId);
    return t.keyId === r.keyId &&
      t.publicKeySpkiDerBase64 === r.publicKeySpkiDerBase64 &&
      t.algorithm === r.algorithm &&
      t.protectionClass === r.protectionClass
      ? r
      : null;
  } catch {
    return null;
  }
}
function Yv(e, t, n) {
  e.update(a.w, (e) => ({ ...e, [t]: n }));
}
function Xv(e, t) {
  e.update(a.w, (e) => {
    let n = { ...e };
    return (delete n[t], n);
  });
}
function Zv(e) {
  return e instanceof Error
    ? e.message.startsWith(`Remote control request failed (404):`) ||
        e.message === `Remote-control client has been revoked` ||
        e.message ===
          `Remote control request failed (401): Remote-control client enrollment is incomplete` ||
        e.message ===
          `Remote control request failed (403): Remote-control client key material missing`
    : !1;
}
async function Qv({ accountUserId: e, clientId: t, deviceKeyClient: n }) {
  let r = await n.createDeviceKey(`allow_os_protected_nonextractable`);
  return {
    accountUserId: e,
    algorithm: r.algorithm,
    clientId: t,
    keyId: r.keyId,
    protectionClass: r.protectionClass,
    publicKeySpkiDerBase64: r.publicKeySpkiDerBase64,
  };
}
async function $v({ deviceKeyClient: e, enrollment: t }) {
  try {
    await e.deleteDeviceKey(t.keyId);
  } catch (e) {
    uv().warning(`remote_control_client_enrollment_key_cleanup_failed`, {
      safe: {},
      sensitive: {
        accountUserId: t.accountUserId,
        clientId: t.clientId,
        keyId: t.keyId,
        error: e,
      },
    });
  }
}
function ey(e) {
  return {
    key_id: e.keyId,
    public_key_spki_der_base64: e.publicKeySpkiDerBase64,
    algorithm: e.algorithm,
    protection_class: e.protectionClass,
  };
}
function ty(e) {
  return (0, f.createHash)(`sha256`)
    .update(
      JSON.stringify({
        algorithm: e.algorithm,
        keyId: e.keyId,
        protectionClass: e.protectionClass,
        publicKeySpkiDerBase64: e.publicKeySpkiDerBase64,
      }),
    )
    .digest(`base64url`);
}
async function ny({
  challenge: e,
  deviceKeyClient: t,
  desktopApiOptions: n,
  enrollment: r,
  expectedPath: i,
  requireDeviceIdentityHash: a,
}) {
  let o = zv(n, i);
  if (
    e.purpose !== `remote_control_client_enrollment` ||
    e.audience !== `remote_control_client_enrollment` ||
    e.account_user_id !== r.accountUserId ||
    e.client_id !== r.clientId ||
    e.target_origin !== o.targetOrigin ||
    e.target_path !== o.targetPath
  )
    throw (
      uv().warning(`remote_control_client_enrollment_challenge_mismatch`, {
        safe: {},
        sensitive: {
          accountUserId: e.account_user_id,
          clientId: e.client_id,
          enrollmentAccountUserId: r.accountUserId,
          enrollmentClientId: r.clientId,
          expectedTargetOrigin: o.targetOrigin,
          expectedTargetPath: o.targetPath,
          targetOrigin: e.target_origin,
          targetPath: e.target_path,
        },
      }),
      Error(
        `Remote control enrollment challenge does not match local enrollment.`,
      )
    );
  if (a && e.device_identity_hash == null)
    throw Error(
      `Remote control enrollment challenge is missing device identity hash.`,
    );
  let s = e.device_identity_hash ?? ty(r);
  if (s !== ty(r))
    throw Error(
      `Remote control enrollment challenge does not match local device identity.`,
    );
  uv().info(`remote_control_client_enrollment_challenge_signing`, {
    safe: {
      challengeExpiresAt: e.challenge_expires_at,
      purpose: e.purpose,
      requireDeviceIdentityHash: a,
    },
    sensitive: {
      accountUserId: e.account_user_id,
      challengeId: e.challenge_id,
      clientId: e.client_id,
      expectedTargetOrigin: o.targetOrigin,
      expectedTargetPath: o.targetPath,
      keyId: r.keyId,
      targetOrigin: e.target_origin,
      targetPath: e.target_path,
    },
  });
  let c = await t.signDeviceKey(r.keyId, {
    type: `remoteControlClientEnrollment`,
    nonce: e.nonce,
    audience: `remote_control_client_enrollment`,
    challengeId: e.challenge_id,
    targetOrigin: e.target_origin,
    targetPath: e.target_path,
    accountUserId: e.account_user_id,
    clientId: e.client_id,
    deviceIdentitySha256Base64url: s,
    challengeExpiresAt: e.challenge_expires_at,
  });
  return (
    uv().info(`remote_control_client_enrollment_challenge_signed`, {
      safe: { algorithm: c.algorithm },
      sensitive: {
        accountUserId: e.account_user_id,
        challengeId: e.challenge_id,
        clientId: e.client_id,
        keyId: r.keyId,
      },
    }),
    {
      challenge_token: e.challenge_token,
      key_id: r.keyId,
      signature_der_base64: c.signatureDerBase64,
      signed_payload_base64: c.signedPayloadBase64,
      algorithm: c.algorithm,
    }
  );
}
async function ry({
  challenge: e,
  deviceKeyClient: t,
  enrollmentKey: n,
  globalState: r,
}) {
  let i = await Jv({
    deviceKeyClient: t,
    enrollmentKey: Sv(n, e.accountUserId),
    globalState: r,
  });
  if (
    (uv().debug(`remote_control_websocket_device_key_challenge`, {
      safe: { audience: e.audience, hasLocalEnrollment: i != null },
      sensitive: {
        accountUserId: e.accountUserId,
        clientId: e.clientId,
        localEnrollmentAccountUserId: i?.accountUserId ?? null,
        localEnrollmentClientId: i?.clientId ?? null,
        sessionId: e.sessionId,
        targetOrigin: e.targetOrigin,
        targetPath: e.targetPath,
      },
    }),
    i == null ||
      i.accountUserId !== e.accountUserId ||
      i.clientId !== e.clientId)
  )
    throw (
      uv().warning(`remote_control_websocket_device_key_challenge_mismatch`, {
        safe: { hasLocalEnrollment: i != null },
        sensitive: {
          accountUserId: e.accountUserId,
          clientId: e.clientId,
          localEnrollmentAccountUserId: i?.accountUserId ?? null,
          localEnrollmentClientId: i?.clientId ?? null,
          sessionId: e.sessionId,
        },
      }),
      Error(`Remote control device-key challenge has no matching local key.`)
    );
  let a = {
      type: `remoteControlClientConnection`,
      nonce: e.nonce,
      audience: e.audience,
      sessionId: e.sessionId,
      targetOrigin: e.targetOrigin,
      targetPath: e.targetPath,
      accountUserId: e.accountUserId,
      clientId: e.clientId,
      tokenSha256Base64url: e.tokenSha256Base64url,
      tokenExpiresAt: e.tokenExpiresAt,
      scopes: e.scopes,
    },
    o = await t.signDeviceKey(i.keyId, a);
  return (
    uv().info(`remote_control_websocket_device_key_challenge_signed`, {
      safe: {
        algorithm: o.algorithm,
        scopes: e.scopes,
        tokenExpiresAt: e.tokenExpiresAt,
      },
      sensitive: {
        accountUserId: e.accountUserId,
        clientId: e.clientId,
        keyId: i.keyId,
        sessionId: e.sessionId,
        targetOrigin: e.targetOrigin,
        targetPath: e.targetPath,
      },
    }),
    {
      type: `device_key_proof`,
      keyId: i.keyId,
      signatureDerBase64: o.signatureDerBase64,
      signedPayloadBase64: o.signedPayloadBase64,
      algorithm: o.algorithm,
    }
  );
}
var iy = `https://chatgpt.com/codex/install.sh`,
  ay = `https://chatgpt.com/codex/install.ps1`;
function oy(e) {
  let t = e.toLowerCase();
  if (t.startsWith(`cygwin`) || t.startsWith(`mingw`) || t.startsWith(`msys`))
    return `windows`;
  switch (t) {
    case `darwin`:
    case `linux`:
      return `posix`;
    default:
      throw Error(`Codex CLI installation is not supported on ${e}`);
  }
}
function sy(e) {
  switch (e) {
    case `posix`:
      return `if command -v curl >/dev/null 2>&1; then installer_script="$(curl -fsSL ${iy})" || exit; elif command -v wget >/dev/null 2>&1; then installer_script="$(wget -qO- ${iy})" || exit; else echo 'curl or wget is required to install the Codex CLI' >&2; exit 127; fi; printf '%s\\n' "$installer_script" | CODEX_RELEASE=latest CODEX_NON_INTERACTIVE=1 sh`;
    case `windows`:
      return `install_dir="$(cygpath -w "\${CODEX_INSTALL_DIR:-$HOME/.local/bin}")" || exit; CODEX_INSTALL_DIR="$install_dir" powershell -ExecutionPolicy ByPass -c ${n.Qn(`$env:CODEX_RELEASE = "latest"; $env:CODEX_NON_INTERACTIVE = "1"; irm ${ay} | iex`)}`;
  }
}
var cy = `codex`,
  ly = 'PATH="${CODEX_INSTALL_DIR:-$HOME/.local/bin}:$PATH"; export PATH',
  uy = 'CODEX_HOME="${CODEX_HOME:-$HOME/.codex}"; export CODEX_HOME';
function dy(e) {
  return `'${e.replace(/'/g, `'\\''`)}'`;
}
function fy(e, t) {
  let n = `${ly}; ${e}`,
    r = t == null ? n : `printf '%b' ${dy(vy(t))}; ${n}`,
    i = `exec /bin/sh -c "$CODEX_REMOTE_PAYLOAD"`,
    a = [
      `set loginsh=1`,
      `if ( -r /etc/csh.login ) source /etc/csh.login`,
      `if ( -r ~/.login ) source ~/.login`,
      i,
    ].join(`; `),
    o = `${uy}; ${i}`;
  return `sh -c ${dy([`if [ -z "$SHELL" ] || [ ! -x "$SHELL" ]; then echo "Codex remote SSH requires SHELL to point to an executable login shell" >&2; exit 127; fi;`, `CODEX_REMOTE_PAYLOAD="$1"; export CODEX_REMOTE_PAYLOAD;`, 'case "${SHELL##*/}" in', `csh|tcsh) exec "$SHELL" -i -c ${dy(a)} ;;`, `nu) exec "$SHELL" -l -i -c ${dy(`exec /bin/sh -c $env.CODEX_REMOTE_PAYLOAD`)} ;;`, `fish|xonsh) exec "$SHELL" -l -i -c ${dy(i)} ;;`, `*) exec "$SHELL" -l -i -c ${dy(o)} ;;`, `esac`].join(` `))} sh ${dy(r)}`;
}
function py() {
  return [
    `sh`,
    `-c`,
    `login_shell="\${SHELL:-}"
if [ -z "$login_shell" ] && command -v getent >/dev/null 2>&1; then
  login_shell="$(getent passwd "$(id -un)" | cut -d: -f7 || true)"
fi
if [ -z "$login_shell" ] || [ ! -x "$login_shell" ]; then
  login_shell="/bin/bash"
fi
exec "$login_shell" -il`,
  ];
}
function my() {
  let e = process.env.CODEX_CLI_PATH;
  if (e == null) return null;
  let t = e.trim();
  return t.length === 0 ? null : t;
}
function hy(e) {
  return /[\\/]/.test(e) || /^[a-zA-Z]:/.test(e);
}
function gy() {
  let e = my();
  return e == null || hy(e) ? null : e;
}
function _y() {
  return gy() ?? cy;
}
function vy(e) {
  return Array.from(e, (e) => `\\${e.toString(8).padStart(3, `0`)}`).join(``);
}
var yy = new Set([
    `INIT_CWD`,
    `npm_command`,
    `npm_execpath`,
    `npm_node_execpath`,
    `PNPM_PACKAGE_NAME`,
    `PNPM_SCRIPT_SRC_DIR`,
  ]),
  by = [`npm_config_`, `npm_lifecycle_`, `npm_package_`];
function xy(e) {
  for (let t of Object.keys(e)) {
    let n = t.toLowerCase();
    (yy.has(t) || by.some((e) => n.startsWith(e))) && delete e[t];
  }
}
var Sy = 5e3,
  Cy = 1500,
  wy = null,
  Ty = null,
  Ey = null;
async function Dy() {
  let e = await Oy({ caller: `startup`, retryFailed: !1 });
  return (e.status !== `loaded` && (await Ay()), e);
}
async function Oy({
  caller: e = `startup`,
  retryFailed: t = !0,
  timeoutMs: n = Sy,
} = {}) {
  if (Ey?.status === `loaded` || (Ey != null && !t)) return Ny(Ey, e);
  if (wy != null && Ty != null && Ty >= n)
    return wy.then((t) => ({ ...t, caller: e }));
  let r = ky(e, n);
  ((wy = r), (Ty = n));
  try {
    let e = await r;
    return (Py(e), e);
  } finally {
    wy === r && ((wy = null), (Ty = null));
  }
}
async function ky(e, t) {
  let i = Date.now();
  c.app.isPackaged || xy(process.env);
  let a = new AbortController(),
    o = null,
    s = await Promise.race([
      n
        .rr({ interactive: !0, extraEnv: { [n.ir]: `1` }, signal: a.signal })
        .then((e) => ({ status: `loaded`, userEnv: e }))
        .catch((e) => ({ status: `failed`, error: e })),
      new Promise((e) => {
        ((o = setTimeout(() => {
          (a.abort(), e({ status: `timed_out` }));
        }, t)),
          o.unref());
      }),
    ]).finally(() => {
      o != null && clearTimeout(o);
    });
  if (s.status === `loaded`)
    return (
      Object.assign(process.env, s.userEnv),
      c.app.isPackaged || xy(process.env),
      { status: `loaded`, path: process.env.PATH ?? null, ...jy(i, e, t) }
    );
  let l =
    s.status === `timed_out`
      ? `Timed out after ${t}ms.`
      : s.error instanceof Error
        ? s.error.message
        : String(s.error);
  return (
    r.r().warning(`Failed to load shell env`, {
      safe: {
        caller: e,
        durationMs: Date.now() - i,
        pathEntryCount: My(process.env.PATH ?? null),
        resultSource: `load`,
        status: s.status,
        timeoutMs: t,
      },
      sensitive: { detail: l, path: process.env.PATH ?? null },
    }),
    {
      status: s.status,
      detail: l,
      path: process.env.PATH ?? null,
      ...jy(i, e, t),
    }
  );
}
function Ay() {
  return new Promise((e) => {
    setTimeout(e, Cy).unref();
  });
}
function jy(e, t, n) {
  let r = process.env.PATH ?? null;
  return {
    caller: t,
    durationMs: Date.now() - e,
    pathEntryCount: My(r),
    resultSource: `load`,
    timeoutMs: n,
  };
}
function My(e) {
  return e == null || e === `` ? 0 : e.split(u.delimiter).length;
}
function Ny(e, t) {
  return { ...e, caller: t, resultSource: `cache` };
}
function Py(e) {
  (Ey?.status === `loaded` && e.status !== `loaded`) || (Ey = e);
}
var Fy = {
    localSshConfigProbe: 5e3,
    proxyWebSocketOpen: 3e4,
    remoteLoginShellCommandMinimum: 6e4,
    remoteCodexInstall: 6e5,
    shellEnvLoad: 1e4,
  },
  Iy = `ws://codex-app-server/rpc`,
  Ly = '"${CODEX_HOME:-$HOME/.codex}/app-server-control"',
  Ry = '"${CODEX_HOME:-$HOME/.codex}/app-server-control/app-server.log"',
  zy = {
    batchMode: `BatchMode=yes`,
    serverAliveIntervalSeconds: 15,
    serverAliveCountMax: 12,
  },
  By = 86,
  Vy = 8,
  Hy = new Map();
function Uy(e) {
  let t = [`-v`, `-o`, zy.batchMode];
  return (
    e != null && t.push(`-o`, `ConnectTimeout=${e}`),
    t.push(
      `-o`,
      `ServerAliveInterval=${zy.serverAliveIntervalSeconds}`,
      `-o`,
      `ServerAliveCountMax=${zy.serverAliveCountMax}`,
    ),
    t
  );
}
function Wy(e) {
  return e
    .split(/\r?\n/u)
    .map((e) => e.trim())
    .filter((e) => e !== `` && !/^debug\d+:/u.test(e))
    .join(
      `
`,
    )
    .trim();
}
function Gy(e) {
  if (e.alias?.trim()) return [e.alias.trim()];
  let t = [];
  return (
    e.identity?.trim() && t.push(`-i`, e.identity.trim()),
    e.port != null && t.push(`-p`, String(e.port)),
    t.push(e.host),
    t
  );
}
function Ky(e) {
  let t = e[k_];
  return t?.sshHost ? t : null;
}
var qy = class {
  kind = `websocket`;
  logger = r.i(`AppServerTransportSshWebsocket`);
  proxyStreams = new Set();
  installedCodexVersion;
  constructor(e) {
    this.options = e;
  }
  supportsReconnect() {
    return !0;
  }
  dispose() {
    for (let e of this.proxyStreams) e.destroy();
    this.proxyStreams.clear();
  }
  async connect() {
    this.installedCodexVersion = void 0;
    let e = this.startShellEnvLoadForSsh();
    return (
      e.promise
        .then((e) => {
          this.logShellEnvLoadForSsh({
            result: e,
            sshAttempted: !0,
            sshPhase: `connect`,
          });
        })
        .catch((t) => {
          this.logger.warning(
            `ssh_websocket_v0.shell_env_load_unexpected_error`,
            {
              safe: {
                ...Qy(e.getState()),
                sshAttempted: !0,
                sshPhase: `connect`,
              },
              sensitive: {
                error: t,
                path: process.env.PATH ?? null,
                sshAlias: this.options.sshConnection.alias,
                sshHost: this.options.sshConnection.host,
                sshPort: this.options.sshConnection.port,
              },
            },
          );
        }),
      await this.waitForShellEnvIfProxyCommandNeedsPath(e, `connect`),
      await this.ensureRemoteAppServer({
        phase: `connect`,
        shellEnv: e.getState(),
      }),
      this.runWithSshStartupGate(async () => {
        let t = { current: null },
          r = new n.zn(Iy, {
            perMessageDeflate: !1,
            createConnection: () => (
              (t.current = this.createSshProxyStream({
                phase: `connect`,
                shellEnv: e.getState(),
              })),
              t.current
            ),
          });
        r.once(`close`, () => {
          t.current?.destroy();
        });
        try {
          await Jy(r);
        } catch (e) {
          throw (
            r.once(`error`, () => void 0),
            t.current?.destroy(),
            r.terminate(),
            e
          );
        }
        return (
          n.Ln(r, {
            onPongTimeout: () => {
              r.terminate();
            },
          }),
          new n.Rn(r)
        );
      })
    );
  }
  startShellEnvLoadForSsh() {
    let e = {
      caller: `ssh_connect`,
      pathEntryCount: $y(process.env.PATH ?? null),
      resultSource: `pending`,
      status: `loading`,
    };
    return {
      getState: () => e,
      promise: Oy({ caller: `ssh_connect`, timeoutMs: Fy.shellEnvLoad }).then(
        (t) => ((e = Zy(t)), t),
      ),
    };
  }
  async waitForShellEnvIfProxyCommandNeedsPath(e, t) {
    if ((await Promise.resolve(), e.getState().status !== `loading`)) return;
    let n = await this.getSshProxyCommandShellEnvDependency();
    if (
      (this.logger.info(`ssh_websocket_v0.shell_env_wait_decision`, {
        safe: {
          needsShellEnv: n.needsShellEnv,
          pathEntryCount: n.pathEntryCount,
          reason: n.reason,
          sshPhase: t,
          timeoutMs: Fy.localSshConfigProbe,
        },
        sensitive: {
          commandName: n.commandName,
          path: process.env.PATH ?? null,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      }),
      !n.needsShellEnv)
    )
      return;
    let r = Date.now();
    try {
      let i = await e.promise;
      this.logger.info(`ssh_websocket_v0.shell_env_wait_completed`, {
        safe: {
          durationMs: Date.now() - r,
          pathEntryCount: i.pathEntryCount,
          reason: n.reason,
          resultSource: i.resultSource,
          sshPhase: t,
          status: i.status,
          timeoutMs: i.timeoutMs,
        },
        sensitive: {
          detail: i.status === `loaded` ? null : i.detail,
          path: i.path,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    } catch (e) {
      this.logger.warning(`ssh_websocket_v0.shell_env_wait_unexpected_error`, {
        safe: { durationMs: Date.now() - r, reason: n.reason, sshPhase: t },
        sensitive: {
          error: e,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    }
  }
  async getSshProxyCommandShellEnvDependency() {
    let e = Date.now();
    try {
      let t = await pb({
        process: n.Yn({
          args: [`ssh`, `-G`, ...Gy(this.options.sshConnection)],
          spawnInsideWsl: !1,
        }),
        timeoutMs: Fy.localSshConfigProbe,
        timeoutMessage: `SSH: local ssh config probe timed out`,
      });
      return t.code === 0
        ? eb(t.stdout, process.env.PATH ?? null)
        : (this.logger.warning(`ssh_websocket_v0.ssh_config_probe_failed`, {
            safe: {
              code: t.code,
              durationMs: Date.now() - e,
              timeoutMs: Fy.localSshConfigProbe,
            },
            sensitive: {
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
              stderr: t.stderr,
              stdout: t.stdout,
            },
          }),
          tb());
    } catch (t) {
      return (
        this.logger.warning(`ssh_websocket_v0.ssh_config_probe_failed`, {
          safe: {
            durationMs: Date.now() - e,
            timeoutMs: Fy.localSshConfigProbe,
          },
          sensitive: {
            error: t,
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
          },
        }),
        tb()
      );
    }
  }
  logShellEnvLoadForSsh({ result: e, sshAttempted: t, sshPhase: n }) {
    this.logger.info(`ssh_websocket_v0.shell_env_load_completed`, {
      safe: {
        caller: e.caller,
        durationMs: e.durationMs,
        pathEntryCount: e.pathEntryCount,
        resultSource: e.resultSource,
        sshAttempted: t,
        sshPhase: n,
        status: e.status,
        timeoutMs: e.timeoutMs,
      },
      sensitive: {
        detail: e.status === `loaded` ? null : e.detail,
        path: e.path,
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
  }
  async ensureRemoteAppServer(e) {
    (this.logger.info(`ssh_websocket_v0.ensure_remote_app_server`, {
      safe: { ...Qy(e.shellEnv), sshAttempted: !0, sshPhase: e.phase },
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    }),
      await this.startRemoteAppServer(e));
  }
  async startRemoteAppServer(e) {
    let t = _y(),
      r;
    try {
      r = await this.runRemoteLoginShellCommand({
        command: [
          `if command -v `,
          n.Qn(t),
          ` >/dev/null 2>&1; then exit 0; fi; exit `,
          String(By),
        ].join(``),
        context: e,
        operation: `codex_path_probe`,
        timeoutMessage: `SSH: codex path probe timed out`,
      });
    } catch (e) {
      throw this.createSshSetupError(`remote_codex_lookup`, e);
    }
    if (r.code === By)
      throw new n.Jn({
        failureReason: `remote-codex-not-found`,
        message:
          "No `codex` found in PATH. Please install the Codex CLI on the remote machine.",
        stage: `remote_codex_lookup`,
      });
    if (r.code !== 0)
      throw this.createSshSetupError(
        `remote_codex_lookup`,
        Error(await this.getSshCommandFailureMessage(r)),
      );
    let i = await this.getInstalledCodexVersion(e);
    if (i != null && !n.Bc(i)) throw Error(n.Rc + i);
    let a = [
        'if [ "${CODEX_SSH_SKIP_APP_SERVER_BOOT:-}" = "true" ]; then exit 0; fi; ',
        `umask 077; mkdir -p -- `,
        Ly,
        ` && (pkill -9 -U "$(id -u)" -f `,
        n.Qn(`${t}.*[d]esktop-ssh-websocket-v0.sock`),
        ` || true) && nohup `,
        n.Qn(t),
        ` -c features.code_mode_host=true`,
        ` app-server --listen `,
        n.Qn(`unix://`),
        ` >${Ry} 2>&1 &`,
      ].join(``),
      o;
    try {
      o = await this.runRemoteLoginShellCommand({
        command: a,
        context: e,
        operation: `app_server_bootstrap`,
        timeoutMessage: `SSH: app-server bootstrap timed out`,
      });
    } catch (e) {
      throw this.createSshSetupError(`remote_app_server_start`, e);
    }
    let { code: s, stdout: c, stderr: l } = o;
    if (s !== 0) {
      this.logger.warning(
        `ssh_websocket_v0.remote_app_server_bootstrap_failed`,
        {
          safe: { code: s },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
            identity: this.options.sshConnection.identity,
            remoteLogPath: Ry,
            stdout: c,
            stderr: l,
          },
        },
      );
      let e = await this.getSshCommandFailureMessage(o);
      throw this.createSshSetupError(`remote_app_server_start`, Error(e));
    }
  }
  createSshSetupError(e, t) {
    return t instanceof n.Jn
      ? t
      : new n.Jn({
          message:
            t instanceof Error
              ? t.message
              : `SSH-backed app-server setup failed: ${String(t)}`,
          stage: e,
        });
  }
  async getSshCommandFailureMessage(e) {
    return (
      Wy(
        [e.stderr, e.stdout].join(`
`),
      ) ||
      (e.code == null
        ? `Remote SSH failed without an exit code or stderr`
        : `Remote SSH failed with exit code ${e.code} and did not return stderr`)
    );
  }
  async getInstalledCodexVersion(e) {
    if (this.installedCodexVersion !== void 0)
      return this.installedCodexVersion;
    let {
      code: t,
      stdout: r,
      stderr: i,
    } = await this.runRemoteLoginShellCommand({
      command: `${n.Qn(_y())} --version`,
      context: e,
      operation: `codex_version_probe`,
      timeoutMessage: `SSH: codex --version check timed out`,
    });
    return t === 0
      ? ((this.installedCodexVersion = n.zc(r || i)),
        this.installedCodexVersion)
      : (this.logger.warning(
          `ssh_websocket_v0.remote_codex_version_probe_failed`,
          {
            safe: { code: t },
            sensitive: {
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
              stdout: r,
              stderr: i,
            },
          },
        ),
        (this.installedCodexVersion = null),
        null);
  }
  async installCodex() {
    let e = Date.now(),
      t = `platform_probe`,
      n = e,
      r = null;
    this.logger.info(`ssh_websocket_v0.remote_codex_install_started`, {
      safe: {},
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
    try {
      ((r = await this.getRemoteCodexInstallPlatform()),
        this.logger.info(
          `ssh_websocket_v0.remote_codex_install_platform_ready`,
          {
            safe: { durationMs: Date.now() - n, platform: r },
            sensitive: {
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
            },
          },
        ),
        (t = `remote_install_command`),
        (n = Date.now()),
        this.logger.info(
          `ssh_websocket_v0.remote_codex_install_remote_command_started`,
          {
            safe: { platform: r, timeoutMs: Fy.remoteCodexInstall },
            sensitive: {
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
            },
          },
        ));
      let i = await this.runRemoteLoginShellCommand({
        command: sy(r),
        operation: `remote_codex_install`,
        timeoutMessage: `SSH: codex install timed out`,
        timeoutMs: Fy.remoteCodexInstall,
      });
      if (i.code !== 0) throw Error(await this.getSshCommandFailureMessage(i));
      this.logger.info(`ssh_websocket_v0.remote_codex_install_completed`, {
        safe: {
          durationMs: Date.now() - e,
          platform: r,
          remoteCommandDurationMs: Date.now() - n,
        },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    } catch (i) {
      throw (
        this.logRemoteCodexInstallFailed({
          error: i,
          platform: r,
          stage: t,
          stageStartedAtMs: n,
          startedAtMs: e,
        }),
        i
      );
    }
  }
  logRemoteCodexInstallFailed({
    error: e,
    platform: t,
    stage: n,
    stageStartedAtMs: r,
    startedAtMs: i,
  }) {
    let a = Date.now();
    this.logger.warning(`ssh_websocket_v0.remote_codex_install_failed`, {
      safe: {
        durationMs: a - i,
        platform: t,
        stage: n,
        stageDurationMs: a - r,
      },
      sensitive: {
        error: e,
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
  }
  async getRemoteCodexInstallPlatform() {
    let e = await this.runRemoteLoginShellCommand({
      command: `uname -s`,
      operation: `remote_codex_platform_probe`,
      timeoutMessage: `SSH: remote platform probe timed out`,
    });
    if (e.code !== 0) throw Error(await this.getSshCommandFailureMessage(e));
    let t = e.stdout.trim();
    if (!t) throw Error(`Remote platform probe did not return an OS`);
    return oy(t);
  }
  async killCodexProcess() {
    let {
      code: e,
      stdout: t,
      stderr: r,
    } = await this.runRemoteLoginShellCommand({
      command: `pkill -9 -U "$(id -u)" -f ${n.Qn(`${_y()}.* app-server.* --listen`)}`,
      operation: `remote_codex_kill`,
      timeoutMessage: `SSH: codex pkill timed out`,
    });
    if (e === 0 || e === 1) {
      this.logger.info(`ssh_websocket_v0.remote_codex_kill_completed`, {
        safe: { matchedProcess: e === 0 },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
          stdout: t,
          stderr: r,
        },
      });
      return;
    }
    throw Error(r || t || `pkill failed with exit code ${e}`);
  }
  async runRemoteLoginShellCommand({
    command: e,
    context: t,
    operation: n,
    timeoutMessage: r,
    timeoutMs: i = Fy.remoteLoginShellCommandMinimum,
  }) {
    return this.runWithSshStartupGate(async () =>
      this.runRemoteLoginShellCommandWithoutGate({
        command: e,
        context: t,
        operation: n,
        timeoutMessage: r,
        timeoutMs: i,
      }),
    );
  }
  async runRemoteLoginShellCommandWithoutGate({
    command: e,
    context: t,
    operation: r,
    timeoutMessage: i,
    timeoutMs: a = Fy.remoteLoginShellCommandMinimum,
  }) {
    let o = Date.now(),
      s = (0, f.randomBytes)(Vy),
      c = this.options.getConnectTimeoutSeconds?.(),
      l = Math.max(Fy.remoteLoginShellCommandMinimum, (c ?? 0) * 1e3, a),
      u = n.Yn({
        args: [`ssh`, ...Uy(c), ...Gy(this.options.sshConnection), fy(e, s)],
        spawnInsideWsl: !1,
      }),
      d = await pb({ process: u, timeoutMs: l, timeoutMessage: i }),
      p = fb(u.getStdout(), s);
    return (
      this.logger.info(
        `ssh_websocket_v0.remote_login_shell_command_completed`,
        {
          safe: {
            code: d.code,
            durationMs: Date.now() - o,
            operation: r,
            shellEnvCaller: t?.shellEnv.caller ?? `unknown`,
            shellEnvPathEntryCount:
              t?.shellEnv.pathEntryCount ?? $y(process.env.PATH ?? null),
            shellEnvResultSource: t?.shellEnv.resultSource ?? `unknown`,
            shellEnvStatus: t?.shellEnv.status ?? `unknown`,
            sshCommandKind: `remote_login_shell`,
            sshPhase: t?.phase ?? `standalone`,
            timeoutMs: l,
          },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
            stdout: p,
            stderr: d.stderr,
          },
        },
      ),
      { ...d, stdout: p }
    );
  }
  createSshProxyStream(e) {
    let t = `${n.Qn(_y())} app-server proxy`,
      i = (0, f.randomBytes)(Vy);
    this.logger.info(`ssh_websocket_v0.proxy_command_starting`, {
      safe: {
        operation: `app_server_proxy`,
        ...Qy(e.shellEnv),
        sshCommandKind: `app_server_proxy`,
        sshPhase: e.phase,
      },
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
    let a = (0, h.spawn)(
        `ssh`,
        [
          `-T`,
          ...Uy(this.options.getConnectTimeoutSeconds?.()),
          ...Gy(this.options.sshConnection),
          fy(t, i),
        ],
        { env: r.t(process.env), stdio: [`pipe`, `pipe`, `pipe`] },
      ),
      { stdin: o, stdout: s, stderr: c } = a;
    if (o == null || s == null || c == null)
      throw (a.kill(), Error(`ssh proxy stdio was unavailable`));
    let l = ``;
    c.on(`data`, (e) => {
      l = `${l}${e.toString(`utf8`)}`.slice(-4e3);
    });
    let u = new b.Duplex({
      read() {
        s.resume();
      },
      write(e, t, n) {
        o.write(e, t, n);
      },
      final(e) {
        (o.end(), e());
      },
      destroy(e, t) {
        (a.kill(), t(e));
      },
    });
    Object.assign(u, {
      setKeepAlive: () => u,
      setNoDelay: () => u,
      setTimeout: () => u,
    });
    let d = (e) => {
      u.destroy(e);
    };
    (o.on(`error`, d),
      this.proxyStreams.add(u),
      u.once(`close`, () => {
        this.proxyStreams.delete(u);
      }));
    let p = !1,
      m = Buffer.alloc(0);
    return (
      s.on(`data`, (e) => {
        let t = e;
        if (!p) {
          let n = Buffer.concat([m, e]),
            r = n.indexOf(i);
          if (r === -1) {
            m = n.subarray(Math.max(0, n.length - (i.length - 1)));
            return;
          }
          ((p = !0), (t = n.subarray(r + i.length)), (m = Buffer.alloc(0)));
        }
        t.length !== 0 && (u.push(t) || s.pause());
      }),
      s.on(`end`, () => {
        u.push(null);
      }),
      a.on(`error`, (e) => {
        u.destroy(e);
      }),
      a.on(`close`, (t, n) => {
        if ((o.removeListener(`error`, d), t === 0)) {
          u.push(null);
          return;
        }
        let r = Wy(l);
        (this.logger.warning(`ssh_websocket_v0.proxy_command_failed`, {
          safe: {
            code: t,
            operation: `app_server_proxy`,
            ...Qy(e.shellEnv),
            signal: n,
            sshCommandKind: `app_server_proxy`,
            sshPhase: e.phase,
          },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
            stderr: l,
          },
        }),
          u.destroy(
            Error(
              `ssh app-server proxy exited with code ${t}, signal ${n}: ${r}`,
            ),
          ));
      }),
      queueMicrotask(() => {
        u.emit(`connect`);
      }),
      u
    );
  }
  async runWithSshStartupGate(e) {
    let t = Xy(this.options.sshConnection),
      n = Hy.get(t),
      r = () => {},
      i = new Promise((e) => {
        r = e;
      }),
      a = (n ?? Promise.resolve()).catch(() => void 0).then(() => i);
    (Hy.set(t, a), await n?.catch(() => void 0));
    try {
      return await e();
    } finally {
      (r(), Hy.get(t) === a && Hy.delete(t));
    }
  }
};
function Jy(e) {
  return e.readyState === n.zn.OPEN
    ? Promise.resolve()
    : new Promise((t, n) => {
        let r = null,
          i = () => {
            (r != null && clearTimeout(r),
              e.off(`open`, a),
              e.off(`error`, o),
              e.off(`close`, s));
          },
          a = () => {
            (i(), t());
          },
          o = (e) => {
            (i(), n(e));
          },
          s = () => {
            (i(), n(Error(`SSH websocket closed before opening`)));
          };
        (e.once(`open`, a),
          e.once(`error`, o),
          e.once(`close`, s),
          (r = setTimeout(() => {
            (i(), n(Error(`SSH websocket open timed out`)));
          }, Fy.proxyWebSocketOpen)),
          r.unref());
      });
}
function Yy(e) {
  if (process.env.CODEX_APP_SERVER_FORCE_CLI === `1` || e.kind !== `ssh`)
    return null;
  let t = Ky(e);
  return t
    ? {
        sshConnection: {
          alias: t.sshAlias,
          host: t.sshHost,
          port: t.sshPort,
          identity: t.identity,
        },
      }
    : null;
}
function Xy(e) {
  let t = e.alias?.trim();
  return t
    ? `alias:${t}`
    : [`direct`, e.host, String(e.port ?? ``), e.identity?.trim() ?? ``].join(`
`);
}
function Zy(e) {
  return {
    caller: e.caller,
    durationMs: e.durationMs,
    pathEntryCount: e.pathEntryCount,
    resultSource: e.resultSource,
    status: e.status,
    timeoutMs: e.timeoutMs,
  };
}
function Qy(e) {
  return {
    shellEnvCaller: e.caller,
    shellEnvPathEntryCount: e.pathEntryCount,
    shellEnvResultSource: e.resultSource,
    shellEnvStatus: e.status,
  };
}
function $y(e) {
  return e == null || e === `` ? 0 : e.split(u.delimiter).length;
}
function eb(e, t) {
  let n = nb(e),
    r = $y(t);
  if (n == null)
    return {
      commandName: null,
      needsShellEnv: !1,
      pathEntryCount: r,
      proxyCommand: null,
      reason: `no_proxy_command`,
    };
  if (n.toLowerCase() === `none`)
    return {
      commandName: null,
      needsShellEnv: !1,
      pathEntryCount: r,
      proxyCommand: n,
      reason: `proxy_command_none`,
    };
  let i = rb(n);
  return i == null
    ? {
        commandName: null,
        needsShellEnv: !1,
        pathEntryCount: r,
        proxyCommand: n,
        reason: `proxy_command_unparsed`,
      }
    : ub(i)
      ? {
          commandName: i,
          needsShellEnv: !1,
          pathEntryCount: r,
          proxyCommand: n,
          reason: `proxy_command_has_path`,
        }
      : db(i, t)
        ? {
            commandName: i,
            needsShellEnv: !1,
            pathEntryCount: r,
            proxyCommand: n,
            reason: `proxy_command_resolved_in_path`,
          }
        : {
            commandName: i,
            needsShellEnv: !0,
            pathEntryCount: r,
            proxyCommand: n,
            reason: `proxy_command_unresolved_in_path`,
          };
}
function tb() {
  return {
    commandName: null,
    needsShellEnv: !1,
    pathEntryCount: $y(process.env.PATH ?? null),
    proxyCommand: null,
    reason: `ssh_config_probe_failed`,
  };
}
function nb(e) {
  let t = null;
  for (let n of e.split(/\r?\n/u)) {
    let e = /^proxycommand\s+(?<command>.*)$/iu.exec(n.trim());
    e?.groups?.command != null && (t = e.groups.command.trim());
  }
  return t;
}
function rb(e) {
  return ib(e, 0);
}
function ib(e, t) {
  let n = sb(e);
  if (n == null) return null;
  for (let e = 0; e < n.length; e += 1) {
    let r = n[e];
    if (!lb(r) && !(r === `exec` || r === `command`) && r !== `env`) {
      if (t < 2 && ab(r)) {
        let r = ob(n, e + 1);
        if (r != null) return ib(r, t + 1);
      }
      return r;
    }
  }
  return null;
}
function ab(e) {
  let t = e.replace(/\\/gu, `/`).toLowerCase(),
    n = t.slice(t.lastIndexOf(`/`) + 1);
  return (
    n === `sh` || n === `bash` || n === `zsh` || n === `dash` || n === `ksh`
  );
}
function ob(e, t) {
  for (let n = t; n < e.length; n += 1) {
    let t = e[n];
    if (t === `-c` || /^-[^-]*c/u.test(t)) return e[n + 1] ?? null;
    if (!t.startsWith(`-`) || t === `--`) return null;
  }
  return null;
}
function sb(e) {
  if (cb(e)) return null;
  try {
    return n.$n(e);
  } catch {
    return null;
  }
}
function cb(e) {
  let t = null,
    n = !1;
  for (let r = 0; r < e.length; r += 1) {
    let i = e[r];
    if (n) {
      n = !1;
      continue;
    }
    if (i === `\\` && t !== `'`) {
      n = !0;
      continue;
    }
    if (t != null) {
      i === t && (t = null);
      continue;
    }
    if (i === `'` || i === `"`) {
      t = i;
      continue;
    }
    if (/[;&|<>]/u.test(i)) return !0;
  }
  return !1;
}
function lb(e) {
  return /^[A-Za-z_][A-Za-z0-9_]*=/u.test(e);
}
function ub(e) {
  return e.includes(`/`) || e.includes(`\\`) || /^[A-Za-z]:/u.test(e);
}
function db(e, t) {
  return t == null || t === ``
    ? !1
    : Ds.default.sync(e, {
        nothrow: !0,
        path: t,
        pathExt: process.env.PATHEXT,
      }) != null;
}
function fb(e, t) {
  let n = e.indexOf(t);
  return n === -1
    ? e.toString(`utf8`)
    : e.subarray(n + t.length).toString(`utf8`);
}
async function pb({ process: e, timeoutMs: t, timeoutMessage: n }) {
  let r = null;
  try {
    return await Promise.race([
      e.wait(),
      new Promise((i, a) => {
        r = setTimeout(() => {
          (e.kill(), a(Error(`${n} after ${t}ms`)));
        }, t);
      }),
    ]);
  } finally {
    r != null && clearTimeout(r);
  }
}
var mb = `/usr/bin/bash`,
  hb = 'PATH="${CODEX_INSTALL_DIR:-$HOME/.local/bin}:$PATH"; export PATH';
function gb(e) {
  return vb(
    e,
    [
      n.Qn(_y()),
      `-c`,
      `features.code_mode_host=true`,
      `app-server`,
      `--analytics-default-enabled`,
    ].join(` `),
  );
}
function _b(e) {
  return vb(e, `${n.Qn(_y())} --version`);
}
function vb(e, t) {
  return [`wsl.exe`, `-d`, e, `--`, mb, `-lc`, `${hb}; exec ${t}`];
}
var yb = class {
    kind = `stdio`;
    stdioTransport;
    installedCodexVersion;
    constructor(e) {
      ((this.options = e), (this.stdioTransport = new n.Nn(e)));
    }
    supportsReconnect() {
      return !0;
    }
    async connect() {
      this.installedCodexVersion = void 0;
      let e = await this.getInstalledCodexVersion();
      if (e != null && !n.Bc(e)) throw Error(n.Rc + e);
      return this.stdioTransport.connect();
    }
    async getInstalledCodexVersion() {
      if (this.installedCodexVersion !== void 0)
        return this.installedCodexVersion;
      let {
        code: e,
        stdout: t,
        stderr: r,
      } = await n
        .Yn({ args: _b(this.options.hostConfig.distro), spawnInsideWsl: !1 })
        .wait();
      return (
        (this.installedCodexVersion = e === 0 ? n.zc(t || r) : null),
        this.installedCodexVersion
      );
    }
    getIoStatsSnapshot() {
      return this.stdioTransport.getIoStatsSnapshot();
    }
  },
  bb = `appHeader.installUpdate.confirmTitle`,
  xb = `Update {appName} now?`,
  Sb = `appHeader.installUpdate.confirmSubtitle`,
  Cb = `{appName} will quit to install the update, which will interrupt active local sessions on this machine`,
  wb = `appHeader.installUpdate.confirmCancel`,
  Tb = `Cancel`,
  Eb = `appHeader.installUpdate.confirmInstall`,
  Db = `Update`,
  Ob = new Set();
function kb() {
  return Ob.size > 0;
}
function Ab(e, t = `default`) {
  e ? Ob.add(t) : Ob.delete(t);
}
var jb = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.windowManager = e),
        (this.origin = t),
        (this.sparkleManager = n));
    }
    checkForUpdates() {
      this.sparkleManager.checkForUpdates();
    }
    async installUpdate() {
      if (process.platform === `darwin` && kb()) {
        let e =
            c.BrowserWindow.fromWebContents(this.origin) ??
            this.windowManager.getPrimaryWindow(),
          t = a.W(),
          n = a.H(),
          r = n.formatMessage({
            messageId: bb,
            defaultMessage: xb,
            values: { appName: t },
          }),
          i = {
            type: `warning`,
            buttons: [
              n.formatMessage({ messageId: Eb, defaultMessage: Db }),
              n.formatMessage({ messageId: wb, defaultMessage: Tb }),
            ],
            defaultId: 0,
            cancelId: 1,
            noLink: !0,
            title: r,
            message: r,
            detail: n.formatMessage({
              messageId: Sb,
              defaultMessage: Cb,
              values: { appName: t },
            }),
          };
        if (
          (e == null
            ? await c.dialog.showMessageBox(i)
            : await c.dialog.showMessageBox(e, i)
          ).response !== 0
        )
          return;
      }
      this.sparkleManager.installUpdatesIfAvailable();
    }
  },
  Mb = r.a(`codex-app-config/apply`);
function Nb({ config: e, globalState: t }) {
  let r = t.get(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS) ?? [],
    i = new Set(
      r.flatMap((e) => {
        let t = e.alias?.trim();
        return t ? [t] : [];
      }),
    ),
    a = [];
  for (let t of e.remoteConnections)
    i.has(t.sshAlias) ||
      (i.add(t.sshAlias),
      a.push({
        hostId: n.Ja(t.sshAlias),
        displayName: t.sshAlias,
        source: `discovered`,
        alias: t.sshAlias,
        hostname: null,
        sshPort: null,
        identity: null,
      }));
  return a.length === 0
    ? !1
    : (t.set(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS, [...r, ...a]), !0);
}
function Pb({
  config: e,
  createRemoteProjectId: t = f.randomUUID,
  globalState: r,
  remoteConnections: i,
}) {
  let a = Rb(i),
    o = new Set(),
    s = [];
  for (let t of e.remoteConnections) {
    let e = a.get(t.sshAlias);
    if (e == null) {
      o.add(t.sshAlias);
      continue;
    }
    s.push({ connectionConfig: t, remoteConnection: e });
  }
  let c = new Set();
  (Fb({ config: e, globalState: r }) &&
    c.add(n.Gs.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS),
    Ib({ globalState: r, remoteConnectionConfigMatches: s }) &&
      c.add(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID));
  let l = Lb({
    createRemoteProjectId: t,
    globalState: r,
    remoteConnectionConfigMatches: s,
  });
  return (
    l.remoteProjectsChanged && c.add(n.Gs.REMOTE_PROJECTS),
    l.projectOrderChanged && c.add(n.Gs.PROJECT_ORDER),
    o.size > 0 &&
      Mb().warning(`skipped_missing_ssh_aliases`, {
        safe: { missingSshAliasCount: o.size },
        sensitive: { sshAliases: Array.from(o) },
      }),
    {
      changedGlobalStateKeys: Array.from(c),
      remoteProjectsChanged: l.remoteProjectsChanged,
      missingSshAliases: Array.from(o),
      remoteConnectionHostIdsToConnect: Array.from(
        new Set(s.map(({ remoteConnection: e }) => e.hostId)),
      ),
    }
  );
}
function Fb({ config: e, globalState: t }) {
  let r = n.Gs.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS,
    i = e.remoteConnectionMaxRetryAttempts;
  return (t.get(r) ?? void 0) === i ? !1 : (t.set(r, i), !0);
}
function Ib({ globalState: e, remoteConnectionConfigMatches: t }) {
  let r = e.get(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID) ?? {},
    i = r,
    a = !1;
  for (let { remoteConnection: e } of t)
    r[e.hostId] !== !0 && ((a ||= ((i = { ...r }), !0)), (i[e.hostId] = !0));
  return a
    ? (e.set(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, i), !0)
    : !1;
}
function Lb({
  createRemoteProjectId: e,
  globalState: t,
  remoteConnectionConfigMatches: r,
}) {
  let i = t.get(n.Gs.REMOTE_PROJECTS) ?? [],
    a = new Map();
  for (let { connectionConfig: t, remoteConnection: o } of r)
    for (let r of t.projects) {
      let t = n.Ci(r.remotePath),
        s = `${o.hostId}:${n.Si(t)}`,
        c = a.get(s) ?? zb(i, o.hostId, t);
      a.set(s, {
        id: c?.id ?? e(),
        hostId: o.hostId,
        remotePath: t,
        label: r.label?.trim() || c?.label || n.xi(t),
      });
    }
  if (a.size === 0)
    return { remoteProjectsChanged: !1, projectOrderChanged: !1 };
  let o = Array.from(a.values()),
    s = t.get(n.Gs.PROJECT_ORDER) ?? [],
    c = o.map((e) => e.id),
    l = [...o, ...i.filter((e) => !a.has(`${e.hostId}:${n.Si(e.remotePath)}`))],
    u = [...c, ...s.filter((e) => !c.includes(e))],
    d = !Bb(i, l),
    f = !Vb(s, u);
  return (
    d && t.set(n.Gs.REMOTE_PROJECTS, l),
    f && t.set(n.Gs.PROJECT_ORDER, u),
    { remoteProjectsChanged: d, projectOrderChanged: f }
  );
}
function Rb(e) {
  let t = new Map();
  for (let n of e) {
    let e = n.sshAlias?.trim();
    !e || t.has(e) || t.set(e, n);
  }
  return t;
}
function zb(e, t, r) {
  let i = n.Si(r);
  return e.find((e) => e.hostId === t && n.Si(e.remotePath) === i) ?? null;
}
function Bb(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          r != null &&
          e.id === r.id &&
          e.hostId === r.hostId &&
          e.remotePath === r.remotePath &&
          e.label === r.label
        );
      })
    : !1;
}
function Vb(e, t) {
  return e.length === t.length ? e.every((e, n) => e === t[n]) : !1;
}
var Hb = 16,
  Ub = 64,
  Wb = 64,
  Gb = 64,
  Kb = 128,
  qb = Intl.DateTimeFormat().resolvedOptions().timeZone;
function Jb() {
  let e = c.screen.getPrimaryDisplay();
  return Yb(
    i.i,
    e.size.height,
    e.size.width,
    e.scaleFactor,
    c.app.getLocale(),
    c.app.getPreferredSystemLanguages(),
    qb,
  );
}
function Yb(e, t, n, r, i, a, o) {
  let s = (i || `unknown`).slice(0, Wb);
  return {
    schemaVersion: 1,
    preferredLanguages: (a.length > 0 ? a : [s])
      .slice(0, Hb)
      .map((e) => e.slice(0, Ub)),
    locale: s,
    timezone: (o || `unknown`).slice(0, Gb),
    screenSizeSum: Math.max(0, Math.round(n + t)),
    screenScale: r,
    appSessionId: e.slice(0, Kb),
  };
}
var Xb = r.i(`BrowserUseOriginState`),
  Zb = {
    origin: `origin`,
    download: `download`,
    upload: `upload`,
    fullCdp: `fullCdp`,
  },
  Qb = {
    origin: `origins`,
    download: `downloads`,
    upload: `uploads`,
    fullCdp: `full_cdp`,
  },
  $b = n.pl(
    n.xl({
      action: n.dl([`add`, `remove`]),
      kind: n.dl([`allowed`, `denied`]),
      origin: n.wl().min(1),
      resource: n.dl(Zb),
    }),
  ),
  ex = class extends a.P {
    constructor(e) {
      (super(), (this.codexHome = e));
    }
    async updateOriginRules(e) {
      let t = $b.parse(e),
        n = await cx(this.codexHome),
        r = t.map((e) => ({
          ...e,
          origin: e.action === `add` ? sx(n, e.resource, e.origin) : e.origin,
        })),
        i = !1;
      for (let e of r) i = ox(n, e) || i;
      return (i && (await ux(this.codexHome, n)), fx(n));
    }
  };
async function tx(e = n.ni()) {
  return fx(await cx(e));
}
async function nx(e, t = n.ni()) {
  let r = await cx(t);
  return ((r.approval_mode = _x(e)), await ux(t, r), fx(r));
}
async function rx(e, t = n.ni()) {
  let r = await cx(t);
  return ((r.history_approval_mode = _x(e)), await ux(t, r), fx(r));
}
async function ix(e, t, r = n.ni()) {
  let i = await cx(r);
  return ((i[vx(e)] = _x(t)), await ux(r, i), fx(i));
}
async function ax(e, t = n.ni()) {
  let r = await cx(t);
  return ((r[Et] = e), await ux(t, r), fx(r));
}
function ox(e, t) {
  let { action: n, kind: r, origin: i, resource: a } = t,
    o = Qb[a];
  if (n === `add`) {
    let t = px(e, o),
      n = bx(t[r]),
      a = mx(r),
      s = bx(t[a]);
    return (
      (t[r] = n.includes(i) ? n : [...n, i]),
      (t[a] = s.filter((e) => e !== i)),
      !n.includes(i) || s.includes(i)
    );
  }
  let s = e[o];
  if (!xx(s)) return !1;
  let c = bx(s[r]);
  return c.includes(i) ? ((s[r] = c.filter((e) => e !== i)), !0) : !1;
}
function sx(e, t, n) {
  let r = e[Qb[t]];
  if (xx(r) && (bx(r.allowed).includes(n) || bx(r.denied).includes(n)))
    return n;
  let i = yx(n);
  if (i == null) throw Error(`Invalid Browser Use origin`);
  return i;
}
async function cx(e) {
  let t = dx(e);
  return (0, p.existsSync)(t) ? lx(t) : {};
}
async function lx(e) {
  try {
    let t = n.Qr(await (0, m.readFile)(e, `utf8`));
    return xx(t) ? t : {};
  } catch (t) {
    return (
      Xb.warning(`browser_use_origin_state_parse_failed`, {
        safe: {},
        sensitive: { error: t, filePath: e },
      }),
      {}
    );
  }
}
async function ux(e, t) {
  let n = dx(e);
  await (0, m.mkdir)(u.default.dirname(n), { recursive: !0 });
  let r = L(t);
  await (0, m.writeFile)(
    n,
    r.endsWith(`
`)
      ? r
      : `${r}\n`,
    `utf8`,
  );
}
function dx(e) {
  return u.default.join(e, Tt);
}
function fx(e) {
  let t = e.origins;
  return {
    fullCdpAccessEnabled: e[Et] === !0,
    approvalMode: hx(e.approval_mode),
    historyApprovalMode: hx(e.history_approval_mode),
    downloadApprovalMode: hx(e.download_approval_mode),
    uploadApprovalMode: hx(e.upload_approval_mode),
    allowedOrigins: gx(t, `allowed`),
    deniedOrigins: gx(t, `denied`),
    allowedDownloadOrigins: gx(e.downloads, `allowed`),
    deniedDownloadOrigins: gx(e.downloads, `denied`),
    allowedUploadOrigins: gx(e.uploads, `allowed`),
    deniedUploadOrigins: gx(e.uploads, `denied`),
    allowedFullCdpOrigins: gx(e.full_cdp, `allowed`),
    deniedFullCdpOrigins: gx(e.full_cdp, `denied`),
  };
}
function px(e, t) {
  if (xx(e[t])) return e[t];
  let n = {};
  return ((e[t] = n), n);
}
function mx(e) {
  return e === `allowed` ? `denied` : `allowed`;
}
function hx(e) {
  return e === `never_ask` ? `neverAsk` : `alwaysAsk`;
}
function gx(e, t) {
  return xx(e) ? bx(e[t]) : [];
}
function _x(e) {
  return e === `neverAsk` ? `never_ask` : `always_ask`;
}
function vx(e) {
  return e === `download` ? `download_approval_mode` : `upload_approval_mode`;
}
function yx(e) {
  let t = e.trim();
  if (t.length === 0) return null;
  let n = /^[a-zA-Z][a-zA-Z\d+.-]*:\/\//.test(t) ? t : `https://${t}`;
  try {
    let e = new URL(n);
    return e.protocol !== `http:` && e.protocol !== `https:` ? null : e.origin;
  } catch {
    return null;
  }
}
function bx(e) {
  if (!Array.isArray(e)) return [];
  let t = new Set(),
    n = [];
  for (let r of e) {
    if (typeof r != `string`) continue;
    let e = r.trim();
    e === `` || t.has(e) || (t.add(e), n.push(e));
  }
  return n;
}
function xx(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
var Sx = { defaultMatch: null, done: !0, matches: [] },
  Cx = class {
    ownerStates = new Map();
    constructor(e, t = async () => null) {
      ((this.createController = e), (this.getFaviconDataURL = t));
    }
    start(e, t, n) {
      let r = this.getOwnerState(e);
      this.clearActiveRequest(r);
      let i = {
        browserTabId: t.browserTabId,
        conversationId: t.conversationId,
        editingSessionId: t.editingSessionId,
        onResult: n.dup(),
        requestId: t.requestId,
      };
      if (
        ((r.activeRequest = i),
        i.onResult.onRpcBroken(() => {
          r.activeRequest === i &&
            (r.controller?.stop(`interaction`),
            this.clearActiveRequest(r),
            (r.editingSessionId = null));
        }),
        r.controller == null)
      ) {
        this.sendResultUpdate(i, Sx);
        return;
      }
      (r.editingSessionId !== t.editingSessionId &&
        (r.controller.resetSession(),
        (r.editingSessionId = t.editingSessionId)),
        r.controller.start(t.input),
        this.sendResult(r, r.controller.result));
    }
    stop(e, t) {
      let n = this.ownerStates.get(e.id);
      n?.activeRequest == null ||
        n.activeRequest.browserTabId !== t.browserTabId ||
        n.activeRequest.conversationId !== t.conversationId ||
        n.activeRequest.editingSessionId !== t.editingSessionId ||
        n.activeRequest.requestId !== t.requestId ||
        (n.controller?.stop(t.reason), this.clearActiveRequest(n));
    }
    getOwnerState(e) {
      let t = this.ownerStates.get(e.id);
      if (t != null) return t;
      let n = {
        activeRequest: null,
        controller: this.createController(),
        editingSessionId: null,
        resultGeneration: 0,
      };
      return (
        n.controller?.on(`result-changed`, (e) => {
          this.sendResult(n, e);
        }),
        e.once(`destroyed`, () => {
          (this.clearActiveRequest(n),
            n.controller?.destroy(),
            this.ownerStates.delete(e.id));
        }),
        this.ownerStates.set(e.id, n),
        n
      );
    }
    clearActiveRequest(e) {
      let t = e.activeRequest;
      ((e.activeRequest = null), t?.onResult[Symbol.dispose]());
    }
    sendResult(e, t) {
      let n = e.activeRequest;
      if (n == null) return;
      let r = ++e.resultGeneration;
      (this.sendResultUpdate(n, wx(t)), this.sendFaviconResult(e, n, t, r));
    }
    async sendFaviconResult(e, t, n, r) {
      let i = new Map(),
        a = async (e) => {
          let t = e.iconURL ?? null,
            n = `${t ?? ``}\0${e.destinationURL}`,
            r = i.get(n);
          return (
            r ??
              ((r = this.getFaviconDataURL({
                destinationURL: e.destinationURL,
                iconURL: t,
              }).catch(() => null)),
              i.set(n, r)),
            Tx(e, await r)
          );
        },
        [o, s] = await Promise.all([
          Promise.all(n.matches.map(a)),
          n.defaultMatch == null ? null : a(n.defaultMatch),
        ]);
      e.activeRequest !== t ||
        e.resultGeneration !== r ||
        (s?.faviconDataURL == null &&
          !o.some((e) => e.faviconDataURL != null)) ||
        this.sendResultUpdate(t, { defaultMatch: s, done: n.done, matches: o });
    }
    sendResultUpdate(e, t) {
      try {
        var n = A_();
        n.u(
          e.onResult({
            browserTabId: e.browserTabId,
            conversationId: e.conversationId,
            editingSessionId: e.editingSessionId,
            requestId: e.requestId,
            result: t,
          }),
        );
      } catch (e) {
        n.e = e;
      } finally {
        n.d();
      }
    }
  };
function wx(e) {
  return {
    defaultMatch: e.defaultMatch == null ? null : Tx(e.defaultMatch, null),
    done: e.done,
    matches: e.matches.map((e) => Tx(e, null)),
  };
}
function Tx(e, t) {
  return {
    contents: e.contents,
    contentsClass: e.contentsClass,
    description: e.description,
    descriptionClass: e.descriptionClass,
    destinationURL: e.destinationURL,
    faviconDataURL: t,
    fillIntoEdit: e.fillIntoEdit,
    inlineAutocompletion: e.inlineAutocompletion,
  };
}
var Ex = class extends a.P {
    constructor(e, t) {
      (super(), (this.autocomplete = e), (this.owner = t));
    }
    start(e, t) {
      this.autocomplete.start(this.owner, e, t);
    }
    stop(e) {
      this.autocomplete.stop(this.owner, e);
    }
  },
  Dx = `persist:codex-browser-`,
  Ox = [`fileSystems`, `indexedDB`, `localStorage`, `webSQL`, `serviceWorkers`];
function kx(e) {
  return `${Dx}${encodeURIComponent(e)}`;
}
async function Ax(e) {
  await jx(Mx(`app`), e);
}
async function jx(e, t) {
  let n = [];
  (t.includes(`cookies`) && n.push(`cookies`),
    t.includes(`siteData`) && n.push(...Ox),
    t.includes(`cache`) && n.push(`cache`),
    t.includes(`downloads`) && n.push(`downloads`),
    t.includes(`history`) && n.push(`history`),
    n.length !== 0 && (await e.clearData({ dataTypes: n })));
}
function Mx(e) {
  return c.session.fromPartition(kx(e));
}
var Nx = kx(`app`),
  Px = n.xl({
    owlProfileImporter: n.xl({
      listImportableProfiles: n.hl((e) => typeof e == `function`),
      importProfile: n.hl((e) => typeof e == `function`),
    }),
  }),
  Fx = n.hl((e) => Px.safeParse(e).success),
  Ix = class e extends a.P {
    constructor(e) {
      (super(), (this.importer = e));
    }
    static create() {
      let t = Lx();
      return t == null ? void 0 : new e(t);
    }
    async importBrowserProfile(e) {
      let t = n.fs.parse(e);
      if (!t.importCookies && !t.importPasswords)
        throw Error(`Select cookies, passwords, or both to import`);
      if (!t.importCookies && t.cookieDomainAllowlist)
        throw Error(`Cookie domain selection requires cookie import`);
      if (
        n
          .pl(n.ds)
          .parse(await this.importer.listImportableProfiles())
          .find(
            (e) => e.source === t.source && e.profilePath === t.profilePath,
          ) == null
      )
        throw Error(`Browser profile is no longer importable`);
      let r = await this.importer.importProfile({ ...t, targetPartition: Nx });
      return n.ps.parse(r);
    }
    async listImportableBrowserProfiles() {
      let e = await this.importer.listImportableProfiles();
      return n.pl(n.ds).parse(e);
    }
  };
function Lx() {
  if ((process.platform !== `darwin` && process.platform !== `win32`) || !i.o())
    return null;
  let e = process._linkedBinding;
  if (typeof e != `function`) return null;
  let t;
  try {
    t = e.call(process, `electron_browser_owl_profile_importer`);
  } catch {
    return null;
  }
  let n = Fx.safeParse(t);
  return n.success ? n.data.owlProfileImporter : null;
}
var Rx = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.messageHandler = e),
        (this.browserSidebarManager = t),
        (this.origin = n));
    }
    async deleteConversation(e) {
      this.messageHandler.deletePersistedBrowserConversation(this.origin, e);
    }
    async getRestorablePageSnapshots({ pages: e }) {
      return this.browserSidebarManager.getRestorableBrowserPageSnapshots(
        this.origin,
        e.map(
          ({ browserStorageId: e, browserTabId: t, conversationId: r }) => ({
            browserStorageId: n.Bo(e),
            browserTabId: n._c(t),
            conversationId: r,
          }),
        ),
      );
    }
    async openSiteInfo({
      anchorBounds: e,
      browserTabId: t,
      conversationId: n,
      siteUrl: r,
    }) {
      return this.browserSidebarManager.openSiteInfo(this.origin, n, t, e, r);
    }
    async registerWebviewHost({
      browserTabId: e,
      conversationId: t,
      hostGeneration: n,
      pagePersistence: r,
      rendererInstanceId: i,
    }) {
      return this.browserSidebarManager.registerWebviewHost(
        this.origin,
        e,
        t,
        n,
        r,
        i,
      );
    }
    async registerWebviewHostSession({ rendererInstanceId: e }) {
      return this.browserSidebarManager.registerWebviewHostSession(
        this.origin,
        e,
      );
    }
    async setPersistenceEnabled({ enabled: e }) {
      this.messageHandler.setBrowserTabPersistenceEnabled(this.origin, e);
    }
  },
  zx = class e extends a.P {
    constructor(e, t) {
      (super(), (this.downloadStore = e), (this.origin = t));
    }
    static create(t, n) {
      return a.j(`OwlDownloads`) ? new e(t, n) : void 0;
    }
    async cancel(e) {
      return this.downloadStore.cancel(e);
    }
    async chooseDownloadDirectory() {
      let e = {
          properties: [`openDirectory`, `createDirectory`],
          title: `Choose Downloads Folder`,
        },
        t = c.BrowserWindow.fromWebContents(this.origin),
        n =
          t == null
            ? await c.dialog.showOpenDialog(e)
            : await c.dialog.showOpenDialog(t, e);
      return n.canceled ? null : (n.filePaths[0] ?? null);
    }
    async getSnapshot() {
      return this.downloadStore.getSnapshot();
    }
    open(e) {
      return this.downloadStore.open(e);
    }
    async pause(e) {
      return this.downloadStore.pause(e);
    }
    async remove(e) {
      return this.downloadStore.remove(e);
    }
    async resume(e) {
      return this.downloadStore.resume(e);
    }
    showDownloadsFolder() {
      return this.downloadStore.showDownloadsFolder();
    }
    async showInFolder(e) {
      return this.downloadStore.showInFolder(e);
    }
  },
  Bx = n.xl({
    hostId: n.wl().trim().min(1),
    marketplacePath: n.wl().nullable(),
    pluginName: n.wl().trim().min(1),
  }),
  Vx = n.xl({
    hostId: n.wl().trim().min(1),
    marketplaceName: n.wl().trim().min(1),
    pluginName: n.wl().trim().min(1),
  }),
  Hx = class extends a.P {
    constructor(e, t) {
      (super(),
        (this.executionHostRegistry = e),
        (this.devRuntimeRepoRoot = t));
    }
    async install(e) {
      let { hostId: t, marketplacePath: r, pluginName: i } = Ux(e),
        a = this.executionHostRegistry.getMaybe(t);
      if (a == null || n.eo(a.hostConfig)) return;
      let o = St({ env: process.env, resourcesPath: process.resourcesPath });
      if (o == null)
        throw Error(`Chrome native host resource sync requires resourcesPath`);
      await n.gn({
        codexHome: await a.codexHome(),
        devRuntimeRepoRoot: this.devRuntimeRepoRoot,
        marketplacePath: r,
        pluginName: i,
        resourcesPath: o,
      });
    }
    async uninstall(e) {
      let { hostId: t, marketplaceName: r, pluginName: i } = Wx(e),
        a = this.executionHostRegistry.getMaybe(t);
      a == null ||
        n.eo(a.hostConfig) ||
        (await n.vn({
          codexHome: await a.codexHome(),
          marketplaceName: r,
          pluginName: i,
        }));
    }
  };
function Ux(e) {
  let t = Bx.safeParse(e);
  if (!t.success) throw Error(`Invalid Chrome native host install request`);
  return t.data;
}
function Wx(e) {
  let t = Vx.safeParse(e);
  if (!t.success) throw Error(`Invalid Chrome native host uninstall request`);
  return t.data;
}
var Gx = class extends a.P {
    async writeText(e) {
      c.clipboard.writeText(e);
    }
  },
  Kx = `input-monitoring-permission.node`,
  qx = `input_monitoring_permission.node`,
  Jx = (0, T.createRequire)(__filename);
function Yx({ addon: e, platform: t = process.platform } = {}) {
  if (t !== `darwin`) return `unavailable`;
  try {
    return (e ?? Xx()).hasInputMonitoringPermission() ? `granted` : `denied`;
  } catch (e) {
    return (
      r.r().warning(`Failed to check input monitoring access`, {
        safe: {},
        sensitive: { error: e },
      }),
      `unavailable`
    );
  }
}
function Xx() {
  let e = Zx().find(p.existsSync);
  if (e == null) throw Error(`Input Monitoring permission addon not found`);
  return Jx(e);
}
function Zx() {
  let e = u.default.join(
      c.app.getAppPath(),
      `native`,
      `input-monitoring-permission-addon`,
      `build`,
      `Release-${process.arch}`,
      qx,
    ),
    t = St({ env: process.env, resourcesPath: process.resourcesPath });
  return t == null ? [e] : [u.default.join(t, `native`, Kx), e];
}
var Qx = r.i(`CodexMicroHostService`),
  $x = `x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent`,
  eS = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.serviceManager = e),
        (this.windowManager = t),
        (this.origin = n));
    }
    async focusWindow() {
      return this.#e()
        ? this.windowManager.showPrimaryWindow({ stealFocus: !0 })
        : !1;
    }
    getState() {
      return this.serviceManager.getState();
    }
    async getInputMonitoringPermissionStatus() {
      return Yx();
    }
    async openInputMonitoringSettings() {
      try {
        await c.shell.openExternal($x);
      } catch (e) {
        Qx.error(`Open input monitoring settings failed`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async stop() {
      this.#e() && (await this.serviceManager.stop());
    }
    async updateLighting(e) {
      return this.#e() ? this.serviceManager.updateLighting(e) : !1;
    }
    #e() {
      return this.windowManager.getPrimaryWindow()?.webContents === this.origin;
    }
  },
  tS = class {
    service = null;
    servicePromise = null;
    constructor(e) {
      this.windowManager = e;
    }
    async getState() {
      let e = await this.getService();
      return (e.start(), e.getState());
    }
    async stop() {
      await (this.service ?? (await this.servicePromise))?.stop();
    }
    async updateLighting(e) {
      return (await this.getService()).updateLighting(e);
    }
    dispose() {
      if (this.service != null) {
        this.service.dispose();
        return;
      }
      this.servicePromise?.then((e) => e.dispose());
    }
    getService() {
      return this.service == null
        ? ((this.servicePromise ??= Promise.resolve()
            .then(() => require(`./codex-micro-service-Bm59YbrT.js`))
            .then(({ CodexMicroService: e }) => {
              let t = new e({
                onDeviceStateChanged: (e) => {
                  this.windowManager.sendMessageToAllWindows({
                    type: `codex-micro-device-state-changed`,
                    state: e,
                  });
                },
                onHidEvent: (e) => {
                  let t = this.windowManager.getPrimaryWindow();
                  t != null &&
                    this.windowManager.sendMessageToWindow(t, {
                      type: `codex-micro-hid-event`,
                      event: e,
                    });
                },
                onJoystickEvent: (e) => {
                  let t = this.windowManager.getPrimaryWindow();
                  t != null &&
                    this.windowManager.sendMessageToWindow(t, {
                      type: `codex-micro-joystick-event`,
                      event: e,
                    });
                },
              });
              return ((this.service = t), t);
            })
            .finally(() => {
              this.servicePromise = null;
            })),
          this.servicePromise)
        : Promise.resolve(this.service);
    }
  },
  nS = class extends a.P {
    async createDesktopNote({ content: e, fileStem: t, parentPath: n }) {
      let r = ne(n),
        i = (0, u.basename)(t);
      for (let t = 0; ; t += 1) {
        let n = (0, u.join)(r, `${i}${t === 0 ? `` : ` (${t})`}.txt`);
        try {
          return (await (0, m.writeFile)(n, e, { flag: `wx` }), { path: R(n) });
        } catch (e) {
          if (
            typeof e != `object` ||
            !e ||
            !(`code` in e) ||
            e.code !== `EEXIST`
          )
            throw e;
        }
      }
    }
    async createSampleChart({ bytes: e, fileStem: t, parentPath: n }) {
      let r = (0, u.join)(ne(n), `${(0, u.basename)(t)}.png`);
      return (await (0, m.writeFile)(r, e), { path: R(r) });
    }
    async requestDesktopRoot() {
      let e = c.app.getPath(`desktop`);
      return (await (await (0, m.opendir)(e)).close(), R(e));
    }
  },
  rS = class extends a.P {
    constructor(e) {
      (super(), (this.executionHostRegistry = e));
    }
    load() {
      return n.Ht({
        appServerClient: this.executionHostRegistry.get(),
        preferWsl: !!process.env.WSL_DISTRO_NAME,
      });
    }
  },
  iS = r.a(`export-logs`);
function aS(e) {
  return e.toString().padStart(2, `0`);
}
function oS(e, t) {
  return (0, u.join)(
    e,
    t.getUTCFullYear().toString(),
    aS(t.getUTCMonth() + 1),
    aS(t.getUTCDate()),
  );
}
function sS(e) {
  return new Date(
    Date.UTC(e.getUTCFullYear(), e.getUTCMonth(), e.getUTCDate()),
  );
}
function cS(e, t) {
  return Array.from({ length: t }, (t, n) => {
    let r = new Date(e);
    return (r.setUTCDate(r.getUTCDate() - n), r);
  });
}
function lS(e, t) {
  let n = sS(e);
  switch (t) {
    case `today`:
      return [n];
    case `last7days`:
    case `session`:
      return cS(n, 7);
  }
}
function uS(e, t, n) {
  switch (e) {
    case `today`:
    case `last7days`:
      return !0;
    case `session`:
      return n.startsWith(`codex-desktop-${t}-`);
  }
}
async function dS(e) {
  let t = lS(e.now, e.scope);
  return (
    await Promise.all(
      t.map(async (t) => {
        let n = oS(e.rootDir, t);
        try {
          return (await (0, m.readdir)(n, { withFileTypes: !0 }))
            .filter((t) => t.isFile() && uS(e.scope, e.appSessionId, t.name))
            .map((e) => (0, u.join)(n, e.name));
        } catch (e) {
          return (
            iS().error(`Failed to read log dir`, {
              safe: {},
              sensitive: { dir: n, error: e },
            }),
            []
          );
        }
      }),
    )
  )
    .flat()
    .sort();
}
function fS(e, t) {
  switch (e) {
    case `today`:
      return `codex-logs-today.txt`;
    case `last7days`:
      return `codex-logs-last7days.txt`;
    case `session`:
      return `codex-logs-session-${t.slice(0, 8)}.txt`;
  }
}
async function pS(e) {
  let t = {
      defaultPath: e.suggestedName,
      filters: [{ name: `Text`, extensions: [`txt`] }],
    },
    n = e.parentWindow
      ? await c.dialog.showSaveDialog(e.parentWindow, t)
      : await c.dialog.showSaveDialog(t);
  return n.canceled || !n.filePath
    ? null
    : (0, u.extname)(n.filePath).toLowerCase() === `.txt`
      ? n.filePath
      : `${n.filePath}.txt`;
}
async function mS(e) {
  try {
    return await (0, m.readFile)(e, `utf8`);
  } catch (e) {
    return (
      iS().error(`Failed to read log file`, {
        safe: {},
        sensitive: { error: e },
      }),
      ``
    );
  }
}
function hS(e) {
  let t = e.indexOf(` `);
  if (t <= 0) return null;
  let n = e.slice(0, t),
    r = Date.parse(n);
  return Number.isFinite(r) ? r : null;
}
async function gS(e) {
  let t = await Promise.all(e.sort().map((e) => mS(e))),
    n = [],
    r = 0;
  for (let e of t)
    for (let t of e.split(/\r?\n/))
      t.length !== 0 &&
        (n.push({ order: r, timestampMs: hS(t), text: t }), (r += 1));
  return (
    n.sort((e, t) => {
      let n = e.timestampMs,
        r = t.timestampMs;
      return n != null && r != null
        ? n - r || e.order - t.order
        : n == null
          ? r == null
            ? e.order - t.order
            : 1
          : -1;
    }),
    n.length > 0
      ? `${n.map((e) => e.text).join(`
`)}\n`
      : ``
  );
}
async function _S(e) {
  let t = new Date(),
    n = await dS({
      rootDir: i.n(),
      now: t,
      scope: e.scope,
      appSessionId: e.appSessionId,
    }),
    r = await pS({
      parentWindow: e.parentWindow,
      suggestedName: fS(e.scope, e.appSessionId),
    });
  r && (await (0, m.writeFile)(r, await gS(n), `utf8`));
}
var vS = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.debugWindowManager = e),
        (this.browserSidebarManager = t),
        (this.origin = n));
    }
    async exportLogs({ scope: e }) {
      await _S({
        appSessionId: i.i,
        parentWindow: c.BrowserWindow.fromWebContents(this.origin),
        scope: e,
      });
    }
    async getBrowserSnapshot() {
      return this.browserSidebarManager.getDebugSnapshot();
    }
    async openBrowserTabOwner(e) {
      return this.browserSidebarManager.openDebugTabOwner(e);
    }
    async openWindow() {
      await this.debugWindowManager?.show(this.origin);
    }
    async resetUpdateSsoState() {}
  },
  yS = class {
    registration = null;
    registeredDeviceId = null;
    constructor(e) {
      this.options = e;
    }
    async ensureCookie(e) {
      let t = e[`oai-did`] ?? null;
      if (t != null && this.registeredDeviceId === t) return;
      if (this.registration != null)
        return (await this.registration, this.ensureCookie(e));
      let n = this.registerCookie(e)
        .then(() => {
          this.registeredDeviceId = t;
        })
        .finally(() => {
          this.registration = null;
        });
      ((this.registration = n), await n);
    }
    async attachToken(e) {
      let t = await this.options.generateToken();
      if (t == null) throw Error(`DeviceCheck token generation is unavailable`);
      e[`x-sentinel-dc`] = JSON.stringify({ token: t });
    }
    async registerCookie(e) {
      let t = await this.options.generateToken();
      if (t == null) throw Error(`DeviceCheck token generation is unavailable`);
      let n = await c.net.fetch(this.options.url, {
        body: JSON.stringify({
          bundle_id: this.options.bundleIdentifier,
          device_token: t,
        }),
        credentials: `include`,
        headers: { ...e, "Content-Type": `application/json` },
        method: `POST`,
      });
      if (!n.ok) throw Error(`DeviceCheck registration failed (${n.status})`);
    }
  },
  bS = e.o(n.Sn());
function xS(e, t) {
  let n = CS(e, t);
  return n != null && TS(n, e) === TS(n, t);
}
function SS(e, t) {
  let n = CS(e, t);
  if (n == null) return !1;
  let r = n.relative(n.resolve(t), n.resolve(e));
  return r === `` || (!r.startsWith(`..`) && !n.isAbsolute(r));
}
function CS(e, t) {
  let n = wS(e);
  return n === wS(t) ? (n ? u.default.win32 : u.default) : null;
}
function wS(e) {
  return /^[A-Za-z]:[\\/]/.test(e) || /^(?:\\\\|\/\/)/.test(e);
}
function TS(e, t) {
  let n = e.resolve(t);
  return e === u.default.win32 ? n.toLowerCase() : n;
}
var ES = n.xl({ name: n.wl().optional() }).passthrough(),
  DS = n.pl(ES);
function OS(e) {
  return e.trim().toLowerCase();
}
function kS(e) {
  let t = DS.safeParse(e);
  if (!t.success) return [];
  let n = new Map();
  for (let e of t.data) {
    let t = e.name?.trim();
    if (t == null || t.length === 0) continue;
    let r = OS(t);
    n.has(r) || n.set(r, t);
  }
  return [...n.values()].sort((e, t) => e.localeCompare(t));
}
function AS(e) {
  let t = new Map();
  for (let n of e) {
    let e = new Map();
    for (let t of n.remoteMcpServerNames) {
      let n = t.trim();
      if (n.length === 0) continue;
      let r = OS(n);
      e.has(r) || e.set(r, n);
    }
    for (let [r, i] of e) {
      let e = t.get(r);
      t.set(r, {
        lastSeenAtMs:
          e?.lastSeenAtMs == null
            ? n.lastActivityAtMs
            : n.lastActivityAtMs == null
              ? e.lastSeenAtMs
              : Math.max(e.lastSeenAtMs, n.lastActivityAtMs),
        name: e?.name ?? i,
        sessionCount: (e?.sessionCount ?? 0) + 1,
        source: `remoteMcpServersConfig`,
      });
    }
  }
  return [...t.values()].sort((e, t) => e.name.localeCompare(t.name));
}
async function jS(e, t) {
  let n = await MS(e);
  if (n == null) return null;
  let r;
  try {
    r = JSON.parse(n);
  } catch {
    return null;
  }
  let i = t.safeParse(r);
  return i.success ? i.data : null;
}
async function MS(e) {
  try {
    return await (0, m.readFile)(e, `utf8`);
  } catch {
    return null;
  }
}
async function NS(e, t) {
  (await (0, m.mkdir)(u.default.dirname(e), { recursive: !0 }),
    await (0, m.writeFile)(
      e,
      t.endsWith(`
`)
        ? t
        : `${t}\n`,
      `utf8`,
    ));
}
async function PS(e) {
  if (!(await IS(e))) return [];
  let t = await (0, m.readdir)(e, { withFileTypes: !0 });
  return (
    await Promise.all(
      t.map(async (t) => {
        let n = u.default.join(e, t.name);
        return t.isDirectory() ? PS(n) : t.isFile() ? [n] : [];
      }),
    )
  ).flat();
}
async function FS(e) {
  try {
    return (await (0, m.stat)(e)).isFile();
  } catch {
    return !1;
  }
}
async function IS(e) {
  try {
    return (await (0, m.stat)(e)).isDirectory();
  } catch {
    return !1;
  }
}
async function LS(e) {
  try {
    return (await (0, m.lstat)(e), !0);
  } catch {
    return !1;
  }
}
var RS = `claude-cowork-import-history.json`,
  zS = n
    .xl({
      contentSha256: n.wl(),
      importedAtMs: n.bl(),
      importedThreadId: n.wl(),
      projectRoot: n.wl().optional(),
      sessionId: n.wl().optional(),
      sourceId: n.wl().optional(),
      sourcePath: n.wl(),
      spaceId: n.wl().optional(),
    })
    .transform((e) => ({
      contentSha256: e.contentSha256,
      importedAtMs: e.importedAtMs,
      importedThreadId: e.importedThreadId,
      projectRoot: e.projectRoot ?? e.spaceId ?? ``,
      sourceId: e.sourceId ?? e.sessionId ?? ``,
      sourcePath: e.sourcePath,
    })),
  BS = n.xl({ importedAtMs: n.bl(), sourceRoot: n.wl(), spaceId: n.wl() }),
  VS = n.xl({
    accountOrgDir: n.wl(),
    importedAtMs: n.bl(),
    itemType: n.dl([`CONFIG`, `PLUGINS`]),
  }),
  HS = n.xl({
    importedAtMs: n.bl(),
    lastSeenAtMs: n.bl().nullable(),
    name: n.wl(),
    sessionCount: n.bl(),
    source: n.vl(`remoteMcpServersConfig`),
  }),
  US = n
    .xl({
      accountSetupItems: n.pl(VS).optional(),
      connectorCandidates: n.pl(HS).optional(),
      projects: n.pl(BS).optional(),
      records: n.pl(zS),
      version: n.vl(1),
    })
    .transform((e) => ({
      accountSetupItems: e.accountSetupItems ?? [],
      connectorCandidates: e.connectorCandidates ?? [],
      projects: e.projects ?? [],
      records: e.records,
      version: e.version,
    }));
async function WS(e) {
  return (
    (await jS(u.default.join(e, `claude-cowork-import-history.json`), US)) ?? {
      accountSetupItems: [],
      connectorCandidates: [],
      projects: [],
      records: [],
      version: 1,
    }
  );
}
async function GS(e) {
  let t = await WS(e);
  return {
    importedSessionCount: t.records.length,
    latestImportedAtMs: [
      ...t.records.map((e) => e.importedAtMs),
      ...t.projects.map((e) => e.importedAtMs),
      ...t.accountSetupItems.map((e) => e.importedAtMs),
      ...t.connectorCandidates.map((e) => e.importedAtMs),
    ].reduce((e, t) => (e == null ? t : Math.max(e, t)), null),
  };
}
async function KS({
  codexHome: e,
  importedAtMs: t = Date.now(),
  transcripts: n,
}) {
  let r = n.map(({ importedThreadId: e, transcript: n }) => ({
    contentSha256: n.contentSha256,
    importedAtMs: t,
    importedThreadId: e,
    projectRoot: n.projectRoot,
    sourceId: n.sourceId,
    sourcePath: n.sourceJsonlPath,
  }));
  if (r.length === 0) return;
  let i = await WS(e);
  await QS(e, { ...i, records: $S(i.records, r, iC) });
}
async function qS({ codexHome: e, importedAtMs: t = Date.now(), items: n }) {
  if (n.length === 0) return;
  let r = await WS(e),
    i = n.map((e) => ({
      accountOrgDir: e.accountOrgDir,
      importedAtMs: t,
      itemType: e.itemType,
    }));
  await QS(e, { ...r, accountSetupItems: $S(r.accountSetupItems, i, eC) });
}
async function JS({ codexHome: e, importedAtMs: t = Date.now(), projects: n }) {
  if (n.length === 0) return;
  let r = await WS(e),
    i = n.map((e) => ({
      importedAtMs: t,
      sourceRoot: e.sourceRoot,
      spaceId: e.spaceId,
    }));
  await QS(e, { ...r, projects: $S(r.projects, i, nC) });
}
async function YS({ candidates: e, codexHome: t }) {
  if (e.length === 0) return;
  let n = await WS(t);
  await QS(t, {
    ...n,
    connectorCandidates: aC({
      existingRecords: n.connectorCandidates,
      nextCandidates: e,
    }),
  });
}
function XS(e, t) {
  return e.projects.some((e) => rC(e, t));
}
function ZS(e, t) {
  return e.accountSetupItems.some((e) => tC(e, t));
}
async function QS(e, t) {
  (await (0, m.mkdir)(e, { recursive: !0 }),
    await (0, m.writeFile)(
      u.default.join(e, RS),
      `${JSON.stringify(t, null, 2)}\n`,
      `utf8`,
    ));
}
function $S(e, t, n) {
  return [...e.filter((e) => !t.some((t) => n(e, t))), ...t];
}
function eC(e, t) {
  return tC(e, t);
}
function tC(e, t) {
  return e.itemType === t.itemType && xS(e.accountOrgDir, t.accountOrgDir);
}
function nC(e, t) {
  return rC(e, t);
}
function rC(e, t) {
  return e.spaceId === t.spaceId && xS(e.sourceRoot, t.sourceRoot);
}
function iC(e, t) {
  return (
    xS(e.projectRoot, t.projectRoot) &&
    e.sourceId === t.sourceId &&
    e.contentSha256 === t.contentSha256
  );
}
function aC({ existingRecords: e, nextCandidates: t }) {
  let n = Date.now(),
    r = new Map(e.map((e) => [oC(e.name, e.source), e]));
  for (let e of t) {
    let t = oC(e.name, e.source),
      i = r.get(t);
    r.set(t, {
      importedAtMs: n,
      lastSeenAtMs:
        i?.lastSeenAtMs == null
          ? e.lastSeenAtMs
          : e.lastSeenAtMs == null
            ? i.lastSeenAtMs
            : Math.max(i.lastSeenAtMs, e.lastSeenAtMs),
      name: sC(i?.name, e.name),
      sessionCount: (i?.sessionCount ?? 0) + e.sessionCount,
      source: e.source,
    });
  }
  return [...r.values()].sort((e, t) => e.name.localeCompare(t.name));
}
function oC(e, t) {
  return `${t}\u001f${OS(e)}`;
}
function sC(e, t) {
  let n = e?.trim();
  return n != null && n.length > 0 ? n : t.trim();
}
function cC(e, t) {
  return {
    ...e,
    spaceConfigPath: t(e.spaceConfigPath),
    sourceFolders: e.sourceFolders.map((e) => t(e)),
  };
}
var lC = `## Imported Claude Cowork project instructions`,
  uC = `claude-cowork`,
  dC = { agents: !0, config: !0, sessions: !0 },
  fC = [`AGENTS_MD`],
  pC = new Set([
    ...[
      `spaces.json`,
      `scheduled-tasks.json`,
      `artifacts.json`,
      `mcp.json`,
      `mcp-servers.json`,
      `plugins.json`,
      `plugins-mcp-skills.json`,
      `skills.json`,
      `agents.json`,
      `cowork_settings.json`,
      `cowork_account_settings.json`,
    ],
    `cowork-clientdata-cache.json`,
    `cowork-gb-cache.json`,
  ]),
  mC = n.Cl(n.wl(), n.El()),
  hC = n.Tl([n.wl(), n.xl({ path: n.wl() }).passthrough()]),
  gC = n
    .xl({
      folders: n.pl(hC).optional(),
      id: n.wl(),
      instructions: n.wl().optional(),
      name: n.wl().optional(),
      title: n.wl().optional(),
      userSelectedFolders: n.pl(hC).optional(),
    })
    .passthrough(),
  _C = n.Tl([n.pl(gC), n.xl({ spaces: n.pl(gC) }).passthrough()]),
  vC = n
    .xl({
      fileName: n.wl(),
      hostPath: n.wl(),
      timestamp: n.Tl([n.bl(), n.wl()]).nullable().optional(),
    })
    .passthrough(),
  yC = n
    .xl({
      cliSessionId: n.wl().nullable().optional(),
      createdAt: n.Tl([n.bl(), n.wl()]).nullable().optional(),
      cwd: n.wl().nullable().optional(),
      fsDetectedFiles: n.pl(vC).optional(),
      id: n.wl().optional(),
      lastActivityAt: n.Tl([n.bl(), n.wl()]).nullable().optional(),
      remoteMcpServersConfig: n.El().optional(),
      scheduledTaskId: n.wl().nullable().optional(),
      sessionId: n.wl().optional(),
      spaceId: n.wl().nullable().optional(),
      title: n.wl().nullable().optional(),
      updatedAt: n.Tl([n.bl(), n.wl()]).nullable().optional(),
      userSelectedFolders: n.pl(hC).optional(),
    })
    .passthrough(),
  bC = n
    .xl({
      id: n.wl().optional(),
      name: n.wl().nullable().optional(),
      spaceId: n.wl().nullable().optional(),
      title: n.wl().nullable().optional(),
    })
    .passthrough(),
  xC = n.Tl([n.pl(bC), n.xl({ tasks: n.pl(bC) }).passthrough()]),
  SC = n
    .xl({
      args: n.pl(n.wl()).optional(),
      command: n.wl().optional(),
      disabled: n.ml().optional(),
      enabled: n.ml().optional(),
      env: n.Cl(n.wl(), n.El()).optional(),
      headers: n.Cl(n.wl(), n.El()).optional(),
      type: n.wl().optional(),
      url: n.wl().optional(),
    })
    .passthrough(),
  CC = n
    .xl({
      mcp_servers: n.Cl(n.wl(), SC).optional(),
      mcpServers: n.Cl(n.wl(), SC).optional(),
    })
    .passthrough(),
  wC = n.Cl(n.wl(), n.El()),
  TC = n
    .xl({
      id: n.wl().optional(),
      name: n.wl().optional(),
      path: n.wl().optional(),
    })
    .passthrough(),
  EC = n.Tl([
    n.pl(TC),
    n
      .xl({ agents: n.pl(TC).optional(), skills: n.pl(TC).optional() })
      .passthrough(),
  ]);
async function DC(e, t) {
  let r = await MS(e);
  if (r == null) return {};
  try {
    let e = mC.safeParse(n.Qr(r));
    if (e.success) return e.data;
  } catch (n) {
    return (t.push(`Could not read Codex config.toml ${e}: ${kC(n)}`), null);
  }
  return (t.push(`Could not read Codex config.toml ${e}`), null);
}
async function OC(e, t) {
  await NS(e, L(t));
}
function kC(e) {
  return e instanceof Error ? e.message : String(e);
}
function AC(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function jC(e) {
  return typeof e == `string` && e.trim() !== `` ? e : null;
}
function MC(e) {
  if (typeof e == `number`) return e > 1e10 ? e : e * 1e3;
  if (typeof e == `string`) {
    let t = Date.parse(e);
    return Number.isNaN(t) ? null : t;
  }
  return null;
}
var NC = n
    .xl({
      description: n.wl().optional(),
      name: n.wl().optional(),
      version: n.wl().optional(),
    })
    .passthrough(),
  PC = `local`,
  FC = `skills`,
  IC = new Set([`.mcp.json`, `mcp.json`]),
  LC = new Set();
async function RC({
  callbacks: e,
  codexHome: t,
  migration: n,
  pluginImports: r = [],
}) {
  let i = [];
  try {
    let a = await BC({
      callbacks: e,
      claimedRolloutImportKeys: i,
      codexHome: t,
      preparedRollouts: n.preparedRollouts,
    });
    return (
      await VC({ codexHome: t, pluginImports: r }),
      await qS({
        codexHome: t,
        items: [...n.accountSetupItemsToRecord, ...zC(r)],
      }),
      await JS({ codexHome: t, projects: n.projectsToRecord }),
      { importedRollouts: a }
    );
  } finally {
    bee(i);
  }
}
function zC(e) {
  return [...new Set(e.map((e) => e.accountOrgDir))].map((e) => ({
    accountOrgDir: e,
    itemType: `PLUGINS`,
  }));
}
async function BC({
  callbacks: e,
  claimedRolloutImportKeys: t,
  codexHome: n,
  preparedRollouts: r,
}) {
  if (r.length === 0) return [];
  let i = await Promise.allSettled(
      r.map(async (r) => {
        let i = QC(n, r);
        i != null && t.push(i);
        try {
          if (i == null) return { importedRollout: null };
          let t = await e.forkThread({
              cwd: $C(e, r.cwd),
              path: $C(e, r.rolloutPath),
              threadId: r.sourceThreadId,
            }),
            n = { importedThreadId: t.threadId, rollout: r };
          if (
            r.workspaceKind === `projectless` &&
            r.projectlessOutputDirectory != null &&
            r.projectlessWorkspaceRoot != null
          )
            try {
              await e.recordProjectlessThread?.({
                outputDirectory: $C(e, r.projectlessOutputDirectory),
                threadId: t.threadId,
                workspaceRoot: $C(e, r.projectlessWorkspaceRoot),
              });
            } catch (e) {
              return { error: e, importedRollout: n };
            }
          try {
            r.title != null &&
              r.title !== `` &&
              (await e.setThreadName({ name: r.title, threadId: t.threadId }));
          } catch (e) {
            return { error: e, importedRollout: n };
          }
          return { importedRollout: n };
        } finally {
          await (0, m.rm)(r.rolloutRoot, { force: !0, recursive: !0 });
        }
      }),
    ),
    a = i.flatMap((e) => (e.status === `fulfilled` ? [e.value] : [])),
    o = a.flatMap((e) =>
      e.importedRollout == null ? [] : [e.importedRollout],
    );
  (await KS({
    codexHome: n,
    transcripts: o.map(({ importedThreadId: e, rollout: t }) => ({
      importedThreadId: e,
      transcript: t,
    })),
  }),
    await YS({ candidates: AS(o.map(({ rollout: e }) => e)), codexHome: n }));
  let s = i.find((e) => e.status === `rejected`);
  if (s?.status === `rejected`) throw s.reason;
  let c = a.find((e) => e.error != null);
  if (c?.error != null) throw c.error;
  return o;
}
async function VC({ codexHome: e, pluginImports: t }) {
  let n = HC(t);
  (await Promise.all(n.map((t) => WC({ codexHome: e, pluginImport: t }))),
    await YC({ codexHome: e, pluginImports: n }));
}
function HC(e) {
  let t = new Map();
  for (let n of e) {
    let e = n.version ?? PC,
      r = [uC, n.pluginName, e].join(``),
      i = t.get(r);
    if (i == null) {
      t.set(r, { ...n, skillPaths: [...n.skillPaths] });
      continue;
    }
    let a = new Map(i.skillPaths.map((e) => [UC(e), e]));
    for (let e of n.skillPaths) {
      let t = UC(e);
      a.has(t) || a.set(t, e);
    }
    i.skillPaths = [...a.values()];
  }
  return [...t.values()];
}
function UC(e) {
  let t = ZC(u.default.dirname(e));
  return process.platform === `win32` ? t.toLowerCase() : t;
}
async function WC({ codexHome: e, pluginImport: t }) {
  let n = t.version ?? PC,
    r = u.default.join(e, `plugins`, `cache`, uC, t.pluginName, n);
  (await (0, m.rm)(r, { force: !0, recursive: !0 }),
    await (0, m.mkdir)(u.default.dirname(r), { recursive: !0 }),
    await (0, m.cp)(t.pluginRoot, r, { filter: qC, recursive: !0 }),
    await GC({ pluginImport: t, targetRoot: r }),
    await KC({ pluginImport: t, targetRoot: r, version: n }));
}
async function GC({ pluginImport: e, targetRoot: t }) {
  await Promise.all(
    e.skillPaths.map(async (n) => {
      if (u.default.relative(e.pluginRoot, n).split(u.default.sep)[0] === FC)
        return;
      let r = u.default.dirname(n),
        i = u.default.join(t, FC, ZC(r));
      (await (0, m.rm)(i, { force: !0, recursive: !0 }),
        await (0, m.mkdir)(u.default.dirname(i), { recursive: !0 }),
        await (0, m.cp)(r, i, { recursive: !0 }));
    }),
  );
}
async function KC({ pluginImport: e, targetRoot: t, version: n }) {
  let r = await JC(e.manifestPath);
  (delete r.skills, delete r.mcpServers, delete r.mcp_servers);
  let i = u.default.join(t, `.codex-plugin`, `plugin.json`);
  (await (0, m.mkdir)(u.default.dirname(i), { recursive: !0 }),
    await (0, m.writeFile)(
      i,
      `${JSON.stringify({ ...r, name: e.pluginName, version: n }, null, 2)}\n`,
      `utf8`,
    ));
}
function qC(e) {
  return !IC.has(u.default.basename(e));
}
async function JC(e) {
  let t = await (0, m.readFile)(e, `utf8`).catch(() => `{}`);
  try {
    let e = NC.safeParse(JSON.parse(t));
    return e.success ? e.data : {};
  } catch {
    return {};
  }
}
async function YC({ codexHome: e, pluginImports: t }) {
  if (t.length === 0) return;
  let n = u.default.join(e, `config.toml`),
    r = await DC(n, []);
  if (r == null) throw Error(`Could not read Codex plugin config`);
  let i = XC(r, `plugins`),
    a = !1;
  for (let e of t) {
    let t = `${e.pluginName}@${uC}`,
      n = i[t];
    (AC(n) && n.enabled === !0) ||
      ((i[t] = { ...(AC(n) ? n : {}), enabled: !0 }), (a = !0));
  }
  a && (await OC(n, r));
}
function XC(e, t) {
  let n = e[t];
  if (AC(n)) return n;
  let r = {};
  return ((e[t] = r), r);
}
function ZC(e) {
  return (
    u.default
      .basename(e)
      .replace(/[^A-Za-z0-9_-]+/g, `-`)
      .replace(/^-+|-+$/g, ``) || `cowork-skill`
  );
}
function QC(e, t) {
  let n = yee(e, t);
  return LC.has(n) ? null : (LC.add(n), n);
}
function yee(e, t) {
  return [e, t.projectRoot, t.sourceId, t.contentSha256].join(``);
}
function bee(e) {
  for (let t of e) LC.delete(t);
}
function $C(e, t) {
  return e.pathForCodexHost?.(t) ?? t;
}
async function ew(e, t) {
  return tw(e.map((e) => u.default.join(e, t)));
}
async function tw(e) {
  let t = await Promise.all(e.map((e) => LS(e)));
  return e.filter((e, n) => t[n]);
}
async function nw(e, t) {
  let n = {},
    r = await tw(
      [`cowork_account_settings.json`, `cowork_settings.json`].flatMap((t) =>
        [...e.accountOrgDirs].reverse().map((e) => u.default.join(e, t)),
      ),
    ),
    i = await Promise.all(
      r.map(async (e) => ({ parsed: await jS(e, wC), sourcePath: e })),
    );
  for (let { parsed: e, sourcePath: r } of i) {
    if (e == null) {
      t.push(`Could not read Cowork settings file: ${r}`);
      continue;
    }
    Object.assign(n, e);
  }
  return n;
}
async function xee(e, t, n) {
  let r = iw(n ?? (await nw(e, t))),
    i = new Map(),
    a = new Set(),
    o = await rw(e);
  for (let { parsed: e, sourcePath: n } of o) {
    if (e == null) {
      t.push(`Could not read Cowork MCP config file: ${n}`);
      continue;
    }
    for (let [o, s] of Object.entries({ ...e.mcpServers, ...e.mcp_servers })) {
      let e = o.trim();
      if (e === ``) {
        t.push(`Cowork MCP config ${n} had an empty server name`);
        continue;
      }
      a.has(e) ||
        (a.add(e),
        !aw({ disabledServerNames: r, serverConfig: s, serverName: e }) &&
          i.set(e, { config: s, sourcePath: n }));
    }
  }
  return i;
}
async function rw(e) {
  let t = await tw(
    e.userDataDirs.flatMap((t) => {
      let n = e.accountOrgDirs.find((e) => SS(e, t));
      return [
        ...(n == null
          ? []
          : [
              u.default.join(n, `mcp.json`),
              u.default.join(n, `mcp-servers.json`),
            ]),
        u.default.join(t, `claude_desktop_config.json`),
      ];
    }),
  );
  return Promise.all(
    t.map(async (e) => ({ parsed: await jS(e, CC), sourcePath: e })),
  );
}
function iw(e) {
  let t = e.disabledMcpjsonServers ?? e.disabledMcpServers;
  return Array.isArray(t)
    ? new Set(
        t
          .filter((e) => typeof e == `string`)
          .map((e) => e.trim())
          .filter((e) => e !== ``),
      )
    : new Set();
}
function aw({ disabledServerNames: e, serverConfig: t, serverName: n }) {
  return e.has(n) || t.disabled === !0 || t.enabled === !1;
}
var See = new Set([
    `bash`,
    `bun`,
    `deno`,
    `node`,
    `perl`,
    `php`,
    `python`,
    `python3`,
    `ruby`,
    `sh`,
    `tsx`,
    `zsh`,
  ]),
  Cee = /^\$\{([A-Za-z_][A-Za-z0-9_]*)\}$/,
  wee = /^Bearer\s+\$\{([A-Za-z_][A-Za-z0-9_]*)\}$/;
async function Tee({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await nw(n.project, n.warnings),
    i = await xee(n.project, n.warnings, r),
    a = Eee(r);
  if (i.size === 0 && Object.keys(a).length === 0) return [];
  await Oee({ servers: i, warnings: n.warnings });
  let o = u.default.join(e, `config.toml`),
    s = await DC(o, n.warnings);
  if (s == null) return [];
  let c = !1;
  for (let [e, t] of Object.entries(a)) s[e] ?? ((s[e] = t), (c = !0));
  if (i.size > 0) {
    let e = zee(s, `mcp_servers`);
    for (let [t, r] of [...i.entries()].sort(([e], [t]) =>
      e.localeCompare(t),
    )) {
      if (e[t] != null) continue;
      let i = Dee({ serverEntry: r, serverName: t, warnings: n.warnings });
      i != null && ((e[t] = i), (c = !0));
    }
  }
  return c ? (await OC(o, s), t.push(o), [o]) : [];
}
function Eee(e) {
  let t = {},
    n = jC(e.model);
  n != null && (t.model = Iee(n));
  let r = Lee(jC(e.permissionMode));
  return (r != null && (t.sandbox_mode = r), t);
}
function Dee({ serverEntry: e, serverName: t, warnings: n }) {
  let r = kee(e),
    i = { enabled: !0 },
    a = r.command?.trim(),
    o = r.url?.trim(),
    s = r.type,
    c = [],
    l;
  if (a != null && a !== ``) {
    if (s != null && s !== `stdio`)
      return (
        n.push(
          `Cowork MCP server ${t} skipped because command transport is incompatible with type ${s}`,
        ),
        null
      );
    ((i.command = a),
      r.args != null && r.args.length > 0 && (i.args = r.args),
      Pee(i, r.env),
      r.headers != null &&
        Object.keys(r.headers).length > 0 &&
        c.push(`headers`),
      o != null && o !== `` && c.push(`url`),
      (l = `command`));
  } else if (o != null && o !== ``) {
    if (
      s != null &&
      s !== `http` &&
      s !== `streamable_http` &&
      s !== `streamable-http`
    )
      return (
        n.push(
          `Cowork MCP server ${t} skipped because HTTP transport is incompatible with type ${s}`,
        ),
        null
      );
    ((i.url = o),
      Nee(i, r.headers),
      r.args != null && r.args.length > 0 && c.push(`args`),
      r.env != null && Object.keys(r.env).length > 0 && c.push(`env`),
      (l = `HTTP`));
  } else
    return (
      n.push(`Cowork MCP server ${t} skipped because it has no command or URL`),
      null
    );
  c.length > 0 &&
    n.push(
      `Cowork MCP server ${t} ignored fields incompatible with ${l} transport: ${c.join(`, `)}`,
    );
  let u = Fee(r);
  return (
    u.length > 0 &&
      n.push(`Cowork MCP server ${t} has unsupported fields: ${u.join(`, `)}`),
    i
  );
}
async function Oee({ servers: e, warnings: t }) {
  let n = await Promise.all(
    [...e.entries()].map(async ([e, t]) => {
      let n = Aee(t);
      return n == null || (await FS(n))
        ? null
        : { executablePath: n, serverName: e };
    }),
  );
  for (let e of n) {
    if (e == null) continue;
    let n = `Cowork MCP server ${e.serverName} config migrated, but executable not found: ${e.executablePath}`;
    t.includes(n) || t.push(n);
  }
}
function kee(e) {
  let { config: t } = e,
    n = t.command?.trim();
  if (n == null || n === ``) return t;
  let r = u.default.dirname(e.sourcePath),
    i = ow(r, n),
    a = jee(t.args, r);
  return i == null
    ? !cw(n) || a === t.args
      ? t
      : { ...t, args: a }
    : a === t.args
      ? { ...t, command: i }
      : { ...t, args: a, command: i };
}
function Aee(e) {
  let { config: t } = e,
    n = t.command?.trim();
  if (n == null || n === ``) return null;
  let r = u.default.dirname(e.sourcePath),
    i = ow(r, n);
  if (i != null) return i;
  if (!cw(n)) return null;
  let a = t.args?.find(sw) ?? null;
  return a == null ? null : ow(r, a);
}
function jee(e, t) {
  if (e == null) return;
  let n = !1,
    r = e.map((e) => {
      let r = ow(t, e);
      return r == null ? e : ((n = !0), r);
    });
  return n ? r : e;
}
function ow(e, t) {
  if (!Mee(t)) return null;
  let n = t.replaceAll(`\\`, u.default.sep);
  return u.default.win32.isAbsolute(t) && !u.default.isAbsolute(t)
    ? null
    : u.default.isAbsolute(n)
      ? n
      : u.default.resolve(e, n);
}
function Mee(e) {
  return u.default.isAbsolute(e) || u.default.win32.isAbsolute(e) || sw(e);
}
function sw(e) {
  return (
    e.startsWith(`./`) ||
    e.startsWith(`../`) ||
    e.startsWith(`.\\`) ||
    e.startsWith(`..\\`)
  );
}
function cw(e) {
  let t = u.default
    .basename(e)
    .replace(/\.(?:bat|cmd|exe)$/i, ``)
    .toLowerCase();
  return See.has(t);
}
function Nee(e, t) {
  if (t == null) return;
  let n = {},
    r = {};
  for (let [i, a] of Object.entries(t)) {
    let t = String(a),
      o = Ree(t);
    if (i.toLowerCase() === `authorization` && o != null) {
      e.bearer_token_env_var = o;
      continue;
    }
    let s = lw(t);
    if (s != null) {
      r[i] = s;
      continue;
    }
    n[i] = t;
  }
  (Object.keys(n).length > 0 && (e.http_headers = n),
    Object.keys(r).length > 0 && (e.env_http_headers = r));
}
function Pee(e, t) {
  if (t == null) return;
  let n = {},
    r = [];
  for (let [e, i] of Object.entries(t)) {
    let t = String(i);
    if (lw(t) === e) {
      r.push(e);
      continue;
    }
    n[e] = t;
  }
  (r.length > 0 && (e.env_vars = r), Object.keys(n).length > 0 && (e.env = n));
}
function Fee(e) {
  let t = new Set([
    `args`,
    `command`,
    `disabled`,
    `enabled`,
    `env`,
    `headers`,
    `name`,
    `scope`,
    `type`,
    `url`,
  ]);
  return Object.keys(e)
    .filter((e) => !t.has(e))
    .sort();
}
function Iee(e) {
  return e.startsWith(`claude-opus`)
    ? `gpt-5.4`
    : e.startsWith(`claude-sonnet`) || e.startsWith(`claude-haiku`)
      ? `gpt-5.4-mini`
      : e;
}
function Lee(e) {
  return e === `acceptEdits`
    ? `workspace-write`
    : e === `readOnly`
      ? `read-only`
      : null;
}
function lw(e) {
  return Cee.exec(e)?.[1] ?? null;
}
function Ree(e) {
  return wee.exec(e)?.[1] ?? null;
}
function zee(e, t) {
  if (AC(e[t])) return e[t];
  let n = {};
  return ((e[t] = n), n);
}
var Bee = `## Claude Cowork Migration`;
async function Vee({ projectImport: e }) {
  let t = u.default.join(e.project.sourceRoot, `AGENTS.md`),
    n = await MS(t);
  if (uw(n, `## Imported Claude Cowork project instructions`) || uw(n, Bee))
    return t;
  let r = e.project.instructions,
    i = r == null || r.trim() === `` ? null : r.trim(),
    a =
      i == null
        ? lC
        : [lC, ``, i].join(`
`),
    o = `

`;
  return (
    n == null ||
    n === `` ||
    n.endsWith(`

`)
      ? (o = ``)
      : n.endsWith(`
`) &&
        (o = `
`),
    await (0, m.writeFile)(t, `${n ?? ``}${o}${a}\n`, `utf8`),
    t
  );
}
function uw(e, t) {
  return (
    e
      ?.split(
        `
`,
      )
      .some((e) => e.trim() === t) === !0
  );
}
function dw(e) {
  return (
    e
      .trim()
      .toLowerCase()
      .replace(/[^a-z0-9_-]+/g, `-`)
      .replace(/^-+|-+$/g, ``) || `cowork-item`
  );
}
var Hee = n
    .xl({ name: n.wl().optional(), version: n.wl().optional() })
    .passthrough(),
  Uee = [
    u.default.join(`.codex-plugin`, `plugin.json`),
    u.default.join(`.claude-plugin`, `plugin.json`),
  ];
async function fw(e) {
  let t = (
      await Promise.all(
        Kee(e).map(async ({ root: e, userDataDir: t }) =>
          (await Gee(e)).map((e) => ({ skillPath: e, userDataDir: t })),
        ),
      )
    ).flat(),
    n = new Map(),
    r = [],
    i = await Promise.all(
      t.map(async ({ skillPath: e, userDataDir: t }) => {
        let n = await qee(e);
        return {
          manifest: n == null ? null : await jS(n, Hee),
          manifestPath: n,
          skillPath: e,
          userDataDir: t,
        };
      }),
    );
  for (let {
    manifest: t,
    manifestPath: a,
    skillPath: o,
    userDataDir: s,
  } of i) {
    if (a == null) {
      let t = u.default.dirname(o);
      r.push({
        accountOrgDir: e.accountOrgDir,
        name: u.default.basename(t),
        skillPath: o,
        skillRoot: t,
      });
      continue;
    }
    let i = u.default.dirname(u.default.dirname(a)),
      c = u.default.relative(s, i),
      l = process.platform === `win32` ? c.toLowerCase() : c,
      d = n.get(l);
    if (d != null) {
      let e = dw(u.default.basename(u.default.dirname(o)));
      d.skillPaths.some(
        (t) => dw(u.default.basename(u.default.dirname(t))) === e,
      ) || d.skillPaths.push(o);
      continue;
    }
    let f = Xee({ manifestName: t?.name, pluginRoot: i });
    n.set(l, {
      accountOrgDir: e.accountOrgDir,
      manifestPath: a,
      pluginName: f,
      pluginRoot: i,
      skillPaths: [o],
      version: Zee(t?.version),
    });
  }
  return { looseSkills: ete(r), pluginBundles: hw([...n.values()]) };
}
async function pw(e) {
  return (await fw(e)).pluginBundles;
}
function Wee(e) {
  return mw(e.accountOrgDir, e.pluginName);
}
function mw(e, t) {
  return [e, t].join(``);
}
function hw(e) {
  return [
    ...new Map(
      e.map((e) => [
        [e.accountOrgDir, e.pluginName, e.pluginRoot].join(``),
        {
          ...e,
          skillPaths: [...new Set(e.skillPaths)].sort((e, t) =>
            e.localeCompare(t),
          ),
        },
      ]),
    ).values(),
  ].sort((e, t) =>
    `${e.accountOrgDir}\u001f${e.pluginName}`.localeCompare(
      `${t.accountOrgDir}\u001f${t.pluginName}`,
    ),
  );
}
async function Gee(e) {
  return (await PS(e))
    .filter((e) => u.default.basename(e) === `SKILL.md`)
    .sort((e, t) => e.localeCompare(t));
}
function Kee(e) {
  let t = u.default.basename(e.accountOrgDir),
    n = u.default.basename(u.default.dirname(e.accountOrgDir));
  return e.userDataDirs.flatMap((r) => {
    let i = u.default.join(
      r,
      u.default.relative(e.userDataDir, e.accountOrgDir),
    );
    return [
      { root: u.default.join(i, `rpm`), userDataDir: r },
      { root: u.default.join(i, `skills`), userDataDir: r },
      { root: u.default.join(i, `cowork_plugins`), userDataDir: r },
      { root: u.default.join(i, `.local-plugins`), userDataDir: r },
      { root: u.default.join(i, `.remote-plugins`), userDataDir: r },
      {
        root: u.default.join(
          r,
          `local-agent-mode-sessions`,
          `skills-plugin`,
          t,
          n,
        ),
        userDataDir: r,
      },
    ];
  });
}
async function qee(e) {
  return (
    (await Promise.all(Jee(u.default.dirname(e)).map((e) => Yee(e)))).find(
      (e) => e != null,
    ) ?? null
  );
}
function Jee(e) {
  let t = [];
  for (let n = e; ; n = u.default.dirname(n))
    if ((t.push(n), u.default.dirname(n) === n)) return t;
}
async function Yee(e) {
  return (
    (
      await Promise.all(
        Uee.map(async (t) => {
          let n = u.default.join(e, t);
          return (await FS(n)) ? n : null;
        }),
      )
    ).find((e) => e != null) ?? null
  );
}
function Xee({ manifestName: e, pluginRoot: t }) {
  let n = e?.trim();
  return Qee(
    n == null || n === `` ? u.default.basename(t) : n,
    `cowork-plugin`,
  );
}
function Zee(e) {
  let t = e?.trim();
  return t == null || t === `` ? null : $ee(t);
}
function Qee(e, t) {
  return e.replace(/[^A-Za-z0-9_-]+/g, `-`).replace(/^-+|-+$/g, ``) || t;
}
function $ee(e) {
  return (
    e.replace(/[^A-Za-z0-9.+_-]+/g, `-`).replace(/^[.+-]+|[.+-]+$/g, ``) ||
    `local`
  );
}
function ete(e) {
  let t = new Map();
  for (let n of e) {
    let e = [n.accountOrgDir, dw(n.name)].join(``);
    t.has(e) || t.set(e, n);
  }
  return [...t.values()].sort((e, t) =>
    `${e.accountOrgDir}\u001f${e.name}`.localeCompare(
      `${t.accountOrgDir}\u001f${t.name}`,
    ),
  );
}
async function tte({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await fw(n.project),
    i = await _w(n.project.accountOrgDirs, `skills`),
    a = await gw(n.project.accountOrgDirs, i.length, `skills`),
    o = new Map();
  for (let t of i) {
    let n = u.default.basename(t, u.default.extname(t)),
      r = a.get(n) ?? a.get(`fallback`) ?? n,
      i = u.default.join(e, `skills`, dw(r), `SKILL.md`);
    o.has(i) || o.set(i, { skillName: r, sourcePath: t });
  }
  let s = await Promise.all(
      [...o].map(async ([e, { skillName: n, sourcePath: r }]) =>
        (await FS(e))
          ? null
          : (await NS(
              e,
              rte({ content: await (0, m.readFile)(r, `utf8`), name: n }),
            ),
            t.push(e),
            e),
      ),
    ).then((e) => e.filter((e) => e != null)),
    c = await Promise.all(
      r.looseSkills.map(async (n) => {
        let r = u.default.join(e, `skills`, dw(n.name)),
          i = u.default.join(r, `SKILL.md`);
        return (await FS(i))
          ? null
          : (await (0, m.cp)(n.skillRoot, r, { recursive: !0 }), t.push(i), i);
      }),
    ).then((e) => e.filter((e) => e != null));
  return [...s, ...c];
}
async function nte({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await _w(n.project.accountOrgDirs, `agents`);
  if (r.length === 0) return [];
  let i = await gw(n.project.accountOrgDirs, r.length, `agents`),
    a = new Map();
  for (let t of r) {
    let n = u.default.basename(t, u.default.extname(t)),
      r = i.get(n) ?? i.get(`fallback`) ?? ate(n),
      o = u.default.join(e, `agents`, `${dw(r)}.toml`);
    a.has(o) || a.set(o, { agentName: r, sourcePath: t });
  }
  return Promise.all(
    [...a].map(async ([e, { agentName: n, sourcePath: r }]) => {
      if (await FS(e)) return null;
      let i = await (0, m.readFile)(r, `utf8`);
      return (
        await OC(e, {
          name: n,
          description: `Migrated Claude Cowork agent ${n}.`,
          developer_instructions: i.trim(),
        }),
        t.push(e),
        e
      );
    }),
  ).then((e) => e.filter((e) => e != null));
}
function rte({ content: e, name: t }) {
  return ite(e)
    ? e
    : [
        `---`,
        `name: ${JSON.stringify(t)}`,
        `description: ${JSON.stringify(`Migrated Claude Cowork skill ${t}.`)}`,
        `---`,
        ``,
        e.trim(),
        ``,
      ].join(`
`);
}
function ite(e) {
  let t = /^---\n([\s\S]*?)\n---\n?/.exec(e);
  return t == null
    ? !1
    : /\bname\s*:/.test(t[1]) && /\bdescription\s*:/.test(t[1]);
}
async function gw(e, t, n) {
  let r = (
      await Promise.all(e.map((e) => jS(u.default.join(e, `${n}.json`), EC)))
    ).flatMap((e) => (e == null ? [] : Array.isArray(e) ? e : (e[n] ?? []))),
    i = new Map();
  for (let e of r) {
    let t = e.name ?? e.id;
    if (t == null) continue;
    let n =
      e.path == null
        ? dw(t)
        : u.default.basename(e.path, u.default.extname(e.path));
    i.has(n) || i.set(n, t);
  }
  if (r.length === 1 && t === 1) {
    let [e] = r,
      t = e?.name ?? e?.id;
    t != null && i.set(`fallback`, t);
  }
  return i;
}
async function _w(e, t) {
  let n = await Promise.all(
      e.map(async (e) => ({
        entries: await (0, m.readdir)(u.default.join(e, t), {
          withFileTypes: !0,
        }).catch(() => []),
        root: e,
      })),
    ),
    r = new Map();
  for (let { entries: e, root: i } of n)
    for (let n of e.sort((e, t) => e.name.localeCompare(t.name))) {
      let e = process.platform === `win32` ? n.name.toLowerCase() : n.name;
      n.isFile() &&
        n.name.endsWith(`.md`) &&
        !r.has(e) &&
        r.set(e, u.default.join(i, t, n.name));
    }
  return [...r.values()];
}
function ate(e) {
  let t = e
    .split(/[-_]+/g)
    .filter((e) => e !== ``)
    .map((e) => `${e.slice(0, 1).toUpperCase()}${e.slice(1)}`)
    .join(` `);
  return t === `` ? `Cowork Agent` : t;
}
async function vw({ codexHome: e, projects: t }) {
  let n = [];
  for (let r of t) {
    let t = await yw({ codexHome: e, projectImport: r });
    n.push(t);
  }
  return { projects: n };
}
async function yw({ codexHome: e, projectImport: t }) {
  let n = [],
    r = [],
    i = null;
  return (
    t.accountSetupSelected &&
      r.push(
        ...(await Tee({ codexHome: e, copiedFiles: n, projectImport: t })),
        ...(await tte({ codexHome: e, copiedFiles: n, projectImport: t })),
        ...(await nte({ codexHome: e, copiedFiles: n, projectImport: t })),
      ),
    t.itemTypes.has(`AGENTS_MD`) && (i = await Vee({ projectImport: t })),
    {
      agentsPath: i,
      copiedFiles: n,
      liveConfigPaths: r,
      projectImport: t,
      transcriptCandidates: t.itemTypes.has(`SESSIONS`)
        ? t.transcriptCandidates
        : [],
    }
  );
}
var bw = `claude-cowork`;
function xw(e) {
  return [
    ...new Set(
      e.flatMap((e) => [
        ...(e.cwd == null || e.cwd === `` ? [] : [e.cwd]),
        ...(e.details?.sessions ?? [])
          .filter((e) => e.workspaceKind !== `projectless`)
          .map((e) => e.cwd),
      ]),
    ),
  ].sort((e, t) => e.localeCompare(t));
}
function Sw(e, t) {
  return {
    ...e,
    cwd: e.cwd == null || e.cwd === `` ? e.cwd : t(e.cwd),
    sourcePath: e.sourcePath == null ? void 0 : t(e.sourcePath),
    details:
      e.details == null
        ? null
        : {
            ...e.details,
            sessions: e.details.sessions.map((e) => ({
              ...e,
              cwd: t(e.cwd),
              path: t(e.path),
            })),
          },
  };
}
function Cw({ projectlessSpaces: e = [], projects: t }) {
  let n = ww(t),
    r = Tw(t),
    i = t.flatMap((e) =>
      e.itemTypes.has(`AGENTS_MD`)
        ? [
            Dw({
              cwd: e.project.sourceRoot,
              description: `Append imported Cowork project instructions for ${e.project.name}`,
              itemType: `AGENTS_MD`,
            }),
          ]
        : [],
    ),
    a = [
      ...t.flatMap((e) =>
        e.transcriptCandidates.map((e) => ({
          cwd: e.projectRoot,
          path: e.sourceJsonlPath,
          title: e.title,
          ...(e.workspaceKind === `projectless`
            ? { workspaceKind: e.workspaceKind }
            : {}),
        })),
      ),
      ...e.flatMap((e) =>
        e.transcriptCandidates.map((e) => ({
          cwd: e.projectRoot,
          path: e.sourceJsonlPath,
          title: e.title,
          ...(e.workspaceKind === `projectless`
            ? { workspaceKind: e.workspaceKind }
            : {}),
        })),
      ),
    ],
    o =
      a.length === 1
        ? `Import 1 Cowork JSONL transcript`
        : `Import ${a.length} Cowork JSONL transcripts`;
  return [
    ...n,
    ...r,
    ...i,
    ...(a.length === 0
      ? []
      : [
          Dw({
            cwd: null,
            description: o,
            details: {
              commands: [],
              hooks: [],
              mcpServers: [],
              plugins: [],
              sessions: a,
              subagents: [],
            },
            itemType: `SESSIONS`,
          }),
        ]),
  ];
}
function ww(e) {
  let t = new Set();
  for (let n of e)
    !n.accountSetupSelected ||
      n.configSurfaces.length === 0 ||
      t.add(n.project.accountOrgDir);
  return [...t]
    .sort((e, t) => e.localeCompare(t))
    .map((e) =>
      Dw({
        cwd: null,
        description: `Migrate supported Cowork account/org setup`,
        itemType: `CONFIG`,
        sourcePath: e,
      }),
    );
}
function Tw(e) {
  let t = new Map();
  for (let n of e) {
    if (!n.pluginSetupSelected || n.pluginCandidates.length === 0) continue;
    let e = n.project.accountOrgDir,
      r = t.get(e) ?? [];
    t.set(e, [...r, ...n.pluginCandidates]);
  }
  return [...t.entries()]
    .map(([e, t]) => {
      let n = new Set();
      for (let e of t) n.add(e.pluginName);
      return Dw({
        cwd: null,
        description: `Install supported Cowork plugins`,
        details: {
          commands: [],
          hooks: [],
          mcpServers: [],
          plugins: [...n]
            .sort((e, t) => e.localeCompare(t))
            .map((e) => ({ name: e })),
          sessions: [],
          subagents: [],
        },
        itemType: `PLUGINS`,
        sourcePath: e,
      });
    })
    .sort((e, t) => (e.sourcePath ?? ``).localeCompare(t.sourcePath ?? ``));
}
function Ew({ skippedProjects: e, workspaceRoots: t }) {
  return t == null
    ? e
    : e.filter((e) => e.sourceFolders.some((e) => t.some((t) => xS(t, e))));
}
function Dw({
  cwd: e,
  description: t,
  details: n = null,
  itemType: r,
  sourcePath: i,
}) {
  return {
    cwd: e,
    description: t,
    details: n,
    itemType: r,
    providerId: bw,
    sourcePath: i,
  };
}
async function Ow(e) {
  let t = [],
    n = Aw(e),
    r = (
      await Promise.all(
        (await kw(n)).map((r) =>
          Pw({ locations: r, options: e, userDataDirs: n, warnings: t }),
        ),
      )
    ).flat(),
    i = r.map((e) => e.project).filter((e) => e != null),
    a = r.map((e) => e.projectlessSpace).filter((e) => e != null),
    o = r.map((e) => e.skippedProject).filter((e) => e != null);
  return (
    i.length === 0 &&
      a.length === 0 &&
      o.length === 0 &&
      t.length === 0 &&
      t.push(`No Claude Cowork spaces were found`),
    { projectlessSpaces: a, projects: i, skippedProjects: o, warnings: t }
  );
}
async function kw(e) {
  let t = new Map(),
    n = await Promise.all(
      e.map(async (e) => {
        let t = u.default.join(e, `local-agent-mode-sessions`),
          n = await Mw(t);
        return (
          await Promise.all(
            n.map(async (n) => {
              let r = u.default.join(t, n);
              return (await Mw(r)).map((t) => ({
                accountOrgDir: u.default.join(r, t),
                accountOrgKey:
                  process.platform === `win32`
                    ? `${n}\u001f${t}`.toLowerCase()
                    : `${n}\u001f${t}`,
                userDataDir: e,
              }));
            }),
          )
        ).flat();
      }),
    );
  for (let { accountOrgDir: e, accountOrgKey: r, userDataDir: i } of n.flat()) {
    let n = t.get(r) ?? [];
    (n.push({ accountOrgDir: e, userDataDir: i }), t.set(r, n));
  }
  return [...t.values()];
}
function Aw(e) {
  let t = [e.userDataDir, ...(e.fallbackUserDataDirs ?? [])];
  return t.filter((e, n) => t.findIndex((t) => xS(t, e)) === n);
}
function jw(e) {
  return e == null
    ? []
    : e.map((e) => (typeof e == `string` ? e : e.path)).filter((e) => e !== ``);
}
async function Mw(e) {
  return (await IS(e))
    ? (await (0, m.readdir)(e, { withFileTypes: !0 }))
        .filter((e) => e.isDirectory())
        .map((e) => e.name)
        .sort((e, t) => e.localeCompare(t))
    : [];
}
function Nw(e, t) {
  let n = u.default.join(t.documentsDir, `Claude`, `Artifacts`);
  return Aw(t).some((t) => SS(e, t)) || SS(e, n);
}
async function Pw({ locations: e, options: t, userDataDirs: n, warnings: r }) {
  let i = Fw(e, t),
    a = e.map((e) => e.accountOrgDir),
    o = await ew(a, `spaces.json`),
    s = await Promise.all(
      o.map(async (e) => ({ parsed: await jS(e, _C), sourcePath: e })),
    ),
    c = new Map();
  for (let e of s) {
    if (e.parsed == null) {
      r.push(`Could not read Claude Cowork spaces file: ${e.sourcePath}`);
      continue;
    }
    let t = Array.isArray(e.parsed) ? e.parsed : e.parsed.spaces;
    for (let n of t)
      c.has(n.id) || c.set(n.id, { sourcePath: e.sourcePath, space: n });
  }
  let l =
      s.find((e) => e.parsed != null)?.sourcePath ??
      o[0] ??
      u.default.join(e[0].accountOrgDir, `spaces.json`),
    d = (await Iw(a))
      ? Rw({
          accountOrgDirs: a,
          identityLocation: i,
          options: t,
          spaceConfigPath: l,
        })
      : null;
  if (c.size === 0)
    return d == null
      ? []
      : [{ project: null, projectlessSpace: d, skippedProject: null }];
  let f = await Promise.all(
    [...c.values()].map(async ({ sourcePath: e, space: r }) =>
      zw({
        accountOrgDirs: a,
        identityLocation: i,
        options: t,
        space: r,
        spaceConfigPath: e,
        userDataDirs: n,
      }),
    ),
  );
  return d == null
    ? f
    : [...f, { project: null, projectlessSpace: d, skippedProject: null }];
}
function Fw(e, t) {
  let n =
      t.fallbackUserDataDirs?.find((e) => !xS(e, t.userDataDir)) ??
      t.userDataDir,
    r = e.find((e) => xS(e.userDataDir, n)) ?? e[0];
  return {
    accountOrgDir: u.default.join(
      n,
      u.default.relative(r.userDataDir, r.accountOrgDir),
    ),
    userDataDir: n,
  };
}
async function Iw(e) {
  return (
    await Promise.all(e.flatMap((e) => [Lw(e), Lw(u.default.join(e, `agent`))]))
  ).some((e) => e.length > 0);
}
async function Lw(e) {
  return (await IS(e))
    ? (await (0, m.readdir)(e, { withFileTypes: !0 }))
        .filter(
          (e) => e.isFile() && e.name.endsWith(`.json`) && !pC.has(e.name),
        )
        .map((e) => e.name)
    : [];
}
function Rw({
  accountOrgDirs: e,
  identityLocation: t,
  options: n,
  spaceConfigPath: r,
}) {
  return {
    accountOrgDir: t.accountOrgDir,
    accountOrgDirs: e,
    documentsDir: n.documentsDir,
    homeDir: n.homeDir ?? null,
    name: `Cowork recents`,
    projectlessImportRoot: u.default.join(
      t.accountOrgDir,
      `projectless-spaces`,
      `account`,
    ),
    spaceConfigPath: r,
    spaceId: null,
    userDataDir: t.userDataDir,
  };
}
async function zw({
  accountOrgDirs: e,
  identityLocation: t,
  options: n,
  space: r,
  spaceConfigPath: i,
  userDataDirs: a,
}) {
  let o = r.name ?? r.title ?? r.id,
    s = jw(r.folders ?? r.userSelectedFolders),
    c = s.map((e) => u.default.resolve(e));
  if (s.length === 0)
    return {
      project: null,
      projectlessSpace: {
        accountOrgDir: t.accountOrgDir,
        accountOrgDirs: e,
        documentsDir: n.documentsDir,
        homeDir: n.homeDir ?? null,
        name: o,
        projectlessImportRoot: u.default.join(
          t.accountOrgDir,
          `projectless-spaces`,
          r.id,
        ),
        spaceConfigPath: i,
        spaceId: r.id,
        userDataDir: t.userDataDir,
      },
      skippedProject: null,
    };
  if (s.length !== 1)
    return {
      project: null,
      projectlessSpace: null,
      skippedProject: {
        name: o,
        reason: `multiple-source-folders`,
        spaceConfigPath: i,
        sourceFolders: c,
        spaceId: r.id,
      },
    };
  let l = c[0];
  return (await IS(l))
    ? Nw(l, n)
      ? {
          project: null,
          projectlessSpace: null,
          skippedProject: {
            name: o,
            reason: `claude-managed-folder`,
            spaceConfigPath: i,
            sourceFolders: [l],
            spaceId: r.id,
          },
        }
      : {
          project: {
            accountOrgDir: t.accountOrgDir,
            accountOrgDirs: e,
            documentsDir: n.documentsDir,
            homeDir: n.homeDir ?? null,
            instructions: r.instructions ?? null,
            name: o,
            sourceRoot: l,
            spaceId: r.id,
            userDataDir: t.userDataDir,
            userDataDirs: a,
          },
          projectlessSpace: null,
          skippedProject: null,
        }
    : {
        project: null,
        projectlessSpace: null,
        skippedProject: {
          name: o,
          reason: `missing-source-folder`,
          spaceConfigPath: i,
          sourceFolders: [l],
          spaceId: r.id,
        },
      };
}
async function Bw({ items: e, options: t }) {
  let n = Vw(e);
  if (n.size === 0) return [];
  let r = await Ow(t);
  return hw(
    (await Promise.all(r.projects.map((e) => pw(e))))
      .flat()
      .filter((e) => n.has(Wee(e))),
  );
}
function Vw(e) {
  return new Set(
    e
      .filter((e) => e.itemType === `PLUGINS`)
      .flatMap((e) => {
        let t = e.sourcePath;
        return t == null || t === ``
          ? []
          : (e.details?.plugins ?? []).flatMap((e) => [mw(t, e.name)]);
      }),
  );
}
var Hw = [
    { kind: `plugins`, relativePath: `plugins.json` },
    { kind: `plugins`, relativePath: `plugins-mcp-skills.json` },
    { kind: `plugins`, relativePath: `.local-plugins` },
    { kind: `plugins`, relativePath: `.remote-plugins` },
    { kind: `skills`, relativePath: `skills.json` },
    { kind: `skills`, relativePath: `skills` },
    { kind: `agents`, relativePath: `agents.json` },
    { kind: `agents`, relativePath: `agents` },
    { kind: `settings`, relativePath: `cowork_account_settings.json` },
    { kind: `settings`, relativePath: `cowork_settings.json` },
  ],
  Uw = [
    { kind: `extensions`, relativePath: `Claude Extensions` },
    { kind: `extensions`, relativePath: `Claude Extensions Settings` },
    { kind: `extensions`, relativePath: `Extensions Update Cache` },
    { kind: `extensions`, relativePath: `extensions-blocklist.json` },
    { kind: `extensions`, relativePath: `extensions-installations.json` },
  ];
async function Ww(e) {
  let t = new Map(),
    n = (e, n) => {
      let r = t.get(e);
      if (r != null) return r;
      let i = Kw(n);
      return (t.set(e, i), i);
    },
    r = await Promise.all(
      e.projects.map(async (e) => {
        let t = [],
          r = await nw(e, t),
          [i, a, o, s] = await Promise.all([
            n(e.accountOrgDir, e.accountOrgDirs),
            Gw(e, t),
            Zw(e, t, r),
            pw(e),
          ]);
        return {
          configSurfaces: o,
          pluginCandidates: s,
          project: e,
          scheduledTasks: a,
          sessionManifests: i,
          warnings: t,
        };
      }),
    ),
    i = await Promise.all(
      e.projectlessSpaces.map(async (e) => ({
        projectlessSpace: e,
        sessionManifests: await n(e.accountOrgDir, e.accountOrgDirs),
      })),
    );
  return {
    configSurfacesByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.configSurfaces]),
    ),
    pluginCandidatesByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.pluginCandidates]),
    ),
    projectlessSpaces: e.projectlessSpaces,
    projects: e.projects,
    scheduledTasksByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.scheduledTasks]),
    ),
    sessionManifestsByProjectlessSpaceRoot: new Map(
      i.map((e) => [
        e.projectlessSpace.projectlessImportRoot,
        e.sessionManifests,
      ]),
    ),
    sessionManifestsByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.sessionManifests]),
    ),
    skippedProjects: e.skippedProjects,
    warnings: [...e.warnings, ...r.flatMap((e) => e.warnings)],
  };
}
async function Gw(e, t) {
  let n = await ew(e.accountOrgDirs, `scheduled-tasks.json`);
  if (n.length === 0) return [];
  let r = await Promise.all(
    n.map(async (e) => ({ parsed: await jS(e, xC), sourcePath: e })),
  );
  for (let e of r)
    e.parsed == null &&
      t != null &&
      t.push(
        `Could not read Claude Cowork scheduled tasks file: ${e.sourcePath}`,
      );
  let i = r.find((e) => e.parsed != null);
  if (i?.parsed == null) return [];
  let a = i.sourcePath,
    o = i.parsed;
  return (Array.isArray(o) ? o : o.tasks)
    .filter((t) => t.spaceId == null || t.spaceId === e.spaceId)
    .map((e, t) => ({
      id: e.id ?? `scheduled-task-${t + 1}`,
      name: e.name ?? e.title ?? null,
      sourcePath: a,
    }));
}
async function Kw(e) {
  let t = await Promise.all(
      e.map(async (e) => {
        let t = [
          ...(await $w(e)),
          ...(await $w(u.default.join(e, `agent`))),
        ].filter((e) => !pC.has(u.default.basename(e)));
        return (await Promise.all(t.map((e) => Xw(e)))).filter(
          (e) => e != null,
        );
      }),
    ),
    n = new Map();
  for (let e of t.flat()) {
    let t = n.get(e.sessionId);
    if (t == null) {
      n.set(e.sessionId, e);
      continue;
    }
    n.set(e.sessionId, qw(t, e));
  }
  return [...n.values()];
}
function qw(e, t) {
  return {
    ...e,
    cliSessionIds: Jw(e.cliSessionIds, t.cliSessionIds),
    cwd: e.cwd ?? t.cwd,
    detectedFiles: Yw(e.detectedFiles, t.detectedFiles),
    lastActivityAtMs: e.lastActivityAtMs ?? t.lastActivityAtMs,
    remoteMcpServerNames: Jw(e.remoteMcpServerNames, t.remoteMcpServerNames),
    scheduledTaskId: e.scheduledTaskId ?? t.scheduledTaskId,
    spaceId: e.spaceId ?? t.spaceId,
    storageDirs: [...e.storageDirs, ...t.storageDirs],
    title: e.title ?? t.title,
    userSelectedFolders: Jw(e.userSelectedFolders, t.userSelectedFolders),
  };
}
function Jw(e, t) {
  return [...new Set([...e, ...t])];
}
function Yw(e, t) {
  let n = new Map(e.map((e) => [e.hostPath, e]));
  for (let e of t) n.has(e.hostPath) || n.set(e.hostPath, e);
  return [...n.values()];
}
async function Xw(e) {
  let t = await jS(e, yC);
  if (t == null) return null;
  let n = t.sessionId ?? t.id ?? u.default.basename(e, `.json`);
  return {
    cliSessionIds: t.cliSessionId == null ? [] : [t.cliSessionId],
    cwd: t.cwd ?? null,
    detectedFiles: (t.fsDetectedFiles ?? []).map((e) => ({
      fileName: e.fileName,
      hostPath: e.hostPath,
      timestampMs: MC(e.timestamp ?? null),
    })),
    lastActivityAtMs: MC(
      t.lastActivityAt ?? t.updatedAt ?? t.createdAt ?? null,
    ),
    manifestPath: e,
    remoteMcpServerNames: kS(t.remoteMcpServersConfig),
    scheduledTaskId: t.scheduledTaskId ?? null,
    sessionId: n,
    spaceId: t.spaceId ?? null,
    storageDirs: [e.slice(0, -u.default.extname(e).length)],
    title: t.title ?? null,
    userSelectedFolders: jw(t.userSelectedFolders),
  };
}
async function Zw(e, t, n) {
  let r = await Qw({
      disabledServerNames: iw(n ?? (t == null ? {} : await nw(e, t))),
      project: e,
      warnings: t,
    }),
    i = (
      await Promise.all(
        [
          ...Hw.map((t) => ({ ...t, roots: e.accountOrgDirs })),
          ...Uw.map((t) => ({ ...t, roots: e.userDataDirs })),
        ].map(async ({ kind: e, relativePath: t, roots: n }) => {
          let [r] = await ew(n, t);
          return r == null ? null : { kind: e, path: r };
        }),
      )
    ).filter((e) => e != null);
  if (e.homeDir != null) {
    let t = u.default.join(e.homeDir, `.operon`);
    (await LS(t)) && i.push({ kind: `operon`, path: t });
  }
  return [...r, ...i];
}
async function Qw({ disabledServerNames: e, project: t, warnings: n }) {
  let r = await rw(t);
  return (
    await Promise.all(
      r.map(async ({ parsed: r, sourcePath: i }) =>
        r == null
          ? (n?.push(`Could not read Cowork MCP config file: ${i}`), null)
          : Object.entries({ ...r.mcpServers, ...r.mcp_servers }).some(
                ([t, n]) => {
                  let r = t.trim();
                  return (
                    r !== `` &&
                    !aw({
                      disabledServerNames: e,
                      serverConfig: n,
                      serverName: r,
                    })
                  );
                },
              )
            ? {
                kind: t.userDataDirs.some(
                  (e) => i === u.default.join(e, `claude_desktop_config.json`),
                )
                  ? `settings`
                  : `mcp`,
                path: i,
              }
            : null,
      ),
    )
  ).filter((e) => e != null);
}
async function $w(e) {
  return (await IS(e))
    ? (await (0, m.readdir)(e, { withFileTypes: !0 }))
        .filter((e) => e.isFile() && e.name.endsWith(`.json`))
        .map((t) => u.default.join(e, t.name))
    : [];
}
var eT = new Set([
    `csv`,
    `doc`,
    `docx`,
    `htm`,
    `html`,
    `jpeg`,
    `jpg`,
    `jsx`,
    `md`,
    `mdx`,
    `mermaid`,
    `pdf`,
    `png`,
    `ppt`,
    `pptx`,
    `svg`,
    `tsv`,
    `txt`,
    `webp`,
    `xls`,
    `xlsm`,
    `xlsx`,
  ]),
  tT = n
    .xl({
      createdBySessionId: n.wl().nullable().optional(),
      id: n.wl(),
      lastModifiedBySessionId: n.wl().nullable().optional(),
      name: n.wl().nullable().optional(),
    })
    .passthrough(),
  nT = n.Tl([n.pl(tT), n.xl({ artifacts: n.pl(tT) }).passthrough()]);
async function rT(e) {
  let [t, n] = await Promise.all([aT(e), cT(e)]);
  return [...t, ...n];
}
async function iT({ candidate: e, outputDirectory: t, sources: n }) {
  if (n.length === 0) return [];
  await (0, m.mkdir)(t, { recursive: !0 });
  let r = new Set();
  return (
    await Promise.all(
      n.map((n) =>
        n.kind === `output`
          ? lT({
              candidate: e,
              outputDirectory: t,
              source: n,
              usedRelativePaths: r,
            })
          : uT({ outputDirectory: t, source: n, usedRelativePaths: r }),
      ),
    )
  ).filter((e) => e != null);
}
async function aT(e) {
  let t = await dT(e.sessionStorageDirs),
    n = await oT(e);
  return hT(
    (
      await Promise.all(
        n.map(async (e) =>
          !sT(e.relativePath) ||
          (await fT(e.sourcePath, t)) ||
          !(await FS(e.sourcePath))
            ? null
            : {
                kind: `output`,
                label: u.default.basename(e.relativePath),
                relativePath: e.relativePath,
                sourcePath: e.sourcePath,
              },
        ),
      )
    ).filter((e) => e != null),
  );
}
async function oT(e) {
  let t = e.detectedFiles.flatMap((e) => {
      let t = _T(e.fileName, e.hostPath);
      return t == null ? [] : [{ relativePath: t, sourcePath: e.hostPath }];
    }),
    n = (
      await Promise.all(
        e.sessionStorageDirs.map(async (e) => {
          let t = u.default.join(e, `outputs`);
          return (await PS(t)).flatMap((e) => {
            let n = _T(u.default.relative(t, e), e);
            return n == null ? [] : [{ relativePath: n, sourcePath: e }];
          });
        }),
      )
    ).flat(),
    r = new Map();
  for (let e of n) {
    let t = gT(e.relativePath);
    r.has(t) || r.set(t, e);
  }
  return [...r.values(), ...t];
}
function sT(e) {
  let t = u.default.extname(e).slice(1).toLowerCase();
  return eT.has(t);
}
async function cT(e) {
  let t = u.default.join(e.documentsDir, `Claude`, `Artifacts`),
    n = new Map(),
    r = await Promise.all(
      e.accountOrgDirs.map((e) => jS(u.default.join(e, `artifacts.json`), nT)),
    );
  for (let e of r.flatMap((e) =>
    e == null ? [] : Array.isArray(e) ? e : e.artifacts,
  )) {
    let r = gT(u.default.join(t, e.id));
    n.has(r) || n.set(r, e);
  }
  return (
    await Promise.all(
      [...n.values()].map(async (n) => {
        if (
          n.createdBySessionId !== e.sourceId &&
          n.lastModifiedBySessionId !== e.sourceId
        )
          return null;
        let r = u.default.join(t, n.id);
        return n.id.trim() === `` || !SS(r, t) || !(await IS(r))
          ? null
          : {
              artifactId: n.id,
              kind: `artifact`,
              label: n.name ?? n.id,
              sourceDirectory: r,
            };
      }),
    )
  ).filter((e) => e != null);
}
async function lT({
  candidate: e,
  outputDirectory: t,
  source: n,
  usedRelativePaths: r,
}) {
  if (e.workspaceKind === `project` && SS(n.sourcePath, e.projectRoot))
    return { kind: `output`, label: n.label, targetPath: n.sourcePath };
  let i = yT(n.relativePath, r),
    a = u.default.join(t, i);
  return SS(a, t)
    ? (xS(n.sourcePath, a) ||
        (await (0, m.mkdir)(u.default.dirname(a), { recursive: !0 }),
        await (0, m.copyFile)(n.sourcePath, a)),
      { kind: `output`, label: n.label, targetPath: a })
    : null;
}
async function uT({ outputDirectory: e, source: t, usedRelativePaths: n }) {
  let r = yT(u.default.join(`artifacts`, vT(t.artifactId)), n),
    i = u.default.join(e, r);
  if (!SS(i, e)) return null;
  (await (0, m.mkdir)(u.default.dirname(i), { recursive: !0 }),
    await (0, m.cp)(t.sourceDirectory, i, { force: !0, recursive: !0 }));
  let a = u.default.join(i, `index.html`);
  return {
    kind: `artifact`,
    label: t.label,
    targetPath: (await FS(a)) ? a : i,
  };
}
async function dT(e) {
  let t = e.map((e) => u.default.join(e, `uploads`)),
    n = (await Promise.all(t.map((e) => PS(e)))).flat(),
    r = await Promise.all(
      n.map(async (e) => ({ fileKey: await pT(e), realPath: await mT(e) })),
    );
  return {
    fileKeys: new Set(r.flatMap(({ fileKey: e }) => e ?? [])),
    realPaths: new Set(r.flatMap(({ realPath: e }) => e ?? [])),
    roots: t,
  };
}
async function fT(e, t) {
  if (t.roots.some((t) => SS(e, t))) return !0;
  let [n, r] = await Promise.all([pT(e), mT(e)]);
  return (n != null && t.fileKeys.has(n)) || (r != null && t.realPaths.has(r));
}
async function pT(e) {
  try {
    let t = await (0, m.stat)(e);
    return `${t.dev}:${t.ino}`;
  } catch {
    return null;
  }
}
async function mT(e) {
  try {
    return await (0, m.realpath)(e);
  } catch {
    return null;
  }
}
function hT(e) {
  let t = new Set(),
    n = [];
  for (let r of e) {
    let e = gT(u.default.resolve(r.sourcePath));
    t.has(e) || (t.add(e), n.push(r));
  }
  return n;
}
function gT(e) {
  let t = u.default.normalize(e);
  return process.platform === `win32` ? t.toLowerCase() : t;
}
function _T(e, t) {
  let n = e.replaceAll(`\\`, `/`);
  if (n.startsWith(`/`) || /^[A-Za-z]:\//.test(n))
    return vT(u.default.basename(t));
  let r = n.split(`/`).filter((e) => e !== ``);
  return r.length === 0 || r.some((e) => e === `..`)
    ? null
    : u.default.join(...r.map(vT));
}
function vT(e) {
  let t = Array.from(e)
    .map((e) => (e.charCodeAt(0) <= 31 || `<>:"|?*`.includes(e) ? `-` : e))
    .join(``)
    .trim();
  return t === `` || t === `.` || t === `..` ? `file` : t;
}
function yT(e, t) {
  let n = u.default.normalize(e),
    r = u.default.extname(n),
    i = n.slice(0, n.length - r.length),
    a = n,
    o = 2;
  for (; t.has(gT(a)); ) ((a = `${i}-${o}${r}`), (o += 1));
  return (t.add(gT(a)), a);
}
function bT(e) {
  let t = xT(e);
  if (t.length === 0) return null;
  let n = t.filter((e) => e.kind === `output`),
    r = t.filter((e) => e.kind === `artifact`);
  return [
    ...(n.length === 0
      ? []
      : [`Imported Cowork generated files:`, ...n.map((e) => ST(e))]),
    ...(r.length === 0
      ? []
      : [
          ...(n.length === 0 ? [] : [``]),
          `Imported Cowork live artifacts:`,
          ...r.map((e) => ST(e)),
        ]),
  ].join(`
`);
}
function xT(e) {
  let t = new Set(),
    n = [];
  for (let r of e) {
    let e = `${r.kind}\u001f${r.targetPath}`;
    t.has(e) || (t.add(e), n.push(r));
  }
  return n;
}
function ST(e) {
  return `- [${CT(e.label)}](<${e.targetPath}>)`;
}
function CT(e) {
  return e
    .replaceAll(`\\`, `\\\\`)
    .replaceAll(`[`, `\\[`)
    .replaceAll(`]`, `\\]`);
}
var wT = 2e3,
  TT = 4e3,
  ET = `external_agent_tool_call`,
  DT = `external_agent_tool_result`,
  OT = n
    .xl({
      content: n.El().optional(),
      isMeta: n.ml().optional(),
      isSidechain: n.ml().optional(),
      message: n.El().optional(),
      role: n.wl().optional(),
      timestamp: n.Tl([n.bl(), n.wl()]).nullable().optional(),
      type: n.wl().optional(),
    })
    .passthrough();
async function kT(e) {
  let t = await (0, m.readFile)(e, `utf8`),
    n = [],
    r = null,
    i = !1;
  for (let e of t.split(/\r?\n/)) {
    if (e.trim() === ``) continue;
    let t = jT(e);
    if (t == null || t.isMeta === !0 || t.isSidechain === !0) continue;
    let a = MT(t);
    if (a == null) continue;
    let o = MC(t.timestamp ?? null);
    ((i ||= ((r = o), !0)), n.push({ ...a, timestampMs: o }));
  }
  return { firstTimestampMs: r, messages: n };
}
async function AT(e) {
  return (await kT(e)).messages.some((e) => e.role === `user`);
}
function jT(e) {
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return null;
  }
  let n = OT.safeParse(t);
  return n.success ? n.data : null;
}
function MT(e) {
  let t = NT(e);
  if (t == null) return null;
  let n = PT(AC(e.message) ? e.message.content : e.content);
  return n.text === `` && n.images.length === 0
    ? null
    : {
        images: t === `user` ? n.images : [],
        role: t === `assistant` || n.onlyToolResult ? `assistant` : `user`,
        text: n.text,
      };
}
function NT(e) {
  let t = e.role ?? e.type;
  return t === `user` ? `user` : t === `assistant` ? `assistant` : null;
}
function PT(e) {
  if (typeof e == `string`)
    return { images: [], onlyToolResult: !1, text: e.trim() };
  if (!Array.isArray(e)) return { images: [], onlyToolResult: !1, text: `` };
  let t = [],
    n = [],
    r = e.length > 0;
  for (let i of e) {
    let e = FT(i);
    (t.push(...e.images),
      e.onlyToolResult || (r = !1),
      e.text !== `` && n.push(e.text));
  }
  return {
    images: t,
    onlyToolResult: r,
    text: n
      .join(
        `

`,
      )
      .trim(),
  };
}
function FT(e) {
  if (typeof e == `string`) return { images: [], onlyToolResult: !1, text: e };
  if (!AC(e)) return { images: [], onlyToolResult: !0, text: `` };
  let t = jC(e.type);
  return t === `text`
    ? { images: [], onlyToolResult: !1, text: jC(e.text) ?? `` }
    : t === `tool_use`
      ? { images: [], onlyToolResult: !1, text: RT(e) }
      : t === `tool_result`
        ? { images: [], onlyToolResult: !0, text: zT(e) }
        : t === `image`
          ? IT(e)
          : t === `thinking`
            ? { images: [], onlyToolResult: !0, text: `` }
            : t == null
              ? { images: [], onlyToolResult: !1, text: jC(e.text) ?? `` }
              : {
                  images: [],
                  onlyToolResult: !1,
                  text: `[external unsupported block: ${t}]`,
                };
}
function IT(e) {
  let t = AC(e.source) ? e.source : null,
    n = t == null ? null : jC(t.type),
    r = t == null ? null : jC(t.media_type),
    i = t == null ? null : jC(t.data);
  return n !== `base64` ||
    r == null ||
    !r.toLowerCase().startsWith(`image/`) ||
    i == null
    ? {
        images: [],
        onlyToolResult: !1,
        text: `[external unsupported block: image]`,
      }
    : { images: [`data:${LT(r)};base64,${i}`], onlyToolResult: !1, text: `` };
}
function LT(e) {
  let t = e.toLowerCase().split(`;`)[0]?.trim() ?? ``;
  return t.startsWith(`image/`) ? t : `image/png`;
}
function RT(e) {
  let t = [`[${ET}: ${jC(e.name) ?? `unknown`}]`],
    n = e.input;
  if (AC(n)) {
    let e = jC(n.description);
    e != null && t.push(`description: ${e}`);
    let r = jC(n.command);
    r != null && t.push(`command: ${r}`);
    let i = jC(n.file_path ?? n.file);
    (i != null && t.push(`file: ${i}`),
      t.length === 1 && t.push(`input: ${UT(HT(n), wT)}`));
  } else n != null && t.push(`input: ${UT(HT(n), wT)}`);
  return (
    t.push(`[/${ET}]`),
    t.join(`
`)
  );
}
function zT(e) {
  let t = e.is_error === !0 ? `[${DT}: error]` : `[${DT}]`,
    n = BT(e.content);
  return n === `` ? `${t}\n[/${DT}]` : `${t}\n${UT(n, TT)}\n[/${DT}]`;
}
function BT(e) {
  return typeof e == `string`
    ? e
    : Array.isArray(e)
      ? VT(e).join(`
`)
      : ``;
}
function VT(e) {
  return e
    .flatMap((e) => {
      if (typeof e == `string`) return [e];
      if (!AC(e)) return [];
      let t = jC(e.text);
      return [
        ...(t == null ? [] : [t]),
        ...(Array.isArray(e.content) ? VT(e.content) : []),
      ];
    })
    .filter((e) => e !== ``);
}
function HT(e) {
  let t = JSON.stringify(e);
  return typeof t == `string` ? t : String(e);
}
function UT(e, t) {
  return e.length <= t ? e : `${e.slice(0, t - 1)}…`;
}
async function WT({
  candidate: e,
  codexHome: t,
  cwd: n,
  projectlessOutputDirectory: r,
  projectlessWorkspaceRoot: i,
  stagedGeneratedFiles: a,
}) {
  let o = (0, f.randomUUID)(),
    s = u.default.join(t, `claude-cowork-transcript-imports`, o),
    c = await KT(e.sourceJsonlPath, { stagedGeneratedFiles: a });
  if (c.items.length === 0) return null;
  let l = qT({
      firstTimestampMs: c.firstTimestampMs,
      lastActivityAtMs: e.lastActivityAtMs,
    }),
    d = u.default.join(s, `rollout-${o}.jsonl`),
    p = [
      {
        payload: {
          agent_nickname: null,
          agent_path: null,
          agent_role: null,
          base_instructions:
            e.workspaceKind === `projectless`
              ? GT({ cwd: n, outputDirectory: r ?? n })
              : null,
          cli_version: `0.0.0`,
          cwd: n,
          dynamic_tools: null,
          id: o,
          memory_mode: null,
          model_provider: null,
          originator: `Claude Cowork`,
          source: `vscode`,
          timestamp: l,
        },
        timestamp: l,
        type: `session_meta`,
      },
      ...c.items.map((e) => ({
        payload: e.payload,
        timestamp: l,
        type: e.type,
      })),
    ];
  return (
    await (0, m.mkdir)(s, { recursive: !0 }),
    await (0, m.writeFile)(
      d,
      `${p.map((e) => JSON.stringify(e)).join(`
`)}\n`,
      `utf8`,
    ),
    {
      contentSha256: e.contentSha256,
      cwd: n,
      lastActivityAtMs: e.lastActivityAtMs,
      projectlessOutputDirectory: r,
      projectlessWorkspaceRoot: i,
      projectRoot: e.projectRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      rolloutPath: d,
      rolloutRoot: s,
      sourceId: e.sourceId,
      sourceJsonlPath: e.sourceJsonlPath,
      sourceThreadId: o,
      stagedGeneratedFiles: a,
      title: e.title,
      workspaceKind: e.workspaceKind,
    }
  );
}
function GT({ cwd: e, outputDirectory: t }) {
  return {
    text: [
      `### Projectless Chat`,
      `This projectless thread starts in a generated directory under the user's Documents/Codex folder.`,
      `Prefer answering inline in chat unless using local files would make the result more useful.`,
      ...(t === e
        ? [
            `When using local files for this projectless thread, write scratch files, drafts, generated assets, and other outputs under ${t}. Do not write directly in the home directory unless the user explicitly asks.`,
          ]
        : [
            `Use work/ for intermediate files, scratch analysis, scripts, drafts, and temporary assets.`,
            `Use ${t} only for user-facing deliverables that should appear as outputs.`,
            `When referring to saved deliverables in the final response, link only files from ${t}. Do not write directly in the home directory unless the user explicitly asks.`,
          ]),
    ].join(`
`),
  };
}
async function KT(e, t = {}) {
  let n = await kT(e),
    r = JT();
  r.firstTimestampMs = n.firstTimestampMs;
  for (let e of n.messages)
    ((r.lastMessageTimestampMs = e.timestampMs),
      YT({
        images: e.images,
        role: e.role,
        state: r,
        text: e.text,
        timestampMs: e.timestampMs,
      }));
  return (
    XT(r, t.stagedGeneratedFiles ?? []),
    ZT(r),
    { firstTimestampMs: r.firstTimestampMs, items: r.items }
  );
}
function qT({ firstTimestampMs: e, lastActivityAtMs: t }) {
  return new Date(e ?? t ?? 0).toISOString();
}
function JT() {
  return {
    currentTurn: null,
    firstTimestampMs: null,
    items: [],
    lastAssistantAgentMessagePayload: null,
    lastAssistantResponseItem: null,
    lastAssistantResponseItemCount: 0,
    lastMessageTimestampMs: null,
    responseItems: [],
    userTurnCount: 0,
  };
}
function YT({ images: e, role: t, state: n, text: r, timestampMs: i }) {
  if (t === `user`) {
    (n.currentTurn != null && n.items.push(tE(n.currentTurn, null)),
      (n.userTurnCount += 1));
    let a = `external-import-turn-${n.userTurnCount}`,
      o = nE(i);
    n.items.push({
      payload: {
        collaboration_mode_kind: `default`,
        model_context_window: null,
        started_at: o ?? void 0,
        turn_id: a,
        type: `task_started`,
      },
      type: `event_msg`,
    });
    let s = QT({ role: t, text: r });
    (n.responseItems.push(s),
      n.items.push({ payload: s, type: `response_item` }),
      n.items.push({
        payload: {
          ...(e.length === 0 ? {} : { images: e }),
          local_images: [],
          message: r,
          text_elements: [],
          type: `user_message`,
        },
        type: `event_msg`,
      }),
      (n.currentTurn = { lastAgentMessage: null, turnId: a }));
    return;
  }
  if (n.currentTurn == null) return;
  let a = QT({ role: t, text: r });
  (n.responseItems.push(a),
    (n.lastAssistantResponseItem = a),
    (n.lastAssistantResponseItemCount = n.responseItems.length),
    n.items.push({ payload: a, type: `response_item` }));
  let o = {
    memory_citation: null,
    message: r,
    phase: null,
    type: `agent_message`,
  };
  (n.items.push({ payload: o, type: `event_msg` }),
    (n.lastAssistantAgentMessagePayload = o),
    (n.currentTurn.lastAgentMessage = r));
}
function XT(e, t) {
  let n = bT(t);
  if (
    n == null ||
    e.currentTurn == null ||
    e.lastAssistantAgentMessagePayload == null ||
    e.lastAssistantResponseItem == null
  )
    return;
  let r = `${e.lastAssistantAgentMessagePayload.message.trimEnd()}\n\n${n}`;
  e.lastAssistantAgentMessagePayload.message = r;
  let i = e.lastAssistantResponseItem.content[0];
  (i != null && (i.text = r), (e.currentTurn.lastAgentMessage = r));
}
function ZT(e) {
  e.currentTurn != null &&
    (e.items.push(
      $T(e.responseItems.slice(0, e.lastAssistantResponseItemCount)),
    ),
    e.items.push(tE(e.currentTurn, e.lastMessageTimestampMs)));
}
function QT(e) {
  return {
    content: [
      e.role === `assistant`
        ? { text: e.text, type: `output_text` }
        : { text: e.text, type: `input_text` },
    ],
    role: e.role,
    type: `message`,
  };
}
function $T(e) {
  let t = {
    cached_input_tokens: 0,
    input_tokens: 0,
    output_tokens: 0,
    reasoning_output_tokens: 0,
    total_tokens: e.map(eE).reduce((e, t) => e + t, 0),
  };
  return {
    payload: {
      info: {
        last_token_usage: t,
        model_context_window: null,
        total_token_usage: t,
      },
      rate_limits: null,
      type: `token_count`,
    },
    type: `event_msg`,
  };
}
function eE(e) {
  return Math.ceil(JSON.stringify(e).length / 4);
}
function tE(e, t) {
  return {
    payload: {
      completed_at: nE(t) ?? void 0,
      last_agent_message: e.lastAgentMessage,
      turn_id: e.turnId,
      type: `task_complete`,
    },
    type: `event_msg`,
  };
}
function nE(e) {
  return e == null ? null : Math.floor(e / 1e3);
}
async function rE({ importHistory: e, limits: t, project: n, sessions: r }) {
  return aE({
    getCandidate: (e) => ({
      accountOrgDir: n.accountOrgDir,
      accountOrgDirs: n.accountOrgDirs,
      contentSha256: e.contentSha256,
      detectedFiles: e.detectedFiles,
      documentsDir: n.documentsDir,
      lastActivityAtMs: e.lastActivityAtMs,
      projectRoot: n.sourceRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      sessionStorageDirs: e.storageDirs,
      sourceId: e.sessionId,
      sourceJsonlPath: e.rawTranscriptPath,
      title: e.title,
      workspaceKind: `project`,
    }),
    importHistory: e,
    importedProjectRoot: n.sourceRoot,
    selectSession: (e) =>
      oE(e, n)
        ? dE(e.lastActivityAtMs, t)
          ? `too-old`
          : `include`
        : `outside-project`,
    sessions: r,
  });
}
async function iE({
  importHistory: e,
  limits: t,
  projectlessSpace: n,
  sessions: r,
}) {
  return aE({
    getCandidate: (e) => ({
      accountOrgDir: n.accountOrgDir,
      accountOrgDirs: n.accountOrgDirs,
      contentSha256: e.contentSha256,
      detectedFiles: e.detectedFiles,
      documentsDir: n.documentsDir,
      lastActivityAtMs: e.lastActivityAtMs,
      projectRoot: n.projectlessImportRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      sessionStorageDirs: e.storageDirs,
      sourceId: e.sessionId,
      sourceJsonlPath: e.rawTranscriptPath,
      title: e.title ?? n.name,
      workspaceKind: `projectless`,
    }),
    importHistory: e,
    importedProjectRoot: n.projectlessImportRoot,
    selectSession: (e) =>
      sE(e, n) ? (dE(e.lastActivityAtMs, t) ? `too-old` : `include`) : `ignore`,
    sessions: r,
  });
}
async function aE({
  getCandidate: e,
  importHistory: t,
  importedProjectRoot: n,
  selectSession: r,
  sessions: i,
}) {
  let a = [],
    o = [];
  for (let e of i) {
    let t = r(e);
    if (t === `include`) {
      o.push(e);
      continue;
    }
    t !== `ignore` &&
      a.push({
        manifestPath: e.manifestPath,
        reason: t,
        sessionId: e.sessionId,
      });
  }
  let s = (await Promise.all(o.map((e) => lE(e)))).flatMap((r) =>
    r.rawTranscriptPath == null
      ? (a.push({
          manifestPath: r.manifestPath,
          reason: r.skippedReason,
          sessionId: r.sessionId,
        }),
        [])
      : t != null && uE(t, n, r)
        ? (a.push({
            manifestPath: r.manifestPath,
            reason: `already-imported`,
            sessionId: r.sessionId,
          }),
          [])
        : [e(r)],
  );
  return (s.sort(fE), { skippedSessions: a, transcriptCandidates: s });
}
function oE(e, t) {
  return e.spaceId === t.spaceId || (e.cwd != null && SS(e.cwd, t.sourceRoot))
    ? !0
    : e.userSelectedFolders.some((e) => SS(e, t.sourceRoot));
}
function sE(e, t) {
  return (
    (t.spaceId == null ? e.spaceId == null : e.spaceId === t.spaceId) &&
    cE(e) &&
    e.userSelectedFolders.length === 0
  );
}
function cE(e) {
  if (e.cwd == null || e.cwd === ``) return !0;
  let t = u.default.posix.normalize(e.cwd);
  if (
    e.storageDirs.some((t) => SS(e.cwd ?? ``, t)) ||
    t === `/sessions` ||
    t.startsWith(`/sessions/`)
  )
    return !0;
  let n = u.default.win32.isAbsolute(e.cwd) ? u.default.win32 : u.default.posix,
    r = n.normalize(e.cwd).split(n.sep),
    i = r.findIndex((e) => e.toLowerCase() === `local-agent-mode-sessions`);
  return i >= 0 && r.lastIndexOf(e.sessionId) > i;
}
async function lE(e) {
  let t = await pE(e.storageDirs, e.cliSessionIds);
  if (t.length === 0)
    return {
      ...e,
      contentSha256: null,
      rawTranscriptPath: null,
      skippedReason: `missing-transcript`,
    };
  let n = (await Promise.all(t.map(AT))).findIndex((e) => e);
  if (n >= 0) {
    let r = t[n];
    return { ...e, contentSha256: await mE(r), rawTranscriptPath: r };
  }
  return {
    ...e,
    contentSha256: null,
    rawTranscriptPath: null,
    skippedReason: `no-importable-turns`,
  };
}
function uE(e, t, n) {
  return e.records.some(
    (e) =>
      xS(e.projectRoot, t) &&
      e.sourceId === n.sessionId &&
      e.contentSha256 === n.contentSha256,
  );
}
function dE(e, t) {
  return e != null && e < t.nowMs - t.maxSessionAgeMs;
}
function fE(e, t) {
  return (t.lastActivityAtMs ?? 0) - (e.lastActivityAtMs ?? 0);
}
async function pE(e, t) {
  let n = (
    await Promise.all(
      e.flatMap((e) =>
        [`transcripts`, `projects`].map((t) =>
          PS(u.default.join(e, `.claude`, t)),
        ),
      ),
    )
  ).flat();
  return t.flatMap((e) =>
    n.filter((t) => u.default.basename(t) === `${e}.jsonl`),
  );
}
async function mE(e) {
  return (0, f.createHash)(`sha256`)
    .update(await (0, m.readFile)(e))
    .digest(`hex`);
}
async function hE({
  detection: e,
  importHistory: t,
  include: n = dC,
  items: r,
  limits: i,
}) {
  let a = kE(i),
    [o, s] = await Promise.all([
      Promise.all(
        e.projects.map(async (i) => {
          let o = bE({ importHistory: t, include: n, items: r, project: i }),
            s = xE({ importHistory: t, include: n, items: r, project: i }),
            c = SE({ importHistory: t, include: n, items: r, project: i });
          if (o.size === 0 && !s && !c) return null;
          let { skippedSessions: l, transcriptCandidates: u } = o.has(
              `SESSIONS`,
            )
              ? await rE({
                  importHistory: t,
                  limits: a,
                  project: i,
                  sessions:
                    e.sessionManifestsByProjectRoot.get(i.sourceRoot) ?? [],
                })
              : { skippedSessions: [], transcriptCandidates: [] },
            d = DE({
              items: r,
              transcriptCandidates: u,
              workspaceKind: `project`,
            });
          return o.size === 0 && !s && !c && d.length === 0
            ? null
            : {
                accountSetupSelected: s,
                configSurfaces: s
                  ? (e.configSurfacesByProjectRoot.get(i.sourceRoot) ?? [])
                  : [],
                itemTypes: o,
                pluginCandidates: c
                  ? (e.pluginCandidatesByProjectRoot.get(i.sourceRoot) ?? [])
                  : [],
                pluginSetupSelected: c,
                project: i,
                scheduledTasks: s
                  ? (e.scheduledTasksByProjectRoot.get(i.sourceRoot) ?? [])
                  : [],
                shouldRecordProjectImport: r != null && OE(r, i),
                skippedSessions: l,
                transcriptCandidates: d.slice(0, a.maxSessions),
                warnings: [...e.warnings],
              };
        }),
      ),
      TE({ detection: e, importHistory: t, include: n, items: r, limits: a }),
    ]);
  return { projectlessSpaces: s, projects: o.filter((e) => e != null) };
}
function gE(e) {
  return xw(e);
}
function _E(e) {
  return e.some(wE);
}
function vE(e) {
  return e.some((e) => e.itemType === `SESSIONS`);
}
function yE(e) {
  return e.some(
    (e) =>
      e.itemType === `SESSIONS` &&
      (e.details?.sessions ?? []).some(
        (e) => e.workspaceKind === `projectless`,
      ),
  );
}
function bE({ importHistory: e, include: t, items: n, project: r }) {
  if (n != null) {
    let e = new Set(
      n
        .filter((e) => e.cwd != null && e.cwd !== `` && xS(e.cwd, r.sourceRoot))
        .map((e) => e.itemType),
    );
    return (
      n.some(
        (e) =>
          e.itemType === `SESSIONS` &&
          (e.details?.sessions ?? []).some(
            (e) => e.workspaceKind !== `projectless` && xS(e.cwd, r.sourceRoot),
          ),
      ) && e.add(`SESSIONS`),
      e
    );
  }
  let i = [],
    a = e != null && XS(e, r);
  return (
    t.agents && !a && i.push(`AGENTS_MD`),
    t.sessions && i.push(`SESSIONS`),
    new Set(i)
  );
}
function xE({ importHistory: e, include: t, items: n, project: r }) {
  return n == null
    ? t.config &&
        (e == null ||
          !ZS(e, { accountOrgDir: r.accountOrgDir, itemType: `CONFIG` }))
    : n.some(
        (e) =>
          wE(e) &&
          e.sourcePath != null &&
          e.sourcePath !== `` &&
          xS(e.sourcePath, r.accountOrgDir),
      );
}
function SE({ importHistory: e, include: t, items: n, project: r }) {
  return n == null
    ? t.config &&
        (e == null ||
          !ZS(e, { accountOrgDir: r.accountOrgDir, itemType: `PLUGINS` }))
    : n.some(
        (e) =>
          e.itemType === `PLUGINS` &&
          e.sourcePath != null &&
          e.sourcePath !== `` &&
          xS(e.sourcePath, r.accountOrgDir),
      );
}
function CE(e) {
  return e?.some((e) => e.itemType === `PLUGINS`) ?? !1;
}
function wE(e) {
  return (e.cwd == null || e.cwd === ``) && e.itemType === `CONFIG`;
}
async function TE({
  detection: e,
  importHistory: t,
  include: n,
  items: r,
  limits: i,
}) {
  return EE({ include: n, items: r })
    ? (
        await Promise.all(
          e.projectlessSpaces.map(async (n) => {
            let { skippedSessions: a, transcriptCandidates: o } = await iE({
                importHistory: t,
                limits: i,
                projectlessSpace: n,
                sessions:
                  e.sessionManifestsByProjectlessSpaceRoot.get(
                    n.projectlessImportRoot,
                  ) ?? [],
              }),
              s = DE({
                items: r,
                transcriptCandidates: o,
                workspaceKind: `projectless`,
              });
            return s.length === 0
              ? null
              : {
                  projectlessSpace: n,
                  skippedSessions: a,
                  transcriptCandidates: s.slice(0, i.maxSessions),
                };
          }),
        )
      ).filter((e) => e != null)
    : [];
}
function EE({ include: e, items: t }) {
  return t == null ? e.sessions : yE(t);
}
function DE({ items: e, transcriptCandidates: t, workspaceKind: n }) {
  if (e == null) return t;
  let r = e.flatMap(
    (e) =>
      e.details?.sessions
        .filter((e) =>
          n === `project`
            ? e.workspaceKind !== `projectless`
            : e.workspaceKind === `projectless`,
        )
        .map((e) => e.path) ?? [],
  );
  return t.filter((e) => r.some((t) => xS(t, e.sourceJsonlPath)));
}
function OE(e, t) {
  let n = bE({ importHistory: void 0, include: dC, items: e, project: t });
  return fC.every((e) => n.has(e));
}
function kE(e) {
  return {
    maxSessionAgeMs: e?.maxSessionAgeMs ?? 2592e6,
    maxSessions: e?.maxSessions ?? 50,
    nowMs: e?.nowMs ?? Date.now(),
  };
}
async function AE({
  importHistory: e,
  include: t,
  items: n,
  limits: r,
  options: i,
  project: a,
  workspaceRoots: o = null,
}) {
  let s = PE({
    sourceIndex:
      a == null
        ? await Ow(i)
        : {
            projectlessSpaces: [],
            projects: [a],
            skippedProjects: [],
            warnings: [],
          },
    workspaceRoots: IE({ items: n, workspaceRoots: o }),
    includeProjectlessSpaces: FE({ items: n, workspaceRoots: o }),
  });
  return {
    importSelection: await hE({
      detection: await Ww(s),
      importHistory: e,
      include: t,
      items: n,
      limits: r,
    }),
    sourceIndex: s,
  };
}
async function jE({
  codexHome: e,
  includeHome: t = !0,
  options: n,
  workspaceRoots: r = null,
}) {
  let i = Ow(n),
    a = WS(e),
    [
      { projectlessSpaces: o, projects: s, skippedProjects: c, warnings: l },
      u,
    ] = await Promise.all([i, a]),
    d = await Ww(
      PE({
        sourceIndex: {
          projectlessSpaces: o,
          projects: s,
          skippedProjects: c,
          warnings: l,
        },
        workspaceRoots: r,
        includeProjectlessSpaces: t,
      }),
    ),
    f = await hE({
      detection: d,
      include: t ? void 0 : { agents: !0, config: !1, sessions: !1 },
      importHistory: u,
      limits: n,
    });
  return {
    items: Cw({ projectlessSpaces: f.projectlessSpaces, projects: f.projects }),
    unsupportedProjects: Ew({ skippedProjects: c, workspaceRoots: r }),
    warnings: d.warnings,
  };
}
async function ME({
  codexHome: e,
  items: t,
  options: n,
  projectlessThreadPaths: r,
}) {
  if (gE(t).length === 0 && !_E(t) && !CE(t) && !vE(t))
    return {
      accountSetupItemsToRecord: [],
      importedProjectRoots: [],
      preparedRollouts: [],
      projectsToRecord: [],
    };
  let { importSelection: i } = await AE({
      importHistory: await WS(e),
      items: t,
      limits: n,
      options: n,
    }),
    a = await vw({ codexHome: e, projects: i.projects }),
    o = [
      ...a.projects.flatMap((e) => e.transcriptCandidates),
      ...i.projectlessSpaces.flatMap((e) => e.transcriptCandidates),
    ],
    s = (
      await Promise.all(
        o.map((t) =>
          LE({ candidate: t, codexHome: e, projectlessThreadPaths: r }),
        ),
      )
    ).filter((e) => e != null);
  return {
    accountSetupItemsToRecord: a.projects.flatMap((e) =>
      UE(e)
        ? [
            {
              accountOrgDir: e.projectImport.project.accountOrgDir,
              itemType: `CONFIG`,
            },
          ]
        : [],
    ),
    importedProjectRoots: VE(a.projects),
    preparedRollouts: s,
    projectsToRecord: a.projects.flatMap((e) =>
      e.projectImport.shouldRecordProjectImport
        ? [e.projectImport.project]
        : [],
    ),
  };
}
async function NE({ callbacks: e, codexHome: t, items: n, options: r }) {
  let [i, a] = await Promise.all([
      ME({
        codexHome: t,
        items: n,
        options: r,
        projectlessThreadPaths: e.createProjectlessThreadPaths,
      }),
      Bw({ items: n, options: r }),
    ]),
    { importedRollouts: o } = await RC({
      callbacks: e,
      codexHome: t,
      migration: i,
      pluginImports: a,
    });
  return { importedProjectRoots: i.importedProjectRoots, importedRollouts: o };
}
function PE({
  includeProjectlessSpaces: e,
  sourceIndex: t,
  workspaceRoots: n,
}) {
  return n == null
    ? { ...t, projectlessSpaces: e ? t.projectlessSpaces : [] }
    : {
        ...t,
        projectlessSpaces: e ? t.projectlessSpaces : [],
        projects: t.projects.filter((e) => n.some((t) => xS(t, e.sourceRoot))),
      };
}
function FE({ items: e, workspaceRoots: t }) {
  return e == null ? t == null : yE(e);
}
function IE({ items: e, workspaceRoots: t }) {
  if (e != null) {
    let n = gE(e);
    return n.length > 0 ? n : _E(e) ? t : [];
  }
  return t;
}
async function LE({ candidate: e, codexHome: t, projectlessThreadPaths: n }) {
  let r = await rT(e);
  if (e.workspaceKind !== `projectless`) {
    let i =
        r.length === 0
          ? null
          : (
              await RE({
                candidate: e,
                codexHome: t,
                projectlessThreadPaths: n,
              })
            ).outputDirectory,
      a =
        i == null
          ? []
          : await iT({ candidate: e, outputDirectory: i, sources: r });
    return WT({
      candidate: e,
      codexHome: t,
      cwd: e.projectRoot,
      projectlessOutputDirectory: null,
      projectlessWorkspaceRoot: null,
      stagedGeneratedFiles: a,
    });
  }
  if (n == null)
    throw Error(
      `Projectless Cowork transcript import requires projectless thread paths`,
    );
  let i;
  try {
    i = await n(e);
  } catch (e) {
    if (BE(e)) return null;
    throw e;
  }
  let a = await iT({
    candidate: e,
    outputDirectory: i.outputDirectory,
    sources: r,
  });
  return WT({
    candidate: e,
    codexHome: t,
    cwd: i.cwd,
    projectlessOutputDirectory: i.outputDirectory,
    projectlessWorkspaceRoot: i.workspaceRoot,
    stagedGeneratedFiles: a,
  });
}
async function RE({ candidate: e, codexHome: t, projectlessThreadPaths: n }) {
  if (n != null) return n(e);
  let r = u.default.join(t, `claude-cowork-generated-files`, zE(e));
  return { cwd: r, outputDirectory: r, workspaceRoot: r };
}
function zE(e) {
  let t = e.sourceId === `` ? `cowork-session` : e.sourceId,
    n =
      t
        .toLowerCase()
        .replace(/[^a-z0-9]+/g, `-`)
        .replace(/^-+|-+$/g, ``)
        .slice(0, 80) || `cowork-session`;
  return `${
    ((e.title?.trim() === `` ? null : e.title) ?? t)
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, `-`)
      .replace(/^-+|-+$/g, ``)
      .slice(0, 80) || n
  }-${n}`;
}
function BE(e) {
  return (
    e instanceof Error &&
    e.message === `Unable to create a unique projectless thread directory`
  );
}
function VE(e) {
  return [
    ...new Set(
      e.flatMap((e) => (HE(e) ? [e.projectImport.project.sourceRoot] : [])),
    ),
  ].sort((e, t) => e.localeCompare(t));
}
function HE(e) {
  return (
    e.projectImport.accountSetupSelected ||
    e.projectImport.pluginSetupSelected ||
    e.projectImport.itemTypes.has(`AGENTS_MD`) ||
    e.projectImport.itemTypes.has(`SESSIONS`)
  );
}
function UE(e) {
  return (
    e.projectImport.accountSetupSelected &&
    e.projectImport.configSurfaces.length > 0
  );
}
function WE() {
  return {
    pathForAgentEnvironment: (e, t) => (t ? n.ri(e) : e),
    pathForDesktopHost: (e, t) => (t ? n.ii(e, n.ai()) : e),
  };
}
function GE(e) {
  return {
    detect: ({ hostId: t, includeHome: n, workspaceRoots: r }) =>
      KE({
        getMigrationOptions: e.getMigrationOptions,
        getRequestAppServerClient: e.getRequestAppServerClient,
        hostId: t,
        includeHome: n,
        pathAdapters: e.pathAdapters,
        workspaceRoots: r,
      }),
    import: ({ hostId: t, items: n }) => qE({ ...e, hostId: t, items: n }),
  };
}
async function KE({
  getMigrationOptions: e,
  getRequestAppServerClient: t,
  hostId: n,
  includeHome: r = !0,
  pathAdapters: i,
  workspaceRoots: a,
}) {
  let o = await QE({
    getMigrationOptions: e,
    getRequestAppServerClient: t,
    hostId: n,
    pathAdapters: i,
  });
  if (o == null) return { items: [], unsupportedProjects: [] };
  let s =
      a == null
        ? null
        : a.map((e) => i.pathForDesktopHost(e, o.shouldUseWslPaths)),
    c = await jE({
      codexHome: o.codexHome,
      includeHome: r,
      options: o.options,
      workspaceRoots: s,
    });
  return {
    items: c.items.map((e) =>
      JE(Sw(e, (e) => i.pathForAgentEnvironment(e, o.shouldUseWslPaths))),
    ),
    unsupportedProjects: c.unsupportedProjects.map((e) => ({
      ...cC(e, (e) => i.pathForAgentEnvironment(e, o.shouldUseWslPaths)),
      providerId: bw,
    })),
  };
}
async function qE({
  getAppServerConnection: e,
  getMigrationOptions: t,
  getRequestAppServerClient: n,
  hostId: r,
  items: i,
  pathAdapters: a,
  recordProjectlessThread: o,
}) {
  let s = await QE({
    getMigrationOptions: t,
    getRequestAppServerClient: n,
    hostId: r,
    pathAdapters: a,
  });
  if (s == null) return { projectRoots: [] };
  let c = i.map((e) =>
    Sw(YE(e), (e) => a.pathForDesktopHost(e, s.shouldUseWslPaths)),
  );
  return {
    projectRoots: (
      await NE({
        callbacks: XE({
          appServerConnection: e(r),
          appServerClient: n(r),
          homeDirectory: s.options.homeDir,
          hostId: r,
          pathAdapters: a,
          recordProjectlessThread: o,
          shouldUseWslPaths: s.shouldUseWslPaths,
        }),
        codexHome: s.codexHome,
        items: c,
        options: s.options,
      })
    ).importedProjectRoots.map((e) =>
      a.pathForAgentEnvironment(e, s.shouldUseWslPaths),
    ),
  };
}
function JE(e) {
  return {
    ...e,
    details:
      e.details == null
        ? null
        : {
            ...e.details,
            skills: [],
            plugins:
              e.details.plugins.length === 0
                ? []
                : [
                    {
                      marketplaceName: n.sc,
                      pluginNames: e.details.plugins.map((e) => e.name),
                    },
                  ],
          },
  };
}
function YE(e) {
  if (e.details == null) return { ...e, details: null };
  let { skills: t, ...n } = e.details;
  return {
    ...e,
    details: {
      ...n,
      plugins: e.details.plugins.flatMap((e) =>
        e.pluginNames.map((e) => ({ name: e })),
      ),
    },
  };
}
function XE({
  appServerConnection: e,
  appServerClient: t,
  homeDirectory: n,
  hostId: r,
  pathAdapters: i,
  recordProjectlessThread: a,
  shouldUseWslPaths: o,
}) {
  return {
    createProjectlessThreadPaths: async (e) => {
      let r = await ii({
        appServerClient: t,
        createSplitDirectories: !0,
        homeDirectory: ZE({
          appServerClient: t,
          homeDirectory: n,
          pathAdapters: i,
          shouldUseWslPaths: o,
        }),
        prompt: e.title,
      });
      return {
        cwd: i.pathForDesktopHost(r.cwd, o),
        outputDirectory: i.pathForDesktopHost(r.outputDirectory, o),
        workspaceRoot: i.pathForDesktopHost(r.workspaceRoot, o),
      };
    },
    forkThread: async (t) => ({ threadId: (await e.forkThread(t)).thread.id }),
    pathForCodexHost: (e) => i.pathForAgentEnvironment(e, o),
    recordProjectlessThread:
      a == null
        ? void 0
        : async ({ outputDirectory: e, threadId: t, workspaceRoot: n }) => {
            await a({
              hostId: r,
              outputDirectory: e,
              threadId: t,
              workspaceRoot: n,
            });
          },
    setThreadName: async ({ name: t, threadId: n }) => {
      await e.setThreadName(n, t);
    },
  };
}
function ZE({
  appServerClient: e,
  homeDirectory: t,
  pathAdapters: n,
  shouldUseWslPaths: r,
}) {
  if (t != null && t !== ``) return n.pathForAgentEnvironment(t, r);
  if (e.hostConfig.home_dir == null || e.hostConfig.home_dir === ``)
    throw Error(`Projectless Cowork import requires a home directory`);
  return e.hostConfig.home_dir;
}
async function QE({
  getMigrationOptions: e,
  getRequestAppServerClient: t,
  hostId: r,
  pathAdapters: i,
}) {
  let a = t(r);
  if (n.eo(a.hostConfig)) return null;
  let o = n.ui(a.hostConfig);
  return {
    codexHome: i.pathForDesktopHost(await a.codexHome(), o),
    options: e(),
    shouldUseWslPaths: o,
  };
}
function $E({
  desktopPaths: e,
  env: t = process.env,
  homeDir: n = (0, l.homedir)(),
  platform: r = process.platform,
}) {
  let i = eD({ desktopPaths: e, homeDir: n, platform: r }),
    a = tD({ env: t, homeDir: n, platform: r });
  return r === `win32`
    ? {
        documentsDir: i,
        fallbackUserDataDirs: [a],
        homeDir: n,
        userDataDir: nD({ env: t, homeDir: n }),
      }
    : { documentsDir: i, homeDir: n, userDataDir: a };
}
function eD({ desktopPaths: e, homeDir: t, platform: n }) {
  return iD(t, e.getPath(`home`), n)
    ? e.getPath(`documents`)
    : aD(n).join(t, `Documents`);
}
function tD({ env: e, homeDir: t, platform: n }) {
  if (n === `darwin`)
    return u.default.posix.join(t, `Library`, `Application Support`, `Claude`);
  if (n === `win32`) {
    let n = e.APPDATA?.trim();
    return n != null && u.default.win32.isAbsolute(n)
      ? u.default.win32.join(n, `Claude`)
      : u.default.win32.join(
          rD({ env: e, homeDir: t }),
          `AppData`,
          `Roaming`,
          `Claude`,
        );
  }
  return u.default.posix.join(t, `.config`, `Claude`);
}
function nD({ env: e, homeDir: t }) {
  let n = e.LOCALAPPDATA?.trim(),
    r =
      n != null && u.default.win32.isAbsolute(n)
        ? n
        : u.default.win32.join(rD({ env: e, homeDir: t }), `AppData`, `Local`);
  return u.default.win32.join(
    r,
    `Packages`,
    `Claude_pzs8sxrjxfjjc`,
    `LocalCache`,
    `Roaming`,
    `Claude`,
  );
}
function rD({ env: e, homeDir: t }) {
  let n = e.USERPROFILE?.trim();
  return n != null && u.default.win32.isAbsolute(n) ? n : t;
}
function iD(e, t, n) {
  let r = aD(n),
    i = r.resolve(e),
    a = r.resolve(t);
  return n === `win32` ? i.toLowerCase() === a.toLowerCase() : i === a;
}
function aD(e) {
  return e === `win32` ? u.default.win32 : u.default.posix;
}
function oD(e) {
  return GE({
    ...e,
    getMigrationOptions: () => $E({ desktopPaths: c.app }),
    pathAdapters: WE(),
  });
}
var sD = n.xl({
    computer_use: n.xl({
      windows: n.xl({ always_allowed_app_ids: n.Cl(n.wl(), n.ml()) }),
    }),
  }),
  cD = n
    .xl({ apps: n.xl({ allowed: n.pl(n.wl()) }).passthrough() })
    .passthrough(),
  lD =
    /(^|\r?\n)\[computer_use\.windows\.always_allowed_app_ids\][\s\S]*?(?=\r?\n\[[^\]\r\n]+\]|$)/;
async function uD(e) {
  let t = u.default.join(u.default.dirname(e), `computer-use`, `config.toml`);
  if (!(0, p.existsSync)(t)) return;
  let n;
  try {
    n = await hD(t);
  } catch {
    return;
  }
  let r = cD.safeParse(n);
  if (!r.success) return;
  let i = bD(r.data.apps.allowed);
  i.length !== 0 &&
    (await mD(e, bD([...((await pD(e)) ?? []), ...i])),
    (r.data.apps.allowed = []),
    await gD(t, r.data));
}
async function dD(e) {
  return (await uD(e), (0, p.existsSync)(e) ? pD(e) : null);
}
async function fD(e, t) {
  (await uD(e), await mD(e, t));
}
async function pD(e) {
  let t;
  try {
    t = await hD(e);
  } catch {
    return null;
  }
  let n = sD.safeParse(t);
  return n.success
    ? bD(
        Object.entries(n.data.computer_use.windows.always_allowed_app_ids)
          .filter(([, e]) => e === !0)
          .map(([e]) => e),
      )
    : null;
}
async function mD(e, t) {
  let r = (0, p.existsSync)(e) ? await (0, m.readFile)(e, `utf8`) : ``;
  (n.Qr(r), await _D(e, vD(r, t)));
}
async function hD(e) {
  if (!(0, p.existsSync)(e)) return {};
  let t = n.Qr(await (0, m.readFile)(e, `utf8`));
  return xD(t) ? t : {};
}
async function gD(e, t) {
  let n = L(t);
  await _D(
    e,
    n.endsWith(`
`)
      ? n
      : `${n}\n`,
  );
}
async function _D(e, t) {
  (await (0, m.mkdir)(u.default.dirname(e), { recursive: !0 }),
    await (0, m.writeFile)(e, t, `utf8`));
}
function vD(e, t) {
  let n = yD(t),
    r = lD.exec(e);
  return r == null
    ? e.trim() === ``
      ? n
      : `${e}${
          e.endsWith(`
`)
            ? `
`
            : `

`
        }${n}`
    : [e.slice(0, r.index), r[1], n, e.slice(r.index + r[0].length)].join(``);
}
function yD(e) {
  let t = L({
    computer_use: {
      windows: {
        always_allowed_app_ids: Object.fromEntries(e.map((e) => [e, !0])),
      },
    },
  });
  return t.endsWith(`
`)
    ? t
    : `${t}\n`;
}
function bD(e) {
  return Array.from(new Set(e.map((e) => e.trim()).filter((e) => e !== ``)));
}
function xD(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
var SD = `ComputerUseAppApprovals.json`,
  CD = `config.toml`,
  wD = u.default.join(`Library`, `Application Support`, `Software`),
  TD = u.default.join(`Library`, `Group Containers`),
  ED = `2DC432GLL2.com.openai.sky.CUAService`,
  DD = n.xl({ approvedBundleIdentifiers: n.pl(n.wl()) }),
  OD = new Map();
async function kD(e = {}) {
  let t = await PD(ND(e));
  if (t == null) return null;
  let n = LD(t);
  return { approvedApps: await RD(n, e), approvedBundleIdentifiers: n };
}
async function AD(e = {}) {
  let t = ND(e);
  return t.format === `json`
    ? (0, p.existsSync)(t.filePath)
    : (await dD(t.filePath)) != null;
}
async function jD(e, t = {}) {
  let n = ND(t),
    r = LD(e);
  return (
    n.format === `toml` ? await fD(n.filePath, r) : await ID(n.filePath, r),
    { approvedApps: await RD(r, t), approvedBundleIdentifiers: r }
  );
}
async function MD(e, t = {}) {
  let n = await kD(t);
  return n == null
    ? null
    : jD(
        n.approvedBundleIdentifiers.filter((t) => t !== e),
        t,
      );
}
function ND(e = {}) {
  let {
    codexHome: t,
    homeDirectory: r = (0, l.homedir)(),
    platform: i = process.platform,
  } = e;
  if (i === `win32`)
    return { filePath: u.default.join(t ?? n.ni(), CD), format: `toml` };
  let a = u.default.join(r, TD, ED, wD);
  return { filePath: u.default.join(a, SD), format: `json` };
}
async function PD(e) {
  return e.format === `toml`
    ? dD(e.filePath)
    : (0, p.existsSync)(e.filePath)
      ? FD(e.filePath)
      : null;
}
async function FD(e) {
  let t = {};
  if ((0, p.existsSync)(e))
    try {
      t = JSON.parse(await (0, m.readFile)(e, `utf8`));
    } catch {
      t = {};
    }
  let n = DD.safeParse(t);
  return n.success ? n.data.approvedBundleIdentifiers : [];
}
async function ID(e, t) {
  (await (0, m.mkdir)(u.default.dirname(e), { recursive: !0 }),
    await (0, m.writeFile)(
      e,
      JSON.stringify({ approvedBundleIdentifiers: t }, null, 2),
      `utf8`,
    ));
}
function LD(e) {
  return Array.from(new Set(e.map((e) => e.trim()).filter((e) => e !== ``)));
}
async function RD(e, t = {}) {
  let n = HD(t.platform);
  return Promise.all(e.map((e) => zD(e, n)));
}
async function zD(e, t) {
  let n = VD(e, t),
    r = OD.get(n);
  if (r != null) return r;
  let i = (await BD(e)) ?? UD(e);
  return (
    t !== `win32` || i.iconDataURL != null ? OD.set(n, i) : OD.delete(n),
    i
  );
}
async function BD(e) {
  try {
    let t = await n.vt(e);
    return t == null
      ? null
      : {
          bundleIdentifier: e,
          displayName: t.displayName,
          iconDataURL: await Ia(t.appPath),
        };
  } catch {
    return null;
  }
}
function VD(e, t) {
  return `${t}:${e}`;
}
function HD(e) {
  return e === `darwin` || e === `win32` ? e : `other`;
}
function UD(e) {
  return { bundleIdentifier: e, displayName: e, iconDataURL: null };
}
var WD = (0, d.promisify)(h.execFile);
async function GD(e) {
  if (process.platform !== `darwin`) return null;
  let t = await qD(e);
  return t == null ? null : KD(t);
}
async function KD(e) {
  let t = await (0, m.mkdtemp)(
      u.default.join((0, l.tmpdir)(), `codex-app-icon-`),
    ),
    n = u.default.join(t, `icon.png`);
  try {
    await WD(`/usr/bin/sips`, [`-s`, `format`, `png`, e, `--out`, n], {
      env: r.t(process.env),
      timeout: 5e3,
    });
    let t = await (0, m.readFile)(n);
    return t.byteLength === 0
      ? null
      : `data:image/png;base64,${t.toString(`base64`)}`;
  } catch {
    return null;
  } finally {
    await (0, m.rm)(t, { force: !0, recursive: !0 });
  }
}
async function qD(e) {
  let t = u.default.join(e, `Contents`, `Info.plist`),
    r = u.default.join(e, `Contents`, `Resources`),
    i =
      (await n.St(t, `CFBundleIconFile`)) ??
      (await n.St(t, `CFBundleIconName`)),
    a = i == null ? [] : [i, JD(i)];
  for (let e of [...a, `AppIcon.icns`]) {
    let t = u.default.join(r, e);
    if ((0, p.existsSync)(t)) return t;
  }
  return null;
}
function JD(e) {
  return u.default.extname(e) === `` ? `${e}.icns` : e;
}
var YD = (0, d.promisify)(h.execFile),
  XD = [
    `computer-use`,
    `Codex Computer Use.app`,
    `Contents`,
    `SharedSupport`,
    `Codex Computer Use Installer.app`,
    `Contents`,
    `MacOS`,
    `Codex Computer Use Installer`,
  ],
  ZD = u.default.join(
    `/System`,
    `Library`,
    `CoreServices`,
    `CoreTypes.bundle`,
    `Contents`,
    `Resources`,
  ),
  QD = u.default.join(ZD, `LockedIcon.icns`),
  $D = `NSComputer`;
async function eO() {
  if (process.platform !== `darwin`) return null;
  try {
    return await rO();
  } catch {
    return !1;
  }
}
async function tO() {
  if (process.platform !== `darwin`)
    return { computerIconDataURL: null, lockIconDataURL: null };
  let e = c.nativeImage.createFromNamedImage($D);
  return {
    computerIconDataURL: e.isEmpty() ? null : e.toDataURL(),
    lockIconDataURL: await KD(QD),
  };
}
async function nO(e) {
  return process.platform === `darwin`
    ? (await iO(e ? `install` : `uninstall`), rO())
    : null;
}
async function rO() {
  return (await iO(`status`)).trim() === `OK: installed`;
}
async function iO(e) {
  let { stdout: t } = await YD(aO(), [e], {
    env: r.t(process.env),
    timeout: 12e4,
  });
  return t;
}
function aO() {
  return u.default.join(n.ni(), ...XD);
}
var oO = (0, d.promisify)(h.execFile),
  sO = `com.openai.sky.CUAService`,
  cO = `computerUseSoundMode`;
async function lO() {
  if (process.platform !== `darwin`) return null;
  try {
    let { stdout: e } = await oO(`/usr/bin/defaults`, [`read`, sO, cO]),
      t = e.trim();
    return t === `` ? null : t;
  } catch {
    return null;
  }
}
async function uO(e) {
  return (
    process.platform === `darwin` &&
      (await oO(`/usr/bin/defaults`, [`write`, sO, cO, e])),
    e
  );
}
var dO = new Map(),
  fO = r.a(`computer-use-capture-handler`),
  pO = 12e4,
  mO = `-10014`,
  hO = /^Codex Computer Use Apple Event error (-?\d+):/,
  gO = /^AE.+ failed with (-?\d+)(?:\s|$)/;
async function _O({
  animationTarget: e,
  bundleIdentifier: t,
  origin: n,
  requestComputerUseCaptureWorker: r,
  requestId: i,
  subscribeComputerUseCaptureWorkerEvent: a,
}) {
  if (t.trim().length === 0 || i.trim().length === 0) return null;
  if (dO.has(i))
    return (
      fO().warning(`Rejected duplicate Codex Computer Use capture request`, {
        safe: { requestId: i },
        sensitive: {},
      }),
      null
    );
  let o = Date.now(),
    s = new AbortController(),
    c = dO.values().next().value?.permissionRequestId ?? i,
    l = SO(i),
    u = a(`update`, (e) => {
      e.requestId === i && vO(e.requestId, e.update);
    });
  (dO.set(i, {
    origin: n,
    abortController: s,
    permissionRequestId: c,
    startedAt: o,
    timeoutId: l,
    unsubscribe: u,
  }),
    fO().debug(`Starting Codex Computer Use capture`, {
      safe: { requestId: i, pendingCaptureCount: dO.size },
      sensitive: {},
    }));
  let d = async () => {
    if (!dO.has(i)) return null;
    try {
      let n = await r({
          method: `start`,
          params: {
            animationTarget: e,
            bundleIdentifier: t,
            permissionRequestId: c,
            requestId: i,
          },
          signal: s.signal,
        }),
        a = dO.get(i);
      return (
        a != null &&
          (n.result === `appshot_permissions_abandoned`
            ? bO(i, a)
            : (clearTimeout(a.timeoutId), (a.timeoutId = SO(i)))),
        fO().debug(`Codex Computer Use capture worker acknowledged start`, {
          safe: { requestId: i, elapsedMs: Date.now() - o, result: n.result },
          sensitive: {},
        }),
        n
      );
    } catch (e) {
      let t = dO.get(i);
      if (t == null) return null;
      if (CO(e))
        return (
          fO().debug(
            `Codex Computer Use capture permissions are still pending; retrying start`,
            { safe: { requestId: i }, sensitive: {} },
          ),
          clearTimeout(t.timeoutId),
          (t.timeoutId = SO(i)),
          d()
        );
      let n = wO(e),
        r =
          n == null
            ? `start_request_failed`
            : `start_request_failed:${n.source}:${n.code}`;
      return (
        fO().warning(`Codex Computer Use capture worker failed`, {
          safe: { failureReason: r, requestId: i },
          sensitive: { error: e },
        }),
        xO(i, t, r),
        null
      );
    }
  };
  return d();
}
function vO(e, t) {
  let n = dO.get(e);
  n != null &&
    (fO().debug(`Codex Computer Use capture update received`, {
      safe: {
        requestId: e,
        updateType: t.type,
        elapsedMs: Date.now() - n.startedAt,
      },
      sensitive: {},
    }),
    yO(n.origin, {
      requestId: e,
      type: `computer-use-capture-updated`,
      update: t,
    }),
    (t.type === `completed` || t.type === `failed`) && bO(e, n));
}
function yO(e, t) {
  e.isDestroyed() || e.send(B, t);
}
function bO(e, t) {
  (dO.delete(e),
    clearTimeout(t.timeoutId),
    t.abortController.abort(),
    t.unsubscribe());
}
function xO(e, t, n) {
  (bO(e, t),
    yO(t.origin, {
      requestId: e,
      type: `computer-use-capture-updated`,
      update: { type: `failed`, failureReason: n },
    }));
}
function SO(e) {
  let t = setTimeout(() => {
    let t = dO.get(e);
    t != null &&
      (fO().warning(`Timed out waiting for Codex Computer Use capture`, {
        safe: { requestId: e },
        sensitive: {},
      }),
      xO(e, t, `capture_timed_out`));
  }, pO);
  return (t.unref(), t);
}
function CO(e) {
  let t = wO(e);
  return t?.source === `computer_use` && t.code === mO;
}
function wO(e) {
  if (!(e instanceof Error)) return null;
  let t = e.message.match(hO)?.[1];
  if (t != null) return { code: t, source: `computer_use` };
  let n = e.message.match(gO)?.[1];
  return n == null ? null : { code: n, source: `os_status` };
}
var TO = 12e4,
  EO = n.xl({
    importId: n.wl(),
    itemTypeResults: n.pl(n.xl({ itemType: n.wl(), failures: n.pl(n.El()) })),
  }),
  DO = r.a(`external-agent-import`);
function OO(e) {
  return {
    "external-agent-import-detect": ({
      hostId: t,
      includeHome: n,
      providers: r,
      workspaceRoots: i,
    }) =>
      kO({
        getAppServerConnection: e.getAppServerConnection,
        hostId: t,
        includeHome: n,
        providerAdapters: e.providerAdapters,
        providers: r,
        workspaceRoots: i,
      }),
    "external-agent-import-import": (t) => AO({ ...e, ...t }),
    "external-agent-import-status": (t) => e.getStatus(t),
  };
}
async function kO({
  getAppServerConnection: e,
  hostId: t,
  includeHome: n = !0,
  providerAdapters: r,
  providers: i,
  workspaceRoots: a,
}) {
  let o = Date.now();
  try {
    let s = new Set(i),
      c = r?.[`claude-cowork`],
      [l, u] = await Promise.all([
        s.has(`claude-code`)
          ? IO({
              detect: () =>
                jO({
                  getAppServerConnection: e,
                  hostId: t,
                  includeHome: n,
                  workspaceRoots: a,
                }),
              providerId: `claude-code`,
              stage: `detect`,
            })
          : Promise.resolve({ items: [] }),
        c != null && BO({ includeHome: n, providerSet: s, workspaceRoots: a })
          ? IO({
              detect: () =>
                c.detect({ hostId: t, includeHome: n, workspaceRoots: a }),
              providerId: `claude-cowork`,
              stage: `detect`,
            }).catch(() => ({ items: [], unsupportedProjects: [] }))
          : Promise.resolve({ items: [], unsupportedProjects: [] }),
      ]),
      d = [...l.items, ...u.items],
      f = {
        items: [
          ...(s.has(`claude-code`) && n && a == null && xw(d).length > 0
            ? await IO({
                detect: async () => ({
                  items: await MO({
                    getAppServerConnection: e,
                    hostId: t,
                    items: l.items,
                    rootItems: d,
                  }),
                }),
                providerId: `claude-code`,
                stage: `detect_project_items`,
              }).then((e) => e.items)
            : l.items),
          ...u.items,
        ],
        unsupportedProjects: u.unsupportedProjects,
      };
    return (
      DO().info(`external_agent_import_detect_finished`, {
        safe: {
          status: `success`,
          durationMs: Date.now() - o,
          includeHome: n,
          requestedProviderIds: i.join(`,`),
          unsupportedProjectCount: f.unsupportedProjects.length,
          workspaceRootCount: a?.length ?? 0,
          ...zO(f.items),
        },
      }),
      f
    );
  } catch (e) {
    throw (
      DO().warning(`external_agent_import_detect_finished`, {
        safe: {
          status: `failure`,
          durationMs: Date.now() - o,
          failureKind: `detect_failed`,
          includeHome: n,
          requestedProviderIds: i.join(`,`),
          workspaceRootCount: a?.length ?? 0,
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function AO({
  getAppServerConnection: e,
  hostId: t,
  items: n,
  providerAdapters: r,
}) {
  if (n.length === 0) return { projectRoots: [] };
  let i = Date.now(),
    a = n.filter(VO),
    o = n.filter(HO),
    s = r?.[`claude-cowork`],
    c = null;
  try {
    a.length > 0 &&
      ((c = `claude-code`),
      await LO({
        importItems: async () => {
          await NO({ appServerConnection: e(t), items: a });
        },
        items: a,
        providerId: `claude-code`,
      }));
    let r;
    o.length > 0 &&
      s != null &&
      ((c = `claude-cowork`),
      (r = await LO({
        importItems: () => s.import({ hostId: t, items: o }),
        items: o,
        providerId: `claude-cowork`,
      })));
    let l = {
      projectRoots: [...new Set([...xw(a), ...(r?.projectRoots ?? [])])],
    };
    return (
      DO().info(`external_agent_import_finished`, {
        safe: {
          status: `success`,
          durationMs: Date.now() - i,
          projectRootsReturnedCount: l.projectRoots.length,
          ...zO(n),
        },
      }),
      l
    );
  } catch (e) {
    throw (
      DO().warning(`external_agent_import_finished`, {
        safe: {
          status: `failure`,
          durationMs: Date.now() - i,
          failedProviderId: c,
          failureKind: RO(c),
          ...zO(n),
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function jO({
  getAppServerConnection: e,
  hostId: t,
  includeHome: n,
  workspaceRoots: r,
}) {
  return !n && r == null
    ? { items: [] }
    : {
        items: (
          await e(t).sendAppServerRequest(`externalAgentConfig/detect`, {
            includeHome: n,
            cwds: r,
          })
        ).items.map(UO),
      };
}
async function MO({
  getAppServerConnection: e,
  hostId: t,
  items: n,
  rootItems: r,
}) {
  let i = xw(r);
  if (i.length === 0) return n;
  let a = await e(t).sendAppServerRequest(`externalAgentConfig/detect`, {
    includeHome: !1,
    cwds: i,
  });
  return GO([...n, ...a.items.map(UO)]);
}
async function NO({ appServerConnection: e, items: t }) {
  let n = t.map(WO),
    r = PO(n) ? FO(e) : null;
  try {
    let { importId: t } = await e.sendAppServerRequest(
      `externalAgentConfig/import`,
      { migrationItems: n },
    );
    if (r != null) {
      let e = Date.now(),
        i = n.filter((e) => e.itemType === `SESSIONS`).length;
      try {
        (await r.waitFor(t),
          DO().info(`external_agent_import_session_completion_finished`, {
            safe: {
              status: `success`,
              durationMs: Date.now() - e,
              sessionItemCount: i,
            },
          }));
      } catch (t) {
        throw (
          DO().warning(`external_agent_import_session_completion_finished`, {
            safe: {
              status: `failure`,
              durationMs: Date.now() - e,
              failureKind: `session_completion_failed`,
              sessionItemCount: i,
            },
            sensitive: { error: t },
          }),
          t
        );
      }
    }
  } catch (e) {
    throw (r?.cancel(), e);
  }
}
function PO(e) {
  return e.some((e) => e.itemType === `SESSIONS`);
}
function FO(e) {
  let t = new Map(),
    n = null,
    r = null,
    i = null,
    a = null,
    o = null,
    s = () => {
      (a != null && (clearTimeout(a), (a = null)), o?.(), (o = null));
    },
    c = (e) => {
      (s(),
        e ? r?.(Error(`External agent session import failed`)) : i?.(),
        (r = null),
        (i = null));
    },
    l = new Promise((l, u) => {
      ((r = u),
        (i = l),
        (a = setTimeout(() => {
          (s(),
            u(Error(`Timed out waiting for external agent import completion`)));
        }, TO)),
        (o = e.registerInternalNotificationHandler((e) => {
          if (e.method !== `externalAgentConfig/import/completed`) return;
          let r = EO.safeParse(e.params);
          if (!r.success) return;
          let i = r.data.itemTypeResults.some(
            (e) => e.itemType === `SESSIONS` && e.failures.length > 0,
          );
          if (n == null) {
            t.set(r.data.importId, i);
            return;
          }
          r.data.importId === n && c(i);
        })));
    });
  return {
    cancel: s,
    waitFor: (e) => {
      n = e;
      let r = t.get(e);
      return (r != null && c(r), t.clear(), l);
    },
  };
}
async function IO({ detect: e, providerId: t, stage: n }) {
  let r = Date.now();
  try {
    let i = await e();
    return (
      DO().info(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `success`,
          stage: n,
          providerId: t,
          durationMs: Date.now() - r,
          unsupportedProjectCount: i.unsupportedProjects?.length ?? 0,
          ...zO(i.items),
        },
      }),
      { items: i.items, unsupportedProjects: i.unsupportedProjects ?? [] }
    );
  } catch (e) {
    throw (
      DO().warning(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `failure`,
          stage: n,
          providerId: t,
          durationMs: Date.now() - r,
          failureKind: `detect_failed`,
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function LO({ importItems: e, items: t, providerId: n }) {
  let r = Date.now();
  try {
    let i = await e();
    return (
      DO().info(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `success`,
          stage: `import`,
          providerId: n,
          durationMs: Date.now() - r,
          projectRootsReturnedCount: i?.projectRoots.length ?? 0,
          ...zO(t),
        },
      }),
      i
    );
  } catch (e) {
    throw (
      DO().warning(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `failure`,
          stage: `import`,
          providerId: n,
          durationMs: Date.now() - r,
          failureKind: RO(n),
          ...zO(t),
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
function RO(e) {
  switch (e) {
    case `claude-code`:
      return `claude_code_import_failed`;
    case `claude-cowork`:
      return `cowork_import_failed`;
    case null:
      return `unknown`;
  }
}
function zO(e) {
  let t = 0,
    n = 0,
    r = 0,
    i = 0,
    a = 0,
    o = new Set();
  for (let s of e)
    (o.add(s.providerId),
      s.providerId === `claude-code` && (t += 1),
      s.providerId === `claude-cowork` && (n += 1),
      s.cwd == null || s.cwd === `` ? (r += 1) : (i += 1),
      s.itemType === `SESSIONS` && (a += 1));
  return {
    claudeCodeItemCount: t,
    claudeCoworkItemCount: n,
    homeItemCount: r,
    itemCount: e.length,
    projectItemCount: i,
    providerIds: [...o].sort().join(`,`),
    sessionItemCount: a,
  };
}
function BO({ includeHome: e, providerSet: t, workspaceRoots: n }) {
  return t.has(`claude-cowork`) && (e || n != null);
}
function VO(e) {
  return e.providerId === `claude-code`;
}
function HO(e) {
  return e.providerId === `claude-cowork`;
}
function UO(e) {
  return {
    cwd: e.cwd,
    description: e.description,
    details: e.details,
    itemType: e.itemType,
    providerId: `claude-code`,
  };
}
function WO(e) {
  return {
    cwd: e.cwd,
    description: e.description,
    details: e.details,
    itemType: e.itemType,
  };
}
function GO(e) {
  let t = new Set();
  return e.filter((e) => {
    let n = JSON.stringify({
      cwd: e.cwd ?? ``,
      description: e.description,
      details: e.details,
      itemType: e.itemType,
      providerId: e.providerId,
    });
    return t.has(n) ? !1 : (t.add(n), !0);
  });
}
var KO = `external_agent_session_imports.json`,
  qO = n
    .xl({
      records: n
        .pl(n.xl({ imported_at: n.bl().optional() }).passthrough())
        .default([]),
    })
    .passthrough();
async function JO({ appServerClient: e }) {
  if (n.eo(e.hostConfig)) return YO;
  let t = await e.codexHome(),
    r = (await e.platformPath()).join(t, KO),
    i = await n.ti.readFile(r, e).catch(() => null);
  return i == null ? YO : XO(i);
}
var YO = { importedSessionCount: 0, latestImportedAtMs: null };
function XO(e) {
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return YO;
  }
  let n = qO.safeParse(t);
  if (!n.success) return YO;
  let r = null;
  for (let e of n.data.records) {
    if (e.imported_at == null) continue;
    let t = e.imported_at * 1e3;
    r = r == null ? t : Math.max(r, t);
  }
  return { importedSessionCount: n.data.records.length, latestImportedAtMs: r };
}
var ZO = { importedSessionCount: 0, latestImportedAtMs: null };
async function QO({
  appServerClient: e,
  codexHomeForLocalFs: t,
  includeClaudeCode: n,
  includeCowork: r,
}) {
  let [i, a] = await Promise.all([
    n ? JO({ appServerClient: e }) : ZO,
    r ? tk({ appServerClient: e, codexHomeForLocalFs: t }) : ZO,
  ]);
  return ek([i, a]);
}
async function $O({ appServerClient: e, providers: t }) {
  let n = new Set(t);
  return QO({
    appServerClient: e,
    includeClaudeCode: n.has(`claude-code`),
    includeCowork: n.has(`claude-cowork`),
  });
}
function ek(e) {
  return {
    importedSessionCount: e.reduce((e, t) => e + t.importedSessionCount, 0),
    latestImportedAtMs: e.reduce(
      (e, t) =>
        t.latestImportedAtMs == null
          ? e
          : Math.max(e ?? t.latestImportedAtMs, t.latestImportedAtMs),
      null,
    ),
  };
}
async function tk({ appServerClient: e, codexHomeForLocalFs: t }) {
  return n.eo(e.hostConfig) ? ZO : GS(t ?? (await nk(e)));
}
async function nk(e) {
  let t = await e.codexHome();
  return n.ui(e.hostConfig) ? n.ii(t, n.ai()) : t;
}
var rk = new Map([
  [`figma`, `connector_figma`],
  [`github`, `connector_github`],
  [`gmail`, `connector_gmail`],
  [`google calendar`, `connector_google_calendar`],
  [`google drive`, `connector_google_drive`],
  [`linear`, `connector_linear`],
  [`notion`, `connector_notion`],
  [`slack`, `connector_slack`],
]);
async function ik({ codexHome: e, providers: t }) {
  return t.includes(`claude-cowork`)
    ? { connectors: (await WS(e)).connectorCandidates.map(ak) }
    : { connectors: [] };
}
function ak(e) {
  return {
    codexAppId: rk.get(e.name.trim().toLowerCase()) ?? null,
    name: e.name,
    sessionCount: e.sessionCount,
    source: e.source,
  };
}
var ok = 6e4,
  sk = [`scripts`, `compile_latex.py`],
  ck =
    process.platform === `win32`
      ? [`python`, `python3`]
      : [`python3`, `python`];
async function lk(e, t) {
  if (t == null) return { contentsBase64: null };
  let n = new AbortController(),
    r = setTimeout(() => {
      n.abort();
    }, ok),
    i = null;
  try {
    ((i = await (0, m.mkdtemp)(
      u.default.join(l.default.tmpdir(), `codex-latex-artifact-`),
    )),
      await (0, m.mkdir)(i, { recursive: !0 }));
    let r = await uk({
      abortSignal: n.signal,
      latexPluginPath: t,
      outputDirectory: i,
      sourcePath: e,
    });
    if (r.contentsBase64 != null) return r;
    let a = hk(t);
    return await pk({
      abortSignal: n.signal,
      outputDirectory: i,
      sourcePath: e,
      tectonicExecutablePath: a,
    });
  } catch {
    return { contentsBase64: null };
  } finally {
    (clearTimeout(r),
      i != null &&
        (await (0, m.rm)(i, { recursive: !0, force: !0 }).catch(() => void 0)));
  }
}
async function uk({
  abortSignal: e,
  latexPluginPath: t,
  outputDirectory: n,
  sourcePath: r,
}) {
  let i = u.default.join(t, ...sk);
  try {
    await (0, m.access)(i);
  } catch {
    return { contentsBase64: null };
  }
  return dk({
    abortSignal: e,
    latexPluginPath: t,
    outputDirectory: n,
    pythonExecutables: ck,
    scriptPath: i,
    sourcePath: r,
  });
}
async function dk({
  abortSignal: e,
  latexPluginPath: t,
  outputDirectory: n,
  pythonExecutables: r,
  scriptPath: i,
  sourcePath: a,
}) {
  let [o, ...s] = r;
  if (o == null) return { contentsBase64: null };
  let c = await fk({
    abortSignal: e,
    latexPluginPath: t,
    outputDirectory: n,
    pythonExecutable: o,
    scriptPath: i,
    sourcePath: a,
  });
  return c.contentsBase64 == null
    ? dk({
        abortSignal: e,
        latexPluginPath: t,
        outputDirectory: n,
        pythonExecutables: s,
        scriptPath: i,
        sourcePath: a,
      })
    : c;
}
async function fk({
  abortSignal: e,
  latexPluginPath: t,
  outputDirectory: r,
  pythonExecutable: i,
  scriptPath: a,
  sourcePath: o,
}) {
  try {
    let s = await n
      .Yn({
        args: [i, a, o, `--output-directory`, r, `--json`],
        cwd: t,
        spawnInsideWsl: !1,
        signal: e,
      })
      .wait();
    if (s.code !== 0) return { contentsBase64: null };
    let c = mk(s.stdout);
    return c?.pdfExists !== !0 || typeof c.pdfPath != `string`
      ? { contentsBase64: null }
      : {
          contentsBase64: (await (0, m.readFile)(c.pdfPath)).toString(`base64`),
        };
  } catch {
    return { contentsBase64: null };
  }
}
async function pk({
  abortSignal: e,
  outputDirectory: t,
  sourcePath: r,
  tectonicExecutablePath: i,
}) {
  let a = u.default.dirname(r),
    o = u.default.basename(r);
  try {
    return (
      await (0, m.access)(i),
      (
        await n
          .Yn({
            args: [
              i,
              `-X`,
              `compile`,
              `--outdir`,
              t,
              `--outfmt`,
              `pdf`,
              `--print`,
              `--untrusted`,
              o,
            ],
            cwd: a,
            env: { TECTONIC_UNTRUSTED_MODE: `1` },
            collectOutput: !1,
            spawnInsideWsl: !1,
            signal: e,
          })
          .wait()
      ).code === 0
        ? {
            contentsBase64: (
              await (0, m.readFile)(
                u.default.join(t, o.replace(/\.[^.]+$/u, `.pdf`)),
              )
            ).toString(`base64`),
          }
        : { contentsBase64: null }
    );
  } catch {
    return { contentsBase64: null };
  }
}
function mk(e) {
  try {
    return JSON.parse(e.trim());
  } catch {
    return null;
  }
}
function hk(e, t = process.platform) {
  return (t === `win32` ? u.default.win32 : u.default.posix).join(
    e,
    `bin`,
    t === `win32` ? `tectonic.exe` : `tectonic`,
  );
}
async function gk(e) {
  try {
    let [t, { marketplaces: n }] = await Promise.all([
        e.codexHome(),
        e.listPlugins(o.l),
      ]),
      r = _k({ codexHome: t, marketplaces: n });
    return r == null ? null : (await (0, m.access)(r), r);
  } catch {
    return null;
  }
}
function _k({
  codexHome: e,
  marketplaces: t,
  marketplaceName: r = n.Ms(i.a.resolve()),
}) {
  for (let i of nn({ marketplaceName: r, marketplaces: t })) {
    let t = i.plugins.find(
      (e) =>
        e.name === `latex` &&
        e.installed &&
        e.enabled &&
        e.source.type === `local`,
    );
    if (t?.source.type === `local`)
      return n.Is({
        codexHome: e,
        localVersion: t.localVersion,
        marketplaceName: i.name,
        pluginName: t.name,
      });
  }
  return null;
}
var vk = r.a(`download-internal-plugins`);
async function yk({
  config: e,
  largeInternalPluginsConfig: t,
  downloadPlugins: r = n.kt,
  errorReporter: i,
}) {
  try {
    return {
      marketplaceUpdated: t == null ? await r(e) : await r(e, void 0, t),
    };
  } catch (t) {
    throw (
      (t instanceof TypeError && t.message === `fetch failed`) ||
        i.reportNonFatal(t instanceof Error ? t : Error(String(t)), {
          kind: `download-internal-plugins`,
          tags: { version: e.version },
        }),
      vk().error(`Failed to download internal plugins`, {
        safe: { version: e.version },
        sensitive: { error: t },
      }),
      t
    );
  }
}
function bk(e) {
  if (e !== `local`)
    throw Error(
      `Internal plugins download is currently only supported on the local host.`,
    );
}
async function xk({ query: e, cwd: t, limit: r }) {
  let i = e.trim();
  if (i.length === 0 || !t) return [];
  let a = u.default.resolve(t),
    o = Ck();
  return (
    (await n.Mt(i, [a], r, {
      rgPath: o ?? void 0,
      runCommand: o == null ? Sk : void 0,
    })) ?? []
  );
}
function Sk() {
  return Promise.resolve({ stdout: ``, code: 2 });
}
function Ck() {
  return n.hr(process.resourcesPath);
}
function wk(e, t) {
  let n = e.split(`+`).filter(Boolean),
    r = n.findIndex(Tk);
  if (r === -1) return e;
  let i = n[r];
  if (i == null) return e;
  let a = Ek(i, t);
  return a == null ? e : n.map((e, t) => (t === r ? a : e)).join(`+`);
}
function Tk(e) {
  return /^[A-Z0-9]$/i.test(e);
}
function Ek(e, t) {
  let n = e.toLowerCase();
  for (let [e, r] of Object.entries(t))
    if (r.toLowerCase() === n) {
      if (/^Key[A-Z]$/.test(e)) return e.slice(3);
      if (/^Digit[0-9]$/.test(e)) return e.slice(5);
    }
  return null;
}
var Dk = `bare-modifier-monitor`,
  Ok = new Set(),
  kk = new Map([
    [`fn`, `Fn`],
    [`leftoption`, `LeftOption`],
    [`leftalt`, `LeftOption`],
    [`rightoption`, `RightOption`],
    [`rightalt`, `RightOption`],
    [`doubleoption`, `DoubleOption`],
    [`doublealt`, `DoubleOption`],
    [`leftoption+rightoption`, `DoubleOption`],
    [`leftalt+rightalt`, `DoubleOption`],
    [`leftcommand`, `LeftCommand`],
    [`leftcmd`, `LeftCommand`],
    [`leftmeta`, `LeftCommand`],
    [`doublecommand`, `DoubleCommand`],
    [`leftcommand+rightcommand`, `DoubleCommand`],
    [`leftcmd+rightcmd`, `DoubleCommand`],
    [`leftmeta+rightmeta`, `DoubleCommand`],
    [`rightcommand`, `RightCommand`],
    [`rightcmd`, `RightCommand`],
    [`rightmeta`, `RightCommand`],
    [`leftcontrol`, `LeftControl`],
    [`leftctrl`, `LeftControl`],
    [`doubleshift`, `DoubleShift`],
    [`leftshift+rightshift`, `DoubleShift`],
  ]),
  Ak = new Set([`rightcontrol`, `rightctrl`, `leftshift`, `rightshift`]),
  jk = new Set([`DoubleOption`, `DoubleCommand`, `DoubleShift`]);
function Mk(e, t, n = `press`) {
  if (process.platform !== `darwin`) return null;
  let r = zk(e);
  if (r == null) return null;
  let i = qk();
  return i == null || Ok.has(r)
    ? null
    : (Ok.add(r), Vk(Uk(i, r, { trigger: n }), r, t));
}
function Nk(e, t = `press`) {
  let n = zk(e);
  process.platform !== `darwin` ||
    n == null ||
    t === `immediatePress` ||
    jk.has(n) ||
    Fk();
}
function Pk(e, t = 3e4) {
  if (process.platform !== `darwin`) return Promise.resolve(null);
  let n = zk(e),
    i = qk();
  return n == null || i == null
    ? Promise.resolve(null)
    : (Nk(n, `immediatePress`),
      new Promise((e) => {
        let a = !1,
          o = Uk(i, n, { immediate: !0 }),
          s = null,
          c = (t) => {
            a || ((a = !0), s != null && clearTimeout(s), o.kill(), e(t));
          };
        ((s = setTimeout(() => {
          c(null);
        }, t)),
          s.unref(),
          Kk(o, (e) => {
            switch (e) {
              case `down`:
                c(n);
                return;
              case `permission-denied`:
                (r
                  .r()
                  .warning(
                    `Bare modifier hotkey monitor needs input monitoring access`,
                  ),
                  c(null));
                return;
              case `ready`:
              case `up`:
              case ``:
                return;
              default:
                return;
            }
          }),
          o.once(`error`, (e) => {
            (r.r().warning(`Bare modifier hotkey capture failed`, {
              safe: {},
              sensitive: { error: e },
            }),
              c(null));
          }),
          o.once(`exit`, () => {
            c(null);
          }));
      }));
}
function Fk() {
  if (process.platform !== `darwin`) return;
  let e = qk();
  if (e != null)
    try {
      (0, h.spawnSync)(e, [`--request-permission`], {
        stdio: `ignore`,
        timeout: 1e4,
      });
    } catch (e) {
      r.r().warning(`Failed to request input monitoring access`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
function Ik(e, t) {
  if (process.platform !== `darwin` || e.length === 0) return null;
  let n = qk();
  return n == null ? null : Gk(Wk(n, e), t);
}
function Lk(e, t = process.platform) {
  return t === `darwin` && zk(e) != null;
}
function Rk(e) {
  let t = Bk(e);
  return kk.has(t) || Ak.has(t);
}
function zk(e) {
  return kk.get(Bk(e)) ?? null;
}
function Bk(e) {
  return e.replace(/[\s_-]/g, ``).toLowerCase();
}
function Vk(e, t, n) {
  let i = !1;
  return (
    Kk(e, (e) => {
      Hk(e, n);
    }),
    e.once(`error`, (e) => {
      i ||
        r.r().warning(`Bare modifier hotkey monitor failed`, {
          safe: {},
          sensitive: { error: e },
        });
    }),
    e.once(`exit`, () => {
      (Ok.delete(t),
        !i && r.r().warning(`Bare modifier hotkey monitor exited`));
    }),
    {
      handlesRelease: !0,
      unregister: () => {
        ((i = !0), Ok.delete(t), e.kill());
      },
    }
  );
}
function Hk(e, t) {
  switch (e) {
    case `ready`:
      return;
    case `down`:
      t.onPressed();
      return;
    case `up`:
      t.onReleased?.();
      return;
    case `permission-denied`:
      r.r().warning(
        `Bare modifier hotkey monitor needs input monitoring access`,
      );
      return;
    case ``:
      return;
    default:
      return;
  }
}
function Uk(e, t, n) {
  let r = [`--key`, t];
  return (
    n?.immediate === !0 || n?.trigger === `immediatePress`
      ? r.push(`--immediate`)
      : n?.trigger === `release` && r.push(`--trigger-on-release`),
    (0, h.spawn)(e, r, { stdio: [`pipe`, `pipe`, `ignore`] })
  );
}
function Wk(e, t) {
  return (0, h.spawn)(e, [`--release-modifiers`, t.join(`,`)], {
    stdio: [`pipe`, `pipe`, `ignore`],
  });
}
function Gk(e, t) {
  let n = !1,
    i = (i) => {
      n ||
        ((n = !0),
        e.kill(),
        i != null &&
          r.r().warning(`Mac hotkey release monitor failed`, {
            safe: {},
            sensitive: { error: i },
          }),
        t());
    };
  return (
    Kk(e, (e) => {
      switch (e) {
        case `up`:
        case `permission-denied`:
          i();
          return;
        case `ready`:
        case `down`:
        case ``:
          return;
        default:
          return;
      }
    }),
    e.once(`error`, i),
    e.once(`exit`, () => {
      i();
    }),
    {
      dispose: () => {
        ((n = !0), e.kill());
      },
    }
  );
}
function Kk(e, t) {
  let n = ``;
  e.stdout?.on(`data`, (e) => {
    n += e.toString(`utf8`);
    let r = n.indexOf(`
`);
    for (; r !== -1; )
      (t(n.slice(0, r).trim()),
        (n = n.slice(r + 1)),
        (r = n.indexOf(`
`)));
  });
}
function qk() {
  for (let e of Jk()) if ((0, p.existsSync)(e)) return e;
  return null;
}
function Jk() {
  if (typeof c.app.getAppPath != `function`) return [];
  let e = u.default.join(
    c.app.getAppPath(),
    `native`,
    Dk,
    `build`,
    `Release-${process.arch}`,
    Dk,
  );
  return process.resourcesPath == null
    ? [e]
    : [u.default.join(process.resourcesPath, `native`, Dk), e];
}
var Yk = new Set([
    `cmdorctrl`,
    `command`,
    `cmd`,
    `control`,
    `ctrl`,
    `alt`,
    `option`,
  ]),
  Xk = new Set([...Yk, `shift`]),
  Zk = new Set(),
  Qk = r.a(`global-hotkey`),
  $k = null;
function eA(e, t, n) {
  if (Rk(e)) return Lk(e) ? Mk(e, t, n?.bareModifierTrigger) : null;
  let r = oA(e),
    i = () => {
      (Qk().debug(`global_hotkey_pressed`, {
        safe: { hotkey: e },
        sensitive: {},
      }),
        t.onPressed());
    },
    a = c.globalShortcut.register(r, i);
  return (
    Qk().debug(`register_global_hotkey`, {
      safe: {
        hotkey: e,
        registrationHotkey: r,
        platform: process.platform,
        registered: a,
      },
      sensitive: {},
    }),
    a
      ? process.platform === `darwin`
        ? sA({ hotkey: e, onPressed: i, registrationHotkey: r })
        : {
            handlesRelease: !1,
            unregister: () => {
              c.globalShortcut.unregister(r);
            },
          }
      : null
  );
}
function tA(e) {
  process.platform === `darwin` &&
    (($k = e),
    Qk().debug(`updated_mac_keyboard_layout_map`, {
      safe: { entryCount: Object.keys(e).length },
      sensitive: {},
    }),
    cA());
}
function nA(e, t = process.platform) {
  let n = aA(e);
  if (Lk(e, t)) return null;
  if (n.some(Rk))
    return n.length === 1
      ? t === `darwin`
        ? Lk(e, t)
          ? null
          : `This shortcut key is not supported.`
        : `Choose a shortcut with Ctrl or Alt plus another key.`
      : `Use Ctrl, Alt, or Command when combining with another key.`;
  if (n.length === 0) return `Shortcut cannot be empty.`;
  let r = !1,
    i = null;
  for (let e of n) {
    let t = e.toLowerCase();
    if (Xk.has(t)) {
      Yk.has(t) && (r = !0);
      continue;
    }
    if (i != null) return `Shortcut must include exactly one non-modifier key.`;
    i = e;
  }
  return i == null
    ? `Shortcut must include a non-modifier key.`
    : r
      ? null
      : `Shortcut must include Cmd/Ctrl or Alt.`;
}
function rA(e, t) {
  Nk(e, t?.bareModifierTrigger);
}
function iA(e) {
  if (process.platform === `darwin`) {
    Fk();
    return;
  }
  rA(e);
}
function aA(e) {
  return e
    .split(`+`)
    .map((e) => e.trim())
    .filter((e) => e.length > 0);
}
function oA(e) {
  return process.platform !== `darwin` || $k == null ? e : wk(e, $k);
}
function sA(e) {
  return (
    Zk.add(e),
    {
      handlesRelease: !1,
      unregister: () => {
        (Zk.delete(e), c.globalShortcut.unregister(e.registrationHotkey));
      },
    }
  );
}
function cA() {
  if ($k != null)
    for (let e of Zk) {
      let t = wk(e.hotkey, $k);
      if (t === e.registrationHotkey) continue;
      let n = c.globalShortcut.register(t, e.onPressed);
      (Qk().debug(`refresh_mac_global_hotkey`, {
        safe: {
          hotkey: e.hotkey,
          previousRegistrationHotkey: e.registrationHotkey,
          registrationHotkey: t,
          registered: n,
        },
        sensitive: {},
      }),
        n &&
          (c.globalShortcut.unregister(e.registrationHotkey),
          (e.registrationHotkey = t)));
    }
}
var lA = `
$ErrorActionPreference = "Stop"

Add-Type @"
using System;
using System.Runtime.InteropServices;

public static class CodexKeyboardState {
  [DllImport("user32.dll")]
  private static extern short GetAsyncKeyState(int key);

  public static bool IsDown(int key) {
    return (GetAsyncKeyState(key) & 0x8000) != 0;
  }
}
"@

$keyGroupSpec = __KEY_GROUP_SPEC__
if ([string]::IsNullOrWhiteSpace($keyGroupSpec)) {
  throw "Missing hotkey release key groups."
}

$keyGroups = $keyGroupSpec.Split(";") | ForEach-Object {
  ,($_.Split(",") | ForEach-Object { [int]$_ })
}

while ($true) {
  foreach ($group in $keyGroups) {
    $groupDown = $false
    foreach ($key in $group) {
      if ([CodexKeyboardState]::IsDown($key)) {
        $groupDown = $true
        break
      }
    }
    if (-not $groupDown) {
      exit 0
    }
  }
  Start-Sleep -Milliseconds 40
}
`,
  uA = new Map([
    [`cmdorctrl`, `command`],
    [`command`, `command`],
    [`cmd`, `command`],
    [`control`, `control`],
    [`ctrl`, `control`],
    [`alt`, `alternate`],
    [`option`, `alternate`],
    [`shift`, `shift`],
  ]),
  dA = new Map([
    [`cmdorctrl`, [17]],
    [`command`, [91, 92]],
    [`cmd`, [91, 92]],
    [`control`, [17]],
    [`ctrl`, [17]],
    [`alt`, [18]],
    [`option`, [18]],
    [`shift`, [16]],
  ]);
function fA(e) {
  return (
    nA(e) ??
    (Lk(e) || bA(e, process.platform)
      ? null
      : `Shortcut key is not supported for global dictation.`)
  );
}
function pA(e, t) {
  switch (process.platform) {
    case `darwin`: {
      let n = Ik(mA(e), t);
      if (n == null)
        throw Error(
          `Global dictation hotkey release watching is not supported.`,
        );
      return n;
    }
    case `win32`: {
      let n = gA(e, process.platform);
      if (n == null)
        throw Error(
          `Global dictation hotkey release watching is not supported.`,
        );
      return _A(
        (0, h.spawn)(
          `powershell.exe`,
          [
            `-NoProfile`,
            `-NonInteractive`,
            `-ExecutionPolicy`,
            `Bypass`,
            `-Command`,
            hA(n.map((e) => e.join(`,`)).join(`;`)),
          ],
          { stdio: `ignore`, windowsHide: !0 },
        ),
        t,
      );
    }
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      throw Error(`Global dictation hotkey release watching is not supported.`);
  }
}
function mA(e) {
  let t = [];
  for (let n of e.split(`+`)) {
    let e = uA.get(n.trim().toLowerCase());
    e != null && !t.includes(e) && t.push(e);
  }
  return t;
}
function hA(e) {
  return lA.replace(`__KEY_GROUP_SPEC__`, yA(e));
}
function gA(e, t) {
  let n = [];
  for (let r of e.split(`+`)) {
    let e = vA(r, t);
    e != null && n.push(e);
  }
  return n.length === 0 ? null : n;
}
function _A(e, t) {
  let n = !1,
    i = (e) => {
      n ||
        ((n = !0),
        e != null &&
          r.r().warning(`Global dictation hotkey release watching failed`, {
            safe: {},
            sensitive: { error: e },
          }),
        t());
    };
  return (
    e.once(`error`, i),
    e.once(`exit`, () => i()),
    {
      dispose: () => {
        ((n = !0), e.kill());
      },
    }
  );
}
function vA(e, t) {
  let n = e.trim().toLowerCase();
  switch (t) {
    case `darwin`:
      return null;
    case `win32`:
      return dA.get(n) ?? null;
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      return null;
  }
}
function yA(e) {
  return `'${e.replace(/'/g, `''`)}'`;
}
function bA(e, t) {
  return t === `darwin` ? mA(e).length > 0 : gA(e, t) != null;
}
var xA = (0, d.promisify)(h.execFile),
  SA = Promise.resolve(),
  CA = n.xl({ appPath: n.wl(), bundleId: n.wl(), displayName: n.wl() }),
  wA = n.pl(CA),
  TA = n.xl({ applications: wA, defaultApplicationPath: n.wl().nullish() });
async function EA(e) {
  return jA([e], wA);
}
async function DA() {
  return jA([`list-web-url-handlers`], TA);
}
async function OA(e) {
  await jA([`notify-download-complete`, e], n.vl(!0));
}
function kA({ preference: e, resourceName: t }) {
  if (process.platform !== `darwin` || !c.app.isPackaged)
    return Promise.resolve(!1);
  let n = AA(),
    r = NA();
  if (n == null || r == null) return Promise.resolve(!1);
  let i = SA.then(async () => {
    try {
      return (
        await xA(n, [`set-app-icon`, r, e, t ?? ``], { timeout: 5e3 }),
        !0
      );
    } catch {
      return !1;
    }
  });
  return ((SA = i.then(() => void 0)), i);
}
function AA() {
  for (let e of MA()) if ((0, p.existsSync)(e)) return e;
  return null;
}
async function jA(e, t) {
  if (process.platform !== `darwin`) return null;
  let n = AA();
  if (n == null) return null;
  try {
    let { stdout: r } = await xA(n, e, { timeout: 2e3 });
    return t.parse(JSON.parse(r));
  } catch {
    return null;
  }
}
function MA() {
  return [
    u.default.join(process.resourcesPath, `native`, `launch-services-helper`),
    u.default.join(
      c.app.getAppPath(),
      `native`,
      `launch-services-helper`,
      `build`,
      `Release-${process.arch}`,
      `launch-services-helper`,
    ),
  ];
}
function NA() {
  let e = u.default.dirname(u.default.dirname(process.execPath)),
    t = u.default.dirname(e);
  return t.endsWith(`.app`) ? t : null;
}
var PA = r.a(`link-opening`);
function FA(e) {
  return (
    e?.get(n.Vi.openLinkInTargetPreference.key) ??
    Ke().defaultLinkOpenTargetPreference
  );
}
async function IA({
  request: e,
  targetWebContents: t,
  settingsStore: r,
  queueCodexDeepLinkUrl: i,
  errorKind: a,
  errorReporter: o,
}) {
  let { url: s } = e,
    l = e.initiator ?? `open_in_browser_bridge`,
    u = (t) => {
      (PA().error(`Failed to open link`, {
        safe: { conversationId: e.conversationId, initiator: l },
        sensitive: { error: t, url: s },
      }),
        o?.reportNonFatal(t instanceof Error ? t : n.Rs(t), {
          kind: a ?? `open-link-in-preferred-target`,
        }));
    };
  if (i?.(s, e.originHostId) === !0) return !0;
  let d = Ke(),
    p = FA(r),
    m =
      r?.getEffective(n.Vi.openLocalUrlInTargetPreference.key) ??
      n.Vi.openLocalUrlInTargetPreference.default;
  if (
    n._s({
      browserPaneEnabled: d.browserPane,
      linksDefaultInAppBrowser: d.linksDefaultInAppBrowser,
      openLinkInTargetPreference: p,
      openLocalUrlInTargetPreference: m,
      openTarget: e.openTarget,
      openTargetIntent: e.openTargetIntent,
      url: s,
      useExternalBrowser: e.useExternalBrowser,
    }) === `in-app-browser`
  )
    return t == null
      ? (u(Error(`In-app browser target requires web contents`)), !1)
      : (t.send(B, {
          type: `toggle-browser-panel`,
          ...(e.disposition === `new-tab`
            ? { browserTabId: n._c(`manual:${(0, f.randomUUID)()}`) }
            : {}),
          open: !0,
          url: s,
          hostId: e.hostId,
          source: e.source ?? `manual`,
          initiator: l,
        }),
        !0);
  if (!n.gs(s))
    return (
      PA().warning(`Invalid link URL`, {
        safe: { initiator: l },
        sensitive: { url: s },
      }),
      !1
    );
  try {
    let e = s,
      t = new URL(s);
    return (
      t.protocol === `https:` &&
        t.hostname === `chatgpt.com` &&
        (t.searchParams.set(`no_universal_links`, `1`), (e = t.toString())),
      await c.shell.openExternal(e),
      !0
    );
  } catch (e) {
    return (u(e), !1);
  }
}
var LA = (0, d.promisify)(h.execFile);
async function RA(e) {
  return e.trim() === ``
    ? null
    : process.platform === `darwin`
      ? zA(e)
      : process.platform === `win32`
        ? va(e)
        : null;
}
async function zA(e) {
  let t = await BA(e);
  if (t == null) return null;
  try {
    if (t.endsWith(`.icns`)) return await HA(t);
    let e = c.nativeImage.createFromPath(t);
    return e.isEmpty() ? null : e.toDataURL();
  } catch {
    return null;
  }
}
async function BA(e) {
  if (!e.endsWith(`.app`)) return null;
  let t = u.join(e, `Contents`, `Resources`),
    n = u.join(e, `Contents`, `Info.plist`),
    r = new Set(),
    i = await VA(n);
  return (
    i != null &&
      i !== `` &&
      (r.add(u.join(t, i.endsWith(`.icns`) ? i : `${i}.icns`)),
      r.add(u.join(t, i))),
    r.add(u.join(t, `AppIcon.icns`)),
    (
      await Promise.all(
        [...r].map(async (e) => {
          try {
            return (await (0, m.access)(e), e);
          } catch {
            return null;
          }
        }),
      )
    ).find((e) => e != null) ?? null
  );
}
async function VA(e) {
  try {
    let { stdout: t } = await LA(
        `plutil`,
        [`-extract`, `CFBundleIconFile`, `raw`, `-o`, `-`, e],
        { encoding: `utf8`, env: r.t(process.env) },
      ),
      n = t.trim();
    return n === `` ? null : n;
  } catch {
    return null;
  }
}
async function HA(e) {
  let t = await (0, m.mkdtemp)(u.join((0, l.tmpdir)(), `codex-app-icon-`)),
    n = u.join(t, `${u.basename(e)}.png`);
  try {
    return (
      await LA(`qlmanage`, [`-t`, `-s`, `32`, `-o`, t, e], {
        env: r.t(process.env),
      }),
      `data:image/png;base64,${(await (0, m.readFile)(n)).toString(`base64`)}`
    );
  } catch {
    return null;
  } finally {
    await (0, m.rm)(t, { recursive: !0, force: !0 });
  }
}
var UA = new Map([
    [`pdf`, [`com.apple.Preview`]],
    [`png`, [`com.apple.Preview`]],
    [`jpg`, [`com.apple.Preview`]],
    [`jpeg`, [`com.apple.Preview`]],
    [`gif`, [`com.apple.Preview`]],
    [`bmp`, [`com.apple.Preview`]],
    [`tiff`, [`com.apple.Preview`]],
    [`ico`, [`com.apple.Preview`]],
    [`webp`, [`com.apple.Preview`]],
    [`mov`, [`com.apple.QuickTimePlayerX`]],
    [`mp4`, [`com.apple.QuickTimePlayerX`]],
    [`m4v`, [`com.apple.QuickTimePlayerX`]],
    [`avi`, [`com.apple.QuickTimePlayerX`]],
    [`mkv`, [`com.apple.QuickTimePlayerX`]],
    [`webm`, [`com.apple.QuickTimePlayerX`]],
    [`mpg`, [`com.apple.QuickTimePlayerX`]],
    [`mpeg`, [`com.apple.QuickTimePlayerX`]],
    [`mp3`, [`com.apple.QuickTimePlayerX`]],
    [`wav`, [`com.apple.QuickTimePlayerX`]],
    [`m4a`, [`com.apple.QuickTimePlayerX`]],
    [`aac`, [`com.apple.QuickTimePlayerX`]],
    [`aiff`, [`com.apple.QuickTimePlayerX`]],
    [`aif`, [`com.apple.QuickTimePlayerX`]],
    [`flac`, [`com.apple.QuickTimePlayerX`]],
    [`caf`, [`com.apple.QuickTimePlayerX`]],
    [`zip`, [`com.apple.archiveutility`]],
    [`tar`, [`com.apple.archiveutility`]],
    [`gz`, [`com.apple.archiveutility`]],
    [`tgz`, [`com.apple.archiveutility`]],
    [`bz2`, [`com.apple.archiveutility`]],
    [`tbz`, [`com.apple.archiveutility`]],
    [`xz`, [`com.apple.archiveutility`]],
    [`txz`, [`com.apple.archiveutility`]],
    [`7z`, [`com.apple.archiveutility`]],
    [`rar`, [`com.apple.archiveutility`]],
    [`sketch`, [`com.bohemiancoding.sketch3`]],
    [`fig`, [`com.figma.Desktop`]],
    [`psd`, [`com.adobe.Photoshop`]],
    [`ai`, [`com.adobe.illustrator`]],
    [`indd`, [`com.adobe.InDesign`]],
    [`xd`, [`com.adobe.xd`]],
    [`doc`, [`com.microsoft.Word`, `com.apple.iWork.Pages`]],
    [`docx`, [`com.microsoft.Word`, `com.apple.iWork.Pages`]],
    [`xls`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`xlsx`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`csv`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`ppt`, [`com.microsoft.Powerpoint`, `com.apple.iWork.Keynote`]],
    [`pptx`, [`com.microsoft.Powerpoint`, `com.apple.iWork.Keynote`]],
    [`key`, [`com.apple.iWork.Keynote`]],
    [`pages`, [`com.apple.iWork.Pages`]],
    [`numbers`, [`com.apple.iWork.Numbers`]],
  ]),
  WA = new Set([...UA.keys(), `html`]);
function GA(e) {
  let t = u.default.extname(e).slice(1).toLowerCase();
  return UA.get(t) ?? [];
}
function KA(e, t = process.platform) {
  return t === `darwin` && YA(e);
}
function qA(e, t = process.platform) {
  return t !== `darwin` && YA(e);
}
function JA(e, t = process.platform) {
  return KA(e, t) || qA(e, t);
}
function YA(e) {
  if (!(0, p.existsSync)(e) || !(0, p.statSync)(e).isFile()) return !1;
  let t = u.default.extname(e).toLowerCase();
  return t ? WA.has(t.slice(1)) : !1;
}
function XA({
  requestedTarget: e,
  requestedTargetWasFiltered: t,
  preferredTarget: n,
  shouldUseFileManager: r,
  shouldUsePlatformDefaultViewer: i,
  hasExplicitTarget: a,
  allowPlatformFallback: o = !0,
  platform: s,
}) {
  if (t)
    return ZA({
      target: null,
      hasExplicitTarget: a,
      allowPlatformFallback: o,
      platform: s,
    });
  let c = e ?? n;
  return (
    !a && e == null && (r ? (c = `fileManager`) : i && (c = `systemDefault`)),
    ZA({
      target: c,
      hasExplicitTarget: a,
      allowPlatformFallback: o,
      platform: s,
    })
  );
}
function ZA({
  target: e,
  hasExplicitTarget: t,
  allowPlatformFallback: n = !0,
  platform: r,
}) {
  return e || (!t && n && r === `win32` ? `fileManager` : null);
}
var QA = n.xl({
  sshAlias: n.wl().nullable(),
  sshHost: n.wl(),
  sshPort: n.bl().nullable(),
  identity: n.wl().nullable(),
});
function $A(e) {
  if (e.kind === `ssh`) {
    let t = rj(tj(e));
    if (t) return t;
    let n = ij(Array.isArray(e.terminal_command) ? e.terminal_command : []);
    if (n) return n;
  }
  return e.name?.trim() || nj(e.id);
}
function ej(e) {
  let t = tj(e),
    n = rj(t);
  if (n) return n;
  if (t != null) {
    if (t.identity?.trim()) return null;
    let e = t.sshHost.trim();
    if (e.length > 0) return sj(e, t.sshPort);
  }
  let r = Array.isArray(e.terminal_command) ? e.terminal_command : [];
  if (oj(r)) return null;
  let i = ij(r);
  return i ? sj(i, aj(r)) : e.name?.trim() || nj(e.id);
}
function tj(e) {
  let t = QA.safeParse(e[k_]);
  return t.success ? t.data : null;
}
function nj(e) {
  let t = e.trim();
  return t.split(/[:/]/).at(-1)?.trim() || t;
}
function rj(e) {
  return e?.sshAlias?.trim() || null;
}
function ij(e) {
  return e.at(-1)?.trim() || null;
}
function aj(e) {
  let t = e.find((e) => /^-p\d+$/.test(e));
  if (t != null) return Number(t.slice(2));
  let n = e.indexOf(`-p`),
    r = n >= 0 ? e[n + 1] : null;
  return r == null || !/^\d+$/.test(r) ? null : Number(r);
}
function oj(e) {
  return e.some((e) => e === `-i` || /^-i\S+/.test(e));
}
function sj(e, t) {
  let n = cj(e);
  return t == null ? n : `${n}:${t}`;
}
function cj(e) {
  let t = e.lastIndexOf(`@`),
    n = t >= 0 ? e.slice(0, t + 1) : ``,
    r = t >= 0 ? e.slice(t + 1) : e;
  return !r.includes(`:`) || r.startsWith(`[`) ? e : `${n}[${r}]`;
}
function lj(e, t, r) {
  if (!n.eo(e)) return t;
  let i = new Map(r.map((e) => [e.id, e]));
  return t.filter((t) => {
    let n = i.get(t);
    return n != null && dj(n, e);
  });
}
function uj(e, t) {
  return e.filter((e) => dj(e, t));
}
function dj(e, t) {
  return n.eo(t)
    ? n.no(t)
      ? e.id === `zed`
        ? ej(t) != null
        : !!e.supportsSsh
      : !1
    : !0;
}
var fj = `app://fs`,
  pj = `/@fs`,
  mj = `apps/vscode.png`,
  hj = n.wl().regex(/^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$/),
  gj = r.a(`open-in-targets`);
function _j(e, t) {
  let r = n.Vi.customFileHandlers.schema.safeParse(e);
  return r.success
    ? Object.entries(r.data).flatMap(([e, n]) => {
        let r = hj.safeParse(e);
        return r.success
          ? [vj(r.data, n, t)]
          : (gj().warning(`Dropping invalid custom file handler`, {
              safe: { id: e },
              sensitive: { error: r.error },
            }),
            []);
      })
    : (e != null &&
        gj().warning(`Dropping invalid custom file handlers config`, {
          safe: {},
          sensitive: { error: r.error },
        }),
      []);
}
function vj(e, t, n) {
  let r = `custom:${e}`;
  return {
    id: r,
    configuredCommand: t.command,
    configuredIcon: t.icon,
    label: t.label,
    icon: yj(t.icon, n, !1),
    kind: `editor`,
    supportsSsh: t.supports_ssh,
    detect: () => wj(t.command, n),
    iconPath: () => null,
    args: () => t.args,
    open: async ({
      command: e,
      path: n,
      appPath: i,
      location: a,
      hostConfig: o,
      remoteWorkspaceRoot: s,
      remotePath: c,
    }) => {
      switch (t.input) {
        case `path`:
          await Ps(e, [...t.args, n]);
          return;
        case `json_argument`:
          await Ps(e, [
            ...t.args,
            JSON.stringify({
              target: r,
              path: n,
              appPath: i ?? null,
              location: a ?? null,
            }),
          ]);
          return;
        case `json_stdin`:
          await Ps(e, t.args, {
            stdin: JSON.stringify({
              target: r,
              path: n,
              appPath: i ?? null,
              location: a ?? null,
              hostConfig: o ?? null,
              remoteWorkspaceRoot: s ?? null,
              remotePath: c ?? null,
            }),
          });
          return;
      }
    },
  };
}
function yj(e, t, n = !0) {
  if (Sj(e) || Cj(e)) return e;
  let r = bj(e, t, n);
  return r == null ? mj : xj(r);
}
function bj(e, t, n) {
  if (e.startsWith(`file:`))
    try {
      return (0, s.fileURLToPath)(e);
    } catch {
      return null;
    }
  return Tj(e, t) && (t === `win32` || !n || (0, p.existsSync)(e)) ? e : null;
}
function xj(e) {
  let t = n.Hs(n.Us(e));
  return `${fj}${pj}${encodeURI(t).replaceAll(`#`, `%23`).replaceAll(`?`, `%3F`)}`;
}
function Sj(e) {
  let t = e.startsWith(`/`) ? e.slice(1) : e;
  return t.startsWith(`apps/`) && !t.split(`/`).includes(`..`);
}
function Cj(e) {
  return /^data:image\/(?:png|jpe?g|gif|webp|bmp|x-icon|vnd\.microsoft\.icon);base64,/i.test(
    e,
  );
}
function wj(e, t) {
  return Tj(e, t) ? ((0, p.existsSync)(e) ? e : null) : As(e);
}
function Tj(e, t) {
  if (t !== `win32`) return u.default.posix.isAbsolute(e);
  let n = u.default.win32.parse(e).root;
  return (
    u.default.win32.isAbsolute(e) && (n.includes(`:`) || n.startsWith(`\\\\`))
  );
}
var Ej = `/Applications`,
  Dj = (0, u.join)((0, l.homedir)(), `Applications`),
  Oj = [Ej, Dj];
function kj(e) {
  return e.startsWith(`${Ej}/`)
    ? [e, (0, u.join)(Dj, e.slice(`${Ej}/`.length))]
    : [e];
}
function Aj(e) {
  if (process.platform !== `darwin`) return null;
  for (let t of e) for (let e of kj(t)) if ((0, p.existsSync)(e)) return e;
  return null;
}
function jj(e, t) {
  if (process.platform !== `darwin`) return null;
  let n = e.toLowerCase();
  for (let e of Oj) {
    let r;
    try {
      r = (0, p.readdirSync)(e);
    } catch {
      continue;
    }
    for (let i of r) {
      let r = i.toLowerCase();
      if (!r.startsWith(n) || !r.endsWith(`.app`)) continue;
      let a = (0, u.join)(e, i, `Contents`, `MacOS`, t);
      if ((0, p.existsSync)(a)) return a;
    }
  }
  return null;
}
function Mj(e) {
  if (process.platform !== `darwin`) return null;
  let t = e.toLowerCase();
  for (let e of Oj) {
    let n;
    try {
      n = (0, p.readdirSync)(e);
    } catch {
      continue;
    }
    for (let r of n) {
      let n = r.toLowerCase();
      if (!n.startsWith(t) || !n.endsWith(`.app`)) continue;
      let i = (0, u.join)(e, r);
      if ((0, p.existsSync)(i)) return i;
    }
  }
  return null;
}
function Nj(e, t) {
  return t
    ? [`--goto`, `${e}:${t.line}:${t.column}`]
    : process.platform === `win32`
      ? [e]
      : [`--goto`, e];
}
function Pj({
  hostConfig: e,
  remoteWorkspaceRoot: t,
  remotePath: n,
  location: r,
}) {
  if (e.kind === `remote-control`)
    throw Error(`Remote control does not support VS Code remote open yet.`);
  let i = `ssh-remote+${$A(e)}`,
    a = t?.trim() || void 0,
    o = n?.trim() || void 0;
  if (o != null && r != null)
    return Fj({
      remoteAuthority: i,
      remoteWorkspaceRoot: a,
      path: o,
      location: r,
    });
  let s = [];
  return (
    a != null && s.push(`--folder-uri`, Ij(i, a)),
    o != null && s.push(`--file-uri`, Ij(i, o)),
    s
  );
}
function Fj({
  remoteAuthority: e,
  remoteWorkspaceRoot: t,
  path: n,
  location: r,
}) {
  let i = [];
  return (
    t != null && i.push(`--folder-uri`, Ij(e, t)),
    i.push(`--remote`, e, `--goto`, `${n}:${r.line}:${r.column}`),
    i
  );
}
function Ij(e, t) {
  return `vscode-remote://${e}${(t.startsWith(`/`) ? t : `/${t}`).split(`/`).map(encodeURIComponent).join(`/`)}`;
}
function Lj({ label: e, icon: t, kind: n, platform: r }) {
  return {
    label: r.label ?? e,
    icon: r.icon ?? t,
    kind: r.kind ?? n,
    hidden: r.hidden,
    detect: r.detect,
    iconPath: r.iconPath,
    args: r.args,
    env: r.env,
    open: r.open,
  };
}
function Rj({
  id: e,
  label: t,
  icon: n,
  kind: r,
  hidden: i,
  darwin: a,
  win32: o,
  linux: s,
}) {
  return {
    id: e,
    platforms: {
      darwin: a
        ? Lj({ label: t, icon: n, kind: r, platform: { hidden: i, ...a } })
        : void 0,
      win32: o
        ? Lj({ label: t, icon: n, kind: r, platform: { hidden: i, ...o } })
        : void 0,
      linux: s
        ? Lj({ label: t, icon: n, kind: r, platform: { hidden: i, ...s } })
        : void 0,
    },
  };
}
function zj({
  id: e,
  label: t,
  icon: n,
  darwinDetect: r,
  win32Detect: i,
  darwinEnv: a,
  darwinArgs: o,
  hidden: s,
}) {
  return {
    id: e,
    platforms: {
      darwin: r
        ? {
            label: t,
            icon: n,
            kind: `editor`,
            hidden: s,
            detect: r,
            env: a,
            args: o ?? Bj,
            supportsSsh: !0,
          }
        : void 0,
      win32: i
        ? {
            label: t,
            icon: n,
            kind: `editor`,
            hidden: s,
            detect: i,
            args: Bj,
            supportsSsh: !0,
          }
        : void 0,
    },
  };
}
var Bj = (e, t, r, i, a) =>
    r != null && n.eo(r) && (i != null || a != null)
      ? Pj({
          hostConfig: r,
          location: t,
          remotePath: a,
          remoteWorkspaceRoot: i,
        })
      : Nj(e, t),
  Vj = zj({
    id: `antigravity`,
    label: `Antigravity`,
    icon: `apps/antigravity.png`,
    darwinDetect: () =>
      Aj([
        `/Applications/Antigravity.app/Contents/Resources/app/bin/antigravity`,
      ]),
    win32Detect: Hj,
  });
function Hj() {
  let e = As(`antigravity.exe`) ?? As(`antigravity.cmd`) ?? As(`antigravity`);
  return e
    ? Us(e)
    : Hs([
        [`Antigravity`, `Antigravity.exe`],
        [`antigravity`, `Antigravity.exe`],
        [`Antigravity`, `bin`, `antigravity.cmd`],
        [`antigravity`, `bin`, `antigravity.cmd`],
        [`Antigravity`, `resources`, `app`, `bin`, `antigravity.cmd`],
        [`antigravity`, `resources`, `app`, `bin`, `antigravity.cmd`],
      ]);
}
var Uj = {
    id: `bbedit`,
    platforms: {
      darwin: {
        label: `BBEdit`,
        icon: `apps/bbedit.png`,
        kind: `editor`,
        detect: () =>
          Mj(`BBEdit`) || Aj([`/Applications/BBEdit.app`]) ? `open` : null,
        args: (e) => [`-a`, `BBEdit`, e],
      },
    },
  },
  Wj = r.a(`open-in-targets`),
  Gj = {
    id: `terminal`,
    platforms: {
      ...Kj({
        id: `terminal`,
        label: `Terminal`,
        icon: `apps/terminal.png`,
        appPaths: [`/System/Applications/Utilities/Terminal.app`],
        appName: `Terminal`,
      }).platforms,
      win32: {
        label: `Terminal`,
        icon: `apps/microsoft-terminal.png`,
        kind: `terminal`,
        detect: $j,
        iconPath: () => null,
        args: eM,
        open: ({ command: e, path: t }) => tM(e, eM(t)),
      },
    },
  };
function Kj({ id: e, label: t, icon: n, appPaths: r, appName: i }) {
  return {
    id: e,
    platforms: {
      darwin: {
        label: t,
        icon: n,
        kind: `terminal`,
        detect: () => (Aj(r) ? `open` : null),
        args: (e) => [`-a`, i, Yj(e)],
        open: async ({ command: t, path: n, location: r }) => {
          (await lM(e, n, r)) || (await Ps(t, [`-a`, i, Yj(n)]));
        },
      },
    },
  };
}
function qj(e) {
  let t = process.env.VISUAL?.trim();
  if (t) return t;
  let n = process.env.EDITOR?.trim();
  if (n) return n;
  for (let t of Xj) {
    let n = e(t);
    if (n) return n;
  }
  return null;
}
function Jj(e) {
  return [
    `-na`,
    `Ghostty.app`,
    `--args`,
    `-e`,
    process.env.SHELL?.trim() || `/bin/zsh`,
    `-lc`,
    e,
  ];
}
function Yj(e) {
  return (0, p.existsSync)(e) && (0, p.statSync)(e).isDirectory()
    ? e
    : (0, u.dirname)(e);
}
var Xj = [`nvim`, `vim`, `nano`, `less`],
  Zj = `wt.exe`,
  Qj = [`Terminal.lnk`, `Windows Terminal.lnk`];
async function $j(e) {
  if (As(Zj) || Gs(Zj)) return Zj;
  let t = Ys(Qj);
  return t ? (Us((await e(t)).target) ?? Zj) : null;
}
function eM(e) {
  return [`-d`, Yj(e)];
}
async function tM(e, t) {
  await Ps(`cmd.exe`, [`/d`, `/s`, `/c`, `start`, ``, e, ...t], { env: nM() });
}
function nM() {
  let e = Ks(),
    t = process.env.Path == null ? `PATH` : `Path`,
    n = (process.env[t] ?? ``).split(`;`).filter(Boolean);
  return (
    n.some((t) => t.toLowerCase() === e.toLowerCase()) || n.unshift(e),
    { ...process.env, [t]: n.join(`;`) }
  );
}
function rM() {
  return qj(As);
}
function iM(e) {
  return e.replace(/\\/g, `\\\\`).replace(/"/g, `\\"`);
}
function aM(e) {
  try {
    let t = n.$n(e),
      r = t.at(0)?.trim();
    return r
      ? {
          args: t,
          executableName: (0, u.basename)(r)
            .replace(/\.exe$/i, ``)
            .toLowerCase(),
        }
      : null;
  } catch {
    return null;
  }
}
function oM(e, t) {
  return t
    ? e === `nvim` || e === `vim`
      ? [`+call cursor(${t.line},${t.column})`]
      : e === `nano`
        ? [`+${t.line},${t.column}`]
        : e === `less`
          ? [`+${t.line}g`]
          : []
    : [];
}
function sM(e, t, r) {
  let i = (0, u.dirname)(t),
    a = aM(e);
  if (a == null) return `cd ${n.Qn(i)} && ${e} ${n.Qn(t)}`;
  let o = oM(a.executableName, r),
    s = a.args.indexOf(`--`),
    c =
      s < 0
        ? [...a.args, ...o, t]
        : [...a.args.slice(0, s), ...o, ...a.args.slice(s), t];
  return `cd ${n.Qn(i)} && ${n.Zn(c)}`;
}
function cM(e, t) {
  let n = iM(t);
  return e === `terminal`
    ? `tell application "Terminal" to do script "${n}"`
    : e === `iterm2`
      ? [
          `tell application "iTerm"`,
          `create window with default profile`,
          `tell current session of current window to write text "` + n + `"`,
          `end tell`,
        ].join(`
`)
      : null;
}
async function lM(e, t, n) {
  let r = rM();
  if (!r || !(0, p.existsSync)(t) || (0, p.statSync)(t).isDirectory())
    return !1;
  let i = sM(r, t, n);
  if (e === `ghostty`)
    try {
      return (await Ps(`open`, Jj(i)), !0);
    } catch (e) {
      return (
        Wj().warning(
          `Failed to run $EDITOR command for Ghostty; falling back to opening directory`,
          { safe: {}, sensitive: { error: e, path: t } },
        ),
        !1
      );
    }
  let a = cM(e, i);
  if (!a) return !1;
  try {
    return (await Ps(`osascript`, [`-e`, a]), !0);
  } catch (n) {
    return (
      Wj().warning(
        `Failed to run $EDITOR command for terminal target; falling back to opening directory`,
        { safe: { targetId: e }, sensitive: { error: n, path: t } },
      ),
      !1
    );
  }
}
var uM = Rj({
  id: `cmder`,
  label: `Cmder`,
  icon: `apps/cmder.png`,
  kind: `terminal`,
  win32: { detect: dM, args: (e) => [`/START`, Yj(e)] },
});
async function dM(e) {
  let t = process.env.CMDER_ROOT?.trim();
  if (t) {
    let e = (0, u.join)(t, `Cmder.exe`);
    if ((0, p.existsSync)(e)) return e;
  }
  let n = As(`cmder.exe`) ?? As(`cmder`);
  if (n) return Us(n);
  let r = Ys([`Cmder.lnk`]);
  return r ? Us((await e(r)).target) : null;
}
var fM = zj({
    id: `cursor`,
    label: `Cursor`,
    icon: `apps/cursor.png`,
    darwinDetect: () => mM()?.electronBin ?? null,
    win32Detect: hM,
    darwinEnv: () => {
      let e = { ...process.env };
      return (
        (e.VSCODE_NODE_OPTIONS = e.NODE_OPTIONS),
        (e.VSCODE_NODE_REPL_EXTERNAL_MODULE = e.NODE_REPL_EXTERNAL_MODULE),
        delete e.NODE_OPTIONS,
        delete e.NODE_REPL_EXTERNAL_MODULE,
        (e.ELECTRON_RUN_AS_NODE = `1`),
        e
      );
    },
    darwinArgs: (...e) => {
      let t = mM();
      if (!t) throw Error(`Cursor CLI entrypoint not available`);
      return [t.cliJs, ...pM(...e)];
    },
  }),
  pM = (e, t, r, i, a) =>
    r != null && n.eo(r) && (i != null || a != null)
      ? Pj({
          hostConfig: r,
          location: t,
          remotePath: a,
          remoteWorkspaceRoot: i,
        })
      : Nj(e, t);
function mM() {
  if (process.platform !== `darwin`) return null;
  let e = [
      `/Applications/Cursor.app`,
      `/Applications/Cursor Preview.app`,
      `/Applications/Cursor Nightly.app`,
    ],
    t = Mj(`Cursor`);
  t && e.push(t);
  for (let t of e)
    for (let e of kj(t)) {
      let t = (0, u.join)(e, `Contents`, `MacOS`, `Cursor`),
        n = (0, u.join)(e, `Contents`, `Resources`, `app`, `out`, `cli.js`);
      if (!(!(0, p.existsSync)(t) || !(0, p.existsSync)(n)))
        return { electronBin: t, cliJs: n };
    }
  return null;
}
function hM() {
  let e = As(`cursor`);
  if (e) {
    let t = Us(e);
    if (t) {
      if ((0, u.basename)(t).toLowerCase() === `cursor.exe`) return t;
      let e = (0, u.dirname)(t);
      if ((0, u.basename)(e).toLowerCase() === `bin`) {
        let t = (0, u.dirname)(e);
        if ((0, u.basename)(t).toLowerCase() === `app`) {
          let e = (0, u.dirname)(t);
          if ((0, u.basename)(e).toLowerCase() === `resources`) {
            let t = (0, u.join)((0, u.dirname)(e), `Cursor.exe`);
            if ((0, p.existsSync)(t)) return t;
          }
        }
      }
    }
  }
  return Hs([[`Cursor`, `Cursor.exe`]]);
}
var gM = {
    id: `emacs`,
    platforms: {
      darwin: {
        label: `Emacs`,
        icon: `apps/emacs.png`,
        kind: `editor`,
        detect: () =>
          Mj(`Emacs`) || Aj([`/Applications/Emacs.app`]) ? `open` : null,
        args: (e) => [`-a`, `Emacs`, e],
      },
    },
  },
  _M = Rj({
    id: `fileManager`,
    label: `Finder`,
    icon: `apps/finder.png`,
    kind: `fileManager`,
    darwin: { detect: () => `open`, args: (e) => Ms(e) },
    win32: {
      label: `File Explorer`,
      icon: `apps/file-explorer.png`,
      detect: vM,
      args: (e) => Ms(e),
      open: async ({ path: e }) => yM(e),
    },
  });
function vM() {
  let e = process.env.SystemRoot ?? process.env.windir;
  if (e) {
    let t = (0, u.join)(e, `explorer.exe`);
    if ((0, p.existsSync)(t)) return t;
  }
  return `explorer.exe`;
}
async function yM(e) {
  let { shell: t } = await import(`electron`),
    n = bM(e);
  if (n && (0, p.statSync)(n).isFile()) {
    t.showItemInFolder(n);
    return;
  }
  let r = n ?? e,
    i = await t.openPath(r);
  if (i) throw Error(i);
}
function bM(e) {
  let t = e;
  for (;;) {
    if ((0, p.existsSync)(t)) return t;
    let e = (0, u.dirname)(t);
    if (e === t) return null;
    t = e;
  }
}
var xM = Kj({
    id: `ghostty`,
    label: `Ghostty`,
    icon: `apps/ghostty.png`,
    appPaths: [`/Applications/Ghostty.app`],
    appName: `Ghostty`,
  }),
  SM = Rj({
    id: `gitBash`,
    label: `Git Bash`,
    icon: `apps/vscode.png`,
    kind: `terminal`,
    win32: { detect: CM, iconPath: wM, args: (e) => [`--cd=${Yj(e)}`] },
  });
function CM() {
  let e = As(`git-bash.exe`) ?? As(`git-bash`);
  if (e) {
    let t = Us(e);
    if (t) return Ws(t, `git-bash.exe`) ?? t;
  }
  return Hs([
    [`Git`, `git-bash.exe`],
    [`Git`, `bin`, `bash.exe`],
  ]);
}
function wM(e) {
  return (
    Ys([`Git Bash.lnk`]) ||
    Hs([[`Git`, `git-bash.exe`]]) ||
    (e ? (Ws(e, `git-bash.exe`) ?? e) : null)
  );
}
var TM = Rj({
  id: `githubDesktop`,
  label: `GitHub Desktop`,
  icon: `apps/vscode.png`,
  kind: `editor`,
  win32: { detect: EM, args: (e) => [Yj(e)] },
});
function EM() {
  let e = As(`github.exe`) ?? As(`github`);
  if (e) {
    let t = Us(e);
    if (t) return Ws(t, `GitHubDesktop.exe`) ?? t;
  }
  let t = process.env.LOCALAPPDATA ?? `${(0, l.homedir)()}\\AppData\\Local`;
  for (let e of [
    [`GitHubDesktop`, `GitHubDesktop.exe`],
    [`GitHub Desktop`, `GitHubDesktop.exe`],
  ]) {
    let n = (0, u.join)(t, ...e);
    if ((0, p.existsSync)(n)) return n;
  }
  return Hs([
    [`GitHub Desktop`, `GitHubDesktop.exe`],
    [`GitHubDesktop`, `GitHubDesktop.exe`],
  ]);
}
var DM = Kj({
    id: `iterm2`,
    label: `iTerm2`,
    icon: `apps/iterm2.png`,
    appPaths: [`/Applications/iTerm.app`, `/Applications/iTerm2.app`],
    appName: `iTerm`,
  }),
  OM = r.a(`open-in-targets`),
  kM = (0, u.join)(
    (0, l.homedir)(),
    `Library`,
    `Application Support`,
    `JetBrains`,
    `Toolbox`,
    `apps`,
  ),
  AM = [process.env.ProgramFiles, process.env[`ProgramFiles(x86)`]].flatMap(
    (e) => (e ? [(0, u.join)(e, `JetBrains`)] : []),
  ),
  jM = [
    {
      target: `androidStudio`,
      bundlePrefixes: [`android studio`],
      executable: `studio`,
    },
    {
      target: `intellij`,
      bundlePrefixes: [`intellij idea`],
      executable: `idea`,
    },
    { target: `rider`, bundlePrefixes: [`rider`], executable: `rider` },
    { target: `goland`, bundlePrefixes: [`goland`], executable: `goland` },
    {
      target: `rustrover`,
      bundlePrefixes: [`rustrover`],
      executable: `rustrover`,
    },
    { target: `pycharm`, bundlePrefixes: [`pycharm`], executable: `pycharm` },
    {
      target: `webstorm`,
      bundlePrefixes: [`webstorm`],
      executable: `webstorm`,
    },
    {
      target: `phpstorm`,
      bundlePrefixes: [`phpstorm`],
      executable: `phpstorm`,
    },
  ],
  MM = null,
  NM = HM({
    id: `androidStudio`,
    label: `Android Studio`,
    icon: `apps/android-studio.png`,
    toolboxTarget: `androidStudio`,
    macExecutable: `studio`,
    windowsPathCommands: [`studio64.exe`, `studio.exe`, `studio`],
    windowsInstallDirPrefixes: [`android studio`],
    windowsInstallExecutables: [`studio64.exe`, `studio.exe`],
    windowsFallbackPaths: [
      [`Android`, `Android Studio`, `bin`, `studio64.exe`],
      [`Android`, `Android Studio`, `bin`, `studio.exe`],
    ],
  }),
  PM = HM({
    id: `intellij`,
    label: `IntelliJ IDEA`,
    icon: `apps/intellij.png`,
    toolboxTarget: `intellij`,
    macExecutable: `idea`,
    windowsPathCommands: [`idea64.exe`, `idea.exe`, `idea`],
    windowsInstallDirPrefixes: [`intellij idea`, `idea`],
    windowsInstallExecutables: [`idea64.exe`, `idea.exe`],
  }),
  FM = HM({
    id: `rider`,
    label: `Rider`,
    icon: `apps/rider.png`,
    toolboxTarget: `rider`,
    macExecutable: `rider`,
    windowsPathCommands: [`rider64.exe`, `rider.exe`, `rider`],
    windowsInstallDirPrefixes: [`rider`],
    windowsInstallExecutables: [`rider64.exe`, `rider.exe`],
  }),
  IM = HM({
    id: `goland`,
    label: `GoLand`,
    icon: `apps/goland.png`,
    toolboxTarget: `goland`,
    macExecutable: `goland`,
  }),
  LM = HM({
    id: `rustrover`,
    label: `RustRover`,
    icon: `apps/rustrover.png`,
    toolboxTarget: `rustrover`,
    macExecutable: `rustrover`,
  }),
  RM = HM({
    id: `pycharm`,
    label: `PyCharm`,
    icon: `apps/pycharm.png`,
    toolboxTarget: `pycharm`,
    macExecutable: `pycharm`,
    windowsPathCommands: [`pycharm64.exe`, `pycharm.exe`, `pycharm`],
    windowsInstallDirPrefixes: [`pycharm`],
    windowsInstallExecutables: [`pycharm64.exe`, `pycharm.exe`],
  }),
  zM = HM({
    id: `webstorm`,
    label: `WebStorm`,
    icon: `apps/webstorm.svg`,
    toolboxTarget: `webstorm`,
    macExecutable: `webstorm`,
    windowsPathCommands: [`webstorm64.exe`, `webstorm.exe`, `webstorm`],
    windowsInstallDirPrefixes: [`webstorm`],
    windowsInstallExecutables: [`webstorm64.exe`, `webstorm.exe`],
  }),
  BM = HM({
    id: `phpstorm`,
    label: `PhpStorm`,
    icon: `apps/phpstorm.png`,
    toolboxTarget: `phpstorm`,
    macExecutable: `phpstorm`,
    windowsPathCommands: [`phpstorm64.exe`, `phpstorm.exe`, `phpstorm`],
    windowsInstallDirPrefixes: [`phpstorm`],
    windowsInstallExecutables: [`phpstorm64.exe`, `phpstorm.exe`],
  });
function VM(e) {
  let t = new Map();
  if (process.platform !== `darwin`) return t;
  let n;
  try {
    n = (0, p.readdirSync)(e);
  } catch {
    return t;
  }
  for (let r of UM(n)) {
    let n = (0, u.join)(e, r),
      i;
    try {
      i = (0, p.readdirSync)(n);
    } catch {
      continue;
    }
    for (let e of UM(i)) {
      let r = (0, u.join)(n, e),
        i;
      try {
        i = (0, p.readdirSync)(r);
      } catch {
        continue;
      }
      for (let e of UM(i)) {
        let n = (0, u.join)(r, e),
          i;
        try {
          i = (0, p.readdirSync)(n);
        } catch {
          continue;
        }
        for (let e of UM(i)) {
          let r = e.toLowerCase();
          if (r.endsWith(`.app`))
            for (let i of jM) {
              if (
                t.has(i.target) ||
                !i.bundlePrefixes.some((e) => r.startsWith(e))
              )
                continue;
              let a = (0, u.join)(n, e, `Contents`, `MacOS`, i.executable);
              (0, p.existsSync)(a) && t.set(i.target, a);
            }
        }
      }
    }
  }
  return t;
}
function HM({
  id: e,
  label: t,
  icon: n,
  toolboxTarget: r,
  macExecutable: i,
  windowsPathCommands: a,
  windowsInstallDirPrefixes: o,
  windowsInstallExecutables: s,
  windowsFallbackPaths: c,
}) {
  return {
    id: e,
    platforms: {
      darwin: {
        label: t,
        icon: n,
        kind: `editor`,
        detect: () =>
          KM(r, [`/Applications/${t}.app/Contents/MacOS/${i}`], t, i),
        args: YM,
      },
      win32:
        a && o && s
          ? {
              label: t,
              icon: n,
              kind: `editor`,
              detect: () =>
                qM({
                  pathCommands: a,
                  installDirPrefixes: o,
                  installExecutables: s,
                  fallbackPaths: c,
                }),
              args: YM,
            }
          : void 0,
    },
  };
}
function UM(e) {
  return [...e].sort((e, t) => t.localeCompare(e));
}
function WM() {
  if (MM) return MM;
  try {
    MM = VM(kM);
  } catch (e) {
    (OM().debug(`Failed to discover JetBrains Toolbox installations`, {
      safe: {},
      sensitive: { error: e },
    }),
      (MM = new Map()));
  }
  return MM;
}
function GM(e, t, n) {
  return Aj(e) ?? jj(t, n);
}
function KM(e, t, n, r) {
  return GM(t, n, r) ?? WM().get(e) ?? null;
}
function qM({
  pathCommands: e,
  installDirPrefixes: t,
  installExecutables: n,
  fallbackPaths: r,
}) {
  for (let t of e) {
    let e = As(t);
    if (!e) continue;
    let n = Us(e);
    if (n) return n;
  }
  return (
    JM({ installDirPrefixes: t, installExecutables: n }) || (r ? Hs(r) : null)
  );
}
function JM({ installDirPrefixes: e, installExecutables: t }) {
  let n = e.map((e) => e.toLowerCase());
  for (let e of AM) {
    let r;
    try {
      r = UM((0, p.readdirSync)(e));
    } catch {
      continue;
    }
    for (let i of r) {
      let r = i.toLowerCase();
      if (n.some((e) => r.startsWith(e)))
        for (let n of t) {
          let t = (0, u.join)(e, i, `bin`, n);
          if ((0, p.existsSync)(t)) return t;
        }
    }
  }
  return null;
}
function YM(e, t) {
  return t
    ? [`--line`, t.line.toString(), `--column`, t.column.toString(), e]
    : [e];
}
function XM(e, t, r, i, a) {
  if (r != null && n.no(r)) {
    let e = ej(r);
    if (e == null) throw Error(`Zed SSH open is not available for this host.`);
    let n = ZM(e, a?.trim() || i);
    if (n != null) return [QM(n, t)];
    throw Error(`Missing remote path for Zed SSH open.`);
  }
  return [QM(e, t)];
}
function ZM(e, t) {
  let n = e.trim(),
    r = t?.trim();
  return !n || !r
    ? null
    : `ssh://${n}${(r.startsWith(`/`) ? r : `/${r}`).split(`/`).map(encodeURIComponent).join(`/`)}`;
}
function QM(e, t) {
  return t ? `${e}:${t.line}:${t.column}` : e;
}
var $M = Rj({
  id: `sublimeText`,
  label: `Sublime Text`,
  icon: `apps/sublime-text.png`,
  kind: `editor`,
  darwin: { detect: eN, args: XM },
  win32: { detect: tN, args: XM },
});
function eN() {
  let e = As(`subl`);
  if (e) return e;
  let t = Mj(`Sublime Text`);
  if (t) {
    let e = (0, u.join)(t, `Contents`, `SharedSupport`, `bin`, `subl`);
    if ((0, p.existsSync)(e)) return e;
  }
  return Aj([`/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl`]);
}
function tN() {
  let e = As(`subl.exe`) ?? As(`subl`);
  if (e) {
    let t = Us(e);
    if (t) return Ws(t, `sublime_text.exe`) ?? t;
  }
  return Hs([
    [`Sublime Text`, `sublime_text.exe`],
    [`Sublime Text`, `subl.exe`],
  ]);
}
var nN = Rj({
  id: `systemDefault`,
  label: `Default app`,
  icon: `apps/file-explorer.png`,
  kind: `systemDefault`,
  hidden: !0,
  darwin: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ appPath: e, path: t }) => rN(t, e),
  },
  win32: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ path: e }) => iN(e),
  },
  linux: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ path: e }) => iN(e),
  },
});
async function rN(e, t) {
  await Ps(`open`, t == null ? [e] : [`-a`, t, e]);
}
async function iN(e) {
  let { shell: t } = await import(`electron`),
    n = await t.openPath(e);
  if (n) throw Error(n);
}
var aN = {
  id: `textmate`,
  platforms: {
    darwin: {
      label: `TextMate`,
      icon: `apps/textmate.png`,
      kind: `editor`,
      detect: () =>
        Mj(`TextMate`) || Aj([`/Applications/TextMate.app`]) ? `open` : null,
      args: oN,
    },
  },
};
function oN(e, t) {
  if (!t) return [`-a`, `TextMate`, e];
  let n = new URL(`txmt://open/`);
  return (
    n.searchParams.set(`url`, (0, s.pathToFileURL)(e).toString()),
    n.searchParams.set(`line`, t.line.toString()),
    n.searchParams.set(`column`, t.column.toString()),
    [`-a`, `TextMate`, n.toString()]
  );
}
var sN = [`2022`, `2019`, `2017`],
  cN = [`Community`, `Professional`, `Enterprise`, `Preview`, `BuildTools`],
  lN = Rj({
    id: `visualStudio`,
    label: `Visual Studio`,
    icon: `apps/vscode.png`,
    kind: `editor`,
    win32: { detect: uN, args: (e) => [e] },
  });
function uN() {
  let e = As(`devenv.exe`) ?? As(`devenv`);
  return e
    ? Us(e)
    : Hs(
        sN.flatMap((e) =>
          cN.map((t) => [
            `Microsoft Visual Studio`,
            e,
            t,
            `Common7`,
            `IDE`,
            `devenv.exe`,
          ]),
        ),
      ) || Js(`**\\Common7\\IDE\\devenv.exe`);
}
var dN = zj({
  id: `vscode`,
  label: `VS Code`,
  icon: `apps/vscode.png`,
  darwinDetect: () =>
    Aj([
      `/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code`,
      `/Applications/Code.app/Contents/Resources/app/bin/code`,
    ]),
  win32Detect: fN,
});
function fN() {
  return qs({
    pathCommand: As(`code`),
    executableName: `Code.exe`,
    installDirName: `Microsoft VS Code`,
  });
}
var pN = zj({
  id: `vscodeInsiders`,
  label: `VS Code Insiders`,
  icon: `apps/vscode-insiders.png`,
  darwinDetect: () =>
    Aj([
      `/Applications/Visual Studio Code - Insiders.app/Contents/Resources/app/bin/code`,
      `/Applications/Code - Insiders.app/Contents/Resources/app/bin/code`,
    ]),
  win32Detect: mN,
});
function mN() {
  return qs({
    pathCommand: As(`code-insiders`),
    executableName: `Code - Insiders.exe`,
    installDirName: `Microsoft VS Code Insiders`,
  });
}
var hN = Kj({
    id: `warp`,
    label: `Warp`,
    icon: `apps/warp.png`,
    appPaths: [`/Applications/Warp.app`],
    appName: `Warp`,
  }),
  gN = zj({
    id: `windsurf`,
    label: `Windsurf`,
    icon: `apps/windsurf.png`,
    darwinDetect: () =>
      Aj([`/Applications/Windsurf.app/Contents/Resources/app/bin/windsurf`]),
  }),
  _N = `wsl.exe`,
  vN = Rj({
    id: `wsl`,
    label: `WSL`,
    icon: `apps/terminal.png`,
    kind: `terminal`,
    win32: {
      detect: xN,
      iconPath: bN,
      args: yN,
      open: ({ command: e, path: t }) => tM(e, yN(t)),
    },
  });
function yN(e) {
  let t = n.ri(Yj(e));
  return t.startsWith(`\\\\`) ? [...eM(e), _N] : [...eM(e), _N, `--cd`, t];
}
function bN() {
  return Ys([`WSL.lnk`]) ?? Gs(_N);
}
async function xN(e) {
  return n.ai() == null ? null : $j(e);
}
var SN = {
    id: `xcode`,
    platforms: {
      darwin: {
        label: `Xcode`,
        icon: `apps/xcode.png`,
        kind: `editor`,
        detect: () => {
          let e = wN();
          return e?.xedPath ?? e?.appPath ?? null;
        },
        args: (e) => [e],
        open: async ({ path: e, location: t }) => {
          await EN(e, t);
        },
      },
    },
  },
  CN = r.a(`open-in-targets`);
function wN() {
  if (process.platform !== `darwin`) return null;
  let e = Mj(`Xcode`) ?? Aj([`/Applications/Xcode.app`]),
    t = null;
  try {
    let e = (0, h.spawnSync)(`xcode-select`, [`-p`], {
        encoding: `utf8`,
        env: r.t(process.env),
        timeout: 1e3,
      }),
      n = e.stdout?.trim();
    if (e.status === 0 && n) {
      let e = (0, u.join)(n, `usr`, `bin`, `xed`);
      (0, p.existsSync)(e) && (t = e);
    }
  } catch (e) {
    CN().debug(`Failed to resolve Xcode via xcode-select`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  if (t == null && e) {
    let n = (0, u.join)(e, `Contents`, `Developer`, `usr`, `bin`, `xed`);
    (0, p.existsSync)(n) && (t = n);
  }
  return t == null && e == null ? null : { appPath: e, xedPath: t };
}
function TN(e) {
  let t = e;
  (!(0, p.existsSync)(e) || !(0, p.statSync)(e).isDirectory()) &&
    (t = (0, u.dirname)(e));
  let n = null;
  for (;;) {
    let e;
    try {
      e = (0, p.readdirSync)(t);
    } catch {
      e = [];
    }
    let r = e.find((e) => e.endsWith(`.xcworkspace`));
    if (r) return (0, u.join)(t, r);
    let i = e.find((e) => e.endsWith(`.xcodeproj`));
    if (i) return (0, u.join)(t, i);
    n == null && e.includes(`Package.swift`) && (n = t);
    let a = (0, u.dirname)(t);
    if (a === t) return n;
    t = a;
  }
}
async function EN(e, t) {
  let n = wN();
  if (!n) throw Error(`Open target "xcode" is not available`);
  if (n.xedPath) {
    let r = TN(e),
      i = [];
    (r && i.push(`--project`, r),
      t && i.push(`--line`, t.line.toString()),
      i.push(e),
      CN().debug(`Opening path in Xcode via xed`, {
        safe: {},
        sensitive: {
          xedPath: n.xedPath,
          args: i,
          xcodeContainerPath: r,
          path: e,
        },
      }),
      await Ps(n.xedPath, i));
    return;
  }
  if (n.appPath == null) throw Error(`Open target "xcode" is not available`);
  (CN().debug(`Opening path in Xcode via LaunchServices fallback`, {
    safe: {},
    sensitive: { appPath: n.appPath, path: e },
  }),
    await Ps(`open`, [`-a`, n.appPath, e]));
}
var DN = {
  id: `zed`,
  platforms: {
    darwin: {
      label: `Zed`,
      icon: `apps/zed.png`,
      kind: `editor`,
      detect: ON,
      args: XM,
      open: jN,
    },
    win32: {
      label: `Zed`,
      icon: `apps/zed.png`,
      kind: `editor`,
      detect: kN,
      args: XM,
    },
  },
};
function ON() {
  return (
    As(`zed`) ??
    Aj([
      `/Applications/Zed.app/Contents/MacOS/zed`,
      `/Applications/Zed Preview.app/Contents/MacOS/zed`,
      `/Applications/Zed Nightly.app/Contents/MacOS/zed`,
    ]) ??
    jj(`Zed`, `zed`)
  );
}
function kN() {
  let e = As(`zed.exe`) ?? As(`zed`);
  return e ? Us(e) : Hs([[`Zed`, `Zed.exe`]]);
}
function AN(e) {
  let t = e.indexOf(`.app/Contents/MacOS/`);
  return t === -1 ? null : e.slice(0, t + 4);
}
async function jN({
  command: e,
  path: t,
  location: n,
  hostConfig: r,
  remoteWorkspaceRoot: i,
  remotePath: a,
}) {
  let o = XM(t, n, r, i, a),
    s = o[0] ?? t,
    c = n == null ? s : (XM(t, null, r, i, a)[0] ?? s),
    l = AN(e);
  if (l) {
    if ((await Ps(`open`, [`-a`, l, c]), !n)) return;
    let t = As(`zed`) ?? e;
    try {
      await Ps(t, o);
    } catch {}
    return;
  }
  await Ps(e, o);
}
var MN = [
  dN,
  pN,
  lN,
  fM,
  Uj,
  gM,
  $M,
  DN,
  gN,
  Vj,
  TM,
  nN,
  _M,
  Gj,
  SM,
  uM,
  vN,
  DM,
  xM,
  hN,
  SN,
  NM,
  PM,
  FM,
  IM,
  LM,
  RM,
  zM,
  BM,
  aN,
];
r.a(`open-in-targets`);
function NN(e) {
  return MN.flatMap((t) => {
    let n = t.platforms[e];
    return n ? [{ id: t.id, ...n }] : [];
  });
}
var PN = NN(process.platform),
  FN = async (e) => c.shell.readShortcutLink(e);
function IN(e, t = PN) {
  return t.some((t) => t.id === e && t.kind === `editor`);
}
async function LN(
  e,
  t,
  {
    appPath: n,
    detectedCommand: r,
    hostConfig: i,
    location: a,
    remotePath: o,
    remoteWorkspaceRoot: s,
    targets: c = PN,
  } = {},
) {
  if (o != null && i?.kind === `remote-control`)
    throw Error(`Remote control does not support open in ${e} yet.`);
  let l = c.find((t) => t.id === e);
  if (!l) throw Error(`Unknown open target "${e}"`);
  let u = r ?? (await l.detect(FN));
  if (!u) throw Error(`Open target "${e}" is not available`);
  if (l.open) {
    await l.open({
      command: u,
      path: t,
      appPath: n,
      location: a,
      hostConfig: i,
      remoteWorkspaceRoot: s,
      remotePath: o,
    });
    return;
  }
  await Ps(u, l.args(t, a, i, s, o), { env: l.env?.() });
}
function RN(e) {
  return e.map(
    ({ id: e, label: t, icon: n, kind: r, hidden: i, supportsSsh: a }) => ({
      id: e,
      label: t,
      icon: n,
      kind: r,
      hidden: i,
      supportsSsh: a,
    }),
  );
}
var zN = r.a(`open-in-targets`),
  BN = new WeakMap(),
  VN = class extends a.P {
    constructor(e, t) {
      (super(), (this.settingsStore = e), (this.requestOpenInWorker = t));
    }
    async detectTarget({ target: e }) {
      if (this.requestOpenInWorker == null)
        throw Error(`Open in worker unavailable`);
      let { command: t } = await this.requestOpenInWorker({
        method: `get-target-command`,
        params: UN(this.settingsStore, e),
      });
      return { available: t != null };
    }
    loadTargetIcon({ target: e }) {
      if (this.requestOpenInWorker == null)
        throw Error(`Open in worker unavailable`);
      return this.requestOpenInWorker({
        method: `load-target-icon`,
        params: UN(this.settingsStore, e),
      });
    }
  };
function HN(e) {
  return RN(QN(e));
}
function UN(e, t) {
  let n = QN(e).find((e) => e.id === t);
  return n?.configuredCommand == null || n.configuredIcon == null
    ? { target: t }
    : {
        target: t,
        customTarget: { command: n.configuredCommand, icon: n.configuredIcon },
      };
}
async function WN(e, t) {
  let n = await Promise.all(
    HN(e).map(async (n) => {
      let r = UN(e, n.id),
        [i, a] = await Promise.all([
          t({ method: `get-target-command`, params: r })
            .then((e) => e.command)
            .catch(
              (e) => (
                zN().error(`Failed to detect open target`, {
                  safe: {},
                  sensitive: { id: n.id, error: e },
                }),
                null
              ),
            ),
          process.platform === `win32`
            ? t({ method: `load-target-icon`, params: r })
                .then((e) => e.icon)
                .catch(
                  (e) => (
                    zN().warning(`Failed to resolve open target icon`, {
                      safe: {},
                      sensitive: { id: n.id, error: e },
                    }),
                    n.icon
                  ),
                )
            : n.icon,
        ]);
      return { command: i, metadata: { ...n, icon: a } };
    }),
  );
  return {
    allAvailableTargets: n.flatMap(({ command: e, metadata: t }) =>
      e == null ? [] : [t.id],
    ),
    targetMetadata: n.map(({ metadata: e }) => e),
  };
}
async function GN(
  e,
  t,
  n,
  {
    appPath: r,
    detectedCommand: i,
    hostConfig: a,
    location: o,
    remotePath: s,
    remoteWorkspaceRoot: c,
  } = {},
) {
  await LN(t, n, {
    appPath: r,
    detectedCommand: i,
    hostConfig: a,
    location: o,
    remotePath: s,
    remoteWorkspaceRoot: c,
    targets: QN(e),
  });
}
function KN(e, t) {
  return IN(t, QN(e));
}
function qN(e) {
  let t = QN(e),
    r = e.getEffective(n.Vi.openInTargetPreferences.key) ?? {},
    i = JN(r.global, t) ?? void 0,
    a = Object.fromEntries(
      Object.entries(r.perPath ?? {}).flatMap(([e, n]) => {
        let r = JN(n, t);
        return r ? [[e, r]] : [];
      }),
    );
  return { global: i, perPath: Object.keys(a).length > 0 ? a : void 0 };
}
function JN(e, t) {
  return e === `finder`
    ? `fileManager`
    : typeof e == `string`
      ? (t.find((t) => t.id === e)?.id ?? null)
      : null;
}
function YN(e, t, n) {
  let r = XN(e, t);
  return r && n.has(r) ? r : (n.values().next().value ?? null);
}
function XN(e, t) {
  let n = qN(e);
  return (t ? n.perPath?.[t] : void 0) ?? n.global ?? null;
}
function ZN(e, t, r) {
  let i = qN(e);
  ((i.global = r),
    t && ((i.perPath = i.perPath ?? {}), (i.perPath[t] = r)),
    e.set(n.Vi.openInTargetPreferences.key, i));
}
function QN(e) {
  let t = e.getEffective(n.Vi.customFileHandlers.key);
  if (t == null) return PN;
  let r = BN.get(t);
  if (r != null) return r;
  let i = _j(t, process.platform),
    a = i.length === 0 ? PN : [...PN, ...i];
  return (BN.set(t, a), a);
}
function $N(e) {
  return eP(e, n.qt());
}
function eP(e, t) {
  let n = t.bindings.find((t) => t.command === e);
  return n == null
    ? { hasBinding: !1, hotkey: null }
    : { hasBinding: !0, hotkey: n.key };
}
async function tP(e, t) {
  return n.Yt({
    commandId: e,
    update: t == null ? { type: `clear` } : { type: `set`, accelerator: t },
  });
}
async function nP({ commandId: e, hotkey: t }) {
  return t == null || $N(e).hasBinding ? null : tP(e, t);
}
function rP({ keymapHotkey: e, legacyHotkey: t }) {
  return e.hasBinding ? e.hotkey : t;
}
function iP() {
  return typeof c.Notification.getPermissionStatus == `function`;
}
async function aP() {
  let e = await c.Notification.getPermissionStatus?.();
  return e === `granted`
    ? `enabled`
    : e === `denied`
      ? `disabled`
      : e === `not-determined`
        ? `not-determined`
        : null;
}
function oP(e, t) {
  return (
    e.on(`before-mouse-event`, t),
    () => {
      e.removeListener(`before-mouse-event`, t);
    }
  );
}
function sP() {
  return typeof c.BrowserWindow.isSystemBackdropSupported == `function`
    ? c.BrowserWindow.isSystemBackdropSupported()
    : process.platform !== `linux`;
}
function cP(e, t) {
  if (
    typeof c.systemPreferences.subscribeWorkspaceNotification != `function` ||
    typeof c.systemPreferences.unsubscribeWorkspaceNotification != `function`
  )
    return null;
  let n = c.systemPreferences.subscribeWorkspaceNotification(e, t);
  return () => {
    c.systemPreferences.unsubscribeWorkspaceNotification(n);
  };
}
function lP({ anchorBounds: e, origin: t, siteUrl: n, webContents: r }) {
  return r == null || r.isDestroyed()
    ? !1
    : r._showPageInfoDialog?.({ anchorBounds: uP(t, e), siteUrl: n }) === !0;
}
function uP(e, t) {
  if (
    t == null ||
    !Number.isFinite(t.x) ||
    !Number.isFinite(t.y) ||
    !Number.isFinite(t.width) ||
    !Number.isFinite(t.height) ||
    t.width <= 0 ||
    t.height <= 0
  )
    return;
  let n = c.BrowserWindow.fromWebContents(e);
  if (n == null || n.isDestroyed()) return;
  let r = n.getContentBounds();
  return {
    height: Math.round(t.height),
    width: Math.round(t.width),
    x: r.x + Math.round(t.x),
    y: r.y + Math.round(t.y),
  };
}
function dP(e, t) {
  e.setHistorySwipeNavigationState?.({
    ...t,
    overlayBounds: t.overlayBounds ?? void 0,
    overlayColor: t.overlayColor ?? void 0,
  });
}
function fP(e) {
  return typeof e.autocomplete?.createController == `function`
    ? e.autocomplete.createController()
    : null;
}
async function pP(e, { destinationURL: t, iconURL: n }) {
  let r = e.faviconService;
  if (r == null) return null;
  if (n != null) {
    let e = mP(await r.getFaviconForIconURL(n).catch(() => null));
    if (e != null) return e;
  }
  return mP(await r.getFavicon(t));
}
function mP(e) {
  return e == null || e.isEmpty() ? null : e.toDataURL();
}
var hP = new Set([301, 302, 303, 307, 308]),
  gP = new Set([`image/png`, `image/webp`]),
  _P = n.xl({
    name: n.wl().trim().min(1),
    description: n
      .wl()
      .nullish()
      .transform((e) => e?.trim() || null),
    imageUrl: n
      .wl()
      .url()
      .transform((e, t) => {
        let r = new URL(e);
        return r.protocol !== `https:` || r.hostname === `localhost`
          ? (t.addIssue({
              code: `custom`,
              message: `Pet spritesheet URL is invalid`,
            }),
            n.Ol)
          : r.toString();
      }),
    spriteVersionNumber: n.Tl([n.vl(1), n.vl(2)]).default(1),
  }),
  vP = class {
    fetchAsset;
    installPreparedPet;
    constructor({
      appServerClient: e,
      fetchAsset: t = fetch,
      installPreparedPet: r = n.Vt,
      preferWsl: i = !1,
    }) {
      ((this.appServerClient = e),
        (this.fetchAsset = t),
        (this.installPreparedPet = r),
        (this.preferWsl = i));
    }
    appServerClient;
    preferWsl;
    async preview({
      name: e,
      description: t,
      imageUrl: n,
      spriteVersionNumber: r,
    }) {
      let i = _P.parse({
          name: e,
          description: t,
          imageUrl: n,
          spriteVersionNumber: r,
        }),
        a = await yP(i.imageUrl, i.spriteVersionNumber, this.fetchAsset);
      return {
        displayName: i.name,
        description: i.description,
        spriteVersionNumber: i.spriteVersionNumber,
        spritesheetDataUrl: a.spritesheetDataUrl,
      };
    }
    async install({
      name: e,
      description: t,
      imageUrl: n,
      spriteVersionNumber: r,
    }) {
      let i = _P.parse({
          name: e,
          description: t,
          imageUrl: n,
          spriteVersionNumber: r,
        }),
        a = await yP(i.imageUrl, i.spriteVersionNumber, this.fetchAsset);
      return {
        id: await this.installPreparedPet({
          appServerClient: this.appServerClient,
          preferWsl: this.preferWsl,
          name: i.name,
          description: i.description,
          spritesheet: a,
        }),
      };
    }
  };
async function yP(e, t, r) {
  let i = await r(e, { redirect: `manual` });
  if (hP.has(i.status))
    throw Error(`Pet spritesheet redirects are not allowed`);
  if (!i.ok) throw Error(`Pet spritesheet download failed (${i.status})`);
  let a = await bP(i),
    o = n.Ut(a, t);
  if (o == null) throw Error(`Pet spritesheet is invalid`);
  return { ...o, bytes: a };
}
async function bP(e) {
  let t = e.headers.get(`content-type`)?.split(`;`)[0]?.trim().toLowerCase();
  if (t == null || !gP.has(t))
    throw Error(`Pet spritesheet response must be an image`);
  let n = Number(e.headers.get(`content-length`));
  if (Number.isFinite(n) && n > 20971520)
    throw Error(`Pet spritesheet is too large`);
  if (e.body == null) throw Error(`Pet spritesheet response had no body`);
  let r = e.body.getReader(),
    i = [],
    a = 0;
  for (;;) {
    let { done: e, value: t } = await r.read();
    if (e) break;
    if (((a += t.byteLength), a > 20971520))
      throw (r.cancel(), Error(`Pet spritesheet is too large`));
    i.push(t);
  }
  return Buffer.concat(i, a);
}
function xP(e) {
  let t = e.get(n.Gs.PINNED_THREAD_IDS);
  return Array.isArray(t) ? t.filter((e) => typeof e == `string`) : [];
}
var SP = (0, d.promisify)(h.execFile),
  CP = r.a(`setting-mirrors`);
function wP(e, t) {
  for (let n of e.mirrors ?? [])
    TP(n, t).catch((t) => {
      CP().warning(`Failed to write setting mirror`, {
        safe: { kind: n.kind, settingKey: e.key },
        sensitive: { error: t },
      });
    });
}
async function TP(e, t) {
  switch (e.kind) {
    case `macos-user-defaults`:
      await EP(e, t);
      return;
  }
}
async function EP(e, t) {
  process.platform === `darwin` &&
    (await SP(`/usr/bin/defaults`, [`write`, e.domain, e.key, ...DP(t)]));
}
function DP(e) {
  if (typeof e == `boolean`) return [`-bool`, e ? `YES` : `NO`];
  if (typeof e == `number`)
    return Number.isInteger(e) ? [`-int`, `${e}`] : [`-float`, `${e}`];
  if (typeof e == `string`) return [`-string`, e];
  throw Error(`Cannot mirror non-primitive setting value`);
}
var OP = [`powershell`, `commandPrompt`],
  kP = [`--login`, `-i`];
function AP() {
  if (process.platform !== `win32`) return [];
  let e = [...OP];
  return (FP() != null && e.push(`gitBash`), IP() != null && e.push(`wsl`), e);
}
function jP(e) {
  if (process.platform !== `win32`) return [n.nr()];
  if (e === `powershell`) {
    let e = NP();
    if (e != null) return [e];
  }
  if (e === `commandPrompt`) return [PP()];
  if (e === `gitBash`) {
    let e = FP();
    if (e != null) return [e, ...kP];
  }
  if (e === `wsl`) {
    let e = IP();
    if (e != null) return [e];
  }
  let t = NP();
  return t == null ? [PP()] : [t];
}
function MP(e) {
  let t = e[0];
  if (t == null || t.length === 0) return `Shell`;
  let r = t.split(/[/\\]/).at(-1)?.toLowerCase();
  if (process.platform === `win32`) {
    if (r === `wsl` || r === `wsl.exe`) return n.Di.wsl;
    if (
      r === `pwsh` ||
      r === `pwsh.exe` ||
      r === `powershell` ||
      r === `powershell.exe`
    )
      return n.Di.powershell;
    if (r === `cmd` || r === `cmd.exe`) return n.Di.commandPrompt;
    if (r === `git-bash.exe` || (r === `bash.exe` && /[\\/]Git[\\/]/i.test(t)))
      return n.Di.gitBash;
  }
  let i = r?.replace(/\.exe$/i, ``) ?? t;
  return i != null && i.length > 0 ? i : `Shell`;
}
function NP() {
  for (let e of [`pwsh.exe`, `powershell.exe`]) {
    let t = As(e);
    if (t != null) return t;
  }
  return null;
}
function PP() {
  let e = process.env.COMSPEC?.trim();
  return e != null && e.length > 0 ? e : `cmd.exe`;
}
function FP() {
  let e = As(`git-bash.exe`);
  if (e != null) return e;
  let t = As(`git.exe`);
  if (t != null) {
    let e = (0, u.dirname)(t),
      n = [
        (0, u.join)(e, `bash.exe`),
        (0, u.join)((0, u.dirname)(e), `bin`, `bash.exe`),
      ];
    for (let e of n) if ((0, p.existsSync)(e)) return e;
  }
  return Hs([[`Git`, `bin`, `bash.exe`]]);
}
function IP() {
  return As(`wsl.exe`);
}
var LP = `THIRD_PARTY_NOTICES.txt`,
  RP = (0, d.promisify)(h.execFile),
  zP = () => {
    let e = process.cwd(),
      t = [];
    return (
      process.resourcesPath &&
        t.push(u.default.join(process.resourcesPath, LP)),
      t.push(u.default.join(e, `assets`, LP)),
      t.push(u.default.join(e, `electron`, `assets`, LP)),
      t
    );
  };
function BP() {
  let e = process.cwd();
  for (;;) {
    if ((0, p.existsSync)(u.default.join(e, `pnpm-workspace.yaml`))) return e;
    let t = u.default.dirname(e);
    if (t === e) return null;
    e = t;
  }
}
async function VP() {
  let e = BP();
  if (!e) return;
  let t = u.default.join(e, `electron`, `assets`, LP);
  (await p.promises.mkdir(u.default.dirname(t), { recursive: !0 }),
    await RP(
      `node`,
      [`./scripts/generate-third-party-notices.mjs`, `--output-file`, t],
      { cwd: e, env: r.t(process.env), timeout: 12e4 },
    ));
}
async function HP() {
  for (let e of zP())
    if ((0, p.existsSync)(e)) return p.promises.readFile(e, `utf8`);
  if (process.env.NODE_ENV !== `production`) {
    try {
      await VP();
    } catch {
      return null;
    }
    for (let e of zP())
      if ((0, p.existsSync)(e)) return p.promises.readFile(e, `utf8`);
  }
  return null;
}
var UP = n.xl({
    threadId: n.wl().min(1).transform(n.uc),
    assignment: n.yi.nullable(),
  }),
  WP = class extends a.P {
    constructor(e, t) {
      (super(), (this.globalState = e), (this.windowManager = t));
    }
    async setAssignment(e) {
      let { threadId: t, assignment: r } = UP.parse(e),
        i = n.vi(this.globalState.get(n.Gs.THREAD_PROJECT_ASSIGNMENTS));
      n._i(i[t], r) ||
        (this.globalState.set(
          n.Gs.THREAD_PROJECT_ASSIGNMENTS,
          n.gi({ assignments: i, conversationId: t, assignment: r }),
        ),
        this.windowManager.sendMessageToAllWindows({
          type: `thread-project-assignment-updated`,
          threadId: t,
          assignment: r,
        }));
    }
  },
  GP = null,
  KP = null;
function qP() {
  if (GP != null)
    throw Error(`Trace recording start confirmation is already pending`);
  return new Promise((e) => {
    GP = e;
  });
}
function JP() {
  if (GP == null) return !1;
  let e = GP;
  return ((GP = null), e(!0), !0);
}
function YP() {
  if (GP == null) return !1;
  let e = GP;
  return ((GP = null), e(!1), !0);
}
function XP() {
  if (KP != null)
    throw Error(`Trace recording upload details are already pending`);
  return new Promise((e) => {
    KP = e;
  });
}
function ZP(e) {
  if (KP == null) return !1;
  let t = KP;
  return ((KP = null), t(e), !0);
}
var QP = (0, T.createRequire)(__filename);
function $P({ loadAddon: e = eF, platform: t = process.platform } = {}) {
  if (t !== `win32`) return `unknown`;
  try {
    let t = e();
    return t == null
      ? `unknown`
      : t.isCurrentAccountAdministratorCapable()
        ? `administrator`
        : `standard`;
  } catch {
    return `unknown`;
  }
}
function eF() {
  let e = St({ env: process.env, resourcesPath: process.resourcesPath });
  if (e == null) return null;
  let t = u.default.join(e, `native`, `windows-account.node`);
  return (0, p.existsSync)(t) ? QP(t) : null;
}
async function tF({
  appServerClient: e,
  directoryPath: t,
  directoriesOnly: r,
  includeHidden: i,
  workspaceRoot: a,
}) {
  if (a.trim().length === 0) throw Error(`workspaceRoot is required`);
  if (!(await n.ti.stat(a, e)).isDirectory())
    throw Error(`workspaceRoot must be a directory`);
  let o = iF(t),
    s = n.eo(e.hostConfig) ? await e.platformPath() : u,
    {
      canonicalDirectoryPath: c,
      realWorkspaceRoot: l,
      resolvedDirectoryPath: d,
      resolvedWorkspaceRoot: f,
    } = await nF({
      appServerClient: e,
      directoryPath: o,
      path: s,
      workspaceRoot: a,
    });
  if (!(await n.ti.stat(d, e)).isDirectory())
    throw Error(`directoryPath must point to a directory`);
  let p = await n.ti.readdir(d, { withFileTypes: !0 }, e);
  return {
    directoryPath: c,
    entries: (
      await Promise.all(
        p
          .filter((e) => i || !e.name.startsWith(`.`))
          .map((t) =>
            rF({
              appServerClient: e,
              entry: t,
              path: s,
              realWorkspaceRoot: l,
              resolvedDirectoryPath: d,
              resolvedWorkspaceRoot: f,
            }),
          ),
      )
    )
      .filter((e) => e != null)
      .filter((e) => !r || e.type === `directory`)
      .sort((e, t) =>
        e.type === t.type
          ? e.name.localeCompare(t.name)
          : e.type === `directory`
            ? -1
            : 1,
      ),
    parentPath: c.length === 0 ? null : oF(c),
  };
}
async function nF({
  appServerClient: e,
  directoryPath: t,
  path: r,
  workspaceRoot: i,
}) {
  let a = r.resolve(i),
    o = r.resolve(a, t);
  if (!sF({ path: r, targetPath: o, workspaceRoot: a }))
    throw Error(`directoryPath must stay within workspaceRoot`);
  if (n.eo(e.hostConfig))
    return {
      canonicalDirectoryPath: aF(r.relative(a, o)),
      realWorkspaceRoot: a,
      resolvedDirectoryPath: o,
      resolvedWorkspaceRoot: a,
    };
  let s = await p.promises.realpath(a);
  if (
    !sF({ path: r, targetPath: await p.promises.realpath(o), workspaceRoot: s })
  )
    throw Error(`directoryPath must stay within workspaceRoot`);
  return {
    canonicalDirectoryPath: aF(r.relative(a, o)),
    realWorkspaceRoot: s,
    resolvedDirectoryPath: o,
    resolvedWorkspaceRoot: a,
  };
}
async function rF({
  appServerClient: e,
  entry: t,
  path: r,
  realWorkspaceRoot: i,
  resolvedDirectoryPath: a,
  resolvedWorkspaceRoot: o,
}) {
  let s = r.join(a, t.name),
    c = typeof t.isSymbolicLink == `function` && t.isSymbolicLink(),
    l = t.isDirectory() ? `directory` : `file`;
  if (c)
    try {
      if ((await n.ti.stat(s, e)).isDirectory()) {
        if (
          n.eo(e.hostConfig) ||
          !sF({
            path: r,
            targetPath: await p.promises.realpath(s),
            workspaceRoot: i,
          })
        )
          return null;
        l = `directory`;
      }
    } catch {}
  return { isSymlink: c, name: t.name, path: aF(r.relative(o, s)), type: l };
}
function iF(e) {
  let t = e?.trim() ?? ``;
  return t.length === 0 || t === `.` ? `` : aF(t);
}
function aF(e) {
  return e
    .replace(/\\/g, `/`)
    .replace(/^\.\/+/, ``)
    .replace(/\/+$/, ``);
}
function oF(e) {
  let t = aF(e);
  if (t.length === 0) return null;
  let n = t.split(`/`).filter(Boolean);
  return n.length === 1 ? `` : n.slice(0, -1).join(`/`);
}
function sF({ path: e, targetPath: t, workspaceRoot: n }) {
  let r = e.relative(n, t);
  return !(r === `..` || r.startsWith(`..${e.sep}`) || e.isAbsolute(r));
}
var cF = !!process.env.WSL_DISTRO_NAME,
  lF = 1e3,
  uF = r.a(`electron-fetch-handler`),
  dF = {
    supported: !1,
    configuredHotkey: null,
    configuredToggleHotkey: null,
    keepVisible: !1,
  },
  fF = {
    getState: () => dF,
    setHotkey: () => ({ success: !1, error: `Not supported.`, state: dF }),
    setKeepVisible: () => dF,
    setToggleHotkey: () => ({
      success: !1,
      error: `Not supported.`,
      state: dF,
    }),
    syncCommandKeybindings: () => void 0,
    getHistory: () => [],
    copyHistoryItem: () => !1,
  },
  pF = (e) => ({ preferWsl: cF, appServerClient: e }),
  mF = 5,
  hF = 5,
  gF = new Set(i.a.values.map((e) => i.r(e)));
async function _F({ appPath: e, bundleId: t, displayName: n }) {
  return {
    id: `app:${e}`,
    target: `systemDefault`,
    label: n,
    icon: (await RA(e)) ?? `apps/finder.png`,
    kind: `native`,
    appPath: e,
    bundleId: t,
    available: !0,
  };
}
async function vF({ filePath: e }) {
  let t = !xu(e),
    r = await n.bt(
      {
        filePath: e,
        ...(t ? {} : { limit: mF }),
        order: `usage`,
        prioritizedBundleIds: GA(e),
      },
      { listApplicationsForFile: EA },
    );
  return Promise.all(
    r
      .filter((e) => !t || !gF.has(e.bundleId))
      .slice(0, mF)
      .map(_F),
  );
}
async function yF(e) {
  let t = e === `scan` ? await DA() : null,
    r = t?.applications.filter((e) => !gF.has(e.bundleId)) ?? [];
  r.length === 0 &&
    (r = await n.xt({ discovery: `known`, limit: hF, order: `usage` }));
  let i =
      t?.defaultApplicationPath ??
      (process.platform === `darwin` || process.platform === `win32`
        ? await c.app
            .getApplicationInfoForProtocol(`https://`)
            .then((e) => e.path)
            .catch(() => null)
        : null),
    a = i == null ? null : await bF(i),
    o = await Promise.all(
      r.map(async (e) => ({
        app: e,
        isDefault: a != null && (await bF(e.appPath)) === a,
      })),
    );
  return Promise.all(
    (0, bS.default)(
      o.sort((e, t) => Number(t.isDefault) - Number(e.isDefault)),
      ({ app: e }) => e.bundleId,
    )
      .slice(0, hF)
      .map(async ({ app: e, isDefault: t }) => ({
        ...(await _F(e)),
        default: t || void 0,
      })),
  );
}
async function bF(e) {
  return p.promises.realpath(e).catch(() => e);
}
function xF(e) {
  if (n.bs(e)) return !0;
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`;
  } catch {
    return !1;
  }
}
function SF({
  persistPreferredTargetPath: e,
  hasExplicitTarget: t,
  hasExistingPreference: n,
}) {
  return e ? (t ? !0 : !n) : !1;
}
var CF = class extends Error {};
function wF(e) {
  return `account` in e ? e.account.hostId : e.hostId;
}
function TF({ globalState: e, threadId: t, pinned: r }) {
  let i = xP(e),
    a = r ? [...i.filter((e) => e !== t), t] : i.filter((e) => e !== t);
  return i.length !== a.length || i.some((e, t) => e !== a[t])
    ? (e.set(n.Gs.PINNED_THREAD_IDS, a), !0)
    : !1;
}
function EF({ globalState: e, threadIds: t }) {
  let r = xP(e);
  return r.length !== t.length || r.some((e, n) => e !== t[n])
    ? (e.set(n.Gs.PINNED_THREAD_IDS, t), !0)
    : !1;
}
var DF = class {
  unsubscribeChatProcessNotificationHandler;
  bundledSkillsRoot;
  chatProcessNotificationHandler;
  chatProcessRegistry;
  pendingProcessManagerRootPids = new Set();
  petInstallManager;
  threadProjectAssignments;
  restricted;
  processManagerSnapshot = null;
  processManagerSnapshotExpiresAtMs = 0;
  processManagerSnapshotPromise = null;
  queuedFollowUpSendLocks = new n.f();
  constructor(
    e,
    t,
    r,
    i,
    a = r.getConnection(V),
    o,
    s,
    c,
    l,
    u,
    d = (e) => {
      throw Error(`App server state for host ID 1 ${e} not found`);
    },
    f = () => {
      throw Error(`Worktree service unavailable`);
    },
    p,
    m,
    h = n.Pt,
    g = process.cwd(),
    _ = fF,
    v = async () => void 0,
    y = () => {},
    b,
    x,
    S = () => {},
    C,
    w = () => {},
    T,
    E = () => [],
    D,
    O,
    k,
  ) {
    ((this.globalState = e),
      (this.getIpcClientForOrigin = t),
      (this.appServerConnectionRegistry = r),
      (this.gitManager = i),
      (this.appServerClient = a),
      (this.remoteConnectionsHandler = o),
      (this.hotkeyWindowLifecycleManager = s),
      (this.windowManager = c),
      (this.terminalManager = l),
      (this.isWorkspaceDependenciesFeatureEnabled = u),
      (this.getAppServerClientForHostId = d),
      (this.getWorktreeService = f),
      (this.requestGitWorker = p),
      (this.sharedObjectRepository = m),
      (this.errorReporter = h),
      (this.repoRoot = g),
      (this.globalDictationHotkeyController = _),
      (this.clearBrowserBrowsingData = v),
      (this.refreshApplicationMenu = y),
      (this.requestComputerUseCaptureWorker = b),
      (this.subscribeComputerUseCaptureWorkerEvent = x),
      (this.refreshBrowserSidebarOwnerCommandAccelerators = S),
      (this.settingsStore = C),
      (this.updateDockIcon = w),
      (this.getFeedbackDesktopLogEntries = E),
      (this.requestOpenInWorker = k),
      (this.threadProjectAssignments = new WP(e, c)),
      (this.bundledSkillsRoot = VF()),
      (this.chatProcessRegistry = new n.rn({
        codexHome: n.ni({ preferWsl: n.ui(sd) }),
        rootPid: process.pid,
      })),
      (this.chatProcessNotificationHandler = new n.an(
        this.chatProcessRegistry,
      )),
      (this.unsubscribeChatProcessNotificationHandler =
        this.appServerConnectionRegistry
          .getConnection(V)
          .registerInternalNotificationHandler((e) => {
            this.chatProcessNotificationHandler.handle(e).catch((t) => {
              uF().warning(`Failed to register chat process notification`, {
                safe: { method: e.method },
                sensitive: { error: t },
              });
            });
          })),
      (this.petInstallManager = new vP({
        appServerClient: this.appServerClient,
        preferWsl: cF,
      })),
      n
        .c({
          refresh: !1,
          preferWsl: cF,
          bundledRepoRoot: this.bundledSkillsRoot,
          appServerClient: this.appServerClient,
        })
        .catch((e) => {
          uF().warning(`Failed to warm recommended skills cache`, {
            safe: {},
            sensitive: { error: e },
          });
        }));
  }
  dispose() {
    this.unsubscribeChatProcessNotificationHandler();
  }
  notifyPinnedThreadsUpdated() {
    this.windowManager.sendMessageToAllRegisteredWindows({
      type: `pinned-threads-updated`,
    });
  }
  async listProcessManagerSnapshot(e = []) {
    for (let t of e) this.pendingProcessManagerRootPids.add(t);
    if (
      this.processManagerSnapshot != null &&
      Date.now() < this.processManagerSnapshotExpiresAtMs
    )
      return this.processManagerSnapshot;
    if (this.processManagerSnapshotPromise != null)
      return this.processManagerSnapshotPromise;
    let t = Date.now(),
      r = this.chatProcessRegistry.list().then(async (e) => {
        let t = Array.from(this.pendingProcessManagerRootPids),
          r = await n.kn(
            HF(process.pid, Array.from(new Set([...WF(e), ...t]))),
          );
        for (let e of t) this.pendingProcessManagerRootPids.delete(e);
        return r;
      });
    this.processManagerSnapshotPromise = r;
    try {
      return (
        (this.processManagerSnapshot = await r),
        (this.processManagerSnapshotExpiresAtMs = t + lF),
        this.processManagerSnapshot
      );
    } finally {
      this.processManagerSnapshotPromise === r &&
        (this.processManagerSnapshotPromise = null);
    }
  }
  handlers = {
    "active-workspace-roots": async () => ({
      roots: se(rt(this.globalState), sd, this.shouldUseWslPaths()),
    }),
    "workspace-root-options": async ({ hostId: e }) => {
      let t =
        e === `local`
          ? sd
          : this.getAppServerClientForHostIdOrThrow(e).hostConfig;
      if (t.id !== `local` && n.eo(t)) return ot(this.globalState, t.id);
      let r = t.id === `local` && this.shouldUseWslPaths(),
        i = le(at(this.globalState), t, r),
        a = it(this.globalState),
        o = await ce(a, t, r);
      return {
        ...(Object.keys(o).length === 0 ? {} : { canonicalPathByRoot: o }),
        roots: se(a, t, r),
        labels: i,
      };
    },
    "add-workspace-root-option": async ({
      root: e,
      label: t,
      setActive: n,
      origin: r,
    }) => {
      let i = re(e, this.shouldUseWslPaths());
      return (
        t && dt(this.globalState, (e) => ({ ...e, [i]: t })),
        ut(this.globalState, (e) => [i, ...(e ?? []).filter((e) => e !== i)]),
        r.send(B, { type: `workspace-root-options-updated` }),
        n &&
          (st(this.globalState, [i]),
          r.send(B, { type: `active-workspace-roots-updated` })),
        { success: !0 }
      );
    },
    "electron-clone-workspace-repo": async () => ({
      success: !1,
      canceled: !1,
      error: `Not implemented in Electron.`,
    }),
    "worktree-create-managed": async ({
      hostId: e,
      cwd: t,
      startingState: n,
      localEnvironmentConfigPath: r,
      streamId: i,
    }) =>
      this.getWorktreeService(e).createManagedWorktree({
        hostId: e,
        cwd: t,
        startingState: n,
        localEnvironmentConfigPath: r,
        streamId: i,
      }),
    "worktree-delete": async ({ hostId: e, worktree: t, reason: n }) => {
      await this.getWorktreeService(e).deleteWorktree(t, n);
    },
    "worktree-set-owner-thread": async ({
      hostId: e,
      worktree: t,
      conversationId: n,
    }) => {
      await this.getWorktreeService(e).setOwnerThread({
        worktree: t,
        conversationId: n,
      });
    },
    "codex-home": async (e) => {
      uF().info(`codex-home request`, {
        safe: { params: e },
        sensitive: { hostId: e.hostId },
      });
      let t = this.getRequestAppServerClient(e.hostId),
        r = t.hostConfig,
        i = r.id === `local` && this.shouldUseWslPaths(),
        a = await t.platformPath(),
        o = n.eo(r) ? await t.codexHome() : n.ni({ preferWsl: cF });
      return {
        codexHome: z(o, i),
        worktreesSegment: z(a.join(o, `worktrees`), i),
      };
    },
    "projectless-thread-cwd": async (e) => {
      let t = await ii({
          appServerClient: this.appServerClient,
          createSplitDirectories: e?.createSplitDirectories !== !1,
          directoryName: e?.directoryName ?? null,
          homeDirectory: (0, l.homedir)(),
          prompt: e?.prompt ?? null,
        }),
        n = this.shouldUseWslPaths();
      return {
        cwd: z(t.cwd, n),
        outputDirectory: z(t.outputDirectory, n),
        workspaceRoot: z(t.workspaceRoot, n),
      };
    },
    "projectless-workspace-root": async () => ({
      workspaceRoot: z(
        await ai({
          appServerClient: this.appServerClient,
          homeDirectory: (0, l.homedir)(),
        }),
        this.shouldUseWslPaths(),
      ),
    }),
    "home-directory": async (e) => ({
      homeDirectory: await this.getHomeDirectory(e?.hostId),
    }),
    "auto-deny-some-permissions": async ({
      hostId: e,
      permissions: t,
      threadDetailLevel: r,
    }) => ({
      permissions: n.un({
        homeDirectory: await this.getHomeDirectory(e),
        permissions: t,
        threadDetailLevel: r,
      }),
    }),
    "download-internal-plugins": async ({
      config: e,
      hostId: t,
      largeInternalPluginsConfig: n,
    }) => (
      bk(t),
      yk({
        config: e,
        errorReporter: this.errorReporter,
        largeInternalPluginsConfig: n,
      })
    ),
    "app-connect-oauth-callback-url": async () => ({
      callbackUrl: `codex://connector/oauth_callback`,
    }),
    "fast-mode-rollout-metrics": async (e) =>
      n.jt({ codexHome: n.ni({ preferWsl: cF }), params: e }),
    "refresh-remote-connections": async () =>
      this.remoteConnectionsHandler.refreshRemoteConnections(),
    "discover-remote-ssh-connections": async () =>
      this.remoteConnectionsHandler.discoverRemoteConnections(),
    "refresh-remote-control-connections": async () =>
      this.remoteConnectionsHandler.refreshRemoteControlConnections(),
    "rename-remote-control-environment": async ({ envId: e, name: t }) =>
      this.remoteConnectionsHandler.renameRemoteControlEnvironment({
        envId: e,
        name: t,
      }),
    "delete-remote-control-environment": async ({ envId: e }) =>
      this.remoteConnectionsHandler.deleteRemoteControlEnvironment({
        envId: e,
      }),
    "authorize-remote-control-connections": async () =>
      this.remoteConnectionsHandler.authorizeRemoteControlConnections(),
    "set-remote-control-connections-enabled": async ({
      enabled: e,
      origin: t,
    }) => {
      if (
        !this.windowManager.getPrimaryWindows().some((e) => e.webContents === t)
      ) {
        let e = this.sharedObjectRepository?.get(`remote_control_connections`);
        if (e == null)
          throw Error(`Remote control connections are unavailable`);
        return { remoteControlConnections: e };
      }
      return this.remoteConnectionsHandler.setRemoteControlConnectionsEnabled(
        e,
      );
    },
    "set-remote-wsl-connections-enabled": async ({ enabled: e, origin: t }) =>
      this.windowManager.getPrimaryWindows().some((e) => e.webContents === t)
        ? this.remoteConnectionsHandler.setRemoteWslConnectionsEnabled(e)
        : {
            remoteWslConnections:
              this.sharedObjectRepository?.get(`remote_wsl_connections`) ?? [],
          },
    "set-local-remote-control-enabled": async ({ enabled: e }) => {
      let t = n.Ot({ enabled: e });
      return (
        this.sharedObjectRepository?.set(`local_remote_control_enabled`, t),
        { enabled: t }
      );
    },
    "save-codex-managed-remote-ssh-connections": async ({
      remoteConnections: e,
    }) => this.remoteConnectionsHandler.saveCodexManagedRemoteSshConnections(e),
    "set-remote-connection-auto-connect": async ({
      hostId: e,
      autoConnect: t,
    }) => this.remoteConnectionsHandler.setRemoteConnectionAutoConnect(e, t),
    "install-remote-codex": async ({ hostId: e }) =>
      this.remoteConnectionsHandler.installRemoteCodex(e),
    "start-remote-chatgpt-login-port-forward": async ({
      hostId: e,
      loginId: t,
    }) =>
      this.remoteConnectionsHandler.startRemoteChatgptLoginPortForward(e, t),
    "stop-remote-chatgpt-login-port-forward": async ({
      hostId: e,
      loginId: t,
    }) => this.remoteConnectionsHandler.stopRemoteChatgptLoginPortForward(e, t),
    "app-server-connection-state": async ({ hostId: e }) =>
      this.remoteConnectionsHandler.appServerConnectionState(e),
    "remote-workspace-directory-entries": async ({
      hostId: e,
      directoryPath: t,
      directoriesOnly: n,
    }) =>
      this.remoteConnectionsHandler.remoteWorkspaceDirectoryEntries(e, t, n),
    "workspace-directory-entries": async ({
      hostId: e,
      workspaceRoot: t,
      directoryPath: r,
      directoriesOnly: i,
      includeHidden: a,
    }) => {
      let o = this.getRequestAppServerClient(e ?? void 0),
        s = o.hostConfig,
        c = s.id === `local` && this.shouldUseWslPaths(),
        l = n.eo(s) ? t : (this.mapAgentPathToLocalPath(t, s) ?? t),
        {
          directoryPath: u,
          entries: d,
          parentPath: f,
        } = await tF({
          appServerClient: o,
          directoryPath: r,
          directoriesOnly: i,
          includeHidden: a,
          workspaceRoot: l,
        });
      return {
        workspaceRoot: z(l, c),
        directoryPath: u,
        parentPath: f,
        entries: d,
      };
    },
    "has-custom-cli-executable": async () => (
      this.appServerConnectionRegistry.getConnection(V),
      { hasCustomCliExecutable: n.Pn() }
    ),
    "account-info": async () => {
      try {
        let e = await this.appServerConnectionRegistry
          .getConnection(V)
          .getAuthToken({ refreshToken: !1 });
        if (!e)
          return {
            accountId: null,
            userId: null,
            plan: null,
            email: null,
            computeResidency: null,
          };
        let [, t] = e.split(`.`);
        if (!t) throw Error(`Missing token payload`);
        let n = JSON.parse(Buffer.from(t, `base64url`).toString(`utf8`)),
          r = n[`https://api.openai.com/auth`],
          i = n[`https://api.openai.com/profile`],
          a = r && typeof r == `object` && r ? r.chatgpt_account_id : null,
          o = r && typeof r == `object` && r ? r.chatgpt_user_id : null,
          s = r && typeof r == `object` && r ? r.chatgpt_plan_type : null,
          c =
            r && typeof r == `object` && r ? r.chatgpt_compute_residency : null,
          l = i && typeof i == `object` && i ? i.email : null;
        if (
          typeof a == `string` &&
          typeof o == `string` &&
          typeof s == `string`
        )
          return {
            accountId: a,
            userId: o,
            plan: s,
            email: typeof l == `string` ? l : null,
            computeResidency: typeof c == `string` ? c : null,
          };
      } catch (e) {
        uF().error(`Unable to extract account id and plan from auth token`, {
          safe: {},
          sensitive: { error: e },
        });
      }
      return {
        accountId: null,
        userId: null,
        plan: null,
        email: null,
        computeResidency: null,
      };
    },
    "email-domain-mail-provider": async ({ domain: e }) => ({
      provider: await n.It(e),
    }),
    "third-party-notices": async () => ({ text: await HP() }),
    "locale-info": async () => ({
      ideLocale: c.app.getLocale(),
      systemLocale: c.app.getSystemLocale(),
    }),
    "os-info": async () => ({
      platform: process.platform,
      osVersion: await n.m(),
      osRelease: l.default.release(),
      isSystemBackdropSupported: sP(),
      hasWsl: n.ai() != null,
      isVsCodeRunningInsideWsl: !1,
      windowsAccountType: $P(),
    }),
    "wsl-bash-availability": async () => n.oi(),
    "chronicle-permissions": async () => {
      let e = BF(this.repoRoot),
        t = this.appServerConnectionRegistry
          .getMaybeConnection(V)
          ?.getChronicleSidecarControlState();
      return {
        accessibility: RF(),
        screenRecording: zF(),
        chronicleSidecarPresent: e,
        chronicleSidecarProcessState: t?.state ?? `disabled`,
      };
    },
    "computer-use-app-approvals-visibility": async () => ({
      hasApprovalStore: await AD(),
    }),
    "computer-use-app-approvals-read": async () => kD(),
    "computer-use-app-approval-remove": async ({ bundleIdentifier: e }) =>
      MD(e),
    "computer-use-sound-mode-read": async () => ({ value: await lO() }),
    "computer-use-sound-mode-write": async ({ value: e }) => ({
      value: await uO(e),
    }),
    "computer-use-background-auth-read": async () => {
      let [e, t] = await Promise.all([eO(), tO()]);
      return { enabled: e, ...t };
    },
    "computer-use-background-auth-write": async ({ enabled: e }) => ({
      enabled: await nO(e),
    }),
    "browser-browsing-data-clear": async ({ dataTypes: e }) => {
      await this.clearBrowserBrowsingData(e);
    },
    "browser-use-origin-state-read": async () => tx(),
    "browser-use-approval-mode-write": async ({ approvalMode: e }) => nx(e),
    "browser-use-history-approval-mode-write": async ({ approvalMode: e }) =>
      rx(e),
    "browser-use-file-transfer-approval-mode-write": async ({
      approvalMode: e,
      kind: t,
    }) => ix(t, e),
    "browser-use-full-cdp-access-enabled-write": async ({ enabled: e }) =>
      ax(e),
    "chrome-extension-settings-open": async ({ extensionId: e }) => {
      await nc({ extensionId: e });
    },
    "chrome-extension-installed-read": async ({ extensionId: e }) => ({
      installed: tc({ extensionId: e }),
    }),
    "child-processes": async () => {
      let e = n.En();
      try {
        let t = await this.listProcessManagerSnapshot(),
          n = await this.chatProcessRegistry.listLiveOsPids(t);
        return {
          isAvailable: !0,
          rootProcess: e,
          processes: UF(t, process.pid, n),
        };
      } catch (t) {
        return (
          uF().error(`Failed to list child processes`, {
            safe: {},
            sensitive: { error: t },
          }),
          { isAvailable: !1, rootProcess: e, processes: [] }
        );
      }
    },
    "child-process-kill": async ({ pid: e }) => {
      try {
        return {
          killed: await n.Dn(process.pid, e, {
            listChildProcesses: async (e) => {
              let t = await n.kn(
                HF(e, WF(await this.chatProcessRegistry.list())),
              );
              return UF(t, e, await this.chatProcessRegistry.listLiveOsPids(t));
            },
          }),
        };
      } catch (e) {
        return (
          uF().error(`Failed to kill child process`, {
            safe: {},
            sensitive: { error: e },
          }),
          { killed: !1 }
        );
      }
    },
    "chat-process-live-pids": async ({
      conversationId: e,
      processTargets: t = [],
      turnId: r,
    }) => {
      try {
        let i = new Set(
            t.flatMap((e) => (e.processId == null ? [] : [e.processId])),
          ),
          a = (await this.chatProcessRegistry.list()).filter(
            (t) =>
              t.conversationId === e &&
              (t.turnId === r || (t.processId != null && i.has(t.processId))),
          ),
          o = GF(t);
        return a.length === 0 && o.length === 0 && !KF(t)
          ? { processes: [] }
          : {
              processes: n.in({
                childProcesses: await this.listProcessManagerSnapshot(o),
                conversationId: e,
                processTargets: t,
                records: a,
                turnId: r,
              }),
            };
      } catch (e) {
        return (
          uF().error(`Failed to list live chat process PIDs`, {
            safe: {},
            sensitive: { error: e },
          }),
          { processes: [] }
        );
      }
    },
    "chat-process-register": async ({ persistIfUnmatched: e, record: t }) => {
      await this.chatProcessRegistry.upsert(t, { persistIfUnmatched: e });
    },
    "chat-processes": async () => ({
      processes: await this.chatProcessRegistry.list(),
    }),
    "node-repl-active-execs-kill": async ({ sessionId: e, turnId: t }) =>
      n._t({
        codexHome: n.ni({ preferWsl: this.shouldUseWslPaths() }),
        sessionId: e,
        turnId: t,
      }),
    "is-packaged": async () => ({ isPackaged: c.app.isPackaged }),
    "global-dictation-hotkey-state": async () =>
      this.globalDictationHotkeyController.getState(),
    "global-dictation-set-hotkey": async ({ hotkey: e }) => {
      let t = this.globalDictationHotkeyController.setHotkey(e);
      return (
        t.success &&
          (await n.Yt({
            commandId: `globalDictationHold`,
            update:
              e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        t
      );
    },
    "global-dictation-set-toggle-hotkey": async ({ hotkey: e }) => {
      let t = this.globalDictationHotkeyController.setToggleHotkey(e);
      return (
        t.success &&
          (await n.Yt({
            commandId: `globalDictationToggle`,
            update:
              e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        t
      );
    },
    "global-dictation-set-keep-visible": async ({ keepVisible: e }) =>
      this.globalDictationHotkeyController.setKeepVisible(e),
    "global-dictation-capture-fn-hotkey": async () => ({
      hotkey: await Pk(`Fn`),
    }),
    "global-dictation-history": async () => ({
      items: this.globalDictationHotkeyController.getHistory(),
    }),
    "global-dictation-copy-history-item": async ({ id: e }) => ({
      success: this.globalDictationHotkeyController.copyHistoryItem(e),
    }),
    "codex-command-keymap-state": async () => {
      await nP({
        commandId: `hotkeyWindow`,
        hotkey: this.hotkeyWindowLifecycleManager.getState().configuredHotkey,
      });
      let e = this.globalDictationHotkeyController.getState();
      return (
        await nP({
          commandId: `globalDictationHold`,
          hotkey: e.configuredHotkey,
        }),
        await nP({
          commandId: `globalDictationToggle`,
          hotkey: e.configuredToggleHotkey,
        }),
        n.Kt()
      );
    },
    "set-codex-command-keybinding": async ({ commandId: e, update: t }) => {
      LF(e, t);
      let r = await n.Yt({ commandId: e, update: t });
      return (
        e === `hotkeyWindow` &&
          this.hotkeyWindowLifecycleManager.syncCommandKeybinding(
            eP(e, r).hotkey,
          ),
        (e === `globalDictationHold` || e === `globalDictationToggle`) &&
          this.globalDictationHotkeyController.syncCommandKeybindings({
            holdHotkey: eP(`globalDictationHold`, r).hotkey,
            toggleHotkey: eP(`globalDictationToggle`, r).hotkey,
          }),
        this.refreshApplicationMenu(),
        this.refreshBrowserSidebarOwnerCommandAccelerators(),
        r
      );
    },
    "reset-codex-command-keybindings": async () => {
      let e = await n.Jt();
      return (
        this.hotkeyWindowLifecycleManager.syncCommandKeybinding(
          eP(`hotkeyWindow`, e).hotkey,
        ),
        this.globalDictationHotkeyController.syncCommandKeybindings({
          holdHotkey: eP(`globalDictationHold`, e).hotkey,
          toggleHotkey: eP(`globalDictationToggle`, e).hotkey,
        }),
        this.refreshApplicationMenu(),
        this.refreshBrowserSidebarOwnerCommandAccelerators(),
        e
      );
    },
    "feedback-desktop-log-archive": async ({
      includeBrowserTabs: e,
      includeDesktopLogs: t,
      includeNativeHostDiagnostics: n,
    }) =>
      o.n(
        new Date(),
        this.getFeedbackDesktopLogEntries({
          includeBrowserTabs: e,
          includeDesktopLogs: t,
        }),
        { includeDesktopLogs: t, includeNativeHostDiagnostics: n },
      ),
    "delete-feedback-desktop-log-archive": async ({ archiveId: e }) => ({
      success: await o.r(e),
    }),
    "openai-api-key": async () => ({
      value: process.env.OPENAI_API_KEY ?? null,
    }),
    "recommended-skills": async ({ hostId: e, refresh: t }) => {
      let r = this.getRequestAppServerClient(e);
      return n.c({
        refresh: t,
        preferWsl: cF,
        bundledRepoRoot: this.bundledSkillsRoot,
        appServerClient: r,
      });
    },
    "local-custom-agents": async ({ roots: e }) =>
      n.Xr({
        roots: e ?? [],
        preferWsl: cF,
        appServerClient: this.appServerClient,
      }),
    "install-recommended-skill": async ({
      hostId: e,
      skillId: t,
      repoPath: r,
      installRoot: i,
      skillStatsigOverride: a,
      forceReinstall: o,
      source: s,
    }) => {
      try {
        let c = this.getRequestAppServerClient(e),
          { destination: l } = await n.s({
            skillId: t,
            repoPath: r,
            installRoot: i,
            skillStatsigOverride: a,
            forceReinstall: o,
            source: s,
            preferWsl: cF,
            bundledRepoRoot: this.bundledSkillsRoot,
            appServerClient: c,
          });
        return { success: !0, destination: l, error: null };
      } catch (e) {
        let t = e instanceof Error ? e.message : String(e);
        return (
          uF().warning(`Failed to install recommended skill`, {
            safe: {},
            sensitive: { error: t },
          }),
          { success: !1, destination: null, error: t }
        );
      }
    },
    "remove-skill": async ({ hostId: e, skillPath: t }) => {
      try {
        let { deletedPath: r } = await n.o({
          skillPath: t,
          preferWsl: cF,
          appServerClient: this.getRequestAppServerClient(e),
        });
        return { success: !0, deletedPath: r, error: null };
      } catch (e) {
        let t = e instanceof Error ? e.message : String(e);
        return (
          uF().warning(`Failed to remove skill`, {
            safe: {},
            sensitive: { error: t },
          }),
          { success: !1, deletedPath: null, error: t }
        );
      }
    },
    "read-file": async ({ path: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t ?? void 0),
        i = await this.getFileRequestPath(e, r);
      return { contents: await n.ti.readFile(i, r) };
    },
    "read-file-metadata": async ({
      contentSampleByteLimit: e,
      contentSampleMaxFileBytes: t,
      path: r,
      hostId: i,
    }) => {
      let a = this.getRequestAppServerClient(i),
        o = await this.getFileRequestPath(r, a),
        s = await n.ti.stat(o, a),
        c = s.isFile(),
        l = await n.do(
          e != null &&
            c &&
            (t == null || typeof s.size != `number` || s.size <= t)
            ? await n.ti.readFileSampleBytes(o, a, e)
            : null,
        );
      return {
        ...(l == null ? {} : { contentKind: l }),
        createdAtMs: s.birthtimeMs > 0 ? s.birthtimeMs : null,
        isFile: c,
        mtimeMs: typeof s.mtimeMs == `number` ? s.mtimeMs : null,
        sizeBytes: typeof s.size == `number` ? s.size : null,
      };
    },
    "write-file": async ({
      content: e,
      expectedMtimeMs: t,
      hostId: r,
      path: i,
    }) => {
      let a = this.getRequestAppServerClient(r),
        o = await this.getFileRequestPath(i, a),
        s = null;
      try {
        let e = await n.ti.stat(o, a, { bypassCache: !0 });
        s = typeof e.mtimeMs == `number` ? e.mtimeMs : null;
      } catch (e) {
        let t = e;
        if (!(`code` in t) || t.code !== `ENOENT`) throw e;
      }
      if (t !== s) return { outcome: `conflict`, mtimeMs: s };
      await n.ti.writeFile(o, e, a);
      let c = await n.ti.stat(o, a);
      return {
        outcome: `saved`,
        mtimeMs: typeof c.mtimeMs == `number` ? c.mtimeMs : null,
      };
    },
    "ensure-directory": async ({ hostId: e, path: t }) => {
      let r = this.getRequestAppServerClient(e),
        i = await this.getFileRequestPath(t, r);
      return (await n.ti.mkdir(i, { recursive: !0 }, r), {});
    },
    "read-file-binary": async ({ path: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t),
        i = await n.A({ appServerClient: r, url: e });
      if (i != null) return i;
      if (e.startsWith(`https://`)) return { contentsBase64: null };
      let a = await this.getFileRequestPath(e, r),
        o = await n.ti.readFileBase64(a, r),
        s = typeof o == `string` ? o : o.toString(`base64`),
        c = await n.fo(typeof o == `string` ? Buffer.from(o, `base64`) : o);
      return { contentsBase64: s, ...(c == null ? {} : { mimeType: c }) };
    },
    "upload-local-file-over-sftp": async ({
      sourcePath: e,
      targetPath: t,
      hostId: n,
    }) => {
      await MF({
        sourcePath: e,
        targetPath: t,
        hostConfig: this.getRequestAppServerClient(n).hostConfig,
      });
    },
    "compile-latex-artifact": async ({ path: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t);
      return n.eo(r.hostConfig)
        ? { contentsBase64: null }
        : lk(
            await this.getFileRequestPath(e, r),
            await gk(
              this.appServerConnectionRegistry.getConnection(r.hostConfig.id),
            ),
          );
    },
    "read-git-file-binary": async ({ cwd: e, hostId: t, path: r, ref: i }) => {
      let a = this.getRequestAppServerClient(t),
        o = a.hostConfig,
        s = a,
        c = this.mapAgentPathToLocalPath(e, o);
      if (c == null) return { contentsBase64: null };
      try {
        let e = (await this.gitManager.getWorktreeRepository(n.oo(c), s))?.root;
        if (!e) return { contentsBase64: null };
        let t = await a.platformPath(),
          o = n.pt(t, e, c, r);
        if (t.isAbsolute(o) || o.startsWith(`..`))
          return { contentsBase64: null };
        let l = await n.mt(
          e,
          [`show`, i === `head` ? `HEAD:${o}` : `:${o}`],
          s,
        );
        if (l.code !== 0) return { contentsBase64: null };
        let u = l.stdoutBuffer;
        if (u == null || u.length === 0) return { contentsBase64: null };
        let d = await n.fo(u);
        return {
          contentsBase64: Buffer.from(u).toString(`base64`),
          ...(d == null ? {} : { mimeType: d }),
        };
      } catch {
        return { contentsBase64: null };
      }
    },
    "codex-agents-md": async ({ hostId: e }) =>
      n.$r(pF(this.getRequestAppServerClient(e))),
    "codex-agents-md-save": async ({ hostId: e, contents: t }) =>
      n.ei(t, pF(this.getRequestAppServerClient(e))),
    "pet-install-preview": async ({
      name: e,
      description: t,
      imageUrl: n,
      spriteVersionNumber: r,
    }) =>
      this.petInstallManager.preview({
        name: e,
        description: t,
        imageUrl: n,
        spriteVersionNumber: r,
      }),
    "pet-install": async ({
      name: e,
      description: t,
      imageUrl: n,
      spriteVersionNumber: r,
    }) =>
      this.petInstallManager.install({
        name: e,
        description: t,
        imageUrl: n,
        spriteVersionNumber: r,
      }),
    "generate-commit-message": async ({
      hostId: e,
      prompt: t,
      cwd: r,
      signal: i,
    }) => {
      try {
        return {
          message: await n.Ft({
            prompt: t,
            cwd: r ?? null,
            model: n.Jr,
            serviceTier: null,
            client: this.appServerConnectionRegistry.getConnection(e),
            timeoutMs: n.K,
            signal: i,
            ...n.Y,
          }),
        };
      } catch (e) {
        return (
          uF().warning(`Failed to generate commit message`, {
            safe: {},
            sensitive: { error: e },
          }),
          { message: null }
        );
      }
    },
    "generate-commit-pull-request-message": async ({
      hostId: e,
      prompt: t,
      cwd: r,
      signal: i,
    }) => {
      try {
        let a = await n.Ft({
          prompt: t,
          cwd: r ?? null,
          model: n.Jr,
          serviceTier: null,
          client: this.appServerConnectionRegistry.getConnection(e),
          timeoutMs: n.q,
          signal: i,
          ...n.X,
        });
        return {
          message: a?.message ?? null,
          title: a?.title ?? null,
          body: a?.body ?? null,
        };
      } catch (e) {
        return (
          uF().warning(`Failed to generate commit and pull request messages`, {
            safe: {},
            sensitive: { error: e },
          }),
          { message: null, title: null, body: null }
        );
      }
    },
    "generate-pull-request-message": async ({
      hostId: e,
      prompt: t,
      cwd: r,
      signal: i,
    }) => {
      try {
        let a = await n.Ft({
          prompt: t,
          cwd: r ?? null,
          model: n.Jr,
          serviceTier: null,
          client: this.appServerConnectionRegistry.getConnection(e),
          timeoutMs: n.J,
          signal: i,
          ...n.Z,
        });
        return { title: a?.title ?? null, body: a?.body ?? null };
      } catch {
        return { title: null, body: null };
      }
    },
    "inbox-items": async ({ limit: e }) => ({
      items: n.S(e ?? 200),
      unreadRunCounts: n.x(),
    }),
    "list-automations": async () => ({ items: n.Cr() }),
    "ambient-suggestions": async ({ domain: e, hostId: t, projectRoot: r }) => {
      let i = await this.getAmbientSuggestionsProjectRoot(r);
      return i.projectRoot == null
        ? { file: n.$c }
        : { file: n.Gr(t, i.projectRoot, e) };
    },
    "ambient-suggestions-refresh": async ({
      domain: e,
      hostId: t,
      projectRoot: r,
      mode: i,
    }) => {
      let a = await this.getAmbientSuggestionsProjectRoot(r);
      if (a.projectRoot != null)
        try {
          let r = this.appServerConnectionRegistry.getConnection(t),
            o = await $e({
              appServerConnection: r,
              settingsStore: this.getSettingsStore(),
            });
          await n.Kr({
            domain: e,
            hostId: t,
            projectRoot: a.projectRoot,
            isProjectlessChat: a.isProjectlessChat,
            enabled: o.enabled,
            appServerConnection: r,
            serviceTier: null,
            staleTimeMs: o.staleTimeMs,
            mode: i,
          });
        } catch (e) {
          uF().warning(`Failed to refresh ambient suggestions`, {
            safe: {},
            sensitive: { error: e, projectRoot: a.projectRoot },
          });
        }
    },
    "ambient-suggestions-generation-statuses": async () => ({
      statuses: n.Wr(),
    }),
    "ambient-suggestion-set-status": async ({
      domain: e,
      hostId: t,
      projectRoot: r,
      suggestionId: i,
      status: a,
    }) => {
      let o = await this.getAmbientSuggestionsProjectRoot(r);
      return o.projectRoot == null
        ? { file: n.$c }
        : {
            file: n.qr({
              domain: e,
              hostId: t,
              projectRoot: o.projectRoot,
              suggestionId: i,
              status: a,
            }),
          };
    },
    "developer-instructions": async ({
      baseInstructions: e,
      cwd: t,
      hostId: n,
      instructionOverrides: r,
      threadToolsEnabled: i,
      threadId: a,
    }) => ({
      instructions: await this.getProjectAwareDeveloperInstructions({
        baseInstructions: e,
        cwd: t,
        hostId: n ?? `local`,
        instructionOverrides: r,
        threadToolsEnabled: i,
        threadId: a,
      }),
    }),
    "automation-run-archive": async ({
      threadId: e,
      archivedAssistantMessage: t,
      archivedUserMessage: r,
      archivedReason: i,
    }) => {
      if (t != null || r != null) n.Lr(e, r ?? null, t ?? null);
      else
        try {
          await this.appServerConnectionRegistry
            .getConnection(V)
            .captureAutomationArchiveMessages(e);
        } catch (e) {
          uF().warning(`Failed to capture automation archive messages`, {
            safe: { error: e instanceof Error ? e.message : String(e) },
            sensitive: {},
          });
        }
      let a = n.Fr(e, i ?? void 0);
      return (
        a &&
          this.appServerConnectionRegistry
            .getConnection(V)
            .notifyAutomationRunsUpdated(),
        { success: a }
      );
    },
    "automation-run-delete": async ({ threadId: e }) => {
      let t = n.Nr(e);
      return (
        t &&
          this.appServerConnectionRegistry
            .getConnection(V)
            .notifyAutomationRunsUpdated(),
        { success: t }
      );
    },
    "delete-archived-thread": async ({ hostId: e, threadId: t }) => {
      let r = this.appServerConnectionRegistry.getConnection(e),
        i = await n.jr({ appServer: r, threadId: t });
      return (r.notifyAutomationRunsUpdated(), { deletedThreadIds: i });
    },
    "delete-all-archived-threads": async ({ hostId: e }) => {
      let t = this.appServerConnectionRegistry.getConnection(e),
        r = await n.Ar({ appServer: t });
      return (
        r.length > 0 && t.notifyAutomationRunsUpdated(),
        { deletedThreadIds: r }
      );
    },
    "list-pinned-threads": async () => ({ threadIds: xP(this.globalState) }),
    "set-thread-pinned": async ({
      threadId: e,
      pinned: t,
      beforeThreadId: r,
    }) => {
      let i =
        t && r !== void 0
          ? EF({
              globalState: this.globalState,
              threadIds: n.Pa({
                threadIds: xP(this.globalState),
                threadId: e,
                beforeThreadId: r,
              }),
            })
          : TF({ globalState: this.globalState, threadId: e, pinned: t });
      return (
        this.notifyPinnedThreadsUpdated(),
        await this.globalState.flush(),
        { success: i }
      );
    },
    "set-pinned-threads-order": async ({ threadIds: e }) => {
      let t = EF({ globalState: this.globalState, threadIds: e });
      return (
        this.notifyPinnedThreadsUpdated(),
        await this.globalState.flush(),
        { success: t }
      );
    },
    "automation-create": async (e) => {
      let t = e.kind === `cron` ? _t(this.globalState, e.projectId) : [];
      e.kind === `cron` &&
        vt({
          compatibilityCwds: t,
          executionEnvironment: e.executionEnvironment,
          projectId: e.projectId,
        });
      let r = n._r(e, t);
      if (!r) throw Error(`Automation create failed.`);
      return { item: r };
    },
    "automation-update": async (e) => {
      let t = e.kind === `cron` ? _t(this.globalState, e.projectId) : [];
      e.kind === `cron` &&
        vt({
          compatibilityCwds: t,
          executionEnvironment: e.executionEnvironment,
          projectId: e.projectId,
        });
      let r = n.Or(e, t);
      if (!r) throw Error(n.Wc);
      return { item: r };
    },
    "automation-delete": async ({ id: e }) => {
      let t = n.yr(e),
        r = n.vr(e),
        i = r === `deleted` || r === `not_found`;
      if (i)
        try {
          (n.Pr(e),
            this.appServerConnectionRegistry
              .getConnection(V)
              .notifyAutomationRunsUpdated());
        } catch {}
      return { item: t, success: i, status: r };
    },
    "automation-run-now": async ({
      id: e,
      collaborationMode: t,
      permissions: n,
    }) => (
      await Ji({
        appVersion: c.app.getVersion(),
        automationId: e,
        appServerConnection: this.appServerConnectionRegistry.getConnection(V),
        desktopFeatureAvailability: Ke(),
        gitManager: this.gitManager,
        globalState: this.globalState,
        threadProjectAssignments: this.threadProjectAssignments,
        settingsStore: this.getSettingsStore(),
        appServerClient: this.appServerClient,
        worktreeLifecycle: {
          requestCleanup: () => {
            this.getWorktreeService(V).requestCleanup();
          },
          setOwnerThread: ({ conversationId: e, worktree: t }) =>
            this.getWorktreeService(V).setOwnerThread({
              conversationId: e,
              worktree: t,
            }),
        },
        isPackaged: c.app.isPackaged,
        repoRoot: this.repoRoot,
        collaborationMode: t,
        permissions: n,
      }),
      { success: !0 }
    ),
    "find-files": async ({ query: e, cwd: t }) => {
      let n =
        typeof t == `string` && t.trim().length > 0
          ? this.mapAgentPathToLocalPath(t)
          : null;
      return {
        files: (
          await xk({ query: e ?? ``, cwd: n ?? this.getWorkspaceRoot() })
        ).map((e) => ({
          ...e,
          path: z(e.path, this.shouldUseWslPaths()),
          fsPath: z(e.fsPath, this.shouldUseWslPaths()),
        })),
      };
    },
    "paths-exist": async ({ hostId: e, paths: t }) => {
      let r = this.getRequestAppServerClient(e),
        i = r.hostConfig.id === `local` && this.shouldUseWslPaths();
      return { existingPaths: te(await n.Zr(ie(t ?? [], i), r), i) };
    },
    "claude-code-import-status": async ({ hostId: e }) =>
      $O({
        appServerClient: this.getRequestAppServerClient(e),
        providers: [`claude-code`],
      }),
    "external-agent-imported-connectors": async ({
      hostId: e,
      providers: t,
    }) => {
      let r = this.getRequestAppServerClient(e);
      if (n.eo(r.hostConfig)) return { connectors: [] };
      let i = await r.codexHome();
      return ik({
        codexHome: this.mapAgentPathToLocalPath(i, r.hostConfig) ?? i,
        providers: t,
      });
    },
    ...OO({
      getAppServerConnection: (e) =>
        this.appServerConnectionRegistry.getConnection(e),
      getStatus: (e) =>
        $O({
          appServerClient: this.getRequestAppServerClient(e.hostId),
          providers: e.providers,
        }),
      providerAdapters: {
        "claude-cowork": oD({
          getAppServerConnection: (e) =>
            this.appServerConnectionRegistry.getConnection(e),
          getRequestAppServerClient: (e) => this.getRequestAppServerClient(e),
          recordProjectlessThread: async ({
            outputDirectory: e,
            threadId: t,
            workspaceRoot: r,
          }) => {
            (this.globalState.update(n.Gs.PROJECTLESS_THREAD_IDS, (e) =>
              e?.includes(t) ? e : [...(e ?? []), t],
            ),
              this.globalState.update(
                n.Gs.THREAD_WORKSPACE_ROOT_HINTS,
                (e) => ({ ...e, [t]: r }),
              ),
              this.globalState.update(
                n.Gs.THREAD_PROJECTLESS_OUTPUT_DIRECTORIES,
                (n) => ({ ...n, [t]: e }),
              ));
          },
        }),
      },
    }),
    "local-environment": async ({ configPath: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths(),
        o = await n.Et(
          n.eo(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e),
          r,
        );
      return { environment: { ...o, configPath: z(o.configPath, a) } };
    },
    "local-environments": async ({ hostId: e, workspaceRoot: t }) => {
      let r = this.getRequestAppServerClient(e),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths();
      return {
        environments: (
          await n.Tt(n.eo(i) ? t : (this.mapAgentPathToLocalPath(t, i) ?? t), r)
        ).map((e) => ({ ...e, configPath: z(e.configPath, a) })),
      };
    },
    "local-environment-config": async ({ configPath: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths(),
        o = n.eo(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e),
        s = await n.Ct(r, o),
        c = z(o, a);
      return s.exists
        ? {
            configPath: c,
            environment: {
              ...s.environment,
              configPath: z(s.environment.configPath, a),
            },
            exists: !0,
            revision: s.revision,
          }
        : { configPath: c, environment: null, exists: !1, revision: null };
    },
    "local-environment-config-save": async ({
      configPath: e,
      hostId: t,
      raw: r,
    }) => {
      let i = this.getRequestAppServerClient(t),
        a = i.hostConfig,
        o = a.id === `local` && this.shouldUseWslPaths(),
        s = n.eo(a) ? e : (this.mapAgentPathToLocalPath(e, a) ?? e);
      return (
        await n.ti.mkdir(u.dirname(s), { recursive: !0 }, i),
        await n.ti.writeFile(s, r, i),
        { configPath: z(s, o), success: !0 }
      );
    },
    "is-copilot-api-available": async () => ({ available: !1 }),
    "get-copilot-api-proxy-info": async () => null,
    "get-global-state": async ({ key: e }) => ({
      value: this.getGlobalStateValue(e),
    }),
    "set-global-state": async ({ key: e, value: t, origin: n }) => (
      this.setGlobalStateValue(e, t, n),
      { success: !0 }
    ),
    "queued-follow-up-send-lock-acquire": async ({
      conversationId: e,
      messageId: t,
      lockId: n,
    }) => ({
      acquired: this.queuedFollowUpSendLocks.tryAcquire({
        conversationId: e,
        messageId: t,
        lockId: n,
      }),
    }),
    "queued-follow-up-send-lock-release": async ({
      conversationId: e,
      messageId: t,
      lockId: n,
      sent: r,
    }) => {
      this.queuedFollowUpSendLocks.release({
        conversationId: e,
        messageId: t,
        lockId: n,
        sent: r,
      });
    },
    "get-configuration": async ({ key: e }) => ({
      value: this.getConfigurationValue(e),
    }),
    "set-configuration": async ({ key: e, value: t }) => (
      this.setConfigurationValue(e, t),
      { success: !0 }
    ),
    "get-setting": async ({ key: e }) => ({ value: this.getSettingValue(e) }),
    "get-settings": async () => ({
      configuredValues: this.getConfiguredSettingValues(),
      values: this.getSettingValues(),
    }),
    "set-setting": async ({ key: e, value: t }) => (
      this.setSettingValue(e, t),
      { success: !0 }
    ),
    "settings-read": async () => ({
      filePath: this.getSettingsStore().getStateFilePath(),
      settings: this.getToolVisibleSettings(),
      effectiveSettings: this.getToolVisibleEffectiveSettings(),
      definitions: n.ji,
    }),
    "settings-write": async ({ settings: e }) => {
      let t = Object.entries(e).map(([e, t]) => {
        let r = n.Oi(e);
        if (r == null) throw Error(`Unknown setting: ${e}`);
        if (r.agentAccess !== `read-write`)
          throw Error(`Setting cannot be written by Codex: ${e}`);
        return { definition: r, value: n.ki(r.key).parse(t) };
      });
      for (let { definition: e, value: n } of t) this.setSettingValue(e.key, n);
      return (
        await this.getSettingsStore().flush(),
        {
          settings: this.getToolVisibleSettings(),
          effectiveSettings: this.getToolVisibleEffectiveSettings(),
        }
      );
    },
    "apply-patch": async ({ origin: e, ...t }) => {
      let r = t.hostConfig,
        i = this.getAppServerClientForHostIdOrThrow(r.id);
      return n.ut(t, this.gitManager, i, ae(n.ui(r)));
    },
    "mcp-codex-config": async ({ cwd: e }) => ({
      config: await this.buildMcpCodexConfig(e),
    }),
    "worktree-shell-environment-config": async ({ cwd: e, hostId: t }) => {
      let r = this.getRequestAppServerClient(t),
        i = r.hostConfig,
        a = n.eo(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e),
        o = await this.gitManager.getWorktreeRepository(n.oo(a), r);
      return { shellEnvironment: o == null ? null : await n.ct(o) };
    },
    "ide-context": async ({ workspaceRoot: e, origin: t }) => {
      if (!e) throw Error(`workspaceRoot is required`);
      let n = this.getIpcClientForOrigin(t);
      if (!n) throw Error(`Missing IPC client for window`);
      let r = await n.sendRequest(`ide-context`, { workspaceRoot: e });
      if (r.resultType === `error`)
        throw Error(`Failed to get ide context: ${r.error}`);
      return { ideContext: r.result.ideContext };
    },
    "ipc-request": async ({
      method: e,
      params: t,
      targetClientId: n,
      timeoutMs: r,
      origin: i,
    }) => {
      let a = this.getIpcClientForOrigin(i);
      if (!a) throw Error(`Missing IPC client for window`);
      return await a.sendRequest(e, t, { targetClientId: n, timeoutMs: r });
    },
    "open-in-targets": async ({
      cwd: e,
      deferEnrichment: t = !1,
      hostId: r,
      nativeBrowserDiscovery: i = `scan`,
      path: a,
    }) => {
      let o = this.getRequestAppServerClient(r ?? void 0),
        s = this.getSettingsStore();
      if (t && a == null) {
        let t = XN(s, e);
        return {
          preferredTarget: t,
          availableTargets: [],
          mode: `editor`,
          targets: uj(HN(s), o.hostConfig).map(
            ({ id: e, label: n, icon: r, kind: i, hidden: a }) => ({
              id: e,
              target: e,
              label: n,
              icon: r,
              kind: i,
              hidden: a,
              default: t === e || void 0,
            }),
          ),
        };
      }
      let { allAvailableTargets: c, targetMetadata: l } = await WN(
          s,
          this.getOpenInWorker(),
        ),
        u = a?.replace(/^([ab])[\\/]/, ``) ?? null,
        d = u != null && xF(u) && !n.eo(o.hostConfig),
        f =
          u == null || d || n.eo(o.hostConfig)
            ? null
            : this.resolveOpenFilePath(
                this.mapAgentPathToLocalPath(u, o.hostConfig) ?? u,
                this.mapAgentPathToLocalPath(e, o.hostConfig) ??
                  this.getWorkspaceRoot(),
              ),
        p = lj(o.hostConfig, c, l),
        m = new Set(p),
        h = YN(s, e, m),
        g = d || (f != null && n.ys(f)),
        _ = f != null && KA(f),
        v = f != null && JA(f),
        y = g ? await yF(i) : _ ? await vF({ filePath: f }) : [];
      return {
        preferredTarget: h,
        availableTargets: Array.from(m),
        mode: g || v ? `native` : `editor`,
        targets: [
          ...l.map(({ id: e, label: t, icon: n, kind: r, hidden: i }) => ({
            id: e,
            target: e,
            label: t,
            icon: n,
            kind: r,
            hidden: i,
            available: m.has(e),
            default: h === e || void 0,
          })),
          ...y,
        ],
      };
    },
    "set-preferred-app": async ({ target: e }) => (
      ZN(this.getSettingsStore(), null, e),
      { success: !0 }
    ),
    "terminal-shell-options": async () => ({ availableShells: AP() }),
    "thread-terminal-snapshot": async ({ threadId: e, origin: t }) => ({
      session: this.terminalManager.getSnapshotForConversationId(e, t.id),
    }),
    "pick-file": async ({ pickerTitle: e }) => ({
      file:
        (await this.pickFilesFromDialog({ allowMultiple: !1, title: e }))[0] ??
        null,
    }),
    "pick-files": async ({ imagesOnly: e, pickerTitle: t }) => ({
      files: await this.pickFilesFromDialog({
        allowMultiple: !0,
        imagesOnly: e,
        title: t,
      }),
    }),
    "save-file": async (e) => {
      let t = await c.dialog.showSaveDialog({
        defaultPath: e.suggestedFilename,
      });
      if (t.canceled || !t.filePath) return { path: null };
      if (e.kind === `contents`)
        return (
          await (0, m.writeFile)(
            t.filePath,
            Buffer.from(e.contentsBase64, `base64`),
          ),
          { path: t.filePath }
        );
      let n = this.getRequestAppServerClient(e.hostId);
      return (
        await NF({
          sourcePath: await this.getFileRequestPath(e.sourcePath, n),
          targetPath: t.filePath,
          hostConfig: n.hostConfig,
        }),
        { path: t.filePath }
      );
    },
    "add-context-file": async ({ hostId: e, path: t, origin: n }) => {
      let r = {
        label: (
          await this.getAppServerClientForHostIdOrThrow(e).platformPath()
        ).basename(t),
        path: t,
        fsPath: t,
      };
      return (
        n.send(B, { type: `add-context-file`, file: r }),
        { success: !0 }
      );
    },
    "open-file": async ({
      path: e,
      target: t,
      appPath: r,
      line: i,
      column: a,
      cwd: o,
      persistPreferredTargetPath: c,
      hostId: l,
      openMode: u,
      origin: d,
    }) => {
      try {
        let f = this.getRequestAppServerClient(l ?? void 0),
          p = f.hostConfig;
        if (t === `systemDefault` && xF(e) && !n.eo(p)) {
          if (r == null) {
            if (
              !(await IA({
                request: {
                  url: e,
                  initiator: `open_in_browser_bridge`,
                  openTarget: `external-browser`,
                },
              }))
            )
              throw Error(`Failed to open URL in system browser`);
          } else {
            let t = await this.getOpenInTargetCommand(`systemDefault`);
            await GN(this.getSettingsStore(), `systemDefault`, e, {
              appPath: r,
              detectedCommand: t,
              hostConfig: p,
            });
          }
          return { success: !0 };
        }
        let m = e.replace(/^([ab])[\\/]/, ``),
          h = this.mapAgentPathToLocalPath(m, p) ?? m,
          g = this.mapAgentPathToLocalPath(o, p) ?? this.getWorkspaceRoot(),
          _ = n.eo(p),
          v = _ ? o : null,
          y = t != null,
          b = await f.platformPath(),
          x = this.resolveOpenFilePath(h, g),
          S =
            (_ ? null : await this.getWorkspacePathType(f, x)) === `directory`,
          C =
            typeof i == `number`
              ? { line: i, column: typeof a == `number` ? a : 1 }
              : null,
          w =
            v != null || (_ && n._(m, b))
              ? this.resolveOpenFilePath(m, v, b)
              : null;
        if (
          !_ &&
          !S &&
          n.xs({
            browserPaneEnabled: Ke().browserPane,
            link: { type: `file`, location: C, path: x, target: t },
          })
        )
          return (
            d.send(B, {
              open: !0,
              type: `toggle-browser-panel`,
              url: (0, s.pathToFileURL)(x).toString(),
              source: `manual`,
              initiator: `open_in_browser_bridge`,
            }),
            { success: !0 }
          );
        let T = this.getSettingsStore(),
          E = t == null ? void 0 : await this.getOpenInTargetCommand(t),
          D;
        if (t == null || E == null) {
          let { allAvailableTargets: e, targetMetadata: t } = await WN(
            T,
            this.getOpenInWorker(),
          );
          D = lj(p, e, t);
        } else D = uj(HN(T), p).some((e) => e.id === t) ? [t] : [];
        let O = new Set(D),
          k = t ?? null,
          A = k != null && !O.has(k);
        if (
          ((k = XA({
            requestedTarget: k,
            requestedTargetWasFiltered: A,
            preferredTarget: YN(T, v ?? g, O),
            shouldUsePlatformDefaultViewer: !_ && !S && qA(x),
            shouldUseFileManager: S && O.has(`fileManager`),
            hasExplicitTarget: y,
            allowPlatformFallback: !_,
            platform: process.platform,
          })),
          !k)
        )
          throw Error(`No available open target`);
        let j = !_ && KA(x);
        SF({
          persistPreferredTargetPath: c,
          hasExplicitTarget: y,
          hasExistingPreference: c != null && XN(T, c) != null,
        }) && ZN(T, c ?? null, k);
        let M = u === `workspace` && C == null ? null : w;
        return (
          j && C == null && !y && KN(T, k) && (k = `fileManager`),
          await GN(T, k, x, {
            appPath: r,
            detectedCommand: k === t ? E : void 0,
            hostConfig: p,
            location: C,
            remotePath: M,
            remoteWorkspaceRoot: v,
          }),
          { success: !0 }
        );
      } catch (e) {
        return (
          uF().error(`Failed to open file`, {
            safe: {},
            sensitive: { error: e },
          }),
          { success: !1 }
        );
      }
    },
    "git-origins": async ({ dirs: e, hostId: t }) => {
      if (!this.requestGitWorker)
        throw Error(`Git worker request function is not configured`);
      let r = this.getRequestAppServerClient(t).hostConfig,
        i = n.ui(r),
        a = ie(e ?? [], i).map((e) => n.so(e)),
        o = z((0, l.homedir)(), i),
        s = it(this.globalState),
        c = rt(this.globalState),
        u = s.length > 0 ? s : (c ?? []),
        d =
          a && a.length > 0
            ? a
            : u.filter((e) => e !== `~`).map((e) => n.so(e)),
        f = n.ht(),
        { origins: p } = await this.requestGitWorker({
          method: `git-origins`,
          params: { dirs: d, hostConfig: r, operationSource: f.source },
        });
      return {
        origins: p.map((e) => ({
          ...e,
          dir: n.oo(z(e.dir, i)),
          root: n.so(z(e.root, i)),
          commonDir: e.commonDir == null ? null : z(e.commonDir, i),
        })),
        homeDir: o,
      };
    },
    "git-push": async ({ signal: e, ...t }) =>
      n.G(t, this.gitManager, this.getRequestAppServerClient(t.hostId), e),
    "git-create-branch": async ({ signal: e, ...t }) =>
      n.tt(t, this.gitManager, this.getRequestAppServerClient(t.hostId), e),
    "git-checkout-branch": ({ signal: e, ...t }) =>
      n.nt(t, this.gitManager, this.getRequestAppServerClient(t.hostId), {
        signal: e,
      }),
    "gh-cli-status": (e) =>
      n.z(this.getRequestAppServerClient(e?.hostId), void 0, e?.hostname),
    "gh-current-user": async (e) =>
      n.B(this.getRequestAppServerClient(e?.hostId), e?.hostname),
    "gh-user-search": ({ signal: e, ...t }) =>
      n.w(t, this.gitManager, this.getRequestAppServerClient(t.hostId), e),
    "gh-pr-create": async ({ signal: e, ...t }) =>
      n.R(t, this.gitManager, this.getRequestAppServerClient(t.hostId), e),
    "gh-pr-board": (e) =>
      n.I(e, this.gitManager, (e) => this.getRequestAppServerClient(e)),
    "gh-pr-search": (e) =>
      n.O({
        ...e,
        appServerClient: this.getRequestAppServerClient(e.account.hostId),
      }),
    "gh-pr-media": (e) =>
      n.k(e, this.getRequestAppServerClient(e.account.hostId)),
    "gh-pr-body": (e) =>
      n.M(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-checks": (e) =>
      n.N(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-comments": (e) =>
      n.P(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-status": (e) =>
      n.F(e, this.gitManager, this.getRequestAppServerClient(wF(e))),
    "gh-pr-diff": (e) =>
      n.j(e, this.gitManager, this.getRequestAppServerClient(wF(e))),
    "gh-pr-comment": async (e) =>
      n.L(e, this.gitManager, this.getRequestAppServerClient(wF(e))),
    "gh-pr-merge": async (e) =>
      n.E(e, this.gitManager, this.getRequestAppServerClient(wF(e))),
    "gh-pr-submit-review": (e) =>
      n.D(e, this.getRequestAppServerClient(e.account.hostId)),
    "gh-pr-update": async (e) =>
      n.T(e, this.gitManager, this.getRequestAppServerClient(wF(e))),
    "git-merge-base": async ({ gitRoot: e, baseBranch: t, hostId: r }) => {
      let i = this.getRequestAppServerClient(r),
        a = await this.resolveGitRootFromRequest(e, i);
      if (!a || !t) return { mergeBaseSha: null };
      let o = await this.gitManager.getWorktreeRepository(n.oo(a), i);
      return { mergeBaseSha: o == null ? null : await n.rt(o, t) };
    },
    "prepare-worktree-snapshot": async ({ gitRoot: e, snapshotBranch: t }) => {
      if (!e) throw Error(`gitRoot is required`);
      let r = await this.gitManager.getWorktreeRepository(
        n.oo(e),
        this.appServerClient,
      );
      if (r == null) throw Error(`gitRoot must belong to a Git worktree`);
      return n.W(r, t ?? null);
    },
    "upload-worktree-snapshot": async ({
      tarballPath: e,
      uploadUrl: t,
      contentLength: r,
      contentType: i,
    }) => {
      try {
        return (
          await n.U({
            tarballPath: e,
            uploadUrl: t,
            contentLength: r,
            contentType: i,
          }),
          { success: !0 }
        );
      } catch (e) {
        return (
          uF().error(`Failed to upload worktree snapshot`, {
            safe: {},
            sensitive: { error: e },
          }),
          { success: !1 }
        );
      }
    },
    "confirm-trace-recording-start": async () => ({ success: JP() }),
    "cancel-trace-recording-start": async () => ({ success: YP() }),
    "submit-trace-recording-details": async ({
      note: e,
      conversationId: t,
    }) => ({ success: ZP({ note: e, conversationId: t }) }),
    "set-vs-context": async () => {
      throw new CF();
    },
    "native-desktop-apps": async (e) => ({ apps: await n.yt(e) }),
    "native-desktop-app-by-bundle-id": async ({ bundleId: e }) => ({
      app: await n.vt(e),
    }),
    "computer-use-frontmost-window": async () =>
      process.platform === `darwin` ? Fa() : null,
    "computer-use-start-capture": async ({
      animationDestination: e,
      bundleIdentifier: t,
      origin: n,
      requestId: r,
    }) => {
      if (
        process.platform !== `darwin` ||
        this.requestComputerUseCaptureWorker == null ||
        this.subscribeComputerUseCaptureWorkerEvent == null
      )
        return null;
      let i = OF({
        backgroundColor: e.backgroundColor,
        cornerRadius: e.cornerRadius,
        primaryTextColor: e.primaryTextColor,
        viewportFrame: e.viewportFrame,
        webContents: n,
      });
      return i == null
        ? null
        : _O({
            animationTarget: i,
            bundleIdentifier: t,
            origin: n,
            requestComputerUseCaptureWorker:
              this.requestComputerUseCaptureWorker,
            requestId: r,
            subscribeComputerUseCaptureWorkerEvent:
              this.subscribeComputerUseCaptureWorkerEvent,
          });
    },
    "native-desktop-app-icon": async ({ appPath: e }) => ({
      iconSmall: await RA(e),
    }),
    "computer-use-native-desktop-app-icon": async ({ appPath: e }) => ({
      iconSmall: await Ia(e),
    }),
  };
  async pickFilesFromDialog({
    allowMultiple: e,
    imagesOnly: t = !1,
    title: r,
  }) {
    let i = await c.dialog.showOpenDialog({
      ...(t ? { filters: [{ extensions: [...n.wi], name: `Images` }] } : {}),
      properties: [`openFile`, ...(e ? [`multiSelections`] : [])],
      title: r,
    });
    return i.canceled || i.filePaths.length === 0
      ? []
      : i.filePaths.map((e) => ({ label: u.basename(e), path: e, fsPath: e }));
  }
  async getWorkspaceDirectoryPath(e, t) {
    let r = t?.trim() || (await this.getDefaultWorkspaceDirectoryPath(e));
    if (!(await n.ti.stat(r, e)).isDirectory())
      throw Error(`Directory not found: ${r}`);
    return r;
  }
  async getDefaultWorkspaceDirectoryPath(e) {
    let t = e.hostConfig;
    if (!n.eo(t)) return (0, l.homedir)();
    let r = await e.platformPath(),
      i = await e.codexHome();
    return r.dirname(i);
  }
  async getParentDirectoryPath(e, t) {
    let n = (await e.platformPath()).dirname(t);
    return n === t ? null : n;
  }
  async getWorkspacePathType(e, t) {
    try {
      return (await n.ti.stat(t, e)).isDirectory() ? `directory` : `file`;
    } catch {
      return `missing`;
    }
  }
  resolveOpenFilePath(e, t, r = u) {
    let i = e.replace(/^([ab])[\\/]/, ``),
      a = r.normalize(process.platform === `win32` ? n.Ws(i) : i);
    if (n._(a, r) || !t) return a;
    let o = a.split(/[\\/]+/).filter(Boolean);
    if (o.length === 0) return t;
    let s = r.join(t, ...o);
    if ((0, p.existsSync)(s)) return s;
    let c = r.basename(t),
      l = o.indexOf(c);
    if (l !== -1) {
      let e = r.join(t, ...o.slice(l + 1));
      return ((0, p.existsSync)(e), e);
    }
    return s;
  }
  async getFileRequestPath(e, t) {
    return n.eo(t.hostConfig)
      ? this.resolveOpenFilePath(e, null, await t.platformPath())
      : this.resolveOpenFilePath(
          this.mapAgentPathToLocalPath(e, t.hostConfig) ?? e,
          this.getWorkspaceRoot(),
        );
  }
  mapRemoteHomeToLocalPath(e, t = sd) {
    if (!e) return e ?? null;
    if (!n.eo(t)) return e;
    let r = t.home_dir;
    if (!r) return e;
    let i = (0, l.homedir)();
    return e === r ? i : e.startsWith(`${r}/`) ? `${i}${e.slice(r.length)}` : e;
  }
  mapAgentPathToLocalPath(e, t = sd) {
    let r = this.mapRemoteHomeToLocalPath(e, t);
    return r && re(r, n.ui(t));
  }
  async getProjectAwareDeveloperInstructions({
    baseInstructions: e,
    cwd: t,
    hostId: r,
    instructionOverrides: i,
    threadToolsEnabled: a,
    threadId: o,
  }) {
    let s = this.getAppServerClientForHostIdOrThrow(r),
      c = await this.isWorkspaceDependenciesFeatureEnabled(r).catch(
        (e) => (
          uF().warning(
            `Failed to check workspace dependencies feature for developer instructions`,
            { safe: {}, sensitive: { error: e } },
          ),
          !1
        ),
      ),
      l = await this.isNonGitWorkspace(t, s),
      u = this.getSettingsStore(),
      d = u.getEffective(n.Ii.conversationDetailMode.key) === `STEPS_PROSE`;
    return n.cn({
      baseInstructions: e,
      gitSettings: {
        branchPrefix: u.getEffective(n.zi.branchPrefix.key),
        commitInstructions: u.getEffective(n.zi.commitInstructions.key),
        pullRequestInstructions: u.getEffective(
          n.zi.pullRequestInstructions.key,
        ),
      },
      isNonGitWorkspace: l,
      instructionOverrides: i,
      threadToolsEnabled: a,
      workspaceDependenciesEnabled: c,
      includeProseDetailLevelInstructions: d,
      threadId: o,
    });
  }
  async confirmPrimaryRuntimeDependencyReset(e) {
    return (
      (
        await c.dialog.showMessageBox({
          type: `warning`,
          buttons: [`Reset and install`, `Cancel`],
          cancelId: 1,
          defaultId: 1,
          message: `Reset Codex Workspace dependencies?`,
          detail: `ChatGPT will delete this directory and install Workspace dependencies again:\n\n${e}`,
        })
      ).response === 0
    );
  }
  async isNonGitWorkspace(e, t) {
    let r = n.eo(t.hostConfig)
      ? e
      : (this.mapAgentPathToLocalPath(e, t.hostConfig) ?? e);
    if (!r) return !1;
    try {
      return (await this.gitManager.getWorktreeRepository(n.oo(r), t)) == null;
    } catch (e) {
      return (
        uF().warning(
          `Failed to determine whether developer instructions should omit git guidance for a non-git workspace`,
          {
            safe: { error: e instanceof Error ? e.message : String(e) },
            sensitive: { error: e },
          },
        ),
        !1
      );
    }
  }
  shouldUseWslPaths() {
    return n.ui(sd);
  }
  async getHomeDirectory(e) {
    let t = this.getRequestAppServerClient(e),
      r = t.hostConfig;
    if (n.eo(r)) {
      let e = await t.platformPath();
      return r.home_dir ?? e.dirname(await t.codexHome());
    }
    return z((0, l.homedir)(), n.ui(r));
  }
  async getAmbientSuggestionsProjectRoot(e) {
    return e == null
      ? { projectRoot: null, isProjectlessChat: !1 }
      : e === `~`
        ? {
            projectRoot: n.oo(z((0, l.homedir)(), this.shouldUseWslPaths())),
            isProjectlessChat: !0,
          }
        : { projectRoot: e, isProjectlessChat: !1 };
  }
  getWorkspaceRoot() {
    return (
      (rt(this.globalState)[0] ?? null) ||
      (it(this.globalState)[0] ?? null ?? null)
    );
  }
  async getSelectedGitRoot(e) {
    let t = rt(this.globalState)[0];
    return t
      ? ((await this.gitManager.getWorktreeRepository(n.oo(t), e))?.root ??
          null)
      : null;
  }
  async resolveGitRootFromRequest(e, t) {
    if (e) {
      let r = e.trim();
      if (r.length > 0) {
        let e = (await this.gitManager.getWorktreeRepository(n.oo(r), t))?.root;
        if (e) return e;
      }
    }
    return this.getSelectedGitRoot(t);
  }
  getRequestAppServerClient(e) {
    return e == null || e === `local`
      ? this.appServerClient
      : this.getAppServerClientForHostId(e);
  }
  async getOpenInTargetCommand(e) {
    let { command: t } = await this.getOpenInWorker()({
      method: `get-target-command`,
      params: UN(this.getSettingsStore(), e),
    });
    if (t == null) throw Error(`Open target "${e}" is not available`);
    return t;
  }
  getOpenInWorker() {
    if (this.requestOpenInWorker == null)
      throw Error(`Open in worker unavailable`);
    return this.requestOpenInWorker;
  }
  getAppServerClientForHostIdOrThrow(e) {
    return e === `local`
      ? this.appServerClient
      : this.getAppServerClientForHostId(e);
  }
  async buildMcpCodexConfig(e) {
    let t = this.appServerConnectionRegistry.getConnection(V);
    return (
      Wr({ settingsStore: this.getSettingsStore(), repoRoot: this.repoRoot }),
      xr({
        appServerConnection: t,
        appVersion: c.app.getVersion(),
        desktopFeatureAvailability: Ke(),
        cwd: e,
        isPackaged: c.app.isPackaged,
        repoRoot: this.repoRoot,
        shouldUseWslPaths: this.shouldUseWslPaths(),
      })
    );
  }
  getGlobalStateValue(e) {
    return this.globalState.get(e);
  }
  setGlobalStateValue(e, t, r) {
    (this.globalState.set(e, t),
      this.windowManager.sendMessageToAllWindows({
        type: `global-state-updated`,
        keys: [e],
      }),
      e === n.Gs.ACTIVE_WORKSPACE_ROOTS &&
        this.windowManager.sendMessageToAllWindows({
          type: `active-workspace-roots-updated`,
        }),
      e === n.Gs.REMOTE_PROJECTS &&
        r.send(B, { type: `workspace-root-options-updated` }));
  }
  getConfigurationValue(e) {
    return this.globalState.get(e);
  }
  setConfigurationValue(e, t) {
    (this.globalState.set(e, t), this.applySettingSideEffects(e, t));
  }
  getSettingValue(e) {
    return e === n.Vi.openLinkInTargetPreference.key
      ? FA(this.getSettingsStore())
      : this.getSettingsStore().getEffective(e);
  }
  getConfiguredSettingValues() {
    let e = this.getSettingsStore();
    return Object.fromEntries(
      n.Pi.flatMap((t) => {
        let n = e.get(t.key);
        return n === void 0 ? [] : [[t.key, n]];
      }),
    );
  }
  getSettingValues() {
    return Object.fromEntries(
      n.Pi.map((e) => [e.key, this.getSettingValue(e.key)]),
    );
  }
  getToolVisibleSettings() {
    let e = this.getSettingsStore();
    return Object.fromEntries(
      n.Ai.flatMap((t) => {
        let n = e.get(t.key);
        return n === void 0 ? [] : [[t.key, n]];
      }),
    );
  }
  getToolVisibleEffectiveSettings() {
    return Object.fromEntries(
      n.Ai.flatMap((e) => {
        let t = this.getSettingValue(e.key);
        return t === void 0 ? [] : [[e.key, t]];
      }),
    );
  }
  setSettingValue(e, t) {
    let r = n.ki(e).parse(t);
    (this.getSettingsStore().set(e, r),
      wP(n.Oi(e), r),
      this.applySettingSideEffects(e, r),
      Hr(e) &&
        Wr({
          settingsStore: this.getSettingsStore(),
          repoRoot: this.repoRoot,
        }));
  }
  applySettingSideEffects(e, t) {
    (e === `appearanceTheme` && a.p(t),
      (e === `appearanceTheme` ||
        e === `appearanceLightChromeTheme` ||
        e === `appearanceDarkChromeTheme`) &&
        this.windowManager.refreshWindowBackdrops(),
      e === n.Gs.DOCK_ICON_PREFERENCE && this.updateDockIcon());
  }
  getSettingsStore() {
    if (this.settingsStore == null) throw Error(`Settings store unavailable`);
    return this.settingsStore;
  }
  handleVSCodeRequest(e, t, r, i, o) {
    try {
      let s = t,
        c = this.handlers[s];
      if (typeof c != `function`)
        throw Error(
          `${t} not implemented in the current Electron process. Restart ${a.W()} to load the latest Electron handlers.`,
        );
      let l = () => c({ ...r, origin: e, signal: o });
      if (i == null) {
        if (n.co(t)) throw Error(`Missing git operation source for ${t}`);
        return l();
      }
      return n.gt({ source: i, requestKind: t }, l);
    } catch (e) {
      throw e instanceof CF ? Error(`${t} not implemented in Electron.`) : e;
    }
  }
};
function OF({
  backgroundColor: e,
  cornerRadius: t,
  primaryTextColor: n,
  viewportFrame: r,
  webContents: i,
}) {
  if (!AF(r) || !kF(t)) return;
  let a = c.BrowserWindow.fromWebContents(i);
  if (a == null || a.isDestroyed()) return;
  let o = i.getZoomFactor(),
    s = a.getContentBounds(),
    l = {
      x: s.x + r.x * o,
      y: s.y + r.y * o,
      width: r.width * o,
      height: r.height * o,
    },
    u = c.screen.getDisplayMatching(l);
  return {
    codexDisplay: {
      bounds: jF(u.bounds),
      id: u.id,
      scaleFactor: u.scaleFactor,
      workArea: jF(u.workArea),
    },
    destinationBackgroundColor: e,
    destinationCornerRadius: t * o,
    destinationFrame: l,
    destinationPrimaryTextColor: n,
  };
}
function kF(e) {
  return e >= 0;
}
function AF(e) {
  return e.width > 0 && e.height > 0;
}
function jF(e) {
  return { height: e.height, width: e.width, x: e.x, y: e.y };
}
async function MF({ sourcePath: e, targetPath: t, hostConfig: n }) {
  await PF({
    command: `put`,
    operation: `upload`,
    sourcePath: e,
    targetPath: t,
    hostConfig: n,
  });
}
async function NF({ sourcePath: e, targetPath: t, hostConfig: n }) {
  await PF({
    command: `get`,
    operation: `download`,
    sourcePath: e,
    targetPath: t,
    hostConfig: n,
  });
}
async function PF({
  command: e,
  operation: t,
  sourcePath: r,
  targetPath: i,
  hostConfig: a,
}) {
  if (!n.no(a)) throw Error(`SFTP ${t}s require an SSH host`);
  let [o, ...s] = a.terminal_command;
  if (o !== `ssh`) throw Error(`SSH host is missing an ssh terminal command`);
  let c = IF(r),
    l = IF(i),
    u = (0, h.spawn)(`sftp`, [`-b`, `-`, ...FF(s)], {
      stdio: [`pipe`, `pipe`, `pipe`],
    });
  u.stdin?.end(`${e} ${c} ${l}\n`);
  let d = [];
  if (
    (u.stderr?.on(`data`, (e) => d.push(e)),
    (await new Promise((e) => {
      (u.on(`close`, (t) => e(t)), u.on(`error`, () => e(null)));
    })) !== 0)
  ) {
    let e = Buffer.concat(d).toString(`utf8`).trim();
    throw Error(`SFTP ${t} failed${e.length > 0 ? `: ${e}` : ``}`);
  }
}
function FF(e) {
  let t = [`-o`, `BatchMode=yes`];
  for (let n = 0; n < e.length; n += 1) {
    let r = e[n];
    if (r === `-p`) {
      let r = e[n + 1];
      if (r == null)
        throw Error(`SSH terminal command is missing a port value`);
      (t.push(`-P`, r), (n += 1));
      continue;
    }
    t.push(r);
  }
  return t;
}
function IF(e) {
  if (/[\r\n]/.test(e)) throw Error(`SFTP batch paths cannot contain newlines`);
  return `"${e.replace(/[\\"]/g, `\\$&`)}"`;
}
function LF(e, t) {
  let n = e === `hotkeyWindow` || !1;
  if (!n && e !== `globalDictationHold` && e !== `globalDictationToggle`)
    return;
  if (t.type === `append`)
    throw Error(`OS-global shortcuts support one keybinding.`);
  if (t.type !== `set` && t.type !== `replace`) return;
  let r = n ? nA(t.accelerator) : fA(t.accelerator);
  if (r != null) throw Error(r);
}
function RF() {
  return process.platform === `darwin`
    ? c.systemPreferences.isTrustedAccessibilityClient(!1)
      ? `granted`
      : `denied`
    : `unknown`;
}
function zF() {
  return process.platform === `darwin`
    ? c.systemPreferences.getMediaAccessStatus(`screen`)
    : `unknown`;
}
function BF(e) {
  return (
    n.mr({
      buildFlavor: i.a.resolve(),
      repoRoot: e,
      resourcesPath: St({
        env: process.env,
        resourcesPath: process.resourcesPath,
      }),
    }) != null
  );
}
function VF() {
  let e = c.app.getAppPath();
  if (c.app.isPackaged) return u.join(process.resourcesPath, `skills`);
  let t = u.join(e, `assets`, `skills`);
  if ((0, p.existsSync)(t)) return t;
  let n = u.join(e, `..`, `assets`, `skills`);
  return (0, p.existsSync)(n) ? n : null;
}
function HF(e, t) {
  return [
    { pid: e, includeRoot: !1 },
    ...t.map((e) => ({ pid: e, includeRoot: !0 })),
  ];
}
function UF(e, t, r) {
  let i = n.Tn(e, [{ pid: t, includeRoot: !1 }]),
    a = new Set(i.map((e) => e.pid));
  return n.Tn(
    e,
    HF(
      t,
      r.filter((e) => !a.has(e)),
    ),
  );
}
function WF(e) {
  return Array.from(
    new Set(
      e.flatMap((e) => {
        let t = qF(e.processId);
        return [
          ...(e.osPid == null ? [] : [e.osPid]),
          ...(t == null ? [] : [t]),
        ];
      }),
    ),
  );
}
function GF(e) {
  return Array.from(
    new Set(
      e.flatMap((e) => {
        let t = qF(e.processId);
        return t == null ? [] : [t];
      }),
    ),
  );
}
function KF(e) {
  return e.some((e) => e.command.trim().length > 0 && qF(e.processId) == null);
}
function qF(e) {
  if (e == null || !/^\d+$/u.test(e)) return null;
  let t = Number(e);
  return Number.isSafeInteger(t) ? t : null;
}
var JF = class extends Error {
  constructor(e, t, n) {
    (super(e), (this.status = t), (this.cause = n));
  }
};
function YF(e, { inferAuth: t = !1 } = {}) {
  let r = (n.ho(e) || t) && !n.bo(e, `authorization`);
  return {
    attachAuth: r,
    tokenSource: r ? `pending` : `not-required`,
    token: null,
  };
}
async function XF({
  appServerClient: e,
  errorStatus: t,
  failureMessage: n,
  refreshToken: r,
  state: i,
}) {
  if (!i.attachAuth) return i;
  if (!r) {
    let t = e.getCachedAuthToken?.();
    if (t !== void 0) return { ...i, tokenSource: `cached`, token: t };
  }
  try {
    let t = await e.getAuthToken({ refreshToken: r });
    return { ...i, tokenSource: r ? `refreshed` : `loaded`, token: t };
  } catch (e) {
    throw new JF(n, t, e);
  }
}
function ZF({ desktopOriginator: e, headers: t, state: n }) {
  !n.attachAuth ||
    n.token == null ||
    B_(t, n.token, { desktopOriginator: e, includeSurfaceHeaders: !1 });
}
function QF(e, t) {
  return e.attachAuth && e.token != null && t.status === 401;
}
var $F = `X-OAI-IS`,
  eI = `X-OAI-IS-Update`,
  tI = /^ois1\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/;
function nI(e) {
  return typeof e != `string` ||
    e.length === 0 ||
    e.length > 2048 ||
    e.trim() !== e
    ? !1
    : tI.test(e);
}
function rI(e) {
  let t = e.get(eI);
  return nI(t) ? t : null;
}
function iI({ currentState: e, expectedState: t, nextState: n }) {
  return nI(n) ? (nI(e) ? e : null) === t : !1;
}
function aI(e) {
  return {
    getRequestState() {
      let t = e.get(a.v);
      return nI(t) ? t : null;
    },
    clear() {
      e.delete(a.v);
    },
    storeResponseUpdate(t, n) {
      return oI(e, rI(t), n);
    },
  };
}
function oI(e, t, n) {
  if (!nI(t)) return !1;
  let r = !1;
  return (
    e.update(a.v, (e) =>
      iI({ currentState: e, expectedState: n, nextState: t })
        ? ((r = !0), t)
        : (e ?? void 0),
    ),
    r
  );
}
var sI = 6,
  cI = new Set([
    `accounts`,
    `backend-api`,
    `by-repo`,
    `conversation`,
    `environments`,
    `account`,
    `announcement`,
    `data-controls`,
    `first-run`,
    `general-settings`,
    `git-settings`,
    `local-environments`,
    `mcp-settings`,
    `open-source-licenses`,
    `personalization`,
    `plan-summary`,
    `select-workspace`,
    `settings`,
    `usage-analytics`,
    `worktrees`,
  ]);
function lI(e) {
  let t = e.length > 0 ? e : `/`;
  return t === `/`
    ? `/`
    : `/${t
        .split(`/`)
        .filter((e) => e.length > 0)
        .map((e) => {
          let t = null;
          try {
            t = decodeURIComponent(e);
          } catch {
            t = null;
          }
          let n = (t ?? e).toLowerCase();
          return n.length <= sI || cI.has(n) ? n : `:param`;
        })
        .join(`/`)}`;
}
function uI(e, t) {
  try {
    let n = new URL(t);
    if (n.protocol !== `http:` && n.protocol !== `https:`)
      return `${e} ${n.protocol}`;
    let r = lI(n.pathname);
    return `${e} ${n.origin}${r}`;
  } catch {
    return `${e} ${t}`;
  }
}
function dI(e) {
  if (!URL.canParse(e)) return;
  let t = new URL(e);
  if (t.protocol !== `http:` && t.protocol !== `https:`) return t.protocol;
  let n = lI(t.pathname);
  return `${t.origin}${n}`;
}
var fI = r.a(`electron-fetch-wrapper`),
  pI = `vscode://codex/`,
  mI = `/codex/dictation-stream-connect-info`,
  hI = `chatgpt-dictation`,
  gI = `openai-bearer.`,
  _I = class {
    abortControllers = new Map();
    constructor(e, t) {
      ((this.fetchHandler = e), (this.options = t));
    }
    getAppServerConnection(e) {
      return this.options.appServerConnectionRegistry.getConnection(e);
    }
    getAbortControllerKey(e, t) {
      return `${e.id}:${t}`;
    }
    async handleRequest(e, t) {
      if (t.url === mI) {
        await this.handleDictationStreamConnectInfoRequest(e, t);
        return;
      }
      let n = new AbortController(),
        r = this.getAbortControllerKey(e, t.requestId);
      if ((this.abortControllers.set(r, n), this.isVsCodeFetchRequest(t.url))) {
        try {
          await this.handleVsCodeFetchRequest(e, t, n.signal);
        } finally {
          this.abortControllers.delete(r);
        }
        return;
      }
      let i = this.ensureAbsoluteUrl(t.url),
        a = uI(t.method, i),
        o = performance.now(),
        s = null;
      try {
        let {
          headers: r,
          body: c,
          binaryResponse: l,
        } = this.prepareFetchInit(t);
        fI().debug(`Fetch`, {
          safe: {
            requestId: t.requestId,
            target: a,
            body: t.body == null ? `<no-body>` : `<body-redacted>`,
          },
          sensitive: {},
        });
        let u = await this.performDesktopFetch({
          body: c,
          headers: r,
          method: t.method,
          onUploadProgress: t.reportUploadProgress
            ? ({ loaded: n, total: r }) => {
                e.send(this.options.messageChannel, {
                  type: `fetch-upload-progress`,
                  requestId: t.requestId,
                  loaded: n,
                  total: r,
                });
              }
            : void 0,
          resolvedUrl: i,
          signal: n.signal,
        });
        s = u.metrics;
        let d = u.response,
          f = yI(d);
        if (d.ok) {
          let n = d.headers.get(`content-type`) ?? ``,
            r = `null`,
            i = 0,
            c = performance.now();
          if (d.status !== 204)
            if (n.includes(`application/json`) && !l)
              ((r = await d.text()), (i = Buffer.byteLength(r)));
            else {
              let e = await d.arrayBuffer();
              i = e.byteLength;
              let t = Buffer.from(e).toString(`base64`);
              r = JSON.stringify({ base64: t, contentType: n });
            }
          ((s.timings.bodyReadMs = vI(c)),
            e.send(this.options.messageChannel, {
              type: `fetch-response`,
              responseType: `success`,
              requestId: t.requestId,
              status: d.status,
              headers: f,
              bodyJsonString: r,
            }),
            this.logFetchTiming({
              bodyBytes: i,
              metrics: s,
              requestId: t.requestId,
              responseType: `success`,
              status: d.status,
              target: a,
              totalMs: vI(o),
            }));
        } else {
          let n,
            r = 0,
            i = performance.now();
          try {
            let e = await d.text();
            ((r = Buffer.byteLength(e)),
              (n =
                e || d.statusText || `Request failed with status ${d.status}`));
          } catch {
            n = d.statusText || `Request failed with status ${d.status}`;
          }
          ((s.timings.bodyReadMs = vI(i)),
            e.send(this.options.messageChannel, {
              type: `fetch-response`,
              responseType: `error`,
              requestId: t.requestId,
              status: d.status,
              error: n,
            }),
            this.logFetchTiming({
              bodyBytes: r,
              metrics: s,
              requestId: t.requestId,
              responseType: `error`,
              status: d.status,
              target: a,
              totalMs: vI(o),
            }));
        }
      } catch (n) {
        let r = n instanceof Error && n.name === `AbortError`,
          i = xI(n),
          c = n instanceof JF ? n : null;
        (e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: t.requestId,
          status: c?.status ?? (r ? 499 : 500),
          error:
            c?.message ?? (n instanceof Error ? n.message : `Unknown error`),
          errorCode: i,
        }),
          this.logFetchTiming({
            bodyBytes: 0,
            metrics: s,
            requestId: t.requestId,
            responseType: `exception`,
            status: c?.status ?? (r ? 499 : 500),
            target: a,
            totalMs: vI(o),
          }));
      } finally {
        this.abortControllers.delete(r);
      }
    }
    cancelRequest(e, t) {
      let n = this.getAbortControllerKey(e, t.requestId),
        r = this.abortControllers.get(n);
      r && (r.abort(), this.abortControllers.delete(n));
    }
    handleStreamRequest(e, t) {
      let n = new AbortController();
      (this.abortControllers.set(this.getAbortControllerKey(e, t.requestId), n),
        this.runStreamRequest(e, t, n));
    }
    cancelStreamRequest(e, t) {
      fI().debug(`Cancel-fetch-stream received with requestId`, {
        safe: { requestId: t.requestId },
        sensitive: {},
      });
      let n = this.getAbortControllerKey(e, t.requestId),
        r = this.abortControllers.get(n);
      r && (r.abort(), this.abortControllers.delete(n));
    }
    async runStreamRequest(e, t, r) {
      let i = this.ensureAbsoluteUrl(t.url),
        a = uI(t.method, i),
        o = performance.now(),
        s = null;
      try {
        let { headers: c, body: l } = this.prepareFetchInit(t);
        fI().debug(`Fetch-stream`, {
          safe: { requestId: t.requestId, target: a },
          sensitive: {},
        });
        let u = await this.performDesktopFetch({
          body: l,
          headers: c,
          method: t.method,
          resolvedUrl: i,
          signal: r.signal,
        });
        s = u.metrics;
        let d = u.response,
          f = yI(d);
        if (!d.ok) {
          let n = null,
            r = 0,
            i = performance.now();
          try {
            ((n = await d.text()), (r = Buffer.byteLength(n)));
          } catch (e) {
            fI().warning(`Fetch-stream error response body read failed`, {
              safe: { requestId: t.requestId, status: d.status },
              sensitive: { error: e, target: a },
            });
          }
          ((s.timings.bodyReadMs = vI(i)),
            this.logFetchTiming({
              bodyBytes: r,
              metrics: s,
              requestId: t.requestId,
              responseType: `stream-error`,
              status: d.status,
              target: a,
              totalMs: vI(o),
            }),
            fI().warning(`Fetch-stream error response`, {
              safe: {
                bodyBytes: r,
                requestId: t.requestId,
                responseHeaderNames: Object.keys(f).sort(),
                status: d.status,
                statusText: d.statusText || null,
              },
              sensitive: {
                cfRay: bI(f, `cf-ray`),
                requestIdHeader: bI(f, `request-id`),
                responseBody: n,
                responseHeaders: f,
                target: a,
                xOaiRequestId: bI(f, `x-oai-request-id`),
              },
            }));
          let c =
            d.statusText.length > 0
              ? `${d.status} ${d.statusText}`
              : `Request failed with status ${d.status}`;
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: t.requestId,
            error: c,
          });
          return;
        }
        this.logFetchTiming({
          bodyBytes: 0,
          metrics: s,
          requestId: t.requestId,
          responseType: `stream-open`,
          status: d.status,
          target: a,
          totalMs: vI(o),
        });
        let p = d.body;
        if (p == null) {
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: t.requestId,
            error: `Streaming response had no body.`,
          });
          return;
        }
        if (
          (e.send(this.options.messageChannel, {
            type: `fetch-stream-response`,
            requestId: t.requestId,
            status: d.status,
            headers: n.yo(d.headers),
          }),
          t.format === `ndjson`)
        )
          for await (let i of n.xo(p, r.signal))
            e.send(this.options.messageChannel, {
              type: `fetch-stream-event`,
              requestId: t.requestId,
              data: i,
            });
        else
          for await (let i of n.d(p, r.signal))
            e.send(this.options.messageChannel, {
              type: `fetch-stream-event`,
              requestId: t.requestId,
              event: i.event,
              data: i.data,
            });
        e.send(this.options.messageChannel, {
          type: `fetch-stream-complete`,
          requestId: t.requestId,
        });
      } catch (i) {
        if (r.signal.aborted || n.u(i)) {
          e.send(this.options.messageChannel, {
            type: `fetch-stream-complete`,
            requestId: t.requestId,
          });
          return;
        }
        (this.logFetchTiming({
          bodyBytes: 0,
          metrics: s,
          requestId: t.requestId,
          responseType: `stream-exception`,
          status: 500,
          target: a,
          totalMs: vI(o),
        }),
          fI().warning(`Fetch-stream failed`, {
            safe: { requestId: t.requestId, target: a },
            sensitive: { error: i },
          }),
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: t.requestId,
            error: i instanceof Error ? i.message : `Unknown error`,
          }));
      } finally {
        this.abortControllers.delete(
          this.getAbortControllerKey(e, t.requestId),
        );
      }
    }
    async performDesktopFetch({
      body: e,
      headers: t,
      method: r,
      onUploadProgress: i,
      resolvedUrl: a,
      signal: o,
    }) {
      let s = n.go(t),
        l = n._o(t),
        u = n.vo(t),
        d = YF(t, { inferAuth: this.shouldInferCodexApiAuth(a) }),
        f = {},
        p = {
          attachAuth: d.attachAuth,
          attachDesktopSurface: s,
          attachDeviceCheckToken: l,
          attachIntegrityState: u,
          hasToken: d.token != null,
          retriedAfter401: !1,
          timings: f,
          tokenSource: d.tokenSource,
        };
      if (d.attachAuth && !this.isDesktopAuthAllowedUrl(a))
        throw new JF(
          `Refusing to attach authentication to non-OpenAI URL`,
          400,
          null,
        );
      if (s && !this.isDesktopAuthAllowedUrl(a))
        throw new JF(
          `Refusing to attach desktop surface to non-OpenAI URL`,
          400,
          null,
        );
      if (l && !s)
        throw new JF(
          `Refusing to attach DeviceCheck token without desktop surface`,
          400,
          null,
        );
      if (l && !this.isDesktopAuthAllowedUrl(a))
        throw new JF(
          `Refusing to attach DeviceCheck token to non-OpenAI URL`,
          400,
          null,
        );
      if (l && !new URL(a).pathname.endsWith(`/ios/attestation_challenge`))
        throw new JF(
          `Refusing to attach DeviceCheck token to unexpected URL`,
          400,
          null,
        );
      if (u && !this.isDesktopAuthAllowedUrl(a))
        throw new JF(
          `Refusing to attach integrity state to non-OpenAI URL`,
          400,
          null,
        );
      let m = () => {
          if (typeof e == `string`) return e;
          if (e == null) return;
          let t = new Uint8Array(e.byteLength);
          return (t.set(e), t.buffer);
        },
        h = async (e) => {
          let n = this.cloneHeaders(t);
          this.deleteHeader(n, $F);
          let d = u ? this.options.integrityStateStore.getRequestState() : null;
          (ZF({
            desktopOriginator: this.options.desktopOriginator,
            headers: n,
            state: e,
          }),
            s && V_(n, { desktopOriginator: this.options.desktopOriginator }),
            u && d != null && this.setHeader(n, $F, d),
            s && (await this.options.deviceCheckCookieManager.ensureCookie(n)),
            l && (await this.options.deviceCheckCookieManager.attachToken(n)));
          let f =
            i == null
              ? await c.net.fetch(a, {
                  method: r,
                  headers: n,
                  body: m(),
                  signal: o,
                  credentials: s ? `include` : `same-origin`,
                })
              : await this.performProgressRequest({
                  body: m(),
                  headers: n,
                  method: r,
                  onUploadProgress: i,
                  resolvedUrl: a,
                  signal: o,
                  useSessionCookies: s,
                });
          return (
            u &&
              this.options.integrityStateStore.storeResponseUpdate(
                f.headers,
                d,
              ),
            f
          );
        },
        g = performance.now();
      ((d = await XF({
        appServerClient: this.getAppServerConnection(V),
        errorStatus: 432,
        failureMessage: `Failed to retrieve authentication token`,
        refreshToken: !1,
        state: d,
      })),
        (f.authMs = vI(g)),
        (p.hasToken = d.token != null),
        (p.tokenSource = d.tokenSource));
      let _ = performance.now(),
        v = await h(d);
      f.fetchMs = vI(_);
      let y = QF(d, v);
      if (
        (v.status === 401 &&
          d.attachAuth &&
          fI().warning(`desktop_fetch_auth_401`, {
            safe: {
              hadToken: d.token != null,
              skipRetryReason: y ? null : `no_token_attached`,
              target: uI(r, a),
              tokenSource: d.tokenSource,
              willRetry: y,
            },
            sensitive: {},
          }),
        y)
      ) {
        p.retriedAfter401 = !0;
        let e = performance.now();
        ((d = await XF({
          appServerClient: this.getAppServerConnection(V),
          errorStatus: 401,
          failureMessage: `Failed to refresh authentication token`,
          refreshToken: !0,
          state: d,
        })),
          (f.retryAuthMs = vI(e)),
          (p.hasToken = d.token != null),
          (p.tokenSource = d.tokenSource));
        let t = performance.now();
        ((v = await h(d)), (f.retryFetchMs = vI(t)));
      }
      return { metrics: p, response: v };
    }
    performProgressRequest({
      body: e,
      headers: t,
      method: n,
      onUploadProgress: r,
      resolvedUrl: i,
      signal: a,
      useSessionCookies: o,
    }) {
      return new Promise((s, l) => {
        let u = c.net.request({
            method: n,
            url: i,
            headers: t,
            useSessionCookies: o,
          }),
          d = -1,
          f = () => {
            let e = u.getUploadProgress();
            !e.started ||
              e.current === d ||
              ((d = e.current), r({ loaded: e.current, total: e.total }));
          },
          p = setInterval(f, 50),
          m = () => {
            clearInterval(p);
          },
          h = () => {
            (m(), a.removeEventListener(`abort`, g));
          },
          g = () => {
            (h(),
              u.abort(),
              l(new DOMException(`The operation was aborted`, `AbortError`)));
          };
        if ((a.addEventListener(`abort`, g, { once: !0 }), a.aborted)) {
          g();
          return;
        }
        (u.on(`error`, (e) => {
          (h(), l(e));
        }),
          u.on(`response`, (e) => {
            (f(), m());
            let t = [];
            (e.on(`data`, (e) => {
              t.push(e);
            }),
              e.on(`error`, (e) => {
                (h(), l(e));
              }),
              e.on(`end`, () => {
                h();
                let n = Buffer.concat(t),
                  r = new Headers();
                for (let [t, n] of Object.entries(e.headers))
                  for (let e of Array.isArray(n) ? n : [n]) r.append(t, e);
                s(
                  new Response(n.length === 0 ? null : n, {
                    status: e.statusCode,
                    statusText: e.statusMessage,
                    headers: r,
                  }),
                );
              }));
          }));
        let _ = e instanceof ArrayBuffer ? Buffer.from(e) : e;
        u.end(_);
      });
    }
    logFetchTiming({
      bodyBytes: e,
      metrics: t,
      requestId: n,
      responseType: r,
      status: i,
      target: a,
      totalMs: o,
    }) {
      fI().debug(`Fetch timing`, {
        safe: {
          requestId: n,
          target: a,
          responseType: r,
          status: i,
          totalMs: o,
          bodyBytes: e,
          attachAuth: t?.attachAuth ?? !1,
          attachDesktopSurface: t?.attachDesktopSurface ?? !1,
          attachDeviceCheckToken: t?.attachDeviceCheckToken ?? !1,
          attachIntegrityState: t?.attachIntegrityState ?? !1,
          hasToken: t?.hasToken ?? !1,
          retriedAfter401: t?.retriedAfter401 ?? !1,
          tokenSource: t?.tokenSource ?? `unknown`,
          authMs: t?.timings.authMs,
          fetchMs: t?.timings.fetchMs,
          retryAuthMs: t?.timings.retryAuthMs,
          retryFetchMs: t?.timings.retryFetchMs,
          bodyReadMs: t?.timings.bodyReadMs,
        },
        sensitive: {},
      });
    }
    prepareFetchInit(e) {
      let t = {},
        n = e.body,
        r = !1,
        i = !1;
      if (e.headers)
        for (let [n, a] of Object.entries(e.headers)) {
          if (n.toLowerCase() === `x-codex-base64`) {
            i = a === `1`;
            continue;
          }
          if (n.toLowerCase() === `x-codex-binary-response`) {
            r = a === `1`;
            continue;
          }
          t[n] = a;
        }
      if (i && typeof n == `string`)
        try {
          n = Buffer.from(n, `base64`);
        } catch (e) {
          fI().warning(`Failed to decode base64 request body`, {
            safe: {},
            sensitive: { body: n, error: e },
          });
        }
      if (!i && typeof n == `string` && !this.hasHeader(t, `Content-Type`)) {
        let e = n.trim();
        if (e.startsWith(`{`) || e.startsWith(`[`))
          try {
            (JSON.parse(n),
              this.setHeader(t, `Content-Type`, `application/json`));
          } catch {}
      }
      return { headers: t, body: n, binaryResponse: r };
    }
    ensureAbsoluteUrl(e) {
      return /^https?:\/\//i.test(e) || e.startsWith(`data:`)
        ? e
        : R_(this.options, e);
    }
    cloneHeaders(e) {
      let t = {};
      for (let [n, r] of Object.entries(e)) t[n] = r;
      return t;
    }
    findHeaderKey(e, t) {
      let n = t.toLowerCase();
      return Object.keys(e).find((e) => e.toLowerCase() === n);
    }
    hasHeader(e, t) {
      return this.findHeaderKey(e, t) != null;
    }
    setHeader(e, t, n) {
      (this.deleteHeader(e, t), (e[t] = n));
    }
    deleteHeader(e, t) {
      let n = this.findHeaderKey(e, t);
      n && delete e[n];
    }
    shouldInferCodexApiAuth(e) {
      if (!this.isDesktopAuthAllowedUrl(e)) return !1;
      let t;
      try {
        t = new URL(e);
      } catch {
        return !1;
      }
      let n = t.pathname.replace(/\/+$/, ``);
      return (
        n === `/wham` ||
        n.startsWith(`/wham/`) ||
        n === `/api/wham` ||
        n.startsWith(`/api/wham/`) ||
        n === `/backend-api/wham` ||
        n.startsWith(`/backend-api/wham/`)
      );
    }
    isDesktopAuthAllowedUrl(e) {
      let t;
      try {
        t = new URL(e);
      } catch {
        return !1;
      }
      if (t.protocol !== `http:` && t.protocol !== `https:`) return !1;
      let n = t.host.toLowerCase();
      return !!(
        n === `localhost` ||
        n === `localhost:8000` ||
        n === `openai.com` ||
        n.endsWith(`.openai.com`) ||
        n === `chatgpt.com` ||
        (n.endsWith(`.chatgpt.com`) && !n.startsWith(`ab.`))
      );
    }
    isVsCodeFetchRequest(e) {
      return e.startsWith(pI);
    }
    async handleVsCodeFetchRequest(e, t, r) {
      let i = t.url.slice(15);
      try {
        let a = await this.fetchHandler.handleVSCodeRequest(
          e,
          i,
          t.body ? JSON.parse(t.body) : void 0,
          n.lo(t.headers?.[`x-codex-git-source`]) ? t.headers[n.ao] : null,
          r,
        );
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `success`,
          requestId: t.requestId,
          status: 200,
          headers: { "content-type": `application/json` },
          bodyJsonString: JSON.stringify(a ?? null),
        });
      } catch (n) {
        let r = xI(n);
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: t.requestId,
          status: 432,
          error:
            n instanceof Error ? n.message : `VS Code bridge request failed`,
          errorCode: r,
        });
      }
    }
    async handleDictationStreamConnectInfoRequest(e, t) {
      try {
        let n = await this.getAppServerConnection(V).getAuthToken({
          refreshToken: !1,
        });
        if (!n) throw Error(`Sign in to ChatGPT to use dictation.`);
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `success`,
          requestId: t.requestId,
          status: 200,
          headers: { "content-type": `application/json` },
          bodyJsonString: JSON.stringify({
            websocketUrl: this.getDictationStreamWebsocketUrl(),
            protocols: [hI, `${gI}${n}`],
          }),
        });
      } catch (n) {
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: t.requestId,
          status: 432,
          error:
            n instanceof Error
              ? n.message
              : `Failed to prepare dictation stream connection.`,
        });
      }
    }
    getDictationStreamWebsocketUrl() {
      let e = new URL(R_(this.options, `/dictation/stream`));
      return (
        (e.protocol = e.protocol === `https:` ? `wss:` : `ws:`),
        e.toString()
      );
    }
  };
function vI(e) {
  return Math.round(performance.now() - e);
}
function yI(e) {
  let t = {};
  return (
    e.headers.forEach((e, n) => {
      t[n] = e;
    }),
    t
  );
}
function bI(e, t) {
  let n = t.toLowerCase(),
    r = Object.keys(e).find((e) => e.toLowerCase() === n);
  return r == null ? void 0 : e[r];
}
function xI(e) {
  if (
    !(
      typeof e != `object` ||
      !e ||
      !(`errorCode` in e) ||
      typeof e.errorCode != `string`
    )
  )
    return e.errorCode;
}
function SI(e) {
  return e.type.startsWith(`avatar-overlay-`);
}
function CI(e) {
  let t = `pointerScreenX` in e ? e.pointerScreenX : void 0,
    n = `pointerScreenY` in e ? e.pointerScreenY : void 0;
  return typeof t != `number` || typeof n != `number`
    ? null
    : { pointerScreenX: t, pointerScreenY: n };
}
async function wI(e, t, n, r = () => {}) {
  switch (n.type) {
    case `avatar-overlay-close`:
      e.close();
      return;
    case `avatar-overlay-hide`:
      e.hide();
      return;
    case `avatar-overlay-open`:
      await e.toggle(t);
      return;
    case `avatar-overlay-open-state-request`:
      r({ type: `avatar-overlay-open-state-changed`, isOpen: e.isOpen() });
      return;
    case `avatar-overlay-drag-start`: {
      let r = CI(n),
        i =
          r == null
            ? {
                pointerWindowX: n.pointerWindowX,
                pointerWindowY: n.pointerWindowY,
              }
            : {
                ...r,
                pointerWindowX: n.pointerWindowX,
                pointerWindowY: n.pointerWindowY,
              };
      e.startDrag(t.id, i, n.usesOrbPhysics ?? !1);
      return;
    }
    case `avatar-overlay-drag-move`: {
      let r = CI(n);
      r ? e.moveDrag(t.id, r) : e.moveDrag(t.id);
      return;
    }
    case `avatar-overlay-drag-end`: {
      let r = CI(n);
      r ? e.endDrag(t.id, r) : e.endDrag(t.id);
      return;
    }
    case `avatar-overlay-drag-release`:
      e.throwWithVelocity(t.id, n.velocityX, n.velocityY, n.shouldBounce);
      return;
    case `avatar-overlay-mascot-resize-start`:
      e.startMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-mascot-resize-move`:
      e.moveMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-mascot-resize-end`:
      e.endMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-element-size-changed`:
      e.setElementSize(t.id, {
        ...(n.elementSizeRevision == null
          ? {}
          : { elementSizeRevision: n.elementSizeRevision }),
        isTrayVisible: n.isTrayVisible,
        mascot: n.mascot,
        nativeCompositionEnabled: n.nativeCompositionEnabled,
        tray: n.tray,
      });
      return;
    case `avatar-overlay-composition-changed`:
      e.setCompositionState(t.id, n.state);
      return;
    case `avatar-overlay-composition-surface-action`:
      e.handleCompositionSurfaceAction(t.id, n.action);
      return;
    case `avatar-overlay-pointer-interaction-changed`:
      e.setPointerInteraction(t.id, n.isInteractive);
      return;
    case `avatar-overlay-keyboard-interaction-changed`:
      e.setKeyboardInteraction(t.id, n.isInteractive);
      return;
  }
}
var TI = n.xl({ feature_status: n.Cl(n.wl(), n.ml()) }),
  EI = r.a(`browser-sidebar-comment-mode-site-status`),
  DI = 1440 * 60 * 1e3,
  OI = `agent`,
  kI = { desktopOriginator: mu, devApiBaseUrl: pu, prodApiBaseUrl: fu };
function AI({
  appServerClient: e,
  desktopApiOptions: t = kI,
  now: n = Date.now,
}) {
  let r = new Map(),
    i = new Map();
  function a(e) {
    let t = MI(e);
    if (t == null) return !1;
    let i = r.get(t);
    return i == null || n() - i.timestampMs >= DI
      ? (i != null && r.delete(t), null)
      : i.blocked;
  }
  async function o(e) {
    let t = a(e);
    if (t != null) return t;
    let n = MI(e);
    if (n == null) return !1;
    let r = i.get(n);
    if (r != null) return r;
    let o = s(e, n).finally(() => {
      i.get(n) === o && i.delete(n);
    });
    return (i.set(n, o), o);
  }
  async function s(e, t) {
    try {
      let i = await l(e);
      if (i == null) return !1;
      let a = i.feature_status[OI] === !0;
      return (r.set(t, { blocked: a, timestampMs: n() }), a);
    } catch (e) {
      return (
        EI().warning(
          `failed to load browser sidebar comment mode site status`,
          { safe: {}, sensitive: { error: e } },
        ),
        !1
      );
    }
  }
  async function l(e) {
    let n = NI(e),
      r = await u(!1),
      i = await c.net.fetch(R_(t, n), { method: `GET`, headers: r });
    return (
      i.status === 401 &&
        ((r = await u(!0)),
        (i = await c.net.fetch(R_(t, n), { method: `GET`, headers: r }))),
      i.ok
        ? TI.parse(JSON.parse(await i.text()))
        : (EI().warning(
            `browser sidebar comment mode site status request failed`,
            { safe: { status: i.status }, sensitive: {} },
          ),
          null)
    );
  }
  function u(n) {
    return z_({
      action: `load browser site status`,
      appServerClient: e,
      desktopOriginator: t.desktopOriginator,
      refreshToken: n,
    });
  }
  return { cachedCommentModeBlocked: a, isCommentModeBlocked: o };
}
function jI() {
  return {
    cachedCommentModeBlocked: () => !1,
    isCommentModeBlocked: async () => !1,
  };
}
function MI(e) {
  try {
    let t = new URL(e);
    if (t.protocol !== `http:` && t.protocol !== `https:`) return null;
    let n = t.hostname.trim().toLowerCase();
    return n.length === 0 ? null : n.startsWith(`www.`) ? n.slice(4) : n;
  } catch {
    return null;
  }
}
function NI(e) {
  return `/aura/site_status?${new URLSearchParams({ site_url: e }).toString()}`;
}
function PI(e) {
  return FI(e)
    ? { type: `step-zoom`, delta: 1 }
    : II(e)
      ? { type: `step-zoom`, delta: -1 }
      : LI(e)
        ? { type: `reset-zoom` }
        : null;
}
function FI(e) {
  return (
    RI(e) &&
    (e.code === `NumpadAdd` ||
      e.code === `Equal` ||
      e.key === `+` ||
      e.key === `=`)
  );
}
function II(e) {
  return (
    RI(e) &&
    (e.code === `NumpadSubtract` ||
      e.code === `Minus` ||
      e.key === `-` ||
      e.key === `_`)
  );
}
function LI(e) {
  return (
    RI(e) &&
    e.shift !== !0 &&
    (e.code === `Digit0` || e.code === `Numpad0` || e.key === `0`)
  );
}
function RI(e) {
  let t = process.platform === `darwin`;
  return (
    e.type === `keyDown` &&
    e.alt !== !0 &&
    (t ? e.meta === !0 && e.control !== !0 : e.control === !0 && e.meta !== !0)
  );
}
var zI = new Set([`Meta`, `Control`, `Alt`, `AltGraph`, `Shift`]),
  BI = new Map([
    [`Escape`, `Esc`],
    [`ArrowUp`, `Up`],
    [`ArrowDown`, `Down`],
    [`ArrowLeft`, `Left`],
    [`ArrowRight`, `Right`],
  ]),
  VI = new Map([
    [`1`, `!`],
    [`2`, `@`],
    [`3`, `#`],
    [`4`, `$`],
    [`5`, `%`],
    [`6`, `^`],
    [`7`, `&`],
    [`8`, `*`],
    [`9`, `(`],
    [`0`, `)`],
    [`-`, `_`],
    [`=`, `+`],
    [`[`, `{`],
    [`]`, `}`],
    [`\\`, `|`],
    [`;`, `:`],
    [`'`, `"`],
    [`,`, `<`],
    [`.`, `>`],
    [`/`, `?`],
    ["`", `~`],
  ]);
function HI(e, t) {
  return e.type !== `keyDown` || t.includes(` `)
    ? !1
    : UI(
        {
          altKey: e.alt === !0,
          code: e.code,
          ctrlKey: e.control === !0,
          key: e.key,
          metaKey: e.meta === !0,
          repeat: e.isAutoRepeat === !0,
          shiftKey: e.shift === !0,
        },
        t,
      );
}
function UI(e, t) {
  let n = WI(t);
  return (
    n.key.length > 0 &&
    KI(e, n) &&
    e.ctrlKey === n.requireCtrl &&
    e.metaKey === n.requireMeta &&
    e.altKey === n.requireAlt &&
    e.shiftKey === n.requireShift
  );
}
function WI(e) {
  let t = e.split(`+`).filter(Boolean),
    n = process.platform === `darwin`,
    r = ``,
    i = !1,
    a = !1,
    o = !1,
    s = !1;
  for (let e of t)
    switch (e) {
      case `CmdOrCtrl`:
        n ? (o = !0) : (a = !0);
        break;
      case `Command`:
      case `Cmd`:
        o = !0;
        break;
      case `Control`:
      case `Ctrl`:
        a = !0;
        break;
      case `Alt`:
      case `Option`:
        i = !0;
        break;
      case `Shift`:
        s = !0;
        break;
      default:
        r = GI(e);
        break;
    }
  return {
    key: r,
    requireAlt: i,
    requireCtrl: a,
    requireMeta: o,
    requireShift: s,
  };
}
function GI(e) {
  switch (e.toLowerCase()) {
    case `space`:
      return ` `;
    case `plus`:
      return `+`;
    case `esc`:
    case `escape`:
      return `escape`;
    case `up`:
    case `arrowup`:
      return `arrowup`;
    case `down`:
    case `arrowdown`:
      return `arrowdown`;
    case `left`:
    case `arrowleft`:
      return `arrowleft`;
    case `right`:
    case `arrowright`:
      return `arrowright`;
    case `pgup`:
    case `pageup`:
      return `pageup`;
    case `pgdn`:
    case `pagedown`:
      return `pagedown`;
    default:
      return e.toLowerCase();
  }
}
function KI(e, t) {
  let n = qI(e);
  return n === t.key || (t.requireShift && n === (VI.get(t.key) ?? t.key));
}
function qI(e) {
  return GI(JI(e) ?? ``);
}
function JI(e) {
  let { key: t, code: n, altKey: r } = e;
  if (zI.has(t)) return null;
  if (r) {
    let e = YI(n);
    if (e != null) return e;
  }
  return t === ` `
    ? `Space`
    : t === `+`
      ? `Plus`
      : (BI.get(t) ??
        (/^f\d{1,2}$/i.test(t) || t.length === 1
          ? t.toUpperCase()
          : (YI(n) ?? t)));
}
function YI(e) {
  return e == null
    ? null
    : /^Key[A-Z]$/.test(e)
      ? e.slice(3)
      : /^Digit[0-9]$/.test(e)
        ? e.slice(5)
        : e === `Space`
          ? `Space`
          : null;
}
function XI({
  allowDebugMenu: e,
  getCloseTabAccelerators: t = sL,
  getCommandKeymapState: n = () => null,
  getState: r,
  sendAction: i,
}) {
  return (a, o) => {
    let s = r();
    if (s?.terminalFocused === !0 && aL(o, `t`)) {
      let e = oL(o, n());
      if (e != null && e.kind !== `webview`) return;
      (a.preventDefault(),
        e != null &&
          (!o.isAutoRepeat || e.allowsKeyRepeat === !0) &&
          i({
            type: `run-command`,
            id: e.id,
            keyboardEvent: {
              altKey: o.alt === !0,
              code: o.code,
              ctrlKey: o.control === !0,
              key: o.key,
              metaKey: o.meta === !0,
              repeat: o.isAutoRepeat === !0,
              shiftKey: o.shift === !0,
            },
          }));
      return;
    }
    let c = ZI(o, { allowDebugMenu: e, closeTabAccelerators: t(), state: s });
    c != null && (a.preventDefault(), i(c));
  };
}
function ZI(
  e,
  { allowDebugMenu: t, closeTabAccelerators: n = sL(), state: r },
) {
  if (t && iL(e)) return { type: `toggle-debug-modal` };
  if (r == null) return null;
  if (r.imagePreviewOpen) {
    let t = PI(e);
    if (t != null) return { type: `image-preview-zoom-command`, command: t };
  }
  let i = null,
    a = null;
  switch (r.focusArea) {
    case `bottom-panel`:
      r.bottomPanelBrowserCanZoom &&
        ((i = r.bottomPanelBrowserConversationId),
        (a = r.bottomPanelBrowserTabId));
      break;
    case `main`:
      break;
    case `right-panel`:
      r.rightPanelBrowserCanZoom &&
        ((i = r.rightPanelBrowserConversationId),
        (a = r.rightPanelBrowserTabId));
      break;
  }
  if (i != null) {
    let t = PI(e);
    if (t != null)
      return {
        type: `browser-sidebar-command`,
        conversationId: i,
        browserTabId: a ?? void 0,
        command: t,
      };
  }
  return r.terminalFocused === !0 && aL(e, `k`)
    ? { type: `clear-active-terminal` }
    : n.some((t) => HI(e, t))
      ? QI(r)
      : null;
}
function QI(e) {
  if (e == null) return null;
  switch (tL(e)) {
    case `bottom`:
      return e.bottomPanelCanCloseActiveTab
        ? { panelId: `bottom`, type: `close-active-app-shell-tab` }
        : null;
    case `right`:
      return e.rightPanelCanCloseActiveTab
        ? { panelId: `right`, type: `close-active-app-shell-tab` }
        : null;
    case null:
      return null;
  }
}
function $I(e) {
  if (e == null) return null;
  switch (e.focusArea) {
    case `bottom-panel`:
      return `bottom`;
    case `main`:
      return `right`;
    case `right-panel`:
      return `right`;
  }
}
function eL(e, t, n, r) {
  switch (tL(e, t, n, r)) {
    case `bottom`:
      return e?.bottomPanelCanCloseActiveTab === !0
        ? { panelId: `bottom`, type: `close-active-app-shell-tab` }
        : null;
    case `right`:
      return e?.rightPanelCanCloseActiveTab === !0
        ? { panelId: `right`, type: `close-active-app-shell-tab` }
        : null;
    case null:
      return null;
  }
}
function tL(e, t, n, r) {
  if (r != null) return r;
  if (t != null && n != null) {
    let r = rL(e, t, n);
    if (r != null) return r;
  }
  return $I(e);
}
function nL(e) {
  switch (e) {
    case `bottom-panel`:
      return `bottom`;
    case `right-panel`:
      return `right`;
    case `hidden-browser-use`:
    case null:
      return null;
  }
}
function rL(e, t, n) {
  return e?.bottomPanelBrowserConversationId === t &&
    e.bottomPanelBrowserTabId === n
    ? `bottom`
    : e?.rightPanelBrowserConversationId === t && e.rightPanelBrowserTabId === n
      ? `right`
      : null;
}
function iL(e) {
  return (
    e.type === `keyDown` &&
    (e.key.toLowerCase() === `d` || e.code === `KeyD`) &&
    e.alt === !0 &&
    e.control !== !0 &&
    e.meta !== !0 &&
    e.shift !== !0
  );
}
function aL(e, t) {
  let n = process.platform === `darwin`;
  return (
    e.type === `keyDown` &&
    e.key.toLowerCase() === t &&
    e.alt !== !0 &&
    e.shift !== !0 &&
    (n ? e.meta === !0 && e.control !== !0 : e.control === !0 && e.meta !== !0)
  );
}
function oL(e, t) {
  return (
    n.nn.find((r) =>
      n
        .Qt({
          commandId: r.id,
          keymapState: t,
          isMacOS: process.platform === `darwin`,
        })
        .some((t) => HI(e, t)),
    ) ?? null
  );
}
function sL() {
  return n.$t({
    commandId: `closeTab`,
    isMacOS: process.platform === `darwin`,
  });
}
var cL = r.a(`browser-sidebar-manager`);
async function lL({
  browserTabId: e,
  browserPageUrl: t,
  conversationId: n,
  owner: r,
  sourceUrl: i,
  webContents: a,
  windowManager: o,
}) {
  if (URL.canParse(i))
    try {
      let s = await fL(await a.session.fetch(i, { referrer: t }), i);
      if (s == null) return;
      o.sendMessageToWebContents(r, {
        type: `browser-sidebar-image-attachment`,
        browserTabId: e,
        conversationId: n,
        image: s,
      });
    } catch (e) {
      cL().warning(`Failed to attach browser image to thread`, {
        safe: { conversationId: n },
        sensitive: { error: e },
      });
    }
}
function uL(e) {
  return (
    e.mediaType === `image` &&
    e.hasImageContents &&
    e.linkURL.length === 0 &&
    e.srcURL.length > 0
  );
}
function dL(e, t, n, r, i) {
  let a = e.get(r.id) ?? null;
  a?.conversationId !== n ||
    a.browserTabId !== t ||
    (e.delete(r.id),
    i.sendMessageToWebContents(r, {
      type: `browser-sidebar-image-drag-state`,
      browserTabId: t,
      conversationId: n,
      isActive: !1,
    }));
}
async function fL(e, t) {
  if (!e.ok) return null;
  let r = e.headers.get(`content-type`)?.split(`;`)[0];
  if (r == null || !n.Ti({ name: t, type: r })) return null;
  let i = Buffer.from(await e.arrayBuffer());
  return i.length === 0
    ? null
    : {
        dataUrl: `data:${r};base64,${i.toString(`base64`)}`,
        filename: pL(t, r),
      };
}
function pL(e, t) {
  try {
    let t = new URL(e).pathname.split(`/`).at(-1);
    if (t && n.Ei(t)) return t;
  } catch {}
  return `browser-image.${t === `image/jpeg` ? `jpg` : t.slice(6)}`;
}
var mL = `data-browser-sidebar-conversation-id`,
  hL = `data-browser-sidebar-browser-tab-id`;
function gL(e) {
  let t = e[mL] ?? e[`data-conversation-id`] ?? null,
    r = e[hL] ?? null;
  if (typeof t == `string` && t.length > 0)
    return {
      browserTabId: typeof r == `string` && r.length > 0 ? n._c(r) : yL(t),
      conversationId: t,
    };
  let i = vL(e.partition);
  if (i == null || !i.startsWith(`persist:codex-browser-app-route:`))
    return null;
  try {
    let e = decodeURIComponent(i.slice(n.Ro.length));
    if (e.length === 0) return null;
    let [t, r] = e.split(`\0`);
    return t == null || t.length === 0
      ? null
      : {
          browserTabId: r == null || r.length === 0 ? yL(t) : n._c(r),
          conversationId: t,
        };
  } catch {
    return null;
  }
}
function _L(e) {
  let t = e.partition;
  if (typeof t != `string`) return null;
  let r = t.lastIndexOf(n.Lo);
  if (r === -1) return null;
  let i = t.slice(r + n.Lo.length),
    a = i.lastIndexOf(`:`);
  if (a === -1) return null;
  let o = i.slice(0, a),
    s = Number(i.slice(a + 1));
  return o.length === 0 || !Number.isInteger(s) || s <= 0
    ? null
    : { hostGeneration: s, rendererInstanceId: o };
}
function vL(e) {
  if (e == null) return null;
  let t = e.lastIndexOf(n.Lo);
  return t === -1 ? e : e.slice(0, t);
}
function yL(e) {
  return n._c(`${e}:legacy`);
}
function bL({ browserTabId: e, conversationId: t }) {
  return n.Bo(e === yL(t) ? t : `${t}\0${e}`);
}
var xL = 1e3,
  SL = `about:blank#codex-browser-sidebar-attach-token=`,
  CL = `CODEX_BROWSER_USE_DEFAULT_VIEWPORT_SIZE`,
  wL = r.a(`browser-sidebar-manager`);
function TL() {
  let e = process.env[CL]?.trim(),
    t = /^(?<width>\d+)x(?<height>\d+)$/u.exec(e ?? ``);
  return t?.groups == null
    ? null
    : {
        width: Number.parseInt(t.groups.width, 10),
        height: Number.parseInt(t.groups.height, 10),
      };
}
async function EL(e, t) {
  let n = e.debugger.isAttached(),
    r = null;
  try {
    (n || e.debugger.attach(`1.3`),
      await Promise.race([
        e.debugger.sendCommand(`Emulation.setEmulatedMedia`, {
          features: [{ name: `prefers-color-scheme`, value: t }],
        }),
        new Promise((e, t) => {
          ((r = setTimeout(() => {
            t(Error(`Timed out syncing browser page color scheme`));
          }, xL)),
            r.unref());
        }),
      ]));
  } catch (t) {
    wL().warning(`failed to sync browser page color scheme`, {
      safe: { webContentsId: e.id },
      sensitive: { error: t },
    });
  } finally {
    if ((r != null && clearTimeout(r), !n && e.debugger.isAttached()))
      try {
        e.debugger.detach();
      } catch (t) {
        wL().warning(`failed to detach browser page debugger`, {
          safe: { webContentsId: e.id },
          sensitive: { error: t },
        });
      }
  }
}
function DL(e, t, n) {
  return n === yL(t) ? `${e}:${t}` : `${e}:${t}:${n}`;
}
function OL({ browserTabId: e, conversationId: t }) {
  return e === yL(t) ? t : `${t}\0${e}`;
}
function kL(e) {
  try {
    return a.H().formatMessage(e);
  } catch {
    return qL(e);
  }
}
function AL(e) {
  return e === `about:blank` ? `` : e;
}
function jL(e, t) {
  let n = AL(e),
    r = AL(t);
  if (n.length === 0 || r.length === 0) return !0;
  try {
    let e = new URL(n),
      t = new URL(r);
    return e.protocol === `file:` || t.protocol === `file:`
      ? e.href !== t.href
      : e.origin !== t.origin;
  } catch {
    return n !== r;
  }
}
function ML(e) {
  try {
    let t = new URL(e);
    return t.protocol === `file:`
      ? `file_html`
      : t.protocol !== `http:` && t.protocol !== `https:`
        ? `unknown`
        : t.hostname === `localhost` ||
            t.hostname === `127.0.0.1` ||
            t.hostname === `[::1]` ||
            t.hostname === `::1`
          ? `local_http`
          : `remote_http`;
  } catch {
    return `unknown`;
  }
}
function NL(e) {
  return e.pendingUrl.length > 0 && e.pendingUrl !== e.url
    ? e.pendingUrl
    : e.url;
}
function PL({ faviconUrl: e, title: t, url: n }) {
  return {
    runtime: `electron-webview`,
    faviconUrl: e,
    navigation: HL(t, n),
    title: t,
    updatedAt: Date.now(),
    url: n,
  };
}
function FL({ faviconUrl: e, title: t, url: n, webContents: r }) {
  return {
    runtime: `electron-webview`,
    faviconUrl: e,
    navigation: VL(t, n, r),
    title: t,
    updatedAt: Date.now(),
    url: n,
  };
}
function IL(e) {
  let t = e?.navigation ?? null;
  if (t == null || t.entries.length === 0) return null;
  let n = t.currentIndex;
  return n == null || n < 0 || n >= t.entries.length
    ? null
    : t.entries.length > 1 ||
        n !== 0 ||
        t.entries.some((e) => e.pageState != null)
      ? t
      : null;
}
function LL(e) {
  return RL(e)?.navigation ?? null;
}
function RL(e) {
  if (e == null) return null;
  let { navigation: t } = e,
    { currentIndex: n } = t;
  if (n == null || n < 0 || n >= t.entries.length) return null;
  let r = t.entries[n]?.url ?? ``;
  if (r.length === 0 || r.startsWith(SL)) return null;
  let i = [],
    a = n;
  for (let [e, r] of t.entries.entries()) {
    if (r.url.startsWith(SL)) {
      e < n && --a;
      continue;
    }
    i.push(r);
  }
  return { ...e, navigation: { ...t, currentIndex: a, entries: i } };
}
function zL(e, t, n) {
  return Math.min(n, Math.max(t, Math.round(e)));
}
function BL(e) {
  return Math.max(1, Math.ceil(e));
}
function VL(e, t, n) {
  let r = n.navigationHistory;
  if (
    t.length > 0 &&
    !n.isDestroyed() &&
    !n.isLoading() &&
    r != null &&
    typeof r.getAllEntries == `function` &&
    typeof r.getActiveIndex == `function`
  ) {
    let e = r
      .getAllEntries()
      .map((e) => ({ pageState: e.pageState, title: e.title, url: e.url }));
    if (e.length > 0) return { currentIndex: r.getActiveIndex(), entries: e };
  }
  return HL(e, t);
}
function HL(e, t) {
  return t.length === 0
    ? { currentIndex: null, entries: [] }
    : { currentIndex: 0, entries: [{ title: e, url: t }] };
}
function UL(e) {
  e.navigationVersion += 1;
}
function WL(e) {
  return e.page == null ? e.snapshot.url : NL(e.page);
}
function GL(e, t, n) {
  if (e.isDestroyed() || t == null || n == null || n.isDestroyed()) return;
  let r = c.screen.getCursorScreenPoint(),
    i = e.getContentBounds(),
    a = r.x - i.x - t.x,
    o = r.y - i.y - t.y;
  a < 0 ||
    o < 0 ||
    a > t.width ||
    o > t.height ||
    n.sendInputEvent({
      type: `mouseMove`,
      x: a,
      y: o,
      movementX: 0,
      movementY: 0,
    });
}
function KL(e, t, n) {
  let r = n.view.webContents;
  return r == null || r.isDestroyed()
    ? null
    : {
        browserTabId: n.browserTabId,
        pageKey: DL(e.owner.id, t, n.browserTabId),
        webContents: r,
        title: n.title || n.fallbackTitle,
        url: NL(n),
        active:
          e.activeConversationId === t &&
          e.activeBrowserTabId === n.browserTabId,
      };
}
function qL({ defaultMessage: e, values: t }) {
  return e.replace(/\{(\w+)\}/g, (e, n) => t?.[n] ?? e);
}
var JL = r.a(`browser-sidebar-page-store`),
  YL = 500,
  XL = 64 * 1024 * 1024,
  ZL = 100,
  QL = n
    .xl({
      faviconUrl: n.wl().nullable(),
      navigation: n
        .xl({
          currentIndex: n.bl().int().nullable(),
          entries: n
            .pl(
              n
                .xl({
                  pageState: n.wl().optional(),
                  title: n.wl(),
                  url: n.wl(),
                })
                .strict(),
            )
            .max(YL),
        })
        .strict(),
      runtime: n.vl(`electron-webview`),
      title: n.wl(),
      updatedAt: n.bl().finite(),
      url: n.wl(),
    })
    .strict()
    .refine(
      ({ navigation: e }) =>
        e.currentIndex == null ||
        (e.currentIndex >= 0 && e.currentIndex < e.entries.length),
    ),
  $L = n
    .xl({
      browserStorageId: n.wl().transform(n.Bo),
      browserTabId: n.wl().transform(n._c),
      conversationId: n.wl(),
      page: QL,
    })
    .strict(),
  eR = n.xl({ pages: n.Cl(n.wl(), n.El()), version: n.vl(1) }).strict(),
  tR = class {
    enabled;
    hasLoadedPersistedEntries = !1;
    maxBytes;
    maxPages;
    entries = new Map();
    constructor(
      e,
      { enabled: t = !0, maxBytes: n = XL, maxPages: r = ZL } = {},
    ) {
      ((this.persistencePath = e),
        (this.enabled = t),
        (this.maxBytes = n),
        (this.maxPages = r),
        t && this.loadPersistedEntries());
    }
    setEnabled(e) {
      this.enabled !== e &&
        ((this.enabled = e), e && this.loadPersistedEntries());
    }
    get(e) {
      return this.enabled ? (this.entries.get(e)?.page ?? null) : null;
    }
    getOwner(e) {
      this.loadPersistedEntries();
      let t = this.entries.get(e);
      return t == null
        ? null
        : { browserTabId: t.browserTabId, conversationId: t.conversationId };
    }
    set(e, t) {
      if (!this.enabled) return;
      let n = iR(t);
      if (n == null) return;
      let r = this.entries.get(e.browserStorageId) ?? null;
      if (
        r != null &&
        (r.browserTabId !== e.browserTabId ||
          r.conversationId !== e.conversationId)
      )
        return;
      let i = r == null ? null : IL(r.page),
        a = IL(n);
      if (i != null && a == null) return;
      let o = new Map(this.entries);
      o.set(e.browserStorageId, {
        browserStorageId: e.browserStorageId,
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        page: n,
      });
      let s = nR(o.values(), this.maxPages, this.maxBytes);
      (r != null &&
        !s.some((t) => t.browserStorageId === e.browserStorageId)) ||
        this.replaceEntries(s);
    }
    reassociate(e, t) {
      if (!this.enabled) return;
      let n = this.entries.get(e);
      n == null ||
        (n.browserTabId === t.browserTabId &&
          n.conversationId === t.conversationId) ||
        this.entries.set(e, {
          browserStorageId: e,
          browserTabId: t.browserTabId,
          conversationId: t.conversationId,
          page: n.page,
        });
    }
    deleteOwnedAndFlush(e) {
      return this.deleteOwnedPagesAndFlush([e]).length === 1;
    }
    deleteOwnedPagesAndFlush(e) {
      this.loadPersistedEntries();
      let t = [],
        n = !1;
      for (let r of e) {
        let e = this.entries.get(r.browserStorageId);
        (e != null &&
          (e.browserTabId !== r.browserTabId ||
            e.conversationId !== r.conversationId)) ||
          (e != null && (this.entries.delete(r.browserStorageId), (n = !0)),
          t.push(r.browserStorageId));
      }
      return (n && this.flushEntries(), t);
    }
    deleteConversationAndFlush(e) {
      this.loadPersistedEntries();
      let t = [];
      for (let n of this.entries.values())
        n.conversationId === e &&
          (this.entries.delete(n.browserStorageId), t.push(n.browserStorageId));
      return (t.length > 0 && this.flushEntries(), t);
    }
    clearAndDisable() {
      if (
        (this.loadPersistedEntries(),
        (this.enabled = !1),
        this.entries.clear(),
        this.persistencePath != null)
      )
        try {
          (0, p.rmSync)(this.persistencePath, { force: !0 });
        } catch (e) {
          JL().warning(`failed to clear persisted browser sidebar pages`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    flush() {
      this.enabled && this.flushEntries();
    }
    flushEntries() {
      let e = nR(this.entries.values(), this.maxPages, this.maxBytes);
      if ((this.replaceEntries(e), this.persistencePath != null))
        try {
          aR(this.persistencePath, e);
        } catch (e) {
          JL().warning(`failed to persist browser sidebar pages`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    replaceEntries(e) {
      this.entries.clear();
      for (let t of e) this.entries.set(t.browserStorageId, t);
    }
    loadPersistedEntries() {
      if (this.hasLoadedPersistedEntries || this.persistencePath == null)
        return;
      this.hasLoadedPersistedEntries = !0;
      let e = nR(
        rR(this.persistencePath, this.maxBytes),
        this.maxPages,
        this.maxBytes,
      );
      this.replaceEntries(e);
    }
  };
function nR(e, t, n) {
  let r = [],
    i = Buffer.byteLength(
      `{"pages":{},"version":1}
`,
      `utf8`,
    ),
    a = Array.from(e).sort((e, t) => t.page.updatedAt - e.page.updatedAt);
  for (let e of a) {
    if (r.length >= t) break;
    let a =
      Buffer.byteLength(
        `${JSON.stringify(e.browserStorageId)}:${JSON.stringify(e)}`,
        `utf8`,
      ) + (r.length === 0 ? 0 : 1);
    i + a <= n && (r.push(e), (i += a));
  }
  return r;
}
function rR(e, t) {
  try {
    if ((0, p.statSync)(e).size > t) return [];
    let n = eR.safeParse(JSON.parse((0, p.readFileSync)(e, `utf8`)));
    return n.success
      ? Object.entries(n.data.pages).flatMap(([e, t]) => {
          let n = $L.safeParse({
            ...(typeof t == `object` && t ? t : {}),
            browserStorageId: e,
          });
          if (!n.success) return [];
          let r = iR(n.data.page);
          return r == null ? [] : [{ ...n.data, page: r }];
        })
      : [];
  } catch {
    return [];
  }
}
function iR(e) {
  let t = QL.safeParse(e);
  return t.success ? RL(t.data) : null;
}
function aR(e, t) {
  let n = (0, u.dirname)(e);
  (0, p.mkdirSync)(n, { recursive: !0 });
  let r = (0, u.join)(
    n,
    `.${(0, u.basename)(e)}.tmp-${Date.now()}-${(0, f.randomUUID)()}`,
  );
  try {
    ((0, p.writeFileSync)(
      r,
      `${JSON.stringify({ pages: Object.fromEntries(t.map(({ browserStorageId: e, ...t }) => [e, t])), version: 1 })}\n`,
      `utf8`,
    ),
      (0, p.renameSync)(r, e));
  } catch (e) {
    throw ((0, p.rmSync)(r, { force: !0 }), e);
  }
}
var oR = class {
  deletedBrowserStorageIds = new Set();
  enabledBrowserStorageIds = new Set();
  constructor(e) {
    this.store = e;
  }
  enablePersistence(e) {
    (this.enabledBrowserStorageIds.add(e.browserStorageId),
      this.store.setEnabled(!0));
  }
  hasDurableStorageIdentity(e) {
    return (
      this.enabledBrowserStorageIds.has(e) ||
      this.deletedBrowserStorageIds.has(e) ||
      this.store.getOwner(e) != null
    );
  }
  isDurableStorageIdentityOwnedBy(e) {
    let t = this.store.getOwner(e.browserStorageId);
    return (
      t != null &&
      t.browserTabId === e.browserTabId &&
      t.conversationId === e.conversationId
    );
  }
  clearAndDisable() {
    (this.deletedBrowserStorageIds.clear(),
      this.enabledBrowserStorageIds.clear(),
      this.store.clearAndDisable());
  }
  deleteOwnedPagesAndFlush(e) {
    this.markBrowserStorageIdsDeleted(this.store.deleteOwnedPagesAndFlush(e));
  }
  deleteConversationAndFlush(e, t) {
    (this.deleteOwnedPagesAndFlush(t),
      this.markBrowserStorageIdsDeleted(
        this.store.deleteConversationAndFlush(e),
      ));
  }
  markBrowserStorageIdsDeleted(e) {
    for (let t of e)
      (this.enabledBrowserStorageIds.delete(t),
        this.deletedBrowserStorageIds.add(t));
  }
  get(e) {
    if (!this.enabledBrowserStorageIds.has(e.browserStorageId)) return null;
    let t = this.store.getOwner(e.browserStorageId);
    return t == null ||
      t.browserTabId !== e.browserTabId ||
      t.conversationId !== e.conversationId
      ? null
      : this.store.get(e.browserStorageId);
  }
  getDebugMetadata(e) {
    let t = this.get(e);
    return t == null
      ? null
      : {
          historyEntryCount: t.navigation.entries.length,
          updatedAtMs: t.updatedAt,
        };
  }
  persistCurrent(e) {
    this.deletedBrowserStorageIds.has(e.browserStorageId) ||
      ((e.serializedPageState = cR(e)),
      this.enabledBrowserStorageIds.has(e.browserStorageId) &&
        e.serializedPageState != null &&
        this.store.set(
          {
            browserStorageId: e.browserStorageId,
            browserTabId: e.browserTabId,
            conversationId: e.conversationId,
          },
          e.serializedPageState,
        ));
  }
  persistCurrentAndFlush(e) {
    (this.persistCurrent(e), this.store.flush());
  }
  deleteAndFlush(e) {
    let t = this.store.deleteOwnedAndFlush(e);
    return (t && this.enabledBrowserStorageIds.delete(e.browserStorageId), t);
  }
  deleteClosedPageAndFlush(e) {
    let t = this.hasDurableStorageIdentity(e.browserStorageId);
    this.deleteAndFlush(e) &&
      t &&
      this.deletedBrowserStorageIds.add(e.browserStorageId);
  }
  reassociateAndFlush(e) {
    this.enabledBrowserStorageIds.has(e.browserStorageId) &&
      (this.store.reassociate(e.browserStorageId, {
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
      }),
      this.store.flush());
  }
  persistWindowBeforeShutdown(e) {
    for (let t of e.threads.values()) {
      let e = t.page;
      e == null ||
        t.pendingTeardown?.reason === `closed` ||
        this.persistCurrent(e);
    }
    this.store.flush();
  }
  registerWindowShutdown(e, t) {
    let n = () => {
      this.persistWindowBeforeShutdown(e);
    };
    (e.window.on(`close`, (t) => {
      (n(),
        (e.isClosing = !t.defaultPrevented),
        queueMicrotask(() => {
          e.isClosing = !t.defaultPrevented;
        }));
    }),
      e.window.on(`query-session-end`, () => {
        ((e.isClosing = !0), n());
      }),
      e.window.on(`closed`, t));
  }
};
function sR(e) {
  return new oR(
    new tR(
      e == null ? null : (0, u.join)(e, `browser-sidebar-page-states.json`),
      { enabled: !1 },
    ),
  );
}
function cR(e) {
  if (e.isColdRestore && e.serializedPageState != null)
    return e.serializedPageState;
  let t = e.view.webContents;
  if (
    t == null ||
    t.isDestroyed() ||
    (e.pendingUrl.length > 0 && n.ms(e.pendingUrl) !== n.ms(AL(t.getURL())))
  )
    return e.serializedPageState;
  let r = FL({
      faviconUrl: e.faviconUrl,
      title: e.title || e.fallbackTitle,
      url: NL(e),
      webContents: t,
    }),
    i = e.serializedPageState;
  return IL(r) != null || i == null || IL(i) == null ? r : i;
}
function lR({ currentUrl: e, requestUrl: t, resourceType: r }) {
  return r === `subFrame` && !uR(t) && !n.bs(t) && e != null && n.bs(e);
}
function uR(e) {
  let t;
  try {
    t = new URL(e);
  } catch {
    return !1;
  }
  return t.protocol === `data:`
    ? !0
    : t.protocol === `about:` &&
        (t.pathname === `blank` || t.pathname === `srcdoc`);
}
var dR = class {
  constructor(e) {
    this.options = e;
  }
  activate(e) {
    let t = this.options.findPageStateForPageKey(e);
    t != null && (t.threadState.isBrowserUseNavigationRestrictionActive = !0);
  }
  assertAllowed(e, t) {
    let n = this.options.findPageStateForPageKey(e);
    if (n == null) return;
    let r = this.getRestrictionError(n.threadState, t);
    if (r != null)
      throw (
        this.options.onNavigationBlocked?.(n, r, t, {
          resourceType: `mainFrame`,
        }),
        r
      );
  }
  isRestrictedForWebContentsId(e, t, n = {}) {
    if (e == null) return !1;
    let r = this.options.findPageStateForWebContentsId(e);
    return r == null ? !1 : this.isRestrictedForPageEntry(r, t, n);
  }
  isRestrictedForPageEntry(e, t, n) {
    if (n.resourceType === `subFrame` && uR(t)) return !1;
    let r = this.getRestrictionError(e.threadState, t);
    return r == null
      ? !1
      : (this.options.onNavigationBlocked?.(e, r, t, n), !0);
  }
  getRestrictionError(e, t) {
    return fR(e, t);
  }
};
function fR(e, t) {
  return e.isBrowserUseNavigationRestrictionActive
    ? wt(t)
      ? null
      : Error(`Blocked browser navigation by Browser Use URL policy: ${t}`)
    : null;
}
function pR(e) {
  return e === `mainFrame` || e === `subFrame`;
}
var mR = new URL(n.kl).origin,
  hR = `/api/auth/link-session`,
  gR = `/api/auth/session`,
  _R = `_account`,
  vR = `_account_residency_region`,
  yR = `_account_is_fedramp`,
  bR = 5 * 6e4,
  xR = n.xl({ account: n.xl({ planType: n.wl() }) }),
  SR = n.xl({
    accessToken: n.wl(),
    error: n.fl().optional(),
    workspaceTokenExchangeError: n.fl().optional(),
  });
async function CR({
  accountId: e,
  getAuthToken: t,
  session: n,
  targetUrl: r,
  webContents: i,
}) {
  if (!jR(r))
    throw Error(`Authenticated ChatGPT webview target must be ChatGPT`);
  i.isDestroyed() ||
    (await OR(i, await DR(t)),
    await Promise.all([
      n.cookies.remove(mR, _R),
      n.cookies.remove(mR, vR),
      n.cookies.remove(mR, yR),
    ]),
    e != null && (await wR(n, e)),
    !i.isDestroyed() && (await i.loadURL(r)));
}
async function wR(e, t) {
  await e.cookies.set({
    name: _R,
    path: `/`,
    sameSite: `lax`,
    secure: !0,
    url: mR,
    value: t,
  });
}
async function TR({ reason: e, session: t, workspaceId: n }) {
  let r = new URL(gR, mR);
  (r.searchParams.set(`exchange_workspace_token`, `true`),
    r.searchParams.set(`workspace_id`, n),
    r.searchParams.set(`reason`, e));
  let i = await t.fetch(r.toString());
  if (!i.ok)
    throw Error(
      `ChatGPT workspace session exchange failed with status ${i.status}`,
    );
  if (!SR.safeParse(await i.json()).success)
    throw Error(`ChatGPT workspace session exchange did not succeed`);
  await wR(t, n);
}
async function ER(e) {
  let t = new URL(gR, mR);
  t.searchParams.set(`refresh_account`, `true`);
  let n = await e.fetch(t.toString());
  if (!n.ok) return null;
  let r = xR.safeParse(await n.json());
  return r.success ? r.data.account.planType : null;
}
async function DR(e) {
  if (e == null) throw Error(`Missing ChatGPT webview auth token provider`);
  let t = await e({ refreshToken: !1 });
  if (
    ((t == null || kR(t)) && (t = await e({ refreshToken: !0 })),
    t == null || kR(t))
  )
    throw Error(`Missing, expired, or near-expiry Codex access token`);
  return t;
}
async function OR(e, t) {
  let n = AR(t);
  if (n == null || n <= Date.now())
    throw Error(`Missing or expired Codex access token`);
  let r = Math.max(1, Math.floor((n - Date.now()) / 1e3)),
    i = new URL(hR, mR).toString();
  (await e.loadURL(new URL(gR, mR).toString()),
    await e.loadURL(i, {
      extraHeaders: [`Content-Type: application/json`, `x-i-am-a-browser: true`]
        .join(`
`),
      postData: [
        {
          bytes: Buffer.from(JSON.stringify({ auth_token: t, expires_in: r })),
          type: `rawData`,
        },
      ],
    }));
}
function kR(e) {
  let t = AR(e);
  return t == null || t <= Date.now() + bR;
}
function AR(e) {
  let t = e.split(`.`)[1];
  if (t == null) return null;
  try {
    let e = JSON.parse(Buffer.from(t, `base64url`).toString(`utf8`));
    return typeof e != `object` || !e || !(`exp` in e)
      ? null
      : typeof e.exp == `number`
        ? e.exp * 1e3
        : null;
  } catch {
    return null;
  }
}
function jR(e) {
  try {
    return new URL(e).origin === mR;
  } catch {
    return !1;
  }
}
var MR = r.a(`checkout-webview`),
  NR = new URL(n.kl).origin,
  PR = `/api/auth/session`,
  FR = `/api/auth/link-session`,
  IR = `${new URL(n.kl).pathname}/purchase/`,
  LR = `${IR}credits`,
  RR = `https://pay.openai.com`,
  zR = `/c/pay/`,
  BR = `/checkout/`,
  VR = `/payments/success`,
  HR = `/payments/success-ind`,
  UR = `/payments/success-credit-purchase`,
  WR = `/codex/cloud/settings/usage`,
  GR = `/payments/success-team`,
  KR = `#complete`,
  qR = `refresh_account`,
  JR = `/pricing/`,
  YR = `/`,
  XR = `#pricing`,
  ZR = `about:`,
  QR = `blank`,
  $R = `/backend-api/subscriptions/update`,
  ez = `https://api.stripe.com`,
  tz = /^\/v1\/payment_intents\/[^/]+\/confirm$/,
  nz = n.xl({ base64Encoded: n.ml().optional(), body: n.wl() }),
  rz = n.xl({ status: n.vl(`succeeded`) });
function iz({ params: e, webPreferences: t }) {
  return az(e)
    ? ((t.session = lz()),
      delete t.preload,
      delete e.disablewebsecurity,
      delete e.webpreferences,
      (e.allowpopups = ``),
      (t.sandbox = !0),
      (t.devTools = !0),
      (t.nodeIntegration = !1),
      (t.nodeIntegrationInSubFrames = !1),
      (t.nodeIntegrationInWorker = !1),
      (t.contextIsolation = !0),
      (t.webSecurity = !0),
      (t.allowRunningInsecureContent = !1),
      (t.webviewTag = !1),
      (t.plugins = !1),
      Object.assign(t, { disablePopups: !1 }),
      !0)
    : !1;
}
function az(e) {
  return e.partition === n.Eo;
}
function oz(e) {
  return e.session === lz();
}
function sz({
  getAuthToken: e,
  onBusinessCheckoutComplete: t,
  onCreditPurchaseCheckoutComplete: n,
  onPersonalCheckoutComplete: r,
  onReturnToCodex: i,
  owner: a,
}) {
  let o = [],
    s = (e, t, n) => {
      iz({ params: n, webPreferences: t }) && o.push(bz(n.src ?? ``));
    },
    c = (a, s) => {
      o.length === 0 ||
        !oz(s) ||
        cz({
          getAuthToken: e,
          initialPersonalPlanType: o.shift() ?? null ?? bz(s.getURL()),
          onBusinessCheckoutComplete: t,
          onCreditPurchaseCheckoutComplete: n,
          onPersonalCheckoutComplete: r,
          onReturnToCodex: i,
          webContents: s,
        });
    },
    l = () => {
      (a.removeListener(`will-attach-webview`, s),
        a.removeListener(`did-attach-webview`, c),
        a.removeListener(`destroyed`, l));
    };
  return (
    a.on(`will-attach-webview`, s),
    a.on(`did-attach-webview`, c),
    a.on(`destroyed`, l),
    l
  );
}
function cz({
  getAuthToken: e,
  initialPersonalPlanType: t,
  onBusinessCheckoutComplete: n,
  onCreditPurchaseCheckoutComplete: r,
  onPersonalCheckoutComplete: i,
  onReturnToCodex: a,
  webContents: o,
}) {
  let s = !1,
    c = wz(o.getURL()),
    l = !1,
    u = !1,
    d = !1,
    f = !1,
    p = !1,
    m = !1,
    h = !1,
    g = !1,
    _ = !1,
    v = !1,
    y = !1,
    b = !1,
    x = null,
    S = t,
    C = null,
    w = null,
    T = null,
    E = (e) => {
      w === e ||
        o.isDestroyed() ||
        ((w = e),
        wR(lz(), e).catch((e) => {
          ((w = null),
            MR().warning(`Failed to set Business workspace cookie`, {
              safe: {},
              sensitive: { error: e },
            }));
        }));
    },
    D = (e) => {
      let t = zz(e);
      return t == null ? null : ((C = t), (d = !0), (l = !0), t);
    },
    O = (e) => {
      Hz(e) && (m = !0);
    },
    k = (e, t) => {
      if (y || o.isDestroyed()) return;
      let n = Bz(e);
      n != null &&
        ((y = !0),
        (async () => {
          (await TR({
            reason: `codex_business_checkout`,
            session: lz(),
            workspaceId: t,
          }),
            (w = t),
            !o.isDestroyed() && (await o.loadURL(n)));
        })().catch((e) => {
          (MR().warning(`Failed to prepare Business onboarding workspace`, {
            safe: {},
            sensitive: { error: e },
          }),
            N());
        }));
    },
    A = (e) => {
      let t = Az(e);
      t != null && ((T = t), (l = !0));
    },
    j = (e) => {
      Nz(e) && ((f = !0), (l = !0));
    },
    M = (e) => {
      T != null && (Gz(e) || Iz(e)) && ((T = null), (x = null));
    },
    N = () => {
      s || ((s = !0), a());
    },
    P = async () => {
      if (e != null)
        try {
          await e({ refreshToken: !0 });
        } catch {}
    },
    F = () => {
      h || C == null || o.isDestroyed() || ((h = !0), o.stop(), n(C));
    },
    I = () => {
      g || o.isDestroyed() || ((g = !0), o.stop(), r());
    },
    ee = () => {
      _ || T == null || o.isDestroyed() || ((_ = !0), o.stop(), i(T), P());
    },
    L = (e) => {
      ((T = e), (l = !0), !o.isDestroyed() && !_ && T === e && ee());
    },
    R = (e) => {
      ((T = e),
        (l = !0),
        (async () => {
          let t = await ER(lz());
          o.isDestroyed() || _ || T !== e || t !== e || L(e);
        })().catch(() => {}));
    },
    z = (e) => {
      (S != null && !xz(S, e)) || L(e);
    },
    te = uz(o, {
      onCreditPaymentConfirmed: () => {
        if (v || !u || o.isDestroyed()) return;
        let e = Dz(o.getURL());
        e != null &&
          ((v = !0),
          o.loadURL(e).catch((e) => {
            ((v = !1),
              MR().warning(`Failed to finalize custom credit checkout`, {
                safe: {},
                sensitive: { error: e },
              }));
          }));
      },
      onPersonalSubscriptionUpdateComplete: (e) => {
        if (e != null) {
          z(e);
          return;
        }
        ER(lz())
          .then((e) => {
            let t = Sz(e);
            t != null && z(t);
          })
          .catch(() => {});
      },
    }),
    ne = (e) => {
      x === e || _ || T == null || !Mz(e) || ((x = e), R(T));
    },
    re = (e, t) => {
      if (
        (e.isMainFrame && (D(t), O(t), j(t), A(t), (u ||= Ez(t))),
        e.isMainFrame && !y && C != null && Vz(t))
      ) {
        (e.preventDefault(), k(t, C));
        return;
      }
      if (e.isMainFrame && T != null && Wz(t)) {
        (e.preventDefault(), ee());
        return;
      }
      if (e.isMainFrame && d && C != null && p && m && Wz(t)) {
        (e.preventDefault(), F());
        return;
      }
      if (e.isMainFrame && f && Fz(t)) {
        (e.preventDefault(), I());
        return;
      }
      if (e.isMainFrame && c && Wz(t)) {
        (e.preventDefault(), N());
        return;
      }
      !e.isMainFrame ||
        Tz(t, l, C != null, u, f, T != null) ||
        (e.preventDefault(), N());
    },
    ie = (e) => {
      re(e, e.url);
    },
    ae = (e, t) => {
      re(e, e.url ?? t);
    },
    oe = (e, t) => {
      re(e, e.url ?? t);
    },
    se = (t) => {
      b ||
        !wz(t) ||
        ((S ??= bz(t)),
        (b = !0),
        _z(o, t, e).catch((e) => {
          MR().warning(`Failed to load checkout webview auth handoff`, {
            safe: {},
            sensitive: { error: e },
          });
        }));
    },
    ce = (e, t, n, r) => {
      r && (D(t), O(t), j(t), A(t), (u ||= Ez(t)));
    },
    le = (e) => {
      if (!wz(e)) {
        if ((D(e), O(e), j(e), A(e), M(e), C != null && Vz(e))) {
          if (!y) {
            k(e, C);
            return;
          }
          (E(C), (p = !0));
          return;
        }
        if ((Lz(e) && (c = !0), Gz(e))) {
          l = !0;
          return;
        }
        if (T != null && Wz(e)) {
          ee();
          return;
        }
        if (f && Fz(e)) {
          I();
          return;
        }
        if (d && C != null && p && m && Wz(e)) {
          F();
          return;
        }
        if (c && Wz(e)) {
          N();
          return;
        }
        Tz(e, l, C != null, u, f, T != null) || N();
      }
    },
    ue = (e, t) => {
      le(t);
    },
    de = (e, t, n) => {
      n && le(t);
    },
    fe = () => {
      let e = o.getURL();
      (se(e), ne(e));
    },
    pe = () => {
      (te(),
        o.removeListener(`will-frame-navigate`, ie),
        o.removeListener(`will-navigate`, ae),
        o.removeListener(`will-redirect`, oe),
        o.removeListener(`did-start-navigation`, ce),
        o.removeListener(`did-navigate`, ue),
        o.removeListener(`did-navigate-in-page`, de),
        o.removeListener(`did-finish-load`, fe),
        o.removeListener(`destroyed`, pe),
        lz()
          .clearStorageData({ storages: [`cookies`] })
          .catch((e) => {
            MR().warning(`Failed to clear checkout webview auth cookies`, {
              safe: {},
              sensitive: { error: e },
            });
          }));
    };
  (o.on(`will-frame-navigate`, ie),
    o.on(`will-navigate`, ae),
    o.on(`will-redirect`, oe),
    o.on(`did-start-navigation`, ce),
    o.on(`did-navigate`, ue),
    o.on(`did-navigate-in-page`, de),
    o.on(`did-finish-load`, fe),
    o.on(`destroyed`, pe),
    se(o.getURL()),
    o.setWindowOpenHandler(({ url: e }) =>
      e === `about:blank` || Kz(o.getURL())
        ? { action: qz(e) ? `allow` : `deny` }
        : (qz(e) && IA({ request: { openTarget: `external-browser`, url: e } }),
          { action: `deny` }),
    ));
}
function lz() {
  return c.session.fromPartition(n.Eo);
}
function uz(
  e,
  { onCreditPaymentConfirmed: t, onPersonalSubscriptionUpdateComplete: n },
) {
  let r = new Map(),
    i = new Set(),
    a = !1,
    o = !1,
    s = !1,
    c = !1,
    l = !1,
    u = (a, c, u) => {
      if (!mz(u)) return;
      let d = hz(u, `requestId`);
      if (d == null) return;
      if (c === `Network.requestWillBeSent`) {
        let t = u.request;
        if (!mz(t)) return;
        let n = hz(t, `method`),
          a = hz(t, `url`),
          o = hz(t, `postData`);
        if (n === `POST` && a != null && dz(a)) {
          i.add(d);
          return;
        }
        if (n !== `POST` || a == null || !fz(a)) return;
        let s = pz(o),
          c =
            s == null
              ? e.debugger
                  .sendCommand(`Network.getRequestPostData`, { requestId: d })
                  .then((e) => (mz(e) ? pz(hz(e, `postData`)) : null))
                  .catch(() => null)
              : Promise.resolve(s);
        r.set(d, c);
        return;
      }
      if (c === `Network.loadingFailed`) {
        (r.delete(d), i.delete(d));
        return;
      }
      if (c === `Network.loadingFinished`) {
        if (!i.delete(d) || o) return;
        e.debugger
          .sendCommand(`Network.getResponseBody`, { requestId: d })
          .then((e) => {
            let n = nz.safeParse(e);
            if (!n.success || o || l) return;
            let r = n.data.base64Encoded
                ? Buffer.from(n.data.body, `base64`).toString(`utf8`)
                : n.data.body,
              i;
            try {
              i = JSON.parse(r);
            } catch {
              return;
            }
            rz.safeParse(i).success && ((o = !0), t());
          })
          .catch(() => {});
        return;
      }
      if (c !== `Network.responseReceived`) return;
      let f = u.response;
      if (!mz(f)) return;
      let p = gz(f, `status`);
      i.has(d) && (p == null || p < 200 || p >= 300) && i.delete(d);
      let m = r.get(d);
      m != null &&
        (r.delete(d),
        !(p == null || p < 200 || p >= 300 || s) &&
          m.then((e) => {
            s || l || ((s = !0), n(e));
          }));
    },
    d = () => {
      l ||
        ((l = !0),
        r.clear(),
        i.clear(),
        c && !e.isDestroyed() && e.debugger.removeListener(`message`, u),
        a &&
          !e.isDestroyed() &&
          e.debugger.isAttached() &&
          e.debugger.detach());
    };
  try {
    (e.debugger.isAttached() || (e.debugger.attach(`1.3`), (a = !0)),
      e.debugger.on(`message`, u),
      (c = !0),
      e.debugger
        .sendCommand(`Network.enable`, { maxPostDataSize: 65536 })
        .catch((e) => {
          l ||
            (MR().warning(`Failed to enable checkout network observation`, {
              safe: {},
              sensitive: { error: e },
            }),
            d());
        }));
  } catch (e) {
    (MR().warning(`Failed to observe checkout network requests`, {
      safe: {},
      sensitive: { error: e },
    }),
      d());
  }
  return d;
}
function dz(e) {
  try {
    let t = new URL(e);
    return t.origin === ez && tz.test(t.pathname);
  } catch {
    return !1;
  }
}
function fz(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && t.pathname === $R;
  } catch {
    return !1;
  }
}
function pz(e) {
  if (e == null) return null;
  try {
    let t = JSON.parse(e);
    if (!mz(t)) return null;
    switch (t.updated_plan) {
      case `chatgptgoplan`:
        return `go`;
      case `chatgptplusplan`:
        return `plus`;
      case `chatgptprolite`:
        return `prolite`;
      case `chatgptpro`:
        return `pro`;
      default:
        return null;
    }
  } catch {
    return null;
  }
}
function mz(e) {
  return typeof e == `object` && !!e;
}
function hz(e, t) {
  let n = e[t];
  return typeof n == `string` ? n : null;
}
function gz(e, t) {
  let n = e[t];
  return typeof n == `number` ? n : null;
}
async function _z(e, t, n) {
  let r = vz(t),
    i = yz(t);
  if (
    r != null &&
    (await lz().clearStorageData({ storages: [`cookies`] }), !e.isDestroyed())
  ) {
    try {
      await CR({
        accountId: i,
        getAuthToken: n,
        session: lz(),
        targetUrl: r,
        webContents: e,
      });
      return;
    } catch (e) {
      MR().warning(
        `Failed to authenticate checkout webview; loading target without handoff`,
        { safe: {}, sensitive: { error: e } },
      );
    }
    e.isDestroyed() || (await e.loadURL(r));
  }
}
function vz(e) {
  let t = new URL(e).searchParams.get(`toUrl`);
  return t == null || !Lz(t) ? null : new URL(t, NR).toString();
}
function yz(e) {
  let t = new URL(e).searchParams.get(`accountId`);
  return t != null && t.length > 0 ? t : null;
}
function bz(e) {
  try {
    let t = new URL(e).searchParams.get(`currentPlan`);
    switch (t) {
      case `free`:
      case `go`:
      case `plus`:
      case `prolite`:
      case `pro`:
        return t;
      default:
        return null;
    }
  } catch {
    return null;
  }
}
function xz(e, t) {
  return Cz(t) > Cz(e);
}
function Sz(e) {
  switch (e) {
    case `go`:
    case `plus`:
    case `prolite`:
    case `pro`:
      return e;
    default:
      return null;
  }
}
function Cz(e) {
  switch (e) {
    case `free`:
      return 0;
    case `go`:
      return 1;
    case `plus`:
      return 2;
    case `prolite`:
      return 3;
    case `pro`:
      return 4;
  }
}
function wz(e) {
  try {
    let t = new URL(e),
      n = t.searchParams.get(`toUrl`);
    return t.protocol === ZR && t.pathname === QR && Lz(n);
  } catch {
    return !1;
  }
}
function Tz(e, t, n, r, i, a) {
  if (e === `about:blank` || wz(e)) return !t;
  try {
    let o = new URL(e);
    return r && Oz(o)
      ? !0
      : o.origin === NR
        ? Lz(e) ||
          (!t && kz(o)) ||
          (i && Pz(o)) ||
          (a && jz(o)) ||
          (n && o.pathname === GR)
        : !1;
  } catch {
    return !1;
  }
}
function Ez(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && t.pathname === LR;
  } catch {
    return !1;
  }
}
function Dz(e) {
  try {
    let t = new URL(e);
    if (t.origin !== NR) return null;
    let n = /^\/checkout\/([^/]+)\/([^/]+)\/?$/.exec(t.pathname);
    if (n == null) return null;
    let [, r, i] = n;
    if (r == null || i == null) return null;
    let a = new URL(UR, NR);
    return (
      a.searchParams.set(`stripe_session_id`, i),
      a.searchParams.set(`processor_entity`, r),
      a.toString()
    );
  } catch {
    return null;
  }
}
function Oz(e) {
  return e.origin === RR && e.pathname.startsWith(zR);
}
function kz(e) {
  return e.pathname === PR || e.pathname === FR;
}
function Az(e) {
  try {
    let t = new URL(e);
    if (!jz(t)) return null;
    let n = t.searchParams.get(`plan_type`);
    switch (n) {
      case `go`:
      case `plus`:
      case `prolite`:
      case `pro`:
        return n;
      default:
        return null;
    }
  } catch {
    return null;
  }
}
function jz(e) {
  return e.origin === NR && (e.pathname === VR || e.pathname === HR);
}
function Mz(e) {
  try {
    return jz(new URL(e));
  } catch {
    return !1;
  }
}
function Nz(e) {
  try {
    return Pz(new URL(e));
  } catch {
    return !1;
  }
}
function Pz(e) {
  return e.origin === NR && e.pathname === UR;
}
function Fz(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && t.pathname === WR;
  } catch {
    return !1;
  }
}
function Iz(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && Rz(t);
  } catch {
    return !1;
  }
}
function Lz(e) {
  if (e == null) return !1;
  try {
    let t = new URL(e, NR);
    return (
      t.origin === NR &&
      (t.pathname.startsWith(IR) || t.pathname.startsWith(BR) || Rz(t))
    );
  } catch {
    return !1;
  }
}
function Rz(e) {
  return e.pathname === JR || (e.pathname === YR && e.hash === XR);
}
function zz(e) {
  try {
    let t = new URL(e);
    if (!Vz(t)) return null;
    let n = t.searchParams.get(`account_id`);
    return n != null && n.length > 0 ? n : null;
  } catch {
    return null;
  }
}
function Bz(e) {
  try {
    let t = new URL(e);
    return !Vz(t) || zz(e) == null
      ? null
      : (t.searchParams.set(qR, `true`), t.toString());
  } catch {
    return null;
  }
}
function Vz(e) {
  try {
    let t = typeof e == `string` ? new URL(e) : e;
    return t.origin === NR && t.pathname === GR;
  } catch {
    return !1;
  }
}
function Hz(e) {
  return Uz(e, KR);
}
function Uz(e, t) {
  try {
    let n = new URL(e);
    return Vz(n) && n.hash === t;
  } catch {
    return !1;
  }
}
function Wz(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && t.pathname === `/` && !Rz(t);
  } catch {
    return !1;
  }
}
function Gz(e) {
  try {
    let t = new URL(e);
    return t.origin === NR && t.pathname.startsWith(BR);
  } catch {
    return !1;
  }
}
function Kz(e) {
  if (Gz(e)) return !0;
  try {
    return Oz(new URL(e));
  } catch {
    return !1;
  }
}
function qz(e) {
  if (e === `about:blank`) return !0;
  try {
    return new URL(e).protocol === `https:`;
  } catch {
    return !1;
  }
}
var Jz = new WeakSet();
function Yz(e) {
  Jz.add(e);
}
function Xz(e) {
  return Jz.has(e);
}
function Zz(e) {
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`;
  } catch {
    return !1;
  }
}
var Qz = r.a(`browser-sidebar-manager`),
  $z = `data-browser-sidebar-retained-webview`,
  eB = `owl-webcontents-adoption-lease`,
  tB = `owl-webcontents-adopted-web-contents-id`,
  nB = new WeakMap(),
  rB = [
    {
      allowedNavigationUrls: n.Ko,
      isEnabled: () => a.j(n.$i),
      partition: n.Jo,
      src: n.qo,
    },
    {
      allowedNavigationUrls: [n.Yo],
      isEnabled: () => a.j(n.ea),
      partition: n.Xo,
      src: n.Yo,
    },
    {
      allowedNavigationUrls: [n.Zo],
      isEnabled: () => a.j(n.ta),
      partition: n.Qo,
      src: n.Zo,
    },
    {
      allowedNavigationUrls: [n.$o],
      isEnabled: () => a.j(n.ra),
      partition: n.es,
      src: n.$o,
    },
    {
      allowedNavigationUrls: [n.ts],
      isEnabled: () => a.j(n.$i),
      partition: n.is,
      src: n.rs,
    },
    {
      allowedNavigationUrls: n.as,
      isEnabled: () => a.j(n.ia),
      partition: n.cs,
      src: n.ss,
    },
  ];
function iB({
  reconcileBrowserStorageId: e,
  attachRetainedWebview: t,
  attachWebview: r,
  configureBrowserSession: i,
  ensureThreadStateForWindow: a,
  isPagePersistenceAllowed: o,
  openBrowserSettingsUrlInTab: s,
  owner: c,
  prepareWebviewAttach: l,
  preloadPath: u,
  recordDebugEvent: d,
  state: f,
  windowId: p,
}) {
  let m = (t, r, s) => {
      if (n.Ba(s.partition) != null || az(s)) return;
      let m = cB(s);
      if (m != null) {
        if (f.browserSettingsWebviewMountIds.size === 0) {
          (Qz().warning(
            `IAB_LIFECYCLE rejected browser settings webview outside settings route`,
            { safe: { ownerWebContentsId: c.id, windowId: p }, sensitive: {} },
          ),
            t.preventDefault());
          return;
        }
        oB({ configureBrowserSession: i, params: s, webPreferences: r });
        let e = SB(s);
        if (e == null) {
          (Qz().warning(
            `IAB_LIFECYCLE rejected browser settings webview attachment without instance id`,
            { safe: { ownerWebContentsId: c.id, windowId: p }, sensitive: {} },
          ),
            t.preventDefault());
          return;
        }
        f.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.set(
          e,
          m.allowedNavigationUrls,
        );
        return;
      }
      let h = vL(s.partition),
        g =
          h == null
            ? null
            : (f.registeredWebviewHostsByRoutePartition.get(h) ?? null),
        _ = gL(s),
        v = _L(s),
        y = g ?? _,
        b = y == null ? null : (f.threads.get(OL(y)) ?? null),
        x =
          g != null ||
          s[`data-browser-sidebar-page-restore`] != null ||
          s[`data-browser-sidebar-page-storage-id`] != null,
        S = y != null && b != null && b.browserStorageId !== bL(y),
        C = (x || S) && o(),
        w = x && C,
        T = w ? g : null,
        E =
          T != null &&
          _ != null &&
          (T.browserTabId !== _.browserTabId ||
            T.conversationId !== _.conversationId),
        D = E ? null : (T ?? _),
        O = w ? s[n.Po] : void 0,
        k = O === `none` || O === `required` ? O : null,
        A = O != null && k == null,
        j = s[`data-browser-sidebar-page-storage-id`] ?? null,
        M = k != null && j?.length > 0 ? n.Bo(j) : null,
        N = T?.pagePersistence ?? null,
        P = N != null && M != null && N.browserStorageId !== M,
        F = N != null && k != null && N.restore !== k,
        I = N == null && k != null && M == null,
        ee;
      !P &&
        !F &&
        (N == null
          ? M != null && k != null && (ee = { browserStorageId: M, restore: k })
          : (ee = N));
      let L = ee?.browserStorageId ?? null,
        R = D == null ? null : (f.threads.get(OL(D)) ?? null),
        z = R != null && !e(R, ee),
        te = L != null && R != null && R.browserStorageId !== L,
        ne = R?.pendingTeardown != null,
        re =
          C &&
          ee == null &&
          (T != null ||
            (D != null && R != null && R.browserStorageId !== bL(D)));
      if (D == null || z || te || A || I || re || P || F || ne) {
        (d({
          browserTabId: D?.browserTabId ?? null,
          conversationId: D?.conversationId ?? null,
          guestWebContentsId: null,
          kind: `browser-webview-attach-rejected`,
          message: `Rejected Browser sidebar webview attachment`,
          ownerWebContentsId: c.id,
          pageKey: null,
          windowId: p,
        }),
          Qz().warning(
            `IAB_LIFECYCLE rejected browser sidebar webview attachment`,
            {
              safe: {
                browserTabId: D?.browserTabId ?? null,
                conversationId: D?.conversationId ?? null,
                hasBrowserStorageReconciliationFailure: z,
                hasBrowserStorageMismatch: te,
                hasInvalidRestoreIntent: A,
                hasMissingBrowserStorageId: I,
                hasMissingRestoreIntent: re,
                hasRegisteredBrowserStorageMismatch: P,
                hasRegisteredRestoreIntentMismatch: F,
                hasRegisteredRouteMismatch: E,
                ownerWebContentsId: c.id,
                threadExists: R != null,
                teardownReason: R?.pendingTeardown?.reason ?? null,
                windowId: p,
              },
              sensitive: {},
            },
          ),
          t.preventDefault());
        return;
      }
      let ie = a(D.conversationId, D.browserTabId);
      if (
        v == null ||
        f.rendererInstanceId !== v.rendererInstanceId ||
        ie.hostGeneration !== v.hostGeneration
      ) {
        (Qz().warning(
          `IAB_LIFECYCLE rejected stale browser sidebar host attachment`,
          {
            safe: {
              browserTabId: D.browserTabId,
              conversationId: D.conversationId,
              hostGeneration: v?.hostGeneration ?? null,
              threadHostGeneration: ie.hostGeneration,
              ownerWebContentsId: c.id,
              windowId: p,
            },
            sensitive: {},
          },
        ),
          t.preventDefault());
        return;
      }
      let { hostGeneration: ae, rendererInstanceId: oe } = v;
      if (R == null && !e(ie, ee)) {
        (d({
          browserTabId: D.browserTabId,
          conversationId: D.conversationId,
          guestWebContentsId: null,
          kind: `browser-webview-attach-rejected`,
          message: `Rejected Browser sidebar webview storage ownership`,
          ownerWebContentsId: c.id,
          pageKey: null,
          windowId: p,
        }),
          t.preventDefault());
        return;
      }
      Qz().info(`IAB_LIFECYCLE browser sidebar will attach webview`, {
        safe: {
          browserTabId: D.browserTabId,
          conversationId: D.conversationId,
          ownerWebContentsId: c.id,
          pendingAdoptedAttachCount:
            f.pendingAdoptedWebviewRoutesByWebContentsId.size,
          pendingAttachCount: f.pendingWebviewRoutesByInstanceId.size,
          pendingRetainedAttachCount:
            f.pendingRetainedWebviewRoutesByInstanceId.size,
          windowId: p,
        },
        sensitive: {},
      });
      let se = l(ie, { title: ie.snapshot.title, url: ie.snapshot.url }, ee);
      if (se == null) {
        t.preventDefault();
        return;
      }
      if (se.view.webContents != null && !se.view.webContents.isDestroyed()) {
        (d({
          browserTabId: D.browserTabId,
          conversationId: D.conversationId,
          guestWebContentsId: se.view.webContents.id,
          kind: `browser-webview-reattach-existing`,
          message: `Reattached existing Browser sidebar webview`,
          ownerWebContentsId: c.id,
          pageKey: null,
          windowId: p,
        }),
          Qz().info(
            `IAB_LIFECYCLE reattaching existing browser sidebar webview`,
            {
              safe: {
                browserTabId: D.browserTabId,
                conversationId: D.conversationId,
                guestWebContentsId: se.view.webContents.id,
                ownerWebContentsId: c.id,
                windowId: p,
              },
              sensitive: {},
            },
          ));
        return;
      }
      if (
        (d({
          browserTabId: D.browserTabId,
          conversationId: D.conversationId,
          details: [
            { label: `hasPendingUrl`, value: String(se.pendingUrl.length > 0) },
            { label: `hasUrl`, value: String(se.url.length > 0) },
          ],
          guestWebContentsId: null,
          kind: `browser-webview-attach-requested`,
          message: `Requested Browser sidebar webview attachment`,
          ownerWebContentsId: c.id,
          pageKey: null,
          windowId: p,
        }),
        Qz().info(
          `IAB_LIFECYCLE requested browser sidebar webview attachment`,
          {
            safe: {
              browserTabId: D.browserTabId,
              conversationId: D.conversationId,
              ownerWebContentsId: c.id,
              hasPendingUrl: se.pendingUrl.length > 0,
              hasUrl: se.url.length > 0,
              windowId: p,
            },
            sensitive: {},
          },
        ),
        aB({
          configureBrowserSession: i,
          params: s,
          preloadPath: u,
          webPreferences: r,
        }),
        EB(s))
      ) {
        let e = DB(s);
        e != null &&
          (f.pendingAdoptedWebviewRoutesByWebContentsId.set(e, {
            hostGeneration: ae,
            rendererInstanceId: oe,
            route: D,
          }),
          Qz().info(`IAB_ADOPTION queued adopted webview attach`, {
            safe: {
              adoptedWebContentsId: e,
              browserTabId: D.browserTabId,
              conversationId: D.conversationId,
              windowId: p,
            },
            sensitive: {},
          }));
        return;
      }
      s.src = ``;
      let ce = TB(s),
        le = SB(s);
      if (le == null) {
        (Qz().warning(
          ce && !se.isSuspended
            ? `IAB_LIFECYCLE rejected retained browser sidebar webview attachment without instance id`
            : `IAB_LIFECYCLE rejected browser sidebar webview attachment without instance id`,
          {
            safe: {
              browserTabId: D.browserTabId,
              conversationId: D.conversationId,
              ownerWebContentsId: c.id,
              windowId: p,
            },
            sensitive: {},
          },
        ),
          t.preventDefault());
        return;
      }
      if (se.isSuspended) {
        f.pendingRetainedWebviewRoutesByInstanceId.set(le, {
          hostGeneration: ae,
          rendererInstanceId: oe,
          route: D,
        });
        return;
      }
      if ((n.bs(NL(se)) && (r.backgroundThrottling = !1), ce)) {
        (f.pendingRetainedWebviewRoutesByInstanceId.set(le, {
          hostGeneration: ae,
          rendererInstanceId: oe,
          route: D,
        }),
          Qz().info(
            `IAB_LIFECYCLE queued retained browser sidebar webview route`,
            {
              safe: {
                browserTabId: D.browserTabId,
                conversationId: D.conversationId,
                instanceId: le,
                ownerWebContentsId: c.id,
                pendingRetainedAttachCount:
                  f.pendingRetainedWebviewRoutesByInstanceId.size,
                windowId: p,
              },
              sensitive: {},
            },
          ));
        return;
      }
      (f.pendingWebviewRoutesByInstanceId.set(le, {
        hostGeneration: ae,
        rendererInstanceId: oe,
        route: D,
      }),
        Qz().info(`IAB_LIFECYCLE queued browser sidebar webview route`, {
          safe: {
            browserTabId: D.browserTabId,
            conversationId: D.conversationId,
            instanceId: le,
            hostGeneration: ae,
            ownerWebContentsId: c.id,
            pendingAttachCount: f.pendingWebviewRoutesByInstanceId.size,
            windowId: p,
          },
          sensitive: {},
        }),
        d({
          browserTabId: D.browserTabId,
          conversationId: D.conversationId,
          details: [
            { label: `instanceId`, value: String(le) },
            { label: `hostGeneration`, value: String(ae) },
          ],
          guestWebContentsId: null,
          kind: `browser-webview-attach-route-queued`,
          message: `Queued Browser sidebar webview route`,
          ownerWebContentsId: c.id,
          pageKey: null,
          windowId: p,
        }));
    },
    h = (e, n) => {
      if (Xz(n.session) || oz(n)) return;
      let i = lB(n, f);
      if (i != null) {
        dB({
          allowedNavigationUrls: i,
          openBrowserSettingsUrlInTab: s,
          state: f,
          webContents: n,
        });
        return;
      }
      (d({
        browserTabId: null,
        conversationId: null,
        guestWebContentsId: n.id,
        kind: `browser-webview-did-attach`,
        message: `Did attach Browser sidebar webview`,
        ownerWebContentsId: c.id,
        pageKey: null,
        windowId: p,
      }),
        Qz().info(`IAB_LIFECYCLE did attach browser sidebar webview`, {
          safe: {
            guestWebContentsId: n.id,
            ownerWebContentsId: c.id,
            pendingAdoptedAttachCount:
              f.pendingAdoptedWebviewRoutesByWebContentsId.size,
            pendingAttachCount: f.pendingWebviewRoutesByInstanceId.size,
            pendingRetainedAttachCount:
              f.pendingRetainedWebviewRoutesByInstanceId.size,
            windowId: p,
          },
          sensitive: {},
        }));
      let a = OB(n, f);
      if (a != null) {
        r(n, a);
        return;
      }
      if (kB(n, f, r)) return;
      let o = uB(n, f);
      if (o != null) {
        let { hostGeneration: e, rendererInstanceId: r, route: i } = o,
          a = f.threads.get(OL(i)) ?? null;
        if (f.rendererInstanceId !== r || a?.hostGeneration !== e) {
          n.isDestroyed() || n.close();
          return;
        }
        t(n, i);
        return;
      }
    };
  return (
    c.on(`will-attach-webview`, m),
    c.on(`did-attach-webview`, h),
    () => {
      (c.removeListener(`will-attach-webview`, m),
        c.removeListener(`did-attach-webview`, h),
        f.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.clear());
      for (let e of f.browserSettingsWebviewNavigationDisposersByWebContentsId.values())
        e();
    }
  );
}
function aB({
  configureBrowserSession: e,
  params: t,
  preloadPath: n,
  webPreferences: r,
}) {
  ((t.partition = kx(`app`)), (r.session = e()), (r.preload = n), sB(t, r));
}
function oB({ configureBrowserSession: e, params: t, webPreferences: n }) {
  ((t.partition = kx(`app`)),
    (n.session = e()),
    delete n.preload,
    sB(t, n),
    (t.allowpopups = ``),
    Object.assign(n, { disablePopups: !1 }));
}
function sB(e, t) {
  (delete e.allowpopups,
    delete e.disablewebsecurity,
    delete e.webpreferences,
    (t.sandbox = !0),
    (t.devTools = !0),
    (t.nodeIntegration = !1),
    (t.nodeIntegrationInSubFrames = !1),
    (t.nodeIntegrationInWorker = !1),
    (t.contextIsolation = !0),
    (t.webSecurity = !0),
    (t.allowRunningInsecureContent = !1),
    (t.webviewTag = !1),
    (t.plugins = !1),
    Object.assign(t, { disablePopups: !0 }));
}
function cB(e) {
  let t =
    rB.find(
      (t) =>
        e.partition === t.partition &&
        _B({
          allowedNavigationUrls: t.allowedNavigationUrls,
          navigationUrl: e.src,
        }),
    ) ?? null;
  return t?.isEnabled() === !0 ? t : null;
}
function lB(e, t) {
  let n = CB(e);
  if (n == null) return null;
  let r =
    t.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.get(n) ??
    null;
  return r == null
    ? null
    : (t.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.delete(
        n,
      ),
      r);
}
function uB(e, t) {
  let n = CB(e);
  if (n == null) return null;
  let r = t.pendingRetainedWebviewRoutesByInstanceId.get(n) ?? null;
  return r == null
    ? null
    : (t.pendingRetainedWebviewRoutesByInstanceId.delete(n), r);
}
function dB({
  allowedNavigationUrls: e,
  openBrowserSettingsUrlInTab: t,
  state: n,
  webContents: r,
}) {
  (n.browserSettingsWebviewNavigationDisposersByWebContentsId.get(r.id)?.(),
    n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.set(r.id, e),
    n.browserSettingsWebContentsByWebContentsId.set(r.id, r));
  let i = () => {
      fB(r, n.browserSettingsWebviewThemeVariant);
    },
    a = gB({
      allowedNavigationUrls: e,
      onDispose: () => {
        (n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.delete(
          r.id,
        ),
          n.browserSettingsWebContentsByWebContentsId.delete(r.id),
          n.browserSettingsWebviewNavigationDisposersByWebContentsId.delete(
            r.id,
          ));
      },
      webContents: r,
    });
  (n.browserSettingsWebviewNavigationDisposersByWebContentsId.set(r.id, () => {
    (r.removeListener(`did-finish-load`, i), a());
  }),
    r.setWindowOpenHandler(({ url: e }) => (Zz(e) && t(e), { action: `deny` })),
    r.on(`did-finish-load`, i),
    i());
}
function fB(e, t) {
  let n = (nB.get(e) ?? Promise.resolve())
    .catch(() => void 0)
    .then(async () => {
      await EL(e, t);
    });
  nB.set(e, n);
}
function pB({ webContentsId: e, windows: t, url: n }) {
  let r = e == null ? null : hB(t, e);
  return r != null && !_B({ allowedNavigationUrls: r, navigationUrl: n });
}
function mB(e, t) {
  if (e.browserSettingsWebviewThemeVariant !== t) {
    e.browserSettingsWebviewThemeVariant = t;
    for (let n of e.browserSettingsWebContentsByWebContentsId.values())
      fB(n, t);
  }
}
function hB(e, t) {
  for (let n of e) {
    let e =
      n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.get(t) ??
      null;
    if (e != null) return e;
  }
  return null;
}
function gB({ allowedNavigationUrls: e, onDispose: t, webContents: n }) {
  let r = (t) => _B({ allowedNavigationUrls: e, navigationUrl: t }),
    i = (e, t) => {
      !e.isMainFrame || r(t) || e.preventDefault();
    },
    a = (e) => {
      i(e, e.url);
    },
    o = (e, t) => {
      i(e, e.url ?? t);
    },
    s = (e, t) => {
      i(e, e.url ?? t);
    },
    c = (t, i) => {
      !i ||
        r(t) ||
        n.loadURL(e[0] ?? `about:blank`).catch(() => {
          n.close();
        });
    },
    l = (e, t) => {
      c(t, !0);
    },
    u = (e, t, n) => {
      c(t, n);
    },
    d = () => {
      (n.removeListener(`will-frame-navigate`, a),
        n.removeListener(`will-navigate`, o),
        n.removeListener(`will-redirect`, s),
        n.removeListener(`did-navigate`, l),
        n.removeListener(`did-navigate-in-page`, u),
        n.removeListener(`destroyed`, d),
        t());
    };
  return (
    n.on(`will-frame-navigate`, a),
    n.on(`will-navigate`, o),
    n.on(`will-redirect`, s),
    n.on(`did-navigate`, l),
    n.on(`did-navigate-in-page`, u),
    n.on(`destroyed`, d),
    d
  );
}
function _B({ allowedNavigationUrls: e, navigationUrl: t }) {
  return e.some((e) => vB({ allowedNavigationUrl: e, navigationUrl: t }));
}
function vB({ allowedNavigationUrl: e, navigationUrl: t }) {
  let n = yB(e),
    r = yB(t);
  return n == null ||
    r == null ||
    r.protocol !== n.protocol ||
    r.host !== n.host
    ? !1
    : bB(n.pathname, r.pathname);
}
function yB(e) {
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
function bB(e, t) {
  let n = xB(e),
    r = xB(t);
  return n === `/` || r === n || r.startsWith(`${n}/`);
}
function xB(e) {
  let t = e.replace(/\/+$/, ``);
  return t.length === 0 ? `/` : t;
}
function SB(e) {
  return wB(e.instanceId);
}
function CB(e) {
  return wB(e.viewInstanceId);
}
function wB(e) {
  let t = Number(e);
  return Number.isInteger(t) && t > 0 ? t : null;
}
function TB(e) {
  return $z in e;
}
function EB(e) {
  let t = e[eB];
  return typeof t == `string` && t.length > 0;
}
function DB(e) {
  let t = Number(e[tB]);
  return Number.isInteger(t) && t > 0 ? t : null;
}
function OB(e, t) {
  let n = CB(e);
  if (n == null) return null;
  let r = t.pendingWebviewRoutesByInstanceId.get(n) ?? null;
  return (t.pendingWebviewRoutesByInstanceId.delete(n), r);
}
function kB(e, t, n) {
  if (e.isDestroyed()) return !1;
  let r = t.pendingAdoptedWebviewRoutesByWebContentsId.get(e.id) ?? null;
  if (r == null)
    return t.canceledAdoptedWebContentsIds.delete(e.id) ? (e.close(), !0) : !1;
  t.pendingAdoptedWebviewRoutesByWebContentsId.delete(e.id);
  let i = t.pendingAdoptedInitialUrlsByWebContents.get(e);
  return (
    t.pendingAdoptedInitialUrlsByWebContents.delete(e),
    n(e, { ...r, adoptedInitialUrl: i }),
    !0
  );
}
var AB = class {
    configured = !1;
    constructor(e) {
      this.options = e;
    }
    configure() {
      let e = c.session.fromPartition(kx(`app`));
      return this.configured
        ? e
        : (e.setPermissionRequestHandler((e, t, n) => {
            n(t === `clipboard-sanitized-write`);
          }),
          e.setPermissionCheckHandler(
            (e, t) => t === `clipboard-sanitized-write`,
          ),
          this.options.downloadStore.registerSession({
            browserSession: e,
            browserSessionRegistry: this.options.browserSessionRegistry,
            findPageStateForWebContentsId:
              this.options.findPageStateForWebContentsId,
          }),
          e.webRequest.onBeforeRequest((e, t) => {
            let n =
              e.webContentsId == null
                ? null
                : this.options.findPageStateForWebContentsId(e.webContentsId);
            t({
              cancel:
                (e.resourceType === `mainFrame` &&
                  pB({
                    webContentsId: e.webContentsId,
                    windows: this.options.getWindows(),
                    url: e.url,
                  })) ||
                (n?.threadState.isAgentControllingBrowser !== !0 &&
                  lR({
                    currentUrl: n == null ? null : n.page.url,
                    resourceType: e.resourceType,
                    requestUrl: e.url,
                  })) ||
                (pR(e.resourceType) &&
                  this.options.isRestrictedBrowserSidebarNavigation(
                    e.webContentsId,
                    e.url,
                    e.resourceType,
                  )),
            });
          }),
          (this.configured = !0),
          e);
    }
    async clearBrowsingData(e) {
      (e.includes(`downloads`) &&
        (await this.options.downloadStore.clearHistory()),
        await Ax(e));
    }
  },
  jB = { maxHeight: 4096, maxWidth: 4096, minHeight: 160, minWidth: 240 };
function MB(e) {
  return e === `about:blank` ? `` : e;
}
function NB(e, t) {
  return t ? e.comments.at(-1)?.designChange != null : !1;
}
function PB(e, t, r) {
  return {
    ...e,
    ...(t == null
      ? {}
      : {
          annotationEditorMode: `comment`,
          annotationFlow:
            t === `comment` ? (e.annotationFlow ?? n.vc.BATCH) : n.vc.BATCH,
          annotationModeEntrySource:
            t === `comment` ? (e.annotationModeEntrySource ?? null) : null,
          interactionMode: t,
        }),
    comments: r ?? e.comments,
    isAnnotationAddModifierPressed: !1,
    isDesignModifierPressed: !1,
    isOriginalViewEnabled: !1,
    isTweaksEditorOpen: !1,
  };
}
function FB(e, t) {
  return e.isAnnotationModeEnabled === t ||
    ((e.isAnnotationModeEnabled = t), t) ||
    ((e.pendingCommentModeActivation = null),
    e.snapshot.interactionMode !== `comment`)
    ? !1
    : ((e.snapshot = PB(e.snapshot, `browse`)), !0);
}
var IB = r.a(`browser-sidebar-manager`),
  LB = 15e3,
  RB = class {
    requests = new Set();
    constructor(e = () => void 0) {
      this.recordDebugEvent = e;
    }
    getDebugSnapshot() {
      return Array.from(this.requests).map((e) => ({
        activeBrowserTabId: null,
        activeConversationId: null,
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        guestWebContentsId: null,
        hasPage: null,
        ownerWebContentsId: e.owner.id,
        ownerRoutePath: null,
        pendingAttachCount: null,
        pendingBrowserUsePanelOpen: null,
        routeActive: null,
        windowId: null,
      }));
    }
    waitForOpen(e, t, n = null, r) {
      return new Promise((i, a) => {
        let o = {
          browserTabId: n,
          conversationId: t,
          owner: e,
          reject: a,
          resolve: i,
          timer: setTimeout(() => {
            let i = Error(
              `Timed out waiting for the Browser webview to attach for this browser-use page`,
            );
            (this.recordDebugEvent({
              browserTabId: n,
              conversationId: t,
              guestWebContentsId: null,
              kind: `browser-open-wait-timeout`,
              message: `Timed out waiting for Browser webview attach`,
              ownerWebContentsId: e.id,
              pageKey: null,
              windowId: null,
            }),
              IB().warning(`IAB_LIFECYCLE browser use open wait timed out`, {
                safe: { conversationId: t, ownerWebContentsId: e.id },
                sensitive: {},
              }),
              this.reject(o, i),
              r?.(i));
          }, LB),
        };
        (o.timer.unref(),
          this.requests.add(o),
          IB().info(`IAB_LIFECYCLE browser use open wait started`, {
            safe: {
              browserTabId: n,
              conversationId: t,
              ownerWebContentsId: e.id,
            },
            sensitive: {},
          }),
          this.recordDebugEvent({
            browserTabId: n,
            conversationId: t,
            guestWebContentsId: null,
            kind: `browser-open-wait-started`,
            message: `Waiting for Browser webview attach`,
            ownerWebContentsId: e.id,
            pageKey: null,
            windowId: null,
          }));
      });
    }
    resolveForWindow(e, t) {
      let n = Array.from(this.requests).filter(
        (t) => t.owner.id === e.owner.id,
      );
      for (let e of n) {
        let n = t(e);
        n != null && this.resolve(e, n);
      }
    }
    rejectForOwner(e, t) {
      for (let n of Array.from(this.requests))
        n.owner.id === e.id && this.reject(n, t);
    }
    rejectForRoute({ browserTabId: e, conversationId: t, error: n, owner: r }) {
      for (let i of Array.from(this.requests))
        i.owner.id === r.id &&
          i.conversationId === t &&
          i.browserTabId === e &&
          this.reject(i, n);
    }
    resolve(e, t) {
      (IB().info(`IAB_LIFECYCLE browser use open wait resolved`, {
        safe: {
          browserTabId: t.browserTabId ?? e.browserTabId,
          conversationId: e.conversationId,
          guestWebContentsId: t.webContents.id,
          ownerWebContentsId: e.owner.id,
          pageKey: t.pageKey,
        },
        sensitive: {},
      }),
        this.recordDebugEvent({
          browserTabId: t.browserTabId ?? e.browserTabId,
          conversationId: e.conversationId,
          guestWebContentsId: t.webContents.id,
          kind: `browser-open-wait-resolved`,
          message: `Resolved Browser webview attach wait`,
          ownerWebContentsId: e.owner.id,
          pageKey: t.pageKey,
          windowId: null,
        }),
        this.requests.delete(e),
        clearTimeout(e.timer),
        e.resolve(t));
    }
    reject(e, t) {
      (this.recordDebugEvent({
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        details: [{ label: `errorMessage`, value: t.message }],
        guestWebContentsId: null,
        kind: `browser-open-wait-rejected`,
        message: `Rejected Browser webview attach wait`,
        ownerWebContentsId: e.owner.id,
        pageKey: null,
        windowId: null,
      }),
        IB().warning(`IAB_LIFECYCLE browser use open wait rejected`, {
          safe: {
            conversationId: e.conversationId,
            errorMessage: t.message,
            ownerWebContentsId: e.owner.id,
          },
          sensitive: {},
        }),
        this.requests.delete(e),
        clearTimeout(e.timer),
        e.reject(t));
    }
  },
  zB = `No focused ChatGPT window is available to open a Browser tab`;
function BB({
  getPreferredWindowState: e,
  getWindowStates: t,
  getWindowId: n,
  getActivePage: r,
}) {
  let i = e();
  if (i != null) {
    let e = r(i);
    if (e != null) return e;
  }
  let a = i == null ? null : n(i);
  for (let e of t()) {
    if (a != null && n(e) === a) continue;
    let t = r(e);
    if (t != null) return t;
  }
  return null;
}
async function VB(e) {
  let t = e.getPreferredWindowState();
  if (t != null) {
    let n = e.getOrCreateActiveVisiblePage(t);
    if (n != null) return n;
  }
  let n = e.getFocusedOwner();
  if (n == null || e.isOwnerDestroyed(n)) throw Error(zB);
  e.sendOpenBrowserPanelMessage(n, { startingUrl: e.startingUrl });
  let r = e.getWindowStateForOwner(n);
  if (r != null) {
    let t = e.getOrCreateActiveVisiblePage(r);
    if (t != null) return t;
  }
  return e.waitForOpenRequest(n);
}
var HB = 16,
  UB = 12,
  WB = 8,
  GB = 8,
  KB = 10,
  qB = class {
    nextSessionId = 0;
    overlaysByKey = new Map();
    constructor(e, t) {
      ((this.windowManager = e), (this.errorReporter = t));
    }
    async prepare(e) {
      let t = c.BrowserWindow.fromWebContents(e.owner),
        n = this.getOverlayState(e.owner, e.conversationId, e.browserTabId);
      if (n != null) {
        let r = ZB(n, t);
        if (n.wantsToBeVisible) {
          if (n.window == null || r) {
            ((n.owner = e.owner),
              (n.hostId = e.hostId),
              (n.browserBounds = e.browserBounds));
            let t = this.createFrame({
              owner: e.owner,
              browserBounds: e.browserBounds,
              viewportScale: e.viewportScale,
              anchorState: n.session.anchorState,
              forceFallback: n.session.placementStrategy === `fallback`,
              body: n.session.body,
              placementSurfaceSize: n.placementSurfaceSize,
              previewSurfaceSize: n.previewSurfaceSize,
              surfaceMode: n.session.surfaceMode,
            });
            ((n.session = {
              ...n.session,
              cwd: e.cwd,
              placementStrategy: t.placementStrategy,
              previewAlignment: t.previewAlignment,
            }),
              (n.viewportScale = e.viewportScale),
              this.recordStateEvent(n, `prepare`),
              this.attachOwnerWindowListeners(n),
              this.positionOverlayWindow(n),
              this.syncOwnerRenderer(n));
            return;
          }
        } else if (r) return;
        else if (n.window == null) {
          let t = iV(e.browserBounds, e.viewportScale);
          ((n.owner = e.owner),
            (n.hostId = e.hostId),
            (n.browserBounds = e.browserBounds),
            (n.viewportScale = e.viewportScale),
            (n.session = {
              ...this.createSession({
                ...e,
                target: { mode: `create` },
                anchorState: t,
                body: ``,
                surfaceMode: `editor`,
              }),
              sessionId: n.session.sessionId,
            }),
            (n.mountedSessionId = null),
            (n.placementSurfaceSize = null),
            (n.previewSurfaceSize = null),
            (n.isPreviewOpen = !1),
            this.recordStateEvent(n, `prepare`),
            this.attachOwnerWindowListeners(n),
            this.positionOverlayWindow(n),
            this.syncOwnerRenderer(n, { shouldPrewarm: !0 }),
            $B(n));
          return;
        }
      }
      let r = iV(e.browserBounds, e.viewportScale),
        i = this.ensureOverlayState({
          ...e,
          target: { mode: `create` },
          anchorState: r,
          body: ``,
          surfaceMode: `editor`,
        });
      i.wantsToBeVisible ||
        ((i.owner = e.owner),
        (i.hostId = e.hostId),
        (i.browserBounds = e.browserBounds),
        (i.viewportScale = e.viewportScale),
        (i.session = this.createSession({
          ...e,
          target: { mode: `create` },
          anchorState: r,
          body: ``,
          surfaceMode: `editor`,
        })),
        (i.mountedSessionId = null),
        (i.isPreviewOpen = !1),
        (i.wantsToBeVisible = !1),
        (i.shouldFocusWhenVisible = !1),
        this.recordStateEvent(i, `prepare`),
        this.attachOwnerWindowListeners(i),
        this.positionOverlayWindow(i),
        this.syncOwnerRenderer(i, { shouldPrewarm: !0 }),
        $B(i));
    }
    async open(e) {
      let t = this.ensureOverlayState(e);
      ((t.owner = e.owner),
        (t.hostId = e.hostId),
        (t.browserBounds = e.browserBounds),
        (t.viewportScale = e.viewportScale),
        (t.session = this.createSession(e)));
      let n = t.session.sessionId;
      if (
        ((t.mountedSessionId = null),
        (t.placementSurfaceSize = null),
        (t.previewSurfaceSize = null),
        (t.isPreviewOpen = !1),
        (t.wantsToBeVisible = !0),
        (t.shouldFocusWhenVisible = e.surfaceMode === `editor`),
        (t.lifetimeCounts.openCount += 1),
        this.recordStateEvent(t, `open`),
        $B(t),
        this.attachOwnerWindowListeners(t),
        this.positionOverlayWindow(t),
        this.syncOwnerRenderer(t),
        this.applyWindowInteractivityPolicy(t),
        !this.isCurrentOverlayState(t) ||
          (t.window != null && t.window.isDestroyed()) ||
          !t.wantsToBeVisible ||
          t.session.sessionId !== n)
      ) {
        $B(t);
        return;
      }
      if (
        (this.positionOverlayWindow(t),
        !(t.window == null || t.window.isDestroyed()))
      ) {
        if (!XB(t)) {
          $B(t);
          return;
        }
        if (e.surfaceMode === `preview`) {
          this.showPreviewWindow(t);
          return;
        }
        ((t.shouldFocusWhenVisible = !1),
          this.recordStateEvent(t, `show-editor`),
          YB(t.window),
          this.scheduleEditorShowVerification(t, `open`));
      }
    }
    async openPreview(e) {
      await this.open({ ...e, surfaceMode: `preview` });
    }
    closePreview(e, t, n, r) {
      let i = this.getOverlayState(e, t, n);
      i == null ||
        i.session.surfaceMode !== `preview` ||
        i.session.target.mode !== `edit` ||
        i.session.target.commentId !== r ||
        (vV(i),
        (i.wantsToBeVisible = !1),
        (i.shouldFocusWhenVisible = !1),
        this.recordStateEvent(i, `close-preview`),
        this.applyWindowInteractivityPolicy(i),
        $B(i));
    }
    updateAnchor({
      owner: e,
      conversationId: t,
      browserTabId: r,
      target: i,
      anchorState: a,
      browserBounds: o,
      viewportScale: s,
    }) {
      let c = this.getOverlayState(e, t, r);
      if (c == null || !n.Cc(c.session.target, i)) return;
      c.browserBounds = o;
      let l =
        a == null
          ? null
          : this.createFrame({
              owner: c.owner,
              browserBounds: o,
              viewportScale: s,
              anchorState: a,
              body: c.session.body,
              placementSurfaceSize: c.placementSurfaceSize,
              previewSurfaceSize: c.previewSurfaceSize,
              surfaceMode: c.session.surfaceMode,
            });
      ((c.session = {
        ...c.session,
        anchorState: a ?? c.session.anchorState,
        ...(l == null
          ? { placementStrategy: `fallback` }
          : {
              placementStrategy: l.placementStrategy,
              previewAlignment: l.previewAlignment,
            }),
      }),
        (c.viewportScale = s),
        this.positionOverlayWindow(c),
        this.syncOwnerRenderer(c),
        this.applyWindowInteractivityPolicy(c));
    }
    dismiss(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      if (r == null) return;
      vV(r);
      let i =
        r.window != null && !r.window.isDestroyed() && r.window.isFocused();
      ((r.wantsToBeVisible = !1),
        (r.shouldFocusWhenVisible = !1),
        this.recordStateEvent(r, `dismiss`),
        r.window != null &&
          !r.window.isDestroyed() &&
          r.window.setIgnoreMouseEvents(!0, { forward: !0 }),
        $B(r),
        i &&
          r.ownerWindow != null &&
          !r.ownerWindow.isDestroyed() &&
          r.ownerWindow.isVisible() &&
          !r.ownerWindow.isMinimized() &&
          r.ownerWindow.focus());
    }
    focus(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      if (
        r == null ||
        r.session.surfaceMode === `preview` ||
        r.window == null ||
        r.window.isDestroyed()
      )
        return;
      let i = c.BrowserWindow.fromWebContents(r.owner);
      i == null ||
        i.isDestroyed() ||
        !i.isVisible() ||
        i.isMinimized() ||
        (vV(r),
        r.wantsToBeVisible ||
          ((r.wantsToBeVisible = !0),
          (r.shouldFocusWhenVisible = !1),
          this.positionOverlayWindow(r),
          this.syncOwnerRenderer(r),
          this.applyWindowInteractivityPolicy(r)),
        XB(r) &&
          (this.recordStateEvent(r, `focus-request`),
          YB(r.window),
          this.scheduleEditorShowVerification(r, `focus`)));
    }
    close(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      r != null && this.closeOverlayState(r);
    }
    transferConversation(e, t, n, r, i) {
      if (t === r && n === i) return;
      let a = this.getOverlayState(e, t, n);
      if (a == null) return;
      let o = this.getOverlayState(e, r, i);
      (o != null && o !== a && this.closeOverlayState(o),
        this.overlaysByKey.delete(QB(e, t, n)),
        (a.browserTabId = i),
        (a.conversationId = r),
        (a.session = {
          ...a.session,
          sessionId: String(this.nextSessionId++),
          conversationId: r,
        }),
        (a.mountedSessionId = null),
        (a.shouldFocusWhenVisible = a.wantsToBeVisible),
        this.overlaysByKey.set(QB(e, r, i), a),
        $B(a),
        rV(a),
        this.syncOwnerRenderer(a));
    }
    claimRendererOpenedWindow({
      browserTabId: e,
      conversationId: t,
      hostId: n,
      owner: r,
      sessionId: i,
      window: a,
    }) {
      let o = this.getOverlayState(r, t, e);
      if (
        o == null ||
        o.hostId !== n ||
        o.session.sessionId !== i ||
        a.isDestroyed()
      ) {
        a.close();
        return;
      }
      (o.window != null && o.window !== a && rV(o),
        a.hide(),
        (o.window = a),
        (o.mountedSessionId = null),
        (o.previewSurfaceSize = null),
        (o.lifetimeCounts.claimCount += 1),
        this.recordStateEvent(o, `claim-window`),
        this.attachOwnerWindowListeners(o),
        a.on(`blur`, () => {
          this.scheduleOverlayVisibilitySync(o);
        }),
        a.on(`focus`, () => {
          vV(o);
        }),
        a.on(`closed`, () => {
          (o.window === a && ((o.window = null), (o.mountedSessionId = null)),
            this.scheduleOverlayVisibilitySync(o));
        }),
        this.positionOverlayWindow(o),
        this.applyWindowInteractivityPolicy(o),
        this.showOverlayWindowIfOwnerFocused(o),
        this.scheduleOverlayVisibilitySync(o));
    }
    handleOverlayMounted({
      owner: e,
      browserTabId: t,
      conversationId: n,
      sessionId: r,
      placementSurfaceSize: i,
      surfaceSize: a,
    }) {
      let o = this.getOverlayState(e, n, t);
      o == null ||
        r !== o.session.sessionId ||
        ((i == null
          ? o.placementSurfaceSize != null
          : _V(o.placementSurfaceSize, i)) &&
          (o.placementSurfaceSize = i ?? null),
        a != null && _V(o.previewSurfaceSize, a) && (o.previewSurfaceSize = a),
        (o.mountedSessionId = r),
        (o.lifetimeCounts.mountCount += 1),
        this.recordStateEvent(o, `mounted`),
        this.positionOverlayWindow(o),
        this.syncOwnerRenderer(o),
        this.applyWindowInteractivityPolicy(o),
        this.showOverlayWindowIfOwnerFocused(o),
        this.scheduleOverlayVisibilitySync(o));
    }
    handleOverlayPreviewOpenChanged(e, t) {
      let n = this.getOverlayState(e, t.conversationId, t.browserTabId);
      n == null ||
        t.sessionId !== n.session.sessionId ||
        ((n.isPreviewOpen = t.previewOpen),
        this.positionOverlayWindow(n),
        this.syncOwnerRenderer(n));
    }
    getOverlayStateForOwner(e, t, n, { includeHidden: r = !1 } = {}) {
      let i = this.getOverlayState(e, t, n);
      return i == null || (!r && !i.wantsToBeVisible)
        ? null
        : {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          };
    }
    updateSessionForOwner(e, t, n, r) {
      let i = this.getOverlayState(e, t, n);
      return i == null
        ? null
        : ((i.session = r(i.session)),
          {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          });
    }
    getOverlayStateForSession(e, t, n, { includeHidden: r = !1 } = {}) {
      let i = [...this.overlaysByKey.values()].find(
        (r) =>
          r.owner === e && r.conversationId === t && r.session.sessionId === n,
      );
      return i == null || (!r && !i.wantsToBeVisible)
        ? null
        : {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          };
    }
    getOverlayState(e, t, n) {
      return this.overlaysByKey.get(QB(e, t, n)) ?? null;
    }
    ensureOverlayState(e) {
      let t = QB(e.owner, e.conversationId, e.browserTabId),
        n = this.getOverlayState(e.owner, e.conversationId, e.browserTabId),
        r = c.BrowserWindow.fromWebContents(e.owner);
      if (n != null) {
        if (n.window == null) return n;
        if (n.window.isDestroyed()) this.overlaysByKey.delete(t);
        else {
          if (ZB(n, r)) return n;
          this.closeOverlayState(n);
        }
      }
      let i = {
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        hostId: e.hostId,
        owner: e.owner,
        window: null,
        mountedSessionId: null,
        wantsToBeVisible: !1,
        shouldFocusWhenVisible: !1,
        isPreviewOpen: !1,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        session: this.createSession(e),
        ownerWindow: r,
        ownerWindowListenersCleanup: null,
        ownerDestroyedCleanup: null,
        visibilitySyncTimeout: null,
        previewSurfaceSize: null,
        placementSurfaceSize: null,
        showVerificationTimeout: null,
        eventSequence: 0,
        recentEvents: [],
        lifetimeCounts: {
          claimCount: 0,
          hideCount: 0,
          mountCount: 0,
          openCount: 0,
        },
        reportedAnomalyKeys: new Set(),
      };
      (this.overlaysByKey.set(t, i), this.attachOwnerWindowListeners(i));
      let a = () => {
        ((i.ownerDestroyedCleanup = null), this.closeOverlayState(i));
      };
      return (
        (i.ownerDestroyedCleanup = () => {
          e.owner.removeListener(`destroyed`, a);
        }),
        e.owner.once(`destroyed`, a),
        i
      );
    }
    closeOverlayState(e) {
      (this.overlaysByKey.delete(QB(e.owner, e.conversationId, e.browserTabId)),
        (e.wantsToBeVisible = !1),
        (e.shouldFocusWhenVisible = !1),
        this.recordStateEvent(e, `close-state`),
        e.window != null && rV(e),
        this.detachOwnerWindowListeners(e),
        e.ownerDestroyedCleanup?.(),
        (e.ownerDestroyedCleanup = null),
        vV(e),
        eV(e));
    }
    attachOwnerWindowListeners(e) {
      let t = c.BrowserWindow.fromWebContents(e.owner);
      if (
        (e.ownerWindow === t && e.ownerWindowListenersCleanup != null) ||
        (this.detachOwnerWindowListeners(e), (e.ownerWindow = t), t == null)
      )
        return;
      let n = () => {
          (this.positionOverlayWindow(e), this.syncOwnerRenderer(e));
        },
        r = () => {
          (this.positionOverlayWindow(e),
            this.syncOwnerRenderer(e),
            this.scheduleOverlayVisibilitySync(e));
        },
        i = () => {
          $B(e);
        },
        a = () => {
          this.scheduleOverlayVisibilitySync(e);
        };
      (t.on(`move`, n),
        t.on(`resize`, n),
        t.on(`focus`, r),
        t.on(`show`, r),
        t.on(`restore`, r),
        t.on(`blur`, a),
        t.on(`hide`, i),
        t.on(`minimize`, i),
        (e.ownerWindowListenersCleanup = () => {
          t.isDestroyed() ||
            (t.removeListener(`move`, n),
            t.removeListener(`resize`, n),
            t.removeListener(`focus`, r),
            t.removeListener(`show`, r),
            t.removeListener(`restore`, r),
            t.removeListener(`blur`, a),
            t.removeListener(`hide`, i),
            t.removeListener(`minimize`, i));
        }));
    }
    detachOwnerWindowListeners(e) {
      (e.ownerWindowListenersCleanup != null && e.ownerWindowListenersCleanup(),
        (e.ownerWindow = null),
        (e.ownerWindowListenersCleanup = null));
    }
    isCurrentOverlayState(e) {
      return (
        this.getOverlayState(e.owner, e.conversationId, e.browserTabId) === e
      );
    }
    scheduleOverlayVisibilitySync(e) {
      (vV(e),
        (e.visibilitySyncTimeout = setTimeout(() => {
          if (
            ((e.visibilitySyncTimeout = null),
            e.window == null || e.window.isDestroyed())
          ) {
            $B(e);
            return;
          }
          if (!e.wantsToBeVisible) {
            $B(e);
            return;
          }
          if (!XB(e)) {
            $B(e);
            return;
          }
          let t = c.BrowserWindow.fromWebContents(e.owner),
            n = c.BrowserWindow.getFocusedWindow();
          if (!(t == null || t.isDestroyed())) {
            if (!t.isVisible() || t.isMinimized()) {
              $B(e);
              return;
            }
            if (n !== e.window) {
              if (n === t) {
                this.showOverlayWindowIfOwnerFocused(e);
                return;
              }
              $B(e);
            }
          }
        }, 0)));
    }
    createSession(e) {
      let t = this.createFrame({
        owner: e.owner,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        anchorState: e.anchorState,
        body: e.body,
        placementSurfaceSize: null,
        previewSurfaceSize: null,
        surfaceMode: e.surfaceMode,
      });
      return {
        sessionId: String(this.nextSessionId++),
        conversationId: e.conversationId,
        target: e.target,
        anchorState: e.anchorState,
        body: e.body,
        ...(e.defaultDesignEditorOpen === !0
          ? { defaultDesignEditorOpen: !0 }
          : {}),
        cwd: e.cwd,
        attachedImages: e.attachedImages,
        designChange: e.designChange,
        designEditorState: e.designEditorState,
        placementStrategy: t.placementStrategy,
        previewAlignment: t.previewAlignment,
        surfaceMode: e.surfaceMode,
        screenshot: e.screenshot,
      };
    }
    positionOverlayWindow(e) {
      let t = this.createGeometry({
        owner: e.owner,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        anchorState: e.session.anchorState,
        forceFallback: e.session.placementStrategy === `fallback`,
        body: e.session.body,
        placementSurfaceSize: e.placementSurfaceSize,
        previewSurfaceSize: e.previewSurfaceSize,
        surfaceMode: e.session.surfaceMode,
        fullWindow: e.isPreviewOpen,
      });
      e.window != null &&
        !e.window.isDestroyed() &&
        e.window.setBounds(t.overlayWindowBounds, !1);
    }
    syncOwnerRenderer(e, { shouldPrewarm: t = !1 } = {}) {
      if (e.owner.isDestroyed()) return;
      let n = XB(e),
        r = this.createGeometry({
          owner: e.owner,
          browserBounds: e.browserBounds,
          viewportScale: e.viewportScale,
          anchorState: e.session.anchorState,
          forceFallback: e.session.placementStrategy === `fallback`,
          body: e.session.body,
          placementSurfaceSize: e.placementSurfaceSize,
          previewSurfaceSize: e.previewSurfaceSize,
          surfaceMode: e.session.surfaceMode,
          fullWindow: e.isPreviewOpen,
        });
      this.windowManager.sendMessageToWebContents(e.owner, {
        type: `browser-sidebar-comment-overlay-session`,
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        session: { ...e.session, previewAlignment: r.previewAlignment },
        overlayWindowBounds: r.overlayWindowBounds,
        editorFrame: r.editorFrame,
        visible: e.wantsToBeVisible && n,
        shouldPrewarm: t || (e.wantsToBeVisible && !n),
      });
    }
    createGeometry({
      owner: e,
      browserBounds: t,
      viewportScale: n,
      anchorState: r,
      forceFallback: i = !1,
      body: a = ``,
      placementSurfaceSize: o,
      previewSurfaceSize: s,
      surfaceMode: l = `editor`,
      fullWindow: u = !1,
    }) {
      return aV({
        ownerWindow: c.BrowserWindow.fromWebContents(e),
        browserBounds: t,
        viewportScale: n,
        anchorState: r,
        forceFallback: i,
        body: a,
        placementSurfaceSize: o,
        previewSurfaceSize: s,
        surfaceMode: l,
        fullWindow: u,
      });
    }
    createFrame({
      owner: e,
      browserBounds: t,
      viewportScale: n,
      anchorState: r,
      forceFallback: i = !1,
      body: a = ``,
      placementSurfaceSize: o,
      previewSurfaceSize: s,
      surfaceMode: l = `editor`,
    }) {
      return oV({
        ownerWindow: c.BrowserWindow.fromWebContents(e),
        browserBounds: t,
        viewportScale: n,
        anchorState: r,
        forceFallback: i,
        body: a,
        placementSurfaceSize: o,
        previewSurfaceSize: s,
        surfaceMode: l,
      });
    }
    showOverlayWindowIfOwnerFocused(e) {
      if (!e.wantsToBeVisible || !XB(e)) return;
      let t = c.BrowserWindow.fromWebContents(e.owner),
        n = c.BrowserWindow.getFocusedWindow();
      if (e.window != null && (t?.isFocused() || n === e.window)) {
        if (
          (this.positionOverlayWindow(e), e.session.surfaceMode === `preview`)
        ) {
          this.showPreviewWindow(e);
          return;
        }
        if (!e.shouldFocusWhenVisible) {
          JB(e.window);
          return;
        }
        ((e.shouldFocusWhenVisible = !1),
          this.recordStateEvent(e, `show-editor`),
          YB(e.window),
          this.scheduleEditorShowVerification(e, `owner-focus-sync`));
      }
    }
    applyWindowInteractivityPolicy(e) {
      let t = e.window;
      t == null ||
        t.isDestroyed() ||
        t.setIgnoreMouseEvents(e.session.surfaceMode === `preview`, {
          forward: e.session.surfaceMode === `preview`,
        });
    }
    showPreviewWindow(e) {
      let t = e.window;
      t == null ||
        t.isDestroyed() ||
        (this.applyWindowInteractivityPolicy(e),
        !t.isVisible() && (this.recordStateEvent(e, `show-preview`), JB(t)));
    }
    recordStateEvent(e, t) {
      nV(e, t);
    }
    scheduleEditorShowVerification(e, t) {
      eV(e);
      let n = e.session.sessionId;
      e.showVerificationTimeout = setTimeout(() => {
        if (
          ((e.showVerificationTimeout = null),
          !this.isCurrentOverlayState(e) ||
            e.session.sessionId !== n ||
            e.window == null ||
            e.window.isDestroyed() ||
            e.session.surfaceMode !== `editor` ||
            !e.wantsToBeVisible ||
            !XB(e))
        )
          return;
        let r = c.BrowserWindow.fromWebContents(e.owner);
        if (r == null || r.isDestroyed() || !r.isVisible() || r.isMinimized())
          return;
        let i = tV(e, r, c.BrowserWindow.getFocusedWindow()),
          a = e.window.isVisible();
        if (a && i === `overlay`) return;
        let o = e.window.getParentWindow() === r;
        this.reportOverlayAnomaly(e, `post-show-verification-failed`, {
          focusedWindowKind: i,
          hasParentOwner: o,
          ownerFocused: r.isFocused(),
          ownerVisible: r.isVisible(),
          showReason: t,
          windowVisible: a,
        });
      }, 0);
    }
    reportOverlayAnomaly(e, t, n) {
      let r = `${t}:${e.session.sessionId}`;
      e.reportedAnomalyKeys.has(r) ||
        (e.reportedAnomalyKeys.add(r),
        this.errorReporter.reportNonFatal(
          Error(`Browser comment overlay lifecycle anomaly`),
          {
            kind: `browser-sidebar-comment-overlay-anomaly`,
            tags: { anomaly: t, surfaceMode: e.session.surfaceMode },
            extra: {
              ...n,
              counts: e.lifetimeCounts,
              hasMountedSession: e.mountedSessionId != null,
              mountedMatchesSession: XB(e),
              recentEvents: e.recentEvents,
              shouldFocusWhenVisible: e.shouldFocusWhenVisible,
              wantsToBeVisible: e.wantsToBeVisible,
            },
          },
        ));
    }
  };
function JB(e) {
  (e.moveTop(), e.showInactive());
}
function YB(e) {
  (e.moveTop(), e.show(), e.focus());
}
function XB(e) {
  return (
    e.window != null &&
    !e.window.isDestroyed() &&
    e.mountedSessionId === e.session.sessionId
  );
}
function ZB(e, t) {
  return (
    e.window != null &&
    !e.window.isDestroyed() &&
    e.window.getParentWindow() === t
  );
}
function QB(e, t, n) {
  return `${e.id}:${t}\0${n}`;
}
function $B(e) {
  (eV(e),
    e.window != null &&
      !e.window.isDestroyed() &&
      ((e.lifetimeCounts.hideCount += 1), nV(e, `hide`), e.window.hide()));
}
function eV(e) {
  e.showVerificationTimeout != null &&
    (clearTimeout(e.showVerificationTimeout),
    (e.showVerificationTimeout = null));
}
function tV(e, t, n) {
  return n == null
    ? `none`
    : n === e.window
      ? `overlay`
      : n === t
        ? `owner`
        : `other`;
}
function nV(e, t) {
  ((e.eventSequence += 1),
    e.recentEvents.push({
      event: t,
      mounted: XB(e),
      sequence: e.eventSequence,
      sessionId: e.session.sessionId,
      surfaceMode: e.session.surfaceMode,
      wantsToBeVisible: e.wantsToBeVisible,
      windowVisible:
        e.window == null || e.window.isDestroyed()
          ? null
          : e.window.isVisible(),
    }),
    e.recentEvents.length > KB && e.recentEvents.shift());
}
function rV(e) {
  (e.window != null && !e.window.isDestroyed() && e.window.close(),
    (e.window = null),
    (e.mountedSessionId = null));
}
function iV(e, t) {
  let n = { x: e.width / t / 2, y: e.height / t / 2 };
  return {
    anchor: {
      kind: `element`,
      pageUrl: ``,
      frameUrl: null,
      title: ``,
      elementPath: ``,
      point: { xPercent: 50, y: n.y },
      rect: { x: n.x, y: n.y, width: 0, height: 0 },
      isFixed: !1,
      role: null,
      name: null,
      selector: null,
      framePath: [],
      nearbyText: null,
    },
    viewportRect: null,
    viewportPoint: n,
  };
}
function aV({
  ownerWindow: e,
  browserBounds: t,
  viewportScale: n = 1,
  anchorState: r,
  forceFallback: i = !1,
  body: a = ``,
  placementSurfaceSize: o,
  previewSurfaceSize: s,
  surfaceMode: c = `editor`,
  fullWindow: l = !1,
}) {
  let u = oV({
    ownerWindow: e,
    browserBounds: t,
    viewportScale: n,
    anchorState: r,
    forceFallback: i,
    body: a,
    placementSurfaceSize: o,
    previewSurfaceSize: s,
    surfaceMode: c,
  });
  if (c === `preview`) {
    let e = uV(u.editorWindowBounds, GB);
    return {
      placementStrategy: u.placementStrategy,
      previewAlignment: u.previewAlignment,
      overlayWindowBounds: e,
      editorFrame: {
        x: GB,
        y: GB,
        width: u.editorWindowBounds.width,
        height: u.editorWindowBounds.height,
      },
    };
  }
  let d = cV({ ownerWindow: e, browserBounds: t }),
    f =
      l && e != null && !e.isDestroyed()
        ? e.getContentBounds()
        : lV(d, u.editorWindowBounds);
  return {
    placementStrategy: u.placementStrategy,
    previewAlignment: u.previewAlignment,
    overlayWindowBounds: f,
    editorFrame: {
      x: u.editorWindowBounds.x - f.x,
      y: u.editorWindowBounds.y - f.y,
      width: u.editorWindowBounds.width,
      height: u.editorWindowBounds.height,
    },
  };
}
function oV({
  ownerWindow: e,
  browserBounds: t,
  viewportScale: r = 1,
  anchorState: i,
  forceFallback: a = !1,
  body: o = ``,
  placementSurfaceSize: s,
  previewSurfaceSize: l,
  surfaceMode: u = `editor`,
}) {
  let d = sV({ ownerWindow: e, browserBounds: t }),
    f = n.Oc(o),
    p = Math.min(294, Math.max(1, d.width - HB * 2)),
    m = hV(l?.width ?? p, p),
    h = gV(l?.height ?? f, d.height - HB * 2),
    g = Math.min(
      Math.max(1, d.width - HB * 2),
      Math.max(294, s?.width ?? 294, l?.width ?? 294),
    ),
    _ = Math.max(1, d.height - HB * 2),
    v = hV(Math.max(l?.width ?? 294, s?.width ?? 0), g),
    y = gV(l?.height ?? 120, _),
    b = u === `preview` ? m : v,
    x = u === `preview` ? h : y,
    S = u === `preview` ? b : hV(s?.width ?? b, g),
    C = u === `preview` ? x : gV(s?.height ?? x, _),
    w = c.screen.getDisplayMatching(d).workArea,
    T =
      u === `preview`
        ? pV({
            browserViewportBounds: d,
            viewportScale: r,
            anchorState: i,
            previewWidth: m,
            previewHeight: h,
          })
        : null,
    E =
      u === `preview`
        ? null
        : dV({ browserViewportBounds: d, cardWidth: b, cardHeight: x }),
    D =
      T ??
      (a
        ? null
        : fV({
            browserViewportBounds: d,
            viewportScale: r,
            anchorState: i,
            cardWidth: b,
            cardHeight: x,
            placementCardWidth: S,
            placementCardHeight: C,
            sideAnchorHeight: 44,
          })),
    O = D?.bounds ?? E;
  if (O == null) throw Error(`Expected preview card bounds`);
  let k = T != null || D != null ? `anchored` : `fallback`,
    A = D?.previewAlignment ?? `end`,
    j = yV(O.x, w.x, w.x + w.width - b),
    M = yV(O.y, w.y, w.y + w.height - x),
    N = u === `preview` ? 0 : Math.min(88, Math.max(M - w.y, 0)),
    P = u === `preview` ? f : Math.max(x, 120) + N;
  return {
    cardWindowBounds: { x: j, y: M, width: b, height: x },
    placementStrategy: k,
    previewAlignment: A,
    editorWindowBounds: {
      x: j,
      y: u === `preview` ? M : M - N,
      width: b,
      height: P,
    },
  };
}
function sV({ ownerWindow: e, browserBounds: t }) {
  if (e == null || e.isDestroyed()) return t;
  let n = e.getContentBounds();
  return {
    x: Math.round(n.x + t.x),
    y: Math.round(n.y + t.y),
    width: t.width,
    height: t.height,
  };
}
function cV({ ownerWindow: e, browserBounds: t }) {
  let n = sV({ ownerWindow: e, browserBounds: t });
  if (e == null || e.isDestroyed()) return n;
  let r = e.getContentBounds(),
    i = t.x <= 0 ? WB : 0,
    a = t.y <= 0 ? WB : 0,
    o = t.x + t.width >= r.width ? WB : 0,
    s = t.y + t.height >= r.height ? WB : 0;
  return {
    x: n.x + i,
    y: n.y + a,
    width: Math.max(n.width - i - o, 1),
    height: Math.max(n.height - a - s, 1),
  };
}
function lV(e, t) {
  let n = Math.min(e.x, t.x),
    r = Math.min(e.y, t.y),
    i = Math.max(e.x + e.width, t.x + t.width),
    a = Math.max(e.y + e.height, t.y + t.height);
  return { x: n, y: r, width: Math.max(i - n, 1), height: Math.max(a - r, 1) };
}
function uV(e, t) {
  return {
    x: e.x - t,
    y: e.y - t,
    width: e.width + t * 2,
    height: e.height + t * 2,
  };
}
function dV({ browserViewportBounds: e, cardWidth: t, cardHeight: n }) {
  return {
    x: e.x + e.width - t - HB,
    y: e.y + e.height - n - HB,
    width: t,
    height: n,
  };
}
function fV({
  browserViewportBounds: e,
  viewportScale: t,
  anchorState: n,
  cardWidth: r,
  sideAnchorHeight: i,
  cardHeight: a,
  placementCardWidth: o,
  placementCardHeight: s,
}) {
  let c =
      n.viewportSize == null || n.viewportSize.width <= 0
        ? t
        : e.width / n.viewportSize.width,
    l =
      n.viewportSize == null || n.viewportSize.height <= 0
        ? t
        : e.height / n.viewportSize.height,
    u = e.x + yV(n.viewportPoint.x * c, 29, e.width - 29),
    d = e.y + yV(n.viewportPoint.y * l, 29, e.height - 29),
    f = e.x + HB,
    p = e.x + e.width - r - HB,
    m = e.x + e.width - o - HB,
    h = e.y + HB,
    g = e.y + e.height - a - HB,
    _ = e.y + e.height - s - HB,
    v = yV(d - i / 2, h, g),
    y = 26 / 2 + UB,
    b = u + y,
    x = u - y - r;
  if (b >= f && b <= m)
    return {
      bounds: { x: b, y: v, width: r, height: a },
      previewAlignment: `start`,
    };
  let S = u - y - o;
  if (S >= f && S <= m)
    return {
      bounds: { x, y: v, width: r, height: a },
      previewAlignment: `end`,
    };
  let C = yV(u - r / 2, f, p),
    w = d + y;
  if (w <= _)
    return {
      bounds: { x: C, y: w, width: r, height: a },
      previewAlignment: mV({ maxCardX: p, minCardX: f, x: C }),
    };
  let T = d - y - a;
  return d - y - s < h
    ? null
    : {
        bounds: { x: C, y: T, width: r, height: a },
        previewAlignment: mV({ maxCardX: p, minCardX: f, x: C }),
      };
}
function pV({
  browserViewportBounds: e,
  viewportScale: t,
  anchorState: n,
  previewWidth: r,
  previewHeight: i,
}) {
  let a =
      n.viewportSize == null || n.viewportSize.width <= 0
        ? t
        : e.width / n.viewportSize.width,
    o =
      n.viewportSize == null || n.viewportSize.height <= 0
        ? t
        : e.height / n.viewportSize.height,
    s = e.x + yV(n.viewportPoint.x * a, 29, e.width - 29),
    c = e.y + yV(n.viewportPoint.y * o, 29, e.height - 29),
    l = e.x + HB,
    u = e.x + e.width - r - HB,
    d = e.y + HB,
    f = e.y + e.height - i - HB,
    p = Math.min(294, Math.max(1, e.width - HB * 2)),
    m = 26 / 2 + UB,
    h = yV(c - i / 2, d, Math.max(d, f)),
    g = s + m;
  if (g + p <= e.x + e.width - HB)
    return {
      bounds: { x: g, y: h, width: r, height: i },
      previewAlignment: `start`,
    };
  if (s - m - p >= l)
    return {
      bounds: { x: s - m - r, y: h, width: r, height: i },
      previewAlignment: `end`,
    };
  let _ = yV(s - r / 2, l, Math.max(l, u)),
    v = c + m;
  return v + 120 <= e.y + e.height - HB
    ? {
        bounds: { x: _, y: v, width: r, height: i },
        previewAlignment: mV({ maxCardX: u, minCardX: l, x: _ }),
      }
    : {
        bounds: { x: _, y: Math.max(d, c - m - i), width: r, height: i },
        previewAlignment: mV({ maxCardX: u, minCardX: l, x: _ }),
      };
}
function mV({ maxCardX: e, minCardX: t, x: n }) {
  return n <= t ? `start` : n >= e ? `end` : `center`;
}
function hV(e, t) {
  return yV(Math.ceil(e), 1, Math.max(1, t));
}
function gV(e, t) {
  return yV(Math.ceil(e), 1, Math.max(1, t));
}
function _V(e, t) {
  return e == null || e.width !== t.width || e.height !== t.height;
}
function vV(e) {
  e.visibilitySyncTimeout != null &&
    (clearTimeout(e.visibilitySyncTimeout), (e.visibilitySyncTimeout = null));
}
function yV(e, t, n) {
  return n < t ? t : Math.min(Math.max(Math.round(e), t), n);
}
function bV(e, t, n, r) {
  if (e.width <= 0 || e.height <= 0 || n.width <= 0 || n.height <= 0)
    return null;
  let i = e.width / n.width,
    a = e.height / n.height,
    o = Math.max(0, Math.floor((t.x - r) * i)),
    s = Math.max(0, Math.floor((t.y - r) * a)),
    c = Math.min(e.width, Math.ceil((t.x + t.width + r) * i)),
    l = Math.min(e.height, Math.ceil((t.y + t.height + r) * a)),
    u = c - o,
    d = l - s;
  return u <= 0 || d <= 0 ? null : { x: o, y: s, width: u, height: d };
}
var xV = r.a(`browser-sidebar-comment-controller`),
  SV = 1e3,
  CV = 32,
  wV = 16,
  TV = 1280,
  EV = 1024,
  DV = 768,
  OV = `data-codex-browser-design-group`,
  kV = n.xl({ root: n.xl({ nodeId: n.bl() }) }),
  AV = n.xl({ nodeId: n.bl() }),
  jV = n.xl({ startLine: n.bl(), startColumn: n.bl() }),
  MV = n
    .xl({
      name: n.wl(),
      value: n.wl().optional(),
      disabled: n.ml().optional(),
      implicit: n.ml().optional(),
      range: jV.optional(),
    })
    .passthrough(),
  NV = n
    .xl({ text: n.wl().optional(), sourceURL: n.wl().optional() })
    .passthrough(),
  PV = n
    .xl({
      rule: n
        .xl({
          selectorList: n.xl({ text: n.wl().optional() }).optional(),
          media: n.pl(NV).optional(),
          containerQueries: n.pl(NV).optional(),
          supports: n.pl(NV).optional(),
          style: n.xl({ cssProperties: n.pl(MV).optional() }).optional(),
        })
        .passthrough(),
    })
    .passthrough(),
  FV = n
    .xl({
      inlineStyle: n.xl({ cssProperties: n.pl(MV).optional() }).optional(),
      matchedCSSRules: n.pl(PV).optional(),
    })
    .passthrough(),
  IV = class {
    overlayManager;
    runtimeCommentScreenshotWaiters = new Map();
    constructor(e, t, n, r = V) {
      ((this.windowManager = e),
        (this.errorReporter = t),
        (this.delegate = n),
        (this.hostId = r),
        (this.overlayManager = new qB(e, t)),
        this.windowManager.addBrowserCommentPopupWindowCreatedHandler((e) => {
          e.hostId === this.hostId &&
            this.overlayManager.claimRendererOpenedWindow(e);
        }));
    }
    prepare(e, t, r, i) {
      !i.visible ||
        i.bounds == null ||
        this.overlayManager
          .prepare({
            owner: e,
            hostId:
              this.windowManager.getHostIdForWebContents(e) ?? this.hostId,
            browserTabId: r,
            conversationId: t,
            browserBounds: i.bounds,
            viewportScale: i.viewportScale,
            cwd: i.cwd,
          })
          .catch((e) => {
            (xV().warning(`failed to prepare browser comment overlay`, {
              safe: { conversationId: t },
              sensitive: { error: e },
            }),
              this.errorReporter.reportNonFatal(
                e instanceof Error ? e : n.Rs(e),
                { kind: `browser-sidebar-comment-overlay-prepare` },
              ));
          });
    }
    dismiss(e, t, n, r) {
      (this.overlayManager.dismiss(e, t, n),
        this.closeRuntimeEditor(r?.page?.view.webContents ?? null));
    }
    close(e, t, n) {
      this.overlayManager.close(e, t, n);
    }
    transferConversation(e, t, n, r, i) {
      this.overlayManager.transferConversation(e, t, n, r, i);
    }
    restoreRuntimeEditor(e, t, n, r) {
      if (r == null || r.isDestroyed()) return;
      let i = this.overlayManager.getOverlayStateForOwner(e, t, n);
      i != null &&
        r.send(B, {
          type: `browser-sidebar-runtime-restore-editor`,
          target: i.session.target,
          anchorState: i.session.anchorState,
        });
    }
    handleOverlayMounted(e, t, n, r, i, a) {
      this.overlayManager.handleOverlayMounted({
        owner: e,
        browserTabId: n,
        conversationId: t,
        sessionId: r,
        placementSurfaceSize: a,
        surfaceSize: i,
      });
    }
    handleOverlayPreviewOpenChanged(e, t) {
      this.overlayManager.handleOverlayPreviewOpenChanged(e, t);
    }
    async handleRuntimeOpenEditor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
      );
      if (
        r == null ||
        r.bounds == null ||
        r.snapshot.interactionMode !== `comment`
      ) {
        this.closeRuntimeEditor(e, t.target);
        return;
      }
      let i = null;
      if (t.target.mode === `edit`) {
        let n = t.target.commentId;
        if (
          ((i = r.snapshot.comments.find((e) => e.id === n) ?? null), i == null)
        ) {
          this.closeRuntimeEditor(e, t.target);
          return;
        }
      }
      let a = this.delegate.canUseTweaks();
      if (
        !a &&
        i?.designChange != null &&
        (i.body?.trim() ?? ``).length === 0
      ) {
        this.closeRuntimeEditor(e, t.target);
        return;
      }
      this.overlayManager.getOverlayStateForOwner(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
        { includeHidden: !0 },
      )?.session.surfaceMode === `preview` &&
        this.overlayManager.close(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
        );
      let o = a ? i?.designChange : void 0,
        s = a ? (t.designEditorState ?? i?.designChange) : void 0,
        c =
          o != null ||
          (t.target.mode === `create` &&
            s != null &&
            r.snapshot.isTweaksEditorOpen === !0);
      (this.setTweaksEditorOpen(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
        r,
        c,
      ),
        await this.overlayManager.open({
          owner: n.owner,
          hostId:
            this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
          target: t.target,
          anchorState: t.anchorState,
          body: i?.body ?? ``,
          ...(t.target.mode === `create` && c
            ? { defaultDesignEditorOpen: !0 }
            : {}),
          cwd: r.cwd,
          attachedImages: i?.attachedImages,
          designChange: o,
          designEditorState: s,
          screenshot: void 0,
          surfaceMode: `editor`,
        }));
    }
    async handleRuntimeOpenDesignEditor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
      );
      if (
        r == null ||
        r.bounds == null ||
        r.snapshot.interactionMode !== `comment`
      ) {
        this.closeRuntimeEditor(e, {
          mode: `design`,
          groupId: t.designEditorState.id,
        });
        return;
      }
      if (!this.delegate.canUseTweaks()) return;
      let i = t.designEditorState;
      (this.overlayManager.getOverlayStateForOwner(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
        { includeHidden: !0 },
      )?.session.surfaceMode === `preview` &&
        this.overlayManager.close(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
        ),
        this.setTweaksEditorOpen(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
          r,
          !0,
        ),
        this.setAnnotationEditorMode(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
          r,
          `design`,
        ),
        await this.overlayManager.open({
          owner: n.owner,
          hostId:
            this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
          target: { mode: `design`, groupId: i.id },
          anchorState: t.anchorState,
          body: t.designChange?.comment ?? ``,
          cwd: r.cwd,
          designChange: t.designChange ?? void 0,
          designEditorState: i,
          screenshot: void 0,
          surfaceMode: `editor`,
        }));
    }
    async handleRuntimeOpenCommentPreview(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
      );
      if (
        r == null ||
        r.bounds == null ||
        r.snapshot.interactionMode !== `comment`
      )
        return;
      let i = r.snapshot.comments.find((e) => e.id === t.commentId) ?? null;
      if (i == null) return;
      let a = this.delegate.canUseTweaks();
      if (!a && i.designChange != null && (i.body?.trim() ?? ``).length === 0)
        return;
      let o = a ? i.designChange : void 0;
      await this.overlayManager.openPreview({
        owner: n.owner,
        hostId:
          this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
        browserTabId: n.page.browserTabId,
        conversationId: n.page.conversationId,
        browserBounds: r.bounds,
        target: { mode: `edit`, commentId: t.commentId },
        anchorState: t.anchorState,
        body: i.body ?? ``,
        cwd: r.cwd,
        attachedImages: i.attachedImages,
        designChange: o,
        viewportScale: r.viewportScale,
        screenshot: void 0,
      });
    }
    handleRuntimeCloseCommentPreview(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      !n ||
        n.owner.isDestroyed() ||
        this.overlayManager.closePreview(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
          t.commentId,
        );
    }
    handleRuntimeUpdateAnchor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
      );
      r == null ||
        r.bounds == null ||
        this.overlayManager.updateAnchor({
          owner: n.owner,
          conversationId: n.page.conversationId,
          browserTabId: n.page.browserTabId,
          target: t.target,
          anchorState: t.anchorState,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
        });
    }
    handleRuntimeFocusEditor(e) {
      let t = this.delegate.findPageStateForRuntimeSender(e);
      !t ||
        t.owner.isDestroyed() ||
        this.overlayManager.focus(
          t.owner,
          t.page.conversationId,
          t.page.browserTabId,
        );
    }
    handleRuntimeCommentScreenshotReady(e, t) {
      let n = this.runtimeCommentScreenshotWaiters.get(e.id);
      n == null ||
        n.commentId !== t.commentId ||
        (this.runtimeCommentScreenshotWaiters.delete(e.id), n.resolve(t));
    }
    async handleOverlaySubmit(e, t) {
      let r = this.overlayManager.getOverlayStateForOwner(
        e,
        t.conversationId,
        t.browserTabId,
      );
      if (r == null || r.session.sessionId !== t.sessionId) return;
      let i = t.browserTabId,
        a = this.delegate.getThreadState(r.owner, r.conversationId, i);
      if (a == null) return;
      let o = t.body?.trim() ?? ``,
        s =
          t.designChange === void 0 && this.delegate.canUseTweaks()
            ? r.session.designChange
            : t.designChange,
        c = s !== void 0 || r.session.designChange != null,
        l = s == null ? null : KV(s, o);
      if (o.length === 0 && l == null) {
        (this.setTweaksEditorOpen(r.owner, r.conversationId, i, a, !1),
          this.dismiss(r.owner, r.conversationId, i, a));
        return;
      }
      let u = r.session.target,
        d = a.page?.view.webContents ?? null,
        p = t.captureScreenshot !== !1,
        m = n.Dc(a.snapshot.annotationFlow),
        h = u.mode === `create` && !m.persistent,
        g = u.mode === `create` && t.submitDirectly === !0 && l == null,
        _ = g ? 1 : n.Tc(a.snapshot.comments) + 1,
        v = u.mode !== `edit` && p && _ >= 10,
        y =
          t.cropScreenshotToRegion === !0 &&
          r.session.anchorState.anchor.kind !== `element`,
        b = r.session.anchorState.viewportRect,
        x = r.session.anchorState.viewportPoint,
        S = v || y ? zV(b, x) : null;
      if (g) {
        let e = GV({
            anchorState: r.session.anchorState,
            body: o,
            commentId: (0, f.randomUUID)(),
            attachedImages: t.attachedImages,
            screenshot: r.session.screenshot,
          }),
          s = m.persistent ? a.snapshot.comments.length + 1 : 1;
        (h && (a.snapshot = PB(a.snapshot, `browse`)),
          this.dismiss(r.owner, r.conversationId, i, a));
        let c = p
          ? await this.captureDirectCommentScreenshot({
              comment: e,
              conversationId: r.conversationId,
              markerViewportPoint: x,
              annotationViewportRect: b,
              page: d,
              screenshotCropRect: S,
              shouldUseCompactScreenshot: v,
              threadState: a,
              runtimeComments: m.persistent ? [...a.snapshot.comments, e] : [e],
            })
          : void 0;
        (h
          ? this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i)
          : this.syncSnapshotRuntimeStateToPage(d, a),
          this.windowManager.sendMessageToWebContents(r.owner, {
            type: `browser-sidebar-direct-comment`,
            browserTabId: i,
            conversationId: r.conversationId,
            sessionId: t.sessionId,
            body: o,
            comment: n.Go(c == null ? e : { ...e, screenshot: c }, s, i),
          }));
        return;
      }
      if (u.mode === `create` || u.mode === `design`) {
        let e = l?.id ?? (0, f.randomUUID)(),
          n = GV({
            anchorState: r.session.anchorState,
            body: o,
            commentId: e,
            attachedImages: t.attachedImages,
            designChange: l,
            screenshot: r.session.screenshot,
          });
        ((a.snapshot = {
          ...a.snapshot,
          annotationEditorMode: `comment`,
          isOriginalViewEnabled: !1,
          comments: qV(a.snapshot.comments, n),
        }),
          h && (a.snapshot = PB(a.snapshot, `browse`)),
          this.compactThreadCommentScreenshotsIfNeeded(a),
          this.dismiss(r.owner, r.conversationId, i, a),
          this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i),
          p &&
            (await this.captureSavedCommentScreenshot({
              commentId: e,
              conversationId: r.conversationId,
              markerViewportPoint: x,
              annotationViewportRect: b,
              owner: r.owner,
              page: a.page?.view.webContents ?? null,
              screenshotCropRect: S,
              shouldUseCompactScreenshot: v,
              threadState: a,
              ...(m.persistent ? {} : { runtimeComments: [n] }),
            })));
      } else
        u.mode === `edit` &&
          ((a.snapshot = {
            ...a.snapshot,
            comments: a.snapshot.comments.map((e) => {
              if (e.id !== u.commentId) return e;
              let n =
                o.length === 0
                  ? YV({ ...e, attachedImages: t.attachedImages })
                  : { ...e, body: o, attachedImages: t.attachedImages };
              return c ? (l == null ? JV(n) : { ...n, designChange: l }) : n;
            }),
          }),
          this.dismiss(r.owner, r.conversationId, i, a),
          this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i));
    }
    handleDesignOverlayUpdate(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.conversationId,
        n.browserTabId,
      );
      r != null &&
        (n.session.designChange?.id ?? n.session.designEditorState?.id) !=
          null &&
        (this.overlayManager.updateSessionForOwner(
          n.owner,
          n.conversationId,
          n.browserTabId,
          (e) => ({ ...e, designChange: t.group ?? void 0 }),
        ),
        this.syncRuntimeStateToPage(
          n.owner,
          n.conversationId,
          r.page?.view.webContents ?? null,
          r,
        ));
    }
    async handleDesignOverlayAdd(e, t) {
      let r = this.getDesignOverlayState(e, t);
      if (r == null) return;
      let i = this.delegate.getThreadState(
        r.owner,
        r.conversationId,
        r.browserTabId,
      );
      if (i == null) return;
      let a = n.Tc(i.snapshot.comments) + 1 >= 10,
        o = r.session.anchorState.viewportRect,
        s = r.session.anchorState.viewportPoint,
        c = a ? zV(o, s) : null,
        l = KV(t.group, t.group.comment?.trim() ?? ``);
      (this.overlayManager.updateSessionForOwner(
        r.owner,
        r.conversationId,
        r.browserTabId,
        (e) => ({ ...e, designChange: l }),
      ),
        (i.snapshot = {
          ...i.snapshot,
          annotationEditorMode: `comment`,
          isOriginalViewEnabled: !1,
          comments: qV(
            i.snapshot.comments,
            GV({
              anchorState: r.session.anchorState,
              body: l.comment ?? ``,
              commentId: l.id,
              designChange: l,
              screenshot: r.session.screenshot,
            }),
          ),
        }),
        this.syncSnapshotRuntimeStateToPage(
          i.page?.view.webContents ?? null,
          i,
        ),
        this.dismiss(r.owner, r.conversationId, r.browserTabId, i),
        this.delegate.syncCommentSnapshot(
          r.owner,
          r.conversationId,
          r.browserTabId,
        ));
      let u = await this.enrichQueuedDesignChangeGroup(
          i.page?.view.webContents ?? null,
          r.session.designEditorState,
          l,
        ),
        d = i.snapshot.comments.find((e) => e.id === l.id)?.designChange;
      (u !== l &&
        $V(d, l) &&
        ((i.snapshot = {
          ...i.snapshot,
          comments: i.snapshot.comments.map((e) =>
            e.id === l.id && e.designChange != null
              ? {
                  ...e,
                  designChange: { ...e.designChange, provenance: u.provenance },
                }
              : e,
          ),
        }),
        this.delegate.syncCommentSnapshot(
          r.owner,
          r.conversationId,
          r.browserTabId,
        )),
        await this.captureSavedDesignChangeScreenshot({
          annotationViewportRect: o,
          conversationId: r.conversationId,
          groupId: l.id,
          markerViewportPoint: s,
          owner: r.owner,
          page: i.page?.view.webContents ?? null,
          screenshotCropRect: c,
          shouldUseCompactScreenshot: a,
          threadState: i,
        }));
    }
    async enrichQueuedDesignChangeGroup(e, t, n) {
      let r = XV(n.declarations, n.provenance, t?.provenance);
      if (
        ZV(n.declarations, r) ||
        e == null ||
        e.isDestroyed() ||
        t == null ||
        t.id !== n.id
      )
        return QV(n.provenance, r) ? n : { ...n, provenance: r };
      let i = await this.enrichDesignEditorProvenance(e, {
          ...t,
          declarations: n.declarations,
          text: n.text,
          provenance: r,
        }),
        a = XV(n.declarations, r, i.provenance);
      return QV(n.provenance, a) ? n : { ...n, provenance: a };
    }
    handleDesignOverlayDelete(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.conversationId,
        n.browserTabId,
      );
      if (r == null) return;
      let i = r.snapshot.comments.flatMap((e) =>
        e.designChange?.id === t.groupId
          ? (e.body?.trim().length ?? 0) > 0 ||
            (e.attachedImages?.length ?? 0) > 0
            ? [JV(e)]
            : []
          : [e],
      );
      (this.overlayManager.updateSessionForOwner(
        n.owner,
        n.conversationId,
        n.browserTabId,
        (e) => ({ ...e, designChange: void 0 }),
      ),
        (r.snapshot = {
          ...r.snapshot,
          isTweaksEditorOpen: !1,
          annotationEditorMode:
            i.length > 0 ? r.snapshot.annotationEditorMode : `comment`,
          comments: i,
        }),
        this.syncSnapshotRuntimeStateToPage(
          r.page?.view.webContents ?? null,
          r,
        ),
        this.dismiss(n.owner, n.conversationId, n.browserTabId, r),
        this.delegate.syncCommentSnapshot(
          n.owner,
          n.conversationId,
          n.browserTabId,
        ));
    }
    compactThreadCommentScreenshotsIfNeeded(e) {
      if (e.snapshot.comments.length < 10) return;
      let t = LV(e.snapshot.comments);
      t !== e.snapshot.comments &&
        (e.snapshot = { ...e.snapshot, comments: t });
    }
    handleOverlayDelete(e, t) {
      let r = this.overlayManager.getOverlayStateForOwner(
        e,
        t.conversationId,
        t.browserTabId,
      );
      if (r == null || r.session.sessionId !== t.sessionId) return;
      let i = t.browserTabId,
        a = this.delegate.getThreadState(r.owner, r.conversationId, i);
      if (a == null) return;
      let o = r.session.target;
      if (o.mode !== `edit` || o.commentId !== t.commentId) return;
      let s = a.snapshot.comments.filter((e) => e.id !== o.commentId);
      ((a.snapshot = {
        ...a.snapshot,
        annotationEditorMode: n.jc(s)
          ? a.snapshot.annotationEditorMode
          : `comment`,
        comments: s,
      }),
        this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i),
        this.dismiss(r.owner, r.conversationId, i, a));
    }
    handleOverlayClose(e, t, r, i) {
      let a = this.overlayManager.getOverlayStateForOwner(e, t, r);
      if (a == null || a.session.sessionId !== i) return;
      let o = this.delegate.getThreadState(a.owner, t, r);
      if (o == null) {
        (this.overlayManager.dismiss(a.owner, t, r),
          this.closeRuntimeEditor(null));
        return;
      }
      let s = o.snapshot,
        c = s,
        l = n.Dc(s.annotationFlow),
        u = l.persistent ? s.comments : [];
      if (a.session.target.mode === `create` && !l.persistent)
        c = PB(s, `browse`);
      else {
        let e = n.jc(s.comments) ? s.annotationEditorMode : `comment`;
        (s.isAnnotationAddModifierPressed === !0 ||
          s.isDesignModifierPressed === !0 ||
          s.isOriginalViewEnabled === !0 ||
          s.annotationEditorMode !== e) &&
          (c = {
            ...s,
            isAnnotationAddModifierPressed: !1,
            isDesignModifierPressed: !1,
            isOriginalViewEnabled: !1,
            annotationEditorMode: e,
          });
      }
      (c !== s &&
        ((o.snapshot = c), this.delegate.syncCommentSnapshot(a.owner, t, r)),
        this.overlayManager.dismiss(a.owner, t, r),
        this.sendRuntimeStateToPage(o.page?.view.webContents ?? null, o, u),
        this.closeRuntimeEditor(o.page?.view.webContents ?? null));
    }
    handleOverlayTweaksOpenChanged(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.conversationId,
        n.browserTabId,
      );
      r != null &&
        (this.setTweaksEditorOpen(
          n.owner,
          n.conversationId,
          n.browserTabId,
          r,
          t.open,
        ),
        this.setAnnotationEditorMode(
          n.owner,
          n.conversationId,
          n.browserTabId,
          r,
          t.open ? `design` : `comment`,
        ));
    }
    handleOverlayDesignScrubChanged(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(
        n.owner,
        n.conversationId,
        n.browserTabId,
      );
      r != null &&
        this.setRuntimeDesignScrubProperty(
          r.page?.view.webContents ?? null,
          t.property,
        );
    }
    getDesignOverlayState(e, t) {
      let n = this.overlayManager.getOverlayStateForSession(
        e,
        t.conversationId,
        t.sessionId,
      );
      return n == null ||
        (n.session.designChange == null && n.session.designEditorState == null)
        ? null
        : n;
    }
    async captureBrowserScreenshot(e, t) {
      try {
        let n =
            t.readyMessage.annotationViewportRect ?? t.annotationViewportRect,
          r = t.readyMessage.markerViewportPoint ?? t.markerViewportPoint,
          i =
            t.screenshotCropRect == null
              ? null
              : (zV(n, r) ?? t.screenshotCropRect);
        return VV(
          BV(await e.capturePage(), {
            annotation: t.annotation,
            screenshotCropRect: i,
            shouldUseCompactScreenshot: t.shouldUseCompactScreenshot,
          }),
          t.shouldUseCompactScreenshot,
          i,
          n,
          r,
        );
      } catch (e) {
        (xV().warning(`failed to capture browser comment screenshot`, {
          safe: {},
          sensitive: { error: e },
        }),
          this.errorReporter.reportNonFatal(
            e instanceof Error ? e : Error(String(e)),
            { kind: `browser-sidebar-comment-screenshot` },
          ));
        return;
      }
    }
    async captureSavedCommentScreenshot({
      annotationViewportRect: e,
      commentId: t,
      conversationId: n,
      markerViewportPoint: r,
      owner: i,
      page: a,
      runtimeComments: o,
      screenshotCropRect: s,
      shouldUseCompactScreenshot: c,
      threadState: l,
    }) {
      if (!(a == null || a.isDestroyed())) {
        (o != null && this.sendRuntimeStateToPage(a, l, o),
          a.send(B, {
            type: `browser-sidebar-runtime-prepare-comment-screenshot`,
            commentId: t,
          }));
        try {
          let o = await this.waitForRuntimeCommentScreenshotReady(a, t);
          if (o == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser comment screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              xV().warning(`timed out waiting for browser comment screenshot`, {
                safe: { commentId: t, conversationId: n },
              }));
            return;
          }
          if (o.skipScreenshotCapture === !0) return;
          let u = l.snapshot.comments.find((e) => e.id === t);
          if (u == null) return;
          let d = await this.captureBrowserScreenshot(a, {
            annotationViewportRect: e,
            annotation: u,
            markerViewportPoint: r,
            readyMessage: o,
            screenshotCropRect: s,
            shouldUseCompactScreenshot: c,
          });
          if (d == null) return;
          ((l.snapshot = {
            ...l.snapshot,
            comments: l.snapshot.comments.map((e) =>
              e.id === t
                ? {
                    ...e,
                    screenshot: d,
                    ...(e.designChange == null
                      ? {}
                      : { designChange: { ...e.designChange, screenshot: d } }),
                  }
                : e,
            ),
          }),
            this.compactThreadCommentScreenshotsIfNeeded(l),
            this.delegate.syncCommentSnapshot(i, n, l.browserTabId));
        } finally {
          a.isDestroyed() ||
            a.send(B, {
              type: `browser-sidebar-runtime-clear-comment-screenshot`,
            });
        }
      }
    }
    async captureSavedDesignChangeScreenshot({
      annotationViewportRect: e,
      conversationId: t,
      groupId: n,
      markerViewportPoint: r,
      owner: i,
      page: a,
      screenshotCropRect: o,
      shouldUseCompactScreenshot: s,
      threadState: c,
    }) {
      if (!(a == null || a.isDestroyed())) {
        a.send(B, {
          type: `browser-sidebar-runtime-prepare-comment-screenshot`,
          commentId: n,
        });
        try {
          let l = await this.waitForRuntimeCommentScreenshotReady(a, n);
          if (l == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser annotation screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              xV().warning(
                `timed out waiting for browser annotation screenshot`,
                { safe: { commentId: n, conversationId: t } },
              ));
            return;
          }
          let u = c.snapshot.comments.find(
            (e) => e.id === n || e.designChange?.id === n,
          );
          if (u?.designChange == null) return;
          let d = await this.captureBrowserScreenshot(a, {
            annotationViewportRect: e,
            annotation: u.designChange,
            markerViewportPoint: r,
            readyMessage: l,
            screenshotCropRect: o,
            shouldUseCompactScreenshot: s,
          });
          if (d == null) return;
          ((c.snapshot = {
            ...c.snapshot,
            comments: c.snapshot.comments.map((e) =>
              e.id === u.id
                ? {
                    ...e,
                    screenshot: d,
                    designChange:
                      e.designChange == null
                        ? void 0
                        : { ...e.designChange, screenshot: d },
                  }
                : e,
            ),
          }),
            this.delegate.syncCommentSnapshot(i, t, c.browserTabId));
        } finally {
          a.isDestroyed() ||
            a.send(B, {
              type: `browser-sidebar-runtime-clear-comment-screenshot`,
            });
        }
      }
    }
    async captureDirectCommentScreenshot({
      annotationViewportRect: e,
      comment: t,
      conversationId: n,
      markerViewportPoint: r,
      page: i,
      runtimeComments: a,
      screenshotCropRect: o,
      shouldUseCompactScreenshot: s,
      threadState: c,
    }) {
      if (!(i == null || i.isDestroyed())) {
        (this.sendRuntimeStateToPage(i, c, a),
          i.send(B, {
            type: `browser-sidebar-runtime-prepare-comment-screenshot`,
            commentId: t.id,
          }));
        try {
          let a = await this.waitForRuntimeCommentScreenshotReady(i, t.id);
          if (a == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser comment screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              xV().warning(`timed out waiting for browser comment screenshot`, {
                safe: { commentId: t.id, conversationId: n },
              }));
            return;
          }
          return a.skipScreenshotCapture === !0
            ? void 0
            : await this.captureBrowserScreenshot(i, {
                annotationViewportRect: e,
                annotation: t,
                markerViewportPoint: r,
                readyMessage: a,
                screenshotCropRect: o,
                shouldUseCompactScreenshot: s,
              });
        } finally {
          i.isDestroyed() ||
            i.send(B, {
              type: `browser-sidebar-runtime-clear-comment-screenshot`,
            });
        }
      }
    }
    async enrichDesignEditorProvenance(e, t) {
      if (e.isDestroyed()) return t;
      let n = e.debugger,
        r = n.isAttached();
      try {
        (r || n.attach(`1.3`),
          await Promise.all([
            n.sendCommand(`DOM.enable`),
            n.sendCommand(`CSS.enable`),
          ]));
        let e = kV.parse(
            await n.sendCommand(`DOM.getDocument`, { pierce: !0 }),
          ),
          i = AV.parse(
            await n.sendCommand(`DOM.querySelector`, {
              nodeId: e.root.nodeId,
              selector: `[${OV}~="${cH(t.draftAttribute)}"]`,
            }),
          );
        return i.nodeId === 0
          ? t
          : rH(
              t,
              FV.parse(
                await n.sendCommand(`CSS.getMatchedStylesForNode`, {
                  nodeId: i.nodeId,
                }),
              ),
            );
      } catch (e) {
        return (
          xV().warning(`failed to inspect browser design style provenance`, {
            safe: { groupId: t.id },
            sensitive: { error: e },
          }),
          t
        );
      } finally {
        if (!r && !e.isDestroyed() && n.isAttached())
          try {
            n.detach();
          } catch (e) {
            xV().warning(`failed to detach browser design debugger`, {
              safe: { groupId: t.id },
              sensitive: { error: e },
            });
          }
      }
    }
    syncSnapshotRuntimeStateToPage(e, t) {
      let r = n.Dc(t.snapshot.annotationFlow).persistent
        ? t.snapshot.comments
        : [];
      this.sendRuntimeStateToPage(e, t, r);
    }
    syncRuntimeStateToPage(e, t, r, i, a = i.snapshot.comments) {
      if (!n.Dc(i.snapshot.annotationFlow).persistent) {
        this.sendRuntimeStateToPage(r, i, []);
        return;
      }
      this.sendRuntimeStateToPage(
        r,
        i,
        this.getRuntimeCommentsForSync(e, t, i.browserTabId, a),
        this.getRuntimeActiveDesignChangeForSync(e, t, i.browserTabId),
      );
    }
    sendRuntimeStateToPage(e, t, r, i) {
      if (e == null || e.isDestroyed()) return;
      let a = this.delegate.canUseTweaks();
      e.send(B, {
        type: `browser-sidebar-runtime-sync`,
        interactionMode: t.snapshot.interactionMode,
        annotationEditorMode: a ? t.snapshot.annotationEditorMode : `comment`,
        isAgentControllingBrowser: t.isAgentControllingBrowser,
        canUseTweaks: a,
        isDesignModifierPressed: a && t.snapshot.isDesignModifierPressed === !0,
        isOriginalViewEnabled: a && t.snapshot.isOriginalViewEnabled === !0,
        isTweaksEditorOpen: a && t.snapshot.isTweaksEditorOpen === !0,
        ...(a && i != null ? { activeDesignChange: i } : {}),
        intlConfig: t.runtimeIntlConfig,
        comments: n.kc(r, a).map(n.Ec),
        viewportScale: t.viewportScale,
        zoomPercent: t.snapshot.zoomPercent,
      });
    }
    getRuntimeCommentsForSync(e, t, n, r) {
      if (t == null) return r;
      let i = this.overlayManager.getOverlayStateForOwner(e, t, n);
      if (i == null) return r;
      let a = i.session.target;
      if (a.mode !== `edit`) return r;
      let o = i.session.designEditorState?.id ?? i.session.designChange?.id;
      return o == null
        ? r
        : r.map((e) =>
            e.id !== a.commentId && e.designChange?.id !== o
              ? e
              : i.session.designChange == null
                ? JV(e)
                : { ...e, designChange: i.session.designChange },
          );
    }
    getRuntimeActiveDesignChangeForSync(e, t, n) {
      if (t == null) return;
      let r = this.overlayManager.getOverlayStateForOwner(e, t, n);
      if (
        !(
          r == null ||
          r.session.target.mode === `edit` ||
          r.session.designChange == null
        )
      )
        return r.session.designChange;
    }
    closeRuntimeEditor(e, t) {
      e == null ||
        e.isDestroyed() ||
        e.send(B, { type: `browser-sidebar-runtime-close-editor`, target: t });
    }
    setTweaksEditorOpen(e, t, n, r, i) {
      (r.snapshot.isTweaksEditorOpen ?? !1) !== i &&
        ((r.snapshot = { ...r.snapshot, isTweaksEditorOpen: i }),
        this.delegate.syncCommentSnapshot(e, t, n));
    }
    setRuntimeDesignScrubProperty(e, t) {
      e == null ||
        e.isDestroyed() ||
        e.send(B, {
          type: `browser-sidebar-runtime-design-scrub-changed`,
          property: t,
        });
    }
    setAnnotationEditorMode(e, t, n, r, i) {
      r.snapshot.annotationEditorMode !== i &&
        ((r.snapshot = { ...r.snapshot, annotationEditorMode: i }),
        this.delegate.syncCommentSnapshot(e, t, n));
    }
    waitForRuntimeCommentScreenshotReady(e, t) {
      return (
        this.runtimeCommentScreenshotWaiters.get(e.id)?.resolve(null),
        new Promise((n) => {
          let r = setTimeout(() => {
            this.runtimeCommentScreenshotWaiters.get(e.id)?.commentId === t &&
              (this.runtimeCommentScreenshotWaiters.delete(e.id), n(null));
          }, SV);
          this.runtimeCommentScreenshotWaiters.set(e.id, {
            commentId: t,
            resolve: (i) => {
              (clearTimeout(r),
                this.runtimeCommentScreenshotWaiters.get(e.id)?.commentId ===
                  t && this.runtimeCommentScreenshotWaiters.delete(e.id),
                n(i));
            },
          });
        })
      );
    }
  };
function LV(e) {
  let t = !1,
    n = e.map((e) => {
      if (e.screenshot == null || e.screenshot.isCompact === !0) return e;
      t = !0;
      let n =
          e.screenshot.annotationViewportRect ??
          e.screenshot.cropViewportRect ??
          e.anchor.rect,
        r = e.screenshot.markerViewportPoint ?? e.markerViewportPoint,
        i = zV(n, r) ?? e.anchor.rect;
      return {
        ...e,
        screenshot: VV(
          RV(c.nativeImage.createFromDataURL(e.screenshot.dataUrl), e, i),
          !0,
          i,
          e.screenshot.annotationViewportRect ?? e.anchor.rect,
          r,
        ),
      };
    });
  return t ? n : e;
}
function RV(e, t, n) {
  let r = t.screenshot;
  return r?.cropViewportRect == null
    ? BV(e, {
        annotation: t,
        screenshotCropRect: n,
        shouldUseCompactScreenshot: !0,
      })
    : WV(
        UV(e, r.cropViewportRect, n, t.viewportSize, r.cropPaddingPx ?? CV),
        DV,
      );
}
function zV(e, t) {
  let n =
    t == null
      ? null
      : { x: t.x - 26 / 2, y: t.y - 26 / 2, width: 26, height: 26 };
  if (e == null) return n;
  if (n == null) return e;
  let r = Math.min(e.x, n.x),
    i = Math.min(e.y, n.y),
    a = Math.max(e.x + e.width, n.x + n.width),
    o = Math.max(e.y + e.height, n.y + n.height);
  return { x: r, y: i, width: a - r, height: o - i };
}
function BV(
  e,
  { annotation: t, screenshotCropRect: n, shouldUseCompactScreenshot: r },
) {
  let i = t.viewportSize,
    a = n != null && i != null && i.width > 0 && i.height > 0;
  return WV(a ? HV(e, n, i, r ? wV : CV) : e, r ? DV : a ? EV : TV);
}
function VV(e, t, n, r, i) {
  let { width: a, height: o } = e.getSize();
  return {
    annotationViewportRect: r ?? void 0,
    ...(n == null ? {} : { cropPaddingPx: t ? wV : CV, cropViewportRect: n }),
    dataUrl: e.toDataURL(),
    ...(t ? { isCompact: !0 } : {}),
    markerViewportPoint: i,
    width: a,
    height: o,
  };
}
function HV(e, t, n, r) {
  let i = bV(e.getSize(), t, n, r);
  return i == null ? e : e.crop(i);
}
function UV(e, t, n, r, i) {
  if (r == null || r.width <= 0 || r.height <= 0) return e;
  let a = Math.max(0, t.x - i),
    o = Math.max(0, t.y - i),
    s = Math.min(r.width, t.x + t.width + i),
    c = Math.min(r.height, t.y + t.height + i),
    l = Math.max(0, n.x - wV),
    u = Math.max(0, n.y - wV),
    d = Math.min(r.width, n.x + n.width + wV),
    f = Math.min(r.height, n.y + n.height + wV),
    p = s - a,
    m = c - o;
  if (p <= 0 || m <= 0) return e;
  let h = e.getSize(),
    g = h.width / p,
    _ = h.height / m,
    v = Math.max(0, Math.floor((l - a) * g)),
    y = Math.max(0, Math.floor((u - o) * _)),
    b = Math.min(h.width, Math.ceil((d - a) * g)),
    x = Math.min(h.height, Math.ceil((f - o) * _)),
    S = b - v,
    C = x - y;
  return S <= 0 || C <= 0 ? e : e.crop({ x: v, y, width: S, height: C });
}
function WV(e, t) {
  let { width: n, height: r } = e.getSize(),
    i = Math.max(n, r);
  if (i <= t) return e;
  let a = t / i;
  return e.resize({
    width: Math.max(1, Math.round(n * a)),
    height: Math.max(1, Math.round(r * a)),
  });
}
function GV({
  anchorState: e,
  body: t,
  commentId: n,
  attachedImages: r,
  designChange: i,
  screenshot: a,
}) {
  return {
    id: n,
    createdAt: new Date().toISOString(),
    anchor: e.anchor,
    color: `blue`,
    ...(e.anchor.kind === `element`
      ? { markerViewportPoint: e.viewportPoint }
      : {}),
    attachedImages: r,
    ...(t == null || t.length === 0 ? {} : { body: t }),
    ...(i == null ? {} : { designChange: i }),
    ...(a == null ? {} : { screenshot: a }),
    themeVariant: e.themeVariant,
    viewportSize: e.viewportSize,
  };
}
function KV(e, t) {
  return {
    ...e,
    ...(t.length === 0 ? {} : { comment: t }),
    status: `queued`,
    updatedAt: new Date().toISOString(),
  };
}
function qV(e, t) {
  return e.some((e) => e.id === t.id)
    ? e.map((e) => (e.id === t.id ? t : e))
    : [...e, t];
}
function JV(e) {
  let { designChange: t, ...n } = e;
  return n;
}
function YV(e) {
  let { body: t, ...n } = e;
  return n;
}
function XV(e, ...t) {
  let n = new Map();
  for (let e of t)
    for (let t of e ?? []) {
      let e = n.get(t.property);
      (e == null || nH(t) >= nH(e)) && n.set(t.property, t);
    }
  return e.flatMap((e) => {
    let t = n.get(e.property);
    return t == null ? [] : [t];
  });
}
function ZV(e, t) {
  return XV(e, t).length === e.length && t.every((e) => nH(e) > 0);
}
function QV(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          e.property === r.property &&
          e.selectorText === r.selectorText &&
          e.sourceUrl === r.sourceUrl &&
          e.sourceLine === r.sourceLine &&
          e.sourceColumn === r.sourceColumn
        );
      })
    : !1;
}
function $V(e, t) {
  return e == null
    ? !1
    : e.id === t.id &&
        e.createdAt === t.createdAt &&
        e.status === t.status &&
        e.draftAttribute === t.draftAttribute &&
        e.selector === t.selector &&
        e.targetLabel === t.targetLabel &&
        n.wc(e.anchor, t.anchor) &&
        e.markerViewportPoint?.x === t.markerViewportPoint?.x &&
        e.markerViewportPoint?.y === t.markerViewportPoint?.y &&
        e.viewportSize?.width === t.viewportSize?.width &&
        e.viewportSize?.height === t.viewportSize?.height &&
        eH(e.declarations, t.declarations) &&
        tH(e.text, t.text);
}
function eH(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          e.property === r.property &&
          e.previousValue === r.previousValue &&
          e.value === r.value
        );
      })
    : !1;
}
function tH(e, t) {
  return e == null || t == null
    ? e == null && t == null
    : e.previousValue === t.previousValue && e.value === t.value;
}
function nH(e) {
  let t = 0;
  return (
    e.selectorText != null && (t += 1),
    e.sourceUrl != null && (t += 1),
    e.sourceLine != null && (t += 1),
    e.sourceColumn != null && (t += 1),
    t
  );
}
function rH(e, t) {
  let n = e.declarations.map((e) => iH(e, t));
  return { ...e, provenance: n };
}
function iH(e, t) {
  let n = aH(e.property, t.inlineStyle?.cssProperties);
  if (n != null)
    return {
      property: e.property,
      selectorText: `element.style`,
      ...(n.range == null
        ? {}
        : {
            sourceLine: n.range.startLine + 1,
            sourceColumn: n.range.startColumn + 1,
          }),
    };
  let r = { property: e.property };
  for (let n of t.matchedCSSRules ?? []) {
    let t = aH(e.property, n.rule.style?.cssProperties);
    t != null &&
      (r = {
        property: e.property,
        selectorText: n.rule.selectorList?.text,
        sourceUrl: sH(n),
        ...(t.range == null
          ? {}
          : {
              sourceLine: t.range.startLine + 1,
              sourceColumn: t.range.startColumn + 1,
            }),
      });
  }
  return r;
}
function aH(e, t) {
  return t?.findLast(
    (t) => oH(e).includes(t.name) && t.disabled !== !0 && t.implicit !== !0,
  );
}
function oH(e) {
  switch (e) {
    case `padding-top`:
    case `padding-right`:
    case `padding-bottom`:
    case `padding-left`:
      return [e, `padding`];
    case `margin-top`:
    case `margin-right`:
    case `margin-bottom`:
    case `margin-left`:
      return [e, `margin`];
    case `row-gap`:
    case `column-gap`:
      return [e, `gap`];
    default:
      return [e];
  }
}
function sH(e) {
  return (
    e.rule.media?.findLast((e) => e.sourceURL != null)?.sourceURL ??
    e.rule.containerQueries?.findLast((e) => e.sourceURL != null)?.sourceURL ??
    e.rule.supports?.findLast((e) => e.sourceURL != null)?.sourceURL
  );
}
function cH(e) {
  return e.replace(/\\/g, `\\\\`).replace(/"/g, `\\"`);
}
function lH(e, t) {
  let n = uH(e),
    r = uH(t);
  if (n == null || r == null) return e === t;
  if (n.type !== r.type) return !1;
  switch (n.type) {
    case `http`:
      return (
        r.type === `http` &&
        n.origin === r.origin &&
        n.pathname === r.pathname &&
        n.search === r.search
      );
    case `file`:
      return (
        r.type === `file` && n.pathname === r.pathname && n.search === r.search
      );
    case `other`:
      return r.type === `other` && n.value === r.value;
  }
}
function uH(e) {
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`
      ? {
          type: `http`,
          origin: t.origin,
          pathname: t.pathname,
          search: t.search,
        }
      : t.protocol === `file:`
        ? { type: `file`, pathname: t.pathname, search: t.search }
        : { type: `other`, value: t.toString() };
  } catch {
    return null;
  }
}
function dH({
  blocklistLookup: e,
  comments: t,
  sendPendingCommentModeRequestToRuntime: n,
  syncRuntimeStateToPage: r,
  syncThreadState: i,
  threadState: a,
  windowState: o,
}) {
  let s = (e) => {
      (!e.changed && e.request == null) ||
        (e.changed &&
          (i(o, a.conversationId, a),
          e.shouldSyncRuntime && a.page != null && r(a.page, a)),
        e.shouldPrepareComments &&
          t.prepare(o.owner, a.conversationId, a.browserTabId, a),
        e.request != null && a.page != null && n(a.page, e.request));
    },
    c = (e) => {
      let n = !1,
        r = null,
        i = !1,
        s = !1,
        c = e ? `site-blocked` : null;
      if (
        (a.snapshot.commentModeDisabledReason !== c &&
          ((a.snapshot = { ...a.snapshot, commentModeDisabledReason: c }),
          (n = !0)),
        e)
      )
        return (
          a.pendingCommentModeActivation != null &&
            (a.pendingCommentModeActivation = null),
          a.snapshot.interactionMode === `comment` &&
            ((a.snapshot = PB(a.snapshot, `browse`)),
            t.dismiss(o.owner, a.conversationId, a.browserTabId, a),
            (n = !0),
            (s = !0)),
          {
            changed: n,
            request: r,
            shouldPrepareComments: i,
            shouldSyncRuntime: s,
          }
        );
      if (a.pendingCommentModeActivation != null) {
        let e = a.pendingCommentModeActivation;
        ((r = e.request),
          (a.pendingCommentModeActivation = null),
          (a.snapshot.interactionMode !== `comment` ||
            a.snapshot.annotationFlow !== e.annotationFlow) &&
            ((a.snapshot = {
              ...PB(a.snapshot, `comment`),
              annotationFlow: e.annotationFlow,
              annotationModeEntrySource: e.annotationModeEntrySource,
            }),
            (n = !0),
            (i = !0),
            (s = !0)));
      }
      return {
        changed: n,
        request: r,
        shouldPrepareComments: i,
        shouldSyncRuntime: s,
      };
    },
    l = (e, t, n) => {
      a.commentModeBlockCheckRequestId === e &&
        ((a.commentModeBlockCheckUrl = null), WL(a) === t && s(c(n)));
    },
    u = WL(a);
  a.pendingCommentModeActivation != null &&
    (a.pendingCommentModeActivation.url !== u ||
      a.pendingCommentModeActivation.navigationVersion !==
        (a.page?.navigationVersion ?? null)) &&
    (a.pendingCommentModeActivation = null);
  let d = e.cachedCommentModeBlocked(u);
  if (d != null) {
    s(c(d));
    return;
  }
  if (a.commentModeBlockCheckUrl === u) return;
  (a.snapshot.commentModeDisabledReason != null &&
    a.snapshot.url !== u &&
    (a.snapshot = { ...a.snapshot, commentModeDisabledReason: null }),
    (a.commentModeBlockCheckUrl = u));
  let f = ++a.commentModeBlockCheckRequestId;
  e.isCommentModeBlocked(u).then((e) => {
    l(f, u, e);
  });
}
var fH = 200;
function pH({
  browserSessionRegistry: e,
  downloadStore: t,
  event: n,
  findPageStateForWebContentsId: r,
  item: i,
  productDownloadsEnabled: a,
  webContents: o,
}) {
  let s = o?.id ?? null,
    c = s == null ? null : r(s),
    l = i.getURL(),
    u = i.getURLChain(),
    d =
      s == null
        ? null
        : e.consumeAllowedBrowserUseDownload({
            url: l,
            urlChain: u,
            webContentsId: s,
          }),
    f = c?.threadState.isAgentControllingBrowser === !0;
  if (d == null && (c == null || f)) {
    (n.preventDefault(), i.cancel());
    return;
  }
  if (d == null && !a) return;
  let p = t.start({
      browserTabId: c?.threadState.browserTabId ?? null,
      conversationId: c?.threadState.conversationId ?? null,
      item: i,
      source: d == null ? `user` : `agent`,
      url: l,
    }),
    m = (t) => {
      d != null &&
        e.notifyBrowserUseDownloadChange({
          filename: p.path,
          id: p.id,
          session_id: d.sessionId,
          status: t,
          url: d.url,
        });
    },
    h = (e, n = e) => {
      (t.update(p, i, n), m(e));
    },
    g = !1,
    _ = (e) => {
      g || ((g = !0), t.finish(p, i, e), m(e));
    };
  (m(`started`),
    i.on(`updated`, (e, t) => {
      if (t === `interrupted`) {
        i.canResume() ? h(`in_progress`, `paused`) : _(`failed`);
        return;
      }
      h(`in_progress`, i.isPaused() ? `paused` : `in_progress`);
    }),
    i.once(`done`, (e, t) => {
      switch (t) {
        case `completed`:
          _(`complete`);
          return;
        case `cancelled`:
          _(`canceled`);
          return;
        case `interrupted`:
          _(`failed`);
          return;
      }
    }));
}
var mH = class {
  activeSavePaths = new Set();
  clearPersistedDownloadHistory;
  downloads = new Map();
  getDownloadDirectory;
  getPromptForUserDownloads;
  persistDownloads;
  productDownloadsEnabled;
  onActiveDownloadChange = null;
  onChange = null;
  constructor(e = {}) {
    ((this.clearPersistedDownloadHistory =
      e.clearPersistedDownloadHistory ?? null),
      (this.productDownloadsEnabled = e.productDownloadsEnabled ?? !1),
      (this.getDownloadDirectory =
        e.getDownloadDirectory ?? (() => c.app.getPath(`downloads`))),
      (this.getPromptForUserDownloads =
        e.getPromptForUserDownloads ?? (() => !1)),
      (this.persistDownloads = e.persistDownloads ?? null));
    for (let t of (this.productDownloadsEnabled
      ? e.persistedDownloads
      : void 0
    )?.slice(0, fH) ?? [])
      this.downloads.set(t.id, {
        browserTabId: null,
        conversationId: t.conversationId,
        filename: t.filename,
        id: t.id,
        item: null,
        path: t.path,
        receivedBytes: t.receivedBytes,
        source: t.source,
        startedAtMs: t.startedAtMs,
        status: hH(t.status) ? t.status : `failed`,
        totalBytes: t.totalBytes,
        updatedAtMs: t.updatedAtMs,
        url: t.url,
      });
  }
  getSnapshot() {
    return {
      capturedAtMs: Date.now(),
      downloads: Array.from(this.downloads.values(), (e) =>
        this.getItem(e),
      ).sort((e, t) => t.startedAtMs - e.startedAtMs),
    };
  }
  hasActiveDownloadForRoute(e) {
    for (let t of this.downloads.values())
      if (
        t.item != null &&
        t.browserTabId === e.browserTabId &&
        t.conversationId === e.conversationId
      )
        return !0;
    return !1;
  }
  registerSession({
    browserSession: e,
    browserSessionRegistry: t,
    findPageStateForWebContentsId: n,
  }) {
    e.on(`will-download`, (e, r, i) => {
      pH({
        browserSessionRegistry: t,
        downloadStore: this,
        event: e,
        findPageStateForWebContentsId: n,
        item: r,
        productDownloadsEnabled: this.productDownloadsEnabled,
        webContents: i,
      });
    });
  }
  cancel({ id: e }) {
    let t = this.downloads.get(e);
    return t == null
      ? gH()
      : t.item == null || hH(t.status)
        ? { ok: !1, reason: `download-not-cancelable` }
        : (t.item.cancel(), this.finish(t, t.item, `canceled`), { ok: !0 });
  }
  async clearHistory() {
    for (let [e, t] of this.downloads) hH(t.status) && this.downloads.delete(e);
    (await this.clearPersistedDownloadHistory?.(), this.notifyChanged(!1));
  }
  finish(e, t, n) {
    e.item == null ||
      hH(e.status) ||
      (this.update(e, t, n, !0),
      this.activeSavePaths.delete(e.path),
      (e.item = null),
      this.onActiveDownloadChange?.());
  }
  open({ id: e }) {
    let t = this.downloads.get(e);
    return t == null ? Promise.resolve(gH()) : _H(t.path);
  }
  pause({ id: e }) {
    let t = this.downloads.get(e);
    return t == null
      ? gH()
      : t.item == null || t.item.isPaused() || hH(t.status)
        ? { ok: !1, reason: `download-not-pausable` }
        : (t.item.pause(), this.update(t, t.item, `paused`, !0), { ok: !0 });
  }
  remove({ id: e }) {
    let t = this.downloads.get(e);
    if (t == null) return gH();
    let n = hH(t.status) ? null : t.item;
    return (
      n != null && (n.cancel(), this.activeSavePaths.delete(t.path)),
      this.downloads.delete(e),
      this.notifyChanged(!0),
      n != null && this.onActiveDownloadChange?.(),
      { ok: !0 }
    );
  }
  reassignRoute(e, t) {
    let n = !1;
    for (let r of this.downloads.values())
      r.browserTabId !== e.browserTabId ||
        r.conversationId !== e.conversationId ||
        ((r.browserTabId = t.browserTabId),
        (r.conversationId = t.conversationId),
        (n = !0));
    n && (this.notifyChanged(!0), this.onActiveDownloadChange?.());
  }
  resume({ id: e }) {
    let t = this.downloads.get(e);
    return t == null
      ? gH()
      : t.item == null || !t.item.isPaused() || hH(t.status)
        ? { ok: !1, reason: `download-not-resumable` }
        : (t.item.resume(),
          this.update(t, t.item, `in_progress`, !0),
          { ok: !0 });
  }
  setChangeListener(e) {
    this.onChange = e;
  }
  setActiveDownloadChangeListener(e) {
    this.onActiveDownloadChange = e;
  }
  showDownloadsFolder() {
    return _H(this.getDownloadDirectory());
  }
  showInFolder({ id: e }) {
    let t = this.downloads.get(e);
    if (t == null) return gH();
    try {
      return (c.shell.showItemInFolder(t.path), { ok: !0 });
    } catch (e) {
      return { message: n.Rs(e), ok: !1, reason: `show-in-folder-failed` };
    }
  }
  start({ browserTabId: e, conversationId: t, item: n, source: r, url: i }) {
    let a = crypto.randomUUID(),
      o = this.reserveSavePath(n.getFilename());
    r === `user` && this.getPromptForUserDownloads()
      ? n.setSaveDialogOptions({ defaultPath: o })
      : n.setSavePath(o);
    let s = Date.now(),
      c = {
        browserTabId: e,
        conversationId: t,
        filename: n.getFilename(),
        id: a,
        item: n,
        path: o,
        receivedBytes: n.getReceivedBytes(),
        source: r,
        startedAtMs: s,
        status: `started`,
        totalBytes: n.getTotalBytes(),
        updatedAtMs: s,
        url: i,
      };
    return (
      this.downloads.set(a, c),
      this.onActiveDownloadChange?.(),
      this.notifyChanged(!0),
      c
    );
  }
  update(e, t, n, r = !1) {
    e.filename = t.getFilename();
    let i = t.getSavePath(),
      a = !1;
    (i &&
      i !== e.path &&
      (this.activeSavePaths.delete(e.path),
      this.activeSavePaths.add(i),
      (e.path = i),
      (a = !0)),
      (e.receivedBytes = t.getReceivedBytes()),
      (e.status = n),
      (e.totalBytes = t.getTotalBytes()),
      (e.updatedAtMs = Date.now()),
      this.notifyChanged(r || a));
  }
  getItem(e) {
    let t = hH(e.status) ? null : e.item,
      n = t?.isPaused() === !0 || e.status === `paused`;
    return {
      canCancel: t != null,
      canPause: t != null && !n,
      canResume: n && t?.canResume() === !0,
      conversationId: e.conversationId,
      exists: (0, p.existsSync)(e.path),
      filename: e.filename,
      id: e.id,
      path: e.path,
      receivedBytes: e.receivedBytes,
      source: e.source,
      startedAtMs: e.startedAtMs,
      status: n ? `paused` : e.status,
      totalBytes: e.totalBytes,
      updatedAtMs: e.updatedAtMs,
      url: e.url,
    };
  }
  reserveSavePath(e) {
    let t = this.getDownloadDirectory(),
      n = (0, u.basename)(e.trim()),
      r = n === `.` || n === `..` || !n ? `download` : n,
      i = (0, u.parse)(r);
    for (let e = 0; ; e += 1) {
      let n = (0, u.join)(t, e === 0 ? r : `${i.name} (${e})${i.ext}`);
      if (!((0, p.existsSync)(n) || this.activeSavePaths.has(n)))
        return (this.activeSavePaths.add(n), n);
    }
  }
  notifyChanged(e) {
    if (e) {
      for (; this.downloads.size > fH; ) {
        let e = Array.from(this.downloads.values())
          .filter((e) => hH(e.status))
          .sort((e, t) => e.startedAtMs - t.startedAtMs)[0];
        if (e == null) break;
        this.downloads.delete(e.id);
      }
      this.productDownloadsEnabled &&
        this.persistDownloads?.(this.getSnapshot().downloads);
    }
    this.productDownloadsEnabled && this.onChange?.();
  }
};
function hH(e) {
  return e === `canceled` || e === `complete` || e === `failed`;
}
function gH() {
  return { ok: !1, reason: `missing-download` };
}
async function _H(e) {
  let t = await c.shell.openPath(e);
  return t ? { message: t, ok: !1, reason: `open-failed` } : { ok: !0 };
}
var vH = e.n((e, t) => {
    t.exports = n.Wt()({
      "&": `&amp;`,
      "<": `&lt;`,
      ">": `&gt;`,
      '"': `&quot;`,
      "'": `&#39;`,
    });
  }),
  yH = e.n((e, t) => {
    var r = vH(),
      i = n.wn(),
      a = /[&<>"']/g,
      o = RegExp(a.source);
    function s(e) {
      return ((e = i(e)), e && o.test(e) ? e.replace(a, r) : e);
    }
    t.exports = s;
  }),
  bH = e.o(yH()),
  xH = `browserSidebar.contextMenu.inspect`,
  SH = `browserSidebar.contextMenu.back`,
  CH = `browserSidebar.contextMenu.forward`,
  wH = `browserSidebar.contextMenu.reload`,
  TH = `browserSidebar.contextMenu.openInBrowser`,
  EH = `browserSidebar.contextMenu.openLinkInNewTab`,
  DH = `browserSidebar.contextMenu.openExternalBrowser`,
  OH = `browserSidebar.contextMenu.copyLink`,
  kH = `browserSidebar.contextMenu.commentWithCodex`,
  AH = `browserSidebar.contextMenu.quickAnnotate`,
  jH = `browserSidebar.contextMenu.attachImage`,
  MH = `browserSidebar.loadError.heading`,
  NH = `This site can't be reached`,
  PH = `browserSidebar.loadError.try`,
  FH = `Try:`,
  IH = `browserSidebar.loadError.checkConnection`,
  LH = `Checking the connection`,
  RH = `browserSidebar.loadError.checkProxyFirewallDns`,
  zH = `Checking the proxy, firewall, and DNS configuration`,
  BH = `browserSidebar.loadError.reload`,
  VH = `Reload`,
  HH = `browserSidebar.crashError.heading`,
  UH = `This page crashed`,
  WH = `browserSidebar.crashError.summary`,
  GH = `{host} crashed unexpectedly`,
  KH = `browserSidebar.crashError.openExternalBrowser`,
  qH = `Open in external browser`,
  JH = `browserSidebar.loadError.dnsSummary`,
  YH = `{host}'s server IP address could not be found`,
  XH = `browserSidebar.loadError.offlineSummary`,
  ZH = `{host} could not be loaded because the computer is offline`,
  QH = `browserSidebar.loadError.refusedSummary`,
  $H = `{host} refused to connect`,
  eU = `browserSidebar.loadError.timeoutSummary`,
  tU = `{host} took too long to respond`,
  nU = `browserSidebar.loadError.certificateSummary`,
  rU = `{host}'s certificate could not be verified`,
  iU = `browserSidebar.loadError.genericSummary`,
  aU = `{host} could not be loaded`,
  oU = `browserSidebar.loadError.internetHeader`,
  sU = `Check your Internet connection`,
  cU = `browserSidebar.loadError.internetBody`,
  lU = `Check any cables and restart any routers, modems, or other network devices you may be using`,
  uU = `browserSidebar.loadError.dnsHeader`,
  dU = `Check your DNS settings`,
  fU = `browserSidebar.loadError.dnsBody`,
  pU = `Contact your network administrator if you are not sure what this means`,
  mU = `browserSidebar.loadError.networkAccessHeader`,
  hU = `Allow {appName} to access the network in your firewall or security settings`,
  gU = `browserSidebar.loadError.networkAccessBody`,
  _U = `If {appName} is already listed as an allowed app, try removing it from the list and adding it again`,
  vU = `browserSidebar.loadError.proxyHeader`,
  yU = `If you use a proxy server`,
  bU = `browserSidebar.loadError.proxyBody`,
  xU = `Open your system network settings and check whether a proxy has been configured for the active network`,
  SU = `data:text/html;charset=utf-8,`,
  CU = `ERR_PROCESS_CRASHED`,
  wU = `codex-browser-sidebar://open-external`,
  TU = `data:image/png;base64,`,
  EU = `data:image/png;base64,`;
function DU({ content: e, errorCode: t, errorDescription: n, failedUrl: r }) {
  let i = HU(n);
  return {
    errorCode: t,
    errorDescription: i,
    failedUrl: r,
    pageUrl:
      SU +
      encodeURIComponent(
        IU({ errorCodeLabel: VU(t, i), content: e, failedUrl: r }),
      ),
    title: e.heading,
  };
}
function OU({
  errorDescription: e,
  failedUrl: t,
  formatMessage: n,
  runtimeIntlConfig: r,
}) {
  let i = HU(e),
    o = LU(t),
    s = zU(r),
    c = a.W();
  return {
    alignActionsEnd: !1,
    details: [
      {
        header: n({ messageId: oU, defaultMessage: sU }),
        body: n({ messageId: cU, defaultMessage: lU }),
      },
      {
        header: n({ messageId: uU, defaultMessage: dU }),
        body: n({ messageId: fU, defaultMessage: pU }),
      },
      {
        header: n({
          messageId: mU,
          defaultMessage: hU,
          values: { appName: c },
        }),
        body: n({ messageId: gU, defaultMessage: _U, values: { appName: c } }),
      },
      {
        header: n({ messageId: vU, defaultMessage: yU }),
        body: n({ messageId: bU, defaultMessage: xU }),
      },
    ],
    externalBrowserButton: null,
    heading: n({ messageId: MH, defaultMessage: NH }),
    language: s,
    reloadButton: n({ messageId: BH, defaultMessage: VH }),
    showErrorCode: !0,
    suggestions: {
      checkConnection: n({ messageId: IH, defaultMessage: LH }),
      detailsLinkLabel: n({ messageId: RH, defaultMessage: zH }),
      tryLabel: n({ messageId: PH, defaultMessage: FH }),
    },
    summary: n({ ...RU(i), values: { host: o } }),
    textDirection: BU(s),
  };
}
function kU({ failedUrl: e, formatMessage: t, runtimeIntlConfig: r }) {
  let i = LU(e),
    a = zU(r);
  return DU({
    content: {
      alignActionsEnd: !0,
      details: [],
      externalBrowserButton: n.hs(e)
        ? {
            actionUrl: `${wU}?url=${encodeURIComponent(e)}`,
            label: t({ messageId: KH, defaultMessage: qH }),
          }
        : null,
      heading: t({ messageId: HH, defaultMessage: UH }),
      language: a,
      reloadButton: t({ messageId: BH, defaultMessage: VH }),
      showErrorCode: !1,
      suggestions: null,
      summary: t({ messageId: WH, defaultMessage: GH, values: { host: i } }),
      textDirection: BU(a),
    },
    errorCode: 0,
    errorDescription: CU,
    failedUrl: e,
  });
}
function AU({ errorCode: e, errorDescription: t, isMainFrame: n }) {
  let r = HU(t);
  return !n || e === -3 || r === `ERR_ABORTED` || r === `ERR_BLOCKED_BY_CLIENT`;
}
function jU(e) {
  return e instanceof Error && /ERR_ABORTED|\(-3\) loading '/.test(e.message);
}
function MU(e) {
  return (
    e instanceof Error && e.message.includes(`navigation is already pending`)
  );
}
function NU(e, t) {
  if (e == null) return !1;
  if (t === e.pageUrl) return !0;
  let n = PU(t),
    r = PU(e.pageUrl);
  return n != null && r != null && n === r;
}
function PU(e) {
  let [t] = e.split(`#`, 1);
  if (!t.startsWith(SU)) return null;
  let n = t.slice(29);
  try {
    return decodeURIComponent(n);
  } catch {
    return n;
  }
}
function FU(e) {
  try {
    let t = new URL(e);
    return t.protocol !== `codex-browser-sidebar:` ||
      t.hostname !== `open-external`
      ? null
      : t.searchParams.get(`url`);
  } catch {
    return null;
  }
}
function IU({ content: e, errorCodeLabel: t, failedUrl: n }) {
  let r = e.details.map(
      (e) => `      <div class="suggestions">
        <div class="suggestion-header">${(0, bH.default)(e.header)}</div>
        <div class="suggestion-body">${(0, bH.default)(e.body)}</div>
      </div>`,
    ).join(`
`),
    i =
      e.suggestions?.detailsLinkLabel == null
        ? ``
        : `            <li><a id="details-link" href="#details" aria-expanded="false">${(0, bH.default)(e.suggestions.detailsLinkLabel)}</a></li>`,
    a =
      e.externalBrowserButton == null
        ? ``
        : `      <button id="open-in-browser-button" type="button">${(0, bH.default)(e.externalBrowserButton.label)}</button>`,
    o = e.alignActionsEnd ? `nav-wrapper nav-wrapper-end` : `nav-wrapper`,
    s = e.showErrorCode
      ? `        <div class="error-code">${(0, bH.default)(t)}</div>`
      : ``,
    c =
      e.suggestions == null
        ? ``
        : `        <div id="suggestions-list">
          <p>${(0, bH.default)(e.suggestions.tryLabel)}</p>
          <ul>
            <li>${(0, bH.default)(e.suggestions.checkConnection)}</li>
${i}
          </ul>
        </div>`;
  return `<!DOCTYPE html>
<html dir="${(0, bH.default)(e.textDirection)}" lang="${(0, bH.default)(e.language)}">
<head>
  <meta charset="utf-8">
  <meta name="color-scheme" content="light dark">
  <meta name="theme-color" content="#fff">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>${(0, bH.default)(e.heading)}</title>
  <style>
    body {
      --background-color: #fff;
      --error-code-color: rgb(95, 99, 104);
      --google-blue-300: rgb(138, 180, 248);
      --google-gray-300: rgb(218, 220, 224);
      --google-gray-500: rgb(154, 160, 166);
      --google-gray-700: rgb(95, 99, 104);
      --google-gray-900: rgb(32, 33, 36);
      --heading-color: var(--google-gray-900);
      --link-color: rgb(88, 88, 88);
      --button-fill-color: rgb(26 28 31 / 5%);
      --button-fill-color-active: rgb(26 28 31 / 10%);
      --button-text-color: #1a1c1f;
      --text-color: var(--google-gray-700);
      background: var(--background-color);
      color: var(--text-color);
      font-family: system-ui, sans-serif;
      font-size: 70%;
      margin: 0;
      overflow-wrap: break-word;
    }

    @media (prefers-color-scheme: dark) {
      body {
        --background-color: var(--google-gray-900);
        --error-code-color: var(--google-gray-500);
        --heading-color: var(--google-gray-500);
        --link-color: var(--google-blue-300);
        --button-fill-color: rgb(255 255 255 / 5%);
        --button-fill-color-active: rgb(255 255 255 / 10%);
        --button-text-color: #fff;
        --text-color: var(--google-gray-500);
      }
    }

    html {
      -webkit-text-size-adjust: 100%;
      font-size: 125%;
    }

    a {
      color: var(--link-color);
      text-decoration: none;
    }

    button {
      align-items: center;
      background: var(--button-fill-color);
      border: 1px solid transparent;
      border-radius: 8px;
      box-sizing: border-box;
      color: var(--button-text-color);
      cursor: pointer;
      display: inline-flex;
      font-family: inherit;
      font-size: 13px;
      font-weight: 500;
      justify-content: center;
      line-height: 18px;
      margin: 0;
      min-height: 28px;
      padding: 0 8px;
      user-select: none;
      white-space: nowrap;
    }

    button:active {
      background: var(--button-fill-color-active);
      outline: 0;
    }

    button:hover {
      background: var(--button-fill-color-active);
      outline: 0;
    }

    .interstitial-wrapper {
      box-sizing: border-box;
      font-size: 1em;
      line-height: 1.6em;
      margin: 20vh auto 0;
      max-width: 600px;
      width: 100%;
    }

    .icon {
      -webkit-user-select: none;
      animation: icon-spin 3s linear infinite;
      background-image: image-set(url("${TU}") 1x, url("${EU}") 2x);
      background-repeat: no-repeat;
      background-size: 26px 28px;
      display: inline-block;
      height: 28px;
      margin: 0 0 24px;
      transform-origin: 50% 50%;
      width: 26px;
    }

    @keyframes icon-spin {
      100% {
        transform: rotate(360deg);
      }
    }

    h1 {
      color: var(--heading-color);
      font-size: 1.2em;
      font-weight: 500;
      line-height: 1.2em;
      margin: 0 0 12px;
    }

    #main-message > p {
      display: inline;
      font-size: 1em;
    }

    #suggestions-list {
      margin-top: 18px;
    }

    #suggestions-list p {
      margin-block-end: 0;
    }

    #suggestions-list ul {
      margin-top: 0;
    }

    .error-code {
      color: var(--error-code-color);
      font-size: .8em;
      margin-top: 24px;
      text-transform: uppercase;
    }

    .nav-wrapper {
      display: flex;
      gap: 8px;
      justify-content: flex-start;
      margin-top: 51px;
    }

    [dir="rtl"] .nav-wrapper {
      justify-content: flex-end;
    }

    .nav-wrapper-end {
      justify-content: flex-end;
    }

    #details {
      display: none;
      margin: 0 0 50px;
    }

    #main-frame-error.showing-details #details {
      display: block;
    }

    .suggestions {
      margin-top: 18px;
    }

    .suggestion-header {
      font-weight: bold;
      margin-bottom: 4px;
    }

    .suggestion-body {
      color: #777;
    }

    @media (prefers-color-scheme: dark) {
      .icon {
        filter: brightness(1.2);
      }

      .suggestion-body {
        color: var(--text-color);
      }
    }

    @media (max-width: 700px) {
      .interstitial-wrapper {
        padding: 0 10%;
      }
    }

    @media (max-width: 420px) {
      .interstitial-wrapper {
        margin: 7vh auto 12px;
        padding: 0 24px;
      }

      .nav-wrapper {
        margin-top: 30px;
      }

      #details {
        margin: 20px 0;
      }

      h1 {
        font-size: 1.5em;
        margin-bottom: 8px;
      }
    }
  </style>
</head>
<body class="neterror">
  <div id="main-frame-error" class="interstitial-wrapper">
    <div id="main-content">
      <div class="icon" aria-hidden="true"></div>
      <div id="main-message">
        <h1>${(0, bH.default)(e.heading)}</h1>
        <p>${(0, bH.default)(e.summary)}</p>
${c}
${s}
      </div>
    </div>
    <div id="buttons" class="${o}">
${a}
      <button id="reload-button" type="button">${(0, bH.default)(e.reloadButton)}</button>
    </div>
    <div id="details">
${r}
    </div>
  </div>
  <script>
    (() => {
      const failedUrl = ${UU(n)};
      const externalBrowserActionUrl = ${UU(e.externalBrowserButton?.actionUrl ?? ``)};
      const main = document.getElementById("main-frame-error");
      const details = document.getElementById("details");
      const detailsLink = document.getElementById("details-link");
      const openInBrowserButton = document.getElementById("open-in-browser-button");
      const reloadButton = document.getElementById("reload-button");
      const setDetailsVisible = (visible) => {
        main.classList.toggle("showing-details", visible);
        detailsLink?.setAttribute("aria-expanded", String(visible));
        if (visible) {
          details.setAttribute("tabindex", "-1");
          details.focus();
        }
      };
      detailsLink?.addEventListener("click", (event) => {
        event.preventDefault();
        setDetailsVisible(!main.classList.contains("showing-details"));
      });
      reloadButton.addEventListener("click", () => {
        window.location.href = failedUrl;
      });
      openInBrowserButton?.addEventListener("click", () => {
        window.location.href = externalBrowserActionUrl;
      });
    })();
  <\/script>
</body>
</html>`;
}
function LU(e) {
  try {
    let t = new URL(e);
    return t.hostname || t.pathname || e;
  } catch {
    return e;
  }
}
function RU(e) {
  switch (e) {
    case `DNS_PROBE_POSSIBLE`:
    case `ERR_NAME_NOT_RESOLVED`:
      return { messageId: JH, defaultMessage: YH };
    case `ERR_INTERNET_DISCONNECTED`:
      return { messageId: XH, defaultMessage: ZH };
    case `ERR_CONNECTION_REFUSED`:
      return { messageId: QH, defaultMessage: $H };
    case `ERR_CONNECTION_TIMED_OUT`:
    case `ERR_TIMED_OUT`:
      return { messageId: eU, defaultMessage: tU };
    default:
      return e.startsWith(`ERR_CERT_`)
        ? { messageId: nU, defaultMessage: rU }
        : { messageId: iU, defaultMessage: aU };
  }
}
function zU(e) {
  return e.locale || e.defaultLocale || `en-US`;
}
function BU(e) {
  return Reflect.get(new Intl.Locale(e.replace(/_/g, `-`)), `textInfo`)
    ?.direction === `rtl`
    ? `rtl`
    : `ltr`;
}
function VU(e, t) {
  return t.length > 0 ? t : e === 0 ? `ERR_FAILED` : `ERR_FAILED (` + e + `)`;
}
function HU(e) {
  return e.trim().toUpperCase();
}
function UU(e) {
  return JSON.stringify(e)
    .replace(/</g, `\\u003C`)
    .replace(/>/g, `\\u003E`)
    .replace(/&/g, `\\u0026`)
    .replace(/\u2028/g, `\\u2028`)
    .replace(/\u2029/g, `\\u2029`);
}
function WU() {
  return { activeMatchOrdinal: 0, matches: 0, query: ``, requestId: null };
}
function GU(e, t, n, r) {
  (e.sendMessageToWebContents(t, {
    type: `browser-sidebar-command`,
    conversationId: n,
    browserTabId: r.browserTabId,
    command: { type: `open-find` },
  }),
    XU(e, t, n, r));
}
function KU(e, t, n, r, i) {
  if (i.length === 0) {
    ZU(e, t, n, r);
    return;
  }
  if (r.findState.query === i) return;
  ((r.findState = {
    ...r.findState,
    activeMatchOrdinal: 0,
    matches: 0,
    query: i,
    requestId: null,
  }),
    XU(e, t, n, r));
  let a = r.page?.view.webContents;
  a == null || a.isDestroyed() || (r.findState.requestId = a.findInPage(i));
}
function qU(e) {
  QU(e, !0);
}
function JU(e) {
  QU(e, !1);
}
function YU(e, t, n, r, i) {
  i.requestId === r.findState.requestId &&
    ((r.findState = {
      ...r.findState,
      activeMatchOrdinal: i.activeMatchOrdinal,
      matches: i.matches,
      requestId: i.finalUpdate ? null : i.requestId,
    }),
    XU(e, t, n, r));
}
function XU(e, t, n, r) {
  if (t.isDestroyed()) return;
  let i = {
    type: `browser-sidebar-find-state`,
    conversationId: n,
    browserTabId: r.browserTabId,
    state: {
      activeMatchOrdinal: r.findState.activeMatchOrdinal,
      matches: r.findState.matches,
      query: r.findState.query,
    },
  };
  e.sendMessageToWebContents(t, i);
}
function ZU(e, t, n, r) {
  if (
    r.findState.query.length === 0 &&
    r.findState.matches === 0 &&
    r.findState.activeMatchOrdinal === 0 &&
    r.findState.requestId == null
  )
    return;
  let i = r.page?.view.webContents;
  (i != null && !i.isDestroyed() && i.stopFindInPage(`clearSelection`),
    (r.findState = WU()),
    XU(e, t, n, r));
}
function QU(e, t) {
  if (e.findState.query.length === 0) return;
  let n = e.page?.view.webContents;
  n == null ||
    n.isDestroyed() ||
    (e.findState.requestId = n.findInPage(e.findState.query, {
      findNext: !0,
      forward: t,
    }));
}
var $U = [`nextTab`, `previousTab`],
  eW = [
    `nextRecentThread`,
    `nextThread`,
    `openBrowserTab`,
    `previousRecentThread`,
    `previousThread`,
  ];
function tW({
  captureTextSelection: e,
  closeBrowserTab: t,
  copyBrowserUrl: n,
  getCanCloseBrowserTab: r,
  getInteractionMode: i,
  getOwnerCommandAccelerators: a = sW,
  isCommentModeAvailable: o,
  owner: s,
  runCommandInOwner: c,
  setInteractionMode: l,
}) {
  let u = iW(a);
  return (d, f) => {
    if (
      ZI(f, {
        allowDebugMenu: !1,
        closeTabAccelerators: a(`closeTab`),
        state: {
          bottomPanelBrowserCanZoom: !1,
          bottomPanelBrowserConversationId: null,
          bottomPanelBrowserTabId: null,
          canAcceptAppshotShortcut: !1,
          bottomPanelCanCloseActiveTab: !1,
          focusArea: `right-panel`,
          imagePreviewOpen: !1,
          isNewChatRoute: !1,
          rightPanelBrowserCanZoom: !1,
          rightPanelBrowserConversationId: null,
          rightPanelBrowserTabId: null,
          rightPanelCanCloseActiveTab: r(),
        },
      })?.type === `close-active-app-shell-tab`
    ) {
      (d.preventDefault(), t());
      return;
    }
    if (HI(f, `CmdOrCtrl+Shift+C`)) {
      (d.preventDefault(), n());
      return;
    }
    let p = u(f);
    if (p != null) {
      if ((d.preventDefault(), s.focus(), p.allowAppShellTabShortcut)) {
        c(p.commandId, oW(f), { allowAppShellTabShortcut: !0 });
        return;
      }
      c(p.commandId, oW(f));
      return;
    }
    let m = nW(f, { interactionMode: i(), isCommentModeAvailable: o() });
    m != null &&
      (d.preventDefault(),
      m.shouldFocusOwner && s.focus(),
      m.command.type === `set-interaction-mode` &&
        l != null &&
        (m.command.interactionMode === `comment` &&
          m.command.annotationModeEntrySource === `keyboard-shortcut` &&
          e?.(),
        l(m.command.interactionMode, m.command.annotationModeEntrySource)));
  };
}
function nW(e, { interactionMode: t, isCommentModeAvailable: n }) {
  return !HI(e, `CmdOrCtrl+.`) || !n
    ? null
    : {
        command: {
          type: `set-interaction-mode`,
          interactionMode: t === `comment` ? `browse` : `comment`,
          ...(t === `comment`
            ? {}
            : { annotationModeEntrySource: `keyboard-shortcut` }),
        },
        shouldFocusOwner: !1,
      };
}
function rW() {
  let e = process.platform === `darwin`,
    t = (t) => n.Gt({ commandId: t, isMacOS: e });
  return {
    closeTab: t(`closeTab`),
    nextTab: t(`nextTab`),
    nextRecentThread: t(`nextRecentThread`),
    nextThread: t(`nextThread`),
    openBrowserTab: t(`openBrowserTab`),
    previousTab: t(`previousTab`),
    previousRecentThread: t(`previousRecentThread`),
    previousThread: t(`previousThread`),
  };
}
function iW(e) {
  return (t) => {
    if (t.type !== `keyDown`) return null;
    let n = oW(t),
      r = aW(t, n, e, $U),
      i = aW(t, n, e, eW);
    return r != null && i != null
      ? { allowAppShellTabShortcut: !0, commandId: i }
      : r == null
        ? i == null
          ? null
          : { commandId: i }
        : { commandId: r };
  };
}
function aW(e, t, r, i) {
  for (let a of i)
    for (let i of r(a)) {
      if (i.includes(` `) || !UI(t, i)) continue;
      let r = n.Zt(a);
      return e.isAutoRepeat &&
        (r == null || r.kind !== `webview` || r.allowsKeyRepeat !== !0)
        ? null
        : a;
    }
  return null;
}
function oW(e) {
  return {
    altKey: e.alt === !0,
    code: e.code,
    ctrlKey: e.control === !0,
    key: e.key,
    metaKey: e.meta === !0,
    repeat: e.isAutoRepeat === !0,
    shiftKey: e.shift === !0,
  };
}
function sW(e) {
  return n.$t({ commandId: e, isMacOS: process.platform === `darwin` });
}
var cW = r.a(`browser-sidebar-manager`);
function lW(e, { conversationId: t, windowId: n }, r) {
  let i = e.getLiveWindowState(n);
  return i == null
    ? (cW().warning(
        `IAB_LIFECYCLE browser use route window missing for page lookup`,
        { safe: { conversationId: t, windowId: n }, sensitive: {} },
      ),
      null)
    : e.getPageHandleForConversation(i, t, r ?? yL(t));
}
function uW(e, t) {
  return TW(e, t);
}
function dW(e, t) {
  return pW(e, t, !0).map(
    ({ active: e, browserTabId: t, title: n, url: r }) => ({
      active: e,
      browserTabId: t,
      title: n,
      url: r,
    }),
  );
}
function fW(e, t) {
  return pW(e, t, !1);
}
function pW(e, { conversationId: t, windowId: r }, i) {
  let a = e.getLiveWindowState(r);
  if (a == null)
    return (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for tab list`,
        { safe: { conversationId: t, windowId: r }, sensitive: {} },
      ),
      []
    );
  let o = [];
  for (let e of a.threads.values()) {
    if (
      e.conversationId !== t ||
      e.pendingTeardown?.reason === `closed` ||
      (i ? e.isBrowserUsePage : !e.isBrowserUsePage)
    )
      continue;
    let r = e.snapshot.title,
      s = e.snapshot.url;
    (i &&
      e.page == null &&
      s.length === 0 &&
      e.snapshot.tabType === n.oc.NEW_TAB_PAGE) ||
      o.push({
        active:
          a.activeConversationId === t &&
          a.activeBrowserTabId === e.browserTabId,
        browserTabId: e.browserTabId,
        origin: e.browserUseTabOrigin ?? `agent`,
        title: r.length > 0 ? r : (e.page?.title ?? ``),
        url: s.length > 0 ? s : e.page == null ? `` : NL(e.page),
      });
  }
  return o;
}
async function mW(
  e,
  {
    browserTabId: t,
    conversationId: n,
    origin: r,
    startingUrl: i,
    windowId: a,
  },
) {
  let o = e.getLiveWindowState(a);
  if (o == null)
    throw (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for page open`,
        { safe: { conversationId: n, windowId: a }, sensitive: {} },
      ),
      Error(
        `ChatGPT browser window ${a} is no longer available for browser session ${n}`,
      )
    );
  let s = t ?? yL(n);
  cW().info(`IAB_LIFECYCLE opening route page`, {
    safe: {
      activeConversationId: o.activeConversationId,
      browserTabId: t,
      conversationId: n,
      hasStartingUrl: i != null,
      ownerWebContentsId: o.owner.id,
      threadExists: e.getThreadStateForWindow(o, n, s) != null,
      windowId: a,
    },
    sensitive: {},
  });
  let c = e.ensureThreadStateForWindow(o, n, s, { isBrowserUseTab: !0 });
  ((c.isBrowserUsePage = !0),
    (c.browserUseTabOrigin = r ?? c.browserUseTabOrigin ?? `agent`));
  let l =
    c.page ??
    e.createThreadPageState(o, c, {
      title: c.snapshot.title,
      url: c.snapshot.url,
    });
  return (
    i != null &&
      NL(l) !== i &&
      e.navigatePageToUrl(l, i, `browser_use`, `browser_use`),
    e.syncThreadState(o, n, c),
    KL(o, n, l) ??
      (cW().info(
        `IAB_LIFECYCLE waiting for browser sidebar webview attachment`,
        {
          safe: {
            conversationId: n,
            ownerWebContentsId: o.owner.id,
            windowId: a,
          },
          sensitive: {},
        },
      ),
      e.browserUseOpenRequests.waitForOpen(o.owner, n, s, (t) => {
        hW(e, { conversationId: n, windowId: a }, s, t);
      }))
  );
}
function hW(e, t, n, r) {
  let { conversationId: i, windowId: a } = t,
    o = e.getLiveWindowState(a);
  if (o == null) {
    cW().warning(
      `IAB_LIFECYCLE browser use route window missing for page close`,
      {
        safe: { browserTabId: n, conversationId: i, windowId: a },
        sensitive: {},
      },
    );
    return;
  }
  let s = e.getThreadStateForWindow(o, i, n) ?? null;
  s != null &&
    (ZU(e.windowManager, o.owner, i, s),
    s.isBrowserUseActive && CW(e, t, !1, n),
    e.browserUseOpenRequests.rejectForRoute({
      browserTabId: n,
      conversationId: i,
      error: r ?? Error(`ChatGPT browser tab was closed by Browser Use`),
      owner: o.owner,
    }),
    e.requestThreadTeardown(o, i, s, `closed`),
    e.windowManager.sendMessageToWebContents(o.owner, {
      type: `close-browser-tab`,
      browserTabId: n,
      conversationId: i,
    }));
}
function gW(e, t, n) {
  let { conversationId: r, windowId: i } = t,
    a = e.getLiveWindowState(i);
  if (a == null) {
    cW().warning(
      `IAB_LIFECYCLE browser use route window missing for page release`,
      {
        safe: { browserTabId: n, conversationId: r, windowId: i },
        sensitive: {},
      },
    );
    return;
  }
  let o = e.getThreadStateForWindow(a, r, n) ?? null;
  if (o == null) return;
  let s = o.isBrowserUseActive;
  (bW(e, t, null, n),
    xW(e, t, null, n),
    (o.isBrowserUsePage = !1),
    (o.isBrowserUseActive = !1),
    (o.browserUseTabOrigin = null),
    (o.pendingBrowserUsePanelOpen = !1),
    (o.hasPendingBrowserUseVisibilityRequest = !1),
    (o.isBrowserUseNavigationRestrictionActive = !1),
    wW(e, t, null, n),
    e.syncThreadState(a, r, o),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-state`,
      conversationId: r,
      browserTabId: n,
      isActive: !1,
    }),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-page-released`,
      conversationId: r,
      browserTabId: n,
    }),
    e.browserSessionRegistry.recordPageReleased({
      browserTabId: n,
      conversationId: r,
      windowId: i,
    }),
    s && e.enforceDetachedPageBudget(a));
}
function _W(e, { conversationId: t, windowId: n }) {
  let r = e.getLiveWindowState(n);
  if (r == null) {
    cW().warning(
      `IAB_LIFECYCLE browser use route window missing for route disposal`,
      { safe: { conversationId: t, windowId: n }, sensitive: {} },
    );
    return;
  }
  for (let n of Array.from(r.threads.values()))
    n.conversationId !== t ||
      !n.isBrowserUsePage ||
      (e.browserUseOpenRequests.rejectForRoute({
        browserTabId: n.browserTabId,
        conversationId: t,
        error: Error(
          `ChatGPT browser route was disposed before the browser pane attached`,
        ),
        owner: r.owner,
      }),
      e.comments.close(r.owner, t, n.browserTabId),
      ZU(e.windowManager, r.owner, t, n),
      e.windowManager.sendMessageToWebContents(r.owner, {
        type: `browser-sidebar-clear-pending-panel-open`,
        browserTabId: n.browserTabId,
        conversationId: t,
      }),
      e.requestThreadTeardown(r, t, n, `closed`));
}
function vW(e, t, n, r) {
  let { conversationId: i, windowId: a } = t,
    o = e.getLiveWindowState(a);
  if (o == null)
    throw (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for browser visibility`,
        { safe: { conversationId: i, windowId: a }, sensitive: {} },
      ),
      Error(
        `ChatGPT browser window ${a} is no longer available for browser session ${i}`,
      )
    );
  let s = r ?? yL(i);
  if (n) {
    (e.ensureThreadStateForWindow(o, i, s, { isBrowserUseTab: !0 }),
      EW(e, o, i, s));
    return;
  }
  let c = e.getThreadStateForWindow(o, i, s) ?? null;
  (c != null &&
    ((c.pendingBrowserUsePanelOpen = !1),
    (c.hasPendingBrowserUseVisibilityRequest = !1),
    (c.presented = !1),
    (c.visible = !1),
    (c.bounds = null),
    e.updateThreadSessionState(c)),
    e.windowManager.sendMessageToWebContents(o.owner, {
      type: `browser-sidebar-clear-pending-panel-open`,
      browserTabId: s,
      conversationId: i,
    }),
    !(o.activeConversationId !== i || o.activeBrowserTabId !== s) &&
      e.windowManager.sendMessageToWebContents(o.owner, {
        type: `toggle-browser-panel`,
        browserTabId: s,
        open: !1,
        source: `browser_use`,
      }));
}
function yW(e, { conversationId: t, windowId: n }, r) {
  let i = e.getLiveWindowState(n);
  if (i == null)
    throw (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for browser visibility read`,
        { safe: { conversationId: t, windowId: n }, sensitive: {} },
      ),
      Error(
        `ChatGPT browser window ${n} is no longer available for browser session ${t}`,
      )
    );
  let a = r ?? yL(t),
    o = e.getThreadStateForWindow(i, t, a) ?? null;
  return (
    i.activeConversationId === t &&
    i.activeBrowserTabId === a &&
    (o?.hasPendingBrowserUseVisibilityRequest === !0 ||
      (o?.visible === !0 && o.bounds != null))
  );
}
function bW(e, { conversationId: t, windowId: n }, r, i) {
  let a = e.getLiveWindowState(n);
  if (a == null)
    throw (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for viewport set`,
        { safe: { conversationId: t, windowId: n }, sensitive: {} },
      ),
      Error(
        `ChatGPT browser window ${n} is no longer available for browser session ${t}`,
      )
    );
  let o =
      r == null
        ? null
        : {
            width: zL(r.width, jB.minWidth, jB.maxWidth),
            height: zL(r.height, jB.minHeight, jB.maxHeight),
          },
    s = i ?? yL(t),
    c =
      o == null
        ? e.getThreadStateForWindow(a, t, s)
        : e.ensureThreadStateForWindow(a, t, s, { isBrowserUseTab: !0 });
  if (c == null) return;
  let l =
    c.emulatedViewportSize?.width !== o?.width ||
    c.emulatedViewportSize?.height !== o?.height;
  ((c.emulatedViewportSize = o),
    e.syncThreadState(a, t, c),
    c.page != null && l && e.queuePageDeviceMetricsSync(c.page, o),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-viewport`,
      conversationId: t,
      browserTabId: c.browserTabId,
      viewportSize: o,
    }));
}
function xW(e, { conversationId: t, windowId: n }, r, i) {
  let a = e.getLiveWindowState(n);
  if (a == null)
    throw (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for capture surface set`,
        { safe: { conversationId: t, windowId: n }, sensitive: {} },
      ),
      Error(
        `ChatGPT browser window ${n} is no longer available for browser session ${t}`,
      )
    );
  let o = e.getThreadStateForWindow(a, t, i ?? yL(t)) ?? null;
  e.windowManager.sendMessageToWebContents(a.owner, {
    type: `browser-sidebar-browser-use-capture-surface`,
    conversationId: t,
    browserTabId: o?.browserTabId,
    surfaceSize:
      r == null ? null : { width: BL(r.width), height: BL(r.height) },
  });
}
function SW(e, t, n, r) {
  let i = e.getThreadStateForWindow(t, n, r) ?? null;
  (i?.visible === !0 && i.bounds != null) ||
    (i != null &&
      ((i.pendingBrowserUsePanelOpen = !1),
      (i.hasPendingBrowserUseVisibilityRequest = t.activeConversationId === n)),
    e.windowManager.sendMessageToWebContents(t.owner, {
      type: `browser-sidebar-open-panel-without-animation`,
      conversationId: n,
      browserTabId: i?.browserTabId ?? r,
      source: `browser_use`,
      initiator: `browser_use`,
    }));
}
function CW(e, t, n, r) {
  let i = e.getLiveWindowState(t.windowId);
  if (i == null) {
    cW().warning(
      `IAB_LIFECYCLE browser use route window missing for active state`,
      { safe: t, sensitive: {} },
    );
    return;
  }
  let a =
    r == null ? (n ? [yL(t.conversationId)] : kW(i, t.conversationId)) : [r];
  for (let r of a) {
    n && DW(e, i, t, r);
    let a =
        e.getThreadStateForWindow(i, t.conversationId, r) ??
        (n
          ? e.ensureThreadStateForWindow(i, t.conversationId, r, {
              isBrowserUseTab: !0,
            })
          : null),
      o = a?.isBrowserUseActive ?? !1;
    n &&
      !o &&
      a != null &&
      a.emulatedViewportSize == null &&
      e.defaultBrowserUseViewportSize != null &&
      bW(e, t, e.defaultBrowserUseViewportSize, r);
    let s = a != null && !n && a.isBrowserUseNavigationRestrictionActive;
    (a != null && o === n && !s) ||
      (a != null &&
        ((a.isBrowserUseActive = n),
        n &&
          (a.page != null && (a.page.pendingSuspensionId = null),
          (a.isBrowserUsePage = !0),
          (a.browserUseTabOrigin ??= `agent`)),
        n || (a.isBrowserUseNavigationRestrictionActive = !1)),
      cW().info(`IAB_LIFECYCLE syncing browser use active state`, {
        safe: {
          browserTabId: r,
          conversationId: t.conversationId,
          isActive: n,
          ownerWebContentsId: i.owner.id,
          windowId: t.windowId,
        },
        sensitive: {},
      }),
      e.windowManager.sendMessageToWebContents(i.owner, {
        type: `browser-sidebar-browser-use-state`,
        conversationId: t.conversationId,
        browserTabId: r,
        isActive: n,
      }),
      n || wW(e, t, null, r));
  }
}
function wW(e, t, n, r) {
  let i = e.getLiveWindowState(t.windowId);
  if (i == null)
    return (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for cursor state`,
        { safe: t, sensitive: {} },
      ),
      !1
    );
  let a =
      r == null
        ? n == null
          ? kW(i, t.conversationId)
          : [yL(t.conversationId)]
        : [r],
    o = !1;
  for (let r of a) {
    let a =
        (n == null
          ? (e.getThreadStateForWindow(i, t.conversationId, r) ?? null)
          : e.ensureThreadStateForWindow(i, t.conversationId, r, {
              isBrowserUseTab: !0,
            })
        )?.browserTabId ?? r,
      s = n != null && OW(e, i, t.conversationId, a),
      c =
        n == null
          ? { visible: !1, x: 0, y: 0 }
          : {
              ...(s ? {} : { animateMovement: !1 }),
              moveSequence: n.moveSequence,
              visible: !0,
              x: n.x,
              y: n.y,
            };
    (e.windowManager.sendMessageToWebContents(i.owner, {
      type: `browser-sidebar-browser-use-cursor-state`,
      conversationId: t.conversationId,
      browserTabId: a,
      ...c,
    }),
      (o = s || o));
  }
  return o;
}
function TW(e, { conversationId: t, windowId: n }) {
  let r = e.getLiveWindowState(n);
  if (r == null)
    return (
      cW().warning(
        `IAB_LIFECYCLE browser use route window missing for page list`,
        { safe: { conversationId: t, windowId: n }, sensitive: {} },
      ),
      []
    );
  let i = [];
  for (let e of r.threads.values()) {
    if (
      e.conversationId !== t ||
      e.pendingTeardown?.reason === `closed` ||
      !e.isBrowserUsePage ||
      e.page == null
    )
      continue;
    let n = KL(r, t, e.page);
    n != null && i.push(n);
  }
  return i;
}
function EW(e, t, n, r) {
  if (t.activeConversationId !== n) {
    SW(e, t, n, r);
    return;
  }
  let i = e.getThreadStateForWindow(t, n, r) ?? null;
  (i?.visible === !0 && i.bounds != null) ||
    (i != null &&
      ((i.pendingBrowserUsePanelOpen = !1),
      (i.hasPendingBrowserUseVisibilityRequest = !0)),
    e.windowManager.sendMessageToWebContents(t.owner, {
      type: `toggle-browser-panel`,
      open: !0,
      browserTabId: r,
      conversationId: n,
      source: `browser_use`,
      initiator: `browser_use`,
    }));
}
function DW(e, t, n, r) {
  for (let i of t.threads.values())
    i.conversationId !== n.conversationId ||
      i.browserTabId === r ||
      !i.isBrowserUseActive ||
      ((i.isBrowserUseActive = !1),
      (i.isBrowserUseNavigationRestrictionActive = !1),
      e.windowManager.sendMessageToWebContents(t.owner, {
        type: `browser-sidebar-browser-use-state`,
        conversationId: n.conversationId,
        browserTabId: i.browserTabId,
        isActive: !1,
      }),
      wW(e, n, null, i.browserTabId));
}
function OW(e, t, r, i) {
  let a = e.getThreadStateForWindow(t, r, i) ?? null;
  return (
    t.window.isFocused() &&
    t.activeConversationId === r &&
    t.activeBrowserTabId === i &&
    a?.visible === !0 &&
    a.bounds != null &&
    a.snapshot.tabType === n.oc.WEB
  );
}
function kW(e, t) {
  let n = [];
  for (let r of e.threads.values())
    r.conversationId === t &&
      (r.isBrowserUsePage || r.isBrowserUseActive) &&
      n.push(r.browserTabId);
  return n;
}
function AW(e) {
  switch (e) {
    case `Input.dispatchKeyEvent`:
    case `Input.dispatchMouseEvent`:
    case `Input.insertText`:
    case `Input.synthesizeScrollGesture`:
      return !0;
    default:
      return !1;
  }
}
function jW(e) {
  let t = `__codexIabExpectedInputTargetToken`,
    n = ((e, t) => {
      if (typeof e != `object` || !e || Array.isArray(e))
        throw Error(`${t} must be an object`);
      return e;
    })(e.params, `params`),
    r = () => {
      let e = n[t];
      if (e == null) return null;
      if (typeof e != `string` || e.length === 0)
        throw Error(`${t} must be a string`);
      return e;
    },
    i = (e) => {
      if (typeof e != `object` || !e) return null;
      let t = e.__codexIabInputTargetToken;
      return typeof t == `string` ? t : null;
    },
    a = (e) => {
      let t = r();
      if (t != null && i(e) !== t)
        throw Error(
          `Focused input target no longer matches the resolved locator`,
        );
    },
    o = (e) => {
      let t = (t) => Math.floor(e / t) % 2 == 1;
      return { altKey: t(1), ctrlKey: t(2), metaKey: t(4), shiftKey: t(8) };
    },
    s = (e, t) => {
      if (typeof e != `number` || !Number.isFinite(e))
        throw Error(`${t} must be a finite number`);
      return e;
    },
    c = (e, t) => {
      if (typeof e != `string`) throw Error(`${t} must be a string`);
      return e;
    },
    l = (e) => e?.ownerDocument?.defaultView ?? window,
    u = (e) =>
      (e.defaultView == null ? e.ownerDocument : e)?.defaultView ?? window,
    d = () => {
      throw Error(
        `Input targets inside cross-origin or inaccessible iframes are not currently supported in the in-app browser`,
      );
    },
    f = (e, t, n) => {
      let r = u(e),
        i = e.elementFromPoint(t, n);
      if (i == null || !(i instanceof r.Element)) return null;
      let a = i.shadowRoot;
      if (a != null) {
        let e = a.elementFromPoint(t, n),
          r = u(a);
        if (e != null && e instanceof r.Element)
          return { target: e, x: t, y: n };
      }
      if (i instanceof r.HTMLIFrameElement)
        try {
          let e = i.contentWindow;
          if (e != null) {
            let r = e.document,
              a = i.getBoundingClientRect();
            return f(r, t - a.left, n - a.top) ?? { target: i, x: t, y: n };
          }
          d();
        } catch {
          d();
        }
      return { target: i, x: t, y: n };
    },
    p = (e, t) => {
      let n = f(document, e, t);
      if (n == null) throw Error(`No element found at point ${e},${t}`);
      return n;
    },
    m = (e) => {
      switch (e) {
        case `left`:
          return 0;
        case `middle`:
          return 1;
        case `right`:
          return 2;
        case `back`:
          return 3;
        case `forward`:
          return 4;
        case `none`:
        case void 0:
          return 0;
        default:
          throw Error(`Unsupported mouse button: ${String(e)}`);
      }
    },
    h = (e, t, n) => {
      let r = l(e),
        i = t.startsWith(`pointer`),
        a = r.PointerEvent,
        o =
          i && typeof a == `function`
            ? new a(t, {
                ...n,
                isPrimary: !0,
                pointerId: 1,
                pointerType: `mouse`,
              })
            : new r.MouseEvent(t, n);
      return e.dispatchEvent(o);
    },
    g = (e, t, n, r) => {
      let i = l(e);
      return {
        ...o(Number(t.modifiers ?? 0)),
        bubbles: !0,
        button: m(t.button),
        buttons: Number(t.buttons ?? 0),
        cancelable: !0,
        clientX: n,
        clientY: r,
        composed: !0,
        detail: Number(t.clickCount ?? 0),
        screenX: n,
        screenY: r,
        view: i,
      };
    },
    _ = (e) => e instanceof l(e).HTMLElement,
    v = (e) =>
      _(e)
        ? e.isContentEditable || e.tabIndex >= 0
          ? !0
          : e.tagName === `A`
            ? e.hasAttribute(`href`)
            : e.tagName === `BUTTON` ||
              e.tagName === `INPUT` ||
              e.tagName === `SELECT` ||
              e.tagName === `TEXTAREA`
        : !1,
    y = (e) => {
      let t = l(e),
        n = null;
      if (e instanceof t.HTMLLabelElement && e.control != null) n = e.control;
      else
        for (let t = e; t != null; t = t.parentElement)
          if (v(t)) {
            n = t;
            break;
          }
      if (n != null)
        try {
          n.focus({ preventScroll: !0 });
        } catch {
          n.focus();
        }
    },
    b = () => {
      let e = globalThis;
      return (
        (e.__codexIabInputTranslationState ??= {}),
        e.__codexIabInputTranslationState
      );
    },
    x = (e, t) =>
      e != null && Math.abs(e.x - t.x) <= 1 && Math.abs(e.y - t.y) <= 1,
    S = () => {
      let e = c(n.type, `type`),
        t = s(n.x, `x`),
        r = s(n.y, `y`),
        { target: i, x: a, y: o } = p(t, r),
        l = g(i, n, a, o),
        u = b();
      if (e === `mouseMoved`) {
        (Number(n.buttons ?? 0) !== 0 &&
          u.mousePress != null &&
          (u.mousePress.moved = !0),
          h(i, `pointermove`, l),
          h(i, `mousemove`, l));
        return;
      }
      if (e === `mousePressed`) {
        u.mousePress = { button: n.button ?? `left`, moved: !1, x: t, y: r };
        let e = h(i, `pointerdown`, l),
          a = h(i, `mousedown`, l);
        e && a && y(i);
        return;
      }
      if (e === `mouseReleased`) {
        (h(i, `pointerup`, l), h(i, `mouseup`, l));
        let e = u.mousePress;
        if (
          ((u.mousePress = null),
          !(
            e != null &&
            !e.moved &&
            e.button === (n.button ?? `left`) &&
            x(e, { x: t, y: r })
          ))
        )
          return;
        if (n.button === `right`) {
          h(i, `contextmenu`, l);
          return;
        }
        if (n.button === `middle`) {
          h(i, `auxclick`, l);
          return;
        }
        (h(i, `click`, l),
          Number(n.clickCount ?? 0) >= 2 && h(i, `dblclick`, l));
        return;
      }
      if (e === `mouseWheel`) {
        T(i, l, Number(n.deltaX ?? 0), Number(n.deltaY ?? 0));
        return;
      }
      throw Error(`Unsupported mouse event type: ${e}`);
    },
    C = (e, t, n) => {
      let r = (e, t, n) => {
        if (n === 0) return !1;
        if (t === `x`) {
          let t = e.scrollWidth - e.clientWidth;
          return n > 0 ? e.scrollLeft < t : e.scrollLeft > 0;
        }
        let r = e.scrollHeight - e.clientHeight;
        return n > 0 ? e.scrollTop < r : e.scrollTop > 0;
      };
      for (let i = e; i != null; i = i.parentElement) {
        let e = getComputedStyle(i),
          a =
            n !== 0 &&
            /(auto|scroll|overlay)/.test(e.overflowY) &&
            r(i, `y`, n);
        if (
          (t !== 0 &&
            /(auto|scroll|overlay)/.test(e.overflowX) &&
            r(i, `x`, t)) ||
          a
        )
          return i;
      }
      return null;
    },
    w = (e, t, n) => {
      let r = C(e, t, n);
      if (r != null) {
        r.scrollBy({ behavior: `auto`, left: t, top: n });
        return;
      }
      l(e).scrollBy({ behavior: `auto`, left: t, top: n });
    },
    T = (e, t, n, r) => {
      let i = new (l(e).WheelEvent)(`wheel`, {
        ...t,
        deltaMode: 0,
        deltaX: n,
        deltaY: r,
      });
      e.dispatchEvent(i) && w(e, n, r);
    },
    E = () => {
      let e = s(n.x, `x`),
        t = s(n.y, `y`),
        r = Number(n.xDistance ?? 0),
        i = Number(n.yDistance ?? 0),
        { target: a, x: o, y: c } = p(e, t);
      T(a, g(a, { buttons: 0, clickCount: 0, modifiers: 0 }, o, c), -r, -i);
    },
    D = (e) => {
      let t = e.activeElement;
      if (t == null) return null;
      let n = u(e);
      if (t instanceof n.HTMLIFrameElement)
        try {
          let e = t.contentWindow?.document;
          if (e != null) return D(e) ?? t;
          d();
        } catch {
          d();
        }
      return t instanceof n.HTMLElement && t.shadowRoot != null
        ? (D(t.shadowRoot) ?? t)
        : t;
    },
    O = (e) => {
      let t = l(e);
      if (e instanceof t.HTMLTextAreaElement) return !0;
      if (!(e instanceof t.HTMLInputElement)) return !1;
      switch (e.type) {
        case `password`:
        case `search`:
        case `tel`:
        case `text`:
        case `url`:
          return !0;
        default:
          return !1;
      }
    },
    k = (e) => {
      if (!(e instanceof l(e).HTMLInputElement)) return !1;
      switch (e.type) {
        case `email`:
        case `number`:
          return !0;
        default:
          return !1;
      }
    },
    A = (e) => e instanceof l(e).HTMLElement && e.isContentEditable,
    j = (e) => e instanceof l(e).HTMLTextAreaElement,
    M = () => D(document) ?? document.body ?? document.documentElement,
    N = () => {
      let e = D(document);
      return e != null && (O(e) || k(e) || A(e)) ? e : null;
    },
    P = () => {
      let e = N();
      return (e != null && a(e), e);
    },
    F = (e, t, n) => {
      let r = l(e);
      return typeof r.InputEvent == `function`
        ? new r.InputEvent(t, n)
        : new r.Event(t, {
            bubbles: n.bubbles,
            cancelable: n.cancelable,
            composed: n.composed,
          });
    },
    I = (e, t, n = `insertText`) => {
      if (e == null) throw Error(`No editable element is focused`);
      let r = F(e, `beforeinput`, {
        bubbles: !0,
        cancelable: !0,
        composed: !0,
        data: t,
        inputType: n,
      });
      if (e.dispatchEvent(r)) {
        if (O(e)) {
          let n = e.selectionStart ?? e.value.length,
            r = e.selectionEnd ?? e.value.length;
          e.setRangeText(t, n, r, `end`);
        } else if (k(e)) {
          if (e.ownerDocument.execCommand?.(`insertText`, !1, t)) return;
          e.value += t;
        } else if (A(e)) {
          let n = l(e).getSelection(),
            r = n?.rangeCount ? n.getRangeAt(0) : null;
          ((r == null || !e.contains(r.commonAncestorContainer)) &&
            ((r = e.ownerDocument.createRange()),
            r.selectNodeContents(e),
            r.collapse(!1)),
            r.deleteContents());
          let i = e.ownerDocument.createTextNode(t);
          (r.insertNode(i),
            r.setStartAfter(i),
            r.collapse(!0),
            n?.removeAllRanges(),
            n?.addRange(r));
        } else throw Error(`Focused element is not editable`);
        e.dispatchEvent(
          F(e, `input`, {
            bubbles: !0,
            cancelable: !1,
            composed: !0,
            data: t,
            inputType: n,
          }),
        );
      }
    },
    ee = (e) => {
      if (e == null) return !1;
      if (O(e)) return (e.setSelectionRange(0, e.value.length), !0);
      if (k(e)) return e.ownerDocument.execCommand?.(`selectAll`) === !0;
      if (A(e)) {
        let t = l(e).getSelection(),
          n = e.ownerDocument.createRange();
        return (
          n.selectNodeContents(e),
          t?.removeAllRanges(),
          t?.addRange(n),
          !0
        );
      }
      return !1;
    },
    L = (e, t) => {
      if (e == null) return !1;
      let n =
          t === `forward` ? `deleteContentForward` : `deleteContentBackward`,
        r = F(e, `beforeinput`, {
          bubbles: !0,
          cancelable: !0,
          composed: !0,
          data: null,
          inputType: n,
        });
      if (!e.dispatchEvent(r)) return !0;
      if (O(e)) {
        let n = e.value,
          r = e.selectionStart ?? n.length,
          i = e.selectionEnd ?? n.length;
        (r === i &&
          (t === `forward`
            ? (i = Math.min(n.length, i + 1))
            : (r = Math.max(0, r - 1))),
          e.setRangeText(``, r, i, `end`));
      } else if (k(e)) {
        let n = t === `forward` ? `forwardDelete` : `delete`;
        if (e.ownerDocument.execCommand?.(n)) return !0;
        t === `backward` && (e.value = e.value.slice(0, -1));
      } else if (A(e)) {
        let n = l(e).getSelection();
        if (n == null || n.rangeCount === 0) return !0;
        let r = n.getRangeAt(0);
        if (!e.contains(r.commonAncestorContainer)) return !0;
        (r.collapsed &&
          (t === `forward`
            ? r.setEnd(
                r.startContainer,
                Math.min(
                  r.startContainer.textContent?.length ?? 0,
                  r.startOffset + 1,
                ),
              )
            : r.setStart(r.startContainer, Math.max(0, r.startOffset - 1))),
          r.deleteContents());
      } else return !1;
      return (
        e.dispatchEvent(
          F(e, `input`, {
            bubbles: !0,
            cancelable: !1,
            composed: !0,
            data: null,
            inputType: n,
          }),
        ),
        !0
      );
    },
    R = (e) => {
      let t = P(),
        n = t?.ownerDocument ?? document,
        r = !1;
      switch (
        (t != null && O(t)
          ? (r = (t.selectionStart ?? 0) !== (t.selectionEnd ?? 0))
          : t != null && A(t) && (r = l(t).getSelection()?.isCollapsed === !1),
        e)
      ) {
        case `selectAll`:
          ee(t) || n.execCommand?.(`selectAll`);
          return;
        case `deleteBackward`:
          L(t, `backward`);
          return;
        case `deleteForward`:
          L(t, `forward`);
          return;
        case `cut`:
          if (t != null) {
            let e = l(t),
              n =
                typeof e.ClipboardEvent == `function`
                  ? new e.ClipboardEvent(`cut`, {
                      bubbles: !0,
                      cancelable: !0,
                      composed: !0,
                    })
                  : new e.Event(`cut`, {
                      bubbles: !0,
                      cancelable: !0,
                      composed: !0,
                    });
            t.dispatchEvent(n) && r && L(t, `forward`);
          }
          return;
        case `copy`:
        case `paste`:
        case `undo`:
        case `redo`:
          n.execCommand?.(e);
          return;
        default:
          return;
      }
    },
    z = () => {
      let e = Number(n.modifiers ?? 0),
        t = Number(n.windowsVirtualKeyCode ?? 0),
        r = typeof n.code == `string` ? n.code : ``,
        i = ``;
      return (
        typeof n.key == `string`
          ? (i = n.key)
          : typeof n.code == `string` && (i = n.code),
        {
          ...o(e),
          bubbles: !0,
          cancelable: !0,
          code: r,
          composed: !0,
          key: i,
          keyCode: t,
          location: Number(n.location ?? 0),
          repeat: !!n.autoRepeat,
          which: t,
        }
      );
    },
    te = (e, t, n) => {
      let r = new (l(e).KeyboardEvent)(t, n),
        i = [
          [`keyCode`, n.keyCode],
          [`which`, n.which],
          [`charCode`, t === `keypress` ? n.keyCode : 0],
        ];
      for (let [e, t] of i)
        try {
          Object.defineProperty(r, e, { get: () => t });
        } catch {}
      return r;
    },
    ne = () => {
      let e = c(n.type, `type`),
        t = M(),
        r = z();
      if (e === `keyUp`) {
        t.dispatchEvent(te(t, `keyup`, r));
        return;
      }
      if ((a(t), e !== `keyDown` && e !== `rawKeyDown` && e !== `char`))
        throw Error(`Unsupported key event type: ${e}`);
      if (!(e === `char` || t.dispatchEvent(te(t, `keydown`, r)))) return;
      let i = Array.isArray(n.commands) ? n.commands : [];
      for (let e of i) typeof e == `string` && R(e);
      let o = typeof n.text == `string` ? n.text : ``;
      if (o.length > 0) {
        let e = o.codePointAt(0) ?? r.keyCode;
        if (
          t.dispatchEvent(
            te(t, `keypress`, { ...r, key: o, keyCode: e, which: e }),
          )
        ) {
          let e = P();
          e != null && I(e, o);
        }
      } else if (i.length === 0) {
        if (r.key === `Backspace`) L(P(), `backward`);
        else if (r.key === `Delete`) L(P(), `forward`);
        else if (r.key === `Enter`) {
          let e = P();
          e != null &&
            (A(e) || j(e)) &&
            I(
              e,
              `
`,
              `insertLineBreak`,
            );
        }
      }
    };
  try {
    switch (e.method) {
      case `Input.dispatchMouseEvent`:
        S();
        break;
      case `Input.dispatchKeyEvent`:
        ne();
        break;
      case `Input.insertText`:
        I(P(), c(n.text, `text`));
        break;
      case `Input.synthesizeScrollGesture`:
        E();
        break;
    }
    return { ok: !0 };
  } catch (e) {
    return { error: e instanceof Error ? e.message : String(e), ok: !1 };
  }
}
var MW = class {
    type;
    schema;
    payload;
    constructor(e, t, n) {
      ((this.type = e), (this.schema = t), (this.payload = n));
    }
    parse() {
      return this.schema.parse(this.payload);
    }
    toJSON() {
      return { type: this.type, ...this.payload };
    }
  },
  NW;
(function (e) {
  e.assertEqual = (e) => {};
  function t(e) {}
  e.assertIs = t;
  function n(e) {
    throw Error();
  }
  ((e.assertNever = n),
    (e.arrayToEnum = (e) => {
      let t = {};
      for (let n of e) t[n] = n;
      return t;
    }),
    (e.getValidEnumValues = (t) => {
      let n = e.objectKeys(t).filter((e) => typeof t[t[e]] != `number`),
        r = {};
      for (let e of n) r[e] = t[e];
      return e.objectValues(r);
    }),
    (e.objectValues = (t) =>
      e.objectKeys(t).map(function (e) {
        return t[e];
      })),
    (e.objectKeys =
      typeof Object.keys == `function`
        ? (e) => Object.keys(e)
        : (e) => {
            let t = [];
            for (let n in e)
              Object.prototype.hasOwnProperty.call(e, n) && t.push(n);
            return t;
          }),
    (e.find = (e, t) => {
      for (let n of e) if (t(n)) return n;
    }),
    (e.isInteger =
      typeof Number.isInteger == `function`
        ? (e) => Number.isInteger(e)
        : (e) =>
            typeof e == `number` && Number.isFinite(e) && Math.floor(e) === e));
  function r(e, t = ` | `) {
    return e.map((e) => (typeof e == `string` ? `'${e}'` : e)).join(t);
  }
  ((e.joinValues = r),
    (e.jsonStringifyReplacer = (e, t) =>
      typeof t == `bigint` ? t.toString() : t));
})((NW ||= {}));
var PW;
(function (e) {
  e.mergeShapes = (e, t) => ({ ...e, ...t });
})((PW ||= {}));
var U = NW.arrayToEnum([
    `string`,
    `nan`,
    `number`,
    `integer`,
    `float`,
    `boolean`,
    `date`,
    `bigint`,
    `symbol`,
    `function`,
    `undefined`,
    `null`,
    `array`,
    `object`,
    `unknown`,
    `promise`,
    `void`,
    `never`,
    `map`,
    `set`,
  ]),
  FW = (e) => {
    switch (typeof e) {
      case `undefined`:
        return U.undefined;
      case `string`:
        return U.string;
      case `number`:
        return Number.isNaN(e) ? U.nan : U.number;
      case `boolean`:
        return U.boolean;
      case `function`:
        return U.function;
      case `bigint`:
        return U.bigint;
      case `symbol`:
        return U.symbol;
      case `object`:
        return Array.isArray(e)
          ? U.array
          : e === null
            ? U.null
            : e.then &&
                typeof e.then == `function` &&
                e.catch &&
                typeof e.catch == `function`
              ? U.promise
              : typeof Map < `u` && e instanceof Map
                ? U.map
                : typeof Set < `u` && e instanceof Set
                  ? U.set
                  : typeof Date < `u` && e instanceof Date
                    ? U.date
                    : U.object;
      default:
        return U.unknown;
    }
  },
  W = NW.arrayToEnum([
    `invalid_type`,
    `invalid_literal`,
    `custom`,
    `invalid_union`,
    `invalid_union_discriminator`,
    `invalid_enum_value`,
    `unrecognized_keys`,
    `invalid_arguments`,
    `invalid_return_type`,
    `invalid_date`,
    `invalid_string`,
    `too_small`,
    `too_big`,
    `invalid_intersection_types`,
    `not_multiple_of`,
    `not_finite`,
  ]),
  IW = class e extends Error {
    get errors() {
      return this.issues;
    }
    constructor(e) {
      (super(),
        (this.issues = []),
        (this.addIssue = (e) => {
          this.issues = [...this.issues, e];
        }),
        (this.addIssues = (e = []) => {
          this.issues = [...this.issues, ...e];
        }));
      let t = new.target.prototype;
      (Object.setPrototypeOf
        ? Object.setPrototypeOf(this, t)
        : (this.__proto__ = t),
        (this.name = `ZodError`),
        (this.issues = e));
    }
    format(e) {
      let t =
          e ||
          function (e) {
            return e.message;
          },
        n = { _errors: [] },
        r = (e) => {
          for (let i of e.issues)
            if (i.code === `invalid_union`) i.unionErrors.map(r);
            else if (i.code === `invalid_return_type`) r(i.returnTypeError);
            else if (i.code === `invalid_arguments`) r(i.argumentsError);
            else if (i.path.length === 0) n._errors.push(t(i));
            else {
              let e = n,
                r = 0;
              for (; r < i.path.length; ) {
                let n = i.path[r];
                (r === i.path.length - 1
                  ? ((e[n] = e[n] || { _errors: [] }), e[n]._errors.push(t(i)))
                  : (e[n] = e[n] || { _errors: [] }),
                  (e = e[n]),
                  r++);
              }
            }
        };
      return (r(this), n);
    }
    static assert(t) {
      if (!(t instanceof e)) throw Error(`Not a ZodError: ${t}`);
    }
    toString() {
      return this.message;
    }
    get message() {
      return JSON.stringify(this.issues, NW.jsonStringifyReplacer, 2);
    }
    get isEmpty() {
      return this.issues.length === 0;
    }
    flatten(e = (e) => e.message) {
      let t = {},
        n = [];
      for (let r of this.issues)
        if (r.path.length > 0) {
          let n = r.path[0];
          ((t[n] = t[n] || []), t[n].push(e(r)));
        } else n.push(e(r));
      return { formErrors: n, fieldErrors: t };
    }
    get formErrors() {
      return this.flatten();
    }
  };
IW.create = (e) => new IW(e);
var LW = (e, t) => {
    let n;
    switch (e.code) {
      case W.invalid_type:
        n =
          e.received === U.undefined
            ? `Required`
            : `Expected ${e.expected}, received ${e.received}`;
        break;
      case W.invalid_literal:
        n = `Invalid literal value, expected ${JSON.stringify(e.expected, NW.jsonStringifyReplacer)}`;
        break;
      case W.unrecognized_keys:
        n = `Unrecognized key(s) in object: ${NW.joinValues(e.keys, `, `)}`;
        break;
      case W.invalid_union:
        n = `Invalid input`;
        break;
      case W.invalid_union_discriminator:
        n = `Invalid discriminator value. Expected ${NW.joinValues(e.options)}`;
        break;
      case W.invalid_enum_value:
        n = `Invalid enum value. Expected ${NW.joinValues(e.options)}, received '${e.received}'`;
        break;
      case W.invalid_arguments:
        n = `Invalid function arguments`;
        break;
      case W.invalid_return_type:
        n = `Invalid function return type`;
        break;
      case W.invalid_date:
        n = `Invalid date`;
        break;
      case W.invalid_string:
        typeof e.validation == `object`
          ? `includes` in e.validation
            ? ((n = `Invalid input: must include "${e.validation.includes}"`),
              typeof e.validation.position == `number` &&
                (n = `${n} at one or more positions greater than or equal to ${e.validation.position}`))
            : `startsWith` in e.validation
              ? (n = `Invalid input: must start with "${e.validation.startsWith}"`)
              : `endsWith` in e.validation
                ? (n = `Invalid input: must end with "${e.validation.endsWith}"`)
                : NW.assertNever(e.validation)
          : (n =
              e.validation === `regex` ? `Invalid` : `Invalid ${e.validation}`);
        break;
      case W.too_small:
        n =
          e.type === `array`
            ? `Array must contain ${e.exact ? `exactly` : e.inclusive ? `at least` : `more than`} ${e.minimum} element(s)`
            : e.type === `string`
              ? `String must contain ${e.exact ? `exactly` : e.inclusive ? `at least` : `over`} ${e.minimum} character(s)`
              : e.type === `number` || e.type === `bigint`
                ? `Number must be ${e.exact ? `exactly equal to ` : e.inclusive ? `greater than or equal to ` : `greater than `}${e.minimum}`
                : e.type === `date`
                  ? `Date must be ${e.exact ? `exactly equal to ` : e.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(e.minimum))}`
                  : `Invalid input`;
        break;
      case W.too_big:
        n =
          e.type === `array`
            ? `Array must contain ${e.exact ? `exactly` : e.inclusive ? `at most` : `less than`} ${e.maximum} element(s)`
            : e.type === `string`
              ? `String must contain ${e.exact ? `exactly` : e.inclusive ? `at most` : `under`} ${e.maximum} character(s)`
              : e.type === `number`
                ? `Number must be ${e.exact ? `exactly` : e.inclusive ? `less than or equal to` : `less than`} ${e.maximum}`
                : e.type === `bigint`
                  ? `BigInt must be ${e.exact ? `exactly` : e.inclusive ? `less than or equal to` : `less than`} ${e.maximum}`
                  : e.type === `date`
                    ? `Date must be ${e.exact ? `exactly` : e.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(e.maximum))}`
                    : `Invalid input`;
        break;
      case W.custom:
        n = `Invalid input`;
        break;
      case W.invalid_intersection_types:
        n = `Intersection results could not be merged`;
        break;
      case W.not_multiple_of:
        n = `Number must be a multiple of ${e.multipleOf}`;
        break;
      case W.not_finite:
        n = `Number must be finite`;
        break;
      default:
        ((n = t.defaultError), NW.assertNever(e));
    }
    return { message: n };
  },
  RW = LW;
function zW() {
  return RW;
}
var BW = (e) => {
  let { data: t, path: n, errorMaps: r, issueData: i } = e,
    a = [...n, ...(i.path || [])],
    o = { ...i, path: a };
  if (i.message !== void 0) return { ...i, path: a, message: i.message };
  let s = ``,
    c = r
      .filter((e) => !!e)
      .slice()
      .reverse();
  for (let e of c) s = e(o, { data: t, defaultError: s }).message;
  return { ...i, path: a, message: s };
};
function G(e, t) {
  let n = zW(),
    r = BW({
      issueData: t,
      data: e.data,
      path: e.path,
      errorMaps: [
        e.common.contextualErrorMap,
        e.schemaErrorMap,
        n,
        n === LW ? void 0 : LW,
      ].filter((e) => !!e),
    });
  e.common.issues.push(r);
}
var VW = class e {
    constructor() {
      this.value = `valid`;
    }
    dirty() {
      this.value === `valid` && (this.value = `dirty`);
    }
    abort() {
      this.value !== `aborted` && (this.value = `aborted`);
    }
    static mergeArray(e, t) {
      let n = [];
      for (let r of t) {
        if (r.status === `aborted`) return K;
        (r.status === `dirty` && e.dirty(), n.push(r.value));
      }
      return { status: e.value, value: n };
    }
    static async mergeObjectAsync(t, n) {
      let r = [];
      for (let e of n) {
        let t = await e.key,
          n = await e.value;
        r.push({ key: t, value: n });
      }
      return e.mergeObjectSync(t, r);
    }
    static mergeObjectSync(e, t) {
      let n = {};
      for (let r of t) {
        let { key: t, value: i } = r;
        if (t.status === `aborted` || i.status === `aborted`) return K;
        (t.status === `dirty` && e.dirty(),
          i.status === `dirty` && e.dirty(),
          t.value !== `__proto__` &&
            (i.value !== void 0 || r.alwaysSet) &&
            (n[t.value] = i.value));
      }
      return { status: e.value, value: n };
    }
  },
  K = Object.freeze({ status: `aborted` }),
  HW = (e) => ({ status: `dirty`, value: e }),
  UW = (e) => ({ status: `valid`, value: e }),
  WW = (e) => e.status === `aborted`,
  GW = (e) => e.status === `dirty`,
  KW = (e) => e.status === `valid`,
  qW = (e) => typeof Promise < `u` && e instanceof Promise,
  q;
(function (e) {
  ((e.errToObj = (e) => (typeof e == `string` ? { message: e } : e || {})),
    (e.toString = (e) => (typeof e == `string` ? e : e?.message)));
})((q ||= {}));
var JW = class {
    constructor(e, t, n, r) {
      ((this._cachedPath = []),
        (this.parent = e),
        (this.data = t),
        (this._path = n),
        (this._key = r));
    }
    get path() {
      return (
        this._cachedPath.length ||
          (Array.isArray(this._key)
            ? this._cachedPath.push(...this._path, ...this._key)
            : this._cachedPath.push(...this._path, this._key)),
        this._cachedPath
      );
    }
  },
  YW = (e, t) => {
    if (KW(t)) return { success: !0, data: t.value };
    if (!e.common.issues.length)
      throw Error(`Validation failed but no issues detected.`);
    return {
      success: !1,
      get error() {
        return ((this._error ||= new IW(e.common.issues)), this._error);
      },
    };
  };
function J(e) {
  if (!e) return {};
  let {
    errorMap: t,
    invalid_type_error: n,
    required_error: r,
    description: i,
  } = e;
  if (t && (n || r))
    throw Error(
      `Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`,
    );
  return t
    ? { errorMap: t, description: i }
    : {
        errorMap: (t, i) => {
          let { message: a } = e;
          return t.code === `invalid_enum_value`
            ? { message: a ?? i.defaultError }
            : i.data === void 0
              ? { message: a ?? r ?? i.defaultError }
              : t.code === `invalid_type`
                ? { message: a ?? n ?? i.defaultError }
                : { message: i.defaultError };
        },
        description: i,
      };
}
var XW = class {
    get description() {
      return this._def.description;
    }
    _getType(e) {
      return FW(e.data);
    }
    _getOrReturnCtx(e, t) {
      return (
        t || {
          common: e.parent.common,
          data: e.data,
          parsedType: FW(e.data),
          schemaErrorMap: this._def.errorMap,
          path: e.path,
          parent: e.parent,
        }
      );
    }
    _processInputParams(e) {
      return {
        status: new VW(),
        ctx: {
          common: e.parent.common,
          data: e.data,
          parsedType: FW(e.data),
          schemaErrorMap: this._def.errorMap,
          path: e.path,
          parent: e.parent,
        },
      };
    }
    _parseSync(e) {
      let t = this._parse(e);
      if (qW(t)) throw Error(`Synchronous parse encountered promise.`);
      return t;
    }
    _parseAsync(e) {
      let t = this._parse(e);
      return Promise.resolve(t);
    }
    parse(e, t) {
      let n = this.safeParse(e, t);
      if (n.success) return n.data;
      throw n.error;
    }
    safeParse(e, t) {
      let n = {
        common: {
          issues: [],
          async: t?.async ?? !1,
          contextualErrorMap: t?.errorMap,
        },
        path: t?.path || [],
        schemaErrorMap: this._def.errorMap,
        parent: null,
        data: e,
        parsedType: FW(e),
      };
      return YW(n, this._parseSync({ data: e, path: n.path, parent: n }));
    }
    "~validate"(e) {
      let t = {
        common: { issues: [], async: !!this[`~standard`].async },
        path: [],
        schemaErrorMap: this._def.errorMap,
        parent: null,
        data: e,
        parsedType: FW(e),
      };
      if (!this[`~standard`].async)
        try {
          let n = this._parseSync({ data: e, path: [], parent: t });
          return KW(n) ? { value: n.value } : { issues: t.common.issues };
        } catch (e) {
          (e?.message?.toLowerCase()?.includes(`encountered`) &&
            (this[`~standard`].async = !0),
            (t.common = { issues: [], async: !0 }));
        }
      return this._parseAsync({ data: e, path: [], parent: t }).then((e) =>
        KW(e) ? { value: e.value } : { issues: t.common.issues },
      );
    }
    async parseAsync(e, t) {
      let n = await this.safeParseAsync(e, t);
      if (n.success) return n.data;
      throw n.error;
    }
    async safeParseAsync(e, t) {
      let n = {
          common: { issues: [], contextualErrorMap: t?.errorMap, async: !0 },
          path: t?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data: e,
          parsedType: FW(e),
        },
        r = this._parse({ data: e, path: n.path, parent: n });
      return YW(n, await (qW(r) ? r : Promise.resolve(r)));
    }
    refine(e, t) {
      let n = (e) =>
        typeof t == `string` || t === void 0
          ? { message: t }
          : typeof t == `function`
            ? t(e)
            : t;
      return this._refinement((t, r) => {
        let i = e(t),
          a = () => r.addIssue({ code: W.custom, ...n(t) });
        return typeof Promise < `u` && i instanceof Promise
          ? i.then((e) => (e ? !0 : (a(), !1)))
          : i
            ? !0
            : (a(), !1);
      });
    }
    refinement(e, t) {
      return this._refinement((n, r) =>
        e(n) ? !0 : (r.addIssue(typeof t == `function` ? t(n, r) : t), !1),
      );
    }
    _refinement(e) {
      return new $G({
        schema: this,
        typeName: Y.ZodEffects,
        effect: { type: `refinement`, refinement: e },
      });
    }
    superRefine(e) {
      return this._refinement(e);
    }
    constructor(e) {
      ((this.spa = this.safeParseAsync),
        (this._def = e),
        (this.parse = this.parse.bind(this)),
        (this.safeParse = this.safeParse.bind(this)),
        (this.parseAsync = this.parseAsync.bind(this)),
        (this.safeParseAsync = this.safeParseAsync.bind(this)),
        (this.spa = this.spa.bind(this)),
        (this.refine = this.refine.bind(this)),
        (this.refinement = this.refinement.bind(this)),
        (this.superRefine = this.superRefine.bind(this)),
        (this.optional = this.optional.bind(this)),
        (this.nullable = this.nullable.bind(this)),
        (this.nullish = this.nullish.bind(this)),
        (this.array = this.array.bind(this)),
        (this.promise = this.promise.bind(this)),
        (this.or = this.or.bind(this)),
        (this.and = this.and.bind(this)),
        (this.transform = this.transform.bind(this)),
        (this.brand = this.brand.bind(this)),
        (this.default = this.default.bind(this)),
        (this.catch = this.catch.bind(this)),
        (this.describe = this.describe.bind(this)),
        (this.pipe = this.pipe.bind(this)),
        (this.readonly = this.readonly.bind(this)),
        (this.isNullable = this.isNullable.bind(this)),
        (this.isOptional = this.isOptional.bind(this)),
        (this[`~standard`] = {
          version: 1,
          vendor: `zod`,
          validate: (e) => this[`~validate`](e),
        }));
    }
    optional() {
      return eK.create(this, this._def);
    }
    nullable() {
      return tK.create(this, this._def);
    }
    nullish() {
      return this.nullable().optional();
    }
    array() {
      return PG.create(this);
    }
    promise() {
      return QG.create(this, this._def);
    }
    or(e) {
      return LG.create([this, e], this._def);
    }
    and(e) {
      return VG.create(this, e, this._def);
    }
    transform(e) {
      return new $G({
        ...J(this._def),
        schema: this,
        typeName: Y.ZodEffects,
        effect: { type: `transform`, transform: e },
      });
    }
    default(e) {
      let t = typeof e == `function` ? e : () => e;
      return new nK({
        ...J(this._def),
        innerType: this,
        defaultValue: t,
        typeName: Y.ZodDefault,
      });
    }
    brand() {
      return new aK({ typeName: Y.ZodBranded, type: this, ...J(this._def) });
    }
    catch(e) {
      let t = typeof e == `function` ? e : () => e;
      return new rK({
        ...J(this._def),
        innerType: this,
        catchValue: t,
        typeName: Y.ZodCatch,
      });
    }
    describe(e) {
      let t = this.constructor;
      return new t({ ...this._def, description: e });
    }
    pipe(e) {
      return oK.create(this, e);
    }
    readonly() {
      return sK.create(this);
    }
    isOptional() {
      return this.safeParse(void 0).success;
    }
    isNullable() {
      return this.safeParse(null).success;
    }
  },
  ZW = /^c[^\s-]{8,}$/i,
  QW = /^[0-9a-z]+$/,
  $W = /^[0-9A-HJKMNP-TV-Z]{26}$/i,
  eG =
    /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i,
  tG = /^[a-z0-9_-]{21}$/i,
  nG = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/,
  rG =
    /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/,
  iG =
    /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i,
  aG = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`,
  oG,
  sG =
    /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/,
  cG =
    /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/,
  lG =
    /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/,
  uG =
    /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/,
  dG = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/,
  fG = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/,
  pG = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`,
  mG = RegExp(`^${pG}$`);
function hG(e) {
  let t = `[0-5]\\d`;
  e.precision
    ? (t = `${t}\\.\\d{${e.precision}}`)
    : (e.precision ?? (t = `${t}(\\.\\d+)?`));
  let n = e.precision ? `+` : `?`;
  return `([01]\\d|2[0-3]):[0-5]\\d(:${t})${n}`;
}
function gG(e) {
  return RegExp(`^${hG(e)}$`);
}
function _G(e) {
  let t = `${pG}T${hG(e)}`,
    n = [];
  return (
    n.push(e.local ? `Z?` : `Z`),
    e.offset && n.push(`([+-]\\d{2}:?\\d{2})`),
    (t = `${t}(${n.join(`|`)})`),
    RegExp(`^${t}$`)
  );
}
function vG(e, t) {
  return !!(
    ((t === `v4` || !t) && sG.test(e)) ||
    ((t === `v6` || !t) && lG.test(e))
  );
}
function yG(e, t) {
  if (!nG.test(e)) return !1;
  try {
    let [n] = e.split(`.`);
    if (!n) return !1;
    let r = n
        .replace(/-/g, `+`)
        .replace(/_/g, `/`)
        .padEnd(n.length + ((4 - (n.length % 4)) % 4), `=`),
      i = JSON.parse(atob(r));
    return !(
      typeof i != `object` ||
      !i ||
      (`typ` in i && i?.typ !== `JWT`) ||
      !i.alg ||
      (t && i.alg !== t)
    );
  } catch {
    return !1;
  }
}
function bG(e, t) {
  return !!(
    ((t === `v4` || !t) && cG.test(e)) ||
    ((t === `v6` || !t) && uG.test(e))
  );
}
var xG = class e extends XW {
  _parse(e) {
    if (
      (this._def.coerce && (e.data = String(e.data)),
      this._getType(e) !== U.string)
    ) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.string,
          received: t.parsedType,
        }),
        K
      );
    }
    let t = new VW(),
      n;
    for (let r of this._def.checks)
      if (r.kind === `min`)
        e.data.length < r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            code: W.too_small,
            minimum: r.value,
            type: `string`,
            inclusive: !0,
            exact: !1,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `max`)
        e.data.length > r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            code: W.too_big,
            maximum: r.value,
            type: `string`,
            inclusive: !0,
            exact: !1,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `length`) {
        let i = e.data.length > r.value,
          a = e.data.length < r.value;
        (i || a) &&
          ((n = this._getOrReturnCtx(e, n)),
          i
            ? G(n, {
                code: W.too_big,
                maximum: r.value,
                type: `string`,
                inclusive: !0,
                exact: !0,
                message: r.message,
              })
            : a &&
              G(n, {
                code: W.too_small,
                minimum: r.value,
                type: `string`,
                inclusive: !0,
                exact: !0,
                message: r.message,
              }),
          t.dirty());
      } else if (r.kind === `email`)
        iG.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `email`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `emoji`)
        ((oG ||= new RegExp(aG, `u`)),
          oG.test(e.data) ||
            ((n = this._getOrReturnCtx(e, n)),
            G(n, {
              validation: `emoji`,
              code: W.invalid_string,
              message: r.message,
            }),
            t.dirty()));
      else if (r.kind === `uuid`)
        eG.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `uuid`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `nanoid`)
        tG.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `nanoid`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `cuid`)
        ZW.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `cuid`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `cuid2`)
        QW.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `cuid2`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `ulid`)
        $W.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            validation: `ulid`,
            code: W.invalid_string,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `url`)
        try {
          new URL(e.data);
        } catch {
          ((n = this._getOrReturnCtx(e, n)),
            G(n, {
              validation: `url`,
              code: W.invalid_string,
              message: r.message,
            }),
            t.dirty());
        }
      else
        r.kind === `regex`
          ? ((r.regex.lastIndex = 0),
            r.regex.test(e.data) ||
              ((n = this._getOrReturnCtx(e, n)),
              G(n, {
                validation: `regex`,
                code: W.invalid_string,
                message: r.message,
              }),
              t.dirty()))
          : r.kind === `trim`
            ? (e.data = e.data.trim())
            : r.kind === `includes`
              ? e.data.includes(r.value, r.position) ||
                ((n = this._getOrReturnCtx(e, n)),
                G(n, {
                  code: W.invalid_string,
                  validation: { includes: r.value, position: r.position },
                  message: r.message,
                }),
                t.dirty())
              : r.kind === `toLowerCase`
                ? (e.data = e.data.toLowerCase())
                : r.kind === `toUpperCase`
                  ? (e.data = e.data.toUpperCase())
                  : r.kind === `startsWith`
                    ? e.data.startsWith(r.value) ||
                      ((n = this._getOrReturnCtx(e, n)),
                      G(n, {
                        code: W.invalid_string,
                        validation: { startsWith: r.value },
                        message: r.message,
                      }),
                      t.dirty())
                    : r.kind === `endsWith`
                      ? e.data.endsWith(r.value) ||
                        ((n = this._getOrReturnCtx(e, n)),
                        G(n, {
                          code: W.invalid_string,
                          validation: { endsWith: r.value },
                          message: r.message,
                        }),
                        t.dirty())
                      : r.kind === `datetime`
                        ? _G(r).test(e.data) ||
                          ((n = this._getOrReturnCtx(e, n)),
                          G(n, {
                            code: W.invalid_string,
                            validation: `datetime`,
                            message: r.message,
                          }),
                          t.dirty())
                        : r.kind === `date`
                          ? mG.test(e.data) ||
                            ((n = this._getOrReturnCtx(e, n)),
                            G(n, {
                              code: W.invalid_string,
                              validation: `date`,
                              message: r.message,
                            }),
                            t.dirty())
                          : r.kind === `time`
                            ? gG(r).test(e.data) ||
                              ((n = this._getOrReturnCtx(e, n)),
                              G(n, {
                                code: W.invalid_string,
                                validation: `time`,
                                message: r.message,
                              }),
                              t.dirty())
                            : r.kind === `duration`
                              ? rG.test(e.data) ||
                                ((n = this._getOrReturnCtx(e, n)),
                                G(n, {
                                  validation: `duration`,
                                  code: W.invalid_string,
                                  message: r.message,
                                }),
                                t.dirty())
                              : r.kind === `ip`
                                ? vG(e.data, r.version) ||
                                  ((n = this._getOrReturnCtx(e, n)),
                                  G(n, {
                                    validation: `ip`,
                                    code: W.invalid_string,
                                    message: r.message,
                                  }),
                                  t.dirty())
                                : r.kind === `jwt`
                                  ? yG(e.data, r.alg) ||
                                    ((n = this._getOrReturnCtx(e, n)),
                                    G(n, {
                                      validation: `jwt`,
                                      code: W.invalid_string,
                                      message: r.message,
                                    }),
                                    t.dirty())
                                  : r.kind === `cidr`
                                    ? bG(e.data, r.version) ||
                                      ((n = this._getOrReturnCtx(e, n)),
                                      G(n, {
                                        validation: `cidr`,
                                        code: W.invalid_string,
                                        message: r.message,
                                      }),
                                      t.dirty())
                                    : r.kind === `base64`
                                      ? dG.test(e.data) ||
                                        ((n = this._getOrReturnCtx(e, n)),
                                        G(n, {
                                          validation: `base64`,
                                          code: W.invalid_string,
                                          message: r.message,
                                        }),
                                        t.dirty())
                                      : r.kind === `base64url`
                                        ? fG.test(e.data) ||
                                          ((n = this._getOrReturnCtx(e, n)),
                                          G(n, {
                                            validation: `base64url`,
                                            code: W.invalid_string,
                                            message: r.message,
                                          }),
                                          t.dirty())
                                        : NW.assertNever(r);
    return { status: t.value, value: e.data };
  }
  _regex(e, t, n) {
    return this.refinement((t) => e.test(t), {
      validation: t,
      code: W.invalid_string,
      ...q.errToObj(n),
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  email(e) {
    return this._addCheck({ kind: `email`, ...q.errToObj(e) });
  }
  url(e) {
    return this._addCheck({ kind: `url`, ...q.errToObj(e) });
  }
  emoji(e) {
    return this._addCheck({ kind: `emoji`, ...q.errToObj(e) });
  }
  uuid(e) {
    return this._addCheck({ kind: `uuid`, ...q.errToObj(e) });
  }
  nanoid(e) {
    return this._addCheck({ kind: `nanoid`, ...q.errToObj(e) });
  }
  cuid(e) {
    return this._addCheck({ kind: `cuid`, ...q.errToObj(e) });
  }
  cuid2(e) {
    return this._addCheck({ kind: `cuid2`, ...q.errToObj(e) });
  }
  ulid(e) {
    return this._addCheck({ kind: `ulid`, ...q.errToObj(e) });
  }
  base64(e) {
    return this._addCheck({ kind: `base64`, ...q.errToObj(e) });
  }
  base64url(e) {
    return this._addCheck({ kind: `base64url`, ...q.errToObj(e) });
  }
  jwt(e) {
    return this._addCheck({ kind: `jwt`, ...q.errToObj(e) });
  }
  ip(e) {
    return this._addCheck({ kind: `ip`, ...q.errToObj(e) });
  }
  cidr(e) {
    return this._addCheck({ kind: `cidr`, ...q.errToObj(e) });
  }
  datetime(e) {
    return typeof e == `string`
      ? this._addCheck({
          kind: `datetime`,
          precision: null,
          offset: !1,
          local: !1,
          message: e,
        })
      : this._addCheck({
          kind: `datetime`,
          precision: e?.precision === void 0 ? null : e?.precision,
          offset: e?.offset ?? !1,
          local: e?.local ?? !1,
          ...q.errToObj(e?.message),
        });
  }
  date(e) {
    return this._addCheck({ kind: `date`, message: e });
  }
  time(e) {
    return typeof e == `string`
      ? this._addCheck({ kind: `time`, precision: null, message: e })
      : this._addCheck({
          kind: `time`,
          precision: e?.precision === void 0 ? null : e?.precision,
          ...q.errToObj(e?.message),
        });
  }
  duration(e) {
    return this._addCheck({ kind: `duration`, ...q.errToObj(e) });
  }
  regex(e, t) {
    return this._addCheck({ kind: `regex`, regex: e, ...q.errToObj(t) });
  }
  includes(e, t) {
    return this._addCheck({
      kind: `includes`,
      value: e,
      position: t?.position,
      ...q.errToObj(t?.message),
    });
  }
  startsWith(e, t) {
    return this._addCheck({ kind: `startsWith`, value: e, ...q.errToObj(t) });
  }
  endsWith(e, t) {
    return this._addCheck({ kind: `endsWith`, value: e, ...q.errToObj(t) });
  }
  min(e, t) {
    return this._addCheck({ kind: `min`, value: e, ...q.errToObj(t) });
  }
  max(e, t) {
    return this._addCheck({ kind: `max`, value: e, ...q.errToObj(t) });
  }
  length(e, t) {
    return this._addCheck({ kind: `length`, value: e, ...q.errToObj(t) });
  }
  nonempty(e) {
    return this.min(1, q.errToObj(e));
  }
  trim() {
    return new e({
      ...this._def,
      checks: [...this._def.checks, { kind: `trim` }],
    });
  }
  toLowerCase() {
    return new e({
      ...this._def,
      checks: [...this._def.checks, { kind: `toLowerCase` }],
    });
  }
  toUpperCase() {
    return new e({
      ...this._def,
      checks: [...this._def.checks, { kind: `toUpperCase` }],
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((e) => e.kind === `datetime`);
  }
  get isDate() {
    return !!this._def.checks.find((e) => e.kind === `date`);
  }
  get isTime() {
    return !!this._def.checks.find((e) => e.kind === `time`);
  }
  get isDuration() {
    return !!this._def.checks.find((e) => e.kind === `duration`);
  }
  get isEmail() {
    return !!this._def.checks.find((e) => e.kind === `email`);
  }
  get isURL() {
    return !!this._def.checks.find((e) => e.kind === `url`);
  }
  get isEmoji() {
    return !!this._def.checks.find((e) => e.kind === `emoji`);
  }
  get isUUID() {
    return !!this._def.checks.find((e) => e.kind === `uuid`);
  }
  get isNANOID() {
    return !!this._def.checks.find((e) => e.kind === `nanoid`);
  }
  get isCUID() {
    return !!this._def.checks.find((e) => e.kind === `cuid`);
  }
  get isCUID2() {
    return !!this._def.checks.find((e) => e.kind === `cuid2`);
  }
  get isULID() {
    return !!this._def.checks.find((e) => e.kind === `ulid`);
  }
  get isIP() {
    return !!this._def.checks.find((e) => e.kind === `ip`);
  }
  get isCIDR() {
    return !!this._def.checks.find((e) => e.kind === `cidr`);
  }
  get isBase64() {
    return !!this._def.checks.find((e) => e.kind === `base64`);
  }
  get isBase64url() {
    return !!this._def.checks.find((e) => e.kind === `base64url`);
  }
  get minLength() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxLength() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
};
xG.create = (e) =>
  new xG({
    checks: [],
    typeName: Y.ZodString,
    coerce: e?.coerce ?? !1,
    ...J(e),
  });
function SG(e, t) {
  let n = (e.toString().split(`.`)[1] || ``).length,
    r = (t.toString().split(`.`)[1] || ``).length,
    i = n > r ? n : r;
  return (
    (Number.parseInt(e.toFixed(i).replace(`.`, ``)) %
      Number.parseInt(t.toFixed(i).replace(`.`, ``))) /
    10 ** i
  );
}
var CG = class e extends XW {
  constructor() {
    (super(...arguments),
      (this.min = this.gte),
      (this.max = this.lte),
      (this.step = this.multipleOf));
  }
  _parse(e) {
    if (
      (this._def.coerce && (e.data = Number(e.data)),
      this._getType(e) !== U.number)
    ) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.number,
          received: t.parsedType,
        }),
        K
      );
    }
    let t,
      n = new VW();
    for (let r of this._def.checks)
      r.kind === `int`
        ? NW.isInteger(e.data) ||
          ((t = this._getOrReturnCtx(e, t)),
          G(t, {
            code: W.invalid_type,
            expected: `integer`,
            received: `float`,
            message: r.message,
          }),
          n.dirty())
        : r.kind === `min`
          ? (r.inclusive ? e.data < r.value : e.data <= r.value) &&
            ((t = this._getOrReturnCtx(e, t)),
            G(t, {
              code: W.too_small,
              minimum: r.value,
              type: `number`,
              inclusive: r.inclusive,
              exact: !1,
              message: r.message,
            }),
            n.dirty())
          : r.kind === `max`
            ? (r.inclusive ? e.data > r.value : e.data >= r.value) &&
              ((t = this._getOrReturnCtx(e, t)),
              G(t, {
                code: W.too_big,
                maximum: r.value,
                type: `number`,
                inclusive: r.inclusive,
                exact: !1,
                message: r.message,
              }),
              n.dirty())
            : r.kind === `multipleOf`
              ? SG(e.data, r.value) !== 0 &&
                ((t = this._getOrReturnCtx(e, t)),
                G(t, {
                  code: W.not_multiple_of,
                  multipleOf: r.value,
                  message: r.message,
                }),
                n.dirty())
              : r.kind === `finite`
                ? Number.isFinite(e.data) ||
                  ((t = this._getOrReturnCtx(e, t)),
                  G(t, { code: W.not_finite, message: r.message }),
                  n.dirty())
                : NW.assertNever(r);
    return { status: n.value, value: e.data };
  }
  gte(e, t) {
    return this.setLimit(`min`, e, !0, q.toString(t));
  }
  gt(e, t) {
    return this.setLimit(`min`, e, !1, q.toString(t));
  }
  lte(e, t) {
    return this.setLimit(`max`, e, !0, q.toString(t));
  }
  lt(e, t) {
    return this.setLimit(`max`, e, !1, q.toString(t));
  }
  setLimit(t, n, r, i) {
    return new e({
      ...this._def,
      checks: [
        ...this._def.checks,
        { kind: t, value: n, inclusive: r, message: q.toString(i) },
      ],
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  int(e) {
    return this._addCheck({ kind: `int`, message: q.toString(e) });
  }
  positive(e) {
    return this._addCheck({
      kind: `min`,
      value: 0,
      inclusive: !1,
      message: q.toString(e),
    });
  }
  negative(e) {
    return this._addCheck({
      kind: `max`,
      value: 0,
      inclusive: !1,
      message: q.toString(e),
    });
  }
  nonpositive(e) {
    return this._addCheck({
      kind: `max`,
      value: 0,
      inclusive: !0,
      message: q.toString(e),
    });
  }
  nonnegative(e) {
    return this._addCheck({
      kind: `min`,
      value: 0,
      inclusive: !0,
      message: q.toString(e),
    });
  }
  multipleOf(e, t) {
    return this._addCheck({
      kind: `multipleOf`,
      value: e,
      message: q.toString(t),
    });
  }
  finite(e) {
    return this._addCheck({ kind: `finite`, message: q.toString(e) });
  }
  safe(e) {
    return this._addCheck({
      kind: `min`,
      inclusive: !0,
      value: -(2 ** 53 - 1),
      message: q.toString(e),
    })._addCheck({
      kind: `max`,
      inclusive: !0,
      value: 2 ** 53 - 1,
      message: q.toString(e),
    });
  }
  get minValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
  get isInt() {
    return !!this._def.checks.find(
      (e) =>
        e.kind === `int` || (e.kind === `multipleOf` && NW.isInteger(e.value)),
    );
  }
  get isFinite() {
    let e = null,
      t = null;
    for (let n of this._def.checks)
      if (n.kind === `finite` || n.kind === `int` || n.kind === `multipleOf`)
        return !0;
      else
        n.kind === `min`
          ? (t === null || n.value > t) && (t = n.value)
          : n.kind === `max` && (e === null || n.value < e) && (e = n.value);
    return Number.isFinite(t) && Number.isFinite(e);
  }
};
CG.create = (e) =>
  new CG({
    checks: [],
    typeName: Y.ZodNumber,
    coerce: e?.coerce || !1,
    ...J(e),
  });
var wG = class e extends XW {
  constructor() {
    (super(...arguments), (this.min = this.gte), (this.max = this.lte));
  }
  _parse(e) {
    if (this._def.coerce)
      try {
        e.data = BigInt(e.data);
      } catch {
        return this._getInvalidInput(e);
      }
    if (this._getType(e) !== U.bigint) return this._getInvalidInput(e);
    let t,
      n = new VW();
    for (let r of this._def.checks)
      r.kind === `min`
        ? (r.inclusive ? e.data < r.value : e.data <= r.value) &&
          ((t = this._getOrReturnCtx(e, t)),
          G(t, {
            code: W.too_small,
            type: `bigint`,
            minimum: r.value,
            inclusive: r.inclusive,
            message: r.message,
          }),
          n.dirty())
        : r.kind === `max`
          ? (r.inclusive ? e.data > r.value : e.data >= r.value) &&
            ((t = this._getOrReturnCtx(e, t)),
            G(t, {
              code: W.too_big,
              type: `bigint`,
              maximum: r.value,
              inclusive: r.inclusive,
              message: r.message,
            }),
            n.dirty())
          : r.kind === `multipleOf`
            ? e.data % r.value !== BigInt(0) &&
              ((t = this._getOrReturnCtx(e, t)),
              G(t, {
                code: W.not_multiple_of,
                multipleOf: r.value,
                message: r.message,
              }),
              n.dirty())
            : NW.assertNever(r);
    return { status: n.value, value: e.data };
  }
  _getInvalidInput(e) {
    let t = this._getOrReturnCtx(e);
    return (
      G(t, {
        code: W.invalid_type,
        expected: U.bigint,
        received: t.parsedType,
      }),
      K
    );
  }
  gte(e, t) {
    return this.setLimit(`min`, e, !0, q.toString(t));
  }
  gt(e, t) {
    return this.setLimit(`min`, e, !1, q.toString(t));
  }
  lte(e, t) {
    return this.setLimit(`max`, e, !0, q.toString(t));
  }
  lt(e, t) {
    return this.setLimit(`max`, e, !1, q.toString(t));
  }
  setLimit(t, n, r, i) {
    return new e({
      ...this._def,
      checks: [
        ...this._def.checks,
        { kind: t, value: n, inclusive: r, message: q.toString(i) },
      ],
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  positive(e) {
    return this._addCheck({
      kind: `min`,
      value: BigInt(0),
      inclusive: !1,
      message: q.toString(e),
    });
  }
  negative(e) {
    return this._addCheck({
      kind: `max`,
      value: BigInt(0),
      inclusive: !1,
      message: q.toString(e),
    });
  }
  nonpositive(e) {
    return this._addCheck({
      kind: `max`,
      value: BigInt(0),
      inclusive: !0,
      message: q.toString(e),
    });
  }
  nonnegative(e) {
    return this._addCheck({
      kind: `min`,
      value: BigInt(0),
      inclusive: !0,
      message: q.toString(e),
    });
  }
  multipleOf(e, t) {
    return this._addCheck({
      kind: `multipleOf`,
      value: e,
      message: q.toString(t),
    });
  }
  get minValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
};
wG.create = (e) =>
  new wG({
    checks: [],
    typeName: Y.ZodBigInt,
    coerce: e?.coerce ?? !1,
    ...J(e),
  });
var TG = class extends XW {
  _parse(e) {
    if (
      (this._def.coerce && (e.data = !!e.data), this._getType(e) !== U.boolean)
    ) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.boolean,
          received: t.parsedType,
        }),
        K
      );
    }
    return UW(e.data);
  }
};
TG.create = (e) =>
  new TG({ typeName: Y.ZodBoolean, coerce: e?.coerce || !1, ...J(e) });
var EG = class e extends XW {
  _parse(e) {
    if (
      (this._def.coerce && (e.data = new Date(e.data)),
      this._getType(e) !== U.date)
    ) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.date,
          received: t.parsedType,
        }),
        K
      );
    }
    if (Number.isNaN(e.data.getTime()))
      return (G(this._getOrReturnCtx(e), { code: W.invalid_date }), K);
    let t = new VW(),
      n;
    for (let r of this._def.checks)
      r.kind === `min`
        ? e.data.getTime() < r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          G(n, {
            code: W.too_small,
            message: r.message,
            inclusive: !0,
            exact: !1,
            minimum: r.value,
            type: `date`,
          }),
          t.dirty())
        : r.kind === `max`
          ? e.data.getTime() > r.value &&
            ((n = this._getOrReturnCtx(e, n)),
            G(n, {
              code: W.too_big,
              message: r.message,
              inclusive: !0,
              exact: !1,
              maximum: r.value,
              type: `date`,
            }),
            t.dirty())
          : NW.assertNever(r);
    return { status: t.value, value: new Date(e.data.getTime()) };
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  min(e, t) {
    return this._addCheck({
      kind: `min`,
      value: e.getTime(),
      message: q.toString(t),
    });
  }
  max(e, t) {
    return this._addCheck({
      kind: `max`,
      value: e.getTime(),
      message: q.toString(t),
    });
  }
  get minDate() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e == null ? null : new Date(e);
  }
  get maxDate() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e == null ? null : new Date(e);
  }
};
EG.create = (e) =>
  new EG({ checks: [], coerce: e?.coerce || !1, typeName: Y.ZodDate, ...J(e) });
var DG = class extends XW {
  _parse(e) {
    if (this._getType(e) !== U.symbol) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.symbol,
          received: t.parsedType,
        }),
        K
      );
    }
    return UW(e.data);
  }
};
DG.create = (e) => new DG({ typeName: Y.ZodSymbol, ...J(e) });
var OG = class extends XW {
  _parse(e) {
    if (this._getType(e) !== U.undefined) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.undefined,
          received: t.parsedType,
        }),
        K
      );
    }
    return UW(e.data);
  }
};
OG.create = (e) => new OG({ typeName: Y.ZodUndefined, ...J(e) });
var kG = class extends XW {
  _parse(e) {
    if (this._getType(e) !== U.null) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.null,
          received: t.parsedType,
        }),
        K
      );
    }
    return UW(e.data);
  }
};
kG.create = (e) => new kG({ typeName: Y.ZodNull, ...J(e) });
var AG = class extends XW {
  constructor() {
    (super(...arguments), (this._any = !0));
  }
  _parse(e) {
    return UW(e.data);
  }
};
AG.create = (e) => new AG({ typeName: Y.ZodAny, ...J(e) });
var jG = class extends XW {
  constructor() {
    (super(...arguments), (this._unknown = !0));
  }
  _parse(e) {
    return UW(e.data);
  }
};
jG.create = (e) => new jG({ typeName: Y.ZodUnknown, ...J(e) });
var MG = class extends XW {
  _parse(e) {
    let t = this._getOrReturnCtx(e);
    return (
      G(t, { code: W.invalid_type, expected: U.never, received: t.parsedType }),
      K
    );
  }
};
MG.create = (e) => new MG({ typeName: Y.ZodNever, ...J(e) });
var NG = class extends XW {
  _parse(e) {
    if (this._getType(e) !== U.undefined) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.void,
          received: t.parsedType,
        }),
        K
      );
    }
    return UW(e.data);
  }
};
NG.create = (e) => new NG({ typeName: Y.ZodVoid, ...J(e) });
var PG = class e extends XW {
  _parse(e) {
    let { ctx: t, status: n } = this._processInputParams(e),
      r = this._def;
    if (t.parsedType !== U.array)
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.array,
          received: t.parsedType,
        }),
        K
      );
    if (r.exactLength !== null) {
      let e = t.data.length > r.exactLength.value,
        i = t.data.length < r.exactLength.value;
      (e || i) &&
        (G(t, {
          code: e ? W.too_big : W.too_small,
          minimum: i ? r.exactLength.value : void 0,
          maximum: e ? r.exactLength.value : void 0,
          type: `array`,
          inclusive: !0,
          exact: !0,
          message: r.exactLength.message,
        }),
        n.dirty());
    }
    if (
      (r.minLength !== null &&
        t.data.length < r.minLength.value &&
        (G(t, {
          code: W.too_small,
          minimum: r.minLength.value,
          type: `array`,
          inclusive: !0,
          exact: !1,
          message: r.minLength.message,
        }),
        n.dirty()),
      r.maxLength !== null &&
        t.data.length > r.maxLength.value &&
        (G(t, {
          code: W.too_big,
          maximum: r.maxLength.value,
          type: `array`,
          inclusive: !0,
          exact: !1,
          message: r.maxLength.message,
        }),
        n.dirty()),
      t.common.async)
    )
      return Promise.all(
        [...t.data].map((e, n) => r.type._parseAsync(new JW(t, e, t.path, n))),
      ).then((e) => VW.mergeArray(n, e));
    let i = [...t.data].map((e, n) =>
      r.type._parseSync(new JW(t, e, t.path, n)),
    );
    return VW.mergeArray(n, i);
  }
  get element() {
    return this._def.type;
  }
  min(t, n) {
    return new e({
      ...this._def,
      minLength: { value: t, message: q.toString(n) },
    });
  }
  max(t, n) {
    return new e({
      ...this._def,
      maxLength: { value: t, message: q.toString(n) },
    });
  }
  length(t, n) {
    return new e({
      ...this._def,
      exactLength: { value: t, message: q.toString(n) },
    });
  }
  nonempty(e) {
    return this.min(1, e);
  }
};
PG.create = (e, t) =>
  new PG({
    type: e,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: Y.ZodArray,
    ...J(t),
  });
function FG(e) {
  if (e instanceof IG) {
    let t = {};
    for (let n in e.shape) {
      let r = e.shape[n];
      t[n] = eK.create(FG(r));
    }
    return new IG({ ...e._def, shape: () => t });
  } else if (e instanceof PG) return new PG({ ...e._def, type: FG(e.element) });
  else if (e instanceof eK) return eK.create(FG(e.unwrap()));
  else if (e instanceof tK) return tK.create(FG(e.unwrap()));
  else if (e instanceof HG) return HG.create(e.items.map((e) => FG(e)));
  else return e;
}
var IG = class e extends XW {
  constructor() {
    (super(...arguments),
      (this._cached = null),
      (this.nonstrict = this.passthrough),
      (this.augment = this.extend));
  }
  _getCached() {
    if (this._cached !== null) return this._cached;
    let e = this._def.shape();
    return (
      (this._cached = { shape: e, keys: NW.objectKeys(e) }),
      this._cached
    );
  }
  _parse(e) {
    if (this._getType(e) !== U.object) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          code: W.invalid_type,
          expected: U.object,
          received: t.parsedType,
        }),
        K
      );
    }
    let { status: t, ctx: n } = this._processInputParams(e),
      { shape: r, keys: i } = this._getCached(),
      a = [];
    if (
      !(this._def.catchall instanceof MG && this._def.unknownKeys === `strip`)
    )
      for (let e in n.data) i.includes(e) || a.push(e);
    let o = [];
    for (let e of i) {
      let t = r[e],
        i = n.data[e];
      o.push({
        key: { status: `valid`, value: e },
        value: t._parse(new JW(n, i, n.path, e)),
        alwaysSet: e in n.data,
      });
    }
    if (this._def.catchall instanceof MG) {
      let e = this._def.unknownKeys;
      if (e === `passthrough`)
        for (let e of a)
          o.push({
            key: { status: `valid`, value: e },
            value: { status: `valid`, value: n.data[e] },
          });
      else if (e === `strict`)
        a.length > 0 &&
          (G(n, { code: W.unrecognized_keys, keys: a }), t.dirty());
      else if (e !== `strip`)
        throw Error(`Internal ZodObject error: invalid unknownKeys value.`);
    } else {
      let e = this._def.catchall;
      for (let t of a) {
        let r = n.data[t];
        o.push({
          key: { status: `valid`, value: t },
          value: e._parse(new JW(n, r, n.path, t)),
          alwaysSet: t in n.data,
        });
      }
    }
    return n.common.async
      ? Promise.resolve()
          .then(async () => {
            let e = [];
            for (let t of o) {
              let n = await t.key,
                r = await t.value;
              e.push({ key: n, value: r, alwaysSet: t.alwaysSet });
            }
            return e;
          })
          .then((e) => VW.mergeObjectSync(t, e))
      : VW.mergeObjectSync(t, o);
  }
  get shape() {
    return this._def.shape();
  }
  strict(t) {
    return (
      q.errToObj,
      new e({
        ...this._def,
        unknownKeys: `strict`,
        ...(t === void 0
          ? {}
          : {
              errorMap: (e, n) => {
                let r = this._def.errorMap?.(e, n).message ?? n.defaultError;
                return e.code === `unrecognized_keys`
                  ? { message: q.errToObj(t).message ?? r }
                  : { message: r };
              },
            }),
      })
    );
  }
  strip() {
    return new e({ ...this._def, unknownKeys: `strip` });
  }
  passthrough() {
    return new e({ ...this._def, unknownKeys: `passthrough` });
  }
  extend(t) {
    return new e({
      ...this._def,
      shape: () => ({ ...this._def.shape(), ...t }),
    });
  }
  merge(t) {
    return new e({
      unknownKeys: t._def.unknownKeys,
      catchall: t._def.catchall,
      shape: () => ({ ...this._def.shape(), ...t._def.shape() }),
      typeName: Y.ZodObject,
    });
  }
  setKey(e, t) {
    return this.augment({ [e]: t });
  }
  catchall(t) {
    return new e({ ...this._def, catchall: t });
  }
  pick(t) {
    let n = {};
    for (let e of NW.objectKeys(t))
      t[e] && this.shape[e] && (n[e] = this.shape[e]);
    return new e({ ...this._def, shape: () => n });
  }
  omit(t) {
    let n = {};
    for (let e of NW.objectKeys(this.shape)) t[e] || (n[e] = this.shape[e]);
    return new e({ ...this._def, shape: () => n });
  }
  deepPartial() {
    return FG(this);
  }
  partial(t) {
    let n = {};
    for (let e of NW.objectKeys(this.shape)) {
      let r = this.shape[e];
      t && !t[e] ? (n[e] = r) : (n[e] = r.optional());
    }
    return new e({ ...this._def, shape: () => n });
  }
  required(t) {
    let n = {};
    for (let e of NW.objectKeys(this.shape))
      if (t && !t[e]) n[e] = this.shape[e];
      else {
        let t = this.shape[e];
        for (; t instanceof eK; ) t = t._def.innerType;
        n[e] = t;
      }
    return new e({ ...this._def, shape: () => n });
  }
  keyof() {
    return YG(NW.objectKeys(this.shape));
  }
};
((IG.create = (e, t) =>
  new IG({
    shape: () => e,
    unknownKeys: `strip`,
    catchall: MG.create(),
    typeName: Y.ZodObject,
    ...J(t),
  })),
  (IG.strictCreate = (e, t) =>
    new IG({
      shape: () => e,
      unknownKeys: `strict`,
      catchall: MG.create(),
      typeName: Y.ZodObject,
      ...J(t),
    })),
  (IG.lazycreate = (e, t) =>
    new IG({
      shape: e,
      unknownKeys: `strip`,
      catchall: MG.create(),
      typeName: Y.ZodObject,
      ...J(t),
    })));
var LG = class extends XW {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = this._def.options;
    function r(e) {
      for (let t of e) if (t.result.status === `valid`) return t.result;
      for (let n of e)
        if (n.result.status === `dirty`)
          return (t.common.issues.push(...n.ctx.common.issues), n.result);
      let n = e.map((e) => new IW(e.ctx.common.issues));
      return (G(t, { code: W.invalid_union, unionErrors: n }), K);
    }
    if (t.common.async)
      return Promise.all(
        n.map(async (e) => {
          let n = { ...t, common: { ...t.common, issues: [] }, parent: null };
          return {
            result: await e._parseAsync({
              data: t.data,
              path: t.path,
              parent: n,
            }),
            ctx: n,
          };
        }),
      ).then(r);
    {
      let e,
        r = [];
      for (let i of n) {
        let n = { ...t, common: { ...t.common, issues: [] }, parent: null },
          a = i._parseSync({ data: t.data, path: t.path, parent: n });
        if (a.status === `valid`) return a;
        (a.status === `dirty` && !e && (e = { result: a, ctx: n }),
          n.common.issues.length && r.push(n.common.issues));
      }
      if (e) return (t.common.issues.push(...e.ctx.common.issues), e.result);
      let i = r.map((e) => new IW(e));
      return (G(t, { code: W.invalid_union, unionErrors: i }), K);
    }
  }
  get options() {
    return this._def.options;
  }
};
LG.create = (e, t) => new LG({ options: e, typeName: Y.ZodUnion, ...J(t) });
var RG = (e) =>
    e instanceof qG
      ? RG(e.schema)
      : e instanceof $G
        ? RG(e.innerType())
        : e instanceof JG
          ? [e.value]
          : e instanceof XG
            ? e.options
            : e instanceof ZG
              ? NW.objectValues(e.enum)
              : e instanceof nK
                ? RG(e._def.innerType)
                : e instanceof OG
                  ? [void 0]
                  : e instanceof kG
                    ? [null]
                    : e instanceof eK
                      ? [void 0, ...RG(e.unwrap())]
                      : e instanceof tK
                        ? [null, ...RG(e.unwrap())]
                        : e instanceof aK || e instanceof sK
                          ? RG(e.unwrap())
                          : e instanceof rK
                            ? RG(e._def.innerType)
                            : [],
  zG = class e extends XW {
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      if (t.parsedType !== U.object)
        return (
          G(t, {
            code: W.invalid_type,
            expected: U.object,
            received: t.parsedType,
          }),
          K
        );
      let n = this.discriminator,
        r = t.data[n],
        i = this.optionsMap.get(r);
      return i
        ? t.common.async
          ? i._parseAsync({ data: t.data, path: t.path, parent: t })
          : i._parseSync({ data: t.data, path: t.path, parent: t })
        : (G(t, {
            code: W.invalid_union_discriminator,
            options: Array.from(this.optionsMap.keys()),
            path: [n],
          }),
          K);
    }
    get discriminator() {
      return this._def.discriminator;
    }
    get options() {
      return this._def.options;
    }
    get optionsMap() {
      return this._def.optionsMap;
    }
    static create(t, n, r) {
      let i = new Map();
      for (let e of n) {
        let n = RG(e.shape[t]);
        if (!n.length)
          throw Error(
            `A discriminator value for key \`${t}\` could not be extracted from all schema options`,
          );
        for (let r of n) {
          if (i.has(r))
            throw Error(
              `Discriminator property ${String(t)} has duplicate value ${String(r)}`,
            );
          i.set(r, e);
        }
      }
      return new e({
        typeName: Y.ZodDiscriminatedUnion,
        discriminator: t,
        options: n,
        optionsMap: i,
        ...J(r),
      });
    }
  };
function BG(e, t) {
  let n = FW(e),
    r = FW(t);
  if (e === t) return { valid: !0, data: e };
  if (n === U.object && r === U.object) {
    let n = NW.objectKeys(t),
      r = NW.objectKeys(e).filter((e) => n.indexOf(e) !== -1),
      i = { ...e, ...t };
    for (let n of r) {
      let r = BG(e[n], t[n]);
      if (!r.valid) return { valid: !1 };
      i[n] = r.data;
    }
    return { valid: !0, data: i };
  } else if (n === U.array && r === U.array) {
    if (e.length !== t.length) return { valid: !1 };
    let n = [];
    for (let r = 0; r < e.length; r++) {
      let i = e[r],
        a = t[r],
        o = BG(i, a);
      if (!o.valid) return { valid: !1 };
      n.push(o.data);
    }
    return { valid: !0, data: n };
  } else if (n === U.date && r === U.date && +e == +t)
    return { valid: !0, data: e };
  else return { valid: !1 };
}
var VG = class extends XW {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e),
      r = (e, r) => {
        if (WW(e) || WW(r)) return K;
        let i = BG(e.value, r.value);
        return i.valid
          ? ((GW(e) || GW(r)) && t.dirty(), { status: t.value, value: i.data })
          : (G(n, { code: W.invalid_intersection_types }), K);
      };
    return n.common.async
      ? Promise.all([
          this._def.left._parseAsync({ data: n.data, path: n.path, parent: n }),
          this._def.right._parseAsync({
            data: n.data,
            path: n.path,
            parent: n,
          }),
        ]).then(([e, t]) => r(e, t))
      : r(
          this._def.left._parseSync({ data: n.data, path: n.path, parent: n }),
          this._def.right._parseSync({ data: n.data, path: n.path, parent: n }),
        );
  }
};
VG.create = (e, t, n) =>
  new VG({ left: e, right: t, typeName: Y.ZodIntersection, ...J(n) });
var HG = class e extends XW {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e);
    if (n.parsedType !== U.array)
      return (
        G(n, {
          code: W.invalid_type,
          expected: U.array,
          received: n.parsedType,
        }),
        K
      );
    if (n.data.length < this._def.items.length)
      return (
        G(n, {
          code: W.too_small,
          minimum: this._def.items.length,
          inclusive: !0,
          exact: !1,
          type: `array`,
        }),
        K
      );
    !this._def.rest &&
      n.data.length > this._def.items.length &&
      (G(n, {
        code: W.too_big,
        maximum: this._def.items.length,
        inclusive: !0,
        exact: !1,
        type: `array`,
      }),
      t.dirty());
    let r = [...n.data]
      .map((e, t) => {
        let r = this._def.items[t] || this._def.rest;
        return r ? r._parse(new JW(n, e, n.path, t)) : null;
      })
      .filter((e) => !!e);
    return n.common.async
      ? Promise.all(r).then((e) => VW.mergeArray(t, e))
      : VW.mergeArray(t, r);
  }
  get items() {
    return this._def.items;
  }
  rest(t) {
    return new e({ ...this._def, rest: t });
  }
};
HG.create = (e, t) => {
  if (!Array.isArray(e))
    throw Error(`You must pass an array of schemas to z.tuple([ ... ])`);
  return new HG({ items: e, typeName: Y.ZodTuple, rest: null, ...J(t) });
};
var UG = class e extends XW {
    get keySchema() {
      return this._def.keyType;
    }
    get valueSchema() {
      return this._def.valueType;
    }
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.parsedType !== U.object)
        return (
          G(n, {
            code: W.invalid_type,
            expected: U.object,
            received: n.parsedType,
          }),
          K
        );
      let r = [],
        i = this._def.keyType,
        a = this._def.valueType;
      for (let e in n.data)
        r.push({
          key: i._parse(new JW(n, e, n.path, e)),
          value: a._parse(new JW(n, n.data[e], n.path, e)),
          alwaysSet: e in n.data,
        });
      return n.common.async
        ? VW.mergeObjectAsync(t, r)
        : VW.mergeObjectSync(t, r);
    }
    get element() {
      return this._def.valueType;
    }
    static create(t, n, r) {
      return n instanceof XW
        ? new e({ keyType: t, valueType: n, typeName: Y.ZodRecord, ...J(r) })
        : new e({
            keyType: xG.create(),
            valueType: t,
            typeName: Y.ZodRecord,
            ...J(n),
          });
    }
  },
  WG = class extends XW {
    get keySchema() {
      return this._def.keyType;
    }
    get valueSchema() {
      return this._def.valueType;
    }
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.parsedType !== U.map)
        return (
          G(n, {
            code: W.invalid_type,
            expected: U.map,
            received: n.parsedType,
          }),
          K
        );
      let r = this._def.keyType,
        i = this._def.valueType,
        a = [...n.data.entries()].map(([e, t], a) => ({
          key: r._parse(new JW(n, e, n.path, [a, `key`])),
          value: i._parse(new JW(n, t, n.path, [a, `value`])),
        }));
      if (n.common.async) {
        let e = new Map();
        return Promise.resolve().then(async () => {
          for (let n of a) {
            let r = await n.key,
              i = await n.value;
            if (r.status === `aborted` || i.status === `aborted`) return K;
            ((r.status === `dirty` || i.status === `dirty`) && t.dirty(),
              e.set(r.value, i.value));
          }
          return { status: t.value, value: e };
        });
      } else {
        let e = new Map();
        for (let n of a) {
          let r = n.key,
            i = n.value;
          if (r.status === `aborted` || i.status === `aborted`) return K;
          ((r.status === `dirty` || i.status === `dirty`) && t.dirty(),
            e.set(r.value, i.value));
        }
        return { status: t.value, value: e };
      }
    }
  };
WG.create = (e, t, n) =>
  new WG({ valueType: t, keyType: e, typeName: Y.ZodMap, ...J(n) });
var GG = class e extends XW {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e);
    if (n.parsedType !== U.set)
      return (
        G(n, { code: W.invalid_type, expected: U.set, received: n.parsedType }),
        K
      );
    let r = this._def;
    (r.minSize !== null &&
      n.data.size < r.minSize.value &&
      (G(n, {
        code: W.too_small,
        minimum: r.minSize.value,
        type: `set`,
        inclusive: !0,
        exact: !1,
        message: r.minSize.message,
      }),
      t.dirty()),
      r.maxSize !== null &&
        n.data.size > r.maxSize.value &&
        (G(n, {
          code: W.too_big,
          maximum: r.maxSize.value,
          type: `set`,
          inclusive: !0,
          exact: !1,
          message: r.maxSize.message,
        }),
        t.dirty()));
    let i = this._def.valueType;
    function a(e) {
      let n = new Set();
      for (let r of e) {
        if (r.status === `aborted`) return K;
        (r.status === `dirty` && t.dirty(), n.add(r.value));
      }
      return { status: t.value, value: n };
    }
    let o = [...n.data.values()].map((e, t) =>
      i._parse(new JW(n, e, n.path, t)),
    );
    return n.common.async ? Promise.all(o).then((e) => a(e)) : a(o);
  }
  min(t, n) {
    return new e({
      ...this._def,
      minSize: { value: t, message: q.toString(n) },
    });
  }
  max(t, n) {
    return new e({
      ...this._def,
      maxSize: { value: t, message: q.toString(n) },
    });
  }
  size(e, t) {
    return this.min(e, t).max(e, t);
  }
  nonempty(e) {
    return this.min(1, e);
  }
};
GG.create = (e, t) =>
  new GG({
    valueType: e,
    minSize: null,
    maxSize: null,
    typeName: Y.ZodSet,
    ...J(t),
  });
var KG = class e extends XW {
    constructor() {
      (super(...arguments), (this.validate = this.implement));
    }
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      if (t.parsedType !== U.function)
        return (
          G(t, {
            code: W.invalid_type,
            expected: U.function,
            received: t.parsedType,
          }),
          K
        );
      function n(e, n) {
        return BW({
          data: e,
          path: t.path,
          errorMaps: [
            t.common.contextualErrorMap,
            t.schemaErrorMap,
            zW(),
            LW,
          ].filter((e) => !!e),
          issueData: { code: W.invalid_arguments, argumentsError: n },
        });
      }
      function r(e, n) {
        return BW({
          data: e,
          path: t.path,
          errorMaps: [
            t.common.contextualErrorMap,
            t.schemaErrorMap,
            zW(),
            LW,
          ].filter((e) => !!e),
          issueData: { code: W.invalid_return_type, returnTypeError: n },
        });
      }
      let i = { errorMap: t.common.contextualErrorMap },
        a = t.data;
      if (this._def.returns instanceof QG) {
        let e = this;
        return UW(async function (...t) {
          let o = new IW([]),
            s = await e._def.args.parseAsync(t, i).catch((e) => {
              throw (o.addIssue(n(t, e)), o);
            }),
            c = await Reflect.apply(a, this, s);
          return await e._def.returns._def.type.parseAsync(c, i).catch((e) => {
            throw (o.addIssue(r(c, e)), o);
          });
        });
      } else {
        let e = this;
        return UW(function (...t) {
          let o = e._def.args.safeParse(t, i);
          if (!o.success) throw new IW([n(t, o.error)]);
          let s = Reflect.apply(a, this, o.data),
            c = e._def.returns.safeParse(s, i);
          if (!c.success) throw new IW([r(s, c.error)]);
          return c.data;
        });
      }
    }
    parameters() {
      return this._def.args;
    }
    returnType() {
      return this._def.returns;
    }
    args(...t) {
      return new e({ ...this._def, args: HG.create(t).rest(jG.create()) });
    }
    returns(t) {
      return new e({ ...this._def, returns: t });
    }
    implement(e) {
      return this.parse(e);
    }
    strictImplement(e) {
      return this.parse(e);
    }
    static create(t, n, r) {
      return new e({
        args: t || HG.create([]).rest(jG.create()),
        returns: n || jG.create(),
        typeName: Y.ZodFunction,
        ...J(r),
      });
    }
  },
  qG = class extends XW {
    get schema() {
      return this._def.getter();
    }
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      return this._def
        .getter()
        ._parse({ data: t.data, path: t.path, parent: t });
    }
  };
qG.create = (e, t) => new qG({ getter: e, typeName: Y.ZodLazy, ...J(t) });
var JG = class extends XW {
  _parse(e) {
    if (e.data !== this._def.value) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, {
          received: t.data,
          code: W.invalid_literal,
          expected: this._def.value,
        }),
        K
      );
    }
    return { status: `valid`, value: e.data };
  }
  get value() {
    return this._def.value;
  }
};
JG.create = (e, t) => new JG({ value: e, typeName: Y.ZodLiteral, ...J(t) });
function YG(e, t) {
  return new XG({ values: e, typeName: Y.ZodEnum, ...J(t) });
}
var XG = class e extends XW {
  _parse(e) {
    if (typeof e.data != `string`) {
      let t = this._getOrReturnCtx(e),
        n = this._def.values;
      return (
        G(t, {
          expected: NW.joinValues(n),
          received: t.parsedType,
          code: W.invalid_type,
        }),
        K
      );
    }
    if (
      ((this._cache ||= new Set(this._def.values)), !this._cache.has(e.data))
    ) {
      let t = this._getOrReturnCtx(e),
        n = this._def.values;
      return (
        G(t, { received: t.data, code: W.invalid_enum_value, options: n }),
        K
      );
    }
    return UW(e.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  get Values() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  get Enum() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  extract(t, n = this._def) {
    return e.create(t, { ...this._def, ...n });
  }
  exclude(t, n = this._def) {
    return e.create(
      this.options.filter((e) => !t.includes(e)),
      { ...this._def, ...n },
    );
  }
};
XG.create = YG;
var ZG = class extends XW {
  _parse(e) {
    let t = NW.getValidEnumValues(this._def.values),
      n = this._getOrReturnCtx(e);
    if (n.parsedType !== U.string && n.parsedType !== U.number) {
      let e = NW.objectValues(t);
      return (
        G(n, {
          expected: NW.joinValues(e),
          received: n.parsedType,
          code: W.invalid_type,
        }),
        K
      );
    }
    if (
      ((this._cache ||= new Set(NW.getValidEnumValues(this._def.values))),
      !this._cache.has(e.data))
    ) {
      let e = NW.objectValues(t);
      return (
        G(n, { received: n.data, code: W.invalid_enum_value, options: e }),
        K
      );
    }
    return UW(e.data);
  }
  get enum() {
    return this._def.values;
  }
};
ZG.create = (e, t) => new ZG({ values: e, typeName: Y.ZodNativeEnum, ...J(t) });
var QG = class extends XW {
  unwrap() {
    return this._def.type;
  }
  _parse(e) {
    let { ctx: t } = this._processInputParams(e);
    return t.parsedType !== U.promise && t.common.async === !1
      ? (G(t, {
          code: W.invalid_type,
          expected: U.promise,
          received: t.parsedType,
        }),
        K)
      : UW(
          (t.parsedType === U.promise ? t.data : Promise.resolve(t.data)).then(
            (e) =>
              this._def.type.parseAsync(e, {
                path: t.path,
                errorMap: t.common.contextualErrorMap,
              }),
          ),
        );
  }
};
QG.create = (e, t) => new QG({ type: e, typeName: Y.ZodPromise, ...J(t) });
var $G = class extends XW {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === Y.ZodEffects
      ? this._def.schema.sourceType()
      : this._def.schema;
  }
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e),
      r = this._def.effect || null,
      i = {
        addIssue: (e) => {
          (G(n, e), e.fatal ? t.abort() : t.dirty());
        },
        get path() {
          return n.path;
        },
      };
    if (((i.addIssue = i.addIssue.bind(i)), r.type === `preprocess`)) {
      let e = r.transform(n.data, i);
      if (n.common.async)
        return Promise.resolve(e).then(async (e) => {
          if (t.value === `aborted`) return K;
          let r = await this._def.schema._parseAsync({
            data: e,
            path: n.path,
            parent: n,
          });
          return r.status === `aborted`
            ? K
            : r.status === `dirty` || t.value === `dirty`
              ? HW(r.value)
              : r;
        });
      {
        if (t.value === `aborted`) return K;
        let r = this._def.schema._parseSync({
          data: e,
          path: n.path,
          parent: n,
        });
        return r.status === `aborted`
          ? K
          : r.status === `dirty` || t.value === `dirty`
            ? HW(r.value)
            : r;
      }
    }
    if (r.type === `refinement`) {
      let e = (e) => {
        let t = r.refinement(e, i);
        if (n.common.async) return Promise.resolve(t);
        if (t instanceof Promise)
          throw Error(
            `Async refinement encountered during synchronous parse operation. Use .parseAsync instead.`,
          );
        return e;
      };
      if (n.common.async === !1) {
        let r = this._def.schema._parseSync({
          data: n.data,
          path: n.path,
          parent: n,
        });
        return r.status === `aborted`
          ? K
          : (r.status === `dirty` && t.dirty(),
            e(r.value),
            { status: t.value, value: r.value });
      } else
        return this._def.schema
          ._parseAsync({ data: n.data, path: n.path, parent: n })
          .then((n) =>
            n.status === `aborted`
              ? K
              : (n.status === `dirty` && t.dirty(),
                e(n.value).then(() => ({ status: t.value, value: n.value }))),
          );
    }
    if (r.type === `transform`)
      if (n.common.async === !1) {
        let e = this._def.schema._parseSync({
          data: n.data,
          path: n.path,
          parent: n,
        });
        if (!KW(e)) return K;
        let a = r.transform(e.value, i);
        if (a instanceof Promise)
          throw Error(
            `Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`,
          );
        return { status: t.value, value: a };
      } else
        return this._def.schema
          ._parseAsync({ data: n.data, path: n.path, parent: n })
          .then((e) =>
            KW(e)
              ? Promise.resolve(r.transform(e.value, i)).then((e) => ({
                  status: t.value,
                  value: e,
                }))
              : K,
          );
    NW.assertNever(r);
  }
};
(($G.create = (e, t, n) =>
  new $G({ schema: e, typeName: Y.ZodEffects, effect: t, ...J(n) })),
  ($G.createWithPreprocess = (e, t, n) =>
    new $G({
      schema: t,
      effect: { type: `preprocess`, transform: e },
      typeName: Y.ZodEffects,
      ...J(n),
    })));
var eK = class extends XW {
  _parse(e) {
    return this._getType(e) === U.undefined
      ? UW(void 0)
      : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
};
eK.create = (e, t) =>
  new eK({ innerType: e, typeName: Y.ZodOptional, ...J(t) });
var tK = class extends XW {
  _parse(e) {
    return this._getType(e) === U.null
      ? UW(null)
      : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
};
tK.create = (e, t) =>
  new tK({ innerType: e, typeName: Y.ZodNullable, ...J(t) });
var nK = class extends XW {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = t.data;
    return (
      t.parsedType === U.undefined && (n = this._def.defaultValue()),
      this._def.innerType._parse({ data: n, path: t.path, parent: t })
    );
  }
  removeDefault() {
    return this._def.innerType;
  }
};
nK.create = (e, t) =>
  new nK({
    innerType: e,
    typeName: Y.ZodDefault,
    defaultValue: typeof t.default == `function` ? t.default : () => t.default,
    ...J(t),
  });
var rK = class extends XW {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = { ...t, common: { ...t.common, issues: [] } },
      r = this._def.innerType._parse({
        data: n.data,
        path: n.path,
        parent: { ...n },
      });
    return qW(r)
      ? r.then((e) => ({
          status: `valid`,
          value:
            e.status === `valid`
              ? e.value
              : this._def.catchValue({
                  get error() {
                    return new IW(n.common.issues);
                  },
                  input: n.data,
                }),
        }))
      : {
          status: `valid`,
          value:
            r.status === `valid`
              ? r.value
              : this._def.catchValue({
                  get error() {
                    return new IW(n.common.issues);
                  },
                  input: n.data,
                }),
        };
  }
  removeCatch() {
    return this._def.innerType;
  }
};
rK.create = (e, t) =>
  new rK({
    innerType: e,
    typeName: Y.ZodCatch,
    catchValue: typeof t.catch == `function` ? t.catch : () => t.catch,
    ...J(t),
  });
var iK = class extends XW {
  _parse(e) {
    if (this._getType(e) !== U.nan) {
      let t = this._getOrReturnCtx(e);
      return (
        G(t, { code: W.invalid_type, expected: U.nan, received: t.parsedType }),
        K
      );
    }
    return { status: `valid`, value: e.data };
  }
};
iK.create = (e) => new iK({ typeName: Y.ZodNaN, ...J(e) });
var aK = class extends XW {
    _parse(e) {
      let { ctx: t } = this._processInputParams(e),
        n = t.data;
      return this._def.type._parse({ data: n, path: t.path, parent: t });
    }
    unwrap() {
      return this._def.type;
    }
  },
  oK = class e extends XW {
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.common.async)
        return (async () => {
          let e = await this._def.in._parseAsync({
            data: n.data,
            path: n.path,
            parent: n,
          });
          return e.status === `aborted`
            ? K
            : e.status === `dirty`
              ? (t.dirty(), HW(e.value))
              : this._def.out._parseAsync({
                  data: e.value,
                  path: n.path,
                  parent: n,
                });
        })();
      {
        let e = this._def.in._parseSync({
          data: n.data,
          path: n.path,
          parent: n,
        });
        return e.status === `aborted`
          ? K
          : e.status === `dirty`
            ? (t.dirty(), { status: `dirty`, value: e.value })
            : this._def.out._parseSync({
                data: e.value,
                path: n.path,
                parent: n,
              });
      }
    }
    static create(t, n) {
      return new e({ in: t, out: n, typeName: Y.ZodPipeline });
    }
  },
  sK = class extends XW {
    _parse(e) {
      let t = this._def.innerType._parse(e),
        n = (e) => (KW(e) && (e.value = Object.freeze(e.value)), e);
      return qW(t) ? t.then((e) => n(e)) : n(t);
    }
    unwrap() {
      return this._def.innerType;
    }
  };
((sK.create = (e, t) =>
  new sK({ innerType: e, typeName: Y.ZodReadonly, ...J(t) })),
  IG.lazycreate);
var Y;
(function (e) {
  ((e.ZodString = `ZodString`),
    (e.ZodNumber = `ZodNumber`),
    (e.ZodNaN = `ZodNaN`),
    (e.ZodBigInt = `ZodBigInt`),
    (e.ZodBoolean = `ZodBoolean`),
    (e.ZodDate = `ZodDate`),
    (e.ZodSymbol = `ZodSymbol`),
    (e.ZodUndefined = `ZodUndefined`),
    (e.ZodNull = `ZodNull`),
    (e.ZodAny = `ZodAny`),
    (e.ZodUnknown = `ZodUnknown`),
    (e.ZodNever = `ZodNever`),
    (e.ZodVoid = `ZodVoid`),
    (e.ZodArray = `ZodArray`),
    (e.ZodObject = `ZodObject`),
    (e.ZodUnion = `ZodUnion`),
    (e.ZodDiscriminatedUnion = `ZodDiscriminatedUnion`),
    (e.ZodIntersection = `ZodIntersection`),
    (e.ZodTuple = `ZodTuple`),
    (e.ZodRecord = `ZodRecord`),
    (e.ZodMap = `ZodMap`),
    (e.ZodSet = `ZodSet`),
    (e.ZodFunction = `ZodFunction`),
    (e.ZodLazy = `ZodLazy`),
    (e.ZodLiteral = `ZodLiteral`),
    (e.ZodEnum = `ZodEnum`),
    (e.ZodEffects = `ZodEffects`),
    (e.ZodNativeEnum = `ZodNativeEnum`),
    (e.ZodOptional = `ZodOptional`),
    (e.ZodNullable = `ZodNullable`),
    (e.ZodDefault = `ZodDefault`),
    (e.ZodCatch = `ZodCatch`),
    (e.ZodPromise = `ZodPromise`),
    (e.ZodBranded = `ZodBranded`),
    (e.ZodPipeline = `ZodPipeline`),
    (e.ZodReadonly = `ZodReadonly`));
})((Y ||= {}));
var X = xG.create,
  Z = CG.create;
(iK.create, wG.create);
var cK = TG.create;
(EG.create, DG.create, OG.create, kG.create, AG.create);
var lK = jG.create;
(MG.create, NG.create);
var Q = PG.create,
  $ = IG.create;
(IG.strictCreate, LG.create, zG.create, VG.create, HG.create);
var uK = UG.create;
(WG.create, GG.create, KG.create, qG.create);
var dK = JG.create,
  fK = XG.create;
(ZG.create,
  QG.create,
  $G.create,
  eK.create,
  tK.create,
  $G.createWithPreprocess,
  oK.create);
var pK = $({ id: X(), description: X() }),
  mK = $({ browser: Q(pK).optional(), tab: Q(pK).optional() }),
  hK = fK([`iab`, `extension`, `cdp`]),
  gK = $({
    apiSupportOverrides: uK(cK()).optional(),
    capabilities: mK,
    id: X(),
    name: X(),
    type: hK,
    metadata: uK(X()).optional(),
  });
($({}),
  Q(gK),
  $({ id: X() }),
  $({}),
  $({ url: X().url() }),
  $({ browser_id: X() }),
  $({ id: X() }),
  $({ browser_id: X(), tab_id: X() }),
  $({}));
var _K = fK([`handoff`, `deliverable`]);
($({ browser_id: X(), keep: Q($({ tab_id: X(), status: _K })).optional() }),
  $({}));
var vK = fK([`handoff`, `deliverable`]);
($({ browser_id: X(), tab_id: X(), status: vK }),
  $({}),
  $({ browser_id: X() }),
  $({ display_truncate_max_chars: Z().int().positive().optional() }),
  $({ browser_id: X(), name: X() }),
  $({}),
  $({ browser_id: X() }),
  $({ id: X().optional() }),
  $({ browser_id: X() }),
  $({ tabs: Q($({ id: X(), url: X().optional(), title: X().optional() })) }));
var yK = fK([`html`, `text`, `domSnapshot`]);
($({
  browser_id: X(),
  urls: Q(X()),
  content_type: yK,
  timeout_ms: Z().int().positive().optional(),
}),
  $({
    results: Q($({ url: X(), title: X().nullable(), content: X().nullable() })),
  }));
var bK = $({
    readOnlyHint: cK().optional(),
    untrustedContentHint: cK().optional(),
  }),
  xK = $({
    name: X(),
    title: X().optional(),
    description: X().optional(),
    input_schema: lK(),
    annotations: bK.optional(),
    origin: X().optional(),
    pageUrl: X().optional(),
  }),
  SK = $({ browser_id: X(), tab_id: X() });
$({ tools: Q(xK) });
var CK = `webmcp_list_tools`;
function wK(e) {
  return new MW(CK, SK, e);
}
var TK = $({
  browser_id: X(),
  tab_id: X(),
  tool_name: X(),
  input: lK(),
  timeout_ms: Z().int().positive().optional(),
});
$({ result: lK() });
var EK = `webmcp_invoke_tool`;
function DK(e) {
  return new MW(EK, TK, e);
}
($({ browser_id: X(), tab_id: X(), url: X() }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({ id: X(), title: X().optional(), url: X().optional() }),
  $({ browser_id: X() }),
  $({
    tabs: Q(
      $({
        id: X(),
        title: X().optional(),
        url: X().optional(),
        lastOpened: X().optional(),
        tabGroup: X().optional(),
      }),
    ),
  }),
  $({
    browser_id: X(),
    queries: Q(X()).min(1).optional(),
    limit: Z().int().positive().optional(),
    from: X().optional(),
    to: X().optional(),
  }),
  $({ items: Q($({ url: X(), title: X().optional(), dateVisited: X() })) }));
var OK = fK([`debug`, `info`, `log`, `warn`, `error`]);
($({
  browser_id: X(),
  tab_id: X(),
  filter: X().optional(),
  levels: Q(OK).optional(),
  limit: Z().int().positive().optional(),
}),
  $({
    logs: Q(
      $({ level: OK, message: X(), timestamp: X(), url: X().optional() }),
    ),
  }));
var kK = fK([`alert`, `beforeunload`, `confirm`, `prompt`]),
  AK = $({ id: X(), type: kK });
($({ browser_id: X(), tab_id: X() }),
  $({ dialog: AK.nullable() }),
  $({
    action: fK([`accept`, `dismiss`]),
    browser_id: X(),
    dialog_id: X(),
    prompt_text: X().optional(),
    tab_id: X(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({ path: X() }));
var jK = fK([`pdf`, `md`, `xlsx`, `csv`, `docx`, `pptx`]);
($({ browser_id: X(), tab_id: X(), format: jK }),
  $({ path: X() }),
  $({ browser_id: X(), tab_id: X() }),
  $({ text: X() }),
  $({ browser_id: X(), tab_id: X(), text: X() }),
  $({}));
var MK = $({
  entries: Q(
    $({
      mime_type: X(),
      text: X().optional(),
      base64: X().optional(),
    }).superRefine((e, t) => {
      (e.text !== void 0) == (e.base64 !== void 0) &&
        t.addIssue({
          code: W.custom,
          message: `Clipboard entries must set exactly one of text or base64`,
        });
    }),
  ),
  presentation_style: fK([`unspecified`, `inline`, `attachment`]).optional(),
});
($({ browser_id: X(), tab_id: X() }), $({ items: Q(MK) }));
var NK = $({
  entries: Q(
    $({
      mime_type: X(),
      text: X().optional(),
      base64: X().optional(),
    }).superRefine((e, t) => {
      (e.text !== void 0) == (e.base64 !== void 0) &&
        t.addIssue({
          code: W.custom,
          message: `Clipboard entries must set exactly one of text or base64`,
        });
    }),
  ),
  presentation_style: fK([`unspecified`, `inline`, `attachment`]).optional(),
});
($({ browser_id: X(), tab_id: X(), items: Q(NK) }),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    fullPage: cK().optional(),
    cropX: Z().optional(),
    cropY: Z().optional(),
    cropWidth: Z().optional(),
    cropHeight: Z().optional(),
  }),
  $({ data: X() }));
var PK = $({
  browser_id: X(),
  selector: X().optional(),
  tab_id: X(),
  script: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({ value: lK().optional() });
var FK = `playwright_evaluate`;
function IK(e) {
  return new MW(FK, PK, e);
}
($({
  browser_id: X(),
  tab_id: X(),
  x: Z(),
  y: Z(),
  button: Z().optional(),
  keys: Q(X()).optional(),
}),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    x: Z(),
    y: Z(),
    timeout_ms: Z().int().positive().optional(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X(), x: Z(), y: Z(), keys: Q(X()).optional() }),
  $({}),
  $({ browser_id: X(), tab_id: X(), keys: Q(X()) }),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    path: Q($({ x: Z(), y: Z() })),
    keys: Q(X()).optional(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X(), x: Z(), y: Z(), keys: Q(X()).optional() }),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    x: Z(),
    y: Z(),
    scroll_x: Z(),
    scroll_y: Z(),
    keys: Q(X()).optional(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X(), text: X() }),
  $({}));
var LK = X({
  required_error: `node_id is required`,
  invalid_type_error: `node_id must be a string`,
}).min(1, `node_id must not be empty`);
($({ browser_id: X(), tab_id: X(), node_id: LK }),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    node_id: LK,
    timeout_ms: Z().int().positive().optional(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X(), node_id: LK }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  lK(),
  $({ browser_id: X(), tab_id: X(), keys: Q(X()) }),
  $({}),
  $({
    browser_id: X(),
    tab_id: X(),
    scroll_x: Z(),
    scroll_y: Z(),
    node_id: LK.optional(),
  }),
  $({}),
  $({ browser_id: X(), tab_id: X(), text: X() }),
  $({}));
var RK = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  modifiers: Q(
    fK([`Alt`, `Control`, `ControlOrMeta`, `Meta`, `Shift`]),
  ).optional(),
  button: fK([`left`, `right`, `middle`]).optional(),
  force: cK().optional(),
  timeout_ms: Z().int().positive().optional(),
});
$({});
var zK = `playwright_locator_click`;
function BK(e) {
  return new MW(zK, RK, e);
}
var VK = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  modifiers: Q(
    fK([`Alt`, `Control`, `ControlOrMeta`, `Meta`, `Shift`]),
  ).optional(),
  button: fK([`left`, `right`, `middle`]).optional(),
  force: cK().optional(),
  timeout_ms: Z().int().positive().optional(),
});
$({});
var HK = `playwright_locator_dblclick`;
function UK(e) {
  return new MW(HK, VK, e);
}
var WK = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({});
var GK = `playwright_locator_download_media`;
function KK(e) {
  return new MW(GK, WK, e);
}
var qK = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  value: X(),
  replace: cK(),
});
$({});
var JK = `playwright_locator_fill`;
function YK(e) {
  return new MW(JK, qK, e);
}
var XK = $({ browser_id: X(), tab_id: X(), selector: X(), value: X() });
$({});
var ZK = `playwright_locator_press`;
function QK(e) {
  return new MW(ZK, XK, e);
}
var $K = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  state: fK([`attached`, `detached`, `visible`, `hidden`]),
  timeout_ms: Z().int().positive().optional(),
});
$({});
var eq = `playwright_locator_wait_for`;
function tq(e) {
  return new MW(eq, $K, e);
}
var nq = $({ browser_id: X(), tab_id: X(), selector: X() });
$({ count: Z().int() });
var rq = `playwright_locator_count`;
function iq(e) {
  return new MW(rq, nq, e);
}
var aq = $({
    value: X().optional(),
    label: X().optional(),
    index: Z().int().nonnegative().optional(),
  }).superRefine((e, t) => {
    e.value === void 0 &&
      e.label === void 0 &&
      e.index === void 0 &&
      t.addIssue({
        code: W.custom,
        message: `Select option requires value, label, or index`,
      });
  }),
  oq = $({
    browser_id: X(),
    tab_id: X(),
    selector: X(),
    selections: Q(aq).min(1),
    timeout_ms: Z().int().positive().optional(),
  });
$({});
var sq = `playwright_locator_select_option`;
function cq(e) {
  return new MW(sq, oq, e);
}
var lq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  checked: cK(),
  force: cK().optional(),
  timeout_ms: Z().int().positive().optional(),
});
$({});
var uq = `playwright_locator_set_checked`;
function dq(e) {
  return new MW(uq, lq, e);
}
var fq = $({ browser_id: X(), tab_id: X(), selector: X() });
$({ value: cK() });
var pq = `playwright_locator_is_visible`;
function mq(e) {
  return new MW(pq, fq, e);
}
var hq = $({ browser_id: X(), tab_id: X(), selector: X() });
$({ value: cK() });
var gq = `playwright_locator_is_enabled`;
function _q(e) {
  return new MW(gq, hq, e);
}
var vq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({ values: Q(X()) });
var yq = `playwright_locator_all_text_contents`;
function bq(e) {
  return new MW(yq, vq, e);
}
var xq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({ value: X().nullable() });
var Sq = `playwright_locator_text_content`;
function Cq(e) {
  return new MW(Sq, xq, e);
}
var wq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({ value: X() });
var Tq = `playwright_locator_inner_text`;
function Eq(e) {
  return new MW(Tq, wq, e);
}
var Dq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  name: X(),
  timeout_ms: Z().int().positive().optional(),
});
$({ value: X().nullable() });
var Oq = `playwright_locator_get_attribute`;
function kq(e) {
  return new MW(Oq, Dq, e);
}
var Aq = $({
  browser_id: X(),
  tab_id: X(),
  selector: X(),
  relative_selector: X().optional(),
  timeout_ms: Z().int().positive().optional(),
});
$({
  values: Q(
    $({
      attributes: uK(X()),
      inner_text: X(),
      text_content: X().nullable(),
    }).nullable(),
  ),
});
var jq = `playwright_locator_read_all`;
function Mq(e) {
  return new MW(jq, Aq, e);
}
var Nq = fK([`load`, `domcontentloaded`, `networkidle`, `commit`]);
($({
  browser_id: X(),
  tab_id: X(),
  url: X(),
  wait_until: Nq.optional(),
  timeout_ms: Z().int().positive().optional(),
}),
  $({ url: X().optional() }));
var Pq = fK([`load`, `domcontentloaded`, `networkidle`]);
($({
  browser_id: X(),
  tab_id: X(),
  state: Pq.optional(),
  timeout_ms: Z().int().positive().optional(),
}),
  $({}),
  $({ browser_id: X(), tab_id: X(), timeout_ms: Z().int().nonnegative() }),
  $({}),
  $({ browser_id: X(), tab_id: X() }),
  $({ dom_snapshot: X() }));
var Fq = $({ x: Z(), y: Z(), width: Z(), height: Z() }),
  Iq = $({
    primary: X().nullable().optional(),
    candidates: Q(X()),
    frameSelectors: Q(X()).optional(),
  }),
  Lq = $({
    nodeId: Z().int().positive().nullable().optional(),
    tagName: X(),
    role: X().nullable().optional(),
    visibleText: X().nullable().optional(),
    ariaName: X().nullable().optional(),
    testId: X().nullable().optional(),
    boundingBox: Fq.nullable().optional(),
    preview: X(),
    selector: Iq,
  });
($({
  browser_id: X(),
  tab_id: X(),
  x: Z(),
  y: Z(),
  include_non_interactable: cK().optional(),
}),
  Q(Lq),
  $({
    browser_id: X(),
    tab_id: X(),
    x: Z(),
    y: Z(),
    include_non_interactable: cK().optional(),
  }),
  $({ data: X() }),
  $({
    browser_id: X(),
    tab_id: X(),
    timeout_ms: Z().int().positive().optional(),
  }),
  $({ download_id: X() }),
  $({
    browser_id: X(),
    tab_id: X(),
    download_id: X(),
    timeout_ms: Z().int().positive().optional(),
  }),
  $({ path: X().nullable() }),
  $({
    browser_id: X(),
    tab_id: X(),
    timeout_ms: Z().int().positive().optional(),
  }),
  $({ file_chooser_id: X(), is_multiple: cK() }),
  $({
    browser_id: X(),
    tab_id: X(),
    file_chooser_id: X(),
    files: Q(X()),
    timeout_ms: Z().int().positive().optional(),
  }),
  $({}));
function Rq(e, t) {
  let n = e instanceof Error ? e.message : String(e),
    r = Error(`${n}\n${t}`);
  return (
    e instanceof Error &&
      e.stack &&
      (r.stack = `${r.name}: ${r.message}\n${e.stack}`),
    r
  );
}
function zq(e, t) {
  let n = Bq(t);
  if (typeof e == `string`) {
    if (e.length === 0) throw Error(`locator.evaluate requires a pageFunction`);
    return `const arg = ${n};\nreturn (${e});`;
  }
  if (typeof e == `function`)
    return [
      `const arg = ${n};`,
      `const __playwrightEvaluate = (${e.toString()});`,
      `return await __playwrightEvaluate(element, arg);`,
    ].join(`
`);
  throw Error(`locator.evaluate requires a string or function`);
}
function Bq(e) {
  if (e === void 0) return `undefined`;
  try {
    let t = JSON.stringify(e);
    if (t !== void 0) return t;
  } catch (e) {
    throw Rq(e, `playwright.evaluate arg must be JSON-serializable`);
  }
  throw Error(`playwright.evaluate arg must be JSON-serializable`);
}
function Vq(e) {
  let t = Array.isArray(e) ? e : [e];
  if (!t.length)
    throw Error(`locator.selectOption requires at least one value`);
  return t.map((e) => {
    if (typeof e == `string`) return { value: e };
    if (!e || typeof e != `object`)
      throw Error(
        `locator.selectOption requires a string or { value?, label?, index? }`,
      );
    let t = {};
    if (e.value !== void 0) {
      if (typeof e.value != `string`)
        throw Error(`locator.selectOption value must be a string`);
      t.value = e.value;
    }
    if (e.label !== void 0) {
      if (typeof e.label != `string`)
        throw Error(`locator.selectOption label must be a string`);
      t.label = e.label;
    }
    if (e.index !== void 0) {
      if (!Number.isInteger(e.index) || e.index < 0)
        throw Error(
          `locator.selectOption index must be a non-negative integer`,
        );
      t.index = e.index;
    }
    if (t.value === void 0 && t.label === void 0 && t.index === void 0)
      throw Error(
        `locator.selectOption requires value, label, or index for each selection`,
      );
    return t;
  });
}
var Hq = Symbol(`browserAuthSelectorDetails`),
  Uq = class e {
    #e;
    #t;
    #n;
    #r;
    #i;
    #a;
    #o;
    constructor({
      browserId: e,
      tabId: t,
      selector: n,
      transport: r,
      collectionReadCache: i,
      collectionReadIndex: a,
      collectionRelativeSelector: o,
    }) {
      ((this.#e = e),
        (this.#t = t),
        (this.#n = n),
        (this.#r = r),
        (this.#i = i),
        (this.#a = a),
        (this.#o = o));
    }
    static browserAuthSelector(e) {
      return e[Hq]();
    }
    [Hq]() {
      return { browserId: this.#e, selector: this.#n, tabId: this.#t };
    }
    async click(e = {}) {
      try {
        (await this.#r.send({
          command: BK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            modifiers: e.modifiers,
            button: e.button,
            force: e.force,
            timeout_ms: e.timeoutMs,
          }),
          timeoutMs: e.timeoutMs,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `waiting on click for selector ${this.#n}`);
      }
    }
    async dblclick(e = {}) {
      try {
        (await this.#r.send({
          command: UK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            modifiers: e.modifiers,
            button: e.button,
            force: e.force,
            timeout_ms: e.timeoutMs,
          }),
          timeoutMs: e.timeoutMs,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `waiting on dblclick for selector ${this.#n}`);
      }
    }
    async selectOption(e, { timeoutMs: t } = {}) {
      let n = Vq(e);
      try {
        (await this.#r.send({
          command: cq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            selections: n,
            timeout_ms: t,
          }),
          timeoutMs: t,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `locator.selectOption failed for selector ${this.#n}`);
      }
    }
    async fill(e, { timeoutMs: t } = {}) {
      if (e == null) throw Error(`locator.fill requires a value`);
      try {
        (await this.#r.send({
          command: YK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            value: e,
            replace: !0,
          }),
          timeoutMs: t,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `locator.fill failed for selector ${this.#n}`);
      }
    }
    async type(e, { timeoutMs: t } = {}) {
      if (e == null) throw Error(`locator.type requires a value`);
      try {
        (await this.#r.send({
          command: YK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            value: e,
            replace: !1,
          }),
          timeoutMs: t,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `locator.type failed for selector ${this.#n}`);
      }
    }
    async press(e, { timeoutMs: t } = {}) {
      if (e == null) throw Error(`locator.press requires a value`);
      try {
        (await this.#r.send({
          command: QK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            value: e,
          }),
          timeoutMs: t,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `locator.press failed for selector ${this.#n}`);
      }
    }
    async setChecked(e, t = {}) {
      if (typeof e != `boolean`)
        throw Error(`locator.setChecked requires a boolean`);
      try {
        (await this.#r.send({
          command: dq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            checked: e,
            force: t.force,
            timeout_ms: t.timeoutMs,
          }),
          timeoutMs: t.timeoutMs,
        }),
          this.#i?.clear());
      } catch (t) {
        throw Rq(t, `locator.setChecked(${e}) failed for selector ${this.#n}`);
      }
    }
    async check(e = {}) {
      await this.setChecked(!0, e);
    }
    async uncheck(e = {}) {
      await this.setChecked(!1, e);
    }
    async waitFor({ state: e, timeoutMs: t }) {
      if (!e) throw Error(`locator.waitFor requires a state`);
      try {
        await this.#r.send({
          command: tq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            state: e,
            timeout_ms: t,
          }),
          timeoutMs: t,
        });
      } catch (t) {
        throw Rq(t, `locator.waitFor(${e}) timed out for selector ${this.#n}`);
      }
    }
    async count() {
      return (
        await this.#r.send({
          command: iq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
          }),
        })
      ).count;
    }
    async all() {
      let t = await this.count(),
        n = new Wq({
          browserId: this.#e,
          tabId: this.#t,
          selector: this.#n,
          transport: this.#r,
        });
      return Array.from(
        { length: t },
        (t, r) =>
          new e({
            browserId: this.#e,
            tabId: this.#t,
            selector: `${this.#n} >> nth=${r}`,
            transport: this.#r,
            collectionReadCache: n,
            collectionReadIndex: r,
          }),
      );
    }
    async textContent({ timeoutMs: e } = {}) {
      let t = await this.cachedRead(e);
      return t
        ? t.text_content
        : (
            await this.#r.send({
              command: Cq({
                browser_id: this.#e,
                tab_id: this.#t,
                selector: this.#n,
                timeout_ms: e,
              }),
              timeoutMs: e,
            })
          ).value;
    }
    async innerText({ timeoutMs: e } = {}) {
      let t = await this.cachedRead(e);
      return t
        ? t.inner_text
        : (
            await this.#r.send({
              command: Eq({
                browser_id: this.#e,
                tab_id: this.#t,
                selector: this.#n,
                timeout_ms: e,
              }),
              timeoutMs: e,
            })
          ).value;
    }
    async getAttribute(e, { timeoutMs: t } = {}) {
      if (!e) throw Error(`locator.getAttribute requires a name`);
      let n = await this.cachedRead(t);
      return n
        ? Object.prototype.hasOwnProperty.call(n.attributes, e)
          ? (n.attributes[e] ?? null)
          : null
        : (
            await this.#r.send({
              command: kq({
                browser_id: this.#e,
                tab_id: this.#t,
                selector: this.#n,
                name: e,
                timeout_ms: t,
              }),
              timeoutMs: t,
            })
          ).value;
    }
    async isVisible() {
      return (
        await this.#r.send({
          command: mq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
          }),
        })
      ).value;
    }
    async isEnabled() {
      return (
        await this.#r.send({
          command: _q({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
          }),
        })
      ).value;
    }
    async allTextContents({ timeoutMs: e } = {}) {
      return (
        await this.#r.send({
          command: bq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            timeout_ms: e,
          }),
          timeoutMs: e,
        })
      ).values;
    }
    async evaluate(e, t, n) {
      return (
        await this.#r.send({
          command: IK({
            browser_id: this.#e,
            script: zq(e, t),
            selector: this.#n,
            tab_id: this.#t,
            timeout_ms: n?.timeoutMs,
          }),
          timeoutMs: n?.timeoutMs,
        })
      ).value;
    }
    async downloadMedia({ timeoutMs: e } = {}) {
      try {
        (await this.#r.send({
          command: KK({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            timeout_ms: e,
          }),
          timeoutMs: e,
        }),
          this.#i?.clear());
      } catch (e) {
        throw Rq(e, `locator.downloadMedia failed for selector ${this.#n}`);
      }
    }
    locator(t, n = {}) {
      if (!t) throw Error(`locator.locator requires a selector`);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${t}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, t) : void 0,
      }).filter(n);
    }
    first() {
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> nth=0`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, `nth=0`) : void 0,
      });
    }
    last() {
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> nth=-1`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, `nth=-1`) : void 0,
      });
    }
    nth(t) {
      if (typeof t != `number`)
        throw Error(`locator.nth requires a numeric index`);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> nth=${t}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, `nth=${t}`) : void 0,
      });
    }
    and(t) {
      if (!(t instanceof e))
        throw Error(`locator.and requires a PlaywrightLocator`);
      if (t.#e !== this.#e || t.#t !== this.#t)
        throw Error(`Locators must belong to the same tab`);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> internal:and=${JSON.stringify(t.#n)}`,
        transport: this.#r,
      });
    }
    or(t) {
      if (!(t instanceof e))
        throw Error(`locator.or requires a PlaywrightLocator`);
      if (t.#e !== this.#e || t.#t !== this.#t)
        throw Error(`Locators must belong to the same tab`);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> internal:or=${JSON.stringify(t.#n)}`,
        transport: this.#r,
      });
    }
    filter(t = {}) {
      let n = [this.#n];
      if (
        (t.hasText !== void 0 &&
          n.push(`internal:has-text=${qq(t.hasText, !1)}`),
        t.hasNotText !== void 0 &&
          n.push(`internal:has-not-text=${qq(t.hasNotText, !1)}`),
        t.has !== void 0 &&
          (this.assertCompatibleLocator(t.has, `locator.filter has`),
          n.push(`internal:has=${JSON.stringify(t.has.#n)}`)),
        t.hasNot !== void 0 &&
          (this.assertCompatibleLocator(t.hasNot, `locator.filter hasNot`),
          n.push(`internal:has-not=${JSON.stringify(t.hasNot.#n)}`)),
        t.visible !== void 0)
      ) {
        if (typeof t.visible != `boolean`)
          throw Error(`locator.filter visible must be a boolean`);
        n.push(`visible=${t.visible}`);
      }
      let r = n.slice(1).join(` >> `),
        i = this.#i && this.#a !== void 0 && (!r || this.#o !== void 0);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: n.join(` >> `),
        transport: this.#r,
        collectionReadCache: i ? this.#i : void 0,
        collectionReadIndex: i ? this.#a : void 0,
        collectionRelativeSelector: i ? Gq(this.#o, r) : void 0,
      });
    }
    getByRole(t, n = {}) {
      let r = Yq(t, n);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${r}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, r) : void 0,
      });
    }
    getByText(t, n = {}) {
      let r = Xq(t, n);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${r}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, r) : void 0,
      });
    }
    getByLabel(t, n = {}) {
      let r = Zq(t, n);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${r}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, r) : void 0,
      });
    }
    getByPlaceholder(t, n = {}) {
      let r = Qq(t, n);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${r}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, r) : void 0,
      });
    }
    getByTestId(t) {
      let n = $q(t);
      return new e({
        browserId: this.#e,
        tabId: this.#t,
        selector: `${this.#n} >> ${n}`,
        transport: this.#r,
        collectionReadCache: this.#i,
        collectionReadIndex: this.#a,
        collectionRelativeSelector:
          this.#i && this.#a !== void 0 ? Gq(this.#o, n) : void 0,
      });
    }
    async cachedRead(e) {
      return !this.#i || this.#a === void 0
        ? null
        : await this.#i.read({
            index: this.#a,
            relativeSelector: this.#o,
            timeoutMs: e,
          });
    }
    assertCompatibleLocator(t, n) {
      if (!(t instanceof e)) throw Error(`${n} requires a PlaywrightLocator`);
      if (t.#e !== this.#e || t.#t !== this.#t)
        throw Error(`Locators must belong to the same tab`);
    }
  },
  Wq = class {
    #e;
    #t;
    #n;
    #r;
    #i = new Map();
    constructor({ browserId: e, tabId: t, selector: n, transport: r }) {
      ((this.#e = e), (this.#t = t), (this.#n = n), (this.#r = r));
    }
    clear() {
      this.#i.clear();
    }
    async read({ index: e, relativeSelector: t, timeoutMs: n }) {
      let r = t ?? ``,
        i = this.#i.get(r);
      return (
        i ||
          ((i = this.#a(t, n).catch((e) => {
            throw (this.#i.delete(r), e);
          })),
          this.#i.set(r, i)),
        (await i)[e] ?? null
      );
    }
    async #a(e, t) {
      return (
        await this.#r.send({
          command: Mq({
            browser_id: this.#e,
            tab_id: this.#t,
            selector: this.#n,
            relative_selector: e,
            timeout_ms: t,
          }),
          timeoutMs: t,
        })
      ).values;
    }
  };
function Gq(e, t) {
  return e ? (t ? `${e} >> ${t}` : e) : t || void 0;
}
function Kq(e) {
  let t = e;
  return e.unicode || t.unicodeSets
    ? String(e)
    : String(e)
        .replace(/(^|[^\\])(\\\\)*(["'`])/g, `$1$2\\\\$3`)
        .replace(/>>/g, `\\>\\>`);
}
function qq(e, t) {
  return typeof e == `string` ? `${JSON.stringify(e)}${t ? `s` : `i`}` : Kq(e);
}
function Jq(e, t) {
  return typeof e == `string`
    ? `"${e.replace(/\\/g, `\\\\`).replace(/["]/g, `\\"`)}"${t ? `s` : `i`}`
    : Kq(e);
}
function Yq(e, t) {
  if (!e) throw Error(`getByRole requires a role`);
  let n = [];
  return (
    t.name !== void 0 && n.push(`[name=${Jq(t.name, !!t.exact)}]`),
    `internal:role=${e}${n.join(``)}`
  );
}
function Xq(e, t) {
  if (typeof e != `string` && !(e instanceof RegExp))
    throw Error(`getByText requires a string or RegExp`);
  return `internal:text=${qq(e, !!t.exact)}`;
}
function Zq(e, t) {
  if (typeof e != `string` && !(e instanceof RegExp))
    throw Error(`getByLabel requires a string or RegExp`);
  return `internal:label=${qq(e, !!t.exact)}`;
}
function Qq(e, t) {
  if (typeof e != `string` && !(e instanceof RegExp))
    throw Error(`getByPlaceholder requires a string or RegExp`);
  return `internal:attr=[placeholder=${Jq(e, !!t.exact)}]`;
}
function $q(e) {
  if (!e) throw Error(`getByTestId requires a testId`);
  return `internal:testid=[data-testid=${Jq(e, !0)}]`;
}
var eJ = class {
    constructor(e, t, n, r) {
      ((this.transport = e),
        (this.browserId = t),
        (this.documentationApi = n),
        (this.info = r));
    }
    get id() {
      return this.info.id;
    }
    async documentation() {
      return await this.documentationApi.get(`capabilities/browser/${this.id}`);
    }
  },
  tJ = class {
    constructor(e, t, n, r, i) {
      ((this.transport = e),
        (this.browserId = t),
        (this.tabId = n),
        (this.documentationApi = r),
        (this.info = i));
    }
    get id() {
      return this.info.id;
    }
    async documentation() {
      return await this.documentationApi.get(`capabilities/tab/${this.id}`);
    }
  };
function nJ({ capability: e, info: t, internalOnly: n }) {
  return {
    capability: e,
    create: (n) => new e({ ...n, info: t }),
    id: t.id,
    info: t,
    ...(n == null ? {} : { internalOnly: n }),
  };
}
function rJ({ capability: e, info: t, internalOnly: n }) {
  return {
    capability: e,
    create: (n) => new e({ ...n, info: t }),
    id: t.id,
    info: t,
    ...(n == null ? {} : { internalOnly: n }),
  };
}
var iJ = $({
    reason: fK([
      `captcha_failed`,
      `access_denied`,
      `challenge_loop`,
      `unexpected_bot_error`,
    ]),
  }),
  aJ = `tab_bot_detection_report`,
  oJ = iJ.extend({ browser_id: X(), tab_id: X() }),
  ote = $({ status: dK(`reported`), hostname: X().nullable() });
function sJ(e) {
  return new MW(aJ, oJ, e);
}
var cJ = rJ({
    capability: class extends tJ {
      constructor({
        browserId: e,
        documentation: t,
        info: n,
        tabId: r,
        transport: i,
      }) {
        super(i, e, r, t, n);
      }
      async report(e) {
        let t = oJ.parse({
            ...e,
            browser_id: this.browserId,
            tab_id: this.tabId,
          }),
          n = await this.transport.send({ command: sJ(t) });
        return ote.parse(n);
      }
    },
    info: {
      id: `botDetection`,
      description: `Use when the current tab is blocked by bot detection, a failed CAPTCHA, a hard access denial, or a repeated challenge/login loop.`,
    },
  }),
  lJ = $({
    session_id: X().min(1).optional(),
    target_id: X().min(1).optional(),
  }).refine(
    (e) => (e.session_id == null) != (e.target_id == null),
    `CDP target must provide exactly one of session_id or target_id.`,
  ),
  uJ = $({
    extensionId: X().optional(),
    sessionId: X().optional(),
    tabId: Z().int().optional(),
    targetId: X().optional(),
  }),
  dJ = $({
    sequence: Z().int().positive(),
    source: uJ,
    method: X().min(1),
    params: uK(X(), lK()).optional(),
  }),
  fJ = `tab_cdp_call`,
  pJ = $({
    browser_id: X(),
    tab_id: X(),
    method: X().min(1),
    params: uK(X(), lK()).optional(),
    target: lJ.optional(),
    timeout_ms: Z().int().positive().optional(),
  }),
  mJ = lK();
function hJ(e) {
  return new MW(fJ, pJ, e);
}
var gJ = `tab_cdp_events`,
  _J = $({
    after_sequence: Z().int().nonnegative().optional(),
    browser_id: X(),
    limit: Z().int().positive().max(1e3).optional(),
    methods: Q(X().min(1)).min(1).optional(),
    tab_id: X(),
    target: lJ.optional(),
    timeout_ms: Z().int().nonnegative().optional(),
  }),
  vJ = $({
    cursor: Z().int().nonnegative(),
    events: Q(dJ),
    hasMore: cK(),
    truncated: cK(),
  });
function yJ(e) {
  return new MW(gJ, _J, e);
}
var bJ = {
    id: `cdp`,
    description: `Send raw Chrome DevTools Protocol commands and read debugger events through a supported tab for developer use cases.`,
  },
  xJ = 2e3,
  SJ = rJ({
    capability: class extends tJ {
      constructor({
        browserId: e,
        documentation: t,
        info: n,
        tabId: r,
        transport: i,
      }) {
        super(i, e, r, t, n);
      }
      async send(e, t, n) {
        let r = await this.transport.send({
          command: hJ({
            browser_id: this.browserId,
            method: e,
            params: t,
            tab_id: this.tabId,
            target: CJ(n?.target),
            timeout_ms: n?.timeoutMs,
          }),
          timeoutMs: n?.timeoutMs,
        });
        return mJ.parse(r);
      }
      async readEvents(e) {
        let t = await this.transport.send({
          command: yJ({
            after_sequence: e?.afterSequence,
            browser_id: this.browserId,
            limit: e?.limit,
            methods: e?.methods,
            tab_id: this.tabId,
            target: CJ(e?.target),
            timeout_ms: e?.timeoutMs,
          }),
          timeoutMs: wJ(e?.timeoutMs),
        });
        return vJ.parse(t);
      }
    },
    info: bJ,
  });
function CJ(e) {
  if (e != null) return { session_id: e.sessionId, target_id: e.targetId };
}
function wJ(e) {
  return e != null && e > 0 ? e + xJ : void 0;
}
var TJ = 300 * 1e3,
  EJ = /^[A-Za-z0-9_-]{1,48}$/,
  DJ = new Set([`__proto__`, `constructor`, `prototype`]),
  OJ = $({
    id: X()
      .regex(EJ)
      .refine((e) => !DJ.has(e)),
    label: X(),
    type: X().min(1),
    autocomplete: X(),
    required: cK(),
    selector: X(),
  }),
  kJ = $({ selector: X(), action: fK([`click`, `press_enter`]) }),
  AJ = $({
    origin: X(),
    reason: X(),
    expires_at: X()
      .datetime({ offset: !0 })
      .refine(
        (e) => Date.parse(e) <= Date.now() + TJ,
        `expires_at must be no more than 5 minutes in the future`,
      ),
    fields: Q(OJ).min(1).max(6),
    submit: kJ.optional(),
  }),
  jJ = `tab_browser_auth_handoff`,
  MJ = AJ.extend({ browser_id: X(), tab_id: X() }),
  NJ = $({
    status: fK([
      `submitted`,
      `declined`,
      `cancelled`,
      `unavailable`,
      `expired`,
      `origin_changed`,
      `page_changed`,
      `locator_invalid`,
      `submission_failed`,
    ]),
  });
function PJ(e) {
  return new MW(jJ, MJ, e);
}
var FJ = rJ({
  capability: class extends tJ {
    constructor({
      browserId: e,
      documentation: t,
      info: n,
      tabId: r,
      transport: i,
    }) {
      super(i, e, r, t, n);
    }
    async request(e) {
      let t = IJ(e, { browserId: this.browserId, tabId: this.tabId }),
        n = await this.transport.send({
          command: PJ({ ...t, browser_id: this.browserId, tab_id: this.tabId }),
        });
      return NJ.parse(n);
    }
  },
  info: {
    id: `browserAuth`,
    description: `MUST read its documentation before the first interaction with any authentication or sign-in flow required for the task. Covers method selection, secure user-provided credentials, and post-authentication verification and recovery.`,
  },
});
function IJ(e, t) {
  let { submit: n, ...r } = e,
    i = {
      ...r,
      fields: r.fields.map((e) => ({ ...e, selector: LJ(e.selector, t) })),
    };
  return (n != null && (i.submit = { ...n, selector: LJ(n.selector, t) }), i);
}
function LJ(e, t) {
  if (typeof e == `string`) return e;
  if (!(e instanceof Uq))
    throw Error(`browserAuth selector must be a string or PlaywrightLocator`);
  let n = Uq.browserAuthSelector(e);
  if (n.browserId !== t.browserId || n.tabId !== t.tabId)
    throw Error(`browserAuth selector locator must belong to this tab`);
  return n.selector;
}
var RJ = fK([`font`, `image`, `script`, `stylesheet`, `video`, `other`]),
  zJ = fK([`font`, `image`, `stylesheet`, `video`]),
  BJ = $({
    kind: fK([`attribute`, `computedStyle`, `resource`]),
    nodeId: Z().int().positive().optional(),
    property: X().optional(),
  }),
  VJ = $({ id: X(), kind: RJ, name: X(), sources: Q(BJ), url: X() }),
  HJ = $({ id: X(), markup: X(), name: X() }),
  UJ = $({
    assets: Q(VJ),
    id: X(),
    inlineSvgs: Q(HJ),
    pageUrl: X().nullable(),
    summary: $({
      byKind: uK(RJ, Z().int().nonnegative()),
      inlineSvgCount: Z().int().nonnegative(),
      totalCount: Z().int().nonnegative(),
    }),
  }),
  WJ = $({
    assetIds: Q(X()).optional(),
    inventoryId: X(),
    kinds: Q(zJ).optional(),
  }),
  GJ = $({
    contentType: X().nullable(),
    id: X(),
    kind: zJ,
    name: X(),
    path: X(),
    url: X(),
  }),
  KJ = $({
    contentType: X().nullable(),
    id: X(),
    name: X(),
    reason: X(),
    url: X(),
  }),
  qJ = $({
    assets: Q(GJ),
    directoryPath: X(),
    failures: Q(KJ),
    manifestPath: X(),
    summary: $({
      downloadedCount: Z().int().nonnegative(),
      elapsedMs: Z().nonnegative(),
      failedCount: Z().int().nonnegative(),
      requestedCount: Z().int().nonnegative(),
    }),
  }),
  JJ = `tab_page_assets_bundle`,
  YJ = WJ.extend({ browser_id: X(), tab_id: X() }),
  XJ = qJ;
function ZJ(e) {
  return new MW(JJ, YJ, e);
}
var QJ = `tab_page_assets_list`,
  $J = $({ browser_id: X(), tab_id: X() }),
  eY = UJ;
function tY(e) {
  return new MW(QJ, $J, e);
}
var nY = rJ({
    capability: class extends tJ {
      constructor({
        browserId: e,
        documentation: t,
        info: n,
        tabId: r,
        transport: i,
      }) {
        super(i, e, r, t, n);
      }
      async list() {
        let e = await this.transport.send({
          command: tY({ browser_id: this.browserId, tab_id: this.tabId }),
        });
        return eY.parse(e);
      }
      async bundle(e) {
        let t = await this.transport.send({
          command: ZJ({ ...e, browser_id: this.browserId, tab_id: this.tabId }),
        });
        return XJ.parse(t);
      }
    },
    info: {
      id: `pageAssets`,
      description: `List assets already observed in the current page state and bundle selected assets into a temporary local artifact.`,
    },
  }),
  rY = rJ({
    capability: class extends tJ {
      constructor({
        browserId: e,
        documentation: t,
        info: n,
        tabId: r,
        transport: i,
      }) {
        super(i, e, r, t, n);
      }
      async listTools() {
        return (
          await this.transport.send({
            command: wK({ browser_id: this.browserId, tab_id: this.tabId }),
          })
        ).tools.map((e) => this.toTool(e));
      }
      async invokeTool({ input: e, timeoutMs: t, toolName: n }) {
        let r = n.trim();
        if (!r)
          throw Error(`tab.capabilities.webmcp.invokeTool requires a toolName`);
        return (
          await this.transport.send({
            command: DK({
              browser_id: this.browserId,
              tab_id: this.tabId,
              tool_name: r,
              input: e,
              ...(t == null ? {} : { timeout_ms: t }),
            }),
          })
        ).result;
      }
      toTool(e) {
        return {
          name: e.name,
          ...(e.title == null ? {} : { title: e.title }),
          ...(e.description == null ? {} : { description: e.description }),
          inputSchema: e.input_schema,
          ...(e.annotations == null ? {} : { annotations: e.annotations }),
          ...(e.origin == null ? {} : { origin: e.origin }),
          ...(e.pageUrl == null ? {} : { pageUrl: e.pageUrl }),
          invoke: async (t, n) =>
            await this.invokeTool({
              toolName: e.name,
              input: t,
              ...(n?.timeoutMs == null ? {} : { timeoutMs: n.timeoutMs }),
            }),
        };
      }
    },
    info: {
      id: `webmcp`,
      description: `List and invoke page-defined WebMCP tools registered through navigator.modelContext in the active tab.`,
    },
    internalOnly: !0,
  });
iY([SJ, cJ, FJ, nY, rY]);
function iY(e) {
  return new Map(e.map((e) => [e.id, e.create]));
}
var aY = e.i({
    PayloadSchema: () => sY,
    ResultSchema: () => cY,
    commandType: () => oY,
    create: () => lY,
  }),
  oY = `browser_visibility_get`,
  sY = $({ browser_id: X() }),
  cY = $({ visible: cK() });
function lY(e) {
  return new MW(oY, sY, e);
}
var uY = e.i({
    PayloadSchema: () => fY,
    ResultSchema: () => pY,
    commandType: () => dY,
    create: () => mY,
  }),
  dY = `browser_visibility_set`,
  fY = $({ browser_id: X(), visible: cK() }),
  pY = $({});
function mY(e) {
  return new MW(dY, fY, e);
}
var hY = e.i({ Get: () => aY, Set: () => uY, commands: () => gY }),
  gY = { [oY]: aY, [dY]: uY },
  _Y = nJ({
    capability: class extends eJ {
      constructor({ browserId: e, documentation: t, info: n, transport: r }) {
        super(r, e, t, n);
      }
      async set(e) {
        let t = await this.transport.send({
          command: mY({ browser_id: this.browserId, visible: e }),
        });
        pY.parse(t);
      }
      async get() {
        let e = await this.transport.send({
          command: lY({ browser_id: this.browserId }),
        });
        return cY.parse(e).visible;
      }
    },
    info: {
      id: `visibility`,
      description: `Use to show or hide the browser to the user, and to determine the browser's current visibility. Keep browser work in the background unless the user asks to see it or live viewing is useful. When the browser should be visible, call set(true).`,
    },
  }),
  vY = e.i({
    PayloadSchema: () => bY,
    ResultSchema: () => xY,
    commandType: () => yY,
    create: () => SY,
  }),
  yY = `browser_viewport_reset`,
  bY = $({ browser_id: X() }),
  xY = $({});
function SY(e) {
  return new MW(yY, bY, e);
}
var CY = e.i({
    PayloadSchema: () => TY,
    ResultSchema: () => EY,
    commandType: () => wY,
    create: () => DY,
  }),
  wY = `browser_viewport_set`,
  TY = $({
    browser_id: X(),
    height: Z().int().positive(),
    width: Z().int().positive(),
  }),
  EY = $({});
function DY(e) {
  return new MW(wY, TY, e);
}
var OY = e.i({ Reset: () => vY, Set: () => CY, commands: () => kY }),
  kY = { [yY]: vY, [wY]: CY },
  AY = nJ({
    capability: class extends eJ {
      constructor({ browserId: e, documentation: t, info: n, transport: r }) {
        super(r, e, t, n);
      }
      async set(e) {
        let t = await this.transport.send({
          command: DY({ browser_id: this.browserId, ...e }),
        });
        EY.parse(t);
      }
      async reset() {
        let e = await this.transport.send({
          command: SY({ browser_id: this.browserId }),
        });
        xY.parse(e);
      }
    },
    info: {
      id: `viewport`,
      description: `Controls an explicit browser viewport override for responsive or device-size testing. Use it when a task calls for specific dimensions or breakpoint validation; otherwise leave it unset so the browser uses its normal viewport. Reset temporary overrides before finishing unless the user asked to keep them.`,
    },
  });
jY([_Y, AY]);
function jY(e) {
  return new Map(e.map((e) => [e.id, e.create]));
}
$({ side_effects: Q(X()) }).passthrough();
function MY(e) {
  return n.qs.isInternal(e) || e === n.qs.PublicBeta;
}
var NY = [_Y.info, AY.info],
  PY = [nY.info];
function FY(e, t) {
  return [...PY, ...IY(e, t)];
}
function IY(e, t) {
  let n = [];
  return (
    e != null && MY(e) && (i.a.isInternal(e) || t.webMcp) && n.push(rY.info),
    n
  );
}
function LY({ browserTabId: e, host: t, payload: n }) {
  return Lt({
    backendName: `Codex in-app browser`,
    commandSets: [hY, OY],
    handlers: BY({ browserTabId: e, host: t }),
    payload: n,
  });
}
function RY(e) {
  return (
    e.type === `browser_visibility_set` ||
    e.type === `browser_viewport_set` ||
    e.type === `browser_viewport_reset`
  );
}
function zY(e) {
  if (e.type === `browser_visibility_set`)
    return { kind: `visibility`, visible: fY.parse(e).visible };
  if (e.type === `browser_viewport_set`) {
    let t = TY.parse(e);
    return {
      kind: `viewport`,
      viewportSize: { width: t.width, height: t.height },
    };
  }
  return e.type === `browser_viewport_reset`
    ? (bY.parse(e), { kind: `viewport`, viewportSize: null })
    : null;
}
function BY({ browserTabId: e, host: t }) {
  return {
    [dY]: (n) => {
      let r = fY.parse(n);
      return (t.setBrowserVisibleForBrowserUse(r.visible, e), pY.parse({}));
    },
    [oY]: (n) => (
      sY.parse(n),
      cY.parse({ visible: t.isBrowserVisibleForBrowserUse(e) })
    ),
    [wY]: (n) => {
      let r = TY.parse(n);
      t.setViewportForBrowserUse({ width: r.width, height: r.height }, e);
      let i = t.waitForPendingDebuggerSync?.(e);
      return i == null ? EY.parse({}) : i.then(() => EY.parse({}));
    },
    [yY]: (n) => {
      (bY.parse(n), t.setViewportForBrowserUse(null, e));
      let r = t.waitForPendingDebuggerSync?.(e);
      return r == null ? xY.parse({}) : r.then(() => xY.parse({}));
    },
  };
}
var VY = `about:blank`,
  HY = 15e3,
  UY = 2e4,
  WY = 1500,
  GY = 1e3,
  KY = 16,
  qY = 1e4,
  JY = `tabs_content`,
  YY = r.a(`browser-use-iab-api`),
  XY = Symbol(`addBrowserUseDownloadChangeListener`),
  ZY = Symbol(`consumeAllowedBrowserUseDownload`),
  QY = Symbol(`notifyBrowserUseDownloadChange`),
  $Y = class {
    tabsById = new Map();
    tabRuntimesById = new Map();
    tabIdsByPageKey = new Map();
    tabIdsByRouteTabKey = new Map();
    userTabsByRouteTabKey = new Map();
    routeTabKeysByTabId = new Map();
    routeKeysByTabId = new Map();
    debuggerListeners = new Map();
    pageStateListeners = new Map();
    debuggerTargetSessionsByTabId = new Map();
    debuggerTargetIdsBySessionId = new Map();
    cachedCdpExpressions = new Map();
    cdpEventListeners = new Set();
    downloadChangeListeners = new Set();
    allowedDownloadGrantsByWebContentsId = new Map();
    disposeBrowserUseNavigationBlockedListener;
    cdpCommandTimeoutMs;
    cursorArrivalTimeoutMs;
    initialPageUrl;
    pageReadyTimeoutMs;
    cursorArrivalWaitersByMoveSequence = new Map();
    selectedTabIdsByRouteKey = new Map();
    hostsByRouteKey = new Map();
    pendingVisibilityRouteKeys = new Set();
    pendingViewportSizesByRouteKey = new Map();
    usedTurnIdsByConversationId = new Map();
    nextCdpTabId = 1;
    nextCursorMoveSequence = 1;
    constructor(e, t, n = {}) {
      ((this.getBrowserHost = e),
        (this.options = n),
        (this.disposeBrowserUseNavigationBlockedListener = t((e) => {
          this.emitBrowserUseNavigationBlockedEvent(e);
        })),
        (this.cdpCommandTimeoutMs = n.cdpCommandTimeoutMs ?? UY),
        (this.cursorArrivalTimeoutMs = n.cursorArrivalTimeoutMs ?? WY),
        (this.initialPageUrl = n.initialPageUrl ?? VY),
        (this.pageReadyTimeoutMs = n.pageReadyTimeoutMs ?? HY));
    }
    ping() {
      return `pong`;
    }
    async getTabs(e) {
      return this.runLoggedRequest(e, `getTabs`, (e) =>
        this.getTabsForBrowserUse(e),
      );
    }
    async createTab(e) {
      return this.runLoggedRequest(
        e,
        `createTab`,
        async (t) => (
          this.recordBrowserUseTurn(t, e),
          this.createTabForBrowserUse(t)
        ),
      );
    }
    async nameSession(e) {
      await this.nameSessionForBrowserUse(this.requireBrowserUseSession(e), e);
    }
    async attach(e) {
      let t = this.requireBrowserUseSession(e);
      (this.recordBrowserUseTurn(t, e), await this.attachForBrowserUse(t, e));
    }
    async attachTarget(e) {
      let t = this.requireBrowserUseSession(e);
      this.recordBrowserUseTurn(t, e);
      let n = await this.getTab(t, e.tabId);
      (await this.attachForBrowserUse(t, e),
        e.targetId !== iX(n) &&
          (await this.attachDebuggerTarget(n, e.targetId)));
    }
    async detach(e) {
      let t = this.requireBrowserUseSession(e);
      (this.recordBrowserUseTurn(t, e), await this.detachForBrowserUse(t, e));
    }
    async detachTarget(e) {
      let t = this.requireBrowserUseSession(e);
      this.recordBrowserUseTurn(t, e);
      let n = await this.getTab(t, e.tabId);
      if (e.targetId !== iX(n)) {
        await this.detachDebuggerTarget(n, e.targetId);
        return;
      }
      await this.detachForBrowserUse(t, e);
    }
    async executeCdp(e) {
      return this.runLoggedRequest(
        e,
        `executeCdp:${e.method}`,
        (t) => (
          this.recordBrowserUseTurn(t, e),
          this.executeCdpForBrowserUse(t, e)
        ),
      );
    }
    async executeCdpWithCachedExpression(e) {
      this.requireBrowserUseSession(e);
      let { expressionCacheKey: t, ...n } = e,
        r = vX(n.commandParams) ? n.commandParams : {},
        i = typeof r.expression == `string` ? r.expression : void 0;
      i != null && this.cachedCdpExpressions.set(t, i);
      let a = i ?? this.cachedCdpExpressions.get(t);
      return a == null
        ? { kind: `cache-miss` }
        : {
            kind: `executed`,
            result: await this.executeCdp({
              ...n,
              commandParams: { ...r, expression: a },
            }),
          };
    }
    async allowDownload(e) {
      return this.runLoggedRequest(e, `allowDownload`, async (t) => {
        if (!Number.isInteger(e.tabId))
          throw Error(`allowDownload requires an integer tabId`);
        if (typeof e.url != `string` || e.url.length === 0)
          throw Error(`allowDownload requires a non-empty url`);
        let n = await this.getTab(t, e.tabId);
        (this.assertCurrentPageAllowed(t, n),
          this.pruneExpiredAllowedDownloadGrants(),
          this.allowedDownloadGrantsByWebContentsId.set(n.webContents.id, {
            grantedAtMs: Date.now(),
            sessionId: t.conversationId,
            url: e.url,
          }));
      });
    }
    async executeUnhandledCommand(e) {
      return this.runLoggedRequest(
        e,
        `executeUnhandledCommand:${e.type}`,
        (t) => {
          if (e.type === JY)
            throw Error(
              `browser.tabs.content is not supported in Codex in-app browser. Open tabs with browser.tabs.new(), navigate them, then read each tab directly.`,
            );
          let n = this.getRequiredBrowserHost(t),
            r = this.getActiveBrowserUseTab(t, {
              assertCurrentPageAllowed: !1,
              markBrowserUseCommand: !1,
            });
          if (r == null && this.getTabMode(t) === `multiTab` && RY(e)) {
            let t = zY(e);
            if (t != null)
              return (this.setPendingCapabilityIntent(n.routeKey, t), {});
          }
          return (
            this.recordBrowserUseTurn(t, e),
            LY({ browserTabId: r?.browserTabId, host: n, payload: e })
          );
        },
      );
    }
    addCdpEventListener(e) {
      return (
        this.cdpEventListeners.add(e),
        () => {
          this.cdpEventListeners.delete(e);
        }
      );
    }
    [XY](e) {
      return (
        this.downloadChangeListeners.add(e),
        () => {
          this.downloadChangeListeners.delete(e);
        }
      );
    }
    [ZY]({ url: e, urlChain: t, webContentsId: n }) {
      this.pruneExpiredAllowedDownloadGrants();
      let r = this.allowedDownloadGrantsByWebContentsId.get(n) ?? null;
      return r == null || (r.url !== e && !t.includes(r.url))
        ? null
        : (this.allowedDownloadGrantsByWebContentsId.delete(n),
          { sessionId: r.sessionId, url: r.url });
    }
    [QY](e) {
      for (let t of this.downloadChangeListeners) t(e);
    }
    async getInfo(e) {
      let t = this.getBrowserUseSession(e);
      YY().info(`IAB_LIFECYCLE iab backend info request received`, {
        safe: this.getRequestSafeFields(t, e, `getInfo`),
        sensitive: {},
      });
      try {
        this.ensureBrowserUseSessionRoute(t);
        let r = this.getMetadata(t),
          i = {
            ...(this.getTabMode(t) === `multiTab`
              ? {
                  apiSupportOverrides: {
                    "BrowserUser.claimTab": !0,
                    "Tab.markDeliverable": !0,
                    "Tab.markHandoff": !0,
                    "Tabs.finalize": !0,
                  },
                }
              : {}),
            name: `Codex In-app Browser`,
            version: n.Nc(n.Lc(c.app.getVersion())),
            type: `iab`,
            capabilities: {
              browser: NY,
              tab: FY(this.options.buildFlavor, Ke()),
            },
            metadata: r,
          };
        return (
          YY().info(`IAB_LIFECYCLE iab backend info request completed`, {
            safe: {
              ...this.getRequestSafeFields(t, e, `getInfo`),
              metadataCodexSessionId: r.codexSessionId,
            },
            sensitive: {},
          }),
          i
        );
      } catch (n) {
        throw (
          YY().warning(`IAB_LIFECYCLE iab backend info request failed`, {
            safe: this.getRequestSafeFields(t, e, `getInfo`),
            sensitive: { error: n },
          }),
          n
        );
      }
    }
    getUserTabs(e) {
      let t = this.requireBrowserUseSession(e);
      return this.getTabMode(t) === `multiTab`
        ? this.getUserTabInfosForSession(t)
        : [];
    }
    getUserHistory(e) {
      throw (
        this.requireBrowserUseSession(e),
        Error(`browser.user.history is only available with the Chrome backend.`)
      );
    }
    async claimUserTab(e) {
      let t = this.requireBrowserUseSession(e);
      if (this.getTabMode(t) !== `multiTab`)
        throw Error(
          `browser.user.claimTab is only available with the Chrome or multi-tab IAB backend.`,
        );
      let n = this.getRequiredBrowserHost(t),
        r = this.tabsById.get(e.tabId) ?? null;
      if (r?.routeKey === n.routeKey) {
        let n = await this.requireLiveTab(t, r);
        return (
          this.selectedTabIdsByRouteKey.set(n.routeKey, n.cdpTabId),
          this.markBrowserUseCommandForTab(t, n),
          this.recordBrowserUseTurn(t, e),
          this.serializeTab(n)
        );
      }
      let i = this.getUserTabForTabId(t, e.tabId);
      if (i == null) throw Error(`Unknown tab: ${e.tabId}`);
      let a = await n.openPageForBrowserUse({
          browserTabId: i.browserTabId,
          origin: `user`,
        }),
        o = this.updateTabForPage(a, n.routeKey, `user`, e.tabId);
      return (
        this.selectedTabIdsByRouteKey.set(o.routeKey, o.cdpTabId),
        this.markBrowserUseCommandForTab(t, o),
        this.recordBrowserUseTurn(t, e),
        this.serializeTab(o)
      );
    }
    async finalizeTabs(e) {
      let t = this.requireBrowserUseSession(e);
      if (this.getTabMode(t) !== `multiTab`)
        throw Error(
          `browser.tabs.finalize is only available with Chrome or multi-tab IAB.`,
        );
      if (!Array.isArray(e.keep))
        throw Error(`finalizeTabs requires a keep array`);
      let n = this.updateTabsForSession(t),
        r = eX(e.keep, new Set(n.map((e) => e.cdpTabId)));
      await Promise.allSettled(
        n.map(async (e) => {
          await this.cleanupDebuggerForTabBestEffort(e, `finalize-tabs`);
          let t = r.get(e.cdpTabId);
          if (!(t === `handoff` && e.origin === `agent`)) {
            if (
              t === `handoff` ||
              t === `deliverable` ||
              e.origin !== `agent`
            ) {
              this.releaseTab(e);
              return;
            }
            this.closeTab(e);
          }
        }),
      );
      for (let e of this.getBrowserHostsForSession(t))
        (e.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(e.routeKey));
      this.clearRecordedBrowserUseTurn(t, e);
    }
    async markTab(e) {
      let t = this.requireBrowserUseSession(e);
      if (this.getTabMode(t) !== `multiTab`)
        throw Error(
          `Tab persistence is only available with Chrome or multi-tab IAB.`,
        );
      if (!Number.isInteger(e.tabId))
        throw Error(`markTab requires an integer tabId`);
      if (e.status !== `handoff` && e.status !== `deliverable`)
        throw Error(`markTab received invalid status ${String(e.status)}`);
      if (typeof e.turn_id != `string`)
        throw Error(`markTab requires a turn_id`);
      this.requireBrowserUseTab(t, e.tabId);
      let n = this.tabsById.get(e.tabId);
      if (n == null) throw Error(`Unknown tab: ${e.tabId}`);
      ((n.mark = { status: e.status, turnId: e.turn_id }),
        this.recordBrowserUseTurn(t, e));
    }
    async moveMouse(e) {
      AX(`moveMouse`, e);
      let t = this.requireBrowserUseSession(e),
        n = await this.getTab(t, e.tabId);
      (this.recordBrowserUseTurn(t, e), this.markBrowserUseCommandForTab(t, n));
      let r = this.nextCursorMoveSequence;
      this.nextCursorMoveSequence += 1;
      let i = !1;
      for (let a of this.getBrowserHostsForSession(t))
        i =
          a.setBrowserUseCursor(
            { moveSequence: r, x: e.x, y: e.y },
            n.browserTabId,
          ) || i;
      e.waitForArrival === !1 || !i || (await this.waitForCursorArrival(r));
    }
    notifyCursorArrived(e) {
      Number.isInteger(e.moveSequence) &&
        this.options.ensureSessionRoute?.(e) === !0 &&
        this.resolveCursorArrivalWaiter(e.moveSequence);
    }
    async releaseSessionControl(e) {
      let t = this.getBrowserHostsForSession(e);
      await this.detachBrowserUseTabsForHostsBestEffort(t);
      for (let e of t)
        (e.setBrowserUseCursor(null),
          e.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(e.routeKey));
    }
    async turnEnded(e) {
      let t = this.getBrowserUseSession(e);
      if (
        (this.ensureBrowserUseSessionRoute(t),
        this.getTabMode(t) !== `multiTab`)
      )
        return;
      let n = e.turn_id;
      if (
        typeof n != `string` ||
        this.usedTurnIdsByConversationId.get(t.conversationId) !== n
      )
        return;
      let r = this.updateTabsForSession(t);
      await Promise.allSettled(
        r.map(async (e) => {
          await this.cleanupDebuggerForTabBestEffort(e, `turn-ended`);
          let t = e.mark?.turnId === n ? e.mark.status : null;
          if (t === `handoff` && e.origin === `agent`) {
            delete e.mark;
            return;
          }
          if (t != null || e.origin !== `agent`) {
            this.releaseTab(e);
            return;
          }
          this.closeTab(e);
        }),
      );
      for (let e of this.getBrowserHostsForSession(t))
        (e.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(e.routeKey));
      this.clearRecordedBrowserUseTurn(t, e);
    }
    disposeRoute(e) {
      let t = this.hostsByRouteKey.get(e) ?? null;
      (this.hostsByRouteKey.delete(e),
        this.selectedTabIdsByRouteKey.delete(e),
        this.usedTurnIdsByConversationId.delete(xX(e)),
        this.forgetTabIdsForRoute(e),
        this.forgetUserTabsForRoute(e),
        t != null &&
          (t.setBrowserUseCursor(null),
          t.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(t.routeKey)));
      for (let t of Array.from(this.tabsById.values()))
        t.routeKey === e &&
          (this.cleanupDebuggerForTabBestEffort(t, `route-disposed`),
          this.removeTab(t));
    }
    reassignRoute(e, t, n, r) {
      let i = this.selectedTabIdsByRouteKey.get(e) ?? null,
        a = i == null ? null : (this.tabsById.get(i) ?? null);
      i != null &&
        (n == null || a?.browserTabId === n) &&
        (this.selectedTabIdsByRouteKey.delete(e),
        this.selectedTabIdsByRouteKey.set(t, i));
      let o = this.hostsByRouteKey.get(e) ?? null;
      o != null &&
        (this.hostsByRouteKey.delete(e), this.hostsByRouteKey.set(t, o));
      for (let [i, a] of this.routeKeysByTabId) {
        if (a !== e) continue;
        let o = this.tabsById.get(i);
        (n != null && o?.browserTabId !== n) ||
          (this.routeKeysByTabId.set(i, t),
          o != null &&
            this.tabsById.set(i, {
              ...o,
              browserTabId: r ?? o.browserTabId,
              routeKey: t,
            }));
      }
      for (let [i, a] of Array.from(this.userTabsByRouteTabKey.entries())) {
        if (a.routeKey !== e || (n != null && a.browserTabId !== n)) continue;
        let o = r ?? a.browserTabId,
          s = SX(t, o),
          c = this.tabIdsByRouteTabKey.get(i) ?? null;
        (this.userTabsByRouteTabKey.delete(i),
          this.userTabsByRouteTabKey.set(s, {
            ...a,
            browserTabId: o,
            routeKey: t,
          }),
          c != null && this.moveRouteTabId(i, s, c, t));
      }
      for (let [i, a] of Array.from(this.tabIdsByRouteTabKey.entries())) {
        let o = CX(e, i);
        o == null ||
          (n != null && o !== n) ||
          this.moveRouteTabId(i, SX(t, r ?? o), a, t);
      }
    }
    dispose() {
      (this.resolveAllCursorArrivalWaiters(),
        this.disposeBrowserUseNavigationBlockedListener());
      for (let e of this.hostsByRouteKey.values()) e.setBrowserUseActive(!1);
      for (let e of this.tabsById.values())
        (this.unregisterPageStateListener(e),
          this.unregisterDebuggerListener(e),
          this.detachTab(e));
      (this.tabsById.clear(),
        this.tabRuntimesById.clear(),
        this.tabIdsByPageKey.clear(),
        this.tabIdsByRouteTabKey.clear(),
        this.userTabsByRouteTabKey.clear(),
        this.routeTabKeysByTabId.clear(),
        this.routeKeysByTabId.clear(),
        this.allowedDownloadGrantsByWebContentsId.clear(),
        this.selectedTabIdsByRouteKey.clear(),
        this.hostsByRouteKey.clear(),
        this.pendingVisibilityRouteKeys.clear(),
        this.pendingViewportSizesByRouteKey.clear(),
        this.usedTurnIdsByConversationId.clear());
    }
    requireBrowserUseSession(e) {
      let t = this.getBrowserUseSession(e);
      return (this.ensureBrowserUseSessionRoute(t), t);
    }
    getBrowserUseSession(e) {
      let t = e?.session_id;
      if (typeof t != `string`)
        throw Error(`Missing required browser session_id`);
      return { conversationId: t };
    }
    ensureBrowserUseSessionRoute(e) {
      if (this.options.ensureSessionRoute?.(e) !== !0)
        throw Error(
          `No ChatGPT browser route is available for browser session ${e.conversationId}`,
        );
    }
    async runLoggedRequest(e, t, n) {
      let r = this.getBrowserUseSession(e);
      try {
        return (this.ensureBrowserUseSessionRoute(r), await n(r));
      } catch (n) {
        throw (
          YY().warning(`IAB_LIFECYCLE iab backend request failed`, {
            safe: this.getRequestSafeFields(r, e, t),
            sensitive: { error: n },
          }),
          n
        );
      }
    }
    getRequestSafeFields(e, t, n) {
      return {
        conversationId: e.conversationId,
        request: n,
        turnId: t.turn_id ?? null,
      };
    }
    async waitForCursorArrival(e) {
      this.cursorArrivalTimeoutMs <= 0 ||
        (await new Promise((t) => {
          let n = null,
            r = () => {
              (n != null && clearTimeout(n),
                this.cursorArrivalWaitersByMoveSequence.delete(e),
                t());
            };
          ((n = setTimeout(r, this.cursorArrivalTimeoutMs)),
            jX(n),
            this.cursorArrivalWaitersByMoveSequence.set(e, r));
        }));
    }
    resolveCursorArrivalWaiter(e) {
      this.cursorArrivalWaitersByMoveSequence.get(e)?.();
    }
    resolveAllCursorArrivalWaiters() {
      for (let e of Array.from(
        this.cursorArrivalWaitersByMoveSequence.values(),
      ))
        e();
    }
    getTabMode(e) {
      return this.getTabModeForRoute(this.getRequiredBrowserHost(e).routeKey);
    }
    getMetadata(e) {
      let t = {};
      return (
        this.options.buildFlavor != null &&
          (t.codexAppBuildFlavor = this.options.buildFlavor),
        this.options.appSessionId != null &&
          (t.codexAppSessionId = this.options.appSessionId),
        (t.codexSessionId = e.conversationId),
        t
      );
    }
    async getTabsForBrowserUse(e) {
      if (this.getTabMode(e) === `multiTab`) {
        let t = this.updateTabsForSession(e),
          n = this.getRequiredBrowserHost(e),
          r = this.selectedTabIdsByRouteKey.get(n.routeKey) ?? null;
        if (r == null || t.every((e) => e.cdpTabId !== r)) {
          let e = t.find((e) => e.active) ?? t[0];
          e != null &&
            this.selectedTabIdsByRouteKey.set(n.routeKey, e.cdpTabId);
        }
        return t.map((e) => this.serializeTab(e));
      }
      let t = this.getActiveBrowserUseTab(e, {
        assertCurrentPageAllowed: !1,
        markBrowserUseCommand: !1,
      });
      return t == null ? [] : [this.serializeTab(t)];
    }
    async createTabForBrowserUse(e) {
      if (this.getTabMode(e) === `multiTab`) {
        let t = this.getRequiredBrowserHost(e),
          r = n._c(`browser-use:${(0, f.randomUUID)()}`),
          i = !1,
          a = !1;
        try {
          (t.setBrowserUseActive(!0, r),
            this.applyPendingCapabilityIntents(t, r));
          let n = t.openPageForBrowserUse({
            browserTabId: r,
            origin: `agent`,
            startingUrl: this.initialPageUrl,
          });
          i = !0;
          let o = await n;
          a = !0;
          let s = this.updateTabForPage(o, t.routeKey, `agent`);
          return (
            YY().info(`IAB_LIFECYCLE iab createTab mapped page to tab`, {
              safe: {
                ...e,
                browserTabId: r,
                pageKey: o.pageKey,
                routeKey: t.routeKey,
                tabId: s.cdpTabId,
                webContentsId: o.webContents.id,
              },
              sensitive: {},
            }),
            this.markBrowserUseCommandForTab(e, s),
            this.selectedTabIdsByRouteKey.set(t.routeKey, s.cdpTabId),
            this.serializeTab(s)
          );
        } catch (e) {
          throw (
            (!i || a) &&
              (t.setBrowserUseActive(!1, r), t.closePageForBrowserUse(r)),
            e
          );
        }
      }
      let t = this.getActiveBrowserUseTab(e, { assertCurrentPageAllowed: !1 });
      if (t != null) {
        let n = await this.requireLiveTab(e, t);
        return (await this.navigateTabToInitialPage(n), this.serializeTab(n));
      }
      let r = this.getRequiredBrowserHost(e);
      r.setBrowserUseActive(!0);
      let i = await r.openPageForBrowserUse({
          origin: `agent`,
          startingUrl: this.initialPageUrl,
        }),
        a = this.updateTabForPage(i, r.routeKey, `agent`);
      return (
        YY().info(`IAB_LIFECYCLE iab createTab mapped page to tab`, {
          safe: {
            ...e,
            pageKey: i.pageKey,
            routeKey: r.routeKey,
            tabId: a.cdpTabId,
            webContentsId: i.webContents.id,
          },
          sensitive: {},
        }),
        this.markBrowserUseCommandForTab(e, a),
        this.selectedTabIdsByRouteKey.set(r.routeKey, a.cdpTabId),
        this.serializeTab(a)
      );
    }
    async nameSessionForBrowserUse(e, t) {
      if ((this.getRequiredBrowserHost(e), typeof t.name != `string`))
        throw Error(`nameSession requires a name`);
    }
    async attachForBrowserUse(e, t) {
      let n = await this.getTab(e, t.tabId);
      (this.markBrowserUseCommandForTab(e, n),
        this.assertCurrentPageAllowed(e, n),
        await this.attachTab(n),
        this.registerDebuggerListener(n));
    }
    async detachForBrowserUse(e, t) {
      this.requireBrowserUseTab(e, t.tabId);
      let n = this.tabsById.get(t.tabId);
      n != null &&
        (this.markBrowserUseCommandForTab(e, n),
        this.unregisterDebuggerListener(n),
        await this.detachTab(n));
    }
    async executeCdpForBrowserUse(e, t) {
      let n = await this.getTabForCommand(e, t),
        r = this.getBrowserHost(e),
        i = await this.getCdpNavigationUrl(e, n, t),
        a = !1;
      if (
        (this.markBrowserUseCommandForTab(e, n),
        t.method === `Target.closeTarget`)
      )
        return this.executeEmulatedTargetCommand(n, t);
      this.assertCurrentPageAllowed(e, n);
      try {
        r != null &&
          i != null &&
          (r.assertBrowserUseNavigationAllowed(n.pageKey, i),
          r.trackPendingBrowserUseNavigation(n.pageKey, i),
          (a = !0));
        let o = tX(t.method);
        if (
          (o || dX(t.method),
          await this.attachForBrowserUse(e, { tabId: n.cdpTabId }),
          await this.waitForTabReadyForCdp(e, n),
          o)
        )
          return await this.executeEmulatedTargetCommand(n, t);
        if (t.method === `Page.reload`)
          return (
            vX(t.commandParams) && t.commandParams.ignoreCache === !0
              ? n.webContents.reloadIgnoringCache()
              : n.webContents.reload(),
            {}
          );
        let s = await this.executeTranslatedInputCommand(e, n, t).catch((e) => {
          if (!rX(e)) throw e;
          return null;
        });
        if (s != null) return s;
        try {
          return (
            await r?.waitForPendingDebuggerSync?.(n.browserTabId),
            await this.executeCdpCommandWithCaptureSurface(n, r, t)
          );
        } finally {
          t.method === `Page.close` &&
            (await this.cleanupDebuggerForTabBestEffort(n, `page-close`));
        }
      } catch (e) {
        throw (
          r != null && a && r.clearPendingBrowserUseNavigation(n.pageKey),
          NX(e) &&
            (await this.cleanupDebuggerForTabBestEffort(n, `cdp-timeout`)),
          e
        );
      }
    }
    getRequiredBrowserHost(e) {
      let t = this.getBrowserHost(e);
      if (t == null)
        throw (
          YY().warning(`IAB_LIFECYCLE iab host lookup returned null`, {
            safe: e,
            sensitive: {},
          }),
          Error(
            `No in-app Browser host is available for this browser-use session`,
          )
        );
      return this.rememberBrowserHost(t);
    }
    rememberBrowserHost(e) {
      return (this.hostsByRouteKey.set(e.routeKey, e), e);
    }
    setPendingCapabilityIntent(e, t) {
      switch (t.kind) {
        case `visibility`:
          t.visible
            ? this.pendingVisibilityRouteKeys.add(e)
            : this.pendingVisibilityRouteKeys.delete(e);
          return;
        case `viewport`:
          t.viewportSize == null
            ? this.pendingViewportSizesByRouteKey.delete(e)
            : this.pendingViewportSizesByRouteKey.set(e, t.viewportSize);
          return;
      }
    }
    applyPendingCapabilityIntents(e, t) {
      let n = this.pendingViewportSizesByRouteKey.get(e.routeKey) ?? null;
      (n != null &&
        (this.pendingViewportSizesByRouteKey.delete(e.routeKey),
        e.setViewportForBrowserUse(n, t)),
        this.pendingVisibilityRouteKeys.has(e.routeKey) &&
          (this.pendingVisibilityRouteKeys.delete(e.routeKey),
          e.setBrowserVisibleForBrowserUse(!0, t)));
    }
    clearPendingCapabilityIntents(e) {
      (this.pendingVisibilityRouteKeys.delete(e),
        this.pendingViewportSizesByRouteKey.delete(e));
    }
    getBrowserHostsForSession(e) {
      let t = new Set(),
        n = this.getBrowserHost(e);
      n != null && t.add(this.rememberBrowserHost(n));
      for (let [n, r] of this.hostsByRouteKey.entries())
        xX(n) === e.conversationId && t.add(r);
      return [...t];
    }
    updateActivePageTab(e) {
      let t = this.getBrowserHost(e);
      if (t == null) return null;
      this.rememberBrowserHost(t);
      let n = t.getPageForBrowserUse();
      return n == null ? null : this.updateTabForPage(n, t.routeKey);
    }
    updateTabsForSession(e) {
      this.clearDestroyedTabRuntimes();
      let t = this.getRequiredBrowserHost(e);
      if (this.getTabMode(e) !== `multiTab`)
        return t
          .getPagesForBrowserUse()
          .filter((e) => !e.webContents.isDestroyed())
          .map((e) => this.updateTabForPage(e, t.routeKey));
      let n = t.getTabsForBrowserUse(),
        r = new Set(),
        i = n.map((e) => {
          let n = SX(t.routeKey, e.browserTabId);
          r.add(n);
          let i = this.updateTabForInfo(e, t.routeKey),
            a = t.getPageForBrowserUse(e.browserTabId);
          return a == null || a.webContents.isDestroyed()
            ? i
            : this.updateTabForPage(a, t.routeKey, e.origin);
        });
      return (
        this.pruneMissingTabs(t.routeKey, r),
        this.clearDestroyedTabRuntimes(),
        i
      );
    }
    getUserTabInfosForSession(e) {
      return this.getUserTabsForSession(e).map((e) => ({
        id: this.getUserTabIdForState(e),
        title: e.title,
        url: e.url,
      }));
    }
    getUserTabForTabId(e, t) {
      this.getUserTabInfosForSession(e);
      let n = this.getRequiredBrowserHost(e),
        r = this.routeTabKeysByTabId.get(t) ?? null;
      return r == null || this.routeKeysByTabId.get(t) !== n.routeKey
        ? null
        : (this.userTabsByRouteTabKey.get(r) ?? null);
    }
    getUserTabsForSession(e) {
      this.clearDestroyedTabRuntimes();
      let t = this.getRequiredBrowserHost(e),
        n = t.getUserTabsForBrowserUse(),
        r = new Set(),
        i = [];
      for (let e of n) {
        let n = SX(t.routeKey, e.browserTabId);
        r.add(n);
        let a = {
          browserTabId: e.browserTabId,
          routeKey: t.routeKey,
          title: e.title,
          url: e.url,
        };
        (this.userTabsByRouteTabKey.set(n, a), i.push(a));
      }
      return (this.pruneMissingUserTabs(t.routeKey, r), i);
    }
    getUserTabIdForState(e) {
      let t = SX(e.routeKey, e.browserTabId),
        n = this.tabIdsByRouteTabKey.get(t) ?? null;
      if (n != null)
        return (
          this.routeTabKeysByTabId.set(n, t),
          this.routeKeysByTabId.set(n, e.routeKey),
          n
        );
      let r = this.nextCdpTabId;
      return (
        (this.nextCdpTabId += 1),
        this.tabIdsByRouteTabKey.set(t, r),
        this.routeTabKeysByTabId.set(r, t),
        this.routeKeysByTabId.set(r, e.routeKey),
        r
      );
    }
    pruneMissingUserTabs(e, t) {
      let n = `${e}\0`;
      for (let e of Array.from(this.userTabsByRouteTabKey.keys()))
        !e.startsWith(n) || t.has(e) || this.userTabsByRouteTabKey.delete(e);
    }
    forgetUserTabsForRoute(e) {
      let t = `${e}\0`;
      for (let e of Array.from(this.userTabsByRouteTabKey.keys()))
        e.startsWith(t) && this.userTabsByRouteTabKey.delete(e);
    }
    pruneMissingTabs(e, t) {
      for (let n of Array.from(this.tabsById.values())) {
        if (n.routeKey !== e) continue;
        let r = SX(n.routeKey, n.browserTabId);
        t.has(r) ||
          (this.forgetRouteTabId(n),
          this.cleanupDebuggerForTabBestEffort(n, `tab-inventory-pruned`),
          this.removeTab(n));
      }
    }
    forgetTabIdsForRoute(e) {
      let t = `${e}\0`;
      for (let [e, n] of Array.from(this.tabIdsByRouteTabKey.entries()))
        e.startsWith(t) &&
          (this.tabIdsByRouteTabKey.delete(e),
          this.routeTabKeysByTabId.delete(n),
          this.routeKeysByTabId.delete(n));
    }
    moveRouteTabId(e, t, n, r) {
      e !== t && this.tabIdsByRouteTabKey.delete(e);
      let i = this.tabIdsByRouteTabKey.get(t) ?? null;
      if (i != null && i !== n) {
        let e = this.tabsById.get(i) ?? null;
        (e != null && this.removeTab(e),
          this.routeTabKeysByTabId.delete(i),
          this.routeKeysByTabId.delete(i));
      }
      (this.tabIdsByRouteTabKey.set(t, n),
        this.routeTabKeysByTabId.set(n, t),
        this.routeKeysByTabId.set(n, r));
    }
    getActiveBrowserUseTab(e, t = {}) {
      this.clearDestroyedTabRuntimes();
      let n =
        this.getTabMode(e) === `multiTab`
          ? this.getSelectedOrFirstTabForSession(e)
          : this.updateActivePageTab(e);
      return n == null
        ? (this.clearDestroyedTabRuntimes(), null)
        : (t.markBrowserUseCommand !== !1 &&
            this.markBrowserUseCommandForTab(e, n),
          t.assertCurrentPageAllowed !== !1 &&
            this.assertCurrentPageAllowed(e, n),
          this.selectedTabIdsByRouteKey.set(n.routeKey, n.cdpTabId),
          this.clearDestroyedTabRuntimes(),
          n);
    }
    getSelectedOrFirstTabForSession(e) {
      let t = this.getRequiredBrowserHost(e),
        n = this.updateTabsForSession(e),
        r = this.selectedTabIdsByRouteKey.get(t.routeKey) ?? null;
      return (
        (r == null ? null : (n.find((e) => e.cdpTabId === r) ?? null)) ??
        n.find((e) => e.active) ??
        n[0] ??
        null
      );
    }
    updateTabForPage(e, t, n = `agent`, r) {
      let i = this.isMultiTabRoute(t)
          ? r
          : (this.tabIdsByPageKey.get(e.pageKey) ??
            this.getTabIdForWebContents(e.webContents) ??
            r),
        a = i == null ? null : (this.tabsById.get(i) ?? null),
        o = this.updateTabForInfo(
          {
            active: e.active,
            browserTabId: e.browserTabId ?? yL(xX(t)),
            origin: n,
            title: e.title,
            url: TX(
              e.url,
              e.webContents.isDestroyed() ? `` : e.webContents.getURL(),
              a?.url,
              n,
            ),
          },
          t,
          i,
        ),
        s = this.tabRuntimesById.get(o.cdpTabId) ?? null,
        c = s?.pageKey ?? null,
        l = s?.webContents ?? null;
      (c != null && c !== e.pageKey && this.tabIdsByPageKey.delete(c),
        l != null &&
          l !== e.webContents &&
          (this.unregisterPageStateListener(o),
          this.unregisterDebuggerListener(o)));
      let u = { ...o, pageKey: e.pageKey, webContents: e.webContents };
      return (
        this.tabRuntimesById.set(u.cdpTabId, {
          pageKey: u.pageKey,
          webContents: u.webContents,
        }),
        this.tabIdsByPageKey.set(e.pageKey, u.cdpTabId),
        this.registerPageStateListener(u),
        u
      );
    }
    updateTabForInfo(e, t, n) {
      let r = SX(t, e.browserTabId),
        i = this.isMultiTabRoute(t)
          ? (this.tabIdsByRouteTabKey.get(r) ?? null)
          : null,
        a = i ?? n ?? this.nextCdpTabId;
      i == null && a >= this.nextCdpTabId && (this.nextCdpTabId = a + 1);
      let o = this.tabsById.get(a) ?? null,
        s = this.routeKeysByTabId.get(a) ?? null;
      (s != null &&
        s !== t &&
        this.selectedTabIdsByRouteKey.get(s) === a &&
        this.selectedTabIdsByRouteKey.delete(s),
        this.isMultiTabRoute(t) &&
          (this.tabIdsByRouteTabKey.set(r, a),
          this.routeTabKeysByTabId.set(a, r)),
        this.routeKeysByTabId.set(a, t));
      let c = {
        browserTabId: e.browserTabId,
        cdpTabId: a,
        origin: o?.origin ?? e.origin,
        routeKey: t,
        title: e.title,
        url: e.url,
        active: e.active,
        ...(s === t && o?.mark != null ? { mark: o.mark } : {}),
      };
      return (this.tabsById.set(a, c), c);
    }
    getTabIdForWebContents(e) {
      for (let [t, n] of this.tabRuntimesById.entries())
        if (n.webContents === e) return t;
      return null;
    }
    serializeTab(e) {
      let t = this.tabRuntimesById.get(e.cdpTabId) ?? null,
        n =
          t == null || t.webContents.isDestroyed()
            ? ``
            : t.webContents.getTitle(),
        r =
          t == null || t.webContents.isDestroyed()
            ? ``
            : t.webContents.getURL();
      return {
        id: e.cdpTabId,
        title: kX(n, r, e.title, e.url),
        active: e.cdpTabId === this.selectedTabIdsByRouteKey.get(e.routeKey),
        url: wX(r, e.url, e.origin),
      };
    }
    markBrowserUseCommandForTab(e, t) {
      let n = this.getBrowserHost(e);
      if (n == null) return;
      this.rememberBrowserHost(n);
      let r = this.tabRuntimesById.get(t.cdpTabId) ?? null;
      (r != null && n.activateBrowserUseNavigationRestriction(r.pageKey),
        n.setBrowserUseActive(!0, t.browserTabId));
    }
    recordBrowserUseTurn(e, t) {
      typeof t.turn_id == `string` &&
        this.usedTurnIdsByConversationId.set(e.conversationId, t.turn_id);
    }
    clearRecordedBrowserUseTurn(e, t) {
      typeof t.turn_id == `string` &&
        this.usedTurnIdsByConversationId.get(e.conversationId) === t.turn_id &&
        this.usedTurnIdsByConversationId.delete(e.conversationId);
    }
    emitBrowserUseNavigationBlockedEvent(e) {
      let t = this.tabIdsByPageKey.get(e.pageKey);
      t != null &&
        this.emitCdpEvent({
          source: { tabId: t },
          method: `Page.navigationBlocked`,
          params: {
            errorText: e.error.message,
            resourceType: e.resourceType,
            url: e.url,
          },
        });
    }
    assertCurrentPageAllowed(e, t) {
      let n = this.tabRuntimesById.get(t.cdpTabId) ?? null;
      n != null &&
        this.getBrowserHost(e)?.assertBrowserUseNavigationAllowed(
          n.pageKey,
          n.webContents.getURL() || t.url,
        );
    }
    assertTabMatchesCurrentBrowserUsePage(e, t) {
      let n = this.getRequiredBrowserHost(e).getPageForBrowserUse(
        t.browserTabId,
      );
      if (
        !(
          n != null &&
          n.pageKey === t.pageKey &&
          n.webContents === t.webContents &&
          !t.webContents.isDestroyed()
        )
      )
        throw (
          YY().warning(`IAB_LIFECYCLE iab rejected stale input tab`, {
            safe: {
              ...e,
              expectedPageKey: n?.pageKey ?? null,
              pageWebContentsDestroyed: n?.webContents.isDestroyed() ?? null,
              tabId: t.cdpTabId,
              tabPageKey: t.pageKey,
              tabWebContentsDestroyed: t.webContents.isDestroyed(),
            },
            sensitive: {},
          }),
          Error(
            `Tab ${t.cdpTabId} is no longer attached to the browser page for session ${e.conversationId}`,
          )
        );
    }
    async getCdpNavigationUrl(e, t, n) {
      let r = fX(n);
      if (r != null) return r;
      let i = pX(n);
      return i == null ? null : this.getHistoryNavigationUrl(e, t, i);
    }
    async getHistoryNavigationUrl(e, t, n) {
      let r = t.webContents.debugger.isAttached();
      (await this.attachTab(t), await this.waitForTabReadyForCdp(e, t));
      try {
        let e = await FX(
            t.webContents.debugger.sendCommand(`Page.getNavigationHistory`, {}),
            this.cdpCommandTimeoutMs,
            `Timed out reading navigation history for tab ${t.cdpTabId}`,
          ),
          r = (Array.isArray(e.entries) ? e.entries : []).find(
            (e) => e.id === n,
          )?.url;
        if (typeof r != `string`)
          throw Error(
            `Unable to resolve navigation history entry ${n} for tab ${t.cdpTabId}`,
          );
        return r;
      } finally {
        r || (await this.detachTab(t));
      }
    }
    async getTab(e, t) {
      this.clearDestroyedTabRuntimes();
      let n =
        this.getTabMode(e) === `multiTab`
          ? this.getSelectedOrFirstTabForSession(e)
          : this.updateActivePageTab(e);
      if ((this.clearDestroyedTabRuntimes(), n?.cdpTabId === t))
        return (
          this.getAttachedLiveTab(this.getRequiredBrowserHost(e), n) ??
          this.requireLiveTab(e, n)
        );
      this.requireBrowserUseTab(e, t);
      let r = this.tabsById.get(t);
      if (r == null) throw Error(`Unknown tab: ${t}`);
      return (
        this.getAttachedLiveTab(this.getRequiredBrowserHost(e), r) ??
        this.requireLiveTab(e, r)
      );
    }
    getAttachedLiveTab(e, t) {
      let n = e.getPageForBrowserUse(t.browserTabId);
      return n == null || n.webContents.isDestroyed()
        ? null
        : this.updateTabForPage(n, t.routeKey, t.origin, t.cdpTabId);
    }
    async requireLiveTab(e, t) {
      let n = this.getRequiredBrowserHost(e),
        r = this.getAttachedLiveTab(n, t);
      if (r != null) return r;
      if (
        !this.isMultiTabRoute(t.routeKey) &&
        (this.tabRuntimesById.get(t.cdpTabId) ?? null) != null
      )
        throw Error(`Unknown tab: ${t.cdpTabId}`);
      let i = await n.openPageForBrowserUse({ browserTabId: t.browserTabId });
      return this.updateTabForPage(i, t.routeKey, t.origin, t.cdpTabId);
    }
    async getTabForCommand(e, t) {
      if (t.target.tabId != null) return this.getTab(e, t.target.tabId);
      if (
        !this.shouldAutoBootstrapNavigateCommand(t) ||
        this.getActiveBrowserUseTab(e) != null
      )
        throw Error(`executeCdp requires a tabId target`);
      let n = await this.createTabForBrowserUse(e);
      return this.getTab(e, n.id);
    }
    shouldAutoBootstrapNavigateCommand(e) {
      return fX(e) != null;
    }
    clearDestroyedTabRuntimes() {
      for (let e of this.tabsById.values()) {
        let t = this.tabRuntimesById.get(e.cdpTabId) ?? null;
        t == null ||
          !t.webContents.isDestroyed() ||
          (this.unregisterDebuggerListener(e),
          this.unregisterPageStateListener(e),
          this.tabIdsByPageKey.get(t.pageKey) === e.cdpTabId &&
            this.tabIdsByPageKey.delete(t.pageKey),
          this.allowedDownloadGrantsByWebContentsId.delete(t.webContents.id),
          this.tabRuntimesById.delete(e.cdpTabId));
      }
    }
    closeTab(e) {
      let t = this.hostsByRouteKey.get(e.routeKey) ?? null,
        n = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      (t == null && n != null
        ? n.webContents.close()
        : t?.closePageForBrowserUse(e.browserTabId),
        this.forgetRouteTabId(e),
        this.removeTab(e),
        this.forgetUserTab(e));
    }
    releaseTab(e) {
      this.hostsByRouteKey
        .get(e.routeKey)
        ?.releasePageForBrowserUse(e.browserTabId);
      let t = SX(e.routeKey, e.browserTabId);
      (this.userTabsByRouteTabKey.set(t, {
        browserTabId: e.browserTabId,
        routeKey: e.routeKey,
        title: e.title,
        url: e.url,
      }),
        this.removeTab(e));
    }
    removeTab(e) {
      (this.unregisterPageStateListener(e),
        this.unregisterDebuggerListener(e, `tab-removed`),
        this.tabsById.delete(e.cdpTabId));
      let t = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      (t != null &&
        this.tabIdsByPageKey.get(t.pageKey) === e.cdpTabId &&
        this.tabIdsByPageKey.delete(t.pageKey),
        t != null &&
          this.allowedDownloadGrantsByWebContentsId.delete(t.webContents.id),
        this.tabRuntimesById.delete(e.cdpTabId),
        this.selectedTabIdsByRouteKey.get(e.routeKey) === e.cdpTabId &&
          this.selectedTabIdsByRouteKey.delete(e.routeKey));
    }
    forgetUserTab(e) {
      let t = SX(e.routeKey, e.browserTabId);
      this.userTabsByRouteTabKey.delete(t);
    }
    forgetRouteTabId(e) {
      if (!this.isMultiTabRoute(e.routeKey)) return;
      let t = SX(e.routeKey, e.browserTabId);
      (this.tabIdsByRouteTabKey.get(t) === e.cdpTabId &&
        this.tabIdsByRouteTabKey.delete(t),
        this.routeTabKeysByTabId.get(e.cdpTabId) === t &&
          this.routeTabKeysByTabId.delete(e.cdpTabId),
        this.routeKeysByTabId.get(e.cdpTabId) === e.routeKey &&
          this.routeKeysByTabId.delete(e.cdpTabId));
    }
    isMultiTabRoute(e) {
      return this.getTabModeForRoute(e) === `multiTab`;
    }
    getTabModeForRoute(e) {
      return this.options.getTabMode?.(e) ?? `singleTab`;
    }
    requireBrowserUseTab(e, t) {
      let n = this.getRequiredBrowserHost(e);
      if (
        this.tabsById.get(t)?.routeKey !== n.routeKey &&
        !(
          this.getTabMode(e) === `multiTab` &&
          (this.updateTabsForSession(e),
          this.tabsById.get(t)?.routeKey === n.routeKey)
        )
      )
        throw (
          YY().warning(`IAB_LIFECYCLE iab rejected tab for route`, {
            safe: {
              ...e,
              expectedRouteKey: n.routeKey,
              tabId: t,
              tabRouteKey: this.routeKeysByTabId.get(t) ?? null,
            },
            sensitive: {},
          }),
          Error(`Tab ${t} is not part of browser session ${e.conversationId}`)
        );
    }
    async detachBrowserUseTabsForHostsBestEffort(e) {
      let t = new Set([...e].map((e) => e.routeKey));
      await Promise.allSettled(
        [...this.tabsById.values()]
          .filter((e) => t.has(e.routeKey))
          .map((e) =>
            this.cleanupDebuggerForTabBestEffort(e, `session-activity-ended`),
          ),
      );
    }
    async cleanupDebuggerForTabBestEffort(e, t) {
      let n = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      this.unregisterDebuggerListener(e, t);
      try {
        await this.detachTab(e, n);
      } catch (r) {
        YY().warning(`IAB_LIFECYCLE failed to detach debugger`, {
          safe: {
            pageKey: n?.pageKey ?? null,
            reason: t,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsDestroyed: n?.webContents.isDestroyed() ?? null,
            webContentsId: n?.webContents.id ?? null,
          },
          sensitive: { error: r },
        });
      }
    }
    registerPageStateListener(e) {
      let t = this.pageStateListeners.get(e.cdpTabId) ?? null;
      if (t?.webContents === e.webContents) return;
      t != null && this.unregisterPageStateListener(e);
      let n = (t, n) => {
          this.updateTabNavigationState(e.cdpTabId, n);
        },
        r = (t, n, r) => {
          r && this.updateTabNavigationState(e.cdpTabId, n);
        },
        i = (t, n) => {
          let r = this.tabsById.get(e.cdpTabId) ?? null;
          r != null && (r.title = EX(n, r));
        },
        a = () => {
          this.unregisterPageStateListener(e);
        };
      (e.webContents.on(`did-navigate`, n),
        e.webContents.on(`did-navigate-in-page`, r),
        e.webContents.on(`page-title-updated`, i),
        e.webContents.on(`destroyed`, a),
        this.pageStateListeners.set(e.cdpTabId, {
          destroyedListener: a,
          didNavigateInPageListener: r,
          didNavigateListener: n,
          pageTitleUpdatedListener: i,
          webContents: e.webContents,
        }));
    }
    unregisterPageStateListener(e) {
      let t = this.pageStateListeners.get(e.cdpTabId);
      t != null &&
        (this.pageStateListeners.delete(e.cdpTabId),
        t.webContents.isDestroyed() ||
          (t.webContents.removeListener(`did-navigate`, t.didNavigateListener),
          t.webContents.removeListener(
            `did-navigate-in-page`,
            t.didNavigateInPageListener,
          ),
          t.webContents.removeListener(
            `page-title-updated`,
            t.pageTitleUpdatedListener,
          )),
        t.webContents.removeListener(`destroyed`, t.destroyedListener));
    }
    updateTabNavigationState(e, t) {
      let n = this.tabsById.get(e) ?? null;
      n == null || t.length === 0 || (n.url = TX(n.url, t, n.url, n.origin));
    }
    registerDebuggerListener(e) {
      if (this.debuggerListeners.has(e.cdpTabId)) return;
      let t = (n, r, i, a) => {
          if (this.debuggerListeners.get(e.cdpTabId)?.listener !== t) return;
          let o = typeof a == `string` && a.length > 0 ? a : void 0,
            s = o == null ? void 0 : this.debuggerTargetIdsBySessionId.get(o),
            c = {
              source: { tabId: e.cdpTabId, sessionId: o, targetId: s },
              method: r,
            };
          (vX(i) && ((c.params = i), this.recordDebuggerTargetEvent(e, r, i)),
            this.emitCdpEvent(c));
        },
        n = () => {
          this.unregisterDebuggerListener(e, `webcontents-destroyed`);
        },
        r = (t, n) => {
          (YY().info(`IAB_LIFECYCLE debugger render process gone`, {
            safe: {
              exitCode: bX(n),
              pageKey: e.pageKey,
              reason: yX(n),
              routeKey: e.routeKey,
              tabId: e.cdpTabId,
              webContentsId: e.webContents.id,
            },
            sensitive: {},
          }),
            this.cleanupDebuggerForTabBestEffort(e, `render-process-gone`));
        };
      (e.webContents.debugger.on(`message`, t),
        e.webContents.once(`destroyed`, n),
        e.webContents.once(`render-process-gone`, r),
        this.debuggerListeners.set(e.cdpTabId, {
          destroyedListener: n,
          listener: t,
          renderProcessGoneListener: r,
          webContents: e.webContents,
          webContentsId: e.webContents.id,
        }),
        YY().info(`IAB_LIFECYCLE registered debugger listener`, {
          safe: {
            pageKey: e.pageKey,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsId: e.webContents.id,
          },
          sensitive: {},
        }));
    }
    unregisterDebuggerListener(e, t = `manual`) {
      this.forgetDebuggerTargetSessionsForTab(e);
      let n = this.debuggerListeners.get(e.cdpTabId);
      if (n == null) return;
      this.debuggerListeners.delete(e.cdpTabId);
      let r = n.webContents.isDestroyed();
      (r || n.webContents.debugger.removeListener(`message`, n.listener),
        n.webContents.removeListener(`destroyed`, n.destroyedListener),
        n.webContents.removeListener(
          `render-process-gone`,
          n.renderProcessGoneListener,
        ),
        YY().info(`IAB_LIFECYCLE unregistered debugger listener`, {
          safe: {
            pageKey: this.tabRuntimesById.get(e.cdpTabId)?.pageKey ?? null,
            reason: t,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsDestroyed: r,
            webContentsId: n.webContentsId,
          },
          sensitive: {},
        }));
    }
    recordDebuggerTargetEvent(e, t, n) {
      if (t === `Target.attachedToTarget`) {
        let t = uX(n, `sessionId`),
          r = sX(lX(n, `targetInfo`));
        t != null && r != null && this.rememberDebuggerTargetSession(e, r, t);
        return;
      }
      if (t === `Target.detachedFromTarget`) {
        let e = uX(n, `sessionId`);
        e != null && this.forgetDebuggerTargetSession(e);
      }
    }
    emitCdpEvent(e) {
      for (let t of this.cdpEventListeners) t(e);
    }
    async attachTab(e) {
      if (e.webContents.isDestroyed())
        throw Error(`Unknown tab: ${e.cdpTabId}`);
      if (!e.webContents.debugger.isAttached())
        try {
          e.webContents.debugger.attach(`1.3`);
        } catch (e) {
          if (
            (e instanceof Error ? e.message : String(e)).includes(
              `already attached`,
            )
          )
            return;
          throw e;
        }
    }
    async attachDebuggerTarget(e, t) {
      if (this.debuggerSessionIdForTarget(e, t) != null) return;
      let n = await this.sendDebuggerCommand(e, `Target.attachToTarget`, {
        flatten: !0,
        targetId: t,
      });
      if (typeof n.sessionId != `string`)
        throw Error(`Target.attachToTarget did not return a sessionId`);
      this.rememberDebuggerTargetSession(e, t, n.sessionId);
    }
    async detachDebuggerTarget(e, t) {
      let n = this.debuggerSessionIdForTarget(e, t);
      if (n != null)
        try {
          await this.sendDebuggerCommand(e, `Target.detachFromTarget`, {
            sessionId: n,
          });
        } finally {
          this.forgetDebuggerTargetSession(n);
        }
    }
    debuggerSessionIdForTarget(e, t) {
      return this.debuggerTargetSessionsByTabId.get(e.cdpTabId)?.get(t) ?? null;
    }
    rememberDebuggerTargetSession(e, t, n) {
      let r = this.debuggerTargetSessionsByTabId.get(e.cdpTabId) ?? new Map();
      (this.debuggerTargetSessionsByTabId.set(e.cdpTabId, r),
        r.set(t, n),
        this.debuggerTargetIdsBySessionId.set(n, t));
    }
    forgetDebuggerTargetSession(e) {
      this.debuggerTargetIdsBySessionId.delete(e);
      for (let t of this.debuggerTargetSessionsByTabId.values())
        for (let [n, r] of t.entries()) r === e && t.delete(n);
    }
    forgetDebuggerTargetSessionsForTab(e) {
      let t = this.debuggerTargetSessionsByTabId.get(e.cdpTabId);
      if (t != null) {
        for (let e of t.values()) this.debuggerTargetIdsBySessionId.delete(e);
        this.debuggerTargetSessionsByTabId.delete(e.cdpTabId);
      }
    }
    async detachTab(e, t = this.tabRuntimesById.get(e.cdpTabId) ?? null) {
      if (t == null || t.webContents.isDestroyed()) {
        this.forgetDebuggerTargetSessionsForTab(e);
        return;
      }
      if (!t.webContents.debugger.isAttached()) {
        this.forgetDebuggerTargetSessionsForTab(e);
        return;
      }
      (t.webContents.debugger.detach(),
        this.forgetDebuggerTargetSessionsForTab(e));
    }
    async ensureTabReadyForCdp(e) {
      if (e.webContents.isDestroyed())
        throw Error(`Unknown tab: ${e.cdpTabId}`);
      e.webContents.getURL().length > 0 ||
        (await FX(
          e.webContents.loadURL(this.initialPageUrl),
          this.pageReadyTimeoutMs,
          `Timed out loading initial page "${this.initialPageUrl}" for tab ${e.cdpTabId}`,
        ));
    }
    async waitForTabReadyForCdp(e, t) {
      let n = this.getBrowserHost(e);
      (n?.waitForPageReady != null &&
        (await FX(
          n.waitForPageReady(t.browserTabId),
          this.pageReadyTimeoutMs,
          `Timed out waiting for tab ${t.cdpTabId} to finish its initial navigation`,
        ),
        this.assertTabMatchesCurrentBrowserUsePage(e, t)),
        await this.ensureTabReadyForCdp(t));
    }
    async navigateTabToInitialPage(e) {
      if (e.webContents.isDestroyed())
        throw Error(`Unknown tab: ${e.cdpTabId}`);
      e.webContents.getURL() !== this.initialPageUrl &&
        (await FX(
          e.webContents.loadURL(this.initialPageUrl),
          this.pageReadyTimeoutMs,
          `Timed out loading initial page "${this.initialPageUrl}" for tab ${e.cdpTabId}`,
        ),
        (e.url = this.initialPageUrl));
    }
    async executeTranslatedInputCommand(e, t, n) {
      if (!n.method.startsWith(`Input.`) || nX(t, n.target)) return null;
      if (!AW(n.method))
        throw Error(
          `${n.method} is not currently supported in the in-app browser because input CDP commands are translated to JavaScript to preserve focus`,
        );
      this.assertTabMatchesCurrentBrowserUsePage(e, t);
      let r = await FX(
        t.webContents.executeJavaScript(
          PX({ method: n.method, params: n.commandParams ?? {} }),
        ),
        this.cdpCommandTimeoutMs,
        `Timed out translating CDP input command "${n.method}" for tab ${t.cdpTabId}`,
      );
      if (vX(r) && r.ok === !0) return {};
      let i =
        vX(r) && typeof r.error == `string`
          ? r.error
          : `unknown translation failure`;
      throw Error(
        `Unable to translate ${n.method} in the in-app browser: ${i}`,
      );
    }
    async executeEmulatedTargetCommand(e, t) {
      if (t.method === `Target.getTargets`)
        return {
          targetInfos: [
            ...[...this.tabsById.values()]
              .filter((t) => t.routeKey === e.routeKey)
              .map((e) => aX(e)),
            ...(await this.getDebuggerFrameTargetInfos(e)),
          ],
        };
      if (t.method === `Target.closeTarget`) {
        if (oX(t) !== iX(e))
          throw Error(
            `Target.closeTarget can only close the current in-app browser tab`,
          );
        return (
          await this.cleanupDebuggerForTabBestEffort(e, `target-close`),
          this.closeTab(e),
          { success: !0 }
        );
      }
      throw Error(
        `Browser Use CDP method is not supported in the in-app browser: ${t.method}`,
      );
    }
    async executeCdpCommandWithCaptureSurface(e, t, n) {
      let r = mX(n);
      if (t == null || r == null) return this.executeCdpCommand(e, n);
      t.setCaptureSurfaceForBrowserUse(r, e.browserTabId);
      try {
        return (
          await this.waitForCaptureSurface(e, r),
          await this.executeCdpCommand(e, n)
        );
      } finally {
        t.setCaptureSurfaceForBrowserUse(null, e.browserTabId);
      }
    }
    async executeCdpCommand(e, t) {
      return (
        dX(t.method),
        this.sendDebuggerCommand(e, t.method, t.commandParams ?? {}, t.target)
      );
    }
    async sendDebuggerCommand(e, t, n, r = {}) {
      let i =
        r.sessionId ??
        (r.targetId == null
          ? void 0
          : (this.debuggerSessionIdForTarget(e, r.targetId) ?? void 0));
      if (r.targetId != null && r.targetId !== iX(e) && i == null)
        throw Error(
          `No in-app browser debugger session is attached for target ${r.targetId}`,
        );
      return await FX(
        e.webContents.debugger.sendCommand(t, n, i),
        this.cdpCommandTimeoutMs,
        `Timed out running CDP command "${t}" for tab ${e.cdpTabId}`,
      );
    }
    async getDebuggerFrameTargetInfos(e) {
      let t = await this.sendDebuggerCommand(e, `Target.getTargets`, {}).catch(
        () => null,
      );
      return !vX(t) || !Array.isArray(t.targetInfos)
        ? []
        : t.targetInfos.filter(cX).map((t) => ({ ...t, tabId: e.cdpTabId }));
    }
    async waitForCaptureSurface(e, t) {
      let n = Date.now() + GY;
      do {
        let r;
        try {
          r = await FX(
            e.webContents.debugger.sendCommand(`Page.getLayoutMetrics`, {}),
            Math.max(1, n - Date.now()),
            `Timed out reading layout metrics for tab ${e.cdpTabId}`,
          );
        } catch {
          return;
        }
        if (hX(r, t)) return;
        await MX(KY);
      } while (Date.now() < n);
    }
    pruneExpiredAllowedDownloadGrants() {
      let e = Date.now() - qY;
      for (let [t, n] of this.allowedDownloadGrantsByWebContentsId)
        n.grantedAtMs < e &&
          this.allowedDownloadGrantsByWebContentsId.delete(t);
    }
  };
function eX(e, t) {
  let n = new Map();
  for (let r of e) {
    if (r == null) throw Error(`finalizeTabs received invalid tab entry`);
    let { tabId: e, status: i } = r;
    if (!Number.isInteger(e))
      throw Error(`finalizeTabs requires an integer tabId`);
    if (!t.has(e)) throw Error(`finalizeTabs cannot keep unknown tab ${e}`);
    if (i !== `handoff` && i !== `deliverable`)
      throw Error(`finalizeTabs received invalid status ${String(i)}`);
    if (n.has(e)) throw Error(`finalizeTabs received duplicate tab ${e}`);
    n.set(e, i);
  }
  return n;
}
function tX(e) {
  return e === `Target.getTargets` || e === `Target.closeTarget`;
}
function nX(e, t) {
  return t.sessionId != null || (t.targetId != null && t.targetId !== iX(e));
}
function rX(e) {
  return (
    e instanceof Error &&
    e.message.includes(
      `Input targets inside cross-origin or inaccessible iframes`,
    )
  );
}
function iX(e) {
  return `browser-use-iab-tab:${e.cdpTabId}`;
}
function aX(e) {
  let t = iX(e);
  return {
    attached: !0,
    canAccessOpener: !1,
    id: t,
    tabId: e.cdpTabId,
    targetId: t,
    title: e.title,
    type: `page`,
    url: e.url,
  };
}
function oX(e) {
  if (!vX(e.commandParams)) return null;
  let t = e.commandParams.targetId;
  return typeof t == `string` ? t : null;
}
function sX(e) {
  return e == null ? null : (uX(e, `targetId`) ?? uX(e, `id`));
}
function cX(e) {
  if (!vX(e)) return !1;
  let t = uX(e, `type`);
  return t !== `iframe` && (t !== `other` || uX(e, `url`) == null)
    ? !1
    : sX(e) != null;
}
function lX(e, t) {
  let n = e[t];
  return vX(n) ? n : null;
}
function uX(e, t) {
  let n = e[t];
  return typeof n == `string` ? n : null;
}
function dX(e) {
  if (e.startsWith(`Target.`) && e !== `Target.setAutoAttach`)
    throw Error(
      `Browser Use CDP method is not supported in the in-app browser: ${e}`,
    );
}
function fX(e) {
  if (e.method !== `Page.navigate` || !vX(e.commandParams)) return null;
  let t = e.commandParams.url;
  return typeof t == `string` ? t : null;
}
function pX(e) {
  if (e.method !== `Page.navigateToHistoryEntry` || !vX(e.commandParams))
    return null;
  let t = e.commandParams.entryId;
  return typeof t == `number` ? t : null;
}
function mX(e) {
  if (
    e.method !== `Page.captureScreenshot` ||
    !vX(e.commandParams) ||
    e.commandParams.captureBeyondViewport !== !0
  )
    return null;
  let t = e.commandParams.clip;
  if (!vX(t)) return null;
  let n = t.width,
    r = t.height;
  return typeof n != `number` ||
    typeof r != `number` ||
    !Number.isFinite(n) ||
    !Number.isFinite(r) ||
    n <= 0 ||
    r <= 0
    ? null
    : { width: Math.ceil(n), height: Math.ceil(r) };
}
function hX(e, t) {
  let n = gX(e);
  return n == null
    ? !1
    : _X(n, `clientWidth`, `width`) >= t.width &&
        _X(n, `clientHeight`, `height`) >= t.height;
}
function gX(e) {
  return vX(e)
    ? vX(e.cssVisualViewport)
      ? e.cssVisualViewport
      : vX(e.visualViewport)
        ? e.visualViewport
        : null
    : null;
}
function _X(e, t, n) {
  let r = e[t];
  if (typeof r == `number`) return r;
  let i = e[n];
  return typeof i == `number` ? i : 0;
}
function vX(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function yX(e) {
  if (!vX(e)) return null;
  let t = e.reason;
  return typeof t == `string` ? t : null;
}
function bX(e) {
  if (!vX(e)) return null;
  let t = e.exitCode;
  return typeof t == `number` ? t : null;
}
function xX(e) {
  return e.slice(e.indexOf(`:`) + 1);
}
function SX(e, t) {
  return `${e}\0${t}`;
}
function CX(e, t) {
  let r = `${e}\0`;
  return t.startsWith(r) ? n._c(t.slice(r.length)) : null;
}
function wX(e, t, n) {
  return DX(e) ? (OX(t, n) ? t : VY) : e || t;
}
function TX(e, t, n, r = `agent`) {
  return t.length === 0
    ? (n ?? e)
    : DX(t)
      ? OX(e, r)
        ? e
        : n != null && OX(n, r)
          ? n
          : VY
      : t;
}
function EX(e, t) {
  return OX(t.url, t.origin) &&
    t.title.length > 0 &&
    t.title !== VY &&
    (e.length === 0 || e === VY)
    ? t.title
    : e;
}
function DX(e) {
  return e === VY;
}
function OX(e, t) {
  return t === `user` && e.length > 0 && e !== VY;
}
function kX(e, t, n, r) {
  return n.length > 0 &&
    n !== `about:blank` &&
    r.length > 0 &&
    r !== `about:blank` &&
    (e.length === 0 ||
      e === `about:blank` ||
      t.length === 0 ||
      t === `about:blank`)
    ? n
    : e || n;
}
function AX(e, t) {
  if (!Number.isFinite(t.x) || !Number.isFinite(t.y))
    throw Error(`${e} requires finite x and y coordinates`);
}
function jX(e) {
  typeof e == `object` && `unref` in e && e.unref();
}
async function MX(e) {
  await new Promise((t) => {
    jX(setTimeout(t, e));
  });
}
function NX(e) {
  return e instanceof Error ? e.message.startsWith(`Timed out`) : !1;
}
function PX(e) {
  return `(${jW.toString()})(${JSON.stringify(e)});`;
}
function FX(e, t, n) {
  return new Promise((r, i) => {
    let a = !1,
      o = setTimeout(() => {
        a || ((a = !0), i(Error(n)));
      }, t);
    (o.unref(),
      e.then(
        (e) => {
          a || ((a = !0), clearTimeout(o), r(e));
        },
        (e) => {
          a || ((a = !0), clearTimeout(o), i(e));
        },
      ));
  });
}
var IX = r.a(`browser-use-native-pipe-server`);
async function LX({
  apiImpl: e,
  nativePipeDirectory: t,
  pipePath: n,
  socketPeerAuthorizer: r = Ud(),
}) {
  let i;
  try {
    i = await Sd({
      nativePipeDirectory: t,
      pipePath: n,
      socketPeerAuthorizer: r,
      events: {
        onListening: (e) => {
          IX().info(`browser-use native pipe listening`, {
            safe: {},
            sensitive: { pipePath: e },
          });
        },
        onAuthorizationError: (e) => {
          IX().warning(`browser-use native pipe peer authorization failed`, {
            safe: {},
            sensitive: { error: e },
          });
        },
        onRejectedSocket: (e) => {
          IX().warning(`browser-use native pipe rejected socket peer`, {
            safe: { reason: e.reason ?? `unauthorized` },
            sensitive: {},
          });
        },
        onSocketError: (e) => {
          IX().warning(`browser-use native pipe socket error`, {
            safe: {},
            sensitive: { error: e },
          });
        },
      },
    });
  } catch (t) {
    throw (e.dispose(), t);
  }
  let a = new It(i, e, {
      onMoveMouseError: (e, t) => {
        IX().warning(`IAB_LIFECYCLE failed to move browser use cursor`, {
          safe: { tabId: t.tabId },
          sensitive: { error: e },
        });
      },
    }),
    o = e.addCdpEventListener((e) => {
      a.sendCdpEvent(e);
    }),
    s = e[XY]((e) => {
      a.sendDownloadChange(e);
    });
  return {
    pipePath: i.pipePath,
    dispose: async () => {
      (o(), s(), e.dispose(), await i.close());
    },
  };
}
var RX = r.a(`browser-session-registry`);
function zX(e) {
  return e;
}
function BX() {
  return { apiImpl: null, server: null, starting: null };
}
var VX = 200,
  HX = class {
    delegate = null;
    browserUseNativePipeEnabled = !1;
    backendStatesBySessionId = new Map();
    backendDisposalsBySessionId = new Map();
    pendingTurnEndsBySessionId = new Map();
    deferredSessionActivityEnds = new Set();
    hostsByRouteKey = new Map();
    debugEvents = [];
    nextDebugEventSequence = 1;
    sessionRoutes = new Map();
    windows = new Map();
    tabModesByRouteKey = new Map();
    constructor(
      e = {
        appSessionId: i.i,
        buildFlavor: i.a.Dev,
        errorReporter: { reportNonFatal: () => void 0 },
      },
    ) {
      this.options = e;
    }
    setDelegate(e) {
      this.delegate = e;
    }
    recordDebugEvent({ details: e = [], ...t }) {
      (this.debugEvents.push({
        ...t,
        details: [...e],
        sequence: this.nextDebugEventSequence,
        timestampMs: Date.now(),
      }),
        (this.nextDebugEventSequence += 1),
        this.debugEvents.length > VX && this.debugEvents.shift());
    }
    getDebugSnapshot() {
      return {
        events: this.debugEvents.map((e) => ({
          ...e,
          details: [...e.details],
        })),
        sessionRoutes: Array.from(this.sessionRoutes.values()).map((e) => {
          let t = Array.from(
              this.windows.get(e.windowId)?.pages.values() ?? [],
            ),
            n =
              t.find(
                (t) =>
                  t.conversationId === e.browserConversationId &&
                  t.browserTabId === yL(e.browserConversationId),
              ) ??
              t.find((t) => t.conversationId === e.browserConversationId) ??
              null;
          return {
            conversationId: e.sessionId,
            disposeAfterSessionActivity: e.disposeAfterSessionActivity,
            ownerWebContentsId: e.ownerWebContentsId,
            page: n == null ? null : { ...n },
            windowId: e.windowId,
          };
        }),
      };
    }
    setBrowserUseNativePipeEnabled(e) {
      if (this.browserUseNativePipeEnabled !== e) {
        if (((this.browserUseNativePipeEnabled = e), !e)) {
          for (let e of this.backendStatesBySessionId.keys())
            this.disposeBackendForSession(e);
          return;
        }
        for (let e of this.sessionRoutes.values())
          this.ensureBackendForSession(e.sessionId);
      }
    }
    setRouteTabMode(e, t) {
      let n = WX(e),
        r = t === `multiTab` ? `multiTab` : `singleTab`;
      this.getRouteTabMode(e) !== r &&
        (this.tabModesByRouteKey.set(n, r),
        RX().info(`IAB_LIFECYCLE set browser route tab mode`, {
          safe: {
            conversationId: e.conversationId,
            requestedTabMode: t,
            tabMode: r,
            windowId: e.windowId,
          },
          sensitive: {},
        }));
    }
    getRouteTabMode(e) {
      return this.tabModesByRouteKey.get(WX(e)) ?? `singleTab`;
    }
    captureSessionRoute({
      conversationId: e,
      browserConversationId: t = e,
      disposeAfterSessionActivity: n = !1,
      multiTabBrowserUseEnabled: r = !1,
      owner: i,
    }) {
      let a = c.BrowserWindow.fromWebContents(i);
      if (a == null || a.isDestroyed()) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          guestWebContentsId: null,
          kind: `session-route-capture-failed`,
          message: `Failed to capture Browser Use session route`,
          ownerWebContentsId: i.id,
          pageKey: null,
          windowId: null,
        }),
          RX().warning(`IAB_LIFECYCLE failed to capture session route`, {
            safe: { conversationId: e, ownerWebContentsId: i.id },
            sensitive: {},
          }));
        return;
      }
      let o = zX(a.id),
        s = { conversationId: t, windowId: o },
        l = r ? `multiTab` : this.getRouteTabMode(s);
      this.setRouteTabMode(s, l);
      let u = this.sessionRoutes.get(e);
      if (u == null && t !== e) {
        let n = this.sessionRoutes.get(t);
        n?.browserConversationId === t &&
          n.windowId === o &&
          (this.sessionRoutes.delete(t),
          this.disposeBackendForSession(t),
          (n.sessionId = e),
          this.sessionRoutes.set(e, n),
          (u = n));
      }
      if (u != null) {
        if (u.windowId === o) {
          if (
            (this.ensureBackendForSession(e),
            u.browserConversationId !== t &&
              (this.reassignRoute(
                {
                  conversationId: u.browserConversationId,
                  windowId: u.windowId,
                },
                s,
                void 0,
                void 0,
                e,
              ),
              this.setRouteTabMode(s, l)),
            u.ownerWebContentsId !== i.id)
          ) {
            let t = u.ownerWebContentsId;
            ((u.ownerWebContentsId = i.id),
              this.recordDebugEvent({
                browserTabId: null,
                conversationId: e,
                details: [
                  { label: `previousOwnerWebContentsId`, value: String(t) },
                ],
                guestWebContentsId: null,
                kind: `session-route-owner-rebound`,
                message: `Rebound Browser Use session route owner`,
                ownerWebContentsId: i.id,
                pageKey: null,
                windowId: a.id,
              }),
              RX().info(`IAB_LIFECYCLE rebound session route owner`, {
                safe: {
                  conversationId: e,
                  ownerWebContentsId: i.id,
                  previousOwnerWebContentsId: t,
                  windowId: a.id,
                },
                sensitive: {},
              }));
          }
          n &&
            !u.disposeAfterSessionActivity &&
            ((u.disposeAfterSessionActivity = !0),
            this.recordDebugEvent({
              browserTabId: null,
              conversationId: e,
              guestWebContentsId: null,
              kind: `session-route-disposal-updated`,
              message: `Marked Browser Use session route disposable`,
              ownerWebContentsId: i.id,
              pageKey: null,
              windowId: a.id,
            }),
            RX().info(`IAB_LIFECYCLE updated session route disposal`, {
              safe: {
                conversationId: e,
                disposeAfterSessionActivity: n,
                ownerWebContentsId: i.id,
                windowId: a.id,
              },
              sensitive: {},
            }));
          return;
        }
        let r = {
            conversationId: u.browserConversationId,
            windowId: u.windowId,
          },
          c = this.getPageRecordForConversation(
            r.windowId,
            u.browserConversationId,
          );
        if (c == null) {
          let t = u.ownerWebContentsId,
            o = r.windowId;
          (this.reassignRoute(r, s, void 0, void 0, e),
            (u.ownerWebContentsId = i.id),
            n &&
              !u.disposeAfterSessionActivity &&
              (u.disposeAfterSessionActivity = !0),
            this.setRouteTabMode(s, l),
            this.recordDebugEvent({
              browserTabId: null,
              conversationId: e,
              details: [
                { label: `previousOwnerWebContentsId`, value: String(t) },
                { label: `previousWindowId`, value: String(o) },
              ],
              guestWebContentsId: null,
              kind: `session-route-capture-reassigned`,
              message: `Reassigned duplicate Browser Use session route capture`,
              ownerWebContentsId: i.id,
              pageKey: null,
              windowId: a.id,
            }),
            RX().info(`IAB_LIFECYCLE reassigned duplicate session route`, {
              safe: {
                conversationId: e,
                ownerWebContentsId: i.id,
                previousOwnerWebContentsId: t,
                previousWindowId: o,
                windowId: a.id,
              },
              sensitive: {},
            }));
          return;
        }
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          details: [
            {
              label: `existingOwnerWebContentsId`,
              value: String(u.ownerWebContentsId),
            },
            { label: `existingWindowId`, value: String(u.windowId) },
          ],
          guestWebContentsId: c.guestWebContentsId,
          kind: `session-route-capture-ignored`,
          message: `Ignored duplicate Browser Use session route capture`,
          ownerWebContentsId: i.id,
          pageKey: c.pageKey,
          windowId: a.id,
        }),
          RX().warning(
            `IAB_LIFECYCLE ignored duplicate session route capture`,
            {
              safe: {
                conversationId: e,
                existingOwnerWebContentsId: u.ownerWebContentsId,
                existingWindowId: u.windowId,
                ownerWebContentsId: i.id,
                windowId: a.id,
              },
              sensitive: {},
            },
          ));
        return;
      }
      this.captureSessionRouteForWindow({
        browserConversationId: t,
        disposeAfterSessionActivity: n,
        ownerWebContentsId: i.id,
        sessionId: e,
        windowId: o,
      });
    }
    bindSessionRouteOwner({
      browserConversationId: e,
      owner: t,
      sessionId: n,
    }) {
      let r = this.sessionRoutes.get(n);
      if (r == null) {
        this.captureSessionRoute({
          browserConversationId: e,
          conversationId: n,
          owner: t,
        });
        return;
      }
      let i = c.BrowserWindow.fromWebContents(t);
      if (i == null || i.isDestroyed()) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: n,
          guestWebContentsId: null,
          kind: `session-route-bind-failed`,
          message: `Failed to bind Browser Use session route owner`,
          ownerWebContentsId: t.id,
          pageKey: null,
          windowId: null,
        }),
          RX().warning(`IAB_LIFECYCLE failed to rebind session route`, {
            safe: { conversationId: n, ownerWebContentsId: t.id },
            sensitive: {},
          }));
        return;
      }
      let a = { conversationId: r.browserConversationId, windowId: r.windowId },
        o = { conversationId: e, windowId: zX(i.id) },
        s = this.getRouteTabMode(o),
        l = this.sessionRoutes.get(e);
      if (
        (e !== n &&
          l?.sessionId === e &&
          l.browserConversationId === e &&
          (this.sessionRoutes.delete(e), this.disposeBackendForSession(e)),
        a.windowId === o.windowId &&
          a.conversationId === o.conversationId &&
          r.ownerWebContentsId === t.id)
      )
        return;
      let u = this.getPageRecordForConversation(
        a.windowId,
        r.browserConversationId,
      );
      if (a.windowId !== o.windowId && u != null) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: n,
          details: [
            {
              label: `previousOwnerWebContentsId`,
              value: String(r.ownerWebContentsId),
            },
            { label: `previousWindowId`, value: String(a.windowId) },
          ],
          guestWebContentsId: u.guestWebContentsId,
          kind: `session-route-live-rebind-ignored`,
          message: `Ignored Browser Use session route rebind with existing page`,
          ownerWebContentsId: t.id,
          pageKey: u.pageKey,
          windowId: o.windowId,
        }),
          RX().warning(`IAB_LIFECYCLE ignored session route rebind`, {
            safe: {
              conversationId: n,
              ownerWebContentsId: t.id,
              previousOwnerWebContentsId: r.ownerWebContentsId,
              previousWindowId: a.windowId,
              windowId: o.windowId,
            },
            sensitive: {},
          }));
        return;
      }
      let d = r.ownerWebContentsId,
        f = a.windowId;
      (this.reassignRoute(a, o, void 0, void 0, n),
        this.setRouteTabMode(o, s),
        (r.ownerWebContentsId = t.id),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: n,
          details: [
            { label: `previousOwnerWebContentsId`, value: String(d) },
            { label: `previousWindowId`, value: String(f) },
          ],
          guestWebContentsId: null,
          kind: `session-route-owner-bound`,
          message: `Bound Browser Use session route to exact owner`,
          ownerWebContentsId: t.id,
          pageKey: null,
          windowId: o.windowId,
        }),
        RX().info(`IAB_LIFECYCLE rebound captured session route`, {
          safe: {
            conversationId: n,
            ownerWebContentsId: t.id,
            previousOwnerWebContentsId: d,
            previousWindowId: f,
            windowId: o.windowId,
          },
          sensitive: {},
        }));
    }
    ensureSessionRoute(e) {
      let t = this.sessionRoutes.get(e.conversationId);
      if (t != null) {
        if (this.delegate?.isWindowAlive(t.windowId) === !0) return !0;
        (this.disposeBrowserUseRoute(
          { conversationId: t.browserConversationId, windowId: t.windowId },
          t.sessionId,
        ),
          this.recordDebugEvent({
            browserTabId: null,
            conversationId: e.conversationId,
            guestWebContentsId: null,
            kind: `session-route-stale-discarded`,
            message: `Discarded stale Browser Use session route`,
            ownerWebContentsId: t.ownerWebContentsId,
            pageKey: null,
            windowId: t.windowId,
          }),
          RX().warning(`IAB_LIFECYCLE discarded stale session route`, {
            safe: {
              conversationId: e.conversationId,
              ownerWebContentsId: t.ownerWebContentsId,
              windowId: t.windowId,
            },
            sensitive: {},
          }));
      }
      return (
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `session-route-missing`,
          message: `Missing exact Browser Use session route owner`,
          ownerWebContentsId: null,
          pageKey: null,
          windowId: null,
        }),
        RX().warning(`IAB_LIFECYCLE missing exact session route owner`, {
          safe: { conversationId: e.conversationId },
          sensitive: {},
        }),
        !1
      );
    }
    captureSessionRouteForWindow({
      browserConversationId: e,
      disposeAfterSessionActivity: t,
      ownerWebContentsId: n,
      sessionId: r,
      windowId: i,
    }) {
      (this.sessionRoutes.set(r, {
        browserConversationId: e,
        disposeAfterSessionActivity: t,
        ownerWebContentsId: n,
        sessionId: r,
        windowId: i,
      }),
        this.ensureBackendForSession(r),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: r,
          details: [{ label: `disposeAfterSessionActivity`, value: String(t) }],
          guestWebContentsId: null,
          kind: `session-route-captured`,
          message: `Captured Browser Use session route`,
          ownerWebContentsId: n,
          pageKey: null,
          windowId: i,
        }),
        RX().info(`IAB_LIFECYCLE captured session route`, {
          safe: {
            conversationId: r,
            disposeAfterSessionActivity: t,
            ownerWebContentsId: n,
            windowId: i,
          },
          sensitive: {},
        }));
    }
    getBrowserUseHost(e) {
      let t = this.resolveBrowserRoute(e);
      return this.getOrCreateBrowserUseHost(t);
    }
    turnEnded({ conversationId: e, turnId: t }) {
      if (!this.sessionRoutes.has(e)) return Promise.resolve();
      let n = this.pendingTurnEndsBySessionId.get(e),
        r =
          n == null
            ? this.dispatchTurnEnded({ conversationId: e, turnId: t })
            : n
                .catch(() => void 0)
                .then(() =>
                  this.dispatchTurnEnded({ conversationId: e, turnId: t }),
                );
      this.pendingTurnEndsBySessionId.set(e, r);
      let i = () => {
        this.pendingTurnEndsBySessionId.get(e) === r &&
          this.pendingTurnEndsBySessionId.delete(e);
      };
      return (r.then(i, i), r);
    }
    endSessionActivity({ conversationId: e }) {
      let t = this.pendingTurnEndsBySessionId.get(e);
      if (t != null) {
        if (this.deferredSessionActivityEnds.has(e)) return;
        (this.deferredSessionActivityEnds.add(e),
          t
            .catch(() => void 0)
            .then(() => {
              (this.deferredSessionActivityEnds.delete(e),
                this.endSessionActivity({ conversationId: e }));
            }));
        return;
      }
      let n = this.sessionRoutes.get(e);
      if (n == null) return;
      let r = { conversationId: n.browserConversationId, windowId: n.windowId },
        i = WX(r);
      this.backendStatesBySessionId
        .get(e)
        ?.apiImpl?.releaseSessionControl({ conversationId: e })
        .catch(() => void 0);
      let a = this.hostsByRouteKey.get(i);
      (a == null
        ? this.delegate?.setBrowserUseActive(r, !1)
        : a.setBrowserUseActive(!1),
        n.disposeAfterSessionActivity &&
          this.disposeBrowserUseRoute(r, n.sessionId),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: n.sessionId,
          guestWebContentsId: null,
          kind: `session-activity-ended`,
          message: `Ended Browser Use session activity`,
          ownerWebContentsId: n.ownerWebContentsId,
          pageKey: null,
          windowId: n.windowId,
        }),
        RX().info(`IAB_LIFECYCLE ended browser use session activity`, {
          safe: {
            conversationId: n.sessionId,
            disposeAfterSessionActivity: n.disposeAfterSessionActivity,
            windowId: n.windowId,
          },
          sensitive: {},
        }));
    }
    notifyBrowserUseCursorArrived({ owner: e, payload: t }) {
      let n = this.sessionRoutes.get(t.conversationId);
      if (n != null) {
        if (n.ownerWebContentsId !== e.id) {
          RX().warning(
            `IAB_LIFECYCLE ignored cursor arrival from stale owner`,
            {
              safe: {
                conversationId: t.conversationId,
                expectedOwnerWebContentsId: n.ownerWebContentsId,
                moveSequence: t.moveSequence,
                ownerWebContentsId: e.id,
              },
              sensitive: {},
            },
          );
          return;
        }
        this.backendStatesBySessionId
          .get(t.conversationId)
          ?.apiImpl?.notifyCursorArrived(t);
      }
    }
    consumeAllowedBrowserUseDownload({
      url: e,
      urlChain: t,
      webContentsId: n,
    }) {
      for (let r of this.backendStatesBySessionId.values()) {
        let i = r.apiImpl?.[ZY]({ url: e, urlChain: t, webContentsId: n });
        if (i != null) return i;
      }
      return null;
    }
    notifyBrowserUseDownloadChange(e) {
      e.session_id != null &&
        this.backendStatesBySessionId.get(e.session_id)?.apiImpl?.[QY](e);
    }
    getOrCreateBrowserUseHost(e) {
      this.assertWindowAlive(e);
      let t = WX(e);
      this.ensureBackendForSession(this.getSessionIdForBrowserRoute(e));
      let n = this.hostsByRouteKey.get(t);
      if (n != null) return n;
      let r = new UX(this, e, t);
      return (
        this.hostsByRouteKey.set(t, r),
        RX().info(`IAB_LIFECYCLE created browser use host`, {
          safe: {
            conversationId: e.conversationId,
            routeKey: t,
            windowId: e.windowId,
          },
          sensitive: {},
        }),
        r
      );
    }
    handleWindowClosed(e) {
      let t = zX(e);
      this.windows.delete(t);
      for (let e of this.tabModesByRouteKey.keys())
        e.startsWith(`${t}:`) && this.tabModesByRouteKey.delete(e);
      for (let [e, n] of this.hostsByRouteKey)
        n.windowId === t &&
          (this.backendStatesBySessionId
            .get(
              this.getSessionIdForBrowserRoute({
                conversationId: n.conversationId,
                windowId: n.windowId,
              }),
            )
            ?.apiImpl?.disposeRoute(e),
          this.hostsByRouteKey.delete(e));
      for (let [e, n] of this.sessionRoutes)
        n.windowId === t &&
          (this.sessionRoutes.delete(e), this.disposeBackendForSession(e));
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: null,
        guestWebContentsId: null,
        kind: `browser-window-removed`,
        message: `Removed Browser session window`,
        ownerWebContentsId: null,
        pageKey: null,
        windowId: t,
      }),
        RX().info(`IAB_LIFECYCLE removed browser session window`, {
          safe: { windowId: t },
          sensitive: {},
        }));
    }
    recordGuestAttached({
      browserTabId: e,
      conversationId: t,
      guestWebContentsId: n,
      pageKey: r,
      windowId: i,
    }) {
      let a = this.ensurePageRecord(
        { browserTabId: e, conversationId: t, windowId: i },
        { pageKey: r },
      );
      ((a.guestWebContentsId = n),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t,
          guestWebContentsId: n,
          kind: `browser-guest-attached`,
          message: `Attached Browser sidebar guest`,
          ownerWebContentsId: this.windows.get(i)?.ownerWebContentsId ?? null,
          pageKey: r,
          windowId: i,
        }),
        RX().info(`IAB_LIFECYCLE attached browser sidebar guest`, {
          safe: {
            conversationId: t,
            browserTabId: e,
            guestWebContentsId: n,
            pageKey: r,
            windowId: i,
          },
          sensitive: {},
        }));
    }
    recordPageClosed(e) {
      (this.forgetPageRecord(e),
        RX().info(`IAB_LIFECYCLE closed browser sidebar page`, {
          safe: e,
          sensitive: {},
        }));
    }
    recordPageReleased(e) {
      (this.forgetPageRecord(e),
        RX().info(`IAB_LIFECYCLE released browser sidebar page`, {
          safe: e,
          sensitive: {},
        }));
    }
    forgetPageRecord(e) {
      let t = this.windows.get(e.windowId) ?? null;
      (t?.pages.delete(GX(e)),
        t != null &&
          !this.hasPageRecordForConversation(t, e.conversationId) &&
          t.conversations.delete(e.conversationId));
      let n = WX(e),
        r = t != null && this.hasPageRecordForConversation(t, e.conversationId);
      (t == null || !r) &&
        (this.hostsByRouteKey.delete(n),
        this.backendStatesBySessionId
          .get(this.getSessionIdForBrowserRoute(e))
          ?.apiImpl?.disposeRoute(n));
    }
    recordPageCreated(e) {
      (this.ensurePageRecord(e),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `browser-page-created`,
          message: `Created Browser sidebar page state`,
          ownerWebContentsId:
            this.windows.get(e.windowId)?.ownerWebContentsId ?? null,
          pageKey: null,
          windowId: e.windowId,
        }),
        RX().info(`IAB_LIFECYCLE created browser sidebar page state`, {
          safe: e,
          sensitive: {},
        }));
    }
    reassignRoute(e, t, n, r, i) {
      if (
        e.conversationId === t.conversationId &&
        e.windowId === t.windowId &&
        (r == null || n === r)
      )
        return;
      let a = WX(e),
        o = WX(t),
        s =
          this.sessionRoutes.get(i ?? e.conversationId) ??
          (i == null ? this.getCapturedSessionRouteForBrowserRoute(e) : null),
        c = i ?? null;
      (c == null &&
        s != null &&
        s.sessionId !== e.conversationId &&
        (c = s.sessionId),
        c == null &&
          e.conversationId !== t.conversationId &&
          this.backendStatesBySessionId
            .get(e.conversationId)
            ?.apiImpl?.disposeRoute(a));
      let l = this.tabModesByRouteKey.get(a) ?? null,
        u = this.windows.get(e.windowId) ?? null,
        d = this.ensureWindowRecord(t.windowId);
      for (let [i, a] of u?.pages ?? []) {
        if (
          a.conversationId !== e.conversationId ||
          (n != null && a.browserTabId !== n)
        )
          continue;
        u?.pages.delete(i);
        let o = r ?? a.browserTabId;
        d.pages.set(
          GX({
            browserTabId: o,
            conversationId: t.conversationId,
            windowId: t.windowId,
          }),
          {
            ...a,
            browserTabId: o,
            conversationId: t.conversationId,
            windowId: t.windowId,
          },
        );
      }
      (u != null &&
        !this.hasPageRecordForConversation(u, e.conversationId) &&
        u.conversations.delete(e.conversationId),
        d.conversations.add(t.conversationId));
      let f = this.hostsByRouteKey.get(a) ?? null;
      (f != null &&
        (this.hostsByRouteKey.delete(a),
        f.reassignRoute(t, o),
        this.hostsByRouteKey.set(o, f)),
        l != null &&
          (this.tabModesByRouteKey.delete(a),
          this.tabModesByRouteKey.set(o, l)),
        s?.windowId === e.windowId &&
          s.browserConversationId === e.conversationId &&
          ((s.browserConversationId = t.conversationId),
          (s.windowId = t.windowId),
          c ??
            (this.sessionRoutes.delete(e.conversationId),
            (s.sessionId = t.conversationId),
            this.sessionRoutes.set(t.conversationId, s))),
        c == null
          ? a === o
            ? this.backendStatesBySessionId
                .get(e.conversationId)
                ?.apiImpl?.reassignRoute(a, o, n, r)
            : e.conversationId === t.conversationId
              ? this.backendStatesBySessionId
                  .get(e.conversationId)
                  ?.apiImpl?.reassignRoute(a, o)
              : (this.disposeBackendForSession(e.conversationId),
                this.ensureBackendForSession(t.conversationId))
          : this.backendStatesBySessionId
              .get(c)
              ?.apiImpl?.reassignRoute(a, o, n, r),
        RX().info(`IAB_LIFECYCLE reassigned browser use route`, {
          safe: {
            sourceConversationId: e.conversationId,
            sourceWindowId: e.windowId,
            targetConversationId: t.conversationId,
            targetWindowId: t.windowId,
          },
          sensitive: {},
        }));
    }
    disposeConversation(e) {
      let t = new Map(),
        n = this.sessionRoutes.get(e) ?? null,
        r = n?.browserConversationId ?? e;
      for (let e of this.windows.values()) {
        if (!e.conversations.has(r)) continue;
        let n = { conversationId: r, windowId: e.windowId };
        t.set(WX(n), n);
      }
      for (let e of this.hostsByRouteKey.values()) {
        if (e.conversationId !== r) continue;
        let n = { conversationId: r, windowId: e.windowId };
        t.set(WX(n), n);
      }
      if (n != null) {
        let e = { conversationId: r, windowId: n.windowId };
        t.set(WX(e), e);
      }
      for (let e of t.values()) this.disposeBrowserUseRoute(e, n?.sessionId);
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: e,
        details: [{ label: `routeCount`, value: String(t.size) }],
        guestWebContentsId: null,
        kind: `browser-conversation-disposed`,
        message: `Disposed Browser Use conversation`,
        ownerWebContentsId: null,
        pageKey: null,
        windowId: null,
      }),
        RX().info(`IAB_LIFECYCLE disposed browser use conversation`, {
          safe: { conversationId: e, routeCount: t.size },
          sensitive: {},
        }));
    }
    recordPageTornDown({
      browserTabId: e,
      conversationId: t,
      guestWebContentsId: n,
      windowId: r,
    }) {
      let i =
        this.windows
          .get(r)
          ?.pages.get(
            GX({ browserTabId: e, conversationId: t, windowId: r }),
          ) ?? null;
      (i?.guestWebContentsId === n && (i.guestWebContentsId = null),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t,
          guestWebContentsId: n,
          kind: `browser-guest-torn-down`,
          message: `Tore down Browser sidebar guest`,
          ownerWebContentsId: this.windows.get(r)?.ownerWebContentsId ?? null,
          pageKey: i?.pageKey ?? null,
          windowId: r,
        }),
        RX().warning(`IAB_LIFECYCLE browser sidebar guest torn down`, {
          safe: {
            conversationId: t,
            browserTabId: e,
            guestWebContentsId: n,
            windowId: r,
          },
          sensitive: {},
        }));
    }
    registerThread(e) {
      (this.ensureWindowRecord(e.windowId).conversations.add(e.conversationId),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `browser-thread-registered`,
          message: `Registered Browser sidebar thread`,
          ownerWebContentsId:
            this.windows.get(e.windowId)?.ownerWebContentsId ?? null,
          pageKey: null,
          windowId: e.windowId,
        }),
        RX().info(`IAB_LIFECYCLE registered browser sidebar thread`, {
          safe: e,
          sensitive: {},
        }));
    }
    registerWindow({ ownerWebContentsId: e, windowId: t }) {
      let n = zX(t),
        r = (this.windows.get(n) ?? null)?.ownerWebContentsId,
        i = this.ensureWindowRecord(n);
      if (((i.ownerWebContentsId = e), r != null && r !== e))
        for (let t of this.sessionRoutes.values())
          t.windowId === n && (t.ownerWebContentsId = e);
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: null,
        details:
          r == null
            ? []
            : [{ label: `previousOwnerWebContentsId`, value: String(r) }],
        guestWebContentsId: null,
        kind:
          r == null
            ? `browser-window-registered`
            : `browser-window-owner-rebound`,
        message:
          r == null
            ? `Registered Browser sidebar window`
            : `Rebound Browser sidebar window owner`,
        ownerWebContentsId: e,
        pageKey: null,
        windowId: n,
      }),
        RX().info(
          r == null
            ? `IAB_LIFECYCLE registered browser sidebar window`
            : `IAB_LIFECYCLE rebound browser sidebar window`,
          {
            safe: {
              ownerWebContentsId: e,
              previousOwnerWebContentsId: r,
              windowId: n,
            },
            sensitive: {},
          },
        ));
    }
    resolveBrowserRoute(e) {
      let t = this.sessionRoutes.get(e.conversationId);
      if (t == null)
        throw (
          RX().warning(`IAB_LIFECYCLE missing browser use session route`, {
            safe: e,
            sensitive: {},
          }),
          Error(
            `No ChatGPT browser route captured for browser session ${e.conversationId}`,
          )
        );
      let n = { conversationId: t.browserConversationId, windowId: t.windowId };
      this.assertWindowAlive(n);
      let r = this.getPageRecordForConversation(
        t.windowId,
        t.browserConversationId,
      );
      return (
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t.sessionId,
          guestWebContentsId: r?.guestWebContentsId ?? null,
          kind: `session-route-resolved`,
          message: `Resolved Browser Use route`,
          ownerWebContentsId: t.ownerWebContentsId,
          pageKey: r?.pageKey ?? null,
          windowId: t.windowId,
        }),
        n
      );
    }
    getBrowserRouteForSession(e) {
      let t = this.sessionRoutes.get(e);
      return t == null
        ? null
        : { conversationId: t.browserConversationId, windowId: t.windowId };
    }
    getCapturedSessionRouteForBrowserRoute(e) {
      for (let t of this.sessionRoutes.values())
        if (
          t.browserConversationId === e.conversationId &&
          t.windowId === e.windowId
        )
          return t;
      return null;
    }
    getSessionIdForBrowserRoute(e) {
      return (
        this.getCapturedSessionRouteForBrowserRoute(e)?.sessionId ??
        e.conversationId
      );
    }
    isSubagentBrowserUseRoute(e) {
      return (
        this.getCapturedSessionRouteForBrowserRoute(e)
          ?.disposeAfterSessionActivity === !0
      );
    }
    recordPageSnapshot({
      browserTabId: e,
      conversationId: t,
      guestWebContentsId: n,
      isLoading: r,
      pageKey: i,
      url: a,
      windowId: o,
    }) {
      let s = this.ensurePageRecord(
          { browserTabId: e, conversationId: t, windowId: o },
          { pageKey: i },
        ),
        c = s.guestWebContentsId,
        l = s.isLoading,
        u = s.pageKey;
      ((s.guestWebContentsId = n),
        (s.isLoading = r),
        (s.pageKey = i),
        (s.url = a),
        (c !== n || l !== r || u !== i) &&
          this.recordDebugEvent({
            browserTabId: null,
            conversationId: t,
            details: [{ label: `isLoading`, value: String(r) }],
            guestWebContentsId: n,
            kind: `browser-page-snapshot-updated`,
            message: `Updated Browser sidebar page snapshot`,
            ownerWebContentsId: this.windows.get(o)?.ownerWebContentsId ?? null,
            pageKey: i,
            windowId: o,
          }));
    }
    getPageSnapshot(e) {
      let t = this.windows.get(e.windowId)?.pages.get(GX(e)) ?? null;
      return t == null ? null : { ...t };
    }
    getWindowSnapshot(e) {
      let t = zX(e),
        n = this.windows.get(t) ?? null;
      return n == null
        ? null
        : {
            conversations: Array.from(n.conversations),
            ownerWebContentsId: n.ownerWebContentsId,
            pages: Array.from(n.pages.values()).map((e) => ({ ...e })),
            windowId: t,
          };
    }
    getSnapshotFields() {
      let e = 0,
        t = 0,
        n = 0,
        r = 0;
      for (let i of this.windows.values()) {
        ((e += i.conversations.size), (r += i.pages.size));
        for (let e of i.pages.values())
          (e.guestWebContentsId != null && (t += 1), e.isLoading && (n += 1));
      }
      return {
        browser_use_window_count: this.windows.size,
        browser_use_conversation_count: e,
        browser_use_page_count: r,
        browser_use_guest_webcontents_count: t,
        browser_use_loading_page_count: n,
        browser_use_session_route_count: this.sessionRoutes.size,
      };
    }
    assertWindowAlive(e) {
      if (this.delegate?.isWindowAlive(e.windowId) !== !0)
        throw (
          RX().warning(`IAB_LIFECYCLE browser use bound window unavailable`, {
            safe: e,
            sensitive: {},
          }),
          Error(
            `ChatGPT browser window ${e.windowId} is no longer available for browser session ${e.conversationId}`,
          )
        );
    }
    async dispatchTurnEnded({ conversationId: e, turnId: t }) {
      let n = this.backendStatesBySessionId.get(e);
      (n?.starting != null && (await n.starting),
        await this.backendStatesBySessionId
          .get(e)
          ?.apiImpl?.turnEnded({ session_id: e, turn_id: t }));
    }
    ensureBackendForSession(e) {
      if (!this.browserUseNativePipeEnabled) return;
      let t = this.backendStatesBySessionId.get(e) ?? BX();
      if (
        (this.backendStatesBySessionId.set(e, t),
        t.server != null || t.starting != null)
      )
        return;
      let n = this.backendDisposalsBySessionId.get(e) ?? null;
      t.starting = (async () => {
        let r = null;
        try {
          if (
            (n != null && (await n),
            !this.browserUseNativePipeEnabled ||
              this.backendStatesBySessionId.get(e) !== t)
          )
            return;
          (RX().info(`browser_use_iab_backend_startup_started`, {
            safe: {
              backend: `iab`,
              platform: process.platform,
              release: c.app.getVersion(),
            },
            sensitive: {
              appSessionId: this.options.appSessionId,
              sessionId: e,
            },
          }),
            (r = new $Y(
              (n) =>
                this.canServeSession(n, e, t)
                  ? this.getBrowserUseHost(n)
                  : null,
              (e) =>
                this.getDelegate().addBrowserUseNavigationBlockedListener(e),
              {
                appSessionId: this.options.appSessionId,
                buildFlavor: this.options.buildFlavor,
                ensureSessionRoute: (n) => this.canServeSession(n, e, t),
                getTabMode: (e) =>
                  this.tabModesByRouteKey.get(e) ?? `singleTab`,
              },
            )),
            (t.apiImpl = r),
            (t.server = await LX({ apiImpl: r })),
            RX().info(`browser_use_iab_backend_startup_ready`, {
              safe: {
                backend: `iab`,
                platform: process.platform,
                release: c.app.getVersion(),
              },
              sensitive: { pipePath: t.server.pipePath, sessionId: e },
            }));
        } catch (n) {
          (RX().warning(`browser_use_iab_backend_startup_failed`, {
            safe: {
              backend: `iab`,
              phase: `native-pipe-startup`,
              platform: process.platform,
              reason: n instanceof Error ? n.name || `Error` : typeof n,
              release: c.app.getVersion(),
            },
            sensitive: { error: n, sessionId: e },
          }),
            r?.dispose(),
            (t.apiImpl = null),
            this.options.errorReporter.reportNonFatal(
              n instanceof Error ? n : Error(String(n)),
              { kind: `browser-use-native-pipe-startup` },
            ));
        } finally {
          t.starting = null;
        }
      })();
    }
    canServeSession(e, t, n) {
      return e.conversationId !== t ||
        this.backendStatesBySessionId.get(t) !== n
        ? !1
        : this.ensureSessionRoute(e);
    }
    async disposeBackendForSession(e) {
      let t = this.backendStatesBySessionId.get(e);
      if (t == null) {
        await this.backendDisposalsBySessionId.get(e);
        return;
      }
      this.backendStatesBySessionId.delete(e);
      let n = (async () => {
        (t.starting != null && (await t.starting),
          await t.server?.dispose(),
          (t.server = null),
          (t.apiImpl = null));
      })();
      this.backendDisposalsBySessionId.set(e, n);
      try {
        await n;
      } finally {
        this.backendDisposalsBySessionId.get(e) === n &&
          this.backendDisposalsBySessionId.delete(e);
      }
    }
    disposeBrowserUseRoute(e, t = this.getSessionIdForBrowserRoute(e)) {
      let n = WX(e);
      (this.backendStatesBySessionId.get(t)?.apiImpl?.disposeRoute(n),
        this.disposeBackendForSession(t),
        this.hostsByRouteKey.delete(n));
      let r = this.sessionRoutes.get(t);
      (r?.browserConversationId === e.conversationId &&
        r.windowId === e.windowId &&
        this.sessionRoutes.delete(t),
        this.tabModesByRouteKey.delete(n),
        this.delegate?.disposeBrowserUseRoute(e));
      let i = this.windows.get(e.windowId) ?? null;
      if ((i?.conversations.delete(e.conversationId), i != null))
        for (let [t, n] of i.pages)
          n.conversationId === e.conversationId && i.pages.delete(t);
      RX().info(`IAB_LIFECYCLE disposed browser use route`, {
        safe: e,
        sensitive: {},
      });
    }
    getDelegate() {
      if (this.delegate == null)
        throw Error(`Browser session registry is not attached`);
      return this.delegate;
    }
    ensureWindowRecord(e) {
      let t = this.windows.get(e);
      if (t != null) return t;
      let n = {
        conversations: new Set(),
        ownerWebContentsId: -1,
        pages: new Map(),
        windowId: e,
      };
      return (this.windows.set(e, n), n);
    }
    ensurePageRecord(e, t = {}) {
      let n = this.ensureWindowRecord(e.windowId);
      n.conversations.add(e.conversationId);
      let r = GX(e),
        i = n.pages.get(r);
      if (i != null)
        return (t.pageKey !== void 0 && (i.pageKey = t.pageKey), i);
      let a = {
        conversationId: e.conversationId,
        browserTabId: e.browserTabId,
        guestWebContentsId: null,
        isLoading: !1,
        pageKey: t.pageKey ?? null,
        url: ``,
        windowId: e.windowId,
      };
      return (n.pages.set(r, a), a);
    }
    hasPageRecordForConversation(e, t) {
      for (let n of e.pages.values()) if (n.conversationId === t) return !0;
      return !1;
    }
    getPageRecordForConversation(e, t) {
      return (
        this.windows
          .get(e)
          ?.pages.values()
          .find((e) => e.conversationId === t) ?? null
      );
    }
  },
  UX = class {
    conversationId;
    windowId;
    openingPagePromisesByBrowserTabId = new Map();
    routeKey;
    constructor(e, t, n) {
      ((this.registry = e),
        (this.conversationId = t.conversationId),
        (this.windowId = t.windowId),
        (this.routeKey = n));
    }
    reassignRoute(e, t) {
      ((this.conversationId = e.conversationId),
        (this.windowId = e.windowId),
        (this.routeKey = t));
    }
    getPageForBrowserUse(e) {
      return this.registry.getDelegate().getPageForBrowserUse(this.route, e);
    }
    getPagesForBrowserUse() {
      return this.registry.getDelegate().getPagesForBrowserUse(this.route);
    }
    getTabsForBrowserUse() {
      return this.registry.getDelegate().getTabsForBrowserUse(this.route);
    }
    getUserTabsForBrowserUse() {
      return this.registry.getDelegate().getUserTabsForBrowserUse(this.route);
    }
    async openPageForBrowserUse(e) {
      let t = e?.browserTabId ?? yL(this.conversationId);
      this.setBrowserUseActive(!0, t);
      let n = this.openingPagePromisesByBrowserTabId.get(t) ?? null;
      if (n != null) return n;
      let r = this.registry
        .getDelegate()
        .openPageForBrowserUse(this.route, {
          browserTabId: t,
          origin: e?.origin,
          startingUrl: e?.startingUrl,
        })
        .catch((e) => {
          throw (
            this.setBrowserUseActive(!1, t),
            this.closePageForBrowserUse(t),
            e
          );
        })
        .finally(() => {
          this.openingPagePromisesByBrowserTabId.delete(t);
        });
      this.openingPagePromisesByBrowserTabId.set(t, r);
      let i = await r;
      return (
        RX().info(`IAB_LIFECYCLE host opened page`, {
          safe: {
            conversationId: this.conversationId,
            pageKey: i.pageKey,
            routeKey: this.routeKey,
            webContentsId: i.webContents.id,
            windowId: this.windowId,
          },
          sensitive: {},
        }),
        i
      );
    }
    setBrowserVisibleForBrowserUse(e, t) {
      if (this.registry.isSubagentBrowserUseRoute(this.route))
        throw Error(`IAB visibility is not supported in a subagent thread`);
      (e && this.setBrowserUseActive(!0, t),
        this.registry
          .getDelegate()
          .setBrowserVisibleForBrowserUse(this.route, e, t));
    }
    isBrowserVisibleForBrowserUse(e) {
      return this.registry
        .getDelegate()
        .isBrowserVisibleForBrowserUse(this.route, e);
    }
    setViewportForBrowserUse(e, t) {
      (this.setBrowserUseActive(!0, t),
        this.registry.getDelegate().setViewportForBrowserUse(this.route, e, t));
    }
    setCaptureSurfaceForBrowserUse(e, t) {
      this.registry
        .getDelegate()
        .setCaptureSurfaceForBrowserUse(this.route, e, t);
    }
    waitForPendingDebuggerSync(e) {
      return this.registry
        .getDelegate()
        .waitForPendingDebuggerSync(this.route, e);
    }
    waitForPageReady(e) {
      return this.registry.getDelegate().waitForPageReady(this.route, e);
    }
    activateBrowserUseNavigationRestriction(e) {
      this.registry.getDelegate().activateBrowserUseNavigationRestriction(e);
    }
    assertBrowserUseNavigationAllowed(e, t) {
      this.registry.getDelegate().assertBrowserUseNavigationAllowed(e, t);
    }
    trackPendingBrowserUseNavigation(e, t) {
      this.registry.getDelegate().trackPendingBrowserUseNavigation(e, t);
    }
    clearPendingBrowserUseNavigation(e) {
      this.registry.getDelegate().clearPendingBrowserUseNavigation(e);
    }
    setBrowserUseActive(e, t) {
      this.registry.getDelegate().setBrowserUseActive(this.route, e, t);
    }
    setBrowserUseCursor(e, t) {
      return this.registry.getDelegate().setBrowserUseCursor(this.route, e, t);
    }
    closePageForBrowserUse(e) {
      this.registry.getDelegate().closePageForBrowserUse(this.route, e);
    }
    releasePageForBrowserUse(e) {
      this.registry.getDelegate().releasePageForBrowserUse(this.route, e);
    }
    get route() {
      return { conversationId: this.conversationId, windowId: this.windowId };
    }
  };
function WX({ conversationId: e, windowId: t }) {
  return `${t}:${e}`;
}
function GX({ browserTabId: e, conversationId: t }) {
  return OL({ browserTabId: e, conversationId: t });
}
function KX({
  browserSessionRegistry: e,
  comments: t,
  downloadStore: r,
  disposeThreadState: i,
  getBrowserUseRouteBrowserTabIds: a,
  isBrowserUseManagedBrowserTab: o,
  getThreadStateForWindow: s,
  setBrowserUseActiveForRoute: c,
  sourceBrowserTabId: l,
  sourceConversationId: u,
  syncRuntimeStateToPage: d,
  syncThreadState: f,
  targetBrowserTabId: p,
  targetConversationId: m,
  windowState: h,
}) {
  if (u === m && (p == null || l === p)) return;
  let g = s(h, u, l) ?? null;
  if (g == null) return;
  let _ = g.browserTabId,
    v = _ === yL(u),
    y = p ?? (v ? yL(m) : _),
    b = OL(g),
    x = OL({ browserTabId: y, conversationId: m }),
    S = n.Ac(b, x);
  if (h.transferredConversationStateKeys.has(S)) {
    (e.recordDebugEvent({
      browserTabId: g.browserTabId,
      conversationId: m,
      details: [
        { label: `sourceConversationId`, value: u },
        { label: `transferKey`, value: S },
      ],
      guestWebContentsId: g.page?.view.webContents?.id ?? null,
      kind: `browser-route-transfer-replayed`,
      message: `Ignored replayed Browser route transfer`,
      ownerWebContentsId: h.owner.id,
      pageKey: null,
      windowId: h.window.id,
    }),
      h.ignoredConversationIds.add(b));
    return;
  }
  let C = h.threads.get(x) ?? null;
  if (C?.page != null) {
    (e.recordDebugEvent({
      browserTabId: y,
      conversationId: m,
      details: [{ label: `sourceConversationId`, value: u }],
      guestWebContentsId: C.page.view.webContents?.id ?? null,
      kind: `browser-route-transfer-target-exists`,
      message: `Ignored Browser route transfer because target page exists`,
      ownerWebContentsId: h.owner.id,
      pageKey: null,
      windowId: h.window.id,
    }),
      h.ignoredConversationIds.add(b),
      h.transferredConversationStateKeys.add(S));
    return;
  }
  C != null && i(h, m, C);
  let w = g.isBrowserUseActive,
    T = o(u, _),
    E = T && a(h, u).length <= 1,
    D = y === yL(m) || T,
    O = zX(h.window.id),
    k = E && D;
  (k &&
    e.reassignRoute(
      { conversationId: u, windowId: O },
      { conversationId: m, windowId: O },
      _,
      y,
    ),
    qX(
      h,
      { browserTabId: _, conversationId: u },
      { browserTabId: y, conversationId: m },
    ),
    h.threads.set(x, g),
    h.threads.delete(b),
    h.activeConversationId === u &&
      h.activeBrowserTabId === g.browserTabId &&
      ((h.activeConversationId = m), (h.activeBrowserTabId = y)),
    (g.conversationId = m),
    (g.browserTabId = y),
    r.reassignRoute(
      { browserTabId: _, conversationId: u },
      { browserTabId: y, conversationId: m },
    ),
    u !== m && (g.ownerRoutePath = null),
    g.page != null &&
      (!k &&
        T &&
        (e.recordPageClosed({
          browserTabId: _,
          conversationId: u,
          windowId: O,
        }),
        c({ conversationId: u, windowId: O }, !1, _)),
      (g.page.conversationId = m),
      (g.page.browserTabId = y),
      !k &&
        D &&
        e.recordPageCreated({
          browserTabId: y,
          conversationId: m,
          windowId: O,
        }),
      w &&
        ((g.isBrowserUseActive = !1),
        c({ conversationId: m, windowId: O }, !0, y))),
    e.registerThread({ conversationId: m, windowId: zX(h.window.id) }),
    t.transferConversation(h.owner, u, _, m, y),
    h.ignoredConversationIds.add(b),
    h.transferredConversationStateKeys.add(S),
    e.recordDebugEvent({
      browserTabId: y,
      conversationId: m,
      details: [
        { label: `sourceBrowserTabId`, value: _ },
        { label: `sourceConversationId`, value: u },
      ],
      guestWebContentsId: g.page?.view.webContents?.id ?? null,
      kind: `browser-route-transferred`,
      message: `Transferred Browser route state`,
      ownerWebContentsId: h.owner.id,
      pageKey: g.page == null ? null : DL(h.owner.id, m, y),
      windowId: h.window.id,
    }),
    f(h, m, g),
    g.page != null && d(g.page, g));
}
function qX(e, t, r) {
  let i = n.zo(t.conversationId, t.browserTabId),
    a = e.registeredWebviewHostsByRoutePartition.get(i);
  a?.browserTabId === t.browserTabId &&
    a.conversationId === t.conversationId &&
    (e.registeredWebviewHostsByRoutePartition.delete(i),
    e.registeredWebviewHostsByRoutePartition.set(
      n.zo(r.conversationId, r.browserTabId),
      { ...r, pagePersistence: a.pagePersistence },
    ));
  for (let [n, i] of e.pendingRetainedWebviewRoutesByInstanceId) {
    let a = i.route;
    a.browserTabId === t.browserTabId &&
      a.conversationId === t.conversationId &&
      e.pendingRetainedWebviewRoutesByInstanceId.set(n, { ...i, route: r });
  }
  for (let [n, i] of e.pendingAdoptedWebviewRoutesByWebContentsId) {
    let a = i.route;
    a.browserTabId === t.browserTabId &&
      a.conversationId === t.conversationId &&
      e.pendingAdoptedWebviewRoutesByWebContentsId.set(n, { ...i, route: r });
  }
  for (let [n, i] of e.pendingWebviewRoutesByInstanceId) {
    let a = i.route;
    a.browserTabId === t.browserTabId &&
      a.conversationId === t.conversationId &&
      e.pendingWebviewRoutesByInstanceId.set(n, { ...i, route: r });
  }
}
var JX = r.a(`browser-sidebar-manager`),
  YX = 512 * 1024,
  XX = {
    "browser-restore-history-failed": `failed`,
    "browser-restore-history-started": `history-restoring`,
    "browser-restore-history-succeeded": `restored`,
    "browser-restore-host-registered": `host-registered`,
    "browser-restore-page-attached": `page-attached`,
    "browser-restore-page-materialized": `page-materialized`,
    "browser-restore-shell-synced": `shell-synced`,
    "browser-restore-snapshot-found": `snapshot-found`,
    "browser-restore-snapshot-missing": `snapshot-missing`,
  };
function ZX(e, t, n, r) {
  (e.recordDebugEvent({
    browserTabId: n.browserTabId,
    conversationId: n.conversationId,
    guestWebContentsId: r,
    kind: `browser-runtime-attach-missing-page`,
    message: `Runtime attach had no Browser page`,
    ownerWebContentsId: t.owner.id,
    pageKey: null,
    windowId: t.window.id,
  }),
    JX().warning(`IAB_LIFECYCLE runtime attach had no page`, {
      safe: {
        browserTabId: n.browserTabId,
        conversationId: n.conversationId,
        guestWebContentsId: r,
        ownerWebContentsId: t.owner.id,
        windowId: t.window.id,
      },
      sensitive: {},
    }));
}
function QX(e, t, n, r, i, a = []) {
  eZ(e, t, n, n.page?.view.webContents?.id ?? null, n.page != null, r, i, a);
}
function $X(e, t, n, r, i, a, o = []) {
  eZ(e, t, n, r, !0, i, a, o);
}
function eZ(e, t, n, r, i, a, o, s) {
  e.recordDebugEvent({
    browserTabId: n.browserTabId,
    conversationId: n.conversationId,
    details: [{ label: `browserStorageId`, value: n.browserStorageId }, ...s],
    guestWebContentsId: r,
    kind: a,
    message: o,
    ownerWebContentsId: t.owner.id,
    pageKey: i ? DL(t.owner.id, n.conversationId, n.browserTabId) : null,
    windowId: t.window.id,
  });
}
function tZ({
  browserSessionRegistry: e,
  browserUseOpenRequests: t,
  getPersistedPageMetadata: n,
  getThreadStateForWindow: r,
  windows: i,
}) {
  let a = Array.from(i),
    o = e.getDebugSnapshot();
  return {
    capturedAtMs: Date.now(),
    events: o.events,
    openRequests: t
      .getDebugSnapshot()
      .map((e) =>
        iZ({ getThreadStateForWindow: r, request: e, windowStates: a }),
      ),
    pendingAttaches: a.flatMap((e) => lZ(e)),
    sessionRoutes: o.sessionRoutes,
    tabs: a.flatMap((e) =>
      Array.from(e.threads.values()).map((t) =>
        aZ({
          events: o.events,
          getPersistedPageMetadata: n,
          threadState: t,
          windowState: e,
        }),
      ),
    ),
    windows: a.map((e) => cZ(e)),
  };
}
function nZ(e) {
  let t = [],
    n = 2,
    r = 0;
  for (let i = e.events.length - 1; i >= 0; --i) {
    let a = e.events[i],
      o = Buffer.byteLength(JSON.stringify(a)),
      s = n + o + (t.length === 0 ? 0 : 1);
    if (s > YX) {
      r += 1;
      continue;
    }
    (t.push(a), (n = s));
  }
  return (
    t.reverse(),
    {
      ...e,
      events: t,
      feedbackMetadata: {
        eventByteLimit: YX,
        eventsTruncated: r > 0,
        includedEventBytes: n,
        includedEventCount: t.length,
        omittedEventCount: r,
      },
    }
  );
}
function rZ({
  browserSessionRegistry: e,
  getLiveWindowState: t,
  getThreadStateForWindow: n,
  options: { browserTabId: r, conversationId: i, windowId: a },
  windowManager: o,
}) {
  let s = t(a);
  if (s == null) return { opened: !1, reason: `missing-window` };
  let c = n(s, i, r);
  return c == null
    ? { opened: !1, reason: `missing-tab` }
    : c.ownerRoutePath == null
      ? { opened: !1, reason: `missing-owner-route` }
      : (o.sendMessageToWebContents(s.owner, {
          type: `navigate-to-route`,
          path: c.ownerRoutePath,
        }),
        s.window.isMinimized() && s.window.restore(),
        s.window.show(),
        s.window.focus(),
        e.recordDebugEvent({
          browserTabId: r,
          conversationId: i,
          details: [{ label: `ownerRoutePath`, value: c.ownerRoutePath }],
          guestWebContentsId: c.page?.view.webContents?.id ?? null,
          kind: `browser-owner-opened`,
          message: `Opened exact Browser owner route`,
          ownerWebContentsId: s.owner.id,
          pageKey: c.page == null ? null : DL(s.owner.id, i, r),
          windowId: s.window.id,
        }),
        { opened: !0 });
}
function iZ({ getThreadStateForWindow: e, request: t, windowStates: n }) {
  let r = n.find((e) => e.owner.id === t.ownerWebContentsId) ?? null,
    i =
      r == null || t.conversationId == null || t.browserTabId == null
        ? null
        : e(r, t.conversationId, t.browserTabId);
  return {
    ...t,
    activeBrowserTabId: r?.activeBrowserTabId ?? null,
    activeConversationId: r?.activeConversationId ?? null,
    guestWebContentsId: i?.page?.view.webContents?.id ?? null,
    hasPage: i == null ? null : i.page != null,
    ownerRoutePath: i?.ownerRoutePath ?? null,
    pendingAttachCount: r == null || i == null ? null : uZ(r, i),
    pendingBrowserUsePanelOpen: i?.pendingBrowserUsePanelOpen ?? null,
    routeActive:
      r == null || i == null
        ? null
        : r.activeConversationId === i.conversationId,
    windowId: r?.window.id ?? null,
  };
}
function aZ({
  events: e,
  getPersistedPageMetadata: t,
  threadState: n,
  windowState: r,
}) {
  let i = n.page,
    a = t(n),
    o = Array.from(r.registeredWebviewHostsByRoutePartition.values()).some(
      (e) =>
        e.pagePersistence.browserStorageId === n.browserStorageId &&
        e.browserTabId === n.browserTabId &&
        e.conversationId === n.conversationId,
    ),
    s = oZ(e, n.conversationId, n.browserTabId);
  return {
    active:
      r.activeConversationId === n.conversationId &&
      r.activeBrowserTabId === n.browserTabId,
    bounds: n.bounds,
    browserStorageId: n.browserStorageId,
    browserTabId: n.browserTabId,
    canOpenOwner: n.ownerRoutePath != null,
    conversationId: n.conversationId,
    guestWebContentsId: i?.view.webContents?.id ?? null,
    hasPage: i != null,
    hasPersistedPageState: a != null,
    hasPendingBrowserUseVisibilityRequest:
      n.hasPendingBrowserUseVisibilityRequest,
    isAgentControllingBrowser: n.isAgentControllingBrowser,
    isBrowserUseActive: n.isBrowserUseActive,
    isBrowserUsePage: n.isBrowserUsePage,
    isLoading: i?.isLoading ?? n.snapshot.isLoading,
    isRestoring: i?.isRestoring ?? !1,
    isSuspended: i?.isSuspended ?? n.snapshot.isSuspended,
    lastLifecycleSequence: n.lastLifecycleSequence,
    lastPresentedSequence: n.lastPresentedSequence,
    mountGeneration: n.mountGeneration,
    ownerRoutePath: n.ownerRoutePath,
    ownerWebContentsId: r.owner.id,
    pageKey:
      i == null ? null : DL(r.owner.id, n.conversationId, n.browserTabId),
    pendingAttachCount: uZ(r, n),
    pendingBrowserUsePanelOpen: n.pendingBrowserUsePanelOpen,
    pendingTeardown: n.pendingTeardown,
    pendingUrl: i?.pendingUrl ?? ``,
    persistedHistoryEntryCount: a?.historyEntryCount ?? null,
    persistedPageStateUpdatedAtMs: a?.updatedAtMs ?? null,
    presented: n.presented,
    restoreInvariant: sZ({
      hasPersistedPageState: a != null,
      isColdRestore: i?.isColdRestore === !0,
      restorePhase: s,
      hasPage: i != null,
    }),
    restorePhase: s,
    sessionState: n.sessionState,
    title: i?.title || n.snapshot.title,
    url: i == null ? n.snapshot.url : NL(i),
    visible: n.visible,
    webviewHostRegistered: o,
    windowId: r.window.id,
  };
}
function oZ(e, t, n) {
  let r = e.findLast(
    (e) =>
      e.conversationId === t &&
      e.browserTabId === n &&
      e.kind.startsWith(`browser-restore-`),
  );
  return r == null ? `idle` : (XX[r.kind] ?? `idle`);
}
function sZ({
  hasPage: e,
  hasPersistedPageState: t,
  isColdRestore: n,
  restorePhase: r,
}) {
  return r === `snapshot-missing`
    ? `Shell restored, snapshot missing`
    : r === `failed`
      ? `Browser history restore failed`
      : t && !e
        ? `Snapshot found, page not materialized`
        : n && !t
          ? `Cold page has no persisted snapshot`
          : null;
}
function cZ(e) {
  return {
    activeBrowserTabId: e.activeBrowserTabId,
    activeConversationId: e.activeConversationId,
    ignoredConversationCount: e.ignoredConversationIds.size,
    ownerWebContentsId: e.owner.id,
    pendingAttachCount: e.pendingWebviewRoutesByInstanceId.size,
    pendingAdoptedWebviewCount:
      e.pendingAdoptedWebviewRoutesByWebContentsId.size,
    pendingLegacyWebviewCount: e.pendingRetainedWebviewRoutesByInstanceId.size,
    transferredConversationStateCount: e.transferredConversationStateKeys.size,
    windowId: e.window.id,
  };
}
function lZ(e) {
  return [
    ...Array.from(e.pendingRetainedWebviewRoutesByInstanceId.values()).map(
      (t) => ({
        browserTabId: t.route.browserTabId,
        conversationId: t.route.conversationId,
        hostGeneration: t.hostGeneration,
        kind: `legacy`,
        webContentsId: null,
        windowId: e.window.id,
      }),
    ),
    ...Array.from(e.pendingAdoptedWebviewRoutesByWebContentsId).map(
      ([t, n]) => ({
        browserTabId: n.route.browserTabId,
        conversationId: n.route.conversationId,
        hostGeneration: n.hostGeneration,
        kind: `adopted`,
        webContentsId: t,
        windowId: e.window.id,
      }),
    ),
    ...Array.from(e.pendingWebviewRoutesByInstanceId.values()).map((t) => ({
      browserTabId: t.route.browserTabId,
      conversationId: t.route.conversationId,
      hostGeneration: t.hostGeneration,
      kind: `instance`,
      webContentsId: null,
      windowId: e.window.id,
    })),
  ];
}
function uZ(e, t) {
  return lZ(e).filter(
    (e) =>
      e.browserTabId === t.browserTabId &&
      e.conversationId === t.conversationId,
  ).length;
}
var dZ = r.a(`browser-sidebar-manager`);
function fZ(e) {
  e?.pendingNavigationUsage?.source === `browser_use` &&
    (e.pendingNavigationUsage = null);
}
function pZ(e, t, n, r, i, a, o) {
  let s = t(n);
  s == null ||
    s.owner.isDestroyed() ||
    e.sendMessageToWebContents(s.owner, {
      type: `browser-sidebar-usage`,
      conversationId: n,
      browserTabId: r,
      kind: `navigated`,
      source: i,
      initiator: a,
      urlKind: o,
    });
}
function mZ(e, t) {
  e != null &&
    (e.pendingNavigationUsage = {
      expectedUrl: n.ms(t),
      initiator: `browser_use`,
      source: `browser_use`,
      urlKind: ML(t),
    });
}
function hZ({ errorReporter: e, pageState: t, webContents: n }) {
  let r = t.isColdRestore
      ? LL(t.serializedPageState)
      : IL(t.serializedPageState),
    i = r?.currentIndex ?? null;
  if (r == null || i == null || i < 0 || i >= r.entries.length) return null;
  try {
    return n.navigationHistory
      .restore({ entries: r.entries, index: i })
      .then(() => !0)
      .catch(
        (n) => (
          dZ().warning(`failed to restore browser sidebar navigation history`, {
            safe: { conversationId: t.conversationId },
            sensitive: { error: n },
          }),
          e.reportNonFatal(n instanceof Error ? n : Error(String(n)), {
            kind: `browser-sidebar-restore-navigation-history`,
          }),
          !1
        ),
      );
  } catch (n) {
    return (
      dZ().warning(`failed to start browser sidebar navigation restore`, {
        safe: { conversationId: t.conversationId },
        sensitive: { error: n },
      }),
      e.reportNonFatal(n instanceof Error ? n : Error(String(n)), {
        kind: `browser-sidebar-restore-navigation-history`,
      }),
      Promise.resolve(!1)
    );
  }
}
function gZ({ navigatePageToUrl: e, pageState: t, webContents: r }) {
  let i = r.getURL();
  return i.length > 0 && n.ms(i) === n.ms(t.pendingUrl)
    ? !1
    : e(
        t.pendingUrl,
        t.pendingNavigationUsage?.source,
        t.pendingNavigationUsage?.initiator,
      );
}
function _Z(e) {
  return e.tabType === n.oc.WEB && e.commentModeDisabledReason == null;
}
function vZ({
  canReload: e,
  isAnnotationModeEnabled: t = !0,
  isMultiBrowserTabsEnabled: r = !1,
  snapshot: i,
  params: o,
  webContents: s,
  window: l,
  onBack: u,
  onAttachImage: d,
  onComment: f,
  onForward: p,
  onInspect: m,
  onOpenInBrowser: h,
  onOpenExternal: g,
  onReload: _,
}) {
  let v = a.H(),
    y = [];
  (t &&
    i != null &&
    _Z(i) &&
    y.push(
      {
        label: v.formatMessage({
          messageId: AH,
          defaultMessage: `Quick annotate`,
        }),
        click: () => {
          s.isDestroyed() || f({ x: o.x, y: o.y }, n.vc.QUICK, CZ(o));
        },
      },
      {
        label: v.formatMessage({ messageId: kH, defaultMessage: `Annotate` }),
        click: () => {
          s.isDestroyed() || f({ x: o.x, y: o.y }, n.vc.BATCH, CZ(o));
        },
      },
      { type: `separator` },
    ),
    uL(o) &&
      y.push(
        {
          label: v.formatMessage({
            messageId: jH,
            defaultMessage: `Attach image to task`,
          }),
          click: () => {
            s.isDestroyed() || d(o.srcURL);
          },
        },
        { type: `separator` },
      ));
  let b = yZ(o, s);
  b.length > 0 && y.push(...b, { type: `separator` });
  let x = o.linkURL.length > 0;
  if (x) {
    let e = y.length;
    (Zz(o.linkURL) &&
      y.push({
        label: r
          ? v.formatMessage({
              messageId: EH,
              defaultMessage: `Open link in new tab`,
            })
          : v.formatMessage({
              messageId: TH,
              defaultMessage: `Open in browser`,
            }),
        click: () => {
          h(o.linkURL);
        },
      }),
      n.gs(o.linkURL) &&
        y.push({
          label: v.formatMessage({
            messageId: DH,
            defaultMessage: `Open in external browser`,
          }),
          click: () => {
            g(o.linkURL);
          },
        }),
      y.length > e && y.push({ type: `separator` }));
  }
  let S = SZ(o),
    C = CZ(o);
  ((x || S || C) &&
    (x &&
      y.push({
        label: v.formatMessage({
          messageId: OH,
          defaultMessage: `Copy link address`,
        }),
        click: () => {
          c.clipboard.writeText(o.linkURL);
        },
      }),
    S && y.push({ role: `cut`, enabled: o.editFlags.canCut }),
    (S || C) && y.push({ role: `copy`, enabled: o.editFlags.canCopy }),
    S && y.push({ role: `paste`, enabled: o.editFlags.canPaste }),
    y.push({ type: `separator` })),
    wZ(o) &&
      y.push(
        {
          label: v.formatMessage({ messageId: SH, defaultMessage: `Back` }),
          enabled: s.canGoBack(),
          click: () => {
            s.isDestroyed() || u();
          },
        },
        {
          label: v.formatMessage({ messageId: CH, defaultMessage: `Forward` }),
          enabled: s.canGoForward(),
          click: () => {
            s.isDestroyed() || p();
          },
        },
        {
          label: v.formatMessage({ messageId: wH, defaultMessage: `Reload` }),
          enabled: e,
          click: () => {
            s.isDestroyed() || _();
          },
        },
        { type: `separator` },
      ),
    y.push({
      label: v.formatMessage({ messageId: xH, defaultMessage: `Inspect` }),
      click: () => {
        s.isDestroyed() || m({ x: o.x, y: o.y });
      },
    }),
    c.Menu.buildFromTemplate(y).popup({ window: l }));
}
function yZ(e, t) {
  let n = [],
    r = e.passwordManager;
  return r == null
    ? n
    : (r.selectPassword != null &&
        n.push({
          label: r.selectPassword.label,
          click: () => {
            bZ(t, `select-password`);
          },
        }),
      r.suggestPassword != null &&
        n.push({
          label: r.suggestPassword.label,
          click: () => {
            bZ(t, `suggest-password`);
          },
        }),
      n);
}
function bZ(e, t) {
  e.isDestroyed() || !xZ(e) || e._executePasswordManagerContextMenuCommand(t);
}
function xZ(e) {
  return (
    `_executePasswordManagerContextMenuCommand` in e &&
    typeof e._executePasswordManagerContextMenuCommand == `function`
  );
}
function SZ(e) {
  return e.isEditable;
}
function CZ(e) {
  return (
    !e.formControlType.endsWith(`password`) && e.selectionText.trim().length > 0
  );
}
function wZ(e) {
  return (
    !e.isEditable &&
    e.formControlType === `none` &&
    e.mediaType === `none` &&
    e.linkURL.length === 0 &&
    e.srcURL.length === 0 &&
    e.selectionText.length === 0
  );
}
function TZ() {
  return {
    annotationFlow: n.vc.BATCH,
    annotationModeEntrySource: null,
    tabType: n.oc.NEW_TAB_PAGE,
    isSuspended: !1,
    title: n.cc,
    url: ``,
    faviconUrl: null,
    securityState: null,
    isAudible: !1,
    isCapturingUserMedia: !1,
    isLoading: !1,
    canGoBack: !1,
    canGoForward: !1,
    zoomPercent: 100,
    commentModeDisabledReason: null,
    interactionMode: `browse`,
    comments: [],
  };
}
var EZ = r.a(`browser-sidebar-manager`),
  DZ = `-owl-native-contents-close`,
  OZ = `being-captured-state-changed`,
  kZ = `password-manager-open-settings`,
  AZ = `page-info-open-site-settings`,
  jZ = `owl-favicon-image-updated`,
  MZ = 50,
  NZ = 100,
  PZ = {
    [n.vc.BATCH]: {
      action: `annotate`,
      annotationModeEntrySource: `context-menu-annotate`,
    },
    [n.vc.QUICK]: {
      action: `quick-annotate`,
      annotationModeEntrySource: `context-menu-quick-annotate`,
    },
  },
  FZ = new WeakMap();
function IZ(e, t) {
  return {
    ...RZ(e, t.title),
    canGoBack: !1,
    canGoForward: !1,
    faviconUrl: null,
    isColdRestore: !1,
    isSuspended: !1,
    pendingUrl: t.url,
    serializedPageState: PL({ faviconUrl: null, title: t.title, url: t.url }),
    title: t.title,
    url: ``,
  };
}
function LZ(e, t) {
  let n = t.navigation.currentIndex;
  return {
    ...RZ(e, t.title),
    canGoBack: n > 0,
    canGoForward: n < t.navigation.entries.length - 1,
    faviconUrl: t.faviconUrl,
    isColdRestore: !0,
    isSuspended: !0,
    pendingUrl: ``,
    serializedPageState: t,
    title: t.title,
    url: t.url,
  };
}
function RZ(e, t) {
  return {
    browserStorageId: e.browserStorageId,
    browserTabId: e.browserTabId,
    conversationId: e.conversationId,
    debuggerSync: Promise.resolve(),
    deviceMetricsSyncVersion: 0,
    dispose: () => void 0,
    fallbackTitle: t,
    initialNavigationSync: Promise.resolve(),
    isAudible: !1,
    isCapturingUserMedia: !1,
    isLoading: !1,
    isRestoring: !1,
    loadError: null,
    navigationVersion: 0,
    pendingNavigationUsage: null,
    pendingSuspensionId: null,
    view: { webContents: null },
  };
}
function zZ(e, t, n, r) {
  let i;
  switch (r.kind) {
    case `fresh`:
      i = IZ(n, r.page);
      break;
    case `cold-restore`:
      i = LZ(n, r.snapshot);
      break;
  }
  let { conversationId: a } = n;
  return (
    (n.page = i),
    e.updateThreadSessionState(n),
    e.isBrowserUseManagedBrowserTab(a, n.browserTabId) &&
      e.browserSessionRegistry.recordPageCreated({
        browserTabId: n.browserTabId,
        conversationId: a,
        windowId: zX(t.window.id),
      }),
    e.syncThreadState(t, a, n),
    e.syncRuntimeStateToPage(i, n),
    n.isAnnotationModeEnabled &&
      e.comments.prepare(t.owner, a, n.browserTabId, n),
    i
  );
}
function BZ(e, t, r, i, a) {
  if (r.view.webContents?.id === i.id) return;
  let o = i,
    s = i,
    l = r.isSuspended,
    u = r.isColdRestore;
  (r.dispose(),
    (r.isRestoring = l),
    (r.isSuspended = !1),
    (r.pendingSuspensionId = null),
    (r.view.webContents = i));
  let d = () => void 0;
  r.initialNavigationSync = new Promise((e) => {
    d = e;
  });
  let f = () => {
      (d(), (d = () => void 0));
    },
    p = (n, i = {}) => {
      let a = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      (a != null &&
        (a.isTabCaptureActive !== n &&
          ((a.isTabCaptureActive = n),
          i.enforceBudget !== !1 && e.enforceDetachedPageBudget(t)),
        WZ(t, a)),
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `browser-sidebar-tab-capture-state`,
          conversationId: r.conversationId,
          browserTabId: r.browserTabId,
          isActive: n,
        }));
    };
  ((r.isAudible = i.isCurrentlyAudible()),
    (r.isCapturingUserMedia =
      `isCapturingUserMedia` in i &&
      typeof i.isCapturingUserMedia == `function` &&
      i.isCapturingUserMedia()),
    p(GZ(i)),
    e.onBrowserSidebarStateChanged(),
    e.isBrowserUseManagedBrowserTab(r.conversationId, r.browserTabId) &&
      e.browserSessionRegistry.recordGuestAttached({
        browserTabId: r.browserTabId,
        conversationId: r.conversationId,
        guestWebContentsId: i.id,
        pageKey: DL(t.owner.id, r.conversationId, r.browserTabId),
        windowId: zX(t.window.id),
      }));
  let m = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
  m != null &&
    (e.updateThreadSessionState(m),
    e.syncPageZoomPercent(r, m.snapshot.zoomPercent),
    e.queuePageDeviceMetricsSync(r, m.emulatedViewportSize),
    e.windowManager.sendMessageToWebContents(t.owner, {
      type: `browser-sidebar-webview-attached`,
      browserTabId: r.browserTabId,
      conversationId: r.conversationId,
      mountGeneration: m.mountGeneration,
    }));
  let h = !1,
    g = r.navigationVersion,
    _ = ({ enforceBudget: n = !0 } = {}) => {
      if (!r.isRestoring || h) return;
      r.isRestoring = !1;
      let i = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      i != null &&
        (e.updateThreadSessionState(i), n && e.enforceDetachedPageBudget(t));
    },
    v = (a) => {
      let o = MB(a ?? i.getURL());
      !NU(r.loadError, o) &&
        (a != null || o.length > 0 || r.url.length === 0) &&
        (a != null || o !== r.url) &&
        (jL(r.url, o) && (r.faviconUrl = null),
        (r.url = o),
        (r.pendingUrl = o),
        a != null && (r.loadError = null));
      let s = i.getTitle(),
        c = i.isLoading();
      ((r.title =
        s ||
        r.loadError?.title ||
        (c && n.ms(o).length > 0 ? r.title : r.fallbackTitle)),
        (r.isLoading = c),
        (r.canGoBack = i.canGoBack()),
        (r.canGoForward = i.canGoForward()),
        !r.isRestoring &&
          !r.isColdRestore &&
          (r.serializedPageState = PL({
            faviconUrl: r.faviconUrl,
            title: r.title,
            url: NL(r),
          })));
      let l = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      if (l != null) {
        let n = l.snapshot.interactionMode;
        (e.syncThreadState(t, r.conversationId, l),
          l.page != null &&
            l.snapshot.interactionMode !== n &&
            e.syncRuntimeStateToPage(r, l));
      }
    },
    y = (n) => {
      let i = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      i != null && WZ(t, i, n);
    },
    b = () => {
      v();
    },
    x = (n) => {
      ((r.faviconUrl = n),
        !r.isRestoring &&
          !r.isColdRestore &&
          (r.serializedPageState = PL({
            faviconUrl: n,
            title: r.title,
            url: NL(r),
          })));
      let i = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      i != null && e.syncThreadState(t, r.conversationId, i);
    },
    S = (e) => {
      x(e.isEmpty() ? null : e.toDataURL({ scaleFactor: 2 }));
    },
    C = () => {
      let e = s.getFaviconImage?.();
      e != null && S(e);
    },
    w = (e, t) => {
      s.getFaviconImage ?? x(t.find((e) => e.length > 0) ?? null);
    },
    T = (e, t) => {
      S(t);
    },
    E = () => {
      v();
    },
    D = () => {
      (_({ enforceBudget: !1 }),
        v(),
        e.enforceDetachedPageBudget(t),
        i.getURL().length > 0 && f());
      let a = r.pendingNavigationUsage;
      a?.source === `browser_use` &&
        n.ms(i.getURL()) === a.expectedUrl &&
        (e.notifyBrowserSidebarNavigationUsage(
          r.conversationId,
          r.browserTabId,
          `browser_use`,
          a.initiator,
          a.urlKind,
        ),
        (r.pendingNavigationUsage = null));
    },
    O = () => {
      let n = MB(i.getURL());
      n.length === 0 ||
        NU(r.loadError, n) ||
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `browser-sidebar-page-loaded`,
        });
    },
    k = () => {
      let n = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      n != null &&
        (e.syncThreadState(t, r.conversationId, n),
        e.enforceDetachedPageBudget(t));
    },
    A = (e) => {
      r.isAudible !== e.audible && ((r.isAudible = e.audible), k());
    },
    j = (e) => {
      r.isCapturingUserMedia !== e.capturing &&
        ((r.isCapturingUserMedia = e.capturing), k());
    },
    M = (e) => {
      p(e.captured);
    },
    N = (n) => {
      f();
      let i = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      if (i != null) {
        if (i.pendingTeardown != null) {
          e.finalizeThreadTeardown(
            t,
            i.conversationId,
            i,
            i.pendingTeardown.teardownId,
          );
          return;
        }
        ZU(e.windowManager, t.owner, r.conversationId, i);
      }
      (e.dismissCommentEditorForCommittedNavigation(t, r, n), v(n));
    },
    P = (e, t) => {
      N(t);
    },
    F = (e, t, n) => {
      n && N(t);
    },
    I = (e) => {
      e.isMainFrame &&
        (r.pendingNavigationUsage?.source === `browser_use` &&
          (r.pendingNavigationUsage = null),
        N(e.url));
    },
    ee = (a, o, s, c, l) => {
      let u = MB(c);
      if (
        (l && c === `about:blank`) ||
        NU(r.loadError, u) ||
        AU({ errorCode: o, errorDescription: s, isMainFrame: l })
      )
        return;
      let d = u || r.pendingUrl || r.url || MB(i.getURL());
      if (d.length === 0) return;
      let f = OU({
        errorDescription: s,
        failedUrl: d,
        formatMessage: kL,
        runtimeIntlConfig:
          e.getThreadStateForWindow(t, r.conversationId, r.browserTabId)
            ?.runtimeIntlConfig ?? n.Sc,
      });
      (e.loadBrowserSidebarErrorPage(t, r, i, {
        failedUrl: d,
        kind: `load-error`,
        loadError: DU({
          content: f,
          errorCode: o,
          errorDescription: s,
          failedUrl: d,
        }),
      }),
        _());
    },
    L = (t) => {
      if (
        (t.isMainFrame && UL(r),
        e.openBrowserSidebarExternalUrl({
          initiatorUrl: t.initiator?.url ?? null,
          pageState: r,
          url: t.url,
          webContents: i,
        }))
      ) {
        t.preventDefault();
        return;
      }
      if (
        e.isRestrictedBrowserSidebarNavigation(
          i.id,
          t.url,
          t.isMainFrame ? `mainFrame` : `subFrame`,
        )
      ) {
        t.preventDefault();
        return;
      }
      t.isMainFrame && y(t.url);
    },
    R = (n, a) => {
      UL(r);
      let o = n.url ?? a;
      if (
        e.openBrowserSidebarExternalUrl({
          initiatorUrl: n.initiator?.url ?? null,
          pageState: r,
          url: o,
          webContents: i,
        })
      ) {
        n.preventDefault();
        return;
      }
      if (e.isRestrictedBrowserSidebarNavigation(i.id, o)) {
        n.preventDefault();
        return;
      }
      let s = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      if (s != null) {
        if (
          (ZU(e.windowManager, t.owner, r.conversationId, s),
          s.pendingTeardown != null)
        ) {
          e.finalizeThreadTeardown(
            t,
            r.conversationId,
            s,
            s.pendingTeardown.teardownId,
          );
          return;
        }
        (e.updateThreadSessionState(s),
          e.syncThreadState(t, r.conversationId, s, o));
      }
    },
    z = (n, a) => {
      n.isMainFrame && UL(r);
      let o = n.url ?? a;
      if (
        e.isRestrictedBrowserSidebarNavigation(
          i.id,
          o,
          n.isMainFrame ? `mainFrame` : `subFrame`,
        )
      ) {
        n.preventDefault();
        return;
      }
      if (n.isMainFrame) {
        y(o);
        let n = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
        n != null && ZU(e.windowManager, t.owner, r.conversationId, n);
      }
    },
    te = () => {
      r.view.webContents?.id === i.id &&
        ((r.pendingSuspensionId = null),
        (r.isRestoring = !1),
        (r.isAudible = !1),
        (r.isCapturingUserMedia = !1),
        p(!1, { enforceBudget: !1 }),
        (r.view.webContents = null),
        (r.dispose = () => void 0),
        e.onBrowserSidebarStateChanged(),
        e.browserSessionRegistry.recordPageTornDown({
          browserTabId: r.browserTabId,
          conversationId: r.conversationId,
          guestWebContentsId: i.id,
          windowId: zX(t.window.id),
        }));
      let n = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      n != null &&
        (e.syncThreadState(t, r.conversationId, n),
        ZU(e.windowManager, t.owner, r.conversationId, n));
    },
    ne = () => {
      t.isClosing ||
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `close-browser-tab`,
          browserTabId: r.browserTabId,
          conversationId: r.conversationId,
        });
    },
    re = null,
    ie = () => re === r.navigationVersion,
    ae = (a) => {
      re = r.navigationVersion;
      let o = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      e.loadBrowserSidebarErrorPage(t, r, i, {
        failedUrl: a,
        kind: `crash`,
        loadError: kU({
          failedUrl: a,
          formatMessage: kL,
          runtimeIntlConfig: o?.runtimeIntlConfig ?? n.Sc,
        }),
      });
    },
    oe = (e, t) => {
      if (t.reason === `clean-exit` || ie()) return;
      EZ().warning(`browser sidebar render process gone`, {
        safe: {
          conversationId: r.conversationId,
          exitCode: t.exitCode,
          reason: t.reason,
        },
        sensitive: { url: i.getURL() },
      });
      let n = r.pendingUrl || r.url || MB(i.getURL());
      n.length !== 0 && ae(n);
    },
    se = () => {
      if (ie()) return;
      EZ().warning(`IAB browser page unresponsive`, {
        safe: { conversationId: r.conversationId },
        sensitive: { url: i.getURL() },
      });
      let e = r.pendingUrl || r.url || MB(i.getURL());
      e.length !== 0 && ae(e);
    },
    ce = () => {
      let n = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      n &&
        (EZ().info(`browser sidebar dom-ready`, {
          safe: {
            conversationId: r.conversationId,
            interactionMode: n.snapshot.interactionMode,
          },
          sensitive: { url: i.getURL() },
        }),
        e.syncRuntimeStateToPage(r, n),
        e.comments.restoreRuntimeEditor(
          t.owner,
          r.conversationId,
          r.browserTabId,
          r.view.webContents,
        ));
    },
    le = (t, n, i) => {
      (EZ().error(`browser sidebar preload failed`, {
        safe: { conversationId: r.conversationId, preloadPath: n },
        sensitive: { error: i },
      }),
        e.errorReporter.reportNonFatal(i, {
          kind: `browser-sidebar-preload-error`,
        }));
    },
    ue = (n, i) => {
      let a = e.getThreadStateForWindow(t, r.conversationId, r.browserTabId);
      a != null && YU(e.windowManager, t.owner, r.conversationId, a, i);
    },
    de = () => e.getThreadStateForWindow(t, r.conversationId, r.browserTabId),
    fe = () => {
      let n = de();
      return eL(
        e.windowManager.getAppShellShortcutState(t.owner.id),
        r.conversationId,
        r.browserTabId,
        nL(n?.hostKind ?? null),
      );
    },
    pe = () => {
      i.send(B, { type: `browser-sidebar-runtime-capture-text-selection` });
    },
    me = tW({
      captureTextSelection: pe,
      closeBrowserTab: () => {
        let n = fe();
        n != null && e.windowManager.sendMessageToWebContents(t.owner, n);
      },
      copyBrowserUrl: () => {
        (c.clipboard.writeText(NL(r)),
          e.windowManager.sendMessageToWebContents(t.owner, {
            type: `browser-sidebar-url-copied`,
            conversationId: r.conversationId,
            browserTabId: r.browserTabId,
          }));
      },
      getCanCloseBrowserTab: () => fe() != null,
      getInteractionMode: () => de()?.snapshot.interactionMode ?? `browse`,
      getOwnerCommandAccelerators: (t) => e.getOwnerCommandAccelerators(t),
      isCommentModeAvailable: () => {
        let e = de();
        return e != null && e.isAnnotationModeEnabled && _Z(e.snapshot);
      },
      owner: t.owner,
      runCommandInOwner: (n, i, a) => {
        let o = de(),
          s = tL(
            e.windowManager.getAppShellShortcutState(t.owner.id),
            r.conversationId,
            r.browserTabId,
            nL(o?.hostKind ?? null),
          );
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `run-command`,
          id: n,
          ...(a?.allowAppShellTabShortcut === !0
            ? { allowAppShellTabShortcut: !0 }
            : {}),
          keyboardEvent: i,
          keyboardOrigin: `browser-web-contents`,
          originPanel: s ?? void 0,
        });
      },
      setInteractionMode: (n, i) => {
        e.setInteractionMode(t.owner, r.conversationId, n, r.browserTabId, i);
      },
    }),
    he = (n, i) => {
      (e.setDesignModifierPressed(t.owner, r.conversationId, i.alt === !0),
        me(n, i));
    },
    ge = oP(i, (n, i) => {
      let a = de();
      i.type !== `mouseDown` ||
        t.owner.isDestroyed() ||
        t.activeConversationId !== r.conversationId ||
        t.activeBrowserTabId !== r.browserTabId ||
        a?.visible !== !0 ||
        a.presented !== !0 ||
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `browser-sidebar-web-contents-pointer-down`,
          conversationId: r.conversationId,
          browserTabId: r.browserTabId,
        });
    }),
    _e = (a, o) => {
      if (i.isDestroyed() || t.window.isDestroyed()) return;
      let s =
          r.loadError != null || r.url.length > 0 || r.pendingUrl.length > 0,
        c = de(),
        l = c?.isAnnotationModeEnabled ?? !0,
        u = c?.snapshot ?? null,
        d = n.Uo(u?.zoomPercent ?? 100);
      (o.selectionText.trim().length > 0 && l && u != null && _Z(u) && pe(),
        vZ({
          canReload: s,
          isAnnotationModeEnabled: l,
          isMultiBrowserTabsEnabled:
            e.browserSessionRegistry.getRouteTabMode({
              conversationId: r.conversationId,
              windowId: zX(t.window.id),
            }) === `multiTab`,
          snapshot: u,
          params: o,
          webContents: i,
          window: t.window,
          onBack: () => {
            e.goBack(t.owner, r.conversationId, r.browserTabId);
          },
          onAttachImage: (n) => {
            lL({
              browserTabId: r.browserTabId,
              browserPageUrl: r.url,
              conversationId: r.conversationId,
              owner: t.owner,
              sourceUrl: n,
              webContents: i,
              windowManager: e.windowManager,
            });
          },
          onComment: (n, i, a) => {
            let { action: o, annotationModeEntrySource: s } = PZ[i];
            (e.windowManager.sendMessageToWebContents(t.owner, {
              type: `browser-sidebar-context-menu-action`,
              action: o,
            }),
              e.startCommentAtPoint(
                t.owner,
                r,
                { x: n.x / d, y: n.y / d },
                i,
                s,
                a,
              ));
          },
          onForward: () => {
            e.goForward(t.owner, r.conversationId, r.browserTabId);
          },
          onInspect: (e) => {
            i.inspectElement(e.x, e.y);
          },
          onOpenInBrowser: (n) => {
            e.windowManager.sendMessageToWebContents(t.owner, {
              type: `open-browser-tab`,
              conversationId: r.conversationId,
              initialUrl: n,
              insertToRightOfTabId: r.browserTabId,
              source: `manual`,
              initiator: `side_panel_menu`,
            });
          },
          onOpenExternal: (t) => {
            e.openBrowserSidebarUrlExternally(r.conversationId, t);
          },
          onReload: () => {
            e.reload(t.owner, r.conversationId, void 0, r.browserTabId);
          },
        }));
    },
    ve = () => {
      t.owner.isDestroyed() ||
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `navigate-to-route`,
          path: n.ns,
        });
    },
    ye = (i, a) => {
      if (!t.owner.isDestroyed()) {
        if (
          e.browserSessionRegistry.getRouteTabMode({
            conversationId: r.conversationId,
            windowId: zX(t.window.id),
          }) !== `multiTab`
        ) {
          e.windowManager.sendMessageToWebContents(t.owner, {
            type: `navigate-to-route`,
            path:
              typeof a == `string`
                ? (n.ls(a) ?? `/settings/browser-use/site-settings`)
                : n.os,
          });
          return;
        }
        e.windowManager.sendMessageToWebContents(t.owner, {
          type: `open-browser-tab`,
          conversationId: r.conversationId,
          initialUrl: n.us(a ?? null) ?? `chrome://settings/content`,
          insertToRightOfTabId: r.browserTabId,
          source: `manual`,
          initiator: `app_menu`,
        });
      }
    };
  (i.on(`page-title-updated`, b),
    i.on(`page-favicon-updated`, w),
    o.on(jZ, T),
    i.on(`audio-state-changed`, A),
    i.on(`user-media-capture-state-changed`, j),
    o.on(OZ, M),
    i.on(`did-start-loading`, E),
    i.on(`did-stop-loading`, D),
    i.on(`did-finish-load`, O),
    i.on(`did-navigate`, P),
    i.on(`did-navigate-in-page`, F),
    i.on(`did-redirect-navigation`, I),
    i.on(`did-fail-load`, ee),
    i.on(`will-frame-navigate`, L),
    i.on(`will-navigate`, R),
    i.on(`will-redirect`, z),
    i.on(`destroyed`, te),
    o.on(DZ, ne),
    i.on(`render-process-gone`, oe),
    i.on(`unresponsive`, se),
    i.on(`dom-ready`, ce),
    i.on(`preload-error`, le),
    i.on(`found-in-page`, ue),
    i.on(`before-input-event`, he),
    i.on(`context-menu`, _e),
    o.on(kZ, ve),
    o.on(AZ, ye),
    i.setWindowOpenHandler(({ disposition: n, referrer: a, url: o }) =>
      e.openBrowserSidebarExternalUrl({
        initiatorUrl: a?.url ?? null,
        pageState: r,
        url: o,
        webContents: i,
      }) ||
      e.isRestrictedBrowserSidebarNavigation(i.id, o) ||
      n === `other`
        ? { action: `deny` }
        : JZ(i) && (n === `foreground-tab` || n === `background-tab`)
          ? {
              action: `allow`,
              createWindow: (i) => {
                let a = YZ(i, t.owner);
                return (
                  t.pendingAdoptedInitialUrlsByWebContents.set(
                    a.webContents,
                    o,
                  ),
                  e.windowManager.sendMessageToWebContents(
                    t.owner,
                    qZ({
                      ...(n === `background-tab` ? { active: !1 } : {}),
                      adoptionLease: a.adoptionLease,
                      adoptedWebContentsId: a.webContents.id,
                      conversationId: r.conversationId,
                      initialUrl: o,
                      insertToRightOfTabId: r.browserTabId,
                    }),
                  ),
                  a.webContents
                );
              },
            }
          : n === `background-tab`
            ? (e.windowManager.sendMessageToWebContents(
                t.owner,
                qZ({
                  active: !1,
                  conversationId: r.conversationId,
                  initialUrl: o,
                  insertToRightOfTabId: r.browserTabId,
                }),
              ),
              { action: `deny` })
            : (e.windowManager.sendMessageToWebContents(t.owner, {
                ...qZ({
                  conversationId: r.conversationId,
                  initialUrl: o,
                  insertToRightOfTabId: r.browserTabId,
                }),
              }),
              { action: `deny` }),
    ),
    (r.dispose = () => {
      (f(),
        i.removeListener(`page-title-updated`, b),
        i.removeListener(`page-favicon-updated`, w),
        o.removeListener(jZ, T),
        i.removeListener(`audio-state-changed`, A),
        i.removeListener(`user-media-capture-state-changed`, j),
        o.removeListener(OZ, M),
        i.removeListener(`did-start-loading`, E),
        i.removeListener(`did-stop-loading`, D),
        i.removeListener(`did-finish-load`, O),
        i.removeListener(`did-navigate`, P),
        i.removeListener(`did-navigate-in-page`, F),
        i.removeListener(`did-redirect-navigation`, I),
        i.removeListener(`did-fail-load`, ee),
        i.removeListener(`will-frame-navigate`, L),
        i.removeListener(`will-navigate`, R),
        i.removeListener(`will-redirect`, z),
        i.removeListener(`destroyed`, te),
        o.removeListener(DZ, ne),
        i.removeListener(`render-process-gone`, oe),
        i.removeListener(`unresponsive`, se),
        i.removeListener(`dom-ready`, ce),
        i.removeListener(`preload-error`, le),
        i.removeListener(`found-in-page`, ue),
        i.removeListener(`before-input-event`, he),
        ge(),
        i.removeListener(`context-menu`, _e),
        o.removeListener(kZ, ve),
        o.removeListener(AZ, ye),
        r.view.webContents?.id === i.id &&
          ((r.isRestoring = !1),
          (r.isAudible = !1),
          (r.isCapturingUserMedia = !1),
          p(!1),
          (r.view.webContents = null),
          e.onBrowserSidebarStateChanged()));
    }));
  let be = hZ({ errorReporter: e.errorReporter, pageState: r, webContents: i });
  h = be != null;
  let xe = () => {
      if (r.view.webContents?.id !== i.id || i.isDestroyed()) return (f(), !1);
      let t = gZ({
        navigatePageToUrl: (t, n, i) => e.navigatePageToUrl(r, t, n, i),
        pageState: r,
        webContents: i,
      });
      return (t || (_(), f()), t);
    },
    Se = (n, a, o) => {
      $X(e.browserSessionRegistry, t, r, i.id, n, a, o);
    };
  (be == null
    ? ((r.isColdRestore = !1), v(), C(), u ? (_(), f()) : xe())
    : (C(),
      u &&
        (Se(`browser-restore-page-attached`, `Attached persisted Browser page`),
        Se(
          `browser-restore-history-started`,
          `Started Browser navigation history restore`,
        )),
      be.then((t) => {
        if (((h = !1), r.view.webContents?.id !== i.id || i.isDestroyed())) {
          f();
          return;
        }
        if (r.navigationVersion !== g) {
          (u &&
            Se(
              `browser-restore-history-failed`,
              `Browser navigation history restore was superseded`,
              [{ label: `reason`, value: `navigation-version-changed` }],
            ),
            r.isColdRestore &&
              ((r.isColdRestore = !1),
              (r.serializedPageState = null),
              e.deletePersistedPageState(r),
              v()),
            _(),
            i.getURL().length > 0 && f());
          return;
        }
        (t
          ? (u &&
              Se(
                `browser-restore-history-succeeded`,
                `Restored Browser navigation history`,
              ),
            (r.isColdRestore = !1),
            v(),
            C(),
            r.pendingUrl.length > 0 ? xe() : f())
          : u
            ? (Se(
                `browser-restore-history-failed`,
                `Failed to restore Browser navigation history`,
              ),
              (r.isColdRestore = !1),
              (r.serializedPageState = null),
              e.deletePersistedPageState(r),
              f(),
              ne())
            : u || ((r.isColdRestore = !1), xe()),
          _());
      })),
    e.finishPageAttach(t, r, i, a),
    e.enforceDetachedPageBudget(t));
}
function VZ(e, t, r, i, a) {
  let o = r.length === 0 ? `about:blank` : r,
    s = n.ms(o),
    c = t.view.webContents;
  if (t.isColdRestore) {
    if (s === n.ms(t.serializedPageState?.url ?? t.url)) return !0;
    ((t.isColdRestore = !1),
      (t.isSuspended = !1),
      (t.isRestoring = !1),
      (t.serializedPageState = null),
      e.deletePersistedPageState(t));
  }
  if (
    ((t.pendingNavigationUsage =
      i == null || a == null
        ? null
        : { expectedUrl: s, initiator: a, source: i, urlKind: ML(o) }),
    c == null)
  )
    return (
      (t.loadError = null),
      (t.pendingUrl = r),
      jL(t.url, o) && (t.faviconUrl = null),
      (t.url = o),
      (t.serializedPageState = PL({
        faviconUrl: t.faviconUrl,
        title: t.title || t.fallbackTitle,
        url: NL(t),
      })),
      !0
    );
  if (e.isRestrictedBrowserSidebarNavigation(c.id, o)) {
    t.pendingNavigationUsage = null;
    let n = MB(c.getURL());
    return NU(t.loadError, n) || (t.pendingUrl === n && t.url === n)
      ? !1
      : (jL(t.url, n) && (t.faviconUrl = null),
        (t.pendingUrl = n),
        (t.url = n),
        (t.serializedPageState = FL({
          faviconUrl: t.faviconUrl,
          title: t.title,
          url: n,
          webContents: c,
        })),
        e.onBrowserSidebarStateChanged(),
        !1);
  }
  ((t.loadError = null),
    (t.pendingUrl = r),
    jL(t.url, o) && (t.faviconUrl = null),
    (t.serializedPageState = PL({
      faviconUrl: t.faviconUrl,
      title: t.title || t.fallbackTitle,
      url: NL(t),
    })),
    UL(t));
  let l = t.navigationVersion,
    u = (i = 0) => {
      if (
        c.isDestroyed() ||
        t.view.webContents?.id !== c.id ||
        t.navigationVersion !== l ||
        (t.pendingUrl.length > 0 && n.ms(t.pendingUrl) !== s)
      )
        return;
      let a = (a) => {
        if (!(t.pendingUrl.length > 0 && n.ms(t.pendingUrl) !== s) && !jU(a)) {
          if (MU(a) && i < NZ) {
            setTimeout(() => {
              u(i + 1);
            }, MZ);
            return;
          }
          (t.pendingNavigationUsage?.expectedUrl === s &&
            (t.pendingNavigationUsage = null),
            !(t.loadError != null && n.ms(t.loadError.failedUrl) === s) &&
              (EZ().warning(`failed to load browser sidebar url`, {
                safe: { conversationId: t.conversationId },
                sensitive: { error: a, url: r },
              }),
              e.errorReporter.reportNonFatal(
                a instanceof Error ? a : Error(String(a)),
                { kind: `browser-sidebar-load-url` },
              )));
        }
      };
      try {
        c.loadURL(o)
          .then(() => {
            let r = t.pendingNavigationUsage,
              i = n.ms(c.getURL());
            (r?.expectedUrl === s &&
              (r.source !== `browser_use` || i === s) &&
              e.notifyBrowserSidebarNavigationUsage(
                t.conversationId,
                t.browserTabId,
                r.source,
                r.initiator,
                r.urlKind,
              ),
              r?.expectedUrl === s && (t.pendingNavigationUsage = null));
          })
          .catch(a);
      } catch (e) {
        a(e);
      }
    };
  return (u(), !0);
}
function HZ(e, t, r, i, a) {
  let o = t.owner;
  if (o.isDestroyed() || i.pendingTeardown?.reason === `closed`) return;
  let s = i.page,
    c = s?.view.webContents ?? null;
  if (s != null && c != null) {
    let n = GZ(c, i.isTabCaptureActive);
    i.isTabCaptureActive !== n &&
      ((i.isTabCaptureActive = n),
      e.enforceDetachedPageBudget(t),
      e.windowManager.sendMessageToWebContents(t.owner, {
        type: `browser-sidebar-tab-capture-state`,
        conversationId: s.conversationId,
        browserTabId: s.browserTabId,
        isActive: n,
      }));
  }
  WZ(t, i, a);
  let l = s == null ? `` : NL(s),
    u = n.ms(l),
    d = n.ms(i.snapshot.url),
    f = s != null && s.isRestoring && d.length > 0 && u.length === 0,
    p =
      s != null &&
      s.isRestoring &&
      s.isLoading &&
      s.loadError == null &&
      u.length > 0 &&
      d === u &&
      i.snapshot.tabType === n.oc.WEB &&
      i.snapshot.title.length > 0 &&
      i.snapshot.title !== s.fallbackTitle,
    m = s != null && !i.isBrowserUsePage && !f && u.length === 0,
    h =
      s == null
        ? i.snapshot
        : m
          ? TZ()
          : {
              ...i.snapshot,
              tabType: n.oc.WEB,
              isSuspended: s.isSuspended,
              title: f || p ? i.snapshot.title : s.title || s.fallbackTitle,
              url: f ? i.snapshot.url : l,
              faviconUrl: f ? i.snapshot.faviconUrl : s.faviconUrl,
              securityState: f ? i.snapshot.securityState : UZ(s),
              isAudible: s.isAudible,
              isCapturingUserMedia: s.isCapturingUserMedia,
              isLoading: s.isLoading,
              canGoBack: s.canGoBack,
              canGoForward: s.canGoForward,
            };
  ((i.snapshot = h),
    s != null &&
      e.isBrowserUseManagedBrowserTab(r, i.browserTabId) &&
      e.browserSessionRegistry.recordPageSnapshot({
        browserTabId: i.browserTabId,
        conversationId: r,
        guestWebContentsId: s.view.webContents?.id ?? null,
        isLoading: s.isLoading,
        pageKey: DL(o.id, r, i.browserTabId),
        url: h.url,
        windowId: zX(t.window.id),
      }),
    XU(e.windowManager, t.owner, r, i),
    e.windowManager.sendMessageToWebContents(o, {
      type: `browser-sidebar-state`,
      conversationId: r,
      browserTabId: i.browserTabId,
      snapshot: h,
    }));
}
function UZ(e) {
  return e.loadError?.errorDescription.startsWith(`ERR_CERT_`) === !0
    ? `certificate-error`
    : null;
}
function WZ(e, t, r) {
  let i = t.page,
    a = i?.view.webContents ?? null;
  if (i == null || a == null || a.isDestroyed()) return;
  let o = GZ(a, t.isTabCaptureActive),
    s = !n.bs(r ?? NL(i)) || (!t.visible && !t.isBrowserUseActive && !o),
    c = FZ.get(a);
  (FZ.set(a, s),
    a.setBackgroundThrottling(s),
    c === !0 &&
      !s &&
      !e.window.isDestroyed() &&
      e.window.isVisible() &&
      e.window.showInactive());
}
function GZ(e, t = !1) {
  return `isBeingCaptured` in e && typeof e.isBeingCaptured == `function`
    ? e.isBeingCaptured()
    : t;
}
function KZ(e, t, r, i, { failedUrl: a, kind: o, loadError: s }) {
  let c =
    e.getThreadStateForWindow(t, r.conversationId, r.browserTabId) ?? null;
  (c != null && ZU(e.windowManager, t.owner, r.conversationId, c),
    (r.loadError = s),
    r.pendingNavigationUsage?.expectedUrl === n.ms(a) &&
      (r.pendingNavigationUsage = null),
    (r.pendingUrl = a),
    (r.faviconUrl = null),
    (r.isAudible = !1),
    (r.isCapturingUserMedia = !1),
    (r.title = s.title),
    (r.isLoading = !1),
    (r.canGoBack = i.canGoBack()),
    (r.canGoForward = i.canGoForward()),
    (r.serializedPageState = PL({
      faviconUrl: r.faviconUrl,
      title: r.title,
      url: NL(r),
    })),
    c != null &&
      (e.syncThreadState(t, r.conversationId, c),
      e.enforceDetachedPageBudget(t)));
  let l = r.navigationVersion,
    u = (t = 0) => {
      if (
        i.isDestroyed() ||
        r.view.webContents?.id !== i.id ||
        r.loadError !== s ||
        r.navigationVersion !== l
      )
        return;
      if (i.isLoading() && t < NZ) {
        setTimeout(() => {
          u(t + 1);
        }, MZ);
        return;
      }
      let n = (n) => {
        if (!(r.loadError !== s || jU(n))) {
          if (MU(n) && t < NZ) {
            setTimeout(() => {
              u(t + 1);
            }, MZ);
            return;
          }
          (EZ().warning(`failed to load browser sidebar error page`, {
            safe: { conversationId: r.conversationId, kind: o },
            sensitive: { error: n, url: a },
          }),
            e.errorReporter.reportNonFatal(
              n instanceof Error ? n : Error(String(n)),
              { kind: `browser-sidebar-load-error-page` },
            ));
        }
      };
      try {
        i.loadURL(s.pageUrl).catch(n);
      } catch (e) {
        n(e);
      }
    };
  u();
}
function qZ({
  active: e = !0,
  adoptionLease: t,
  adoptedWebContentsId: n,
  conversationId: r,
  initialUrl: i,
  insertToRightOfTabId: a,
}) {
  return {
    type: `open-browser-tab`,
    conversationId: r,
    initialUrl: i,
    insertToRightOfTabId: a,
    adoptionLease: t,
    adoptedWebContentsId: n,
    ...(e ? {} : { active: e }),
    source: `manual`,
    initiator: `window_open`,
  };
}
function JZ(e) {
  return (
    typeof e == `object` &&
    !!e &&
    `_createWebViewAdoptionLease` in e &&
    typeof e._createWebViewAdoptionLease == `function`
  );
}
function YZ(e, t) {
  let n = `webContents` in e && JZ(e.webContents) ? e.webContents : null;
  if (n == null)
    throw Error(`Owl browser sidebar window.open child WebContents missing`);
  let r = n._createWebViewAdoptionLease(t);
  if (typeof r != `string` || r.length === 0)
    throw Error(`Owl browser sidebar webview adoption lease is empty`);
  return { adoptionLease: r, webContents: n };
}
var XZ = class {
  isEnabled = !0;
  ownerWindowId = null;
  constructor(e, t, n, r, i) {
    ((this.browserPageSnapshots = e),
      (this.browserSessionRegistry = t),
      (this.windowManager = n),
      (this.windows = r),
      (this.lifecycle = i));
  }
  setEnabled(e, t) {
    if (!(t ? this.isPersistenceOwner(e) : this.isCleanupAllowed(e))) return !1;
    if (((this.isEnabled = t), t)) return !0;
    this.browserPageSnapshots.clearAndDisable();
    for (let e of this.windows.values())
      e.registeredWebviewHostsByRoutePartition.clear();
    return !0;
  }
  getRestorablePageSnapshots(e, t) {
    return e == null || !this.isPagePersistenceAllowed(e)
      ? t.map(() => null)
      : t.map((e) => {
          if (!this.browserPageSnapshots.isDurableStorageIdentityOwnedBy(e))
            return null;
          this.browserPageSnapshots.enablePersistence(e);
          let t = this.browserPageSnapshots.get(e);
          if (t == null) return null;
          let r = t.navigation.currentIndex;
          return {
            ...TZ(),
            canGoBack: r > 0,
            canGoForward: r < t.navigation.entries.length - 1,
            faviconUrl: t.faviconUrl,
            isSuspended: !0,
            tabType: n.oc.WEB,
            title: t.title,
            url: t.url,
          };
        });
  }
  deleteConversation(e, t) {
    if (!this.isCleanupAllowed(e)) return !1;
    let n = [];
    for (let e of this.windows.values())
      for (let r of e.threads.values()) r.conversationId === t && n.push(r);
    return (this.browserPageSnapshots.deleteConversationAndFlush(t, n), !0);
  }
  isPersistenceOwner(e) {
    return e != null && this.isInitialPrimaryWindow(e);
  }
  getPagePersistence(e, t) {
    if (t != null) return this.isPagePersistenceAllowed(e) ? t : void 0;
  }
  isPagePersistenceAllowed(e) {
    return this.isEnabled && this.isInitialPrimaryWindow(e);
  }
  hasBrowserStorageOwner(e, t) {
    for (let n of this.windows.values()) {
      for (let r of n.threads.values())
        if (r !== t && r.browserStorageId === e) return !0;
      for (let r of n.registeredWebviewHostsByRoutePartition.values())
        if (
          r.pagePersistence.browserStorageId === e &&
          (t == null ||
            r.browserTabId !== t.browserTabId ||
            r.conversationId !== t.conversationId)
        )
          return !0;
    }
    return !1;
  }
  reconcileBrowserStorageId(e, t, r) {
    if (r == null) return !0;
    if (r.browserStorageId === t.browserStorageId)
      return (
        r.restore !== `required` ||
        !this.hasBrowserStorageOwner(r.browserStorageId, t)
      );
    if (
      t.pendingTeardown != null ||
      t.sessionState !== `cold` ||
      t.browserStorageId !==
        bL({
          browserTabId: t.browserTabId,
          conversationId: t.conversationId,
        }) ||
      this.browserPageSnapshots.hasDurableStorageIdentity(t.browserStorageId) ||
      e.registeredWebviewHostsByRoutePartition.has(
        n.zo(t.conversationId, t.browserTabId),
      )
    )
      return !1;
    let i = t.page;
    return r.restore === `required`
      ? i != null ||
        t.isBrowserUsePage ||
        this.hasBrowserStorageOwner(r.browserStorageId, t)
        ? !1
        : ((t.browserStorageId = r.browserStorageId), !0)
      : (this.browserPageSnapshots.hasDurableStorageIdentity(
            r.browserStorageId,
          ) &&
            !this.browserPageSnapshots.isDurableStorageIdentityOwnedBy({
              browserStorageId: r.browserStorageId,
              browserTabId: t.browserTabId,
              conversationId: t.conversationId,
            })) ||
          this.hasBrowserStorageOwner(r.browserStorageId, t)
        ? !1
        : i == null
          ? t.isBrowserUsePage
            ? !1
            : ((t.browserStorageId = r.browserStorageId), !0)
          : !t.isBrowserUsePage ||
              t.browserUseTabOrigin !== `agent` ||
              i.browserStorageId !== t.browserStorageId ||
              i.view.webContents != null ||
              i.isColdRestore ||
              i.isRestoring ||
              i.isSuspended ||
              i.pendingSuspensionId != null
            ? !1
            : ((t.browserStorageId = r.browserStorageId),
              (i.browserStorageId = r.browserStorageId),
              !0);
  }
  prepareWebviewAttach(e, t, n, r) {
    if (r != null) {
      if (
        r.restore === `required` &&
        this.preparePage(e, t, r).kind === `missing`
      )
        return null;
      this.registerWebviewHost(e, t, r);
    }
    return (
      t.page ??
      this.lifecycle.materializePageState(e, t, { kind: `fresh`, page: n })
    );
  }
  preparePage(e, t, n) {
    n != null && this.browserPageSnapshots.enablePersistence(t);
    let r = ZZ({
      browserPageSnapshots: this.browserPageSnapshots,
      browserSessionRegistry: this.browserSessionRegistry,
      materializeColdRestoredPage: (n) =>
        this.lifecycle.materializePageState(e, t, {
          kind: `cold-restore`,
          snapshot: n,
        }),
      pagePersistence: n,
      threadState: t,
      windowState: e,
    });
    if (r.kind === `missing`) {
      let n = {
        browserTabId: t.browserTabId,
        conversationId: t.conversationId,
      };
      (this.lifecycle.removeClosedPage(e, t),
        e.ignoredConversationIds.add(OL(n)),
        this.windowManager.sendMessageToWebContents(e.owner, {
          type: `close-browser-tab`,
          ...n,
        }));
    }
    return r;
  }
  registerWebviewHost(e, t, r) {
    let i = n.zo(t.conversationId, t.browserTabId);
    r == null
      ? e.registeredWebviewHostsByRoutePartition.delete(i)
      : (this.browserPageSnapshots.enablePersistence(t),
        e.registeredWebviewHostsByRoutePartition.set(i, {
          browserTabId: t.browserTabId,
          conversationId: t.conversationId,
          pagePersistence: r,
        }),
        t.page?.isColdRestore === !0 &&
          QX(
            this.browserSessionRegistry,
            e,
            t,
            `browser-restore-host-registered`,
            `Registered persisted Browser webview host`,
          ));
  }
  unregisterWebviewHost(e, t) {
    let r = n.zo(t.conversationId, t.browserTabId),
      i = e.registeredWebviewHostsByRoutePartition.get(r) ?? null;
    i?.pagePersistence.browserStorageId === t.browserStorageId &&
      i.browserTabId === t.browserTabId &&
      i.conversationId === t.conversationId &&
      e.registeredWebviewHostsByRoutePartition.delete(r);
  }
  isCleanupAllowed(e) {
    return (
      e != null &&
      this.windowManager.getPrimaryWindows().some((t) => t.id === e.id)
    );
  }
  isInitialPrimaryWindow(e) {
    return (
      (this.ownerWindowId ??=
        this.windowManager.getPrimaryWindows()[0]?.id ?? null),
      this.ownerWindowId === e.id
    );
  }
};
function ZZ({
  browserPageSnapshots: e,
  browserSessionRegistry: t,
  materializeColdRestoredPage: n,
  pagePersistence: r,
  threadState: i,
  windowState: a,
}) {
  if (r?.restore !== `required`) return { kind: `not-requested` };
  let o = i.page,
    s = o?.view.webContents;
  if (o?.isColdRestore === !0 || (s != null && !s.isDestroyed()))
    return { kind: `ready` };
  QX(t, a, i, `browser-restore-shell-synced`, `Persisted Browser shell synced`);
  let c = e.get(i);
  return c == null
    ? (QX(
        t,
        a,
        i,
        `browser-restore-snapshot-missing`,
        `Persisted Browser shell had no page snapshot`,
      ),
      e.deleteClosedPageAndFlush(i),
      { kind: `missing` })
    : (QX(
        t,
        a,
        i,
        `browser-restore-snapshot-found`,
        `Found persisted Browser page snapshot`,
        [
          {
            label: `historyEntryCount`,
            value: String(c.navigation.entries.length),
          },
        ],
      ),
      o != null && (o.dispose(), (i.page = null)),
      n(c),
      QX(
        t,
        a,
        i,
        `browser-restore-page-materialized`,
        `Materialized persisted Browser page`,
      ),
      { kind: `ready` });
}
var QZ = 32,
  $Z = 30 * 6e4,
  eQ = class {
    tabActivitySequence = 0;
    activeConversationByWindow = new Map();
    selectedTabsByWindow = new Map();
    workingSetExpiryTimersByWindow = new Map();
    constructor(e) {
      this.options = e;
    }
    markTabActivity(e) {
      ((this.tabActivitySequence += 1),
        (e.lastTabActivitySequence = this.tabActivitySequence));
    }
    markTabSelected(e, t) {
      this.markTabActivity(t);
      let n = e.window.id,
        r = this.now(),
        i = this.selectedTabsByWindow.get(n);
      i ?? ((i = new Map()), this.selectedTabsByWindow.set(n, i));
      let a = this.activeConversationByWindow.get(n),
        o = a == null ? null : i.get(a);
      (o != null && (o.selectedAt = r),
        i.set(t.conversationId, {
          browserTabId: t.browserTabId,
          selectedAt: r,
        }),
        this.activeConversationByWindow.set(n, t.conversationId));
    }
    isTabProtectedFromSuspension(e, t) {
      return this.getProtection(e, t, this.now()).protected;
    }
    enforceDetachedPageBudgets() {
      for (let e of this.options.windows.values())
        this.enforceDetachedPageBudget(e);
    }
    enforceDetachedPageBudget(e) {
      let t = e.window.id;
      this.clearWorkingSetExpiryTimer(t);
      let n = nQ(e);
      if (
        n.length <= QZ ||
        Array.from(e.threads.values()).some(
          (e) =>
            e.page?.isRestoring === !0 ||
            e.pendingTeardown?.reason === `suspended`,
        )
      )
        return;
      let r = this.now(),
        i = n
          .filter(
            (t) =>
              t.pendingTeardown == null &&
              !this.getProtection(e, t, r).protected,
          )
          .sort(
            (t, n) =>
              tQ(this.getCandidateRank(e, t)) -
                tQ(this.getCandidateRank(e, n)) ||
              t.lastTabActivitySequence - n.lastTabActivitySequence ||
              t.browserTabId.localeCompare(n.browserTabId),
          )[0];
      if (i == null) {
        this.scheduleWorkingSetExpiry(e, n, r);
        return;
      }
      let a = this.getCandidateRank(e, i),
        o = this.getProtectionCounts(e, n, r);
      (this.recordTabBudgetEvent(
        e,
        i,
        `browser-tab-budget-suspension-selected`,
        `Selected Browser tab for budget suspension`,
        {
          budget: QZ,
          candidateRank: a,
          activeTabProtectedCount: o.activeTab,
          liveDetachedRendererCount: n.length,
          pressureReason: `count`,
          workingSetSelectedProtectedCount: o.workingSetSelected,
        },
      ),
        this.options.suspendDetachedThread(e, i));
    }
    recordTabSuspended(e, t) {
      this.recordTabBudgetEvent(
        e,
        t,
        `browser-tab-budget-tab-suspended`,
        `Suspended Browser tab selected by the tab budget`,
        { pressureReason: `count` },
      );
    }
    handleWindowClosed(e) {
      (this.clearWorkingSetExpiryTimer(e),
        this.activeConversationByWindow.delete(e),
        this.selectedTabsByWindow.delete(e));
    }
    getCandidateRank(e, t) {
      return e.activeBrowserTabId != null &&
        e.activeConversationId === t.conversationId
        ? `current-thread-secondary`
        : this.getSelectedTab(e, t)?.browserTabId === t.browserTabId
          ? `inactive-selected`
          : `inactive-secondary`;
    }
    getProtection(e, t, n) {
      let r =
          e.activeConversationId === t.conversationId &&
          e.activeBrowserTabId === t.browserTabId,
        i = this.getSelectedTab(e, t),
        a = !r && i?.browserTabId === t.browserTabId && n - i.selectedAt < $Z;
      return {
        activeTab: r,
        workingSetSelected: a,
        protected:
          r ||
          a ||
          t.presented ||
          t.isBrowserUseActive ||
          t.isTabCaptureActive === !0 ||
          t.page?.isAudible === !0 ||
          t.page?.isCapturingUserMedia === !0 ||
          t.page?.isLoading === !0 ||
          t.page?.isRestoring === !0 ||
          this.options.hasActiveDownload(t),
      };
    }
    getSelectedTab(e, t) {
      return this.selectedTabsByWindow.get(e.window.id)?.get(t.conversationId);
    }
    getProtectionCounts(e, t, n) {
      let r = { activeTab: 0, workingSetSelected: 0 };
      for (let i of t) {
        let t = this.getProtection(e, i, n);
        ((r.activeTab += t.activeTab ? 1 : 0),
          (r.workingSetSelected += t.workingSetSelected ? 1 : 0));
      }
      return r;
    }
    scheduleWorkingSetExpiry(e, t, n) {
      let r = 1 / 0;
      for (let i of t) {
        if (!this.getProtection(e, i, n).workingSetSelected) continue;
        let t = this.getSelectedTab(e, i);
        t != null && (r = Math.min(r, t.selectedAt + $Z));
      }
      if (!Number.isFinite(r)) return;
      let i = e.window.id,
        a = setTimeout(() => {
          this.workingSetExpiryTimersByWindow.delete(i);
          let e = this.options.windows.get(i);
          e != null && this.enforceDetachedPageBudget(e);
        }, r - n);
      (a.unref(), this.workingSetExpiryTimersByWindow.set(i, a));
    }
    clearWorkingSetExpiryTimer(e) {
      let t = this.workingSetExpiryTimersByWindow.get(e);
      t != null &&
        (clearTimeout(t), this.workingSetExpiryTimersByWindow.delete(e));
    }
    recordTabBudgetEvent(e, t, n, r, i) {
      this.options.browserSessionRegistry.recordDebugEvent({
        browserTabId: t.browserTabId,
        conversationId: t.conversationId,
        details: Object.entries(i).map(([e, t]) => ({
          label: e,
          value: String(t),
        })),
        guestWebContentsId: t.page?.view.webContents?.id ?? null,
        kind: n,
        message: r,
        ownerWebContentsId: e.owner.id,
        pageKey: null,
        windowId: e.window.id,
      });
    }
    now() {
      return this.options.now?.() ?? Date.now();
    }
  };
function tQ(e) {
  switch (e) {
    case `inactive-secondary`:
      return 0;
    case `inactive-selected`:
      return 1;
    case `current-thread-secondary`:
      return 2;
  }
}
function nQ(e) {
  return Array.from(e.threads.values()).filter(
    (e) =>
      e.sessionState === `live-detached` &&
      e.page?.view.webContents != null &&
      !e.page.view.webContents.isDestroyed(),
  );
}
var rQ = r.a(`browser-sidebar-manager`);
function iQ({ action: e, isBrowserUseTab: t, threadState: n, windows: r }) {
  let i = t ?? n.isBrowserUsePage,
    a = 0,
    o = 0,
    s = 0,
    c = 0;
  for (let e of r.values())
    for (let t of e.threads.values())
      uQ(t) &&
        ((a += 1),
        t.conversationId === n.conversationId && (s += 1),
        t.isBrowserUsePage &&
          ((o += 1), t.conversationId === n.conversationId && (c += 1)));
  return {
    type: `browser-sidebar-tab-lifecycle`,
    action: e,
    conversationId: n.conversationId,
    threadBrowserTabCount: s,
    appBrowserTabCount: a,
    threadBrowserUseTabCount: c,
    appBrowserUseTabCount: o,
    isBrowserUseTab: i,
  };
}
function aQ({ windowManager: e, windowState: t, ...n }) {
  if (!uQ(n.threadState)) return;
  let r = iQ(n);
  (rQ().info(`IAB_TAB_ANALYTICS browser tab lifecycle`, {
    safe: {
      action: r.action,
      appBrowserTabCount: r.appBrowserTabCount,
      appBrowserUseTabCount: r.appBrowserUseTabCount,
      browserTabId: n.threadState.browserTabId,
      conversationId: r.conversationId,
      isBrowserUseTab: r.isBrowserUseTab,
      ownerWebContentsId: t.owner.id,
      threadBrowserTabCount: r.threadBrowserTabCount,
      threadBrowserUseTabCount: r.threadBrowserUseTabCount,
    },
    sensitive: {},
  }),
    e.sendMessageToWebContents(t.owner, r));
}
function oQ(e, t, n, r) {
  aQ({
    action: `opened`,
    threadState: r,
    windowManager: e,
    windows: t,
    windowState: n,
  });
}
function sQ(e, t, n, r, i) {
  (e.registerThread({
    conversationId: i.conversationId,
    windowId: zX(r.window.id),
  }),
    oQ(t, n, r, i));
}
function cQ(e, t, n, r) {
  aQ({
    action: `closed`,
    isBrowserUseTab: r.isBrowserUsePage,
    threadState: r,
    windowManager: e,
    windows: t,
    windowState: n,
  });
}
function lQ(e, t, n, r) {
  let i = OL(r);
  (n.threads.delete(i),
    n.ignoredConversationIds.delete(i),
    n.activeConversationId === r.conversationId &&
      n.activeBrowserTabId === r.browserTabId &&
      ((n.activeConversationId = null), (n.activeBrowserTabId = null)),
    cQ(e, t, n, r));
}
function uQ(e) {
  return e.isBrowserUsePage || e.browserTabId !== yL(e.conversationId);
}
var dQ = r.a(`browser-sidebar-manager`);
function fQ({ browserSessionRegistry: e, source: t }) {
  t?.registerInternalNotificationHandler((t) => {
    if (mQ(t)) {
      e.turnEnded({
        conversationId: t.params.threadId,
        turnId: t.params.turn.id,
      }).catch((e) => {
        dQ().warning(`IAB_LIFECYCLE failed to end browser use turn`, {
          safe: { conversationId: t.params.threadId, turnId: t.params.turn.id },
          sensitive: { error: e },
        });
      });
      return;
    }
    if (pQ(t)) {
      e.disposeConversation(t.params.threadId);
      return;
    }
    hQ(t) && e.endSessionActivity({ conversationId: t.params.threadId });
  });
}
function pQ(e) {
  return (
    e.method === `thread/archived` && typeof e.params?.threadId == `string`
  );
}
function mQ(e) {
  return (
    e.method === `turn/completed` &&
    typeof e.params?.threadId == `string` &&
    typeof e.params.turn == `object` &&
    e.params.turn != null &&
    `id` in e.params.turn &&
    typeof e.params.turn.id == `string`
  );
}
function hQ(e) {
  let t = e.params?.status;
  return (
    e.method === `thread/status/changed` &&
    typeof e.params?.threadId == `string` &&
    typeof t == `object` &&
    !!t &&
    `type` in t &&
    t.type === `idle`
  );
}
function gQ(e, t, n) {
  return (
    n ?? (e.activeConversationId === t ? e.activeBrowserTabId : null) ?? yL(t)
  );
}
function _Q(e, t, n) {
  let r = gQ(e, t, n);
  return e.threads.get(OL({ browserTabId: r, conversationId: t })) ?? null;
}
function vQ(e, t) {
  for (let n of e.threads.values()) if (n.conversationId === t) return !0;
  return !1;
}
function yQ({
  browserSessionRegistry: e,
  browserTabId: t,
  conversationId: r,
  options: { isBrowserUseTab: i = !1 } = {},
  tabBudget: a,
  windowManager: o,
  windows: s,
  windowState: c,
}) {
  let l = gQ(c, r, t),
    u = OL({ browserTabId: l, conversationId: r }),
    d = c.threads.get(u);
  if (d != null) return d;
  let f = bQ({
    annotationModeEnabled: !0,
    browserStorageId: bL({ browserTabId: l, conversationId: r }),
    browserTabId: l,
    conversationId: r,
    hostKind: null,
    emulatedViewportSize: null,
    cwd: null,
    isBrowserUsePage: i,
    runtimeIntlConfig: n.Sc,
  });
  return (a.markTabActivity(f), c.threads.set(u, f), sQ(e, o, s, c, f), f);
}
function bQ({
  annotationModeEnabled: e,
  browserStorageId: t,
  browserTabId: n,
  conversationId: r,
  cwd: i,
  emulatedViewportSize: a,
  hostKind: o,
  isBrowserUsePage: s,
  runtimeIntlConfig: c,
}) {
  return {
    browserStorageId: t,
    browserTabId: n,
    conversationId: r,
    findState: WU(),
    page: null,
    hostKind: o,
    pendingBrowserUsePanelOpen: !1,
    hasPendingBrowserUseVisibilityRequest: !1,
    pendingCommentModeActivation: null,
    visible: !1,
    bounds: null,
    presented: !1,
    hostGeneration: null,
    mountGeneration: 0,
    pendingTeardown: null,
    sessionState: `cold`,
    lastTabActivitySequence: 0,
    lastPresentedSequence: 0,
    lastLifecycleSequence: 0,
    emulatedViewportSize: a,
    viewportScale: 1,
    cwd: i,
    isAgentControllingBrowser: !1,
    isAnnotationModeEnabled: e,
    isBrowserUseActive: !1,
    isBrowserUsePage: s,
    isTabCaptureActive: !1,
    browserUseTabOrigin: null,
    isBrowserUseNavigationRestrictionActive: !1,
    ownerRoutePath: null,
    commentModeBlockCheckRequestId: 0,
    commentModeBlockCheckUrl: null,
    themeVariant: `light`,
    runtimeIntlConfig: c,
    snapshot: TZ(),
  };
}
var xQ = r.a(`browser-sidebar-manager`);
function SQ({ ensureWindowState: e, windowId: t, windows: n }) {
  let r = n.get(t) ?? null;
  if (r == null) {
    let n = c.BrowserWindow.fromId(t);
    n != null &&
      !n.isDestroyed() &&
      !n.webContents.isDestroyed() &&
      (r = e(n, n.webContents));
  }
  return r == null || r.window.isDestroyed() || r.owner.isDestroyed()
    ? (xQ().warning(`IAB_LIFECYCLE route window is not live`, {
        safe: {
          hasWindowState: r != null,
          ownerDestroyed: r?.owner.isDestroyed() ?? null,
          windowDestroyed: r?.window.isDestroyed() ?? null,
          windowId: t,
        },
        sensitive: {},
      }),
      null)
    : r;
}
function CQ({ getLiveWindowState: e, lastSyncedWindowId: t, windows: n }) {
  let r = c.BrowserWindow.getFocusedWindow();
  if (r != null && !r.isDestroyed()) return r.webContents;
  let i = c.BrowserWindow.getAllWindows().filter((e) => !e.isDestroyed());
  if (i.length === 1) return i[0]?.webContents ?? null;
  if (t != null) {
    let n = e(t);
    if (n != null) return n.owner;
  }
  for (let t of n) {
    let n = e(t.window.id);
    if (n != null) return n.owner;
  }
  return null;
}
function wQ({ getLiveWindowState: e, windows: t }) {
  let n = c.BrowserWindow.getFocusedWindow();
  return n == null || n.isDestroyed() || !t.has(n.id) ? null : e(n.id);
}
function TQ({
  getFocusedWindowState: e,
  getLiveWindowState: t,
  lastSyncedWindowId: n,
  windows: r,
}) {
  let i = e();
  if (i != null) return i;
  if (n != null) {
    let e = t(n);
    if (e != null) return e;
  }
  for (let e of r) {
    let n = t(e.window.id);
    if (n != null) return n;
  }
  return null;
}
function EQ({
  conversationId: e,
  getFocusedWindowState: t,
  hasThreadForConversation: n,
  lastSyncedWindowId: r,
  windows: i,
}) {
  let a = t();
  if (a != null && !a.owner.isDestroyed() && n(a, e)) return a;
  if (r != null) {
    let t = i.get(r) ?? null;
    if (t != null && !t.owner.isDestroyed() && n(t, e)) return t;
  }
  for (let t of i.values()) if (!t.owner.isDestroyed() && n(t, e)) return t;
  return null;
}
var DQ = r.a(`browser-sidebar-screenshot`);
async function OQ(e) {
  let t = await e.capturePage();
  c.clipboard.writeImage(t);
}
async function kQ({
  browserTabId: e,
  conversationId: t,
  errorReporter: r,
  origin: i,
  pageState: a,
  windowManager: o,
}) {
  let s = a?.browserTabId ?? e ?? n._c(crypto.randomUUID()),
    c = a?.view.webContents;
  if (c == null || c.isDestroyed()) {
    o.sendMessageToWebContents(i, {
      type: `browser-sidebar-screenshot-copy-failed`,
      conversationId: t,
      browserTabId: s,
    });
    return;
  }
  try {
    (await OQ(c),
      o.sendMessageToWebContents(i, {
        type: `browser-sidebar-screenshot-copied`,
        conversationId: t,
        browserTabId: s,
      }));
  } catch (e) {
    (DQ().warning(`failed to capture browser sidebar screenshot`, {
      safe: { conversationId: t },
      sensitive: { error: e },
    }),
      r.reportNonFatal(e instanceof Error ? e : Error(String(e)), {
        kind: `browser-sidebar-screenshot`,
      }),
      o.sendMessageToWebContents(i, {
        type: `browser-sidebar-screenshot-copy-failed`,
        conversationId: t,
        browserTabId: s,
      }));
  }
}
async function AQ(e, t) {
  await e.executeJavaScript(jQ(t));
}
function jQ(e) {
  return `(() => {
    const scroll = ${JSON.stringify(e)};
    const root = document.scrollingElement ?? document.documentElement;
    switch (scroll.type) {
      case "pixels":
        window.scrollBy({ top: scroll.y, behavior: "auto" });
        return;
      case "pages":
        window.scrollBy({
          top: window.innerHeight * scroll.count,
          behavior: "auto",
        });
        return;
      case "edge":
        root.scrollTo({
          top: scroll.edge === "top" ? 0 : root.scrollHeight,
          behavior: "auto",
        });
        return;
    }
  })();`;
}
var MQ = r.a(`browser-sidebar-manager`),
  NQ = (0, u.join)(__dirname, `comment-preload.js`),
  PQ = class {
    downloadStore;
    windows = new Map();
    tabBudget;
    browserUseOpenRequests;
    browserUseNavigationBlockedListeners = new Set();
    activeDraggedImagesByOwnerWebContentsId = new Map();
    onBrowserSidebarStateChanged = () => {};
    lastSyncedWindowId = null;
    ownerCommandAccelerators = rW();
    comments;
    browserPageSnapshots;
    browserSessionService;
    browserPersistence;
    isTweaksFeatureEnabled = !1;
    lifecycleSequence = 0;
    browserUseNavigationRestriction = new dR({
      findPageStateForPageKey: (e) => this.findPageStateForPageKey(e),
      findPageStateForWebContentsId: (e) =>
        this.findPageStateForWebContentsId(e),
      onNavigationBlocked: (e, t, n, r) => {
        (MQ().warning(`blocked browser-use navigation`, {
          safe: {},
          sensitive: { error: t, url: n },
        }),
          this.notifyBrowserUseNavigationBlocked({
            error: t,
            pageKey: DL(
              e.windowState.owner.id,
              e.conversationId,
              e.threadState.browserTabId,
            ),
            resourceType: r.resourceType,
            url: n,
          }));
      },
    });
    constructor(e, t, n = V, r = jI(), i = new HX(), a = TL(), o = {}) {
      ((this.windowManager = e),
        (this.errorReporter = t),
        (this.commentModeBlocklistLookup = r),
        (this.browserSessionRegistry = i),
        (this.defaultBrowserUseViewportSize = a),
        (this.downloadStore = new mH(o)),
        (this.browserPageSnapshots =
          o.browserPageSnapshots ?? sR(o.browserPersistenceDirectory)),
        (this.browserSessionService = new AB({
          browserSessionRegistry: this.browserSessionRegistry,
          downloadStore: this.downloadStore,
          findPageStateForWebContentsId:
            this.findPageStateForWebContentsId.bind(this),
          getWindows: this.windows.values.bind(this.windows),
          isRestrictedBrowserSidebarNavigation:
            this.isRestrictedBrowserSidebarNavigation.bind(this),
        })),
        (this.browserPersistence = new XZ(
          this.browserPageSnapshots,
          this.browserSessionRegistry,
          this.windowManager,
          this.windows,
          {
            materializePageState: this.materializeThreadPageState.bind(this),
            removeClosedPage: this.removeClosedThreadState.bind(this),
          },
        )),
        (this.browserUseOpenRequests = new RB((e) => {
          this.browserSessionRegistry.recordDebugEvent(e);
        })),
        (this.tabBudget = new eQ({
          browserSessionRegistry: this.browserSessionRegistry,
          hasActiveDownload: (e) =>
            this.downloadStore.hasActiveDownloadForRoute(e),
          suspendDetachedThread: (e, t) => {
            this.suspendDetachedThread(e, t);
          },
          windows: this.windows,
        })),
        this.downloadStore.setActiveDownloadChangeListener(() => {
          this.tabBudget.enforceDetachedPageBudgets();
        }),
        this.browserSessionRegistry.setDelegate({
          addBrowserUseNavigationBlockedListener: (e) =>
            this.addBrowserUseNavigationBlockedListener(e),
          activateBrowserUseNavigationRestriction: (e) =>
            this.activateBrowserUseNavigationRestriction(e),
          assertBrowserUseNavigationAllowed: (e, t) =>
            this.assertBrowserUseNavigationAllowed(e, t),
          clearPendingBrowserUseNavigation: (e) =>
            fZ(this.findPageStateForPageKey(e)?.page ?? null),
          disposeBrowserUseRoute: (e) => this.disposeBrowserUseRouteForRoute(e),
          getPageForBrowserUse: (e, t) =>
            this.getPageForBrowserUseForRoute(e, t),
          getPagesForBrowserUse: (e) => this.getPagesForBrowserUseForRoute(e),
          getTabsForBrowserUse: (e) => this.getTabsForBrowserUseForRoute(e),
          getUserTabsForBrowserUse: (e) =>
            this.getUserTabsForBrowserUseForRoute(e),
          isWindowAlive: (e) => this.getLiveWindowState(e) != null,
          closePageForBrowserUse: (e, t) =>
            this.closePageForBrowserUseForRoute(e, t),
          releasePageForBrowserUse: (e, t) =>
            this.releasePageForBrowserUseForRoute(e, t),
          openPageForBrowserUse: (e, t) =>
            this.openPageForBrowserUseForRoute({
              ...e,
              browserTabId: t?.browserTabId,
              origin: t?.origin,
              startingUrl: t?.startingUrl,
            }),
          isBrowserVisibleForBrowserUse: (e, t) =>
            this.isBrowserVisibleForBrowserUseForRoute(e, t),
          setBrowserVisibleForBrowserUse: (e, t, n) =>
            this.setBrowserVisibleForBrowserUseForRoute(e, t, n),
          setViewportForBrowserUse: (e, t, n) =>
            this.setViewportForBrowserUseForRoute(e, t, n),
          setCaptureSurfaceForBrowserUse: (e, t, n) =>
            this.setCaptureSurfaceForBrowserUseForRoute(e, t, n),
          waitForPendingDebuggerSync: (e, t) =>
            this.waitForPendingDebuggerSyncForRoute(e, t),
          waitForPageReady: (e, t) => this.waitForPageReadyForRoute(e, t),
          setBrowserUseActive: (e, t, n) =>
            this.setBrowserUseActiveForRoute(e, t, n),
          setBrowserUseCursor: (e, t, n) =>
            this.setBrowserUseCursorForRoute(e, t, n),
          trackPendingBrowserUseNavigation: (e, t) =>
            mZ(this.findPageStateForPageKey(e)?.page ?? null, t),
        }),
        (this.comments = new IV(
          e,
          t,
          {
            canUseTweaks: () => this.isTweaksFeatureEnabled,
            getThreadState: (e, t, n) => this.getThreadState(e, t, n),
            findPageStateForRuntimeSender: (e) =>
              this.findPageStateForRuntimeSender(e),
            syncCommentSnapshot: (e, t, n) => {
              let r = this.getThreadState(e, t, n);
              r != null && this.syncCommentSnapshot(e, t, r);
            },
          },
          n,
        )),
        fQ({
          browserSessionRegistry: this.browserSessionRegistry,
          source: o.appServerNotificationSource,
        }));
    }
    setOnBrowserSidebarStateChanged(e) {
      this.onBrowserSidebarStateChanged = e;
    }
    refreshOwnerCommandAccelerators() {
      this.ownerCommandAccelerators = rW();
    }
    browserSettingsWebviewMounted(e, t, n = `light`) {
      let r = this.ensureCurrentWindowState(e);
      r != null && (r.browserSettingsWebviewMountIds.add(t), mB(r, n));
    }
    browserSettingsWebviewThemeChanged(e, t) {
      let n = this.ensureCurrentWindowState(e);
      n != null && mB(n, t);
    }
    browserSettingsWebviewUnmounted(e, t) {
      this.getCurrentWindowState(e)?.browserSettingsWebviewMountIds.delete(t);
    }
    sync(e, t) {
      let n = this.ensureCurrentWindowState(e);
      if (n == null) return;
      this.lastSyncedWindowId = n.window.id;
      let r = n.activeConversationId,
        i = n.activeBrowserTabId,
        a = t.mountGeneration ?? 0,
        o = t.presented ?? (t.visible && t.bounds != null),
        s = gQ(n, t.conversationId, t.browserTabId),
        c = { browserTabId: s, conversationId: t.conversationId },
        l = this.browserPersistence.getPagePersistence(
          n.window,
          t.pagePersistence,
        );
      t.transferSourceConversationId != null &&
        this.transferConversationState(
          n,
          t.transferSourceConversationId,
          t.transferSourceBrowserTabId,
          t.conversationId,
          s,
        );
      let u = OL(c);
      if (n.ignoredConversationIds.delete(u)) return;
      (o || n.activeConversationId == null) &&
        ((n.activeConversationId = t.conversationId),
        (n.activeBrowserTabId = s));
      let d = n.threads.has(u),
        f = this.ensureThreadState(n, t, s),
        p = f.presented;
      if (
        ((f.presented = o),
        o && !p && this.tabBudget.markTabSelected(n, f),
        (!d ||
          f.mountGeneration !== a ||
          f.presented !== o ||
          f.visible !== t.visible ||
          t.transferSourceConversationId != null) &&
          this.browserSessionRegistry.recordDebugEvent({
            ...c,
            details: [
              { label: `mountGeneration`, value: String(a) },
              { label: `presented`, value: String(o) },
              { label: `visible`, value: String(t.visible) },
              ...(t.transferSourceConversationId == null
                ? []
                : [
                    {
                      label: `transferSourceConversationId`,
                      value: t.transferSourceConversationId,
                    },
                  ]),
            ],
            guestWebContentsId: null,
            kind: `browser-sidebar-sync`,
            message: `Synced Browser sidebar route`,
            ownerWebContentsId: e.id,
            pageKey: null,
            windowId: n.window.id,
          }),
        !this.browserPersistence.reconcileBrowserStorageId(n, f, l))
      )
        return;
      switch (
        (this.setThreadAnnotationModeEnabled(
          e,
          n,
          f,
          t.annotationModeEnabled ?? !0,
        ),
        this.browserPersistence.preparePage(n, f, l).kind)
      ) {
        case `missing`:
          return;
        case `not-requested`:
        case `ready`:
          break;
      }
      ((f.cwd = t.cwd ?? null),
        (f.hostKind = t.hostKind ?? null),
        (f.visible = t.visible),
        (f.mountGeneration = a),
        o &&
          f.pendingTeardown?.reason === `suspended` &&
          ((f.pendingTeardown = null),
          f.page != null && (f.page.pendingSuspensionId = null)),
        (f.hasPendingBrowserUseVisibilityRequest = !1),
        (f.isAgentControllingBrowser = t.isAgentControllingBrowser),
        t.isAgentControllingBrowser ||
          (f.isBrowserUseNavigationRestrictionActive = !1),
        (f.runtimeIntlConfig = t.runtimeIntlConfig));
      let m = f.themeVariant;
      ((f.themeVariant = t.themeVariant ?? f.themeVariant),
        (f.bounds = t.bounds
          ? {
              x: Math.round(t.bounds.x),
              y: Math.round(t.bounds.y),
              width: Math.round(t.bounds.width),
              height: Math.round(t.bounds.height),
            }
          : null));
      let h = t.emulatedViewportSize ?? null,
        g =
          f.emulatedViewportSize?.width !== h?.width ||
          f.emulatedViewportSize?.height !== h?.height;
      if (
        ((f.emulatedViewportSize = h),
        (f.viewportScale = t.viewportScale ?? 1),
        this.updateThreadSessionState(f),
        this.syncThreadState(n, t.conversationId, f),
        f.page != null)
      ) {
        if (
          (o && (f.page.pendingSuspensionId = null),
          g && this.queuePageDeviceMetricsSync(f.page, f.emulatedViewportSize),
          m !== f.themeVariant)
        ) {
          let e = f.page.view.webContents;
          e != null &&
            !e.isDestroyed() &&
            this.queuePageColorSchemeSync(f.page, e, f.themeVariant);
        }
        this.syncRuntimeStateToPage(f.page, f);
      }
      if (
        (this.comments[f.isAnnotationModeEnabled ? `prepare` : `dismiss`](
          e,
          t.conversationId,
          s,
          f,
        ),
        o && r != null && i != null && (r !== t.conversationId || i !== s))
      ) {
        let t = _Q(n, r, i) ?? null;
        (t != null && (t.hasPendingBrowserUseVisibilityRequest = !1),
          this.comments.dismiss(e, r, i, t),
          this.comments.close(e, r, i));
      }
      ((!f.presented || f.bounds == null) &&
        this.comments.dismiss(e, t.conversationId, s, f),
        this.tabBudget.enforceDetachedPageBudget(n),
        this.resolveBrowserUseOpenRequests(n));
    }
    updateThreadSessionState(e) {
      e.sessionState;
      let t = this.lifecycleSequence + 1;
      if (
        ((this.lifecycleSequence = t),
        (e.lastLifecycleSequence = t),
        e.presented && (e.lastPresentedSequence = t),
        e.page == null)
      )
        e.sessionState = e.snapshot.isSuspended ? `suspended` : `cold`;
      else if (e.pendingTeardown != null) return;
      else
        e.page.isSuspended
          ? (e.sessionState = `suspended`)
          : e.page.view.webContents == null ||
              e.page.view.webContents.isDestroyed()
            ? (e.sessionState = `cold`)
            : (e.sessionState = e.presented
                ? `live-attached`
                : `live-detached`);
      e.sessionState;
    }
    setThreadAnnotationModeEnabled(e, t, n, r) {
      if (FB(n, r)) {
        let { browserTabId: r, conversationId: i, page: a } = n;
        (this.comments.dismiss(e, i, r, n),
          this.syncThreadState(t, i, n),
          a != null && this.syncRuntimeStateToPage(a, n));
      }
    }
    suspendDetachedThread(e, t) {
      if (
        !(
          t.presented ||
          this.tabBudget.isTabProtectedFromSuspension(e, t) ||
          t.page == null ||
          t.page.isSuspended
        ) &&
        this.suspend(
          e.owner,
          t.conversationId,
          crypto.randomUUID(),
          t.browserTabId,
        ) &&
        t.page.pendingSuspensionId != null
      ) {
        if (this.tabBudget.isTabProtectedFromSuspension(e, t)) {
          ((t.page.pendingSuspensionId = null),
            this.tabBudget.enforceDetachedPageBudget(e));
          return;
        }
        this.requestThreadTeardown(e, t.conversationId, t, `suspended`);
      }
    }
    requestThreadTeardown(e, t, n, r) {
      if (n.pendingTeardown?.reason === r) return;
      (r === `closed` && this.browserPageSnapshots.deleteClosedPageAndFlush(n),
        this.clearPendingWebviewRoutes(e, n));
      let i = n.page;
      if (i == null && r === `suspended`) return;
      let a = crypto.randomUUID();
      ((n.pendingTeardown = {
        mountGeneration: n.mountGeneration,
        reason: r,
        teardownId: a,
      }),
        i == null &&
          this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
          this.browserSessionRegistry.recordPageClosed({
            browserTabId: n.browserTabId,
            conversationId: t,
            windowId: zX(e.window.id),
          }),
        this.windowManager.sendMessageToWebContents(e.owner, {
          type: `browser-sidebar-destroy-webview`,
          browserTabId: n.browserTabId,
          conversationId: t,
          mountGeneration: n.mountGeneration,
          reason: r,
          teardownId: a,
        }));
    }
    finalizeThreadTeardown(e, t, n, r) {
      let i = n.pendingTeardown;
      if (i?.teardownId !== r) return;
      let a = i.reason;
      if (
        a === `suspended` &&
        (n.presented ||
          this.tabBudget.isTabProtectedFromSuspension(e, n) ||
          i.mountGeneration !== n.mountGeneration)
      ) {
        ((n.pendingTeardown = null),
          n.page != null && (n.page.pendingSuspensionId = null),
          this.updateThreadSessionState(n),
          this.syncThreadState(e, t, n),
          this.tabBudget.enforceDetachedPageBudget(e));
        return;
      }
      let o = n.page,
        s = o?.view.webContents ?? null;
      if (
        (s != null && !s.isDestroyed() && s.close(),
        o != null &&
          ((o.pendingSuspensionId = null),
          (o.isRestoring = !1),
          (o.isSuspended = a === `suspended`),
          o.dispose(),
          (o.dispose = () => void 0),
          this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
            (a === `closed`
              ? this.browserSessionRegistry.recordPageClosed({
                  browserTabId: n.browserTabId,
                  conversationId: t,
                  windowId: zX(e.window.id),
                })
              : s != null &&
                this.browserSessionRegistry.recordPageTornDown({
                  browserTabId: n.browserTabId,
                  conversationId: t,
                  guestWebContentsId: s.id,
                  windowId: zX(e.window.id),
                }))),
        (n.pendingTeardown = null),
        a === `closed`)
      ) {
        (this.removeClosedThreadState(e, n),
          this.tabBudget.enforceDetachedPageBudget(e));
        return;
      }
      (this.updateThreadSessionState(n),
        this.syncThreadState(e, t, n),
        this.tabBudget.recordTabSuspended(e, n),
        this.tabBudget.enforceDetachedPageBudget(e));
    }
    findThreadStateForPendingTeardown(e, t) {
      for (let n of e.threads.values())
        if (n.pendingTeardown?.teardownId === t) return n;
      return null;
    }
    syncOwner(e, t, n, r = !1, i, a = !0, o) {
      let s = this.ensureCurrentWindowState(e);
      if (s == null) return;
      this.lastSyncedWindowId = s.window.id;
      let c =
        n ??
        (s.activeConversationId === t ? s.activeBrowserTabId : null) ??
        yL(t);
      ((s.activeConversationId = t),
        this.browserSessionRegistry.setRouteTabMode(
          { conversationId: t, windowId: zX(s.window.id) },
          r ? `multiTab` : `singleTab`,
        ),
        s.ignoredConversationIds.delete(
          OL({ browserTabId: c, conversationId: t }),
        ),
        (s.activeBrowserTabId = c),
        this.browserSessionRegistry.bindSessionRouteOwner({
          browserConversationId: t,
          owner: e,
          sessionId: o ?? t,
        }));
      let l = this.ensureThreadStateForWindow(s, t, c);
      this.tabBudget.markTabSelected(s, l);
      for (let n of s.threads.values())
        n.conversationId === t &&
          ((n.ownerRoutePath = i ?? null),
          this.setThreadAnnotationModeEnabled(e, s, n, a));
      (this.browserSessionRegistry.recordDebugEvent({
        browserTabId: c,
        conversationId: t,
        details: [{ label: `ownerRoutePath`, value: i ?? `missing` }],
        guestWebContentsId: l?.page?.view.webContents?.id ?? null,
        kind: `browser-owner-synced`,
        message: `Synced exact Browser owner`,
        ownerWebContentsId: e.id,
        pageKey: l?.page == null ? null : DL(e.id, t, c),
        windowId: s.window.id,
      }),
        l?.pendingBrowserUsePanelOpen === !0 &&
          ((l.pendingBrowserUsePanelOpen = !1),
          this.openBrowserPanelWithoutAnimation(s, t, c)));
    }
    getActivePageForBrowserUse() {
      return BB({
        getPreferredWindowState: () =>
          this.getPreferredWindowStateForBrowserUse(),
        getWindowStates: () => this.windows.values(),
        getWindowId: (e) => e.window.id,
        getActivePage: (e) => this.getActivePageHandle(e),
      });
    }
    isBrowserSidebarPresentedForConversation(e, t) {
      let n = this.browserSessionRegistry.getBrowserRouteForSession(t),
        r = n?.conversationId ?? t,
        i = this.windows.get(e.id);
      return (
        (n == null || n.windowId === zX(e.id)) &&
        i?.activeConversationId === r &&
        _Q(i, r)?.presented === !0
      );
    }
    getDebugSnapshot() {
      return tZ({
        browserSessionRegistry: this.browserSessionRegistry,
        browserUseOpenRequests: this.browserUseOpenRequests,
        getPersistedPageMetadata:
          this.browserPageSnapshots.getDebugMetadata.bind(
            this.browserPageSnapshots,
          ),
        getThreadStateForWindow: _Q,
        windows: this.windows.values(),
      });
    }
    getFeedbackDiagnostics() {
      return nZ(this.getDebugSnapshot());
    }
    openDebugTabOwner(e) {
      return rZ({
        browserSessionRegistry: this.browserSessionRegistry,
        getLiveWindowState: this.getLiveWindowState.bind(this),
        getThreadStateForWindow: _Q,
        options: e,
        windowManager: this.windowManager,
      });
    }
    async openActivePageForBrowserUse({ startingUrl: e } = {}) {
      return VB({
        startingUrl: e,
        getPreferredWindowState: () =>
          this.getPreferredWindowStateForBrowserUse(),
        getOrCreateActiveVisiblePage: (e) =>
          this.getOrCreatePageForBrowserUse(e, e.activeConversationId, {
            requireVisible: !0,
          }),
        getFocusedOwner: () => this.getFocusedOwnerWebContents(),
        isOwnerDestroyed: (e) => e.isDestroyed(),
        sendOpenBrowserPanelMessage: (e, t) => {
          let n = c.BrowserWindow.fromWebContents(e),
            r =
              n == null || n.isDestroyed()
                ? null
                : (this.windows.get(n.id) ?? null);
          this.windowManager.sendMessageToWebContents(e, {
            type: `open-browser-tab`,
            conversationId: r?.activeConversationId ?? void 0,
            initialUrl: t.startingUrl,
            source: `browser_use`,
            initiator: `browser_use`,
          });
        },
        getWindowStateForOwner: (e) => {
          let t = c.BrowserWindow.fromWebContents(e);
          return t == null || t.isDestroyed()
            ? null
            : (this.windows.get(t.id) ?? null);
        },
        waitForOpenRequest: (e) =>
          this.browserUseOpenRequests.waitForOpen(e, null),
      });
    }
    getPageForBrowserUse(e) {
      let t = yL(e);
      return BB({
        getPreferredWindowState: () =>
          this.getPreferredWindowStateForConversation(e),
        getWindowStates: () => this.windows.values(),
        getWindowId: (e) => e.window.id,
        getActivePage: (n) =>
          n.owner.isDestroyed()
            ? null
            : this.getPageHandleForConversation(n, e, t),
      });
    }
    async openPageForBrowserUse({ conversationId: e, startingUrl: t }) {
      let n = this.getPreferredWindowStateForConversation(e);
      if (n == null) {
        let t = this.getFocusedOwnerWebContents();
        if (t != null && !t.isDestroyed()) {
          let r = c.BrowserWindow.fromWebContents(t);
          r != null &&
            !r.isDestroyed() &&
            ((n = this.ensureWindowState(r, t)),
            (this.lastSyncedWindowId = r.id),
            this.ensureThreadStateForCommand(t, e, yL(e)));
        }
      }
      if (n == null)
        throw Error(
          `No ChatGPT browser pane available for browser session ${e}`,
        );
      let r = yL(e),
        i = _Q(n, e, r) ?? this.ensureThreadStateForCommand(n.owner, e, r);
      i != null &&
        ((i.isBrowserUsePage = !0), (i.browserUseTabOrigin ??= `agent`));
      let a =
        i?.page ??
        this.ensureThreadPageForCommand(n.owner, e, r, {
          title: i?.snapshot.title ?? `New tab`,
          url: i?.snapshot.url ?? ``,
        });
      n.activeConversationId === e && n.activeBrowserTabId === r
        ? i?.visible !== !0 || i?.bounds == null
          ? (i != null && (i.pendingBrowserUsePanelOpen = !1),
            this.windowManager.sendMessageToWebContents(n.owner, {
              type: `toggle-browser-panel`,
              open: !0,
              browserTabId: r,
              url: t,
              source: `browser_use`,
              initiator: `browser_use`,
            }))
          : i != null &&
            a != null &&
            a.view.webContents == null &&
            (t != null &&
              a.pendingUrl.length === 0 &&
              a.url.length === 0 &&
              this.navigatePageToUrl(a, t, `browser_use`, `browser_use`),
            this.syncThreadState(n, e, i))
        : i != null && this.openBrowserPanelWithoutAnimation(n, e, r);
      let o = a == null ? null : KL(n, e, a);
      if (o == null) {
        if (n.activeConversationId === e) {
          let t = { conversationId: e, windowId: zX(n.window.id) };
          return this.browserUseOpenRequests.waitForOpen(n.owner, e, r, (e) => {
            this.closePageForBrowserUseForRoute(t, r, e);
          });
        }
        throw Error(
          `No ChatGPT browser pane available for browser session ${e}`,
        );
      }
      return o;
    }
    getBrowserUseRouteActionHelpers() {
      return {
        browserSessionRegistry: this.browserSessionRegistry,
        browserUseOpenRequests: this.browserUseOpenRequests,
        comments: this.comments,
        createThreadPageState: (e, t, n) =>
          this.materializeThreadPageState(e, t, { kind: `fresh`, page: n }),
        defaultBrowserUseViewportSize: this.defaultBrowserUseViewportSize,
        ensureThreadStateForWindow: this.ensureThreadStateForWindow.bind(this),
        enforceDetachedPageBudget:
          this.tabBudget.enforceDetachedPageBudget.bind(this.tabBudget),
        getLiveWindowState: this.getLiveWindowState.bind(this),
        getPageHandleForConversation:
          this.getPageHandleForConversation.bind(this),
        getThreadStateForWindow: _Q,
        navigatePageToUrl: this.navigatePageToUrl.bind(this),
        queuePageDeviceMetricsSync: this.queuePageDeviceMetricsSync.bind(this),
        requestThreadTeardown: this.requestThreadTeardown.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        updateThreadSessionState: this.updateThreadSessionState.bind(this),
        windowManager: this.windowManager,
      };
    }
    getPageForBrowserUseForRoute(e, t) {
      return lW(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    getPagesForBrowserUseForRoute(e) {
      return uW(this.getBrowserUseRouteActionHelpers(), e);
    }
    getTabsForBrowserUseForRoute(e) {
      return fW(this.getBrowserUseRouteActionHelpers(), e);
    }
    getUserTabsForBrowserUseForRoute(e) {
      return dW(this.getBrowserUseRouteActionHelpers(), e);
    }
    async openPageForBrowserUseForRoute({
      browserTabId: e,
      conversationId: t,
      origin: n,
      startingUrl: r,
      windowId: i,
    }) {
      return mW(this.getBrowserUseRouteActionHelpers(), {
        browserTabId: e,
        conversationId: t,
        origin: n,
        startingUrl: r,
        windowId: i,
      });
    }
    closePageForBrowserUseForRoute(e, t, n) {
      hW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    releasePageForBrowserUseForRoute(e, t) {
      gW(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    disposeBrowserUseRouteForRoute(e) {
      _W(this.getBrowserUseRouteActionHelpers(), e);
    }
    setBrowserVisibleForBrowserUseForRoute(e, t, n) {
      vW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    isBrowserVisibleForBrowserUseForRoute(e, t) {
      return yW(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    setViewportForBrowserUseForRoute(e, t, n) {
      bW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setCaptureSurfaceForBrowserUseForRoute(e, t, n) {
      xW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    async waitForPendingDebuggerSyncForRoute(e, t) {
      let n = this.getLiveWindowState(e.windowId);
      if (n == null) return;
      let r =
        (_Q(n, e.conversationId, t ?? yL(e.conversationId)) ?? null)?.page ??
        null;
      r != null && (await r.debuggerSync.catch(() => void 0));
    }
    async waitForPageReadyForRoute(e, t) {
      let n = this.getLiveWindowState(e.windowId);
      await (
        n == null
          ? null
          : (_Q(n, e.conversationId, t ?? yL(e.conversationId))?.page ?? null)
      )?.initialNavigationSync;
    }
    openBrowserPanelWithoutAnimation(e, t, n) {
      SW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setBrowserUseActiveForRoute(e, t, n) {
      let r = this.getLiveWindowState(e.windowId),
        i = new Set(
          r == null
            ? []
            : Array.from(r.threads.values()).filter(
                (t) =>
                  t.conversationId === e.conversationId && t.isBrowserUseActive,
              ),
        );
      if ((CW(this.getBrowserUseRouteActionHelpers(), e, t, n), r == null))
        return;
      let a = !1;
      for (let t of r.threads.values())
        t.conversationId === e.conversationId &&
          i.has(t) !== t.isBrowserUseActive &&
          (WZ(r, t),
          (a = !0),
          t.isBrowserUseActive && this.tabBudget.markTabActivity(t));
      a && this.tabBudget.enforceDetachedPageBudget(r);
    }
    setBrowserUseCursorForRoute(e, t, n) {
      return wW(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setBrowserUseActiveForConversation(e, t) {
      let n = this.getPreferredWindowStateForConversation(e);
      n != null &&
        this.setBrowserUseActiveForRoute(
          { conversationId: e, windowId: zX(n.window.id) },
          t,
        );
    }
    activateBrowserUseNavigationRestriction(e) {
      this.browserUseNavigationRestriction.activate(e);
    }
    addBrowserUseNavigationBlockedListener(e) {
      return (
        this.browserUseNavigationBlockedListeners.add(e),
        () => {
          this.browserUseNavigationBlockedListeners.delete(e);
        }
      );
    }
    assertBrowserUseNavigationAllowed(e, t) {
      this.browserUseNavigationRestriction.assertAllowed(e, t);
    }
    setBrowserTabPersistenceEnabled(e, t) {
      return this.browserPersistence.setEnabled(this.getCurrentWindow(e), t);
    }
    getRestorableBrowserPageSnapshots(e, t) {
      return this.browserPersistence.getRestorablePageSnapshots(
        this.getCurrentWindow(e),
        t,
      );
    }
    deletePersistedBrowserConversation(e, t) {
      return this.browserPersistence.deleteConversation(
        this.getCurrentWindow(e),
        t,
      );
    }
    isBrowserTabPersistenceOwner(e) {
      return this.browserPersistence.isPersistenceOwner(
        this.getCurrentWindow(e),
      );
    }
    setTweaksFeatureEnabled(e) {
      if (this.isTweaksFeatureEnabled !== e) {
        this.isTweaksFeatureEnabled = e;
        for (let t of this.windows.values())
          for (let [r, i] of t.threads) {
            if (!e) {
              let e = n.kc(i.snapshot.comments, !1),
                a = {
                  ...i.snapshot,
                  annotationEditorMode: `comment`,
                  isDesignModifierPressed: !1,
                  isOriginalViewEnabled: !1,
                  isTweaksEditorOpen: !1,
                  comments: e,
                };
              (i.snapshot.annotationEditorMode !== a.annotationEditorMode ||
                i.snapshot.isDesignModifierPressed !==
                  a.isDesignModifierPressed ||
                i.snapshot.isOriginalViewEnabled !== a.isOriginalViewEnabled ||
                i.snapshot.isTweaksEditorOpen !== a.isTweaksEditorOpen ||
                i.snapshot.comments.some((e) => e.designChange != null)) &&
                ((i.snapshot = a), this.syncThreadState(t, r, i));
            }
            i.page != null && this.syncRuntimeStateToPage(i.page, i);
          }
      }
    }
    notifyBrowserUseNavigationBlocked(e) {
      for (let t of this.browserUseNavigationBlockedListeners) t(e);
    }
    handleCommentOverlayMounted(e, t, n, r, i, a) {
      this.comments.handleOverlayMounted(e, t, n, r, i, a);
    }
    handleCommentOverlayPreviewOpenChanged(e, t) {
      this.comments.handleOverlayPreviewOpenChanged(e, t);
    }
    handleRuntimeCommentScreenshotReady(e, t) {
      this.comments.handleRuntimeCommentScreenshotReady(e, t);
    }
    goBack(e, t, n) {
      this.navigateCurrentPageHistory(e, t, `back`, n);
    }
    goForward(e, t, n) {
      this.navigateCurrentPageHistory(e, t, `forward`, n);
    }
    getActiveVisiblePageForWindow(e) {
      if (e.isDestroyed()) return null;
      let t = this.windows.get(e.id) ?? null,
        n = t?.activeConversationId ?? null,
        r = t?.activeBrowserTabId ?? null;
      if (t == null || n == null || r == null) return null;
      let i = _Q(t, n, r),
        a = i?.page ?? null;
      return i == null ||
        !i.visible ||
        i.bounds == null ||
        a?.view.webContents == null
        ? null
        : { conversationId: n, page: a, threadState: i, windowState: t };
    }
    getVisiblePageForWebContents(e, t) {
      let n = this.findPageStateForWebContentsId(t.id);
      return n == null ||
        n.windowState.window !== e ||
        !n.threadState.visible ||
        n.threadState.bounds == null
        ? null
        : n;
    }
    getReloadTargetForWindow(e, t) {
      return (
        (t == null ? null : this.getVisiblePageForWebContents(e, t)) ??
        this.getActiveVisiblePageForWindow(e)
      );
    }
    canReloadActiveVisiblePage(e, t = null) {
      return this.getReloadTargetForWindow(e, t) != null;
    }
    getFocusedVisibleBrowserTabPanel(e, t) {
      if (t == null) return null;
      let n = this.getVisiblePageForWebContents(e, t);
      return n == null
        ? null
        : tL(
            this.windowManager.getAppShellShortcutState(n.windowState.owner.id),
            n.conversationId,
            n.threadState.browserTabId,
            nL(n.threadState.hostKind),
          );
    }
    runFocusedVisiblePageCommand(e, t, n) {
      let r = t == null ? null : this.getVisiblePageForWebContents(e, t),
        i = r == null ? null : nL(r.threadState.hostKind),
        a =
          r == null
            ? null
            : this.windowManager.getAppShellShortcutState(
                r.windowState.owner.id,
              )?.focusArea,
        o = r;
      if (
        (a != null &&
          !(
            (i === `bottom` && a === `bottom-panel`) ||
            (i === `right` && a === `right-panel`)
          ) &&
          (o = null),
        o == null)
      )
        return !1;
      let {
        conversationId: s,
        threadState: { browserTabId: c, hostKind: l },
        windowState: { owner: u },
      } = o;
      switch (n.type) {
        case `find-next`:
          this.findNext(u, s, c);
          break;
        case `find-previous`:
          this.findPrevious(u, s, c);
          break;
        case `focus-address-bar`:
          (u.focus(),
            this.windowManager.sendMessageToWebContents(u, {
              type: `run-command`,
              id: `focusBrowserAddressBar`,
              originPanel:
                tL(
                  this.windowManager.getAppShellShortcutState(u.id),
                  s,
                  c,
                  nL(l),
                ) ?? void 0,
            }));
          break;
        case `go-back`:
          this.goBack(u, s, c);
          break;
        case `go-forward`:
          this.goForward(u, s, c);
          break;
        case `open-find`:
          (u.focus(), this.openFind(u, s, c));
          break;
        case `reset-zoom`:
          this.resetZoom(u, s, c);
          break;
        case `step-zoom`:
          this.stepZoom(u, s, n.delta, c);
          break;
      }
      return !0;
    }
    closeFocusedVisibleBrowserTab(e, t) {
      if (t == null) return !1;
      let n = this.getVisiblePageForWebContents(e, t);
      if (n == null) return !1;
      let r = eL(
        this.windowManager.getAppShellShortcutState(n.windowState.owner.id),
        n.conversationId,
        n.threadState.browserTabId,
        nL(n.threadState.hostKind),
      );
      return r == null
        ? !1
        : (this.windowManager.sendMessageToWebContents(n.windowState.owner, r),
          !0);
    }
    reloadActiveVisiblePage(e, t = null) {
      this.reloadActiveVisiblePageWithOptions(e, {}, t);
    }
    reloadActiveVisiblePageWithOptions(e, t = {}, n = null) {
      let r = this.getReloadTargetForWindow(e, n);
      r != null &&
        this.reload(
          r.windowState.owner,
          r.conversationId,
          t,
          r.threadState.browserTabId,
        );
    }
    reloadOpenPages() {
      for (let { owner: e, threads: t } of this.windows.values())
        for (let { browserTabId: n, conversationId: r, page: i } of t.values())
          i?.view.webContents != null && this.reload(e, r, void 0, n);
    }
    async clearBrowsingData(e) {
      await this.browserSessionService.clearBrowsingData(e);
    }
    scroll(e, t, r, i) {
      let a = this.getPageState(e, t, i)?.view.webContents ?? null;
      a != null &&
        AQ(a, r).catch((e) => {
          (MQ().warning(`failed to scroll browser sidebar page`, {
            safe: { conversationId: t },
            sensitive: { error: e },
          }),
            this.errorReporter.reportNonFatal(
              e instanceof Error ? e : n.Rs(e),
              { kind: `browser-sidebar-scroll` },
            ));
        });
    }
    openFind(e, t, n) {
      let r = this.ensureThreadCommandContext(e, t, n);
      r != null &&
        GU(this.windowManager, r.windowState.owner, t, r.threadState);
    }
    openSiteInfo(e, t, n, r, i) {
      let a = this.getPageState(e, t, n)?.view.webContents;
      return lP({ anchorBounds: r, origin: e, siteUrl: i, webContents: a });
    }
    setFindQuery(e, t, n, r) {
      let i = this.ensureThreadCommandContext(e, t, r);
      i != null &&
        KU(this.windowManager, i.windowState.owner, t, i.threadState, n);
    }
    findNext(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && qU(r);
    }
    findPrevious(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && JU(r);
    }
    closeFind(e, t, n) {
      let r = this.getCurrentWindowState(e);
      if (r == null) return;
      let i = _Q(r, t, n);
      i != null && ZU(this.windowManager, r.owner, t, i);
    }
    reload(e, t, { ignoreCache: n = !1 } = {}, r) {
      let i = this.getCurrentPageCommandContext(e, t, r);
      if (i == null) return;
      let { page: a, threadState: o, windowState: s } = i;
      if ((ZU(this.windowManager, s.owner, t, o), a.loadError != null)) {
        this.navigatePageToUrl(a, a.loadError.failedUrl);
        return;
      }
      if (a.url.length === 0 && a.pendingUrl.length > 0) {
        if (
          (this.navigatePageToUrl(a, a.pendingUrl), a.view.webContents == null)
        ) {
          let n = this.getThreadState(e, t, r);
          n != null && this.syncThreadStateFromOrigin(e, t, n);
        }
        return;
      }
      if ((UL(a), n)) {
        a.view.webContents?.reloadIgnoringCache();
        return;
      }
      a.view.webContents?.reload();
    }
    reset(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      if (r == null) return;
      let i = this.ensureCurrentWindowState(e);
      if (i == null) return;
      (this.comments.close(e, t, r.browserTabId),
        (r.pendingBrowserUsePanelOpen = !1),
        (r.pendingCommentModeActivation = null),
        (r.commentModeBlockCheckUrl = null),
        (r.commentModeBlockCheckRequestId += 1),
        this.browserPageSnapshots.deleteAndFlush(r),
        ZU(this.windowManager, i.owner, t, r));
      let a = this.isBrowserUseManagedBrowserTab(t, r.browserTabId);
      (r.page?.dispose(),
        this.clearPendingWebviewRoutes(i, r),
        (r.page = null),
        (r.isBrowserUsePage = !1),
        (r.browserUseTabOrigin = null),
        (r.isBrowserUseNavigationRestrictionActive = !1),
        (r.snapshot = TZ()),
        a &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: zX(i.window.id) },
            !1,
            r.browserTabId,
          ),
        this.syncThreadState(i, t, r));
    }
    stop(e, t, n) {
      let r = this.getPageState(e, t, n);
      r && r.view.webContents?.stop();
    }
    print(e, t, n) {
      let r = this.getCurrentPageCommandContext(e, t, n);
      if (r == null) return;
      let i = r.page.view.webContents;
      i == null || i.isDestroyed() || i.print();
    }
    suspend(e, t, n, r) {
      let i = this.getCurrentPageCommandContext(e, t, r);
      if (i == null) return !1;
      let { page: a, threadState: o } = i,
        s = a.view.webContents;
      return s == null || s.isDestroyed() || o.isBrowserUseActive
        ? !1
        : (this.browserPageSnapshots.persistCurrentAndFlush(a),
          (a.pendingSuspensionId = n),
          !0);
    }
    browserSidebarWebviewDestroyed(e, t, n, r) {
      let i = this.getCurrentWindowState(e);
      if (i == null) return;
      let a = _Q(i, t, n) ?? this.findThreadStateForPendingTeardown(i, r);
      a != null && this.finalizeThreadTeardown(i, a.conversationId, a, r);
    }
    registerWebviewHostSession(e, t) {
      let n = this.ensureCurrentWindowState(e);
      if (n == null) return !1;
      if (n.rendererInstanceId === t) return !0;
      n.rendererInstanceId = t;
      for (let e of n.threads.values()) e.hostGeneration = null;
      return !0;
    }
    registerWebviewHost(e, t, n, r, i, a) {
      let o = this.ensureCurrentWindowState(e);
      if (o == null || o.rendererInstanceId !== a) return !1;
      let s = this.browserPersistence.getPagePersistence(o.window, i),
        c = _Q(o, n, t) ?? this.ensureThreadStateForWindow(o, n, t);
      return (c.hostGeneration != null && c.hostGeneration > r) ||
        !this.browserPersistence.reconcileBrowserStorageId(o, c, s) ||
        this.browserPersistence.preparePage(o, c, s).kind === `missing`
        ? !1
        : ((c.hostGeneration = r),
          this.browserPersistence.registerWebviewHost(o, c, s),
          !0);
    }
    attachWebview(e, t, n) {
      let { hostGeneration: r, rendererInstanceId: i, route: a } = n,
        o = e.threads.get(OL(a)) ?? null;
      if (o?.page == null) {
        (ZX(this.browserSessionRegistry, e, a, t.id),
          t.isDestroyed() || t.close());
        return;
      }
      if (e.rendererInstanceId !== i || o.hostGeneration !== r) {
        (this.browserSessionRegistry.recordDebugEvent({
          browserTabId: a.browserTabId,
          conversationId: a.conversationId,
          details: [
            { label: `hostGeneration`, value: String(r) },
            { label: `threadHostGeneration`, value: String(o.hostGeneration) },
          ],
          guestWebContentsId: t.id,
          kind: `browser-runtime-attach-stale`,
          message: `Rejected stale Browser sidebar attach`,
          ownerWebContentsId: e.owner.id,
          pageKey: null,
          windowId: e.window.id,
        }),
          MQ().warning(`IAB_LIFECYCLE rejected stale browser sidebar attach`, {
            safe: {
              browserTabId: a.browserTabId,
              conversationId: a.conversationId,
              guestWebContentsId: t.id,
              hostGeneration: r,
              ownerWebContentsId: e.owner.id,
              threadHostGeneration: o.hostGeneration,
              windowId: e.window.id,
            },
            sensitive: {},
          }),
          t.isDestroyed() || t.close());
        return;
      }
      let s = this.findPageStateForWebContentsId(t.id);
      if (
        s != null &&
        (s.conversationId !== a.conversationId ||
          s.threadState.browserTabId !== a.browserTabId)
      ) {
        (this.browserSessionRegistry.recordDebugEvent({
          browserTabId: a.browserTabId,
          conversationId: a.conversationId,
          details: [
            {
              label: `previousBrowserTabId`,
              value: s.threadState.browserTabId,
            },
            { label: `previousConversationId`, value: s.conversationId },
          ],
          guestWebContentsId: t.id,
          kind: `browser-runtime-attach-route-rebind-rejected`,
          message: `Rejected Browser sidebar route rebind`,
          ownerWebContentsId: e.owner.id,
          pageKey: null,
          windowId: e.window.id,
        }),
          MQ().warning(`IAB_LIFECYCLE rejected browser sidebar route rebind`, {
            safe: {
              browserTabId: a.browserTabId,
              conversationId: a.conversationId,
              guestWebContentsId: t.id,
              ownerWebContentsId: e.owner.id,
              previousBrowserTabId: s.threadState.browserTabId,
              previousConversationId: s.conversationId,
              windowId: e.window.id,
            },
            sensitive: {},
          }),
          t.isDestroyed() || t.close());
        return;
      }
      (this.browserSessionRegistry.recordDebugEvent({
        browserTabId: a.browserTabId,
        conversationId: a.conversationId,
        guestWebContentsId: t.id,
        kind: `browser-runtime-attached`,
        message: `Runtime attached Browser sidebar webview`,
        ownerWebContentsId: e.owner.id,
        pageKey: DL(e.owner.id, a.conversationId, a.browserTabId),
        windowId: e.window.id,
      }),
        MQ().info(`IAB_LIFECYCLE runtime attached browser sidebar webview`, {
          safe: {
            browserTabId: a.browserTabId,
            conversationId: a.conversationId,
            guestWebContentsId: t.id,
            ownerWebContentsId: e.owner.id,
            windowId: e.window.id,
          },
          sensitive: {},
        }),
        this.attachPageWebContents(e, o.page, t, o.themeVariant),
        n.adoptedInitialUrl != null &&
          this.navigateBlankAdoptedWindowOpenChildAfterAttach(
            o.page,
            t,
            n.adoptedInitialUrl,
          ));
    }
    setZoomPercent(e, t, r, i) {
      let a = this.ensureThreadStateForCommand(e, t, i);
      a != null && this.setThreadZoomPercent(e, t, a, n.Ho(r));
    }
    stepZoom(e, t, r, i) {
      let a = this.ensureThreadStateForCommand(e, t, i);
      a != null &&
        this.setThreadZoomPercent(e, t, a, n.Wo(a.snapshot.zoomPercent, r));
    }
    resetZoom(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && this.setThreadZoomPercent(e, t, r, 100);
    }
    navigate(e, t, n, r, i, a, o) {
      let s = this.ensureCurrentWindowState(e);
      if (s == null) return;
      let c = this.browserPersistence.getPagePersistence(s.window, o),
        l = this.ensureThreadCommandContext(e, t, a);
      if (l == null) return;
      let { threadState: u, windowState: d } = l;
      if (!this.browserPersistence.reconcileBrowserStorageId(d, u, c)) return;
      r === `browser_use` &&
        ((u.isBrowserUsePage = !0), (u.browserUseTabOrigin ??= `agent`));
      let f =
        u.page ??
        this.ensureThreadPageForCommand(
          e,
          t,
          a,
          { title: u.snapshot.title, url: u.snapshot.url },
          c,
        );
      f &&
        (ZU(this.windowManager, d.owner, t, u),
        lH(NL(f), n) || this.comments.dismiss(e, t, u.browserTabId, u),
        this.navigatePageToUrl(f, n, r, i),
        f.view.webContents ?? this.syncThreadStateFromOrigin(e, t, u));
    }
    async captureScreenshotToClipboard(e, t, n) {
      this.getThreadState(e, t, n)?.isAnnotationModeEnabled !== !1 &&
        (await kQ({
          browserTabId: n,
          conversationId: t,
          errorReporter: this.errorReporter,
          origin: e,
          pageState: this.getPageState(e, t, n),
          windowManager: this.windowManager,
        }));
    }
    refreshCursor(e, t, n) {
      let r = this.getCurrentPageCommandContext(e, t, n);
      r != null &&
        GL(r.windowState.window, r.threadState.bounds, r.page.view.webContents);
    }
    setThreadZoomPercent(e, t, n, r) {
      let i = n.snapshot.zoomPercent !== r;
      if (
        (i && (n.snapshot = { ...n.snapshot, zoomPercent: r }),
        n.page != null &&
          (this.syncPageZoomPercent(n.page, r),
          this.syncRuntimeStateToPage(n.page, n)),
        !i)
      )
        return;
      let a = c.BrowserWindow.fromWebContents(e);
      if (a == null || a.isDestroyed()) return;
      let o = this.ensureWindowState(a, e);
      this.syncThreadState(o, t, n);
    }
    syncPageZoomPercent(e, t) {
      let r = e.view.webContents;
      r == null || r.isDestroyed() || r.setZoomFactor(n.Uo(t));
    }
    queuePageDeviceMetricsSync(e, t) {
      e.deviceMetricsSyncVersion += 1;
      let n = e.deviceMetricsSyncVersion;
      this.queuePageDebuggerSync(e, async () => {
        n === e.deviceMetricsSyncVersion &&
          (await this.syncPageDeviceMetrics(e, t));
      });
    }
    queuePageDebuggerSync(e, t) {
      return (
        (e.debuggerSync = e.debuggerSync.catch(() => void 0).then(t)),
        e.debuggerSync
      );
    }
    queuePageColorSchemeSync(e, t, n) {
      return this.queuePageDebuggerSync(e, async () => {
        await EL(t, n);
      });
    }
    async syncPageDeviceMetrics(e, t) {
      let n = e.view.webContents;
      if (n == null || n.isDestroyed()) return;
      let r = n.debugger,
        i = !1;
      try {
        if ((r.isAttached() || (r.attach(`1.3`), (i = !0)), t == null)) {
          await r.sendCommand(`Emulation.clearDeviceMetricsOverride`);
          return;
        }
        await r.sendCommand(`Emulation.setDeviceMetricsOverride`, {
          deviceScaleFactor: 1,
          height: Math.round(t.height),
          mobile: !1,
          width: Math.round(t.width),
        });
      } catch (t) {
        MQ().warning(`failed to sync browser sidebar device metrics`, {
          safe: { conversationId: e.conversationId },
          sensitive: { error: t },
        });
      } finally {
        i && !n.isDestroyed() && r.isAttached() && r.detach();
      }
    }
    setInteractionMode(e, t, r, i, a) {
      let o = this.ensureThreadCommandContext(e, t, i);
      if (o == null) return;
      let { threadState: s, windowState: c } = o;
      if (!(r === `comment` && !s.isAnnotationModeEnabled)) {
        if (r !== `comment`) {
          if (
            s.snapshot.interactionMode === r &&
            s.pendingCommentModeActivation == null
          )
            return;
          ((s.pendingCommentModeActivation = null),
            (s.snapshot = PB(s.snapshot, r)),
            this.comments.dismiss(e, t, s.browserTabId, s),
            this.syncThreadState(c, t, s),
            s.page != null && this.syncRuntimeStateToPage(s.page, s),
            this.comments.prepare(e, t, s.browserTabId, s));
          return;
        }
        this.requestCommentMode(
          c,
          t,
          s,
          n.vc.BATCH,
          a ?? null,
          a === `keyboard-shortcut` ? { kind: `text` } : void 0,
        );
      }
    }
    startCommentAtPoint(e, t, r, i, a, o) {
      let s = this.getThreadState(e, t.conversationId, t.browserTabId);
      if (s == null) return;
      let c = this.ensureCurrentWindowState(e);
      if (c == null) return;
      let l =
        n.Dc(i).persistent &&
        this.isTweaksFeatureEnabled &&
        s.snapshot.annotationEditorMode === `design` &&
        n.jc(s.snapshot.comments);
      this.requestCommentMode(
        c,
        t.conversationId,
        s,
        i,
        a,
        o
          ? { kind: `text`, fallbackViewportPoint: r }
          : { kind: l ? `design` : `comment`, viewportPoint: r },
      );
    }
    startDesignEditAtPoint(e, t, r) {
      if (!this.isTweaksFeatureEnabled) return;
      let i = this.getThreadState(e, t.conversationId, t.browserTabId);
      if (i == null) return;
      let a = this.ensureCurrentWindowState(e);
      a != null &&
        this.requestCommentMode(a, t.conversationId, i, n.vc.BATCH, null, {
          kind: `design`,
          viewportPoint: r,
        });
    }
    requestCommentMode(e, t, r, i, a, o) {
      if (!r.isAnnotationModeEnabled) return;
      let s = WL(r);
      if (s.length === 0) return;
      let c = n.Dc(i),
        l = this.commentModeBlocklistLookup.cachedCommentModeBlocked(s);
      if (l === !0) {
        this.refreshCommentModeBlockStatus(e, r);
        return;
      }
      if (l === !1) {
        ((r.pendingCommentModeActivation = null),
          (r.snapshot.interactionMode !== `comment` ||
            r.snapshot.annotationFlow !== i) &&
            ((r.snapshot = {
              ...PB(r.snapshot, `comment`),
              annotationFlow: i,
              annotationModeEntrySource: a ?? null,
              isTweaksEditorOpen:
                c.persistent && NB(r.snapshot, this.isTweaksFeatureEnabled),
            }),
            this.syncThreadState(e, t, r),
            r.page != null && this.syncRuntimeStateToPage(r.page, r),
            this.comments.prepare(e.owner, t, r.browserTabId, r)),
          o != null &&
            r.page != null &&
            this.sendPendingCommentModeRequestToRuntime(r.page, o));
        return;
      }
      ((r.pendingCommentModeActivation = {
        annotationFlow: i,
        annotationModeEntrySource: a ?? null,
        url: s,
        navigationVersion: r.page?.navigationVersion ?? null,
        request: o ?? null,
      }),
        this.refreshCommentModeBlockStatus(e, r));
    }
    clearComments(e, t, n) {
      this.clearAnnotations(e, t, n, !0);
    }
    discardPendingAnnotations(e, t, n) {
      this.clearAnnotations(e, t, n, !1);
    }
    clearAnnotations(e, t, n, r) {
      let i = this.ensureThreadStateForCommand(e, t, n);
      i != null &&
        ((i.snapshot = PB(i.snapshot, r ? `browse` : void 0, [])),
        r && this.comments.dismiss(e, t, i.browserTabId, i),
        this.syncThreadStateFromOrigin(e, t, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i));
    }
    addAnnotationsToComposer(e, t, n) {
      let r = this.ensureThreadCommandContext(e, t, n);
      if (r == null) return;
      let { threadState: i, windowState: a } = r;
      ((i.pendingCommentModeActivation = null),
        (i.snapshot = PB(i.snapshot, `browse`)),
        this.comments.dismiss(e, t, i.browserTabId, i),
        this.syncThreadState(a, t, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i),
        this.comments.prepare(e, t, i.browserTabId, i));
    }
    closePage(e, t, n, r) {
      let i = this.getCurrentWindowState(e);
      if (i == null) return;
      let a = _Q(i, t, n);
      if (a == null || (r != null && a.browserStorageId !== r)) {
        n != null &&
          r != null &&
          !this.browserPersistence.hasBrowserStorageOwner(r) &&
          this.browserPageSnapshots.deleteClosedPageAndFlush({
            browserStorageId: r,
            browserTabId: n,
            conversationId: t,
          });
        return;
      }
      (ZU(this.windowManager, i.owner, t, a),
        i.activeConversationId === t &&
          i.activeBrowserTabId === a.browserTabId &&
          (i.activeBrowserTabId = null),
        a.isBrowserUseActive &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: zX(i.window.id) },
            !1,
            a.browserTabId,
          ),
        this.requestThreadTeardown(i, t, a, `closed`));
    }
    transferConversation(e, t, n, r, i) {
      let a = this.ensureCurrentWindowState(e);
      a != null && this.transferConversationState(a, t, r, n, i);
    }
    transferConversationState(e, t, n, r, i) {
      KX({
        browserSessionRegistry: this.browserSessionRegistry,
        comments: this.comments,
        downloadStore: this.downloadStore,
        disposeThreadState: this.disposeThreadState.bind(this),
        getBrowserUseRouteBrowserTabIds:
          this.getBrowserUseRouteBrowserTabIds.bind(this),
        isBrowserUseManagedBrowserTab:
          this.isBrowserUseManagedBrowserTab.bind(this),
        getThreadStateForWindow: _Q,
        setBrowserUseActiveForRoute:
          this.setBrowserUseActiveForRoute.bind(this),
        sourceBrowserTabId: n,
        sourceConversationId: t,
        syncRuntimeStateToPage: this.syncRuntimeStateToPage.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        targetBrowserTabId: i,
        targetConversationId: r,
        windowState: e,
      });
      let a = _Q(e, r, i);
      a != null &&
        (this.browserPageSnapshots.reassociateAndFlush(a),
        e.activeConversationId === r &&
          e.activeBrowserTabId === a.browserTabId &&
          this.tabBudget.markTabSelected(e, a));
    }
    selectComment(e, t, n, r) {
      let i = this.getThreadState(e, t, r),
        a = this.getPageState(e, t, r),
        o = a?.view.webContents ?? null;
      i == null ||
        a == null ||
        o == null ||
        o.isDestroyed() ||
        (this.syncRuntimeStateToPage(a, i),
        o.send(B, {
          type: `browser-sidebar-runtime-select-comment`,
          commentId: n,
        }));
    }
    async handleRuntimeOpenEditor(e, t) {
      await this.comments.handleRuntimeOpenEditor(e, t);
    }
    async handleRuntimeOpenDesignEditor(e, t) {
      this.isTweaksFeatureEnabled &&
        (await this.comments.handleRuntimeOpenDesignEditor(e, t));
    }
    async handleRuntimeOpenCommentPreview(e, t) {
      await this.comments.handleRuntimeOpenCommentPreview(e, t);
    }
    handleRuntimeCloseCommentPreview(e, t) {
      this.comments.handleRuntimeCloseCommentPreview(e, t);
    }
    handleRuntimeUpdateAnchor(e, t) {
      this.comments.handleRuntimeUpdateAnchor(e, t);
    }
    handleRuntimeFocusEditor(e) {
      this.comments.handleRuntimeFocusEditor(e);
    }
    handleRuntimeExitCommentMode(e) {
      let t = this.findPageStateForRuntimeSender(e);
      !t ||
        t.owner.isDestroyed() ||
        this.addAnnotationsToComposer(
          t.owner,
          t.page.conversationId,
          t.page.browserTabId,
        );
    }
    handleRuntimeDesignModifierState(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      !n ||
        n.owner.isDestroyed() ||
        this.setDesignModifierPressed(
          n.owner,
          n.page.conversationId,
          t.pressed,
        );
    }
    setDesignModifierPressed(e, t, n) {
      let r = this.getThreadState(e, t),
        i = this.getCurrentWindowState(e);
      if (r == null || i == null) return;
      let a =
        this.isTweaksFeatureEnabled &&
        r.snapshot.interactionMode === `comment` &&
        n;
      (r.snapshot.isDesignModifierPressed ?? !1) !== a &&
        ((r.snapshot = { ...r.snapshot, isDesignModifierPressed: a }),
        this.syncThreadState(i, t, r),
        r.page != null && this.syncRuntimeStateToPage(r.page, r));
    }
    setAnnotationAddModifierPressed(e, t, n) {
      let r = this.getThreadState(e, t),
        i = this.getCurrentWindowState(e);
      if (r == null || i == null) return;
      let a = r.snapshot.interactionMode === `comment` && n;
      (r.snapshot.isAnnotationAddModifierPressed ?? !1) !== a &&
        ((r.snapshot = { ...r.snapshot, isAnnotationAddModifierPressed: a }),
        this.syncThreadState(i, t, r));
    }
    setOriginalViewEnabled(e, t, r) {
      let i = this.getThreadState(e, t),
        a = this.getCurrentWindowState(e);
      if (i == null || a == null) return;
      let o =
        i.snapshot.interactionMode === `comment` &&
        n.Mc(i.snapshot.comments) &&
        r;
      (i.snapshot.isOriginalViewEnabled ?? !1) !== o &&
        ((i.snapshot = { ...i.snapshot, isOriginalViewEnabled: o }),
        this.syncThreadState(a, t, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i));
    }
    handleRuntimeImageDragStarted(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      n == null ||
        n.owner.isDestroyed() ||
        (this.activeDraggedImagesByOwnerWebContentsId.set(n.owner.id, {
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          sourceUrl: t.sourceUrl,
        }),
        this.windowManager.sendMessageToWebContents(n.owner, {
          type: `browser-sidebar-image-drag-state`,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          isActive: !0,
        }));
    }
    handleRuntimeImageDragEnded(e) {
      let t = this.findPageStateForRuntimeSender(e);
      t == null ||
        t.owner.isDestroyed() ||
        dL(
          this.activeDraggedImagesByOwnerWebContentsId,
          t.page.browserTabId,
          t.page.conversationId,
          t.owner,
          this.windowManager,
        );
    }
    async attachDraggedImageToThread(e, t, n) {
      let r = this.activeDraggedImagesByOwnerWebContentsId.get(e.id) ?? null;
      if (r?.conversationId !== t || r.browserTabId !== n) return;
      let i = this.getPageState(e, t, n),
        a = i?.view.webContents ?? null;
      i == null ||
        a == null ||
        a.isDestroyed() ||
        (dL(
          this.activeDraggedImagesByOwnerWebContentsId,
          n,
          t,
          e,
          this.windowManager,
        ),
        await lL({
          browserTabId: n,
          browserPageUrl: i.url,
          conversationId: i.conversationId,
          owner: e,
          sourceUrl: r.sourceUrl,
          webContents: a,
          windowManager: this.windowManager,
        }));
    }
    handleCommentOverlaySubmit(e, t) {
      return this.comments.handleOverlaySubmit(e, t);
    }
    handleDesignOverlayUpdate(e, t) {
      this.comments.handleDesignOverlayUpdate(e, t);
    }
    async handleDesignOverlayAdd(e, t) {
      await this.comments.handleDesignOverlayAdd(e, t);
    }
    handleDesignOverlayDelete(e, t) {
      this.comments.handleDesignOverlayDelete(e, t);
    }
    handleCommentOverlayDelete(e, t) {
      this.comments.handleOverlayDelete(e, t);
    }
    handleCommentOverlayClose(e, t, n, r) {
      this.comments.handleOverlayClose(e, t, n, r);
    }
    handleCommentOverlayTweaksOpenChanged(e, t) {
      this.comments.handleOverlayTweaksOpenChanged(e, t);
    }
    handleCommentOverlayDesignScrubChanged(e, t) {
      this.isTweaksFeatureEnabled &&
        this.comments.handleOverlayDesignScrubChanged(e, t);
    }
    ensureWindowState(e, t) {
      let n = e.id,
        r = this.windows.get(n);
      if (r)
        return (
          (r.owner = t),
          this.browserSessionRegistry.registerWindow({
            ownerWebContentsId: t.id,
            windowId: n,
          }),
          r
        );
      let i = {
        owner: t,
        isClosing: !1,
        window: e,
        threads: new Map(),
        activeConversationId: null,
        activeBrowserTabId: null,
        ignoredConversationIds: new Set(),
        rendererInstanceId: null,
        registeredWebviewHostsByRoutePartition: new Map(),
        pendingRetainedWebviewRoutesByInstanceId: new Map(),
        browserSettingsWebviewMountIds: new Set(),
        browserSettingsWebviewThemeVariant: `light`,
        pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId:
          new Map(),
        browserSettingsWebviewAllowedNavigationUrlsByWebContentsId: new Map(),
        browserSettingsWebContentsByWebContentsId: new Map(),
        browserSettingsWebviewNavigationDisposersByWebContentsId: new Map(),
        pendingAdoptedWebviewRoutesByWebContentsId: new Map(),
        pendingAdoptedInitialUrlsByWebContents: new WeakMap(),
        canceledAdoptedWebContentsIds: new Set(),
        pendingWebviewRoutesByInstanceId: new Map(),
        transferredConversationStateKeys: new Set(),
        disposeWebviewListeners: () => void 0,
      };
      return (
        (i.disposeWebviewListeners = iB({
          reconcileBrowserStorageId: (e, t) =>
            this.browserPersistence.reconcileBrowserStorageId(i, e, t),
          attachRetainedWebview: (e, r) => {
            let a = i.threads.get(OL(r)) ?? null;
            if (a?.page == null) {
              (MQ().warning(
                `IAB_LIFECYCLE retained browser sidebar attach had no page`,
                {
                  safe: {
                    browserTabId: r.browserTabId,
                    conversationId: r.conversationId,
                    guestWebContentsId: e.id,
                    ownerWebContentsId: t.id,
                    windowId: n,
                  },
                  sensitive: {},
                },
              ),
                e.isDestroyed() || e.close());
              return;
            }
            this.attachPageWebContents(i, a.page, e, a.themeVariant);
          },
          attachWebview: (e, t) => {
            this.attachWebview(i, e, t);
          },
          configureBrowserSession: () => this.browserSessionService.configure(),
          ensureThreadStateForWindow: (e, t) =>
            this.ensureThreadStateForWindow(i, e, t),
          isPagePersistenceAllowed: () =>
            this.browserPersistence.isPagePersistenceAllowed(i.window),
          openBrowserSettingsUrlInTab: (e) =>
            i.activeConversationId == null
              ? !1
              : (this.windowManager.sendMessageToWebContents(t, {
                  type: `open-browser-tab`,
                  conversationId: i.activeConversationId,
                  initialUrl: e,
                  source: `manual`,
                  initiator: `window_open`,
                }),
                !0),
          owner: t,
          prepareWebviewAttach: (e, t, n) =>
            this.browserPersistence.prepareWebviewAttach(i, e, t, n),
          preloadPath: NQ,
          recordDebugEvent: (e) => {
            this.browserSessionRegistry.recordDebugEvent(e);
          },
          state: i,
          windowId: n,
        })),
        this.windows.set(n, i),
        this.browserSessionRegistry.registerWindow({
          ownerWebContentsId: t.id,
          windowId: n,
        }),
        this.browserPageSnapshots.registerWindowShutdown(i, () => {
          this.disposeWindowState(n);
        }),
        i
      );
    }
    ensureThreadState(e, t, n) {
      let r = { browserTabId: n, conversationId: t.conversationId },
        i = OL(r),
        a = e.threads.get(i);
      if (a) return a;
      let o = bQ({
        annotationModeEnabled: t.annotationModeEnabled ?? !0,
        browserStorageId: bL(r),
        browserTabId: n,
        conversationId: t.conversationId,
        hostKind: t.hostKind ?? null,
        emulatedViewportSize: t.emulatedViewportSize ?? null,
        cwd: t.cwd ?? null,
        isBrowserUsePage: !1,
        runtimeIntlConfig: t.runtimeIntlConfig,
      });
      return (
        this.tabBudget.markTabActivity(o),
        e.threads.set(i, o),
        sQ(this.browserSessionRegistry, this.windowManager, this.windows, e, o),
        o
      );
    }
    removeClosedThreadState(e, t) {
      (this.browserPersistence.unregisterWebviewHost(e, t),
        this.removeThreadState(e, t));
    }
    getPageLifecycleHelpers() {
      return {
        browserSessionRegistry: this.browserSessionRegistry,
        comments: this.comments,
        dismissCommentEditorForCommittedNavigation:
          this.dismissCommentEditorForCommittedNavigation.bind(this),
        enforceDetachedPageBudget:
          this.tabBudget.enforceDetachedPageBudget.bind(this.tabBudget),
        errorReporter: this.errorReporter,
        finalizeThreadTeardown: this.finalizeThreadTeardown.bind(this),
        finishPageAttach: this.finishPageAttach.bind(this),
        getThreadStateForWindow: _Q,
        goBack: this.goBack.bind(this),
        goForward: this.goForward.bind(this),
        isBrowserUseManagedBrowserTab:
          this.isBrowserUseManagedBrowserTab.bind(this),
        isRestrictedBrowserSidebarNavigation:
          this.isRestrictedBrowserSidebarNavigation.bind(this),
        loadBrowserSidebarErrorPage:
          this.loadBrowserSidebarErrorPage.bind(this),
        navigatePageToUrl: this.navigatePageToUrl.bind(this),
        notifyBrowserSidebarNavigationUsage: (e, t, n, r, i) =>
          pZ(
            this.windowManager,
            this.getPreferredWindowStateForConversation.bind(this),
            e,
            t,
            n,
            r,
            i,
          ),
        onBrowserSidebarStateChanged: this.onBrowserSidebarStateChanged,
        deletePersistedPageState: this.browserPageSnapshots.deleteAndFlush.bind(
          this.browserPageSnapshots,
        ),
        openBrowserSidebarExternalUrl:
          this.openBrowserSidebarExternalUrl.bind(this),
        openBrowserSidebarUrlExternally:
          this.openBrowserSidebarUrlExternally.bind(this),
        getOwnerCommandAccelerators: (e) =>
          this.ownerCommandAccelerators[e] ?? [],
        queuePageDeviceMetricsSync: this.queuePageDeviceMetricsSync.bind(this),
        reload: (e, t, n, r) => {
          this.reload(e, t, n == null ? void 0 : { ignoreCache: n }, r);
        },
        setDesignModifierPressed: this.setDesignModifierPressed.bind(this),
        setInteractionMode: this.setInteractionMode.bind(this),
        startCommentAtPoint: this.startCommentAtPoint.bind(this),
        startDesignEditAtPoint: this.startDesignEditAtPoint.bind(this),
        syncPageZoomPercent: this.syncPageZoomPercent.bind(this),
        syncRuntimeStateToPage: this.syncRuntimeStateToPage.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        updateThreadSessionState: this.updateThreadSessionState.bind(this),
        windowManager: this.windowManager,
      };
    }
    isBrowserUseManagedBrowserTab(e, t) {
      if (t === yL(e)) return !0;
      for (let n of this.windows.values())
        if ((_Q(n, e, t) ?? null)?.isBrowserUsePage === !0) return !0;
      return !1;
    }
    getBrowserUseRouteBrowserTabIds(e, t) {
      let n = [];
      for (let r of e.threads.values())
        r.conversationId === t &&
          (r.isBrowserUsePage || r.isBrowserUseActive) &&
          n.push(r.browserTabId);
      return n;
    }
    materializeThreadPageState(e, t, n) {
      return zZ(this.getPageLifecycleHelpers(), e, t, n);
    }
    attachPageWebContents(e, t, n, r) {
      BZ(this.getPageLifecycleHelpers(), e, t, n, r);
    }
    async finishPageAttach(e, t, n, r) {
      (await this.queuePageColorSchemeSync(t, n, r),
        !(t.view.webContents?.id !== n.id || n.isDestroyed()) &&
          this.resolveBrowserUseOpenRequests(e));
    }
    isRestrictedBrowserSidebarNavigation(e, t, n = `mainFrame`) {
      let r = e == null ? null : this.findPageStateForWebContentsId(e);
      return (
        !(r != null && NU(r.page.loadError, MB(t))) &&
        this.browserUseNavigationRestriction.isRestrictedForWebContentsId(
          e,
          t,
          { resourceType: n },
        )
      );
    }
    navigatePageToUrl(e, t, n, r) {
      if (VZ(this.getPageLifecycleHelpers(), e, t, n, r)) return !0;
      let i = e.view.webContents;
      if (i == null) return !1;
      let a = this.findPageStateForWebContentsId(i.id);
      return (
        a != null &&
          this.syncThreadState(a.windowState, a.conversationId, a.threadState),
        !1
      );
    }
    navigateBlankAdoptedWindowOpenChildAfterAttach(e, t, n) {
      if (FQ(n)) return;
      let r = !1,
        i = () => {
          setTimeout(o, 0);
        },
        a = () => {
          if (r) return;
          r = !0;
          function e() {
            ((r = !1),
              t.removeListener(`did-stop-loading`, n),
              t.removeListener(`did-fail-load`, n),
              t.removeListener(`destroyed`, e));
          }
          function n() {
            (e(), i());
          }
          (t.on(`did-stop-loading`, n),
            t.on(`did-fail-load`, n),
            t.on(`destroyed`, e));
        },
        o = () => {
          if (
            t.isDestroyed() ||
            e.view.webContents?.id !== t.id ||
            !FQ(t.getURL())
          ) {
            MQ().info(`IAB_ADOPTION skipped adopted fallback load`, {
              safe: {
                browserTabId: e.browserTabId,
                conversationId: e.conversationId,
                guestWebContentsId: t.id,
                isDestroyed: t.isDestroyed(),
                isStillAttached: e.view.webContents?.id === t.id,
                wasBlank: !t.isDestroyed() && FQ(t.getURL()),
                hasInitialUrl: n.length > 0,
              },
              sensitive: {},
            });
            return;
          }
          if (t.isLoading()) {
            (MQ().info(`IAB_ADOPTION waiting for adopted child load`, {
              safe: {
                browserTabId: e.browserTabId,
                conversationId: e.conversationId,
                guestWebContentsId: t.id,
                hasInitialUrl: n.length > 0,
              },
              sensitive: {},
            }),
              a());
            return;
          }
          (MQ().info(`IAB_ADOPTION loading blank adopted child target`, {
            safe: {
              browserTabId: e.browserTabId,
              conversationId: e.conversationId,
              guestWebContentsId: t.id,
              hasInitialUrl: n.length > 0,
            },
            sensitive: {},
          }),
            this.navigatePageToUrl(e, n, `manual`, `window_open`));
        };
      if (t.isLoading()) {
        (MQ().info(`IAB_ADOPTION waiting for adopted child initial load`, {
          safe: {
            browserTabId: e.browserTabId,
            conversationId: e.conversationId,
            guestWebContentsId: t.id,
            hasInitialUrl: n.length > 0,
          },
          sensitive: {},
        }),
          a());
        return;
      }
      i();
    }
    syncThreadState(e, t, n, r) {
      (this.refreshCommentModeBlockStatus(e, n),
        HZ(this.getPageLifecycleHelpers(), e, t, n, r));
    }
    syncThreadStateFromOrigin(e, t, n) {
      if (n.page == null) return;
      let r = this.getCurrentWindowState(e);
      r != null && this.syncThreadState(r, t, n);
    }
    dismissCommentEditor(e, t, n) {
      let r = this.getThreadState(e, t, n);
      r != null && this.comments.dismiss(e, t, n, r);
    }
    dismissCommentEditorForCommittedNavigation(e, t, n) {
      let r = _Q(e, t.conversationId, t.browserTabId);
      r == null ||
        r.snapshot.interactionMode !== `comment` ||
        lH(t.url, n) ||
        this.comments.dismiss(e.owner, t.conversationId, t.browserTabId, r);
    }
    syncRuntimeStateToPage(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        this.comments.syncRuntimeStateToPage(
          this.findPageStateForWebContentsId(n.id)?.windowState.owner ?? n,
          e.conversationId,
          n,
          t,
        );
    }
    sendRuntimeCreateCommentAtPoint(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        n.send(B, {
          type: `browser-sidebar-runtime-create-comment-at-point`,
          viewportPoint: t,
        });
    }
    sendRuntimeOpenDesignEditorAtPoint(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        n.send(B, {
          type: `browser-sidebar-runtime-open-design-editor-at-point`,
          viewportPoint: t,
        });
    }
    sendPendingCommentModeRequestToRuntime(e, t) {
      switch (t.kind) {
        case `comment`:
          this.sendRuntimeCreateCommentAtPoint(e, t.viewportPoint);
          return;
        case `design`:
          this.sendRuntimeOpenDesignEditorAtPoint(e, t.viewportPoint);
          return;
        case `text`: {
          let n = e.view.webContents;
          if (n == null || n.isDestroyed()) return;
          n.send(B, {
            type: `browser-sidebar-runtime-create-comment-from-selection`,
            ...(t.fallbackViewportPoint == null
              ? {}
              : { fallbackViewportPoint: t.fallbackViewportPoint }),
          });
          return;
        }
      }
    }
    syncCommentSnapshot(e, t, n) {
      (this.syncThreadStateFromOrigin(e, t, n),
        n.page != null && this.syncRuntimeStateToPage(n.page, n));
    }
    loadBrowserSidebarErrorPage(
      e,
      t,
      n,
      { failedUrl: r, kind: i, loadError: a },
    ) {
      KZ(this.getPageLifecycleHelpers(), e, t, n, {
        failedUrl: r,
        kind: i,
        loadError: a,
      });
    }
    openBrowserSidebarExternalUrl({
      initiatorUrl: e,
      pageState: t,
      url: r,
      webContents: i,
    }) {
      if (!NU(t.loadError, i.getURL()) || e !== t.loadError?.pageUrl) return !1;
      let a = FU(r);
      return a == null || !n.hs(a)
        ? !1
        : (this.openBrowserSidebarUrlExternally(t.conversationId, a), !0);
    }
    openBrowserSidebarUrlExternally(e, t) {
      IA({
        request: {
          conversationId: e,
          url: t,
          initiator: `open_in_browser_bridge`,
          openTarget: `external-browser`,
        },
      });
    }
    handleRuntimeMouseNavigation(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      if (!(n == null || n.owner.isDestroyed()))
        switch (t.direction) {
          case `back`:
            this.goBack(n.owner, n.page.conversationId, n.page.browserTabId);
            break;
          case `forward`:
            this.goForward(n.owner, n.page.conversationId, n.page.browserTabId);
            break;
        }
    }
    findPageStateForRuntimeSender(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (r?.view.webContents?.id === e.id)
            return { owner: t.owner, page: r };
        }
      return null;
    }
    findPageStateForPageKey(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (
            r != null &&
            DL(t.owner.id, n.conversationId, n.browserTabId) === e
          )
            return {
              conversationId: n.conversationId,
              page: r,
              threadState: n,
              windowState: t,
            };
        }
      return null;
    }
    findPageStateForWebContentsId(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (r?.view.webContents?.id === e)
            return {
              conversationId: n.conversationId,
              page: r,
              threadState: n,
              windowState: t,
            };
        }
      return null;
    }
    navigateCurrentPageHistory(e, t, n, r) {
      let i = this.getCurrentPageCommandContext(e, t, r);
      if (i == null) return;
      let { page: a, threadState: o, windowState: s } = i,
        c = a.view.webContents;
      if (
        c != null &&
        !(n === `back` && !c.canGoBack()) &&
        !(n === `forward` && !c.canGoForward())
      ) {
        if ((ZU(this.windowManager, s.owner, t, o), UL(a), n === `back`)) {
          c.goBack();
          return;
        }
        c.goForward();
      }
    }
    getCurrentPageCommandContext(e, t, n) {
      let r = this.getCurrentWindowState(e);
      if (r == null) return null;
      let i = _Q(r, t, n);
      return i?.page == null
        ? null
        : { page: i.page, threadState: i, windowState: r };
    }
    ensureThreadCommandContext(e, t, n) {
      let r = this.ensureCurrentWindowState(e);
      if (r == null) return null;
      let i = gQ(r, t, n);
      ((r.activeConversationId = t), (r.activeBrowserTabId = i));
      let a = this.ensureThreadStateForWindow(r, t, i);
      return (
        this.tabBudget.markTabSelected(r, a),
        { threadState: a, windowState: r }
      );
    }
    ensureCurrentWindowState(e) {
      let t = this.getCurrentWindow(e);
      return t == null ? null : this.ensureWindowState(t, e);
    }
    getCurrentWindowState(e) {
      let t = this.getCurrentWindow(e);
      return t == null ? null : (this.windows.get(t.id) ?? null);
    }
    getCurrentWindow(e) {
      let t = c.BrowserWindow.fromWebContents(e);
      return t == null || t.isDestroyed() ? null : t;
    }
    getPageState(e, t, n) {
      return this.getThreadState(e, t, n)?.page ?? null;
    }
    getThreadState(e, t, n) {
      let r = this.getCurrentWindowState(e);
      return r == null ? null : _Q(r, t, n);
    }
    getThreadPageState(e, t, n) {
      let r = this.getCurrentWindowState(e);
      return r == null ? null : (_Q(r, t, n)?.page ?? null);
    }
    ensureThreadStateForCommand(e, t, n) {
      return this.ensureThreadCommandContext(e, t, n)?.threadState ?? null;
    }
    ensureThreadStateForWindow(e, t, n, r = {}) {
      return yQ({
        browserSessionRegistry: this.browserSessionRegistry,
        browserTabId: n,
        conversationId: t,
        options: r,
        tabBudget: this.tabBudget,
        windowManager: this.windowManager,
        windows: this.windows,
        windowState: e,
      });
    }
    refreshCommentModeBlockStatus(e, t) {
      dH({
        blocklistLookup: this.commentModeBlocklistLookup,
        comments: this.comments,
        sendPendingCommentModeRequestToRuntime: (e, t) => {
          this.sendPendingCommentModeRequestToRuntime(e, t);
        },
        syncRuntimeStateToPage: (e, t) => {
          this.syncRuntimeStateToPage(e, t);
        },
        syncThreadState: (e, t, n) => {
          this.syncThreadState(e, t, n);
        },
        windowState: e,
        threadState: t,
      });
    }
    ensureThreadPageForCommand(e, t, n, r, i) {
      let a = this.getThreadPageState(e, t, n);
      if (a != null) return a;
      let o = this.ensureThreadStateForCommand(e, t, n);
      if (o == null) return null;
      let s = this.ensureCurrentWindowState(e);
      return s == null || i?.restore === `required`
        ? null
        : (o.page ??
            this.materializeThreadPageState(s, o, { kind: `fresh`, page: r }),
          o.page);
    }
    disposeThreadState(e, t, n) {
      this.comments.close(e.owner, t, n.browserTabId);
      let r = n.page;
      (this.clearPendingWebviewRoutes(e, n),
        r != null &&
          (r.dispose(),
          this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
            this.browserSessionRegistry.recordPageClosed({
              browserTabId: n.browserTabId,
              conversationId: t,
              windowId: zX(e.window.id),
            })),
        n.isBrowserUseActive &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: zX(e.window.id) },
            !1,
            n.browserTabId,
          ));
    }
    removeThreadState(e, t) {
      lQ(this.windowManager, this.windows, e, t);
    }
    clearPendingWebviewRoutes(e, t) {
      for (let [n, r] of e.pendingRetainedWebviewRoutesByInstanceId) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          e.pendingRetainedWebviewRoutesByInstanceId.delete(n);
      }
      for (let [n, r] of e.pendingAdoptedWebviewRoutesByWebContentsId) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          (e.canceledAdoptedWebContentsIds.add(n),
          e.pendingAdoptedWebviewRoutesByWebContentsId.delete(n));
      }
      for (let [n, r] of e.pendingWebviewRoutesByInstanceId) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          e.pendingWebviewRoutesByInstanceId.delete(n);
      }
    }
    disposeWindowState(e) {
      let t = this.windows.get(e);
      if (t) {
        (t.isClosing ||
          this.browserPageSnapshots.persistWindowBeforeShutdown(t),
          (t.isClosing = !0),
          this.browserUseOpenRequests.rejectForOwner(
            t.owner,
            Error(
              `The ChatGPT window closed while waiting for a Browser webview to attach`,
            ),
          ));
        for (let e of t.threads.values())
          this.disposeThreadState(t, e.conversationId, e);
        (t.disposeWebviewListeners(),
          t.threads.clear(),
          this.windows.delete(e),
          this.tabBudget.handleWindowClosed(e),
          this.lastSyncedWindowId === e && (this.lastSyncedWindowId = null),
          this.browserSessionRegistry.handleWindowClosed(e));
      }
    }
    getLiveWindowState(e) {
      return SQ({
        ensureWindowState: this.ensureWindowState.bind(this),
        windowId: e,
        windows: this.windows,
      });
    }
    getFocusedOwnerWebContents() {
      return CQ({
        getLiveWindowState: this.getLiveWindowState.bind(this),
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows.values(),
      });
    }
    getFocusedWindowState() {
      return wQ({
        getLiveWindowState: this.getLiveWindowState.bind(this),
        windows: this.windows,
      });
    }
    getPreferredWindowStateForBrowserUse() {
      return TQ({
        getFocusedWindowState: this.getFocusedWindowState.bind(this),
        getLiveWindowState: this.getLiveWindowState.bind(this),
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows.values(),
      });
    }
    getPreferredWindowStateForConversation(e) {
      return EQ({
        conversationId: e,
        getFocusedWindowState: this.getFocusedWindowState.bind(this),
        hasThreadForConversation: vQ,
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows,
      });
    }
    getActivePageHandle(e) {
      let t = e.activeConversationId;
      return t == null
        ? null
        : this.getPageHandleForConversation(
            e,
            t,
            e.activeBrowserTabId ?? yL(t),
          );
    }
    getPageHandleForConversation(e, t, n) {
      if (t == null) return null;
      let r = _Q(e, t, n ?? void 0) ?? null;
      return r?.pendingTeardown != null ||
        r?.page == null ||
        r.page.view.webContents == null ||
        r.page.view.webContents.isDestroyed()
        ? null
        : KL(e, t, r.page);
    }
    getOrCreatePageForBrowserUse(e, t, { requireVisible: n }) {
      if (t == null) return null;
      let r = _Q(e, t, yL(t)) ?? null;
      if (n && (r == null || !r.visible || r.bounds == null)) return null;
      let i = r?.snapshot ?? TZ(),
        a = yL(t),
        o =
          r?.page ??
          this.ensureThreadPageForCommand(e.owner, t, a, {
            title: i.title,
            url: i.url,
          });
      return o == null ? null : KL(e, t, o);
    }
    getOrCreateActivePageForBrowserUse(e) {
      return this.getOrCreatePageForBrowserUse(e, e.activeConversationId, {
        requireVisible: !0,
      });
    }
    resolveBrowserUseOpenRequests(e) {
      this.browserUseOpenRequests.resolveForWindow(e, (t) =>
        t.conversationId == null
          ? this.getOrCreateActivePageForBrowserUse(e)
          : this.getPageHandleForConversation(
              e,
              t.conversationId,
              t.browserTabId,
            ),
      );
    }
  };
function FQ(e) {
  let t = n.ms(e);
  return t.length === 0 || t.startsWith(`about:blank#`);
}
var IQ = r.a(`remote-localhost-navigation`);
async function LQ({
  appServerConnectionRegistry: e,
  getHostConfigForHostId: t,
  hostId: r,
  url: i,
}) {
  if (r == null || r === `local`) return i;
  let a = t(r),
    o = n.vs(i);
  if (!n.no(a) || o == null) return i;
  try {
    let t = await e.getConnection(r).startRemoteLocalhostPortForward(o),
      n = new URL(i);
    return ((n.hostname = `localhost`), (n.port = String(t)), n.toString());
  } catch (e) {
    return (
      IQ().warning(`browser_sidebar.remote_localhost_port_forward_failed`, {
        safe: { remotePort: o },
        sensitive: { error: e, hostId: r, url: i },
      }),
      null
    );
  }
}
var RQ = class {
    id;
    destroyedListeners = new Set();
    focusChangedListeners = new Set();
    constructor(e, t = null) {
      ((this.webContents = e), (this.browserWindow = t), (this.id = e.id));
    }
    send(e, t) {
      this.webContents.send(e, t);
    }
    isDestroyed() {
      return this.webContents.isDestroyed();
    }
    isFocused() {
      return this.browserWindow?.isFocused() === !0;
    }
    onDestroyed(e) {
      let t = () => {
        e();
      };
      return (
        this.destroyedListeners.size === 0 &&
          this.webContents.once(`destroyed`, this.handleDestroyed),
        this.destroyedListeners.add(t),
        {
          dispose: () => {
            this.destroyedListeners.delete(t) &&
              (this.destroyedListeners.size > 0 ||
                this.webContents.removeListener(
                  `destroyed`,
                  this.handleDestroyed,
                ));
          },
        }
      );
    }
    onFocusChanged(e) {
      if (this.browserWindow == null) return { dispose: () => {} };
      let t = (t) => {
        e(t);
      };
      return (
        this.focusChangedListeners.size === 0 &&
          (this.browserWindow.on(`focus`, this.handleFocus),
          this.browserWindow.on(`blur`, this.handleBlur)),
        this.focusChangedListeners.add(t),
        {
          dispose: () => {
            this.focusChangedListeners.delete(t) &&
              (this.focusChangedListeners.size > 0 ||
                (this.browserWindow?.removeListener(`focus`, this.handleFocus),
                this.browserWindow?.removeListener(`blur`, this.handleBlur)));
          },
        }
      );
    }
    handleDestroyed = () => {
      let e = Array.from(this.destroyedListeners);
      this.destroyedListeners.clear();
      for (let t of e) t();
    };
    handleFocus = () => {
      this.notifyFocusChanged(!0);
    };
    handleBlur = () => {
      this.notifyFocusChanged(!1);
    };
    notifyFocusChanged(e) {
      for (let t of Array.from(this.focusChangedListeners)) t(e);
    }
  },
  zQ = n.xl({
    legacyRoot: n.wl().min(1).nullable(),
    projectId: n.wl().trim().min(1),
    root: n.wl().optional(),
  }),
  BQ = n.xl({
    legacyRoot: n.wl().min(1).nullable(),
    projectId: n.wl().trim().min(1),
    root: n.wl(),
  }),
  VQ = n.xl({
    legacyRoot: n.wl().min(1).nullable(),
    projectId: n.wl().trim().min(1),
  });
function HQ({
  getProjectWritableRootsValue: e,
  getRootCompareKeys: t = UQ,
  getRootForStorage: r = (e) => e,
  notifyChanged: i,
  runSerializedMutation: a,
  selectWritableRoot: o,
  setProjectWritableRootsValue: s,
}) {
  let c = () => n.ja(e()),
    l = Promise.resolve(),
    u =
      a ??
      ((e) => {
        let t = l.then(e, e);
        return (
          (l = t.then(
            () => void 0,
            () => void 0,
          )),
          t
        );
      }),
    d = (e, t, n) => (
      s({ ...e, [t]: n }),
      i(),
      { changed: !0, projectId: t, roots: n }
    );
  return {
    addRoot: async (e) => {
      let { legacyRoot: n, projectId: i, root: a } = WQ(e),
        s = await o(a);
      return s == null
        ? { changed: !1, projectId: i, roots: qQ(c(), i, n) }
        : u(async () => {
            let e = c(),
              a = qQ(e, i, n),
              o = await t(s);
            return (await Promise.all(a.map((e) => t(e.path)))).some((e) =>
              JQ(e, o),
            )
              ? { changed: !1, projectId: i, roots: a }
              : d(e, i, [...a, { kind: `local`, path: r(s) }]);
          });
    },
    removeRoot: async (e) => {
      let { legacyRoot: n, projectId: r, root: i } = GQ(e);
      return u(async () => {
        let e = c(),
          a = qQ(e, r, n);
        if (a.length === 0) return { changed: !1, projectId: r, roots: [] };
        let o = await t(i),
          s = (
            await Promise.all(
              a.map(async (e) => ({
                compareKeys: await t(e.path),
                writableRoot: e,
              })),
            )
          )
            .filter(({ compareKeys: e }) => !JQ(e, o))
            .map(({ writableRoot: e }) => e);
        return s.length === a.length
          ? { changed: !1, projectId: r, roots: a }
          : d(e, r, s);
      });
    },
    clearRoots: async (e) => {
      let { legacyRoot: t, projectId: n } = KQ(e);
      return u(async () => {
        let e = c(),
          r = Object.hasOwn(e, n),
          i = qQ(e, n, t);
        return r && i.length === 0
          ? { changed: !1, projectId: n, roots: [] }
          : d(e, n, []);
      });
    },
  };
}
async function UQ(e) {
  let t = u.resolve(e);
  try {
    return (await (0, m.stat)(t)).isDirectory()
      ? [n.zs(t), n.zs(await (0, m.realpath)(t))]
      : [n.zs(t)];
  } catch {
    return [n.zs(t)];
  }
}
function WQ(e) {
  let t = zQ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots add request`);
  return t.data;
}
function GQ(e) {
  let t = BQ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots remove request`);
  return t.data;
}
function KQ(e) {
  let t = VQ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots clear request`);
  return t.data;
}
function qQ(e, t, n) {
  return Object.hasOwn(e, t)
    ? (e[t] ?? [])
    : n == null
      ? []
      : [{ kind: `local`, path: n }];
}
function JQ(e, t) {
  return e.some((e) => t.includes(e));
}
var YQ = 3e4,
  XQ = 5e3,
  ZQ = 6e4,
  QQ = class {
    logger = r.a(`sampler-manager`);
    appStateHeartbeat;
    appStateSnapshotRequestById = new n.wo({ ttlMs: ZQ });
    appStateTriggerMinIntervalGate = new n.za({ minIntervalMs: XQ });
    constructor(e, t, n, r, i) {
      ((this.hostId = e),
        (this.windowManager = t),
        (this.desktopSentry = n),
        (this.electronSampler = r),
        (this.options = i),
        (this.appStateHeartbeat = setInterval(() => {
          this.requestAppStateSnapshot(`heartbeat`);
        }, YQ)),
        this.appStateHeartbeat.unref());
    }
    dispose() {
      clearInterval(this.appStateHeartbeat);
    }
    handleRendererReady(e) {
      this.requestAppStateSnapshot(`heartbeat`, e);
    }
    async handleMessage(e, t) {
      switch (t.type) {
        case `electron-app-state-snapshot-trigger`:
          this.requestAppStateSnapshot(t.reason, e);
          break;
        case `electron-app-state-snapshot-response`:
          await this.handleAppStateSnapshotResponse(e, t);
          break;
      }
    }
    async enrichWithHostSnapshotFields(e, t) {
      let n = await this.electronSampler.collectSnapshotFields(e);
      return this.addHostSnapshotFields(t, n);
    }
    async enrichPerformanceWithHostSnapshotFields(e, t) {
      let n = await this.electronSampler.collectPerformanceSnapshotFields(e);
      return this.addHostSnapshotFields(t, n);
    }
    addHostSnapshotFields(e, t) {
      let n = this.options.getStdioIoStatsSnapshot(),
        r = { ...e, ...t, ...this.options.getBrowserUseSnapshotFields() };
      return (
        n != null &&
          Object.assign(r, {
            app_server_stdio_bytes_read_total: n.bytesReadTotal,
            app_server_stdio_bytes_written_total: n.bytesWrittenTotal,
            app_server_stdio_bytes_read_last_30s: n.bytesReadLast30s,
            app_server_stdio_bytes_written_last_30s: n.bytesWrittenLast30s,
          }),
        r
      );
    }
    async handleAppStateSnapshotResponse(e, t) {
      let n = E.performance.now(),
        r = this.appStateSnapshotRequestById.get(t.requestId, n);
      if (!r || r.webContentsId !== e.id) return;
      this.appStateSnapshotRequestById.delete(t.requestId);
      let i = await this.enrichWithHostSnapshotFields(e, t.fields);
      (this.logger().debug(`app_state_snapshot`, { safe: i, sensitive: {} }),
        this.desktopSentry.addBreadcrumb({
          category: `app_state`,
          level: `info`,
          message: `app_state_snapshot`,
          data: i,
        }));
    }
    requestAppStateSnapshot(e, t) {
      let n = E.performance.now();
      if (e !== `heartbeat` && !this.appStateTriggerMinIntervalGate.canPass(n))
        return;
      let r = t == null ? this.windowManager.getPrimaryWindow() : null;
      if (e === `heartbeat` && t == null && r != null && !r.isFocused()) return;
      let i = t ?? r?.webContents;
      if (!i || i.isDestroyed() || !this.windowManager.isWebContentsReady(i.id))
        return;
      this.appStateTriggerMinIntervalGate.mark(n);
      let a = (0, f.randomUUID)();
      (this.appStateSnapshotRequestById.set(a, { webContentsId: i.id }, n),
        this.windowManager.sendMessageToWebContents(i, {
          type: `electron-app-state-snapshot-request`,
          hostId: this.hostId,
          requestId: a,
          reason: e,
        }));
    }
  },
  $Q = r.a(`content-tracing`);
async function e$(e) {
  let t = c.app.getAppMetrics().map((e) => ({
      pid: e.pid,
      type: e.type,
      memory:
        e.memory == null
          ? null
          : {
              workingSetSize: e.memory.workingSetSize,
              peakWorkingSetSize: e.memory.peakWorkingSetSize,
              privateBytes: e.memory.privateBytes ?? 0,
              sharedBytes: 0,
            },
    })),
    n =
      e == null || e.isDestroyed()
        ? null
        : await (async () => {
            let n = e.webContents;
            if (typeof n.getProcessMemoryInfo == `function`)
              return n
                .getProcessMemoryInfo()
                .then((e) => ({
                  private: e.private,
                  shared: e.shared,
                  residentSet: e.residentSet,
                }))
                .catch(() => null);
            let r = n.getOSProcessId();
            if (r <= 0) return null;
            let i = t.find((e) => e.pid === r);
            return i?.memory == null
              ? null
              : {
                  private: i.memory.privateBytes,
                  shared: i.memory.sharedBytes,
                  residentSet: i.memory.workingSetSize,
                };
          })(),
    r = Object.fromEntries(Object.entries(c.app.getGPUFeatureStatus())),
    i = null,
    a = null;
  try {
    let e = t$(await c.app.getGPUInfo(`basic`)),
      t = e?.gpuDevice ?? [],
      n = t.find((e) => e.active) ?? t[0];
    i = n?.vendorString ?? (n?.vendorId == null ? null : String(n.vendorId));
    let r = e?.auxAttributes ?? {},
      o = r.glRenderer ?? r.gl_renderer;
    a = typeof o == `string` ? o : null;
  } catch (e) {
    $Q().warning(`Failed to collect GPU info for trace upload.`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  return {
    appMetrics: t,
    rendererMemory: n,
    gpuFeatureStatus: r,
    gpuVendor: i,
    gpuRenderer: a,
    hardwareAccelerationEnabled: !c.app.commandLine.hasSwitch(`disable-gpu`),
  };
}
function t$(e) {
  return typeof e != `object` || !e ? null : e;
}
var n$ = r.a(`content-tracing`),
  r$ = `Start Performance Trace`,
  i$ = `Stop Performance Trace`,
  a$ = `Waiting to Start Trace…`,
  o$ = `Saving Trace…`,
  s$ = `Waiting for Trace Details…`,
  c$ = `Uploading Trace…`,
  l$ = `toggle-trace-recording`,
  u$ = process.env.CODEX_TRACE_SHORTCUT != null,
  d$ = [
    `*`,
    `disabled-by-default-v8.cpu_profiler`,
    `disabled-by-default-v8.cpu_profiler.hires`,
  ],
  f$ = `idle`,
  p$ = null,
  m$ = null;
function h$({
  allowDevtools: e,
  accelerator: t,
  windowManager: n,
  traceRecordingUploadOptions: r,
}) {
  let i = async (e) => {
    if (
      !(
        f$ === `awaiting-start-confirmation` ||
        f$ === `saving` ||
        f$ === `awaiting-upload-details` ||
        f$ === `uploading`
      )
    )
      try {
        let t = null;
        if (f$ === `idle`) {
          if ((v$(n, e, `awaiting-start-confirmation`), !(await qP()))) {
            v$(n, e, `idle`);
            return;
          }
          (await c.contentTracing.startRecording({
            included_categories: d$,
            recording_mode: `record-continuously`,
          }),
            (p$ = Date.now()),
            v$(n, e, `recording`),
            n$().info(`Started content trace recording.`));
          return;
        }
        v$(n, e, `saving`);
        let i = await c.contentTracing.stopRecording(
            (0, u.join)((0, l.tmpdir)(), `codex-trace-${Date.now()}.json`),
          ),
          a = p$ == null ? null : Date.now() - p$;
        if (
          (n$().info(`Stopped content trace recording.`, {
            safe: { recordingDurationMs: a },
            sensitive: { tracePath: i },
          }),
          r != null)
        )
          try {
            let o = c.BrowserWindow.getFocusedWindow(),
              s = o != null && !o.isDestroyed() ? o : null,
              l = s?.id ?? null,
              u =
                s == null || !n.hasRegisteredWebContents(s.webContents)
                  ? null
                  : V,
              d = e$(s);
            v$(n, e, `awaiting-upload-details`);
            let [f, p] = await Promise.all([XP(), d]);
            v$(n, e, `uploading`);
            let { uploadTraceRecording: m } = await Promise.resolve().then(() =>
              require(`./trace-recording-upload-C_qw5x6Z.js`),
            );
            ((t = await m({
              ...r,
              tracePath: i,
              recordingDurationMs: a,
              correlation: {
                conversationId: f.conversationId,
                hostId: u,
                windowId: l,
              },
              runtimeHealth: p,
              traceRecordingNote: f.note.length > 0 ? f.note : null,
            })),
              n$().info(`Uploaded content trace recording.`, {
                safe: { sourceWindowId: l, sourceHostId: u },
                sensitive: {
                  tracePath: i,
                  traceConversationId: f.conversationId,
                },
              }));
          } finally {
            await b$(i);
          }
        ((p$ = null),
          v$(n, e, `idle`),
          t != null &&
            n.sendMessageToAllRegisteredWindows({
              type: `trace-recording-uploaded`,
              reportId: t,
            }));
      } catch (t) {
        let r = x$();
        (r === `saving`
          ? v$(n, e, `recording`)
          : (r === `awaiting-start-confirmation` ||
              r === `awaiting-upload-details` ||
              r === `uploading`) &&
            ((p$ = null), v$(n, e, `idle`)),
          n$().warning(`Failed to toggle content trace recording.`, {
            safe: {},
            sensitive: { error: t },
          }));
      }
  };
  return (
    (m$ = i),
    {
      id: l$,
      label: y$(f$),
      accelerator: e && u$ ? t : void 0,
      click: async (e) => {
        await i(e);
      },
    }
  );
}
function g$(e) {
  e.sendMessageToAllRegisteredWindows({
    type: `trace-recording-state-changed`,
    state: f$,
  });
}
async function _$() {
  let e = c.Menu.getApplicationMenu()?.getMenuItemById(l$);
  e == null || m$ == null || (await m$(e));
}
function v$(e, t, n) {
  ((f$ = n), (t.label = y$(n)), g$(e));
}
function y$(e) {
  return e === `recording`
    ? i$
    : e === `awaiting-start-confirmation`
      ? a$
      : e === `saving`
        ? o$
        : e === `awaiting-upload-details`
          ? s$
          : e === `uploading`
            ? c$
            : r$;
}
async function b$(e) {
  try {
    await (0, m.rm)(e, { force: !0 });
  } catch (t) {
    n$().warning(`Failed to remove temporary trace recording file.`, {
      safe: {},
      sensitive: { error: t, tracePath: e },
    });
  }
}
function x$() {
  return f$;
}
var S$ = e.o(a.M()),
  C$ = 32e3,
  w$ = r.a(`worktree-service`),
  T$ = class {
    statesById = new Map();
    newbornWorktreeRoots = new Set();
    newbornWorktreeRootsByStreamId = new Map();
    cleanupRunning = !1;
    cleanupPending = !1;
    constructor(e) {
      ((this.options = e),
        this.options.subscribeGitWorkerEvent(`create-worktree-path`, (e) => {
          this.handleCreateWorktreePath(e);
        }),
        this.options.subscribeGitWorkerEvent(
          `create-worktree-setup-started`,
          (e) => {
            this.handleCreateWorktreeSetupStarted(e);
          },
        ),
        this.options.subscribeGitWorkerEvent(`create-worktree-stream`, (e) => {
          this.handleCreateWorktreeStream(e);
        }),
        this.publish());
    }
    create(e) {
      if (this.statesById.has(e.id)) return;
      let t = {
        ...e,
        createdAt: Date.now(),
        attempt: 1,
        phase: `queued`,
        labelEdited: !1,
        worktreeOutputText: ``,
        setupOutputText: ``,
        errorMessage: null,
        worktreeWorkspaceRoot: null,
        worktreeGitRoot: null,
        needsAttention: !1,
        isPinned: !1,
        pinnedBeforeThreadId: null,
      };
      (this.statesById.set(t.id, { entry: t, runtime: null }),
        this.publish(),
        this.start(t.id));
    }
    cancel(e) {
      let t = this.statesById.get(e);
      (t?.runtime?.abortController.abort(),
        this.statesById.delete(e) &&
          (this.publish(),
          t?.entry.worktreeGitRoot != null &&
            this.deleteWorktree(
              t.entry.worktreeGitRoot,
              `new-branch-cleanup`,
            )));
    }
    dismiss(e) {
      let t = this.statesById.get(e);
      (t?.runtime?.abortController.abort(),
        this.statesById.delete(e) &&
          (this.publish(),
          t?.entry.phase === `failed` &&
            t.entry.worktreeGitRoot != null &&
            this.deleteWorktree(
              t.entry.worktreeGitRoot,
              `new-branch-cleanup`,
            )));
    }
    retry(e) {
      let t = this.statesById.get(e);
      if (!t) return;
      let n = t.entry.phase === `failed` ? t.entry.worktreeGitRoot : null;
      (t.runtime?.abortController.abort(),
        (t.entry = {
          ...t.entry,
          attempt: t.entry.attempt + 1,
          phase: `queued`,
          worktreeOutputText: ``,
          setupOutputText: ``,
          errorMessage: null,
          worktreeWorkspaceRoot: null,
          worktreeGitRoot: null,
          needsAttention: !1,
        }),
        (t.runtime = null),
        this.publish(),
        n != null && this.deleteWorktree(n, `new-branch-cleanup`),
        this.start(e));
    }
    continueWithoutSetup(e) {
      this.updateEntry(e, (e) =>
        e.phase !== `failed` ||
        e.worktreeGitRoot == null ||
        e.worktreeWorkspaceRoot == null
          ? e
          : {
              ...e,
              phase: `worktree-ready`,
              needsAttention: !1,
              setupOutputText: E$(
                e.setupOutputText,
                `[info] Continuing without local environment setup
`,
              ),
            },
      );
    }
    updateMetadata(e, t) {
      this.updateEntry(e, (e) => {
        switch (t.type) {
          case `isPinned`:
            return { ...e, isPinned: t.isPinned };
          case `pinnedBeforeThreadId`:
            return { ...e, pinnedBeforeThreadId: t.beforeThreadId };
          case `label`:
            return { ...e, label: t.label };
          case `labelEdited`:
            return { ...e, labelEdited: t.labelEdited };
          case `needsAttention`:
            return { ...e, needsAttention: t.needsAttention };
        }
      });
    }
    getPendingWorktrees() {
      return Array.from(this.statesById.values(), (e) => e.entry);
    }
    async start(e) {
      let t = this.statesById.get(e);
      if (!t) return;
      let { entry: r } = t,
        i = {
          abortController: new AbortController(),
          outputDecoder: new TextDecoder(),
          streamId: (0, f.randomUUID)(),
        };
      ((t.runtime = i),
        this.updateEntry(e, (e) => ({
          ...e,
          phase: `creating`,
          worktreeOutputText: `[info] Starting worktree creation
`,
        })));
      try {
        let t = await this.requestGitWorker({
          method: `create-worktree`,
          params: {
            hostConfig: this.options.hostConfig,
            operationSource: `worktree_pending_create`,
            cwd: n.oo(r.sourceWorkspaceRoot),
            startingState: r.startingState,
            localEnvironmentConfigPath: r.localEnvironmentConfigPath,
            streamId: i.streamId,
            allowSetupFailure: !0,
            worktreesRoot: gi(this.options.settingsStore),
            setUpSyncedBranch:
              r.launchMode === `create-stable-worktree` ? !1 : void 0,
          },
          signal: i.abortController.signal,
        });
        if (t.setupError != null) {
          (w$().info(`[worktree-create] setup-failed`, {
            safe: {
              worktreeId: await n.dt(t.worktreeGitRoot),
              flow: `pending`,
              launchMode: r.launchMode,
              hasLocalEnvironment: r.localEnvironmentConfigPath != null,
            },
            sensitive: {},
          }),
            this.updateEntry(e, (e) => ({
              ...e,
              phase: `failed`,
              errorMessage: t.setupError,
              worktreeGitRoot: t.worktreeGitRoot,
              worktreeWorkspaceRoot: t.worktreeWorkspaceRoot,
              needsAttention: !0,
            })));
          let i = this.statesById.get(e);
          (i && (i.runtime = null), this.notifyFailure(e, t.setupError));
          return;
        }
        if (
          (this.updateEntry(e, (e) => ({
            ...e,
            phase: `worktree-ready`,
            worktreeGitRoot: t.worktreeGitRoot,
            worktreeWorkspaceRoot: t.worktreeWorkspaceRoot,
          })),
          w$().info(`[worktree-create] ready`, {
            safe: {
              worktreeId: await n.dt(t.worktreeGitRoot),
              flow: `pending`,
              launchMode: r.launchMode,
              hasLocalEnvironment: r.localEnvironmentConfigPath != null,
              wasNewbornProtected: this.newbornWorktreeRoots.has(
                t.worktreeGitRoot,
              ),
              protectedNewbornCount: this.newbornWorktreeRoots.size,
            },
            sensitive: {},
          }),
          r.launchMode === `create-stable-worktree`)
        ) {
          (this.forgetNewbornWorktreeStream(i.streamId),
            this.addWorkspaceRoot(t.worktreeWorkspaceRoot, r.label),
            this.statesById.delete(e),
            this.publish());
          return;
        }
        let a = this.statesById.get(e);
        (a && (a.runtime = null), this.runCleanup());
      } catch (t) {
        this.forgetNewbornWorktreeStream(i.streamId);
        let n = this.statesById.get(e);
        if ((n && (n.runtime = null), O$(t))) return;
        let r = t instanceof Error ? t.message : String(t);
        (this.updateEntry(e, (e) =>
          D$(
            { ...e, phase: `failed`, errorMessage: r, needsAttention: !0 },
            `[stderr] ${r}\n`,
            e.phase,
          ),
        ),
          this.notifyFailure(e, r));
      }
    }
    async handleArchiveCleanup({
      conversationId: e,
      cwd: t,
      replacementOwnerThreadId: r,
      replacementOwnerCwd: i,
    }) {
      let a = n.oo(t);
      if (a == null) return;
      let o = await this.requestGitWorker({
        method: `resolve-worktree-for-thread`,
        params: {
          cwd: a,
          conversationId: n.uc(e),
          hostConfig: this.options.hostConfig,
          operationSource: `worktree_archive_cleanup`,
          worktreesRoot: gi(this.options.settingsStore),
        },
      });
      if (o.worktreeGitRoot == null) return;
      let s = o.worktreeGitRoot;
      if (
        !bi({ globalState: this.options.globalState, worktreeDir: String(s) })
      ) {
        if (r != null && i != null && k$(i, String(s)))
          try {
            (await this.setOwnerThread({
              worktree: s,
              conversationId: n.uc(r),
            }),
              w$().info(`[worktree-archive-cleanup] owner-transferred`, {
                safe: {
                  worktreeId: await n.dt(s),
                  archivedThreadId: e,
                  replacementOwnerThreadId: r,
                },
                sensitive: {},
              }));
            return;
          } catch (t) {
            w$().warning(`[worktree-archive-cleanup] owner-transfer-failed`, {
              safe: {
                worktreeId: await n.dt(s),
                archivedThreadId: e,
                replacementOwnerThreadId: r,
              },
              sensitive: { error: t },
            });
            return;
          }
        await this.deleteWorktree(s, `archive-cleanup`);
      }
    }
    async createManagedWorktree({
      hostId: e,
      cwd: t,
      startingState: r,
      localEnvironmentConfigPath: i,
      streamId: a,
    }) {
      try {
        let o = await this.requestGitWorker({
            method: `create-worktree`,
            params: {
              hostConfig: this.options.getHostConfigForHostId(e),
              operationSource: `worktree_managed_create`,
              cwd: n.oo(t),
              startingState: r,
              localEnvironmentConfigPath: i,
              streamId: a,
              worktreesRoot: gi(this.options.settingsStore),
            },
          }),
          s = this.newbornWorktreeRoots.has(o.worktreeGitRoot);
        return (
          this.newbornWorktreeRoots.add(o.worktreeGitRoot),
          w$().info(`[worktree-create] ready`, {
            safe: {
              worktreeId: await n.dt(o.worktreeGitRoot),
              flow: `managed`,
              hasLocalEnvironment: i != null,
              wasNewbornProtected: s,
              protectedNewbornCount: this.newbornWorktreeRoots.size,
            },
            sensitive: {},
          }),
          this.runCleanup(),
          o
        );
      } catch (e) {
        throw (this.forgetNewbornWorktreeStream(a), e);
      }
    }
    async deleteWorktree(e, t) {
      (this.forgetNewbornWorktreeRoot(e),
        await this.requestGitWorker({
          method: `delete-worktree`,
          params: {
            worktree: n.so(e),
            force: !0,
            reason: t,
            hostConfig: this.options.hostConfig,
            operationSource: j$(t),
          },
        }),
        this.options.windowManager.sendMessageToAllWindows({
          type: `worktrees-reload-requested`,
          hostId: this.options.hostId,
        }),
        this.runCleanup());
    }
    async setOwnerThread({ worktree: e, conversationId: t }) {
      let r = n.so(e);
      (await this.requestGitWorker({
        method: `set-worktree-owner-thread`,
        params: {
          worktree: r,
          conversationId: t,
          hostConfig: this.options.hostConfig,
          operationSource: `worktree_set_owner_thread`,
        },
      }),
        w$().info(`[worktree-create] owner-thread-set`, {
          safe: { worktreeId: await n.dt(r), ownerThreadId: t },
          sensitive: {},
        }),
        this.forgetNewbornWorktreeRoot(e));
    }
    requestCleanup() {
      this.runCleanup();
    }
    async runCleanup() {
      if (((this.cleanupPending = !0), !this.cleanupRunning)) {
        this.cleanupRunning = !0;
        try {
          for (; this.cleanupPending; ) {
            this.cleanupPending = !1;
            try {
              await this.runCleanupOnce();
            } catch (e) {
              w$().warning(`[worktree-cleanup] run-failed`, {
                safe: {},
                sensitive: { error: e },
              });
            }
          }
        } finally {
          this.cleanupRunning = !1;
        }
      }
    }
    async runCleanupOnce() {
      if (
        !_i(
          this.options.settingsStore,
          this.options.globalState,
          c.app.isPackaged,
        )
      )
        return;
      let e = this.options.executionHost,
        t = gi(this.options.settingsStore);
      await n.ot(e, t);
      let r = hi(this.options.settingsStore),
        { worktrees: i } = await this.requestGitWorker({
          method: `codex-worktrees`,
          params: {
            hostConfig: this.options.hostConfig,
            operationSource: `worktree_auto_cleanup`,
            worktreesRoot: t,
          },
        });
      if (i.length <= r) return;
      let a = yi({ globalState: this.options.globalState, worktrees: i }),
        o = fi(this.options.globalState),
        s = new Map(),
        l = await Promise.all(
          i.map(async (t) => {
            let r = null,
              i = !1;
            try {
              let i = await this.options.gitManager.getWorktreeRepository(
                n.oo(t.dir),
                e,
              );
              r = i == null ? null : ((await n.H(i))?.ownerThreadId ?? null);
            } catch {
              ((r = null), (i = !0));
            }
            s.set(String(t.dir), i);
            let a = null;
            try {
              let r = await n.ti.stat(t.dir, e);
              a = r.birthtimeMs > 0 ? r.birthtimeMs : null;
            } catch {
              a = null;
            }
            return { worktree: t, ownerThreadId: r, createdAtMs: a };
          }),
        ),
        u = Array.from(
          new Set([
            ...l.map((e) => e.ownerThreadId).filter((e) => e != null),
            ...o,
          ]),
        ),
        d = await di(this.options.appServerConnection, e, u);
      if (mi({ pinnedThreadIds: o, threadMetadataById: d })) {
        w$().info(
          `[worktree-cleanup] skip-pinned-thread-metadata-unavailable`,
          { safe: { pinnedThreadCount: o.length }, sensitive: {} },
        );
        return;
      }
      let f = pi({ pinnedThreadIds: o, threadMetadataById: d, worktrees: i }),
        p = Array.from(
          this.statesById.values(),
          (e) => e.entry.worktreeGitRoot,
        ).filter((e) => e != null),
        m = Array.from(this.newbornWorktreeRoots),
        h = n.V({
          records: l,
          keepCount: r,
          threadMetadataById: d,
          pinnedThreadIds: o,
          protectPreMigrationOwnerlessWorktrees: vi(this.options.globalState),
          protectedWorktreeDirs: Array.from(new Set([...a, ...f, ...p, ...m])),
        }),
        g = new Set(a),
        _ = h.candidates.filter((e) => !g.has(String(e.worktree.dir)));
      w$().info(`[worktree-cleanup] plan-summary`, {
        safe: {
          totalWorktrees: i.length,
          keepCount: r,
          protectedWorkspaceRootCount: a.length,
          protectedPinnedThreadCount: f.length,
          protectedPendingCount: p.length,
          protectedNewbornCount: m.length,
          ownerlessCount: l.filter((e) => e.ownerThreadId == null).length,
          candidatesCount: h.candidates.length,
          deletesPlanned: _.length,
        },
        sensitive: {},
      });
      let v = new Set([...a, ...f, ...p, ...m]),
        y = _.slice(0, 20);
      (await Promise.all(
        y.map(async (e) => {
          let t = String(e.worktree.dir),
            r = s.get(t) === !0,
            i = e.ownerThreadId == null ? null : (d[e.ownerThreadId] ?? null);
          w$().info(`[worktree-cleanup] delete-candidate`, {
            safe: {
              worktreeId: await n.dt(e.worktree.dir),
              reason: e.ownerThreadId == null ? `ownerless` : `owned`,
              ownerThreadId: e.ownerThreadId,
              ownerThreadUpdatedAtMs: i?.updatedAtMs ?? null,
              ownerThreadInProgress: i?.isInProgress ?? !1,
              createdAtMs: e.createdAtMs,
              ownerReadFailed: r,
              isProtected: v.has(t),
            },
            sensitive: {},
          });
        }),
      ),
        (
          await Promise.allSettled(
            _.map(async (e) => {
              await this.requestGitWorker({
                method: `delete-worktree`,
                params: {
                  worktree: e.worktree.dir,
                  force: !0,
                  reason: `startup-cleanup`,
                  hostConfig: this.options.hostConfig,
                  operationSource: `worktree_auto_cleanup`,
                },
              });
            }),
          )
        ).some((e) => e.status === `fulfilled`) &&
          this.options.windowManager.sendMessageToAllWindows({
            type: `worktrees-reload-requested`,
            hostId: this.options.hostId,
          }));
    }
    async handleCreateWorktreePath(e) {
      e.hostId === this.options.hostId &&
        (this.newbornWorktreeRoots.add(e.worktreeGitRoot),
        this.newbornWorktreeRootsByStreamId.set(e.streamId, e.worktreeGitRoot),
        w$().info(`[worktree-create] path-allocated`, {
          safe: {
            worktreeId: await n.dt(e.worktreeGitRoot),
            protectedNewbornCount: this.newbornWorktreeRoots.size,
          },
          sensitive: {},
        }));
    }
    forgetNewbornWorktreeStream(e) {
      let t = this.newbornWorktreeRootsByStreamId.get(e);
      t != null &&
        (this.newbornWorktreeRootsByStreamId.delete(e),
        this.newbornWorktreeRoots.delete(t));
    }
    forgetNewbornWorktreeRoot(e) {
      this.newbornWorktreeRoots.delete(e);
      for (let [t, n] of this.newbornWorktreeRootsByStreamId)
        n === e && this.newbornWorktreeRootsByStreamId.delete(t);
    }
    handleCreateWorktreeStream(e) {
      let t = Array.from(this.statesById.entries()).find(
        ([, t]) => t.runtime?.streamId === e.streamId,
      );
      if (!t) return;
      let [n, r] = t;
      if (r.runtime == null) return;
      let i = r.runtime.outputDecoder.decode(e.data, { stream: !0 });
      this.updateEntry(n, (e) => D$(e, i, e.phase));
    }
    handleCreateWorktreeSetupStarted(e) {
      let t = Array.from(this.statesById.entries()).find(
        ([, t]) => t.runtime?.streamId === e.streamId,
      );
      t &&
        this.updateEntry(t[0], (e) =>
          e.phase === `creating` ? { ...e, phase: `setting-up` } : e,
        );
    }
    requestGitWorker(e) {
      return this.options.requestGitWorker(e);
    }
    addWorkspaceRoot(e, t) {
      let n = (0, S$.default)([...it(this.options.globalState), e]);
      (ct(this.options.globalState, n),
        dt(this.options.globalState, (n) => ({ ...n, [e]: t })),
        rt(this.options.globalState).length === 0 &&
          (st(this.options.globalState, [e]),
          this.options.windowManager.sendMessageToAllWindows({
            type: `active-workspace-roots-updated`,
          })),
        this.options.windowManager.sendMessageToAllWindows({
          type: `workspace-root-options-updated`,
        }));
    }
    notifyFailure(e, t) {
      let r = this.options.settingsStore.getEffective(n.Ri.turnMode.key);
      if (r === `off`) return;
      let i = this.options.windowManager.getPrimaryWindow();
      if (!i || (r === `unfocused` && i.isFocused())) return;
      let a = {
        id: `worktree-init-failed-${e}`,
        kind: `turn-complete`,
        title: `Worktree setup failed`,
        body: t,
      };
      this.options.desktopNotificationManager.showNotification(
        a,
        i.webContents,
        () => {},
      );
    }
    publish() {
      let e = Array.from(this.statesById.values(), (e) => e.entry).sort(
        (e, t) => e.createdAt - t.createdAt,
      );
      this.options.setSharedPendingWorktrees(e);
    }
    updateEntry(e, t) {
      let n = this.statesById.get(e);
      n && ((n.entry = t(n.entry)), this.publish());
    }
  };
function E$(e, t) {
  if (!t) return e;
  let n = `${e}${t}`;
  return n.length <= C$ ? n : n.slice(-C$);
}
function D$(e, t, n) {
  return n === `setting-up`
    ? { ...e, setupOutputText: E$(e.setupOutputText, t) }
    : { ...e, worktreeOutputText: E$(e.worktreeOutputText, t) };
}
function O$(e) {
  return e instanceof Error && e.name === `AbortError`;
}
function k$(e, t) {
  let n = A$(e),
    r = A$(t);
  return r === `/` ? n === `/` : n === r || n.startsWith(`${r}/`);
}
function A$(e) {
  let t = n.at(e).replace(/\/+$/, ``);
  return t || (e.startsWith(`/`) ? `/` : t);
}
function j$(e) {
  switch (e) {
    case `settings-delete-targeted`:
      return `worktree_settings_delete`;
    case `archive-cleanup`:
      return `worktree_archive_cleanup`;
    case `startup-cleanup`:
      return `worktree_auto_cleanup`;
    case `new-branch-cleanup`:
      return `worktree_new_branch_cleanup`;
    case `remove-workspace-root-cleanup`:
      return `worktree_remove_workspace_root_cleanup`;
  }
}
var M$ = r.a(`electron-message-handler`),
  N$ = 560,
  P$ = 1090,
  F$ = 760,
  I$ = 100,
  L$ = `New project`,
  R$ = 5e3,
  z$ = 5 * 6e4,
  B$ = 5e3;
function V$(e) {
  return e.mode === `onboarding`
    ? e.onboardingVariant === `v2`
      ? { width: P$, height: F$ }
      : { width: N$, height: N$ }
    : null;
}
var H$ = {
    "thread-follower-start-turn": `thread-follower-start-turn-request`,
    "thread-follower-load-complete-history": `thread-follower-load-complete-history-request`,
    "thread-follower-compact-thread": `thread-follower-compact-thread-request`,
    "thread-follower-steer-turn": `thread-follower-steer-turn-request`,
    "thread-follower-interrupt-turn": `thread-follower-interrupt-turn-request`,
    "thread-follower-update-thread-settings": `thread-follower-update-thread-settings-request`,
    "thread-follower-edit-last-user-turn": `thread-follower-edit-last-user-turn-request`,
    "thread-follower-command-approval-decision": `thread-follower-command-approval-decision-request`,
    "thread-follower-file-approval-decision": `thread-follower-file-approval-decision-request`,
    "thread-follower-permissions-request-approval-response": `thread-follower-permissions-request-approval-response-request`,
    "thread-follower-submit-user-input": `thread-follower-submit-user-input-request`,
    "thread-follower-submit-mcp-server-elicitation-response": `thread-follower-submit-mcp-server-elicitation-response-request`,
    "thread-follower-set-queued-follow-ups-state": `thread-follower-set-queued-follow-ups-state-request`,
  },
  U$ = class {
    sharedObjectSubscribers = new Map();
    sharedObjectSubscriberOrigins = new WeakSet();
    host;
    primaryWindowRestoreBounds = null;
    primaryWindowMode = null;
    datadogLogger;
    maxLogLevel;
    powerSaveBlockerId = null;
    powerSaveBlockingWebContentsIds = new Set();
    pluggedInRemoteControlPowerSaveWebContentsIds = new Set();
    powerSaveTrackedWebContentsIds = new Set();
    pendingThreadFollowerStartTurnRequests = new Map();
    pendingThreadFollowerLoadCompleteHistoryRequests = new Map();
    pendingThreadFollowerCompactThreadRequests = new Map();
    pendingThreadFollowerSteerTurnRequests = new Map();
    pendingThreadFollowerInterruptTurnRequests = new Map();
    pendingThreadFollowerUpdateThreadSettingsRequests = new Map();
    pendingThreadFollowerEditLastUserTurnRequests = new Map();
    pendingThreadFollowerCommandApprovalDecisionRequests = new Map();
    pendingThreadFollowerFileApprovalDecisionRequests = new Map();
    pendingThreadFollowerPermissionsRequestApprovalResponseRequests = new Map();
    pendingThreadFollowerSubmitUserInputRequests = new Map();
    pendingThreadFollowerSubmitMcpServerElicitationRequests = new Map();
    pendingThreadFollowerSetQueuedFollowUpsStateRequests = new Map();
    pendingThreadRoleRequests = new Map();
    pendingDynamicToolsForThreadStartRequests = new Map();
    samplerManager;
    worktreeServicesByHostId = new Map();
    projectWritableRootsMutationQueue = Promise.resolve();
    requestGitWorker;
    subscribeGitWorkerEvent;
    browserSessionRegistry;
    browserSidebarManager;
    browserSidebarAutocompleteCoordinator;
    automationSchedulerController = null;
    onBrowserSidebarStateChanged = () => {};
    constructor(
      e,
      t,
      r,
      o,
      s,
      l,
      u,
      d,
      f,
      p,
      m,
      h,
      g,
      _,
      v,
      y,
      b,
      x,
      S,
      C,
      w,
      T,
      E,
      D = (e) => {
        if (e === this.hostId) return this.host;
        throw Error(`Host config for host ID ${e} not found`);
      },
      O,
      k,
    ) {
      ((this.hostId = e),
        (this.appServerClient = r),
        (this.windowManager = o),
        (this.avatarOverlayManager = s),
        (this.appServerConnectionRegistry = l),
        (this.fetchWrapper = u),
        (this.globalState = d),
        (this.sharedObjectRepository = f),
        (this.terminalManager = p),
        (this.gitManager = m),
        (this.desktopNotificationManager = g),
        (this.errorReporter = _),
        (this.desktopSentry = v),
        (this.updateWorkerSentryUser = y),
        (this.getIpcClientForWebContents = b),
        (this.inboxManager = x),
        (this.getHostConfigForHostId = D),
        (this.hasActiveComputerUseTurn = O),
        (this.settingsStore = k),
        (this.host = t),
        this.sharedObjectRepository.addSubscriber((e, t) => {
          this.sharedObjectSubscribers.get(e)?.forEach((n, r) => {
            this.sendSharedObjectUpdate(r, e, t);
          });
        }),
        (this.datadogLogger = S),
        (this.maxLogLevel = C),
        (this.requestGitWorker = h),
        (this.subscribeGitWorkerEvent = T),
        c.powerMonitor.on(`on-ac`, () => {
          this.syncPowerSaveBlocker();
        }),
        c.powerMonitor.on(`on-battery`, () => {
          this.syncPowerSaveBlocker();
        }),
        (this.browserSessionRegistry = new HX({
          appSessionId: i.i,
          buildFlavor: w,
          errorReporter: this.errorReporter,
        })));
      let A = c.session.fromPartition(kx(`app`));
      this.browserSidebarAutocompleteCoordinator = new Cx(
        () => fP(A),
        (e) => pP(A, e),
      );
      let j = this.globalState.get(a.g);
      ((this.browserSidebarManager = new PQ(
        this.windowManager,
        this.errorReporter,
        this.hostId,
        AI({ appServerClient: this.getAppServerConnection(this.hostId) }),
        this.browserSessionRegistry,
        void 0,
        {
          appServerNotificationSource: this.getAppServerConnection(this.hostId),
          browserPersistenceDirectory: c.app.getPath(`userData`),
          clearPersistedDownloadHistory: async () => {
            (this.globalState.delete(a.g),
              await this.globalState.flushOrThrow());
          },
          getDownloadDirectory: () =>
            this.settingsStore?.getEffective(n.Ui.downloadDirectory.key) ??
            c.app.getPath(`downloads`),
          persistedDownloads:
            j != null && !Array.isArray(j) ? j.downloads : void 0,
          productDownloadsEnabled: a.j(n.ea),
          persistDownloads: (e) => {
            this.globalState.set(a.g, { downloads: e, version: 1 });
          },
          getPromptForUserDownloads: () =>
            this.settingsStore?.getEffective(
              n.Ui.promptForDownloadLocation.key,
            ) ?? !1,
        },
      )),
        (this.samplerManager = new QQ(
          this.hostId,
          this.windowManager,
          this.desktopSentry,
          E,
          {
            getBrowserUseSnapshotFields: () =>
              this.browserSessionRegistry.getSnapshotFields(),
            getStdioIoStatsSnapshot: () =>
              this.getAppServerConnection(
                this.hostId,
              ).getStdioIoStatsSnapshot(),
          },
        )),
        this.ensureWorktreeService(this.hostId));
    }
    getWorktreeService(e = this.hostId) {
      return this.ensureWorktreeService(e);
    }
    setAutomationSchedulerController(e) {
      this.automationSchedulerController = e;
    }
    getBrowserSidebarManager() {
      return this.browserSidebarManager;
    }
    enrichPerformanceAppStateSnapshot(e, t) {
      return this.samplerManager.enrichPerformanceWithHostSnapshotFields(e, t);
    }
    getBrowserSidebarAutocompleteCoordinator() {
      return this.browserSidebarAutocompleteCoordinator;
    }
    setBrowserTabPersistenceEnabled(e, t) {
      let n = this.browserSidebarManager.setBrowserTabPersistenceEnabled(e, t);
      !t && n && this.clearBrowserTabPersistedAtomState(e);
    }
    deletePersistedBrowserConversation(
      e,
      { browserConversationId: t, conversationId: r },
    ) {
      this.browserSidebarManager.deletePersistedBrowserConversation(e, t) &&
        this.deletePersistedAtomStateKeys(e, [n.Vo(r), n.hi(r)]);
    }
    getBrowserSessionRegistry() {
      return this.browserSessionRegistry;
    }
    createProjectWritableRootsService(e) {
      return HQ({
        getProjectWritableRootsValue: () =>
          this.globalState.get(n.Gs.PROJECT_WRITABLE_ROOTS),
        getRootCompareKeys: (e) => UQ(ue(e, this.host)),
        getRootForStorage: (e) => z(e, this.shouldUseWslPaths()),
        notifyChanged: () => {
          this.windowManager.sendMessageToAllWindows({
            type: `global-state-updated`,
            keys: [n.Gs.PROJECT_WRITABLE_ROOTS],
          });
        },
        runSerializedMutation: (e) => this.runProjectWritableRootsMutation(e),
        selectWritableRoot: async (t) =>
          t == null
            ? ((await this.pickLocalWorkspaceRoots(e))[0] ?? null)
            : this.resolveWorkspaceRoot(t),
        setProjectWritableRootsValue: (e) => {
          this.globalState.set(n.Gs.PROJECT_WRITABLE_ROOTS, e);
        },
      });
    }
    runProjectWritableRootsMutation(e) {
      let t = this.projectWritableRootsMutationQueue.then(e, e);
      return (
        (this.projectWritableRootsMutationQueue = t.then(
          () => void 0,
          () => void 0,
        )),
        t
      );
    }
    setOnBrowserSidebarStateChanged(e) {
      ((this.onBrowserSidebarStateChanged = e),
        this.browserSidebarManager.setOnBrowserSidebarStateChanged(e));
    }
    getAppServerConnection(e) {
      return this.appServerConnectionRegistry.getConnection(e);
    }
    ensureWorktreeService(e) {
      let t = this.worktreeServicesByHostId.get(e);
      if (t) return t;
      let n = this.getAppServerConnection(e);
      return (
        (t = new T$({
          appServerConnection: n,
          desktopNotificationManager: this.desktopNotificationManager,
          globalState: this.globalState,
          settingsStore: this.getSettingsStore(),
          getHostConfigForHostId: this.getHostConfigForHostId,
          gitManager: this.gitManager,
          executionHost: e === this.hostId ? this.appServerClient : n,
          hostConfig: this.getHostConfigForHostId(e),
          hostId: e,
          requestGitWorker: this.requestGitWorker,
          setSharedPendingWorktrees: () => {
            this.publishPendingWorktrees();
          },
          subscribeGitWorkerEvent: this.subscribeGitWorkerEvent,
          windowManager: this.windowManager,
        })),
        this.worktreeServicesByHostId.set(e, t),
        this.publishPendingWorktrees(),
        t
      );
    }
    publishPendingWorktrees() {
      this.sharedObjectRepository.set(
        `pending_worktrees`,
        Array.from(this.worktreeServicesByHostId.values()).flatMap((e) =>
          e.getPendingWorktrees(),
        ),
      );
    }
    async handleMessage(e, t) {
      if (SI(t)) {
        await wI(this.avatarOverlayManager, e, t, (t) => {
          this.windowManager.sendMessageToWebContents(e, t);
        });
        return;
      }
      switch (t.type) {
        case `open-codex-bundle`:
          break;
        case `reload-bundled-plugins`:
          break;
        case `debug-window-origin-conversation-changed`:
          this.windowManager.debugWindowManager?.setOriginConversation(
            e,
            t.conversationId,
          );
          break;
        case `remote-hosted-pip-host-layout-changed`:
        case `remote-hosted-pip-active-thread-changed`:
        case `remote-hosted-pip-hidden-thread-ids-changed`:
          break;
        case `open-mcp-app-sandbox-devtools`:
          this.windowManager.openMcpAppSandboxDevtools(e, t.sandboxId);
          break;
        case `set-telemetry-user`: {
          this.updateWorkerSentryUser(
            t.authMethod,
            t.userId,
            t.email,
            t.accountId,
          );
          let e =
            t.authMethod == null
              ? null
              : { id: t.userId ?? void 0, authMethod: t.authMethod };
          (this.datadogLogger.setUserInfo(e),
            this.desktopSentry.setUser(
              e == null ? null : { ...e, account_id: t.accountId ?? void 0 },
            ));
          break;
        }
        case `ready`:
          (this.windowManager.markWebContentsReady(e),
            this.avatarOverlayManager.handleRendererReady(e.id),
            this.terminalManager.track(e));
          for (let t of this.appServerConnectionRegistry.getAllHostIds())
            this.getAppServerConnection(
              t,
            ).sendRequestUserInputAutoResolutionSnapshot(new RQ(e));
          (this.sendCustomPrompts(e).catch((e) => {
            this.errorReporter.reportNonFatal(
              e instanceof Error ? e : n.Rs(e),
              { kind: `send-custom-prompts` },
            );
          }),
            this.sendPersistedAtomState(e),
            this.samplerManager.handleRendererReady(e),
            M$().info(`Handled 'ready' message, sent ide-context-updated`));
          break;
        case `open-in-main-window`:
        case `open-browser-in-main-window`:
        case `open-current-main-window`:
        case `open-in-new-window`:
        case `mac-menu-bar-enabled-changed`:
        case `electron-avatar-overlay-feedback-diagnostics-changed`:
        case `electron-avatar-overlay-restore-ready`:
        case `electron-sparkle-gates-changed`:
        case `electron-desktop-features-changed`:
        case `global-dictation-enabled-changed`:
        case `global-dictation-force-lock-changed`:
        case `global-dictation-pet-renderer-ready`:
        case `global-dictation-renderer-ready`:
        case `global-dictation-stop-requested`:
        case `global-dictation-pointer-interaction-changed`:
        case `global-dictation-recording-stopped`:
        case `global-dictation-dismiss`:
        case `global-dictation-close`:
        case `global-dictation-completed`:
        case `global-dictation-record-history-item`:
        case `global-dictation-in-app-started`:
        case `global-dictation-failed`:
          break;
        case `power-save-blocker-set`:
          this.updatePowerSaveBlocker(
            e,
            t.shouldBlock,
            t.keepRemoteControlAwakeWhilePluggedIn,
          );
          break;
        case `local-thread-activity-changed`:
          Ab(t.hasInProgressLocalConversation, e.id);
          break;
        case `electron-set-window-mode`:
          this.setPrimaryWindowMode(e, {
            mode: t.mode,
            onboardingVariant: t.onboardingVariant,
          });
          break;
        case `inbox-item-set-read-state`:
          if (t.isRead) {
            let e = Date.now();
            this.inboxManager?.markRead(t.id, e);
            break;
          }
          this.inboxManager?.markUnread(t.id);
          break;
        case `inbox-automation-runs-mark-all-read`:
          this.inboxManager?.markAllAutomationRunsRead(t.readAt);
          break;
        case `inbox-items-create`: {
          if (!this.inboxManager) break;
          let e = Date.now(),
            n = t.conversationId || t.turnId || (0, f.randomUUID)(),
            r = t.items.map((r, i) => ({
              id: t.items.length > 1 && !r.id ? `${n}-${i + 1}` : r.id || n,
              automationId: null,
              automationName: null,
              title: r.title ?? null,
              description: r.description ?? null,
              archivedAssistantMessage: null,
              archivedUserMessage: null,
              archivedReason: null,
              sourceCwd: null,
              threadId: t.conversationId ?? null,
              readAt: null,
              createdAt: e,
              status: null,
            }));
          this.inboxManager.persistAndNotify(r);
          break;
        }
        case `archive-thread`:
          this.getAppServerConnection(t.hostId).registerArchiveThread(
            t.conversationId,
            {
              cwd: t.cwd,
              cleanupWorktree: t.cleanupWorktree,
              replacementOwnerThreadId: t.replacementOwnerThreadId,
              replacementOwnerCwd: t.replacementOwnerCwd,
            },
          );
          break;
        case `unarchive-thread`:
          this.getAppServerConnection(t.hostId).registerUnarchiveThread(
            t.conversationId,
          );
          break;
        case `thread-archived`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`thread-archived`, {
              hostId: t.hostId,
              conversationId: t.conversationId,
              cwd: t.cwd,
            }));
          break;
        }
        case `thread-unarchived`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`thread-unarchived`, {
              hostId: t.hostId,
              conversationId: t.conversationId,
            }));
          break;
        }
        case `thread-queued-followups-changed`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`thread-queued-followups-changed`, {
              conversationId: t.conversationId,
              messages: t.messages,
            }));
          break;
        }
        case `thread-stream-state-changed`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(
              `thread-stream-state-changed`,
              {
                conversationId: t.conversationId,
                hostId: t.hostId,
                change: t.change,
              },
              { targetClientIds: t.targetClientIds },
            ));
          break;
        }
        case `thread-stream-following-changed`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(
              `thread-stream-following-changed`,
              {
                conversationId: t.conversationId,
                hostId: t.hostId,
                following: t.following,
              },
              { targetClientIds: t.targetClientIds },
            ));
          break;
        }
        case `thread-stream-following-status-requested`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`thread-stream-following-status-requested`, {
              conversationId: t.conversationId,
              hostId: t.hostId,
            }));
          break;
        }
        case `thread-read-state-changed`: {
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`thread-read-state-changed`, {
              conversationId: t.conversationId,
              hasUnreadTurn: t.hasUnreadTurn,
            }));
          break;
        }
        case `query-cache-invalidate`: {
          t.queryKey[0] === `plugins` &&
            Sr(this.getAppServerConnection(this.hostId));
          let n = this.getIpcClientForWebContents(e);
          n &&
            (await n.sendBroadcast(`query-cache-invalidate`, {
              queryKey: t.queryKey,
            }));
          break;
        }
        case `thread-follower-start-turn-response`:
          this.handleThreadFollowerStartTurnResponse(e, t);
          break;
        case `thread-follower-load-complete-history-response`:
          this.handleThreadFollowerLoadCompleteHistoryResponse(e, t);
          break;
        case `thread-follower-compact-thread-response`:
          this.handleThreadFollowerCompactThreadResponse(e, t);
          break;
        case `thread-follower-steer-turn-response`:
          this.handleThreadFollowerSteerTurnResponse(e, t);
          break;
        case `thread-follower-interrupt-turn-response`:
          this.handleThreadFollowerInterruptTurnResponse(e, t);
          break;
        case `thread-follower-update-thread-settings-response`:
          this.handleThreadFollowerUpdateThreadSettingsResponse(e, t);
          break;
        case `thread-follower-edit-last-user-turn-response`:
          this.handleThreadFollowerEditLastUserTurnResponse(e, t);
          break;
        case `thread-follower-command-approval-decision-response`:
          this.handleThreadFollowerCommandApprovalDecisionResponse(e, t);
          break;
        case `thread-follower-file-approval-decision-response`:
          this.handleThreadFollowerFileApprovalDecisionResponse(e, t);
          break;
        case `thread-follower-permissions-request-approval-response-response`:
          this.handleThreadFollowerPermissionsRequestApprovalResponse(e, t);
          break;
        case `thread-follower-submit-user-input-response`:
          this.handleThreadFollowerSubmitUserInputResponse(e, t);
          break;
        case `thread-follower-submit-mcp-server-elicitation-response-response`:
          this.handleThreadFollowerSubmitMcpServerElicitationResponse(e, t);
          break;
        case `thread-follower-set-queued-follow-ups-state-response`:
          this.handleThreadFollowerSetQueuedFollowUpsStateResponse(e, t);
          break;
        case `thread-role-response`:
          this.handleThreadRoleResponse(e, t);
          break;
        case `dynamic-tools-for-thread-start-response`:
          this.handleDynamicToolsForThreadStartResponse(e, t);
          break;
        case `persisted-atom-sync-request`:
          this.sendPersistedAtomState(e);
          break;
        case `persisted-atom-update`:
          this.updatePersistedAtomState(e, t.key, t.deleted ? void 0 : t.value);
          break;
        case `browser-sidebar-owner-sync`:
          if (
            (M$().info(`IAB_LIFECYCLE received browser sidebar owner sync`, {
              safe: {
                browserTabId: t.browserTabId ?? null,
                conversationId: t.conversationId,
                originWebContentsId: e.id,
                windowId: c.BrowserWindow.fromWebContents(e)?.id ?? null,
              },
              sensitive: { ownerRoutePath: t.ownerRoutePath },
            }),
            this.getBrowserSidebarOwner(e) !== e)
          ) {
            M$().warning(
              `IAB_LIFECYCLE ignored browser sidebar owner sync from auxiliary window`,
              {
                safe: {
                  conversationId: t.conversationId,
                  originWebContentsId: e.id,
                  windowId: c.BrowserWindow.fromWebContents(e)?.id ?? null,
                },
                sensitive: {},
              },
            );
            break;
          }
          (this.browserSidebarManager.syncOwner(
            e,
            t.conversationId,
            t.browserTabId,
            t.multiTabBrowserUseEnabled === !0,
            t.ownerRoutePath,
            t.annotationModeEnabled,
            t.sessionConversationId,
          ),
            this.onBrowserSidebarStateChanged());
          break;
        case `browser-sidebar-sync`:
          (this.browserSidebarManager.sync(e, t.payload),
            this.onBrowserSidebarStateChanged());
          break;
        case `browser-settings-webview-mounted`:
          this.browserSidebarManager.browserSettingsWebviewMounted(
            e,
            t.mountId,
            t.themeVariant,
          );
          break;
        case `browser-settings-webview-theme-changed`:
          this.browserSidebarManager.browserSettingsWebviewThemeChanged(
            e,
            t.themeVariant,
          );
          break;
        case `browser-settings-webview-unmounted`:
          this.browserSidebarManager.browserSettingsWebviewUnmounted(
            e,
            t.mountId,
          );
          break;
        case `browser-sidebar-webview-destroyed`:
          (this.browserSidebarManager.browserSidebarWebviewDestroyed(
            e,
            t.conversationId,
            t.browserTabId,
            t.teardownId,
          ),
            this.onBrowserSidebarStateChanged());
          break;
        case `browser-use-session-route-capture`: {
          let n = this.getBrowserSidebarOwner(e);
          if (
            (M$().info(
              `IAB_LIFECYCLE received browser use session route capture`,
              {
                safe: {
                  conversationId: t.conversationId,
                  disposeAfterSessionActivity:
                    t.disposeAfterSessionActivity ?? !1,
                  originWebContentsId: e.id,
                  ownerWebContentsId: n?.id ?? null,
                  windowId: c.BrowserWindow.fromWebContents(e)?.id ?? null,
                },
                sensitive: {},
              },
            ),
            n == null)
          ) {
            M$().warning(
              `IAB_LIFECYCLE ignored browser use session route capture without primary window`,
              {
                safe: {
                  conversationId: t.conversationId,
                  originWebContentsId: e.id,
                },
                sensitive: {},
              },
            );
            break;
          }
          this.browserSessionRegistry.captureSessionRoute({
            browserConversationId: t.browserConversationId,
            conversationId: t.conversationId,
            disposeAfterSessionActivity: t.disposeAfterSessionActivity,
            multiTabBrowserUseEnabled: t.multiTabBrowserUseEnabled === !0,
            owner: n,
          });
          break;
        }
        case `browser-use-session-activity-ended`:
          this.browserSessionRegistry.endSessionActivity({
            conversationId: t.conversationId,
          });
          break;
        case `browser-use-cursor-arrived`:
          this.browserSessionRegistry.notifyBrowserUseCursorArrived({
            owner: e,
            payload: {
              conversationId: t.conversationId,
              moveSequence: t.moveSequence,
            },
          });
          break;
        case `open-browser-tab`:
          this.sendMessageToView(e, t);
          break;
        case `browser-sidebar-tweaks-enabled-changed`:
          this.browserSidebarManager.setTweaksFeatureEnabled(t.enabled);
          break;
        case `browser-sidebar-command`:
          switch (t.command.type) {
            case `navigate`: {
              let n = await LQ({
                appServerConnectionRegistry: this.appServerConnectionRegistry,
                getHostConfigForHostId: this.getHostConfigForHostId,
                hostId: t.command.hostId,
                url: t.command.url,
              });
              if (n == null) break;
              this.browserSidebarManager.navigate(
                e,
                t.conversationId,
                n,
                t.command.source,
                t.command.initiator,
                t.browserTabId,
                t.pagePersistence,
              );
              break;
            }
            case `capture-screenshot`:
              await this.browserSidebarManager.captureScreenshotToClipboard(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `set-interaction-mode`:
              this.browserSidebarManager.setInteractionMode(
                e,
                t.conversationId,
                t.command.interactionMode,
                t.browserTabId,
                t.command.annotationModeEntrySource,
              );
              break;
            case `set-design-modifier-pressed`:
              this.browserSidebarManager.setDesignModifierPressed(
                e,
                t.conversationId,
                t.command.pressed,
              );
              break;
            case `set-original-view-enabled`:
              this.browserSidebarManager.setOriginalViewEnabled(
                e,
                t.conversationId,
                t.command.enabled,
              );
              break;
            case `clear-comments`:
              this.browserSidebarManager.clearComments(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `discard-pending-annotations`:
              this.browserSidebarManager.discardPendingAnnotations(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `add-annotations-to-composer`:
              this.browserSidebarManager.addAnnotationsToComposer(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `close-tab`:
              this.browserSidebarManager.closePage(
                e,
                t.conversationId,
                t.browserTabId,
                t.pagePersistence?.browserStorageId,
              );
              break;
            case `go-back`:
              this.browserSidebarManager.goBack(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `go-forward`:
              this.browserSidebarManager.goForward(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `scroll`:
              this.browserSidebarManager.scroll(
                e,
                t.conversationId,
                t.command.scroll,
                t.browserTabId,
              );
              break;
            case `open-find`:
              this.browserSidebarManager.openFind(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `set-find-query`:
              this.browserSidebarManager.setFindQuery(
                e,
                t.conversationId,
                t.command.query,
                t.browserTabId,
              );
              break;
            case `find-next`:
              this.browserSidebarManager.findNext(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `find-previous`:
              this.browserSidebarManager.findPrevious(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `close-find`:
              this.browserSidebarManager.closeFind(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `focus-address`:
              break;
            case `refresh-cursor`:
              this.browserSidebarManager.refreshCursor(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `step-zoom`:
              this.browserSidebarManager.stepZoom(
                e,
                t.conversationId,
                t.command.delta,
                t.browserTabId,
              );
              break;
            case `set-zoom-percent`:
              this.browserSidebarManager.setZoomPercent(
                e,
                t.conversationId,
                t.command.zoomPercent,
                t.browserTabId,
              );
              break;
            case `reset-zoom`:
              this.browserSidebarManager.resetZoom(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `reload`:
              this.browserSidebarManager.reload(
                e,
                t.conversationId,
                { ignoreCache: t.command.ignoreCache },
                t.browserTabId,
              );
              break;
            case `print`:
              this.browserSidebarManager.print(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `reset`:
              this.browserSidebarManager.reset(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `stop`:
              this.browserSidebarManager.stop(
                e,
                t.conversationId,
                t.browserTabId,
              );
              break;
            case `select-comment`:
              this.browserSidebarManager.selectComment(
                e,
                t.conversationId,
                t.command.commentId,
                t.browserTabId,
              );
              break;
            case `transfer-conversation`:
              this.browserSidebarManager.transferConversation(
                e,
                t.conversationId,
                t.command.targetConversationId,
                t.browserTabId,
                t.command.targetBrowserTabId,
              );
              break;
          }
          break;
        case `browser-sidebar-runtime-open-comment-preview`:
          this.browserSidebarManager.handleRuntimeOpenCommentPreview(e, t);
          break;
        case `browser-sidebar-runtime-close-comment-preview`:
          this.browserSidebarManager.handleRuntimeCloseCommentPreview(e, t);
          break;
        case `browser-sidebar-runtime-open-editor`:
          await this.browserSidebarManager.handleRuntimeOpenEditor(e, t);
          break;
        case `browser-sidebar-runtime-open-design-editor`:
          await this.browserSidebarManager.handleRuntimeOpenDesignEditor(e, t);
          break;
        case `browser-sidebar-runtime-comment-screenshot-ready`:
          this.browserSidebarManager.handleRuntimeCommentScreenshotReady(e, t);
          break;
        case `browser-sidebar-runtime-update-anchor`:
          this.browserSidebarManager.handleRuntimeUpdateAnchor(e, t);
          break;
        case `browser-sidebar-runtime-focus-editor`:
          this.browserSidebarManager.handleRuntimeFocusEditor(e);
          break;
        case `browser-sidebar-runtime-exit-comment-mode`:
          this.browserSidebarManager.handleRuntimeExitCommentMode(e);
          break;
        case `browser-sidebar-runtime-design-modifier-state`:
          this.browserSidebarManager.handleRuntimeDesignModifierState(e, t);
          break;
        case `browser-sidebar-runtime-image-drag-started`:
          this.browserSidebarManager.handleRuntimeImageDragStarted(e, t);
          break;
        case `browser-sidebar-runtime-image-drag-ended`:
          this.browserSidebarManager.handleRuntimeImageDragEnded(e);
          break;
        case `browser-sidebar-runtime-mouse-navigation`:
          this.browserSidebarManager.handleRuntimeMouseNavigation(e, t);
          break;
        case `browser-sidebar-attach-dragged-image`:
          await this.browserSidebarManager.attachDraggedImageToThread(
            e,
            t.conversationId,
            t.browserTabId,
          );
          break;
        case `browser-sidebar-comment-overlay-submit`:
          await this.browserSidebarManager.handleCommentOverlaySubmit(e, t);
          break;
        case `browser-sidebar-design-overlay-update`:
          this.browserSidebarManager.handleDesignOverlayUpdate(e, t);
          break;
        case `browser-sidebar-design-overlay-add`:
          await this.browserSidebarManager.handleDesignOverlayAdd(e, t);
          break;
        case `browser-sidebar-design-overlay-delete`:
          this.browserSidebarManager.handleDesignOverlayDelete(e, t);
          break;
        case `browser-sidebar-comment-overlay-delete`:
          this.browserSidebarManager.handleCommentOverlayDelete(e, t);
          break;
        case `browser-sidebar-comment-overlay-mounted`:
          this.browserSidebarManager.handleCommentOverlayMounted(
            e,
            t.conversationId,
            t.browserTabId,
            t.sessionId,
            t.surfaceSize,
            t.placementSurfaceSize,
          );
          break;
        case `browser-sidebar-comment-overlay-preview-open-changed`:
          this.browserSidebarManager.handleCommentOverlayPreviewOpenChanged(
            e,
            t,
          );
          break;
        case `browser-sidebar-comment-overlay-tweaks-open-changed`:
          this.browserSidebarManager.handleCommentOverlayTweaksOpenChanged(
            e,
            t,
          );
          break;
        case `browser-sidebar-comment-overlay-design-scrub-changed`:
          this.browserSidebarManager.handleCommentOverlayDesignScrubChanged(
            e,
            t,
          );
          break;
        case `browser-sidebar-comment-overlay-close`:
          this.browserSidebarManager.handleCommentOverlayClose(
            e,
            t.conversationId,
            t.browserTabId,
            t.sessionId,
          );
          break;
        case `persisted-atom-reset`:
          this.resetPersistedAtomState();
          break;
        case `heartbeat-automations-enabled-changed`:
          this.automationSchedulerController?.setHeartbeatAutomationsEnabled(
            t.enabled,
          );
          break;
        case `codex-runtimes-config-changed`:
          this.sharedObjectRepository.set(`codex_runtimes_config`, t.config);
          break;
        case `heartbeat-automation-thread-state-changed`:
          this.automationSchedulerController?.setHeartbeatThreadRendererState({
            threadId: t.threadId,
            isEligible: t.isEligible,
            collaborationMode: t.collaborationMode,
            permissions: t.permissions,
            reason: t.reason,
          });
          break;
        case `codex-app-server-restart`: {
          let e = this.getAppServerConnection(t.hostId);
          (await e.restart({ killCodexProcess: t.killCodexProcess ?? !1 }),
            t.remoteControlEnabled &&
              (await e.sendAppServerRequest(`remoteControl/enable`, null)),
            M$().info(`Codex app-server restart requested`));
          break;
        }
        case `debug-run-eval-control-request`: {
          let n =
            this.windowManager.debugWindowManager?.getOwnerWebContentsId(
              e.id,
            ) ?? null;
          if (n != null) {
            this.windowManager.sendMessageToWebContentsId(n, {
              type: `debug-run-eval-control-request`,
              requestId: t.requestId,
              sourceWebContentsId: e.id,
              action: t.action,
            });
            break;
          }
          let r = c.BrowserWindow.fromWebContents(e),
            i =
              r != null && this.windowManager.getPrimaryWindows().includes(r)
                ? r
                : this.windowManager.getPrimaryWindow();
          if (i == null) {
            this.windowManager.sendMessageToWebContents(e, {
              type: `debug-run-eval-control-response`,
              requestId: t.requestId,
              ok: !1,
              errorMessage: `No primary app window is available`,
            });
            break;
          }
          this.windowManager.sendMessageToWindow(i, {
            type: `debug-run-eval-control-request`,
            requestId: t.requestId,
            sourceWebContentsId: e.id,
            action: t.action,
          });
          break;
        }
        case `debug-run-eval-control-response`:
          this.windowManager.sendMessageToWebContentsId(t.sourceWebContentsId, {
            type: `debug-run-eval-control-response`,
            requestId: t.requestId,
            ok: t.ok,
            result: t.result,
            errorMessage: t.errorMessage,
          });
          break;
        case `set-primary-runtime-install-release`:
          this.windowManager.sendMessageToAllWindows(t);
          break;
        case `toggle-trace-recording`:
          await _$();
          break;
        case `log-message`: {
          let { level: r } = t;
          if (!n.g(r, this.maxLogLevel)) break;
          M$().log(r, t.message, {
            safe: {
              ...t.tags?.safe,
              ...this.windowManager.getRendererWindowLogFields(e),
            },
            sensitive: t.tags?.sensitive ?? {},
          });
          break;
        }
        case `electron-app-state-snapshot-trigger`:
        case `electron-app-state-snapshot-response`:
          await this.samplerManager.handleMessage(e, t);
          break;
        case `shared-object-subscribe`: {
          this.sharedObjectSubscriberOrigins.has(e) ||
            (this.sharedObjectSubscriberOrigins.add(e),
            e.once(`destroyed`, () => {
              for (let [t, n] of this.sharedObjectSubscribers)
                (n.delete(e),
                  n.size === 0 && this.sharedObjectSubscribers.delete(t));
            }));
          let n = this.sharedObjectSubscribers.get(t.key) ?? new Map();
          (n.set(e, (n.get(e) ?? 0) + 1),
            this.sharedObjectSubscribers.set(t.key, n));
          let r = this.sharedObjectRepository.get(t.key);
          this.sendSharedObjectUpdate(e, t.key, r);
          break;
        }
        case `shared-object-unsubscribe`: {
          let n = this.sharedObjectSubscribers.get(t.key);
          if (n == null) break;
          let r = (n.get(e) ?? 0) - 1;
          if (r > 0) {
            n.set(e, r);
            break;
          }
          (n.delete(e),
            n.size === 0 && this.sharedObjectSubscribers.delete(t.key));
          break;
        }
        case `shared-object-set`:
          this.sharedObjectRepository.set(t.key, t.value);
          break;
        case `mcp-request`: {
          M$().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-request`,
              requestId: String(t.request.id),
              method: t.request.method,
              originWebcontentsId: e.id,
              originHostId: t.hostId,
            },
            sensitive: {},
          });
          let n = t.request;
          try {
            await this.getAppServerConnection(t.hostId).handleClientRequest(
              new RQ(e),
              n,
            );
          } catch (n) {
            this.sendMessageToView(e, {
              type: `mcp-response`,
              hostId: t.hostId,
              message: {
                id: t.request.id,
                error: {
                  code: -32e3,
                  message:
                    n instanceof Error
                      ? n.message
                      : `Desktop app server request failed`,
                },
              },
            });
          }
          break;
        }
        case `thread-prewarm-start`:
          (M$().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `thread-prewarm-start`,
              requestId: String(t.request.id),
              method: t.request.method,
              originWebcontentsId: e.id,
              originHostId: t.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(
              t.hostId,
            ).handlePrewarmThreadStart(new RQ(e), t.request));
          break;
        case `mcp-notification`:
          (M$().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-notification`,
              method: t.request.method,
              originWebcontentsId: e.id,
              originHostId: t.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(
              t.hostId,
            ).handleClientNotification(t.request));
          break;
        case `mcp-response`:
          (M$().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-response`,
              requestId: String(t.response.id),
              originWebcontentsId: e.id,
              originHostId: t.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(t.hostId).handleClientResponse(
              t.response,
            ));
          break;
        case `fetch`:
          await this.fetchWrapper.handleRequest(e, t);
          break;
        case `cancel-fetch`:
          this.fetchWrapper.cancelRequest(e, t);
          break;
        case `fetch-stream`:
          this.fetchWrapper.handleStreamRequest(e, t);
          break;
        case `cancel-fetch-stream`:
          this.fetchWrapper.cancelStreamRequest(e, t);
          break;
        case `pending-worktree-cancel`:
          this.getWorktreeService(t.hostId).cancel(t.id);
          break;
        case `pending-worktree-create`:
          this.getWorktreeService(t.hostId).create(t.request);
          break;
        case `pending-worktree-dismiss`:
          this.getWorktreeService(t.hostId).dismiss(t.id);
          break;
        case `pending-worktree-retry`:
          this.getWorktreeService(t.hostId).retry(t.id);
          break;
        case `pending-worktree-continue`:
          this.getWorktreeService(t.hostId).continueWithoutSetup(t.id);
          break;
        case `pending-worktree-update-metadata`:
          this.getWorktreeService(t.hostId).updateMetadata(t.id, t.update);
          break;
        case `show-diff`:
          this.windowManager.sendMessageToWebContents(e, {
            type: `toggle-diff-panel`,
            open: !0,
          });
          break;
        case `show-plan-summary`:
          break;
        case `update-diff-if-open`:
          break;
        case `electron-add-new-workspace-root-option`:
          await this.addWorkspaceRootOption(e, { root: t.root });
          break;
        case `electron-create-new-workspace-root-option`: {
          let n = await this.createDefaultWorkspaceRoot(t.projectName ?? L$);
          await this.addWorkspaceRootOption(e, { root: n });
          break;
        }
        case `electron-pick-workspace-root-option`:
          await this.addWorkspaceRootOption(e, {
            allowMultiple: t.allowMultiple,
            shouldPersist: !1,
          });
          break;
        case `electron-onboarding-skip-workspace`:
          await this.skipOnboardingWorkspace(e, t.projectName);
          break;
        case `electron-onboarding-pick-workspace-or-create-default`:
          await this.pickOnboardingWorkspaceOrCreateDefault(
            e,
            t.defaultProjectName,
          );
          break;
        case `electron-update-workspace-root-options`: {
          let n = de(t.roots, this.host);
          (ct(this.globalState, n),
            dt(this.globalState, (e) => {
              let t = fe(e, this.host);
              if (Object.keys(t).length === 0) return;
              let r = {};
              return (
                n.forEach((e) => {
                  t[e] !== void 0 && (r[e] = t[e]);
                }),
                r
              );
            }));
          let r = rt(this.globalState),
            i = r.filter((e) => n.includes(e));
          (i.length === 0 && n.length > 0 && (i = [n[0]]),
            (r.length !== i.length || r.some((e, t) => e !== i[t])) &&
              (st(this.globalState, i),
              e.send(B, { type: `active-workspace-roots-updated` })),
            e.send(B, { type: `workspace-root-options-updated` }));
          break;
        }
        case `electron-rename-workspace-root-option`: {
          let n = ue(t.root, this.host),
            r = t.label.trim();
          if (!de(it(this.globalState), this.host).includes(n)) break;
          (dt(this.globalState, (e) => {
            let t = fe(e, this.host);
            return (r ? (t[n] = r) : delete t[n], t);
          }),
            e.send(B, { type: `workspace-root-options-updated` }));
          break;
        }
        case `electron-set-active-workspace-root`:
          try {
            let n = ue(t.root, this.host),
              r = rt(this.globalState),
              i = it(this.globalState),
              a = at(this.globalState),
              o = de(i, this.host),
              s = o.includes(n) ? o : [n, ...o],
              c = [n],
              l = fe(a, this.host),
              u = !(0, af.default)(r, c),
              d = !(0, af.default)(i, s),
              f = !(0, af.default)(a, l);
            if (!u && !d && !f) break;
            (u && st(this.globalState, c),
              d && ct(this.globalState, s),
              f && dt(this.globalState, () => l),
              e.send(B, { type: `active-workspace-roots-updated` }));
          } catch (e) {
            M$().error(`Failed to set active workspace root`, {
              safe: {},
              sensitive: { error: e },
            });
          }
          break;
        case `electron-clear-active-workspace-root`:
          if (rt(this.globalState).length === 0) break;
          (st(this.globalState, []),
            e.send(B, { type: `active-workspace-roots-updated` }));
          break;
        case `open-in-browser`:
          await IA({
            request: t,
            targetWebContents: e,
            settingsStore: this.settingsStore,
            queueCodexDeepLinkUrl:
              this.windowManager.queueCodexDeepLinkUrl.bind(this.windowManager),
          });
          break;
        case `electron-set-badge-count`:
          c.app.setBadgeCount(t.count);
          break;
        case `view-focused`:
        case `quit-app`:
        case `tray-menu-threads-changed`:
          break;
        case `keyboard-layout-map-changed`:
          tA(t.keyboardLayoutMap);
          break;
        case `app-shell-shortcut-state-changed`:
          this.windowManager.setAppShellShortcutState(e.id, {
            bottomPanelBrowserCanZoom: t.bottomPanelBrowserCanZoom,
            bottomPanelBrowserConversationId:
              t.bottomPanelBrowserConversationId,
            bottomPanelBrowserTabId: t.bottomPanelBrowserTabId,
            canAcceptAppshotShortcut: t.canAcceptAppshotShortcut,
            bottomPanelCanCloseActiveTab: t.bottomPanelCanCloseActiveTab,
            focusArea: t.focusArea,
            imagePreviewOpen: t.imagePreviewOpen,
            isNewChatRoute: t.isNewChatRoute,
            terminalFocused: t.terminalFocused,
            rightPanelBrowserCanZoom: t.rightPanelBrowserCanZoom,
            rightPanelBrowserConversationId: t.rightPanelBrowserConversationId,
            rightPanelBrowserTabId: t.rightPanelBrowserTabId,
            rightPanelCanCloseActiveTab: t.rightPanelCanCloseActiveTab,
          });
          break;
        case `subagent-thread-opened`:
          this.getAppServerConnection(t.hostId).markSubagentThreadOpened(
            t.conversationId,
          );
          break;
        case `electron-window-focus-request`: {
          let t = c.BrowserWindow.fromWebContents(e);
          this.windowManager.sendMessageToWebContents(e, {
            type: `electron-window-focus-changed`,
            isFocused: t?.isFocused() ?? !1,
          });
          break;
        }
        case `electron-window-zoom-changed`:
          this.windowManager.setWindowZoom(e, t.windowZoom);
          break;
        case `worker-request`:
        case `worker-request-cancel`:
          break;
        case `navigate-in-new-editor-tab`:
        case `open-vscode-command`:
        case `open-extension-settings`:
        case `open-keyboard-shortcuts`:
        case `open-config-toml`:
        case `show-settings`:
        case `install-wsl`:
          throw Error(`"${t.type}" is not implemented in Electron.`);
        case `terminal-create`:
        case `terminal-attach`:
          await this.terminalManager.createOrAttach(e, t);
          break;
        case `terminal-write`:
          this.terminalManager.write(e, t.sessionId, t.data);
          break;
        case `terminal-run-action`:
          this.terminalManager.runAction(e, t.sessionId, {
            cwd: t.cwd,
            command: t.command,
          });
          break;
        case `terminal-resize`:
          this.terminalManager.resize(e, t.sessionId, t.cols, t.rows);
          break;
        case `terminal-close`:
          this.terminalManager.close(e, t.sessionId);
          break;
      }
    }
    setPrimaryWindowMode(e, t) {
      let n = c.BrowserWindow.fromWebContents(e);
      if (!n || n.isDestroyed()) return;
      let r = this.windowManager.getPrimaryWindow();
      if (!r || r.isDestroyed() || r.id !== n.id) return;
      let i = V$(t);
      if ((0, af.default)(this.primaryWindowMode, t)) {
        i != null && this.showPrimaryWindow(n);
        return;
      }
      if (((this.primaryWindowMode = t), i != null)) {
        ((this.primaryWindowRestoreBounds ||= {
          bounds: n.getNormalBounds(),
          wasMaximized: n.isMaximized(),
          wasFullScreen: n.isFullScreen(),
        }),
          n.isFullScreen() && n.setFullScreen(!1),
          n.isMaximized() && n.unmaximize(),
          n.setResizable(!0));
        let e = c.screen.getDisplayMatching(n.getNormalBounds()).workAreaSize,
          t =
            process.platform === `win32`
              ? {
                  width: Math.max(
                    0,
                    n.getBounds().width - n.getContentBounds().width,
                  ),
                  height: Math.max(
                    0,
                    n.getBounds().height - n.getContentBounds().height,
                  ),
                }
              : { width: 0, height: 0 },
          r = {
            width: Math.min(i.width, e.width - t.width),
            height: Math.min(i.height, e.height - t.height),
          },
          a = this.windowManager.getPrimaryMinimumSize();
        (n.setMaximizable(!1),
          n.setFullScreenable(!1),
          n.setMinimumSize(
            Math.min(a.width, r.width),
            Math.min(a.height, r.height),
          ),
          n.setSize(r.width, r.height),
          n.center(),
          this.showPrimaryWindow(n));
        return;
      }
      if (
        (n.setResizable(!0),
        n.setMaximizable(!0),
        n.setFullScreenable(!0),
        this.primaryWindowRestoreBounds)
      ) {
        let {
          bounds: e,
          wasMaximized: t,
          wasFullScreen: r,
        } = this.primaryWindowRestoreBounds;
        this.primaryWindowRestoreBounds = null;
        let i = this.windowManager.getPrimaryMinimumSize();
        n.setMinimumSize(i.width, i.height);
        let a = {
          ...e,
          width: Math.max(e.width, i.width),
          height: Math.max(e.height, i.height),
        };
        (n.setBounds(a), t && n.maximize(), r && n.setFullScreen(!0));
      }
      (this.windowManager.syncPrimaryMinimumSize(), this.showPrimaryWindow(n));
    }
    showPrimaryWindow(e) {
      e.isVisible() || (e.show(), e.focus());
    }
    flushTelemetry() {
      this.datadogLogger.flushNow();
    }
    dispose() {
      this.samplerManager.dispose();
    }
    sendMessageToOrigin(e, t) {
      this.windowManager.sendMessageToWebContents(e, t);
    }
    async getThreadRole(e, t) {
      return new Promise((r, i) => {
        let a = (0, f.randomUUID)(),
          o = setTimeout(() => {
            (this.pendingThreadRoleRequests.delete(a),
              i(Error(`thread-role-timeout`)));
          }, R$);
        this.pendingThreadRoleRequests.set(a, {
          originId: e.id,
          resolve: r,
          reject: i,
          timeout: o,
        });
        let s = {
          type: `thread-role-request`,
          hostId: this.hostId,
          requestId: n.dc(a),
          conversationId: t,
        };
        this.sendMessageToOrigin(e, s);
      });
    }
    requestDynamicToolsForThreadStart(e, t = !1) {
      let n = this.windowManager.getPrimaryWindow();
      return n == null
        ? Promise.resolve([])
        : new Promise((r) => {
            let i = (0, f.randomUUID)(),
              a = setTimeout(() => {
                (this.pendingDynamicToolsForThreadStartRequests.delete(i),
                  r([]));
              }, B$);
            (this.pendingDynamicToolsForThreadStartRequests.set(i, {
              originId: n.webContents.id,
              resolve: r,
              timeout: a,
            }),
              this.sendMessageToOrigin(n.webContents, {
                type: `dynamic-tools-for-thread-start-requested`,
                hostId: this.hostId,
                requestId: i,
                featureOverrides: e,
                responseTarget: `electron`,
                mode: `default`,
                threadStartKind: `default`,
                threadToolsEnabled: t,
                useDefaultFeatureOverrides: !0,
              }));
          });
    }
    forwardThreadFollowerRequest(e, t, r, i, a = R$) {
      return new Promise((o, s) => {
        let c = setTimeout(() => {
          (r.delete(t.requestId), s(Error(i)));
        }, a);
        r.set(t.requestId, {
          originId: e.id,
          resolve: o,
          reject: s,
          timeout: c,
        });
        let l = {
          type: H$[t.method],
          hostId: this.hostId,
          requestId: n.dc(t.requestId),
          params: t.params,
        };
        this.sendMessageToOrigin(e, l);
      });
    }
    async handleThreadFollowerStartTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerStartTurnRequests,
        `thread-follower-start-turn-timeout`,
      );
    }
    async handleThreadFollowerLoadCompleteHistoryRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerLoadCompleteHistoryRequests,
        `thread-follower-load-complete-history-timeout`,
        z$,
      );
    }
    async handleThreadFollowerCompactThreadRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerCompactThreadRequests,
        `thread-follower-compact-thread-timeout`,
      );
    }
    async handleThreadFollowerSteerTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSteerTurnRequests,
        `thread-follower-steer-turn-timeout`,
      );
    }
    async handleThreadFollowerInterruptTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerInterruptTurnRequests,
        `thread-follower-interrupt-turn-timeout`,
      );
    }
    async handleThreadFollowerUpdateThreadSettingsRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerUpdateThreadSettingsRequests,
        `thread-follower-update-thread-settings-timeout`,
      );
    }
    async handleThreadFollowerEditLastUserTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerEditLastUserTurnRequests,
        `thread-follower-edit-last-user-turn-timeout`,
      );
    }
    async handleThreadFollowerCommandApprovalDecisionRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerCommandApprovalDecisionRequests,
        `thread-follower-command-approval-decision-timeout`,
      );
    }
    async handleThreadFollowerFileApprovalDecisionRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerFileApprovalDecisionRequests,
        `thread-follower-file-approval-decision-timeout`,
      );
    }
    async handleThreadFollowerPermissionsRequestApprovalResponseRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests,
        `thread-follower-permissions-request-approval-response-timeout`,
      );
    }
    async handleThreadFollowerSubmitUserInputRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSubmitUserInputRequests,
        `thread-follower-submit-user-input-timeout`,
      );
    }
    async handleThreadFollowerSubmitMcpServerElicitationRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSubmitMcpServerElicitationRequests,
        `thread-follower-submit-mcp-server-elicitation-response-timeout`,
      );
    }
    async handleThreadFollowerSetQueuedFollowUpsStateRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSetQueuedFollowUpsStateRequests,
        `thread-follower-set-queued-follow-ups-state-timeout`,
      );
    }
    rejectPendingThreadFollowerActionRequestsForOrigin(e) {
      for (let [t, n] of this.pendingThreadFollowerStartTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerStartTurnRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerLoadCompleteHistoryRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerLoadCompleteHistoryRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerCompactThreadRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerCompactThreadRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerSteerTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSteerTurnRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerInterruptTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerInterruptTurnRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerUpdateThreadSettingsRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerUpdateThreadSettingsRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerEditLastUserTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerEditLastUserTurnRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerCommandApprovalDecisionRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerCommandApprovalDecisionRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerFileApprovalDecisionRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerFileApprovalDecisionRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.delete(
            t,
          ));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerSubmitUserInputRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSubmitUserInputRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerSubmitMcpServerElicitationRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSubmitMcpServerElicitationRequests.delete(
            t,
          ));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.delete(t));
      for (let [t, n] of this.pendingThreadRoleRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadRoleRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingDynamicToolsForThreadStartRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.resolve([]),
          this.pendingDynamicToolsForThreadStartRequests.delete(t));
    }
    handleThreadFollowerStartTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerStartTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(`Unknown thread follower start-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerStartTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower start-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerLoadCompleteHistoryResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerLoadCompleteHistoryRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower load-complete-history response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerLoadCompleteHistoryRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(
          Error(`Missing thread follower load-complete-history response`),
        );
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerCompactThreadResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerCompactThreadRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower compact-thread response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerCompactThreadRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower compact-thread response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSteerTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSteerTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(`Unknown thread follower steer-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerSteerTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower steer-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerInterruptTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerInterruptTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower interrupt-turn response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerInterruptTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower interrupt-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerUpdateThreadSettingsResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerUpdateThreadSettingsRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower update-thread-settings response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerUpdateThreadSettingsRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(
          Error(`Missing thread follower update-thread-settings response`),
        );
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerEditLastUserTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerEditLastUserTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower edit-last-user-turn response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerEditLastUserTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower edit-last-user-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerCommandApprovalDecisionResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerCommandApprovalDecisionRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower command-approval response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerCommandApprovalDecisionRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower command-approval response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerFileApprovalDecisionResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerFileApprovalDecisionRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower file-approval response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerFileApprovalDecisionRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower file-approval response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerPermissionsRequestApprovalResponse(e, t) {
      let n = String(t.requestId),
        r =
          this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.get(
            n,
          );
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower permissions-request response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.delete(
          n,
        ),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower permissions-request response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSubmitUserInputResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSubmitUserInputRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower submit-user-input response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerSubmitUserInputRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower submit-user-input response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSubmitMcpServerElicitationResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSubmitMcpServerElicitationRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower submit-mcp-server-elicitation response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerSubmitMcpServerElicitationRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(
          Error(
            `Missing thread follower submit-mcp-server-elicitation response`,
          ),
        );
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSetQueuedFollowUpsStateResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(
          `Unknown thread follower queued-follow-ups response requestId`,
          { safe: { requestId: n }, sensitive: {} },
        );
        return;
      }
      if (
        (this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower queued-follow-ups response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadRoleResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadRoleRequests.get(n);
      if (!r || r.originId !== e.id) {
        M$().warning(`Unknown thread role response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadRoleRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      r.resolve(t.role);
    }
    handleDynamicToolsForThreadStartResponse(e, t) {
      let n = this.pendingDynamicToolsForThreadStartRequests.get(t.requestId);
      if (!n || n.originId !== e.id) {
        M$().warning(`Unknown dynamic tools response requestId`, {
          safe: { requestId: t.requestId },
          sensitive: {},
        });
        return;
      }
      (this.pendingDynamicToolsForThreadStartRequests.delete(t.requestId),
        clearTimeout(n.timeout),
        n.resolve(t.dynamicTools));
    }
    sendPersistedAtomState(e) {
      let t = this.globalState.get(`electron-persisted-atom-state`) ?? {},
        r = this.browserSidebarManager.isBrowserTabPersistenceOwner(e)
          ? t
          : Object.fromEntries(
              Object.entries(t).filter(([e]) => !e.startsWith(n.Io)),
            );
      this.sendMessageToView(e, { type: `persisted-atom-sync`, state: r });
    }
    updatePersistedAtomState(e, t, n) {
      if (
        t.startsWith(`thread-browser-tabs-v1:`) &&
        !this.browserSidebarManager.isBrowserTabPersistenceOwner(e)
      )
        return;
      let r = {
        ...(this.globalState.get(`electron-persisted-atom-state`) ?? {}),
      };
      (n === void 0 ? delete r[t] : (r[t] = n),
        this.globalState.set(`electron-persisted-atom-state`, r),
        this.broadcastPersistedAtomUpdate(e, t, n));
    }
    broadcastPersistedAtomUpdate(e, t, n) {
      let r = n === void 0,
        i = {
          type: `persisted-atom-updated`,
          key: t,
          value: r ? null : n,
          deleted: r,
        };
      if (!t.startsWith(`thread-browser-tabs-v1:`) || r) {
        this.windowManager.sendMessageToAllWindows(i);
        return;
      }
      let a = c.BrowserWindow.fromWebContents(e);
      a != null && this.windowManager.sendMessageToWindow(a, i);
    }
    clearBrowserTabPersistedAtomState(e) {
      let t = this.globalState.get(`electron-persisted-atom-state`) ?? {};
      this.deletePersistedAtomStateKeys(
        e,
        Object.keys(t).filter((e) => e.startsWith(n.Io)),
      );
    }
    deletePersistedAtomStateKeys(e, t) {
      let n = this.globalState.get(`electron-persisted-atom-state`) ?? {},
        r = t.filter((e) => e in n);
      if (r.length === 0) return;
      let i = { ...n };
      for (let e of r) delete i[e];
      this.globalState.set(`electron-persisted-atom-state`, i);
      for (let t of r) this.broadcastPersistedAtomUpdate(e, t, void 0);
    }
    resetPersistedAtomState() {
      (this.globalState.set(`electron-persisted-atom-state`, {}),
        this.windowManager.sendMessageToAllWindows({
          type: `persisted-atom-sync`,
          state: {},
        }));
    }
    async skipOnboardingWorkspace(e, t) {
      let n = null;
      try {
        ((n = await this.createDefaultWorkspaceRoot(t ?? L$)),
          this.setOnboardingWorkspace(e, n),
          e.send(B, {
            type: `electron-onboarding-skip-workspace-result`,
            success: !0,
            root: n,
          }),
          this.navigateToOnboardingWorkspace(e));
      } catch (t) {
        M$().error(`Failed to skip onboarding workspace selection`, {
          safe: {},
          sensitive: { error: t },
        });
        let r = t instanceof Error ? t.message : String(t);
        e.send(B, {
          type: `electron-onboarding-skip-workspace-result`,
          success: !1,
          root: n ?? void 0,
          error: r,
        });
      }
    }
    async pickOnboardingWorkspaceOrCreateDefault(e, t) {
      let n = null,
        r = `picked`;
      try {
        ((n = (await this.pickLocalWorkspaceRoots(e))[0] ?? null),
          (n ??=
            ((r = `created_default`),
            await this.createDefaultWorkspaceRoot(t))),
          this.setOnboardingWorkspace(e, n),
          e.send(B, {
            type: `electron-onboarding-pick-workspace-or-create-default-result`,
            success: !0,
            source: r,
            root: n,
          }),
          this.navigateToOnboardingWorkspace(e));
      } catch (t) {
        M$().error(`Failed onboarding workspace pick/create`, {
          safe: {},
          sensitive: { error: t },
        });
        let i = t instanceof Error ? t.message : String(t);
        e.send(B, {
          type: `electron-onboarding-pick-workspace-or-create-default-result`,
          success: !1,
          source: r,
          root: n ?? void 0,
          error: i,
        });
      }
    }
    setOnboardingWorkspace(e, t) {
      let n = de([t, ...it(this.globalState)], this.host);
      (ct(this.globalState, n),
        lt(this.globalState, t),
        dt(this.globalState, (e) => fe(e, this.host)),
        e.send(B, { type: `workspace-root-options-updated` }),
        st(this.globalState, [ue(t, this.host)]),
        e.send(B, { type: `active-workspace-roots-updated` }));
    }
    navigateToOnboardingWorkspace(e) {
      e.send(B, {
        type: `navigate-to-route`,
        path: `/`,
        state: { focusComposerNonce: Date.now() },
      });
    }
    async pickLocalWorkspaceRoots(e, t = !1) {
      if (this.host.id !== `local`)
        throw Error(`Workspace picker is only supported for local hosts`);
      let n = [`openDirectory`, `createDirectory`];
      (t && n.push(`multiSelections`),
        (await this.shouldShowHiddenFilesInPicker()) &&
          n.push(`showHiddenFiles`));
      let r = { properties: n, title: `Select Project Root` },
        i = c.BrowserWindow.fromWebContents(e),
        a =
          i == null
            ? await c.dialog.showOpenDialog(r)
            : await c.dialog.showOpenDialog(i, r);
      return a.canceled
        ? []
        : (
            await Promise.all(
              a.filePaths.map((e) => this.resolveWorkspaceRoot(e)),
            )
          ).filter((e) => e != null);
    }
    sanitizeDefaultProjectName(e) {
      let t = u.basename(e).trim();
      return t.length === 0 || t === `.` || t === `..` ? L$ : t;
    }
    async findAvailableDefaultWorkspaceRoot(e, t, n) {
      let r = n == null ? t : `${t} ${n}`,
        i = u.join(e, r);
      if (!(await this.folderExists(i))) return i;
      let a = n == null ? 2 : n + 1;
      return this.findAvailableDefaultWorkspaceRoot(e, t, a);
    }
    async createDefaultWorkspaceRoot(e) {
      if (n.eo(this.host))
        throw Error(
          `Creating a default workspace is only supported for local hosts`,
        );
      let t = this.sanitizeDefaultProjectName(e),
        r = await this.findAvailableDefaultWorkspaceRoot(
          c.app.getPath(`documents`),
          t,
          void 0,
        );
      (await n.ti.mkdir(r, { recursive: !0 }, this.appServerClient),
        this.getSettingsStore().getEffective(
          n.Ii.conversationDetailMode.key,
        ) !== `STEPS_PROSE` && (await this.initializeDefaultWorkspace(r)));
      let i = fe(at(this.globalState), this.host),
        a = de(it(this.globalState), this.host).some(
          (e) => (i[e]?.trim() || u.basename(e).trim()) === t,
        );
      return (
        u.basename(r) !== t &&
          !a &&
          dt(this.globalState, (e) => ({ ...fe(e, this.host), [r]: t })),
        r
      );
    }
    async folderExists(e) {
      try {
        return (await n.ti.stat(e, this.appServerClient), !0);
      } catch {
        return !1;
      }
    }
    async initializeDefaultWorkspace(e) {
      try {
        let t = this.appServerClient;
        if (!(await n.mt(e, [`version`], t)).success) return;
        let r = await n.mt(e, [`init`], t);
        if (r.success) {
          this.gitManager.invalidateStableMetadata();
          return;
        }
        let i = r.stderr || r.stdout || `Failed to run git init`;
        M$().warning(`Failed to initialize default workspace git repository`, {
          safe: {},
          sensitive: { message: i },
        });
      } catch (e) {
        M$().warning(`Failed to initialize default workspace git repository`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async addWorkspaceRootOption(
      e,
      { allowMultiple: t = !1, root: n, shouldPersist: r = !0 } = {},
    ) {
      if (this.host.id !== `local`) {
        this.windowManager.sendMessageToWebContents(e, {
          type: `remote-workspace-root-requested`,
          mode: r ? `add` : `pick`,
          host: this.host.display_name,
          hostId: this.host.id,
          setActive: r ? !0 : void 0,
        });
        return;
      }
      let i = n
        ? [await this.resolveWorkspaceRoot(n)].filter((e) => e != null)
        : await this.pickLocalWorkspaceRoots(e, t);
      if (!r) {
        for (let t of i)
          e.send(B, {
            type: `workspace-root-option-picked`,
            root: z(t, this.shouldUseWslPaths()),
          });
        return;
      }
      let a = i[0];
      if (a == null) return;
      let o = de(it(this.globalState), this.host),
        s = o.includes(a) ? o : [a, ...o];
      (s.length !== o.length &&
        (ct(this.globalState, s),
        lt(this.globalState, a),
        e.send(B, { type: `workspace-root-options-updated` })),
        st(this.globalState, [a]),
        e.send(B, { type: `active-workspace-roots-updated` }),
        e.send(B, {
          type: `workspace-root-option-added`,
          root: z(a, this.shouldUseWslPaths()),
        }),
        e.send(B, {
          type: `navigate-to-route`,
          path: `/`,
          state: { focusComposerNonce: Date.now() },
        }));
    }
    async resolveWorkspaceRoot(e) {
      try {
        let t = ue(e, this.host);
        return (await (0, m.stat)(t)).isDirectory() ? u.resolve(t) : null;
      } catch (e) {
        return (
          M$().warning(`Failed to stat workspace root`, {
            safe: {},
            sensitive: { error: e },
          }),
          null
        );
      }
    }
    async shouldShowHiddenFilesInPicker() {
      return (await this.readHiddenFilesPreference()) ?? !1;
    }
    async readHiddenFilesPreference() {
      return process.platform === `darwin`
        ? this.readMacHiddenFilesPreference()
        : null;
    }
    async readMacHiddenFilesPreference() {
      let e = await this.readCommandOutput([
        `defaults`,
        `read`,
        `com.apple.finder`,
        `AppleShowAllFiles`,
      ]);
      if (e == null) return null;
      let t = e.trim().toLowerCase();
      return t === `1` || t === `true` || t === `yes`
        ? !0
        : t === `0` || t === `false` || t === `no`
          ? !1
          : null;
    }
    async readCommandOutput(e) {
      let t = new AbortController(),
        r = !1,
        i = setTimeout(() => {
          ((r = !0), t.abort());
        }, I$);
      try {
        let { stdout: i, code: a } = await n
          .Yn({ args: e, signal: t.signal })
          .wait();
        return r || a !== 0 ? null : i;
      } catch (e) {
        return (
          r ||
            M$().warning(`Failed to read hidden files preference`, {
              safe: {},
              sensitive: { error: e },
            }),
          null
        );
      } finally {
        clearTimeout(i);
      }
    }
    revealWindowsReviewRequest(e, t) {
      if (!W$(t, this.hasActiveComputerUseTurn)) return;
      let r = this.getPrimaryWindow();
      r != null &&
        (M$().info(`[windows-review-request] reveal requested`, {
          safe: {
            conversationId: t.params.threadId,
            hostId: e,
            method: t.method,
            requestId: t.id,
            wasMinimized: r.isMinimized(),
          },
          sensitive: {},
        }),
        r.webContents.isDestroyed() ||
          this.windowManager.sendMessageToWebContents(r.webContents, {
            type: `navigate-to-route`,
            path: n._a(n.uc(t.params.threadId)),
          }),
        this.restoreShowAndFocusWindow(r, { stealFocus: !0 }));
    }
    getPrimaryWindow() {
      let e = this.windowManager.getPrimaryWindow();
      return e == null || e.isDestroyed() ? null : e;
    }
    getBrowserSidebarOwner(e) {
      let t = c.BrowserWindow.fromWebContents(e);
      return t != null && this.windowManager.getPrimaryWindows().includes(t)
        ? e
        : (this.getPrimaryWindow()?.webContents ?? null);
    }
    restoreShowAndFocusWindow(e, { stealFocus: t }) {
      (e.isMinimized() && e.restore(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!0),
        e.show(),
        e.focus(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!1));
    }
    updatePowerSaveBlocker(e, t, n) {
      let r = e.id;
      (this.powerSaveTrackedWebContentsIds.has(r) ||
        (this.powerSaveTrackedWebContentsIds.add(r),
        e.once(`destroyed`, () => {
          (this.powerSaveTrackedWebContentsIds.delete(r),
            this.powerSaveBlockingWebContentsIds.delete(r),
            this.pluggedInRemoteControlPowerSaveWebContentsIds.delete(r),
            this.syncPowerSaveBlocker());
        })),
        t
          ? this.powerSaveBlockingWebContentsIds.add(r)
          : this.powerSaveBlockingWebContentsIds.delete(r),
        n
          ? this.pluggedInRemoteControlPowerSaveWebContentsIds.add(r)
          : this.pluggedInRemoteControlPowerSaveWebContentsIds.delete(r),
        this.syncPowerSaveBlocker());
    }
    syncPowerSaveBlocker() {
      let e =
        this.powerSaveBlockingWebContentsIds.size > 0 ||
        (!c.powerMonitor.isOnBatteryPower() &&
          this.pluggedInRemoteControlPowerSaveWebContentsIds.size > 0);
      if (e && this.powerSaveBlockerId == null) {
        this.powerSaveBlockerId = c.powerSaveBlocker.start(
          `prevent-app-suspension`,
        );
        return;
      }
      !e &&
        this.powerSaveBlockerId != null &&
        (c.powerSaveBlocker.stop(this.powerSaveBlockerId),
        (this.powerSaveBlockerId = null));
    }
    async sendCustomPrompts(e) {
      let t = await n.p();
      this.sendMessageToView(e, { type: `custom-prompts-updated`, prompts: t });
    }
    sendMessageToView(e, t) {
      e.isDestroyed() || e.send(B, t);
    }
    sendSharedObjectUpdate(e, t, n) {
      e.isDestroyed() ||
        this.sendMessageToView(e, {
          type: `shared-object-updated`,
          key: t,
          value: n,
        });
    }
    shouldUseWslPaths() {
      return n.ui(this.host);
    }
    getSettingsStore() {
      if (this.settingsStore == null) throw Error(`Settings store unavailable`);
      return this.settingsStore;
    }
  };
function W$(e, t) {
  return process.platform !== `win32` ||
    (e.method !== `item/commandExecution/requestApproval` &&
      e.method !== `mcpServer/elicitation/request`) ||
    e.params.turnId == null
    ? !1
    : t({ sessionId: e.params.threadId, turnId: e.params.turnId });
}
function G$(e) {
  return new Promise((t, n) => {
    let r = new C.Worker(
      (0, u.join)(__dirname, `child-process-snapshot-worker.js`),
      { name: `child-process-snapshot`, workerData: e },
    );
    (r.once(`message`, (e) => {
      e.type === `ok` ? t(e.value) : n(Error(e.error.message));
    }),
      r.once(`error`, n),
      r.unref());
  });
}
var K$ = r.a(`electron-sampler`),
  q$ = 5,
  J$ = 5,
  Y$ = 5,
  X$ = 5,
  Z$ = 5,
  Q$ = class {
    previousDescendantProcessSnapshot = null;
    previousRendererWorkingSetKbTotal = null;
    constructor(e) {
      this.options = e;
    }
    async collectSnapshotFields(e) {
      let t = process.memoryUsage(),
        n = {
          main_process_rss_bytes: t.rss,
          main_process_heap_total_bytes: t.heapTotal,
          main_process_heap_used_bytes: t.heapUsed,
          main_process_external_bytes: t.external,
          main_process_array_buffers_bytes: t.arrayBuffers,
          system_free_memory_bytes: (0, l.freemem)(),
          renderer_webcontents_id: e.id,
        },
        r = e.getOSProcessId();
      try {
        let t = c.app.getAppMetrics(),
          i = t.find((e) => e.pid === process.pid),
          a = t.find((e) => e.pid === r);
        (i && this.addMainProcessFields(n, i),
          a && this.addRendererMemoryFields(n, a),
          this.addRendererProcessAggregateFields(n, e, t));
      } catch (e) {
        K$().warning(`Failed to collect renderer process memory snapshot`, {
          safe: {},
          sensitive: { error: e },
        });
      }
      let i = this.options.gitWorkerInvocationSampler.getSnapshot();
      return (
        (n.git_worker_invocations_total = i.totalInvocations),
        (n.git_worker_invocations_last_30s = i.invocationsLast30s),
        await this.addChildProcessFields(n),
        n
      );
    }
    async collectPerformanceSnapshotFields(e) {
      let t = await this.collectSnapshotFields(e);
      return Object.fromEntries(
        Object.entries(t).filter(
          ([e, t]) =>
            !e.includes(`command`) &&
            (typeof t != `string` ||
              e.endsWith(`_kind`) ||
              e.includes(`_process_kind_`)),
        ),
      );
    }
    addMainProcessFields(e, t) {
      let n = t.cpu;
      n &&
        `percentCPUUsage` in n &&
        (e.main_process_cpu_percent = n.percentCPUUsage);
    }
    addRendererMemoryFields(e, t) {
      let n = t.cpu;
      n &&
        `percentCPUUsage` in n &&
        (e.renderer_process_cpu_percent = n.percentCPUUsage);
      let r = t.memory;
      r &&
        (`workingSetSize` in r &&
          (e.renderer_process_working_set_kb = r.workingSetSize),
        `peakWorkingSetSize` in r &&
          (e.renderer_process_peak_working_set_kb = r.peakWorkingSetSize),
        `privateBytes` in r &&
          (e.renderer_process_private_bytes_kb = r.privateBytes),
        `sharedBytes` in r &&
          (e.renderer_process_shared_bytes_kb = r.sharedBytes));
    }
    addRendererProcessAggregateFields(e, t, n) {
      let r = c.webContents.getAllWebContents().filter((e) => !e.isDestroyed()),
        i = new Map();
      for (let e of r) {
        let t = e.getOSProcessId();
        if (t <= 0) continue;
        let n = i.get(t) ?? [];
        (n.push(e), i.set(t, n));
      }
      let a = new Map(n.map((e) => [e.pid, e])),
        o = [];
      for (let [e, n] of i) {
        let r = a.get(e)?.memory;
        r == null ||
          !(`workingSetSize` in r) ||
          o.push({
            hasCurrentWebContents: n.some((e) => e.id === t.id),
            peakWorkingSetKb:
              `peakWorkingSetSize` in r ? r.peakWorkingSetSize : null,
            webContentsCount: n.length,
            workingSetKb: r.workingSetSize,
          });
      }
      let s = o.reduce((e, t) => e + t.workingSetKb, 0);
      ((e.renderer_process_count_total = o.length),
        (e.renderer_webcontents_count_total = r.length),
        (e.renderer_process_working_set_kb_total = s),
        this.previousRendererWorkingSetKbTotal != null &&
          (e.renderer_process_working_set_kb_delta_since_last_snapshot =
            s - this.previousRendererWorkingSetKbTotal),
        (this.previousRendererWorkingSetKbTotal = s),
        o
          .sort((e, t) => t.workingSetKb - e.workingSetKb)
          .slice(0, Z$)
          .forEach((t, n) => {
            let r = n + 1;
            ((e[`renderer_process_rank_${r}_working_set_kb`] = t.workingSetKb),
              (e[`renderer_process_rank_${r}_webcontents_count`] =
                t.webContentsCount),
              (e[`renderer_process_rank_${r}_has_current_webcontents`] =
                t.hasCurrentWebContents),
              t.peakWorkingSetKb != null &&
                (e[`renderer_process_rank_${r}_peak_working_set_kb`] =
                  t.peakWorkingSetKb));
          }));
    }
    async addChildProcessFields(e) {
      try {
        let t = await (process.platform === `win32`
            ? G$(process.pid)
            : n.On(process.pid)),
          r = n.An(t),
          a = r.byKind.find((e) => e.kind === `app_server`) ?? null,
          o = r.byKind.find((e) => e.kind === `git`) ?? null;
        ((e.host_child_process_count_total = r.totalCount),
          (e.host_child_process_rss_kb_total = r.totalRssKb),
          (e.host_child_process_count_direct = r.directCount),
          (e.host_child_process_rss_kb_direct = r.directRssKb),
          (e.host_descendant_process_count_total = r.totalCount),
          (e.host_descendant_process_rss_kb_total = r.totalRssKb),
          (e.host_descendant_process_count_direct = r.directCount),
          (e.host_descendant_process_rss_kb_direct = r.directRssKb),
          (e.host_descendant_process_max_depth = t.reduce(
            (e, t) => Math.max(e, t.depth),
            0,
          )),
          (e.host_child_git_process_count = o?.count ?? 0),
          (e.host_child_git_process_rss_kb = o?.rssKb ?? 0),
          (e.host_child_app_server_process_count = a?.count ?? 0),
          (e.host_child_app_server_rss_kb = a?.rssKb ?? 0),
          r.oldestAgeSeconds != null &&
            ((e.host_child_process_oldest_age_seconds = r.oldestAgeSeconds),
            (e.host_descendant_process_oldest_age_seconds =
              r.oldestAgeSeconds)),
          this.previousDescendantProcessSnapshot != null &&
            ((e.host_descendant_process_count_delta_since_last_snapshot =
              r.totalCount - this.previousDescendantProcessSnapshot.count),
            (e.host_descendant_process_rss_kb_delta_since_last_snapshot =
              r.totalRssKb - this.previousDescendantProcessSnapshot.rssKb)),
          (this.previousDescendantProcessSnapshot = {
            count: r.totalCount,
            rssKb: r.totalRssKb,
          }),
          r.byKind.slice(0, q$).forEach((t, n) => {
            let r = n + 1;
            ((e[`host_descendant_process_kind_${r}`] = t.kind),
              (e[`host_descendant_process_kind_${r}_count`] = t.count),
              (e[`host_descendant_process_kind_${r}_rss_kb`] = t.rssKb),
              (e[`host_descendant_process_kind_${r}_direct_count`] =
                t.directCount),
              (e[`host_descendant_process_kind_${r}_direct_rss_kb`] =
                t.directRssKb));
          }));
        for (let t of r.byKind)
          ((e[`host_descendant_${t.kind}_process_count`] = t.count),
            (e[`host_descendant_${t.kind}_rss_kb`] = t.rssKb),
            (e[`host_descendant_${t.kind}_direct_process_count`] =
              t.directCount),
            (e[`host_descendant_${t.kind}_direct_rss_kb`] = t.directRssKb));
        (i.a.isInternal(this.options.buildFlavor) && $$(e, t),
          e1(e, t, i.a.isInternal(this.options.buildFlavor)));
        let s = new Map(t.map((e) => [e.pid, e.kind]));
        t.filter((e) => e.rssKb != null)
          .sort((e, t) => (t.rssKb ?? 0) - (e.rssKb ?? 0))
          .slice(0, Y$)
          .forEach((t, n) => {
            let r = n + 1;
            ((e[`host_descendant_process_rank_${r}_kind`] = t.kind),
              (e[`host_descendant_process_rank_${r}_rss_kb`] = t.rssKb ?? 0),
              (e[`host_descendant_process_rank_${r}_depth`] = t.depth),
              (e[`host_descendant_process_rank_${r}_root_kind`] =
                s.get(t.rootChildPid) ?? t.kind),
              i.a.isInternal(this.options.buildFlavor) &&
                (e[`host_descendant_process_rank_${r}_command_name`] = t1(
                  t.command,
                )),
              t.ageSeconds != null &&
                (e[`host_descendant_process_rank_${r}_age_seconds`] =
                  t.ageSeconds));
          });
      } catch (e) {
        K$().warning(`Failed to collect child process snapshot`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
  };
function $$(e, t) {
  let n = new Map();
  for (let e of t) {
    let t = t1(e.command),
      r = n.get(t) ?? { commandName: t, count: 0, rssKb: 0 };
    ((r.count += 1), (r.rssKb += e.rssKb ?? 0), n.set(t, r));
  }
  Array.from(n.values())
    .sort((e, t) =>
      t.rssKb === e.rssKb
        ? t.count === e.count
          ? e.commandName.localeCompare(t.commandName)
          : t.count - e.count
        : t.rssKb - e.rssKb,
    )
    .slice(0, J$)
    .forEach((t, n) => {
      let r = n + 1;
      ((e[`host_descendant_process_command_${r}`] = t.commandName),
        (e[`host_descendant_process_command_${r}_count`] = t.count),
        (e[`host_descendant_process_command_${r}_rss_kb`] = t.rssKb));
    });
}
function e1(e, t, n) {
  let r = new Map(t.filter((e) => e.depth === 1).map((e) => [e.pid, e])),
    i = new Map();
  for (let e of t) {
    let t = r.get(e.rootChildPid);
    if (t == null) continue;
    let n = i.get(e.rootChildPid) ?? {
      commandName: t1(t.command),
      count: 0,
      kind: t.kind,
      rssKb: 0,
    };
    ((n.count += 1), (n.rssKb += e.rssKb ?? 0), i.set(e.rootChildPid, n));
  }
  Array.from(i.values())
    .sort((e, t) =>
      t.rssKb === e.rssKb
        ? t.count === e.count
          ? e.kind.localeCompare(t.kind)
          : t.count - e.count
        : t.rssKb - e.rssKb,
    )
    .slice(0, X$)
    .forEach((t, r) => {
      let i = r + 1;
      ((e[`host_descendant_root_${i}_kind`] = t.kind),
        (e[`host_descendant_root_${i}_process_count`] = t.count),
        (e[`host_descendant_root_${i}_rss_kb`] = t.rssKb),
        n && (e[`host_descendant_root_${i}_command_name`] = t.commandName));
    });
}
function t1(e) {
  let t = /^(?:"([^"]+)"|'([^']+)'|(\S+))/.exec(e.trim()),
    n = t?.[1] ?? t?.[2] ?? t?.[3];
  return n == null
    ? `unknown`
    : (0, u.basename)(n.replaceAll(`\\`, `/`)).replace(/^\((.+)\)$/, `$1`);
}
var n1 = class {
    hosts = new Map();
    constructor(e = V) {
      this.defaultHostId = e;
    }
    add(e, t) {
      this.hosts.set(e, t);
    }
    get(e = this.defaultHostId) {
      let t = this.getMaybe(e);
      if (t == null)
        throw Error(`App server client for host ID ${e} not found`);
      return t;
    }
    getMaybe(e = this.defaultHostId) {
      return this.hosts.get(e);
    }
    remove(e) {
      this.hosts.delete(e);
    }
  },
  r1 = new Map([
    [`image/gif`, `gif`],
    [`image/jpeg`, `jpg`],
    [`image/png`, `png`],
    [`image/webp`, `webp`],
  ]),
  i1 = class extends a.P {
    constructor(e) {
      (super(), (this.executionHostRegistry = e));
    }
    countFolderFiles({ folderPath: e, hostId: t }) {
      return n.At(e, this.executionHostRegistry.get(t));
    }
    async persistImageFileToTemp({ bytes: e, mimeType: t }) {
      let n = r1.get(t);
      if (n == null) return null;
      let r = (0, u.join)(
        (0, l.tmpdir)(),
        `codex-clipboard-${(0, f.randomUUID)()}.${n}`,
      );
      return (await (0, m.writeFile)(r, e, { flag: `wx`, mode: 384 }), r);
    }
  },
  a1;
function o1(e) {
  if (process.platform !== `darwin`) return !1;
  try {
    return s1()?.startFileDrag(e) === !0;
  } catch {
    return !1;
  }
}
function s1() {
  if (a1 !== void 0) return a1;
  let e = St({ env: process.env, resourcesPath: process.resourcesPath });
  try {
    a1 = Ea({ electronAppPath: c.app.getAppPath(), resourcesPath: e });
  } catch {
    a1 = null;
  }
  return a1;
}
var c1 = class extends a.P {
  constructor(e, t) {
    (super(), (this.getAppServerClient = e), (this.globalState = t));
  }
  async downloadCopy({ hostId: e, path: t }) {
    let r = this.getAppServerClient(e);
    if (n.eo(r.hostConfig))
      throw Error(`Downloading a copy is only supported for local files`);
    let i = await h1(t, r, this.globalState),
      a = c.app.getPath(`downloads`),
      o = u.parse(i);
    for (let e = 0; ; e += 1) {
      let t = e === 0 ? o.base : `${o.name} (${e})${o.ext}`,
        s = u.join(a, t);
      try {
        (await n.ti.copyFile(i, s, r, { exclusive: !0 }), OA(s));
        return;
      } catch (e) {
        if (typeof e != `object` || !e || !(`code` in e) || e.code !== `EEXIST`)
          throw e;
      }
    }
  }
  async getDownloadsFolderIcon() {
    let e = c.app.getPath(`downloads`),
      t = await c.app.getFileIcon(e, { size: `normal` });
    if (t.isEmpty())
      throw Error(`Could not load the system Downloads folder icon`);
    return t.toDataURL();
  }
  async read({ hostId: e, path: t, representation: r }) {
    let i = this.getAppServerClient(e),
      a = await h1(t, i, this.globalState);
    if (r === `text`) {
      let [e, t] = await Promise.all([
        n.ti.readFile(a, i),
        n.ti.stat(a, i, { bypassCache: !0 }),
      ]);
      return { etag: p1(t), text: e };
    }
    let [o, s] = await Promise.all([
        n.ti.readFileBase64(a, i),
        n.ti.stat(a, i, { bypassCache: !0 }),
      ]),
      c = d1(o),
      l = p1(s);
    switch (r) {
      case `auto`:
        return { ...(await l1(c)), etag: l };
      case `blob`:
        return { blob: u1(o), etag: l };
    }
  }
  async writeIfMatch({ bytes: e, hostId: t, ifMatch: r, path: i }) {
    if (n.mc(e.byteLength)) return { maxBytes: n.pc, outcome: `too-large` };
    let a = this.getAppServerClient(t),
      o = await h1(i, a, this.globalState),
      s = p1(await f1(o, a));
    return r === s
      ? (await n.ti.writeFileBuffer(o, Buffer.from(e), a),
        { etag: p1(await f1(o, a)), outcome: `saved` })
      : { etag: s, outcome: `conflict` };
  }
};
async function l1(e) {
  return (await n.do(e.subarray(0, 4096))) === `text`
    ? { text: e.toString(`utf8`) }
    : { blob: e.toString(`base64`) };
}
function u1(e) {
  return typeof e == `string` ? e : e.toString(`base64`);
}
function d1(e) {
  return typeof e == `string` ? Buffer.from(e, `base64`) : e;
}
async function f1(e, t) {
  try {
    return await n.ti.stat(e, t, { bypassCache: !0 });
  } catch (e) {
    if (m1(e, `ENOENT`)) return null;
    throw e;
  }
}
function p1(e) {
  return e == null
    ? `missing`
    : `stat:${(0, f.createHash)(`sha256`).update(`${e.mtimeMs}:${e.ctimeMs}:${e.size}`).digest(`base64url`)}`;
}
function m1(e, t) {
  return e instanceof Error && `code` in e && e.code === t;
}
async function h1(e, t, r) {
  return n.eo(t.hostConfig) ? _1(e, null, await t.platformPath()) : g1(e, t, r);
}
function g1(e, t, r) {
  return _1(re(e, n.ui(t.hostConfig)), rt(r)[0] ?? it(r)[0] ?? null);
}
function _1(e, t, r = u) {
  let i = r.normalize(r.sep === `\\` ? n.Ws(e) : e);
  if (n._(i, r) || t == null) return i;
  let a = i.split(r.sep).filter(Boolean);
  if (a.length === 0) return t;
  let o = r.join(t, ...a);
  if ((0, p.existsSync)(o)) return o;
  let s = a.indexOf(r.basename(t));
  return s === -1 ? o : r.join(t, ...a.slice(s + 1));
}
var v1 = n.xl({ hostId: n.wl().trim().min(1), path: n.wl().min(1) }),
  y1 = class extends a.P {
    pendingFilePath = null;
    preparedFileDrag = null;
    constructor(e, t, n) {
      (super(),
        (this.executionHostRegistry = e),
        (this.globalState = t),
        (this.origin = n));
    }
    async prepareDrag(e) {
      let t = null;
      try {
        if (
          ((t = this.#e(e)),
          t == null ||
            this.preparedFileDrag?.filePath === t ||
            this.pendingFilePath === t ||
            (0, p.statSync)(t, { throwIfNoEntry: !1 })?.isFile() !== !0)
        )
          return;
        ((this.preparedFileDrag = null), (this.pendingFilePath = t));
        let n = await c.app.getFileIcon(t, { size: `normal` });
        this.pendingFilePath === t &&
          (this.preparedFileDrag = { filePath: t, icon: n });
      } catch {
        this.pendingFilePath === t && (this.preparedFileDrag = null);
      } finally {
        this.pendingFilePath === t && (this.pendingFilePath = null);
      }
    }
    startDrag = (e) => {
      try {
        let t = this.#e(e);
        return t == null ||
          (0, p.statSync)(t, { throwIfNoEntry: !1 })?.isFile() !== !0
          ? !1
          : this.origin.startDrag == null
            ? o1(t)
            : this.preparedFileDrag?.filePath === t
              ? (this.origin.startDrag({
                  file: t,
                  icon: this.preparedFileDrag.icon,
                }),
                !0)
              : !1;
      } catch {
        return !1;
      }
    };
    #e(e) {
      let t = v1.safeParse(e);
      if (!t.success) return null;
      let { hostId: r, path: i } = t.data,
        a = this.executionHostRegistry.get(r);
      return n.eo(a.hostConfig) ? null : g1(i, a, this.globalState);
    }
  },
  b1 = class extends a.P {
    constructor(e, t) {
      (super(), (this.lifecycleManager = e), (this.origin = t));
    }
    getState() {
      return this.lifecycleManager.getState();
    }
    setEnabled(e) {
      this.lifecycleManager.setGateEnabled(e);
    }
    open(e) {
      return this.lifecycleManager.openFromRenderer(this.origin, e);
    }
    collapseToHome() {
      return this.lifecycleManager.collapseToHome();
    }
    dismiss() {
      this.lifecycleManager.dismiss();
    }
    setHotkey(e) {
      return this.lifecycleManager.setHotkey(e);
    }
    setDevOverrideEnabled(e) {
      return this.lifecycleManager.setDevOverrideEnabled(e);
    }
    transitionDone(e) {
      this.lifecycleManager.handleTransitionDone(this.origin, e);
    }
    homePointerInteractionChanged(e) {
      this.lifecycleManager.handleHomePointerInteractionChanged(this.origin, e);
    }
    homeLayoutChanged(e) {
      this.lifecycleManager.handleHomeLayoutChanged(this.origin, e);
    }
    homeDragStart(e) {
      this.lifecycleManager.handleHomeDragStart(this.origin, e);
    }
    homeDragMove() {
      this.lifecycleManager.handleHomeDragMove(this.origin);
    }
    homeDragEnd() {
      this.lifecycleManager.handleHomeDragEnd(this.origin);
    }
  },
  x1 = class extends a.P {
    watchMetaRelease() {
      return new Promise((e) => {
        Ik([`command`], e) ?? e();
      });
    }
  };
function S1(e, t) {
  let n = u.default.relative(t, e);
  return (
    n !== `` &&
    n !== `..` &&
    !n.startsWith(`..${u.default.sep}`) &&
    !u.default.isAbsolute(n)
  );
}
var C1 = r.a(`library-files`);
function w1(e, t) {
  return u.default.join(t(e), `generated_images`);
}
async function T1(e) {
  try {
    let t = await (0, m.lstat)(e);
    if (!t.isDirectory() || t.isSymbolicLink()) return [];
    let n = await (0, m.realpath)(e);
    return (await E1(n, n)).sort((e, t) => e.path.localeCompare(t.path));
  } catch (t) {
    return (
      C1().warning(`Failed to inspect generated images directory`, {
        safe: {},
        sensitive: { error: t, generatedImagesDirectory: e },
      }),
      []
    );
  }
}
async function E1(e, t) {
  try {
    let r = await (0, m.readdir)(e, { withFileTypes: !0 });
    return (
      await Promise.all(
        r.map(async (r) => {
          let i = u.default.join(e, r.name);
          try {
            let e = await (0, m.lstat)(i);
            if (e.isSymbolicLink()) return [];
            let a = await (0, m.realpath)(i);
            if (!S1(a, t)) return [];
            if (e.isDirectory()) return await E1(a, t);
            if (!e.isFile() || !n.Ei(r.name)) return [];
            let o = u.default.relative(t, a),
              s = o.indexOf(u.default.sep);
            return [
              {
                desktopPath: a,
                modifiedAt: e.mtime.toISOString(),
                name: r.name,
                path: a,
                relativePath: o,
                sizeBytes: e.size,
                threadId: s === -1 ? null : o.slice(0, s),
              },
            ];
          } catch (e) {
            return (
              C1().warning(`Failed to inspect generated image entry`, {
                safe: {},
                sensitive: { entryPath: i, error: e },
              }),
              []
            );
          }
        }),
      )
    ).flat();
  } catch (t) {
    return (
      C1().warning(`Failed to read generated images directory`, {
        safe: {},
        sensitive: { directory: e, error: t },
      }),
      []
    );
  }
}
var D1 = r.a(`library-files`);
function O1({
  configuredOutputDirectories: e,
  shouldUseWslPaths: t,
  threads: n,
  workspaceRoot: r,
}) {
  let i = {};
  for (let [n, r] of Object.entries(e ?? {})) i[n] = re(r, t);
  for (let r of n) {
    let n =
      e?.[r.id] == null
        ? u.default.join(re(r.cwd, t), `outputs`)
        : re(e[r.id], t);
    i[r.id] = n;
  }
  let a = Object.values(i).filter((e) => S1(u.default.resolve(e), r));
  return { allowedOutputDirectories: [...new Set(a)], outputDirectories: i };
}
async function k1({
  allowedOutputDirectories: e,
  outputDirectories: t,
  workspaceRoot: n,
}) {
  let r = new Map(),
    i = new Set(e.map(M1)),
    a = u.default.resolve(n),
    o;
  try {
    let e = await (0, m.lstat)(a);
    if (!e.isDirectory() || e.isSymbolicLink()) return [];
    o = await (0, m.realpath)(a);
  } catch (e) {
    return (
      D1().warning(`Failed to inspect projectless workspace root`, {
        safe: {},
        sensitive: { error: e, workspaceRootPath: a },
      }),
      []
    );
  }
  let s = new Map();
  for (let [e, n] of Object.entries(t)) {
    let t = M1(n),
      r = s.get(t);
    s.set(t, {
      outputDirectory: n,
      threadId: r == null || r.threadId === e ? e : null,
    });
  }
  for (let t of e) {
    let e = M1(t);
    s.set(e, s.get(e) ?? { outputDirectory: t, threadId: null });
  }
  let c = [...s.values()].filter(({ outputDirectory: e }) => i.has(M1(e))),
    l = await Promise.all(
      c.map(({ outputDirectory: e, threadId: t }) => A1(e, o, t)),
    );
  for (let e of l)
    for (let t of e) {
      let e = M1(t.path);
      r.has(e) || r.set(e, t);
    }
  return [...r.values()].sort((e, t) => e.path.localeCompare(t.path));
}
async function A1(e, t, n) {
  try {
    let r = await (0, m.lstat)(e);
    if (!r.isDirectory() || r.isSymbolicLink()) return [];
    let i = u.default.resolve(e),
      a = await (0, m.realpath)(i);
    return S1(a, t)
      ? await j1({
          directory: i,
          outputDirectory: i,
          outputDirectoryRealPath: a,
          threadId: n,
        })
      : [];
  } catch (t) {
    return (
      D1().warning(`Failed to inspect projectless output directory`, {
        safe: {},
        sensitive: { error: t, outputDirectory: e },
      }),
      []
    );
  }
}
async function j1({
  directory: e,
  outputDirectory: t,
  outputDirectoryRealPath: n,
  threadId: r,
}) {
  let i = await (0, m.readdir)(e, { withFileTypes: !0 });
  return (
    await Promise.all(
      i.map(async (i) => {
        let a = u.default.join(e, i.name);
        try {
          let e = await (0, m.lstat)(a);
          return e.isSymbolicLink() || !S1(await (0, m.realpath)(a), n)
            ? []
            : e.isDirectory()
              ? await j1({
                  directory: a,
                  outputDirectory: t,
                  outputDirectoryRealPath: n,
                  threadId: r,
                })
              : e.isFile()
                ? [
                    {
                      modifiedAt: e.mtime.toISOString(),
                      name: i.name,
                      path: a,
                      relativePath: u.default.relative(t, a),
                      sizeBytes: e.size,
                      threadId: r,
                    },
                  ]
                : [];
        } catch (e) {
          return (
            D1().warning(`Failed to inspect projectless output entry`, {
              safe: {},
              sensitive: { entryPath: a, error: e },
            }),
            []
          );
        }
      }),
    )
  ).flat();
}
function M1(e) {
  let t = u.default.normalize(e);
  return process.platform === `win32` ? t.toLowerCase() : t;
}
var N1 = class {
    generatedImagesDirectory;
    shouldUseWslPaths;
    constructor(e, t) {
      ((this.appServerConnection = e),
        (this.globalState = t),
        (this.shouldUseWslPaths = n.ui(sd)),
        (this.generatedImagesDirectory = w1(
          n.ni({ preferWsl: this.shouldUseWslPaths }),
          (e) => this.convertPathForDesktopHost(e),
        )));
    }
    convertPathForDesktopHost(e) {
      return re(e, this.shouldUseWslPaths);
    }
    async getAllowedLibraryDirectories() {
      let { allowedOutputDirectories: e } =
        await this.loadProjectlessOutputDirectories(this.getWorkspaceRoot());
      return [...e, this.generatedImagesDirectory];
    }
    async listGeneratedImages() {
      return (await T1(this.generatedImagesDirectory)).map((e) => ({
        ...e,
        path: this.convertPathForAgentEnvironment(e.path),
      }));
    }
    async listOutputFiles() {
      let e = this.getWorkspaceRoot();
      return (
        await k1({
          ...(await this.loadProjectlessOutputDirectories(e)),
          workspaceRoot: e,
        })
      ).map((e) => ({
        ...e,
        path: this.convertPathForAgentEnvironment(e.path),
      }));
    }
    convertPathForAgentEnvironment(e) {
      return z(e, this.shouldUseWslPaths);
    }
    getWorkspaceRoot() {
      return n.Oa({ homeDirectory: (0, l.homedir)(), path: u.default });
    }
    async listLocalThreads() {
      let e = [],
        t = null;
      do {
        let n = await this.appServerConnection.listThreads({
          archived: !1,
          cursor: t,
          limit: 200,
          modelProviders: null,
          sortKey: `updated_at`,
        });
        (e.push(...n.data), (t = n.nextCursor));
      } while (t != null);
      return e;
    }
    async loadProjectlessOutputDirectories(e) {
      return O1({
        configuredOutputDirectories: this.globalState.get(
          n.Gs.THREAD_PROJECTLESS_OUTPUT_DIRECTORIES,
        ),
        shouldUseWslPaths: this.shouldUseWslPaths,
        threads: await this.listLocalThreads(),
        workspaceRoot: e,
      });
    }
  },
  P1 = class {
    allowedLibraryRealPathsPromise = null;
    previewPaths = new Set();
    constructor(e) {
      this.backend = e;
    }
    async prepareFilePreview(e) {
      let t = await this.openAllowedFile(e),
        n = null;
      try {
        n = await (0, m.mkdtemp)(
          u.default.join((0, l.tmpdir)(), `codex-library-preview-`),
        );
        let r = u.default.join(n, u.default.basename(e.replaceAll(`\\`, `/`)));
        return (
          await (0, x.pipeline)(
            t.createReadStream({ autoClose: !1 }),
            (0, p.createWriteStream)(r, { flags: `wx`, mode: 384 }),
          ),
          this.previewPaths.add(r),
          r
        );
      } catch {
        throw (
          n != null && (await (0, m.rm)(n, { force: !0, recursive: !0 })),
          Error(`Library file is unavailable`)
        );
      } finally {
        await t.close();
      }
    }
    async releaseFilePreview(e) {
      this.previewPaths.delete(e) &&
        (await (0, m.rm)(u.default.dirname(e), { force: !0, recursive: !0 }));
    }
    async openAllowedFile(e) {
      let t = this.backend.convertPathForDesktopHost(e),
        n,
        r;
      try {
        let [e, i] = await Promise.all([(0, m.lstat)(t), (0, m.realpath)(t)]);
        if (!e.isFile() || e.isSymbolicLink())
          throw Error(`Invalid Library file`);
        ((r = e), (n = i));
      } catch {
        throw Error(`Library file is unavailable`);
      }
      if (
        !(await this.getAllowedLibraryRealPaths()).some(
          (e) => e != null && S1(n, e),
        )
      )
        throw Error(`Library file is unavailable`);
      try {
        let e = await (0, m.open)(
          n,
          p.constants.O_RDONLY | p.constants.O_NOFOLLOW,
        );
        try {
          let [t, i, a] = await Promise.all([
            e.stat(),
            (0, m.lstat)(n),
            (0, m.realpath)(n),
          ]);
          if (
            !t.isFile() ||
            t.dev !== r.dev ||
            t.ino !== r.ino ||
            i.isSymbolicLink() ||
            t.dev !== i.dev ||
            t.ino !== i.ino ||
            a !== n
          )
            throw Error(`Invalid Library file`);
          return e;
        } catch {
          throw (await e.close(), Error(`Library file is unavailable`));
        }
      } catch {
        throw Error(`Library file is unavailable`);
      }
    }
    getAllowedLibraryRealPaths() {
      return (
        (this.allowedLibraryRealPathsPromise ??=
          this.loadAllowedLibraryRealPaths()),
        this.allowedLibraryRealPathsPromise
      );
    }
    async loadAllowedLibraryRealPaths() {
      try {
        return await Promise.all(
          (await this.backend.getAllowedLibraryDirectories()).map(async (e) => {
            try {
              let [t, n] = await Promise.all([
                (0, m.lstat)(e),
                (0, m.realpath)(e),
              ]);
              return t.isDirectory() && !t.isSymbolicLink() ? n : null;
            } catch {
              return null;
            }
          }),
        );
      } finally {
        this.allowedLibraryRealPathsPromise = null;
      }
    }
  },
  F1 = { compact: 96, large: 320 },
  I1 = 5e3,
  L1 = (0, d.promisify)(h.execFile),
  R1 = class {
    queue = Promise.resolve();
    constructor(e) {
      this.fileAccess = e;
    }
    getDataUrl(e, t) {
      let n = this.queue.then(async () => {
        let n = await this.fileAccess.openAllowedFile(e);
        try {
          let r = await (0, m.mkdtemp)(
              u.default.join((0, l.tmpdir)(), `codex-library-source-`),
            ),
            i = u.default.join(r, u.default.basename(e.replaceAll(`\\`, `/`)));
          try {
            await (0, x.pipeline)(
              n.createReadStream({ autoClose: !1 }),
              (0, p.createWriteStream)(i, { flags: `wx` }),
            );
            let e = F1[t],
              r = await z1(i, e);
            if (r != null) return { dataUrl: r };
            let a = await c.nativeImage.createThumbnailFromPath(i, {
              height: e,
              width: e,
            });
            return { dataUrl: a.isEmpty() ? null : a.toDataURL() };
          } finally {
            await (0, m.rm)(r, { force: !0, recursive: !0 });
          }
        } finally {
          await n.close();
        }
      });
      return (
        (this.queue = n.then(
          () => void 0,
          () => void 0,
        )),
        n
      );
    }
  };
async function z1(e, t) {
  if (process.platform !== `darwin`) return null;
  let n = await (0, m.mkdtemp)(
      u.default.join((0, l.tmpdir)(), `codex-library-thumbnail-`),
    ),
    i = u.default.join(n, `${u.default.basename(e)}.png`);
  try {
    return (
      await L1(`qlmanage`, [`-t`, `-s`, String(t), `-o`, n, e], {
        env: r.t(process.env),
        timeout: I1,
      }),
      `data:image/png;base64,${(await (0, m.readFile)(i)).toString(`base64`)}`
    );
  } catch {
    return null;
  } finally {
    await (0, m.rm)(n, { recursive: !0, force: !0 });
  }
}
var B1 = class extends a.P {
    fileAccess;
    thumbnailGenerator;
    constructor(e) {
      (super(),
        (this.backend = e),
        (this.fileAccess = new P1(e)),
        (this.thumbnailGenerator = new R1(this.fileAccess)));
    }
    getThumbnailDataUrl({ size: e, sourcePath: t }) {
      return this.thumbnailGenerator.getDataUrl(t, e);
    }
    listGeneratedImages() {
      return this.backend.listGeneratedImages();
    }
    listOutputFiles() {
      return this.backend.listOutputFiles();
    }
    async prepareFilePreview({ sourcePath: e }) {
      return { previewPath: await this.fileAccess.prepareFilePreview(e) };
    }
    releaseFilePreview({ previewPath: e }) {
      return this.fileAccess.releaseFilePreview(e);
    }
  },
  V1 = class extends a.P {
    constructor(e) {
      (super(), (this.getExecutionHost = e));
    }
    async saveConfig({
      configPath: e,
      expectedRevision: t,
      hostId: r,
      raw: i,
    }) {
      let a = this.getExecutionHost(r),
        o = n.ui(a.hostConfig),
        s = n.eo(a.hostConfig) ? e : re(e, o);
      return {
        configPath: z(s, o),
        ...(await n.wt(a, { configPath: s, expectedRevision: t, raw: i })),
      };
    }
  },
  H1 = class extends a.P {
    onUpdate = null;
    managerSubscription = null;
    lastRevision = 0;
    lastIsComplete = !1;
    hostsById = new Map();
    constructor(e) {
      (super(), (this.manager = e));
    }
    readSnapshot() {
      return U1(this.manager.readSnapshot());
    }
    subscribe(e) {
      (this.unsubscribe(),
        (this.onUpdate = e.dup()),
        this.onUpdate.onRpcBroken(() => this.unsubscribe()),
        (this.managerSubscription = this.manager.subscribe((e) => {
          this.#e(e);
        })));
    }
    unsubscribe() {
      (this.managerSubscription?.dispose(),
        (this.managerSubscription = null),
        this.onUpdate?.[Symbol.dispose](),
        (this.onUpdate = null));
    }
    async requestSync() {
      await this.manager.requestSync();
    }
    async requestStartupSync() {
      await this.manager.requestStartupSync();
    }
    [Symbol.dispose]() {
      this.unsubscribe();
    }
    #e(e) {
      if (e.type === `snapshot`) {
        let t = U1(e.snapshot);
        ((this.lastRevision = t.revision),
          (this.lastIsComplete = t.isComplete),
          this.hostsById.clear());
        for (let e of t.hosts) this.hostsById.set(e.hostId, e.isComplete);
        this.#t({ type: `snapshot`, snapshot: t });
        return;
      }
      let { delta: t } = e;
      if (t.revision < this.lastRevision) return;
      let n = new Map(this.hostsById);
      for (let e of t.changedHosts) n.set(e.hostId, e.isComplete);
      for (let e of t.removedHostIds) n.delete(e);
      let r = Array.from(n.values()).every(Boolean),
        i =
          n.size !== this.hostsById.size ||
          Array.from(n).some(([e, t]) => this.hostsById.get(e) !== t),
        a = t.changedEntries.length > 0 || t.removedEntries.length > 0,
        o = r !== this.lastIsComplete;
      if ((!a && !o && !i) || (t.revision === this.lastRevision && !o && !i))
        return;
      this.hostsById.clear();
      for (let [e, t] of n) this.hostsById.set(e, t);
      let s = {
        baseRevision: this.lastRevision,
        revision: t.revision,
        isComplete: r,
        changedHosts: t.changedHosts,
        removedHostIds: t.removedHostIds,
        changedEntries: t.changedEntries,
        removedEntries: t.removedEntries,
      };
      ((this.lastRevision = s.revision),
        (this.lastIsComplete = s.isComplete),
        this.#t({ type: `delta`, delta: s }));
    }
    #t(e) {
      try {
        var t = A_();
        t.u(this.onUpdate?.(e));
      } catch (e) {
        t.e = e;
      } finally {
        t.d();
      }
    }
  };
function U1(e) {
  return {
    revision: e.revision,
    isComplete: e.hosts.every((e) => e.isComplete),
    hosts: e.hosts,
    entries: e.entries,
  };
}
var W1 = n.xl({
    item: n.xl({
      aggregatedOutput: n.wl().nullish(),
      command: n.wl(),
      commandActions: n.pl(n.xl({ command: n.wl() })).optional(),
      cwd: n.wl().nullish(),
      durationMs: n.bl().int().nonnegative().nullish(),
      exitCode: n.bl().int().nullish(),
      id: n.wl().min(1),
      source: n
        .dl([
          `agent`,
          `userShell`,
          `unifiedExecStartup`,
          `unifiedExecInteraction`,
        ])
        .nullish(),
      status: n.dl([`inProgress`, `completed`, `failed`, `declined`]),
      type: n.vl(`commandExecution`),
    }),
    threadId: n.wl().min(1),
    turnId: n.wl().nullish(),
  }),
  G1 = new Map([
    [-4, `SIGILL`],
    [-6, `SIGABRT`],
    [-7, `SIGBUS`],
    [-11, `SIGSEGV`],
    [132, `SIGILL`],
    [134, `SIGABRT`],
    [135, `SIGBUS`],
    [139, `SIGSEGV`],
  ]),
  K1 = [
    { pattern: /\b(?:abort trap(?::\s*\d+)?|sigabrt)\b/iu, signal: `SIGABRT` },
    { pattern: /\b(?:segmentation fault|sigsegv)\b/iu, signal: `SIGSEGV` },
    { pattern: /\b(?:bus error|sigbus)\b/iu, signal: `SIGBUS` },
    { pattern: /\b(?:illegal instruction|sigill)\b/iu, signal: `SIGILL` },
  ],
  q1 = /^[A-Za-z_][A-Za-z0-9_]*=/u;
function J1({
  captureMessage: e,
  diagnosePrimaryRuntimeDependencies: t,
  getPlatformOs: n,
  getPrimaryRuntimeDependencyRoot: r,
  registerInternalNotificationHandler: i,
}) {
  let a = new Set();
  return i((i) => {
    let o = Y1(i);
    o == null ||
      a.has(o.dedupeKey) ||
      (a.add(o.dedupeKey),
      X1({
        captureMessage: e,
        crash: o,
        diagnosePrimaryRuntimeDependencies: t,
        getPlatformOs: n,
        getPrimaryRuntimeDependencyRoot: r,
      }));
  });
}
function Y1(e) {
  if (e.method !== `item/completed`) return null;
  let t = W1.safeParse(e.params);
  if (!t.success) return null;
  let { item: n } = t.data;
  if (n.source === `userShell` || n.status === `declined`) return null;
  let r = G1.get(n.exitCode ?? 0) ?? null,
    i = `exit-code`;
  if (
    (r == null &&
      n.status === `failed` &&
      n.exitCode !== 0 &&
      ((r =
        K1.find(({ pattern: e }) => e.test(n.aggregatedOutput ?? ``))?.signal ??
        null),
      (i = `output`)),
    r == null)
  )
    return null;
  let a =
      n.commandActions == null || n.commandActions.length === 0
        ? [n.command]
        : n.commandActions.map(({ command: e }) => e),
    o = Z1(a);
  return {
    commands: a,
    cwd: n.cwd ?? null,
    dedupeKey: `${t.data.threadId}:${t.data.turnId ?? ``}:${n.id}`,
    durationMs: n.durationMs ?? null,
    executable: o,
    exitCode: n.exitCode ?? null,
    signal: r,
    signalSource: i,
    source: n.source ?? `agent`,
  };
}
async function X1({
  captureMessage: e,
  crash: t,
  diagnosePrimaryRuntimeDependencies: n,
  getPlatformOs: r,
  getPrimaryRuntimeDependencyRoot: i,
}) {
  let [a, o] = await Promise.all([
      r().catch(() => process.platform),
      i().catch(() => null),
    ]),
    s = $1({ crash: t, primaryRuntimeRoot: o });
  if (
    t.signalSource === `output` &&
    t.executable !== `soffice` &&
    s?.attribution !== `primary-runtime-script`
  )
    return;
  let c = s == null ? null : await n().catch(() => null),
    l = s != null,
    u = l && c?.installed === !0;
  try {
    e(`Native command crashed`, {
      level: `error`,
      fingerprint: [`native-command-crash`, a, t.executable, t.signal],
      tags: {
        component: `codex-execution`,
        "event.process": t.executable,
        "exit.code": t.exitCode ?? void 0,
        "exit.reason": `crashed`,
        "exit.signal": t.signal,
        "runtime.bundle_version": u ? c.bundleVersion : void 0,
        "runtime.script_name": s?.scriptName,
      },
      extra: {
        commandSource: t.source,
        durationMs: t.durationMs,
        processAttribution:
          s?.attribution ??
          (t.executable === `unknown` ? `unknown` : `direct-command`),
        usedPrimaryRuntime: l,
      },
    });
  } catch {}
}
function Z1(e) {
  let t = null;
  for (let n of e)
    for (let e of Q1(n)) {
      let n = i0(e);
      if (n == null || (t != null && n !== t)) return `unknown`;
      t = n;
    }
  return t ?? `unknown`;
}
function Q1(e) {
  let t = [];
  for (let n of n0(e))
    for (let e of t0(n))
      if (!q1.test(e)) {
        t.push(e);
        break;
      }
  return t;
}
function $1({ crash: e, primaryRuntimeRoot: t }) {
  let [n] = e.commands;
  if (t == null || n == null || e.commands.some((e) => e !== n)) return null;
  let r = n0(n)
      .map((e) => t0(e))
      .filter((e) => e.some((e) => !q1.test(e))),
    [i] = r;
  if (i == null || r.length !== 1) return null;
  let a = i.findIndex((e) => !q1.test(e)),
    o = i[a];
  if (o == null) return null;
  let s = e0({ args: i.slice(a + 1), executable: e.executable });
  return s != null &&
    r0({
      allowBareRelativePath: !0,
      cwd: e.cwd,
      primaryRuntimeRoot: t,
      value: s,
    })
    ? {
        attribution: `primary-runtime-script`,
        scriptName: u.default.basename(s),
      }
    : r0({
          allowBareRelativePath: !1,
          cwd: e.cwd,
          primaryRuntimeRoot: t,
          value: o,
        })
      ? { attribution: `primary-runtime-executable` }
      : null;
}
function e0({ args: e, executable: t }) {
  let [n] = e;
  if (n == null || n.startsWith(`-`)) return null;
  let r = u.default.extname(n).toLowerCase();
  switch (t) {
    case `node`:
      return [`.cjs`, `.js`, `.mjs`].includes(r) ? n : null;
    case `python`:
      return r === `.py` ? n : null;
    case `git`:
    case `pdfinfo`:
    case `pdftoppm`:
    case `soffice`:
    case `unknown`:
      return null;
  }
}
function t0(e) {
  try {
    return n.$n(e);
  } catch {
    return [];
  }
}
function n0(e) {
  let t = [],
    n = !1,
    r = null,
    i = 0;
  for (let a = 0; a < e.length; a += 1) {
    let o = e[a];
    if (n) {
      n = !1;
      continue;
    }
    if (o === `\\` && r !== `'`) {
      n = !0;
      continue;
    }
    if (r != null) {
      o === r && (r = null);
      continue;
    }
    if (o === `"` || o === `'`) {
      r = o;
      continue;
    }
    let s = e[a + 1],
      c = 0;
    if (
      o === `;` ||
      o ===
        `
`
    )
      c = s === o ? 2 : 1;
    else if (o === `|`) c = s === `|` || s === `&` ? 2 : 1;
    else if (o === `&`) {
      let t = e[a - 1];
      s === `&` ? (c = 2) : s !== `>` && t !== `>` && t !== `<` && (c = 1);
    }
    c !== 0 && (t.push(e.slice(i, a)), (a += c - 1), (i = a + 1));
  }
  return (t.push(e.slice(i)), t);
}
function r0({
  allowBareRelativePath: e,
  cwd: t,
  primaryRuntimeRoot: n,
  value: r,
}) {
  let i = null;
  return (
    u.default.isAbsolute(r)
      ? (i = u.default.resolve(r))
      : t != null &&
        (e || r.includes(u.default.sep)) &&
        (i = u.default.resolve(t, r)),
    i != null && S1(i, u.default.resolve(n))
  );
}
function i0(e) {
  let t = u.default.win32
    .basename(e)
    .toLowerCase()
    .replace(/\.exe$/u, ``);
  if (/^python(?:3(?:\.\d+)*)?$/u.test(t)) return `python`;
  switch (t) {
    case `git`:
    case `node`:
    case `pdfinfo`:
    case `pdftoppm`:
      return t;
    case `libreoffice`:
    case `soffice`:
      return `soffice`;
    default:
      return null;
  }
}
var a0 = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.performanceLogger = e),
        (this.messageHandler = t),
        (this.origin = n));
    }
    report(e, t) {
      if (t == null) {
        this.performanceLogger.report(e, null);
        return;
      }
      this.messageHandler
        .enrichPerformanceAppStateSnapshot(this.origin, t)
        .then(
          (t) => this.performanceLogger.report(e, t),
          () => this.performanceLogger.report(e, t),
        );
    }
  },
  o0 = class extends a.P {
    constructor(e) {
      (super(), (this.appServer = e));
    }
    async list({
      buildFlavor: e,
      cwds: t,
      hiddenMarketplaceNames: r,
      marketplaceKinds: i,
    }) {
      try {
        let [{ marketplaces: a }, o, s] = await Promise.all([
            this.appServer.listPlugins({
              ...(t.length > 0 ? { cwds: t } : {}),
              ...(i == null ? {} : { marketplaceKinds: i }),
            }),
            this.appServer.codexHome(),
            this.appServer.platformPath(),
          ]),
          c = n.Ms(e);
        return await n.ln({
          codexHome: o,
          filesystem: this.appServer,
          marketplaces: a.filter(
            (e) => !r.includes(e.name) && (!n.Ns(e.name) || e.name === c),
          ),
          path: s,
        });
      } catch {
        return { groups: [] };
      }
    }
  },
  s0 = r.a(`install-primary-runtime`),
  c0 = 3600 * 1e3,
  l0 = 3e4,
  u0 = new Map(),
  d0 = Promise.resolve();
function f0() {
  let e = !1;
  for (let { controller: t } of u0.values()) (t.abort(), (e = !0));
  return e;
}
function p0(e) {
  return typeof e != `object` || !e
    ? !1
    : Object.entries(e).some(
        ([e, t]) => e === `workspace_dependencies` && t === !0,
      );
}
async function m0(e) {
  let t = async (n) => {
    let r = await e.sendAppServerRequest(`experimentalFeature/list`, {
      cursor: n,
      limit: 100,
    });
    return r.data.some(
      (e) => e.name === `workspace_dependencies` && e.enabled === !0,
    )
      ? !0
      : r.nextCursor == null
        ? !1
        : t(r.nextCursor);
  };
  return t(null);
}
var h0 = class {
  startupChecked = !1;
  nextUpdateAtMs = null;
  running = !1;
  constructor(e) {
    this.options = e;
  }
  shouldPoll() {
    return this.getStaticDisabledReason() == null;
  }
  shouldTickForSharedObjectUpdate(e, t) {
    return (
      (e === `statsig_default_enable_features` && p0(t)) ||
      e === `codex_runtimes_config`
    );
  }
  async getStatus() {
    let e = await this.getDisabledReason();
    return {
      disabledReason: e,
      enabled: e == null,
      isRunning: this.running,
      nextRunAt: this.nextUpdateAtMs,
      startupChecked: this.startupChecked,
    };
  }
  async pollIfDue() {
    if (this.getStaticDisabledReason() != null) return;
    let e = Date.now(),
      t = this.nextUpdateAtMs != null && e >= this.nextUpdateAtMs;
    if (!(this.startupChecked && !t) && !this.running) {
      this.running = !0;
      try {
        if ((await this.getDisabledReason()) != null) {
          ((this.startupChecked = !0),
            (this.nextUpdateAtMs = this.getNextUpdateAtMs(e)));
          return;
        }
        let n = `latest`,
          r = await this.options.getCacheStatus({
            release: n,
            runtimeInstallConfig: this.getInstallConfig(),
            target: this.options.getTarget(),
          });
        if (!this.startupChecked) {
          if (
            ((this.startupChecked = !0),
            (this.nextUpdateAtMs = this.getNextUpdateAtMs(e)),
            r.status !== `missing`)
          )
            return;
        } else if (!t) return;
        if (
          ((this.nextUpdateAtMs = this.getNextUpdateAtMs(e)),
          r.status === `current` || r.status === `unsupported`)
        )
          return;
        await this.installUpdate(n);
      } catch (e) {
        s0().warning(`primary_runtime_update_poll_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      } finally {
        this.running = !1;
      }
    }
  }
  async runNow(e) {
    let t = await this.getDisabledReason();
    if (t != null)
      return {
        bundleVersion: null,
        nextRunAt: this.nextUpdateAtMs,
        reason: t,
        status: `skipped`,
      };
    if (this.running)
      return {
        bundleVersion: null,
        nextRunAt: this.nextUpdateAtMs,
        reason: `already-running`,
        status: `skipped`,
      };
    this.running = !0;
    try {
      let t = await this.options.getCacheStatus({
        release: e,
        runtimeInstallConfig: this.getInstallConfig(),
        target: this.options.getTarget(),
      });
      if (t.status === `current`)
        return {
          bundleVersion: t.installedBundleVersion,
          nextRunAt: this.nextUpdateAtMs,
          reason: `current`,
          status: `already-current`,
        };
      if (t.status === `unsupported`)
        return {
          bundleVersion: null,
          nextRunAt: this.nextUpdateAtMs,
          reason: `unsupported-windows-version`,
          status: `skipped`,
        };
      let n = await this.installUpdate(e);
      return {
        bundleVersion: n.bundleVersion,
        nextRunAt: this.nextUpdateAtMs,
        reason: n.status === `already-current` ? `current` : null,
        status: n.status,
      };
    } finally {
      this.running = !1;
    }
  }
  getStaticDisabledReason() {
    return this.options.hostId === `local`
      ? this.options.sharedObjectRepository?.get(`codex_runtimes_config`) ==
        null
        ? `runtime-config-missing`
        : p0(
              this.options.sharedObjectRepository?.get(
                `statsig_default_enable_features`,
              ),
            )
          ? null
          : `feature-gate-disabled`
      : `not-local-host`;
  }
  async getDisabledReason() {
    let e = this.getStaticDisabledReason();
    if (e != null) return e;
    try {
      return (await this.options.isWorkspaceDependenciesFeatureEnabled())
        ? null
        : `feature-gate-disabled`;
    } catch (e) {
      return (
        s0().warning(`workspace_dependencies_feature_check_failed`, {
          safe: {},
          sensitive: { error: e },
        }),
        `feature-gate-disabled`
      );
    }
  }
  getInstallConfig() {
    return (
      this.options.sharedObjectRepository?.get(`codex_runtimes_config`)
        ?.runtimes[n.Vn] ?? n.Xi
    );
  }
  getNextUpdateAtMs(e) {
    let t = this.getJitterMs(),
      n = Math.floor(e / c0) * c0 + t;
    return n > e ? n : n + c0;
  }
  getJitterMs() {
    let e = this.options.globalState.get(n.Gs.PRIMARY_RUNTIME_UPDATE_JITTER_MS);
    if (typeof e == `number` && Number.isInteger(e) && e >= 0 && e < c0)
      return e;
    let t = Math.floor(Math.random() * c0);
    return (
      this.options.globalState.set(n.Gs.PRIMARY_RUNTIME_UPDATE_JITTER_MS, t),
      t
    );
  }
  async installUpdate(e) {
    return this.options.installForConnection({
      connection: this.options.appServerConnectionRegistry.getConnection(
        this.options.hostId,
      ),
      errorReporter: this.options.errorReporter,
      onProgress: this.options.onProgress,
      release: e,
      runtimeConfig: this.options.sharedObjectRepository?.get(
        `codex_runtimes_config`,
      ),
    });
  }
};
async function g0({
  connection: e,
  errorReporter: t,
  installRuntime: r = n.qn,
  onProgress: i,
  runtimeConfig: a,
  release: o,
  target: s = n.Un(),
}) {
  let c = a?.runtimes[n.Vn] ?? n.Xi,
    l = `${s.platform}-${s.arch}`,
    u = a?.runtimes[n.Vn] != null;
  s0().info(`primary_runtime_install_started`, {
    safe: { hasConfiguredRuntime: u, release: o, target: l },
  });
  let d;
  try {
    d = await E0({
      installRuntime: r,
      onProgress: i,
      release: o,
      runtimeOptions: c,
      target: s,
    });
  } catch (e) {
    throw (
      w0(e) ||
        T0({
          error: e,
          errorReporter: t,
          hasConfiguredRuntime: u,
          release: o,
          targetKey: l,
        }),
      e
    );
  }
  try {
    (i?.({
      bundleVersion: d.bundleVersion,
      downloadedBytes: null,
      errorMessage: null,
      phase: `configuring`,
      release: o,
      totalBytes: null,
    }),
      await v0(e, d.paths),
      await y0(e, d.paths),
      _0({ connection: e, release: o, targetKey: l }));
  } catch (e) {
    throw (
      w0(e) ||
        T0({
          error: e,
          errorReporter: t,
          hasConfiguredRuntime: u,
          release: o,
          targetKey: l,
        }),
      i?.({
        bundleVersion: d.bundleVersion,
        downloadedBytes: null,
        errorMessage: e instanceof Error ? e.message : String(e),
        phase: `error`,
        release: o,
        totalBytes: null,
      }),
      e
    );
  }
  return (
    s0().info(`primary_runtime_install_finished`, {
      safe: {
        bundleVersion: d.bundleVersion,
        release: o,
        status: d.status,
        target: l,
      },
    }),
    i?.({
      bundleVersion: d.bundleVersion,
      downloadedBytes: null,
      errorMessage: null,
      phase: `ready`,
      release: o,
      totalBytes: null,
    }),
    { bundleVersion: d.bundleVersion, status: d.status }
  );
}
function _0({ connection: e, release: t, targetKey: n }) {
  e.sendAppServerRequest(`skills/list`, { forceReload: !0 }).catch((e) => {
    s0().warning(`primary_runtime_skills_reload_failed`, {
      safe: { release: t, target: n },
      sensitive: { error: e },
    });
  });
}
async function v0(e, t) {
  if (t.bundledPluginMarketplacePaths.length === 0) return;
  let n = (
    await Promise.all(
      t.bundledPluginMarketplacePaths.map(async (t) => ({
        marketplaceRoot: t,
        synced: await Cc({ appServerConnection: e, marketplaceRoot: t }),
      })),
    )
  ).find((e) => !e.synced);
  if (n != null)
    throw Error(
      `Failed to sync primary runtime bundled plugin marketplace: ${n.marketplaceRoot}`,
    );
}
async function y0(e, t) {
  if (t.skillsToRemove.length === 0 && t.bundledSkillPaths.length === 0) return;
  let r = await e.codexHome();
  if (
    (await b0({ codexHome: r, skillsToRemove: t.skillsToRemove }),
    t.bundledSkillPaths.length === 0)
  )
    return;
  let i = u.default.join(r, `skills`, n.Vn);
  (await m.default.rm(i, { force: !0, recursive: !0 }),
    await m.default.mkdir(i, { recursive: !0 }),
    await Promise.all(
      t.bundledSkillPaths.map((e) =>
        m.default.cp(
          u.default.dirname(e),
          u.default.join(i, u.default.basename(u.default.dirname(e))),
          { recursive: !0 },
        ),
      ),
    ));
}
async function b0({ codexHome: e, skillsToRemove: t }) {
  if (t.length === 0) return;
  let n = u.default.join(e, `skills`);
  await Promise.all(
    t.map(async (t) => {
      let r = x0({ skillDir: t, skillsRoot: n });
      if (
        !(
          await m.default.stat(r).catch((e) => {
            if (C0(e, `ENOENT`)) return null;
            throw e;
          })
        )?.isDirectory()
      )
        return;
      let i = u.default.join(
        e,
        `.tmp`,
        `legacy-primary-runtime-skills`,
        `${u.default.basename(r)}-${Date.now()}-${(0, f.randomUUID)()}`,
      );
      (await m.default.mkdir(u.default.dirname(i), { recursive: !0 }),
        (await m.default
          .rename(r, i)
          .then(() => !0)
          .catch((e) => {
            if (C0(e, `ENOENT`)) return !1;
            throw e;
          })) &&
          s0().info(`primary_runtime_legacy_skill_migrated`, {
            safe: { skillDir: t },
            sensitive: { backupPath: i, skillRoot: r },
          }));
    }),
  );
}
function x0({ skillDir: e, skillsRoot: t }) {
  let n = u.default.resolve(t),
    r = u.default.resolve(t, e);
  if (S0(r, n)) return r;
  throw Error(
    `Primary runtime removed skill path must stay within the Codex skills directory: ${e}`,
  );
}
function S0(e, t) {
  let n = u.default.relative(t, e);
  return n !== `` && !n.startsWith(`..`) && !u.default.isAbsolute(n);
}
function C0(e, t) {
  return typeof e == `object` && !!e && `code` in e && e.code === t;
}
function w0(e) {
  return e instanceof Error || e instanceof DOMException
    ? e.name === `AbortError` || e.message.toLowerCase().includes(`aborted`)
    : !1;
}
function T0({
  error: e,
  errorReporter: t,
  hasConfiguredRuntime: n,
  release: r,
  targetKey: i,
}) {
  if (
    e instanceof Error &&
    e.message ===
      `Codex Workspace dependencies are not supported on Windows 10.`
  ) {
    s0().info(`primary_runtime_install_unsupported_host`, {
      safe: {
        errorName: e.name,
        hasConfiguredRuntime: n,
        release: r,
        target: i,
      },
      sensitive: { error: e },
    });
    return;
  }
  (s0().error(`primary_runtime_install_failed`, {
    safe: {
      errorName: e instanceof Error ? e.name : typeof e,
      hasConfiguredRuntime: n,
      release: r,
      target: i,
    },
    sensitive: { error: e },
  }),
    !(e instanceof TypeError && e.message === `fetch failed`) &&
      t.reportNonFatal(e instanceof Error ? e : Error(String(e)), {
        kind: `install-primary-runtime`,
        tags: { release: r, target: i },
      }));
}
async function E0({
  installRuntime: e,
  onProgress: t,
  release: n,
  runtimeOptions: r,
  target: i,
}) {
  let a = JSON.stringify({ release: n, runtimeOptions: r, target: i }),
    o = u0.get(a);
  if (o != null) return o.promise;
  let s = new AbortController(),
    c = d0
      .then(
        () => (
          s.signal.throwIfAborted(),
          e(i, r, n, {
            onProgress: (e) => {
              t?.({ ...e, errorMessage: null, release: n });
            },
            signal: s.signal,
          })
        ),
      )
      .catch((e) => {
        throw (
          t?.({
            bundleVersion: null,
            downloadedBytes: null,
            errorMessage: e instanceof Error ? e.message : String(e),
            phase: `error`,
            release: n,
            totalBytes: null,
          }),
          e
        );
      });
  (u0.set(a, { controller: s, promise: c }),
    (d0 = c.then(
      () => void 0,
      () => void 0,
    )));
  try {
    return await c;
  } finally {
    u0.delete(a);
  }
}
async function D0({ hostId: e }) {
  let t = await O0({ hostId: e });
  return {
    bundleVersion: t.bundleVersion,
    installed: t.installed,
    instructions: t.installed ? t.instructions : null,
  };
}
async function O0({ hostId: e }) {
  s0().info(`primary_runtime_dependencies_diagnose_started`, {
    safe: { hostId: e },
    sensitive: {},
  });
  let t = await n.Gn();
  return (
    s0().info(`primary_runtime_dependencies_diagnose_finished`, {
      safe: {
        bundleVersion: t.bundleVersion,
        installed: t.installed,
        problemCount: t.problems.length,
      },
      sensitive: {},
    }),
    t
  );
}
async function k0({
  connection: e,
  errorReporter: t,
  installRuntime: r = n.qn,
  onProgress: i,
  release: a,
  runtimeConfig: o,
  target: s = n.Un(),
  confirmDeleteRuntimeRoot: c,
}) {
  let l = await n.Kn();
  if (l != null && (await c?.(l)) === !1)
    throw Error(`Codex dependency reset was canceled.`);
  return (
    await n.Hn(),
    g0({
      connection: e,
      errorReporter: t,
      installRuntime: r,
      onProgress: i,
      release: a,
      runtimeConfig: o,
      target: s,
    })
  );
}
function A0(e) {
  if (e !== `local`)
    throw Error(`Codex runtime installation only supports the local host.`);
}
var j0 = class extends a.P {
    workspaceDependenciesFeatureEnabledByHostId = new Map();
    updatePoller;
    constructor(e, t, r, i, a) {
      (super(),
        (this.appServerConnectionRegistry = e),
        (this.errorReporter = t),
        (this.sharedObjectRepository = i),
        (this.windowManager = a),
        (this.updatePoller = new h0({
          appServerConnectionRegistry: e,
          errorReporter: t,
          getCacheStatus: n.Wn,
          getTarget: n.Un,
          globalState: r,
          hostId: V,
          installForConnection: g0,
          isWorkspaceDependenciesFeatureEnabled: () =>
            this.isWorkspaceDependenciesFeatureEnabled(V),
          onProgress: (e) => {
            this.#n(V, e);
          },
          sharedObjectRepository: i,
        })));
    }
    install({ hostId: e, release: t }) {
      return this.#t(e, t);
    }
    cancelInstall({ hostId: e }) {
      return (A0(e), { canceled: f0() });
    }
    async diagnoseDependencies({ hostId: e }) {
      A0(e);
      let t = await O0({ hostId: e });
      return {
        artifactToolVersion: t.artifactToolVersion,
        bundleVersion: t.bundleVersion,
        installed: t.installed,
        libreOfficeVersion: t.libreOfficeVersion,
        problems: t.problems,
      };
    }
    async loadDependencies({ hostId: e }) {
      return (
        A0(e),
        (await this.isWorkspaceDependenciesFeatureEnabled(e))
          ? D0({ hostId: e })
          : { bundleVersion: null, installed: !1, instructions: null }
      );
    }
    finishInstall({ hostId: e, release: t }) {
      return this.#t(e, t);
    }
    getUpdateStatus() {
      return this.updatePoller.getStatus();
    }
    runUpdateNow({ release: e }) {
      return this.updatePoller.runNow(e);
    }
    async resetDependencies({ hostId: e, release: t }) {
      return (
        A0(e),
        await this.#e(e),
        k0({
          confirmDeleteRuntimeRoot: void 0,
          connection: this.appServerConnectionRegistry.getConnection(e),
          errorReporter: this.errorReporter,
          onProgress: (t) => {
            this.#n(e, t);
          },
          release: t,
          runtimeConfig: this.sharedObjectRepository.get(
            `codex_runtimes_config`,
          ),
        })
      );
    }
    isWorkspaceDependenciesFeatureEnabled = (e) => {
      let t = this.workspaceDependenciesFeatureEnabledByHostId.get(e);
      if (t != null) return t;
      let n = m0(this.appServerConnectionRegistry.getConnection(e)).catch(
        (t) => {
          throw (this.workspaceDependenciesFeatureEnabledByHostId.delete(e), t);
        },
      );
      return (this.workspaceDependenciesFeatureEnabledByHostId.set(e, n), n);
    };
    startUpdatePolling = () => {
      let e = () => {
          this.updatePoller.shouldPoll() && this.updatePoller.pollIfDue();
        },
        t = this.sharedObjectRepository.addSubscriber((t, n) => {
          (t === `statsig_default_enable_features` &&
            this.workspaceDependenciesFeatureEnabledByHostId.clear(),
            this.updatePoller.shouldTickForSharedObjectUpdate(t, n) && e());
        });
      e();
      let n = setInterval(e, l0);
      return (
        n.unref(),
        () => {
          (clearInterval(n), t());
        }
      );
    };
    async #e(e) {
      if (!n.Zi({ osRelease: l.default.release(), platform: process.platform }))
        throw Error(n.Bn);
      if (!(await this.isWorkspaceDependenciesFeatureEnabled(e)))
        throw Error(`Codex dependencies are disabled in settings.`);
    }
    async #t(e, t) {
      return (
        A0(e),
        await this.#e(e),
        g0({
          connection: this.appServerConnectionRegistry.getConnection(e),
          errorReporter: this.errorReporter,
          onProgress: (t) => {
            this.#n(e, t);
          },
          release: t,
          runtimeConfig: this.sharedObjectRepository.get(
            `codex_runtimes_config`,
          ),
        })
      );
    }
    #n(e, t) {
      this.windowManager.sendMessageToAllWindows({
        type: `primary-runtime-install-progress`,
        hostId: e,
        progress: t,
      });
    }
  },
  M0 = class extends a.P {
    constructor(e, t) {
      (super(), (this.controller = e), (this.origin = t));
    }
    addToComposer(e) {
      return this.controller.addToComposer(e);
    }
    clearPrewarm() {
      this.controller.clearPrewarm(this.origin);
    }
    open({ conversationId: e, popoverBounds: t }) {
      return this.controller.open(this.origin, e, t);
    }
    prewarm() {
      this.controller.prewarm(this.origin);
    }
    rendererReady(e) {
      this.controller.handleRendererReady(this.origin, e);
    }
  };
function N0(e, t) {
  for (let n of t) e = e.replace(RegExp(`\\` + n, `g`), `\\` + n);
  return e;
}
function P0(e, t) {
  return (
    (e = N0(e, `\\()[]{}.+^$|`)),
    (e = e.replace(/\*/g, `.*`).replace(/\?/g, `.?`)),
    RegExp(`^(?:` + e + `)$`).test(t)
  );
}
function F0(e, t) {
  let n = Array.isArray(e) ? e : e.split(/,/),
    r = !1;
  for (let e of n)
    if (e[0] == `!` && P0(e.slice(1), t)) return !1;
    else P0(e, t) && (r = !0);
  return r;
}
var I0 = /\s/,
  L0 = /\r|\n/,
  R0 = /^(Host|Match)$/i,
  z0 =
    /^(GlobalKnownHostsFile|Host|IPQoS|SendEnv|UserKnownHostsFile|ProxyCommand|Match|CanonicalDomains)$/i,
  B0 = /^(?:CertificateFile|IdentityFile|IdentityAgent|User)$/i,
  V0 = /^(Include|IdentityFile)$/i,
  H0;
(function (e) {
  ((e[(e.DIRECTIVE = 1)] = `DIRECTIVE`),
    (e[(e.COMMENT = 2)] = `COMMENT`),
    (e[(e.EMPTY = 3)] = `EMPTY`));
})((H0 ||= {}));
var U0 = [
  `IdentityFile`,
  `LocalForward`,
  `RemoteForward`,
  `DynamicForward`,
  `CertificateFile`,
];
function W0(e, t) {
  return t.hasOwnProperty(e.param) && t[e.param] === e.value;
}
function G0(e) {
  for (let t of e)
    if (t.type === H0.DIRECTIVE && `config` in t) {
      for (let e of t.config) if (e.before) return e.before;
    }
  return `  `;
}
function K0(e, t) {
  let n = (e, n) => {
    switch (e.toLowerCase()) {
      case `all`:
        return !0;
      case `final`:
        return t.inFinalPass ? !0 : ((t.doFinalPass = !0), !1);
      case `exec`:
        return (
          (0, h.spawnSync)(
            `function main {
          ${n}
        }
        main`,
            { shell: !0 },
          ).status === 0
        );
      case `host`:
        return F0(n, t.params.HostName);
      case `originalhost`:
        return F0(n, t.params.OriginalHost);
      case `user`:
        return F0(n, t.params.User);
      case `localuser`:
        return F0(n, t.params.LocalUser);
    }
  };
  for (let t in e) {
    let r = e[t];
    if (!n(t, Array.isArray(r) ? r.map(({ val: e }) => e) : r)) return !1;
  }
  return !0;
}
var q0 = class e extends Array {
  static DIRECTIVE = H0.DIRECTIVE;
  static COMMENT = H0.COMMENT;
  static EMPTY = H0.EMPTY;
  static parse(e) {
    return J0(e);
  }
  static stringify(e) {
    return Y0(e);
  }
  compute(e, t) {
    typeof e == `string` && (e = { Host: e });
    let n;
    try {
      n = l.default.userInfo();
    } catch {
      n = { username: process.env.USER || process.env.USERNAME || `` };
    }
    let r = {
        params: {
          Host: e.Host,
          HostName: e.Host,
          OriginalHost: e.Host,
          User: n.username,
          LocalUser: n.username,
        },
        inFinalPass: !1,
        doFinalPass: !1,
      },
      i = {},
      a = (e, n) => {
        let a = t?.ignoreCase ? e.toLowerCase() : e,
          o;
        o = Array.isArray(n)
          ? /ProxyCommand/i.test(a)
            ? n
                .map(
                  ({ val: e, separator: t, quoted: n }) =>
                    `${t}${n ? `"${e.replace(/"/g, `\\"`)}"` : e}`,
                )
                .join(``)
                .trim()
            : n.map(({ val: e }) => e)
          : n;
        let s = Array.isArray(o) ? o[0] : o;
        U0.some((t) => t.toLowerCase() === e.toLowerCase())
          ? (i[a] || (i[a] = [])).push(...[].concat(o))
          : (i[a] ??
            (e === `HostName`
              ? (r.params.HostName = s)
              : e === `User` && (r.params.User = s),
            (i[a] = o)));
      };
    e.User !== void 0 && a(`User`, e.User);
    let o = () => {
      for (let e of this)
        if (e.type === H0.DIRECTIVE)
          if (
            /^host$/i.test(e.param) &&
            F0(
              Array.isArray(e.value) ? e.value.map(({ val: e }) => e) : e.value,
              r.params.Host,
            )
          ) {
            let t = !1,
              n = [];
            a(e.param, e.value);
            for (let r of e.config)
              r.type === H0.DIRECTIVE &&
                (a(r.param, r.value),
                /^CanonicalizeHostName$/i.test(r.param) &&
                  r.value === `yes` &&
                  (t = !0),
                /^CanonicalDomains$/i.test(r.param) &&
                  Array.isArray(r.value) &&
                  (n = r.value.map(({ val: e }) => e)));
            if (n.length > 0 && t && r.params.Host === r.params.OriginalHost)
              for (let e of n) {
                let t = `${r.params.OriginalHost}.${e}`,
                  { status: n, stderr: i } = (0, h.spawnSync)(`nslookup`, [t]);
                if (n === 0 && !/can't find/.test(i.toString())) {
                  ((r.params.Host = t), a(`Host`, t), o());
                  break;
                }
              }
          } else if (
            /^match$/i.test(e.param) &&
            `criteria` in e &&
            K0(e.criteria, r)
          )
            for (let t of e.config)
              t.type === H0.DIRECTIVE && a(t.param, t.value);
          else /^(host|match)$/i.test(e.param) || a(e.param, e.value);
    };
    return (
      o(),
      r.doFinalPass &&
        ((r.inFinalPass = !0), (r.params.Host = r.params.HostName), o()),
      i
    );
  }
  find(e) {
    if (typeof e == `function`) return super.find(e);
    if (!(e && (`Host` in e || `Match` in e)))
      throw Error(`Can only find by Host or Match`);
    return super.find((t) => `param` in t && W0(t, e));
  }
  remove(e) {
    let t;
    if (typeof e == `function`) t = super.findIndex(e);
    else if (e && (`Host` in e || `Match` in e))
      t = super.findIndex((t) => `param` in t && W0(t, e));
    else throw Error(`Can only remove by Host or Match`);
    if (t >= 0) return this.splice(t, 1);
  }
  toString() {
    return Y0(this);
  }
  append(t) {
    let n = G0(this),
      r = this.length > 0 ? this[this.length - 1] : null,
      i = (r && r.config) || this,
      a = this,
      o = i.length > 0 ? i[i.length - 1] : r;
    o &&
      !o.after &&
      (o.after = `
`);
    let s = i !== a;
    for (let r in t) {
      let c = t[r],
        l = {
          type: H0.DIRECTIVE,
          param: r,
          separator: ` `,
          value: Array.isArray(c)
            ? c.map((e, t) => ({ val: e, separator: t === 0 ? `` : ` ` }))
            : c,
          before: s ? n : n.replace(/  |\t/, ``),
          after: `
`,
        };
      (R0.test(r)
        ? ((s = !0),
          (l.before = n.replace(/  |\t/, ``)),
          (i = a),
          o &&
            o.after ===
              `
` &&
            (o.after += `
`),
          i.push(l),
          (i = l.config = new e()))
        : i.push(l),
        (o = l));
    }
    return a;
  }
  prepend(t, n = !1) {
    let r = G0(this),
      i = this,
      a = 0;
    if (n) {
      for (; a < this.length && !(`config` in this[a]); ) a += 1;
      if (a >= this.length) return this.append(t);
    }
    let o = !1,
      s = 0;
    for (let n in t) {
      s += 1;
      let c = t[n],
        l = {
          type: H0.DIRECTIVE,
          param: n,
          separator: ` `,
          value: Array.isArray(c)
            ? c.map((e, t) => ({ val: e, separator: t === 0 ? `` : ` ` }))
            : c,
          before: ``,
          after: `
`,
        };
      if (R0.test(n)) {
        ((l.before = r.replace(/  |\t/, ``)),
          i.splice(a, 0, l),
          (i = l.config = new e()),
          (o = !0));
        continue;
      }
      if (
        (s === Object.keys(t).length &&
          (l.after += `
`),
        !o)
      ) {
        (i.splice(a, 0, l),
          (a += 1),
          V0.test(n) &&
            (l.after += `
`));
        continue;
      }
      ((l.before = r), i.push(l));
    }
    return i;
  }
};
function J0(e) {
  let t = typeof e == `string` ? e : e.toString(`utf-8`),
    n = 0,
    r = o(),
    i = new q0(),
    a = i;
  function o() {
    return t[n++];
  }
  function s() {
    let e = ``;
    for (; I0.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function c() {
    let e = ``;
    for (; L0.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function l() {
    let e = ``;
    for (; r && /[^ \t=]/.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function u() {
    let e = s();
    return (r === `=` && ((e += r), (r = o())), e + s());
  }
  function d() {
    let e = ``,
      t = !1,
      n = !1;
    for (; r && !L0.test(r); ) {
      if (n) ((e += r === `"` ? r : `\\${r}`), (n = !1));
      else if (r === `"` && (!e || t)) t = !t;
      else if (r === `\\`) n = !0;
      else if (r === `#` && !t) break;
      else e += r;
      r = o();
    }
    if (t || n) throw Error(`Unexpected line break at ${e}`);
    return e.trim();
  }
  function f() {
    let e = H0.COMMENT,
      t = ``;
    for (; r && !L0.test(r); ) ((t += r), (r = o()));
    return { type: e, content: t, before: ``, after: `` };
  }
  function p() {
    let e = [],
      t = ``,
      n = !1,
      i = ` `,
      a = !1,
      s = !1;
    for (; r && !L0.test(r); ) {
      if (s) ((t += r === `"` ? r : `\\${r}`), (s = !1));
      else if (r === `"`) a = !a;
      else if (r === `\\`) s = !0;
      else if (a) ((t += r), (n = !0));
      else if (/[ \t=]/.test(r))
        t &&
          (e.push({ val: t, separator: i, quoted: n }),
          (t = ``),
          (n = !1),
          (i = r));
      else if (r === `#` && e.length > 0) break;
      else t += r;
      r = o();
    }
    if (a || s)
      throw Error(
        `Unexpected line break at ${e
          .map(({ val: e }) => e)
          .concat(t)
          .join(` `)}`,
      );
    return (
      t && e.push({ val: t, separator: i, quoted: n }),
      e.length > 1 ? e : e[0].val
    );
  }
  function m() {
    let e = H0.DIRECTIVE,
      t = l(),
      n = z0.test(t),
      i = {
        type: e,
        param: t,
        separator: u(),
        quoted: !n && r === `"`,
        value: n ? p() : d(),
        before: ``,
        after: ``,
      };
    if ((i.quoted || delete i.quoted, /^Match$/i.test(t))) {
      let e = {};
      typeof i.value == `string` &&
        (i.value = [{ val: i.value, separator: ``, quoted: i.quoted }]);
      let t = 0;
      for (; t < i.value.length; ) {
        let { val: n } = i.value[t];
        switch (n.toLowerCase()) {
          case `all`:
          case `canonical`:
          case `final`:
            ((e[n] = []), (t += 1));
            break;
          default:
            if (t + 1 >= i.value.length)
              throw Error(`Missing value for match criteria ${n}`);
            ((e[n] = i.value[t + 1].val), (t += 2));
            break;
        }
      }
      i.criteria = e;
    }
    return i;
  }
  function h() {
    let e = s(),
      t = r === `#` ? f() : m(),
      n = c();
    return ((t.before = e), (t.after = n), t);
  }
  for (; r; ) {
    let e = h();
    e.type === H0.DIRECTIVE && R0.test(e.param)
      ? ((i = a), i.push(e), (i = e.config = new q0()))
      : e.type === H0.DIRECTIVE && !e.param
        ? i.length === 0
          ? a.length === 0
            ? a.push({ type: H0.EMPTY, before: ``, after: e.before })
            : (a[a.length - 1].after += e.before)
          : (i[i.length - 1].after += e.before)
        : i.push(e);
  }
  return a;
}
function Y0(e) {
  let t = ``;
  function n(e, t) {
    if (Array.isArray(e)) {
      let t = ``;
      for (let { val: r, separator: i, quoted: a } of e)
        t += (t ? i : ``) + n(r, a || I0.test(r));
      return t;
    }
    return t ? `"${e}"` : e;
  }
  function r(e) {
    let t =
        e.quoted ||
        (B0.test(e.param) && typeof e.value == `string` && I0.test(e.value)),
      r = n(e.value, t);
    return `${e.param}${e.separator}${r}`;
  }
  let i = (e) => {
    ((t += e.before),
      e.type === H0.COMMENT
        ? (t += e.content)
        : e.type === H0.DIRECTIVE && U0.includes(e.param)
          ? (Array.isArray(e.value) ? e.value : [e.value]).forEach(
              (n, i, a) => {
                ((t += r({ ...e, value: typeof n == `string` ? n : n.val })),
                  i < a.length - 1 && (t += `\n${e.before}`));
              },
            )
          : e.type === H0.DIRECTIVE && (t += r(e)),
      (t += e.after),
      `config` in e && e.config.forEach(i));
  };
  return (e.forEach(i), t);
}
var X0 = r.a(`remote-connections/discovery`),
  Z0 = /[!*?[\]]/,
  Q0 = /[*?[\]{}()]/,
  $0 = new Set([`colima`]),
  e2 = new Set([`github.com`]);
async function t2(e) {
  try {
    if (!p.default.existsSync(e))
      return (
        X0().info(`ssh_config_entrypoint_missing`, {
          safe: {},
          sensitive: { entrypointPath: e },
        }),
        []
      );
    X0().info(`discovery_started`, {
      safe: {},
      sensitive: { entrypointPath: e },
    });
    let t = n2(e),
      n = t.filter((e) => !$0.has(e.trim().toLowerCase())),
      r = (
        await Promise.all(n.map((t) => r2({ entrypointPath: e, sshAlias: t })))
      ).filter((e) => e != null);
    return (
      X0().info(`discovery_completed`, {
        safe: {
          collectedAliasCount: t.length,
          filteredAliasCount: t.length - n.length,
          resolvedConnectionCount: r.length,
          droppedAliasCount: n.length - r.length,
        },
        sensitive: {
          entrypointPath: e,
          collectedAliases: t,
          filteredAliases: n,
        },
      }),
      r.sort((e, t) => e.displayName.localeCompare(t.displayName))
    );
  } catch (e) {
    throw (
      X0().warning(`failed to discover remote SSH connections`, {
        safe: {},
        sensitive: { error: e },
      }),
      Error(`Failed to discover remote SSH connections.`)
    );
  }
}
function n2(e) {
  let t = new Set(),
    n = new Set(),
    r = u.default.dirname(u.default.resolve(e));
  return (
    i2({
      configPath: u.default.resolve(e),
      sshConfigRootDirectory: r,
      discoveredAliases: t,
      visitedConfigPaths: n,
    }),
    Array.from(t)
  );
}
async function r2({ entrypointPath: e, sshAlias: t }) {
  let {
    stdout: r,
    stderr: i,
    code: a,
  } = await n
    .Yn({ args: [`ssh`, `-G`, `-F`, e, t], spawnInsideWsl: !1 })
    .wait();
  if (a !== 0)
    return (
      X0().warning(`failed to resolve discovered SSH alias`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: t, stderr: i },
      }),
      null
    );
  let o = a2(r);
  if (!o.hostname)
    return (
      X0().info(`resolved_ssh_alias_missing_hostname`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: t },
      }),
      null
    );
  let s = o.hostname.trim().toLowerCase();
  return e2.has(s)
    ? (X0().info(`resolved_ssh_alias_excluded`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: t, hostname: o.hostname },
      }),
      null)
    : {
        hostId: n.Ja(t),
        displayName: t,
        source: `discovered`,
        autoConnect: !1,
        sshAlias: t,
        sshHost: o.hostname,
        sshPort: o.port,
        identity: o.identity,
      };
}
function i2({
  configPath: e,
  sshConfigRootDirectory: t,
  discoveredAliases: n,
  visitedConfigPaths: r,
}) {
  if (r.has(e)) return;
  r.add(e);
  let i = q0.parse(p.default.readFileSync(e, `utf8`));
  for (let e of i) {
    if (e.type !== H0.DIRECTIVE) continue;
    let i = e.param.toLowerCase();
    if (i === `host` && `config` in e) {
      let t = c2(e)[0];
      t != null && n.add(t);
      continue;
    }
    if (i === `include`)
      for (let i of s2({ sshConfigRootDirectory: t, includeValue: l2(e) }))
        i2({
          configPath: i,
          sshConfigRootDirectory: t,
          discoveredAliases: n,
          visitedConfigPaths: r,
        });
  }
}
function a2(e) {
  let t = q0.parse(e),
    n = null,
    r = null,
    i = null;
  for (let e of o2(t)) {
    if (e.type !== H0.DIRECTIVE) continue;
    let t = e.param.toLowerCase(),
      a = u2(e);
    if (a != null) {
      if (t === `hostname` && n == null) {
        n = a;
        continue;
      }
      if (t === `port` && r == null) {
        let e = Number(a);
        Number.isSafeInteger(e) && (r = e);
        continue;
      }
      t === `identityfile` && i == null && a !== `none` && (i = a);
    }
  }
  return { hostname: n, port: r, identity: i };
}
function o2(e) {
  let t = [];
  for (let n of e)
    n.type === H0.DIRECTIVE &&
      (t.push(n), `config` in n && t.push(...o2(n.config)));
  return t;
}
function s2({ sshConfigRootDirectory: e, includeValue: t }) {
  let n = [];
  for (let r of t) {
    let t = d2(r),
      i = u.default.isAbsolute(t) ? t : u.default.resolve(e, t),
      a = p.default.globSync(i);
    if (a.length === 0 && !Q0.test(i)) {
      p.default.existsSync(i) && n.push(i);
      continue;
    }
    n.push(...a.filter((e) => p.default.statSync(e).isFile()));
  }
  return n;
}
function c2(e) {
  return l2(e)
    .filter((e) => e.length > 0)
    .filter((e) => !Z0.test(e));
}
function l2(e) {
  return Array.isArray(e.value)
    ? e.value.map((e) => e.val).filter(Boolean)
    : e.value.split(/\s+/u).filter(Boolean);
}
function u2(e) {
  if (Array.isArray(e.value))
    return e.value
      .map((e) => e.val)
      .join(``)
      .trim();
  let t = e.value.trim();
  return t.length === 0 ? null : t;
}
function d2(e) {
  return e === `~`
    ? l.default.homedir()
    : e.startsWith(`~/`) || e.startsWith(`~\\`)
      ? u.default.join(l.default.homedir(), e.slice(2))
      : e;
}
var f2 = `app_EMoamEEZ73f0CkXaXp7hrann`,
  p2 = `https://auth.openai.com`,
  m2 = `CODEX_APP_SERVER_LOGIN_ISSUER`,
  h2 = [1455, 1457],
  g2 = `/auth/callback`,
  _2 = 10 * 6e4,
  v2 = `codex.remote_control.enroll`,
  y2 = `remote_control`,
  b2 = `codex://settings/connections`,
  x2 = n.xl({ access_token: n.wl().min(1) }).passthrough();
async function S2({
  accountId: e,
  desktopApiOptions: t,
  fetchToken: n = (e, t) => c.net.fetch(e, t),
  openExternalUrl: r = (e) =>
    IA({
      request: {
        url: e,
        initiator: `open_in_browser_bridge`,
        openTarget: `external-browser`,
      },
    }),
  timeoutMs: i = _2,
}) {
  let a = w2(),
    o = E2(),
    s = D2(32),
    l = await k2({ state: s, timeoutMs: i });
  l.authorizationCode.catch(() => {});
  try {
    if (
      (await r(
        C2({
          issuer: a,
          clientId: f2,
          redirectUri: l.redirectUri,
          codeChallenge: o.codeChallenge,
          state: s,
          originator: t.desktopOriginator,
          accountId: e,
        }),
      )) === !1
    )
      throw Error(
        `Failed to open remote control login in the default browser.`,
      );
    return (
      await O2({
        code: await l.authorizationCode,
        codeVerifier: o.codeVerifier,
        clientId: f2,
        issuer: a,
        redirectUri: l.redirectUri,
        fetchToken: n,
      })
    ).access_token;
  } finally {
    l.close();
  }
}
function C2({
  issuer: e,
  clientId: t,
  redirectUri: n,
  codeChallenge: r,
  state: i,
  originator: a,
  accountId: o,
}) {
  let s = new URL(`/oauth/authorize`, T2(e)),
    c = new URLSearchParams({
      response_type: `code`,
      client_id: t,
      redirect_uri: n,
      scope: v2,
      code_challenge: r,
      code_challenge_method: `S256`,
      state: i,
      originator: a,
      reauth: y2,
      max_age: `0`,
      codex_cli_simplified_flow: `true`,
    });
  return (
    o != null &&
      (c.set(`allowed_workspace_id`, o), c.set(`current_workspace_id`, o)),
    (s.search = c.toString()),
    s.toString()
  );
}
function w2() {
  return process.env[m2]?.trim() || p2;
}
function T2(e) {
  return e.replace(/\/+$/, ``);
}
function E2() {
  let e = D2(32);
  return {
    codeVerifier: e,
    codeChallenge: (0, f.createHash)(`sha256`).update(e).digest(`base64url`),
  };
}
function D2(e) {
  return (0, f.randomBytes)(e).toString(`base64url`);
}
async function O2({
  code: e,
  codeVerifier: t,
  clientId: n,
  issuer: r,
  redirectUri: i,
  fetchToken: a,
}) {
  let o = await a(new URL(`/oauth/token`, T2(r)).toString(), {
    method: `POST`,
    headers: { "Content-Type": `application/x-www-form-urlencoded` },
    body: new URLSearchParams({
      grant_type: `authorization_code`,
      code: e,
      redirect_uri: i,
      client_id: n,
      code_verifier: t,
    }).toString(),
  });
  if (!o.ok)
    throw Error(
      `Remote control step-up token exchange failed with status ${o.status}.`,
    );
  return x2.parse(await o.json());
}
async function k2({ state: e, timeoutMs: t }) {
  let n = null,
    r = null,
    i = null,
    a = null,
    o = null,
    s = !1,
    c = (e) => {
      s || ((s = !0), i != null && (clearTimeout(i), (i = null)), e());
    },
    l = new Promise((e, t) => {
      ((a = e), (o = t));
    });
  for (let t of h2) {
    let i = (0, w.createServer)((n, r) => {
      try {
        if (n.url == null) {
          A2(r, 400, `Bad Request`);
          return;
        }
        let i = new URL(n.url, `http://localhost:${t}`);
        if (i.pathname !== g2) {
          A2(r, 404, `Not Found`);
          return;
        }
        if (i.searchParams.get(`state`) !== e) {
          A2(r, 400, `State mismatch`);
          return;
        }
        let s = i.searchParams.get(`error`);
        if (s != null) {
          let e = i.searchParams.get(`error_description`);
          (A2(r, 400, `Remote control login failed`),
            c(() => {
              o?.(Error(e == null || e.length === 0 ? s : `${s}: ${e}`));
            }));
          return;
        }
        let l = i.searchParams.get(`code`);
        if (l == null || l.length === 0) {
          A2(r, 400, `Missing authorization code`);
          return;
        }
        (j2(r),
          c(() => {
            a?.(l);
          }));
      } catch (e) {
        (A2(r, 400, `Bad Request`),
          c(() => {
            o?.(e);
          }));
      }
    });
    try {
      (await new Promise((e, n) => {
        (i.once(`error`, n), i.listen(t, `localhost`, e));
      }),
        (n = i),
        (r = t));
      break;
    } catch (e) {
      if (
        (i.close(),
        !(e instanceof Error) || !(`code` in e) || e.code !== `EADDRINUSE`)
      )
        throw e;
    }
  }
  if (n == null || r == null)
    throw Error(
      `Remote control login callback ports 1455 and 1457 are already in use.`,
    );
  return (
    (i = setTimeout(() => {
      c(() => {
        o?.(Error(`Timed out waiting for remote control step-up login.`));
      });
    }, t)),
    {
      redirectUri: `http://localhost:${r}${g2}`,
      authorizationCode: l,
      close: () => {
        (n?.close(),
          c(() => {
            o?.(Error(`Remote control step-up login was cancelled.`));
          }));
      },
    }
  );
}
function A2(e, t, n) {
  (e.writeHead(t, {
    "Content-Type": `text/plain; charset=utf-8`,
    Connection: `close`,
  }),
    e.end(n));
}
function j2(e) {
  let t = a.W();
  (e.writeHead(200, {
    "Content-Type": `text/html; charset=utf-8`,
    Connection: `close`,
  }),
    e.end(`<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="color-scheme" content="light dark">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Remote control authorized · ${t}</title>
    <style>
      :root {
        --background: #ffffff;
        --surface: #ffffff;
        --text-primary: #0d0d0d;
        --text-secondary: rgb(0 0 0 / 60%);
        --text-tertiary: rgb(0 0 0 / 29%);
        --border: rgb(0 0 0 / 5%);
        --button: #0d0d0d;
        --button-hover: #2f2f2f;
        --button-text: #ffffff;
        --card-shadow: 0 10px 15px -3px rgb(0 0 0 / 8%), 0 4px 6px -4px rgb(0 0 0 / 8%);
      }

      * {
        box-sizing: border-box;
      }

      body {
        align-items: center;
        background: var(--background);
        color: var(--text-primary);
        display: flex;
        font-family: ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        justify-content: center;
        margin: 0;
        min-height: 100vh;
        padding: 24px;
      }

      .success-card {
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 16px;
        box-shadow: var(--card-shadow);
        display: flex;
        flex-direction: column;
        gap: 24px;
        max-width: 576px;
        padding: 32px;
        text-align: center;
        width: 100%;
      }

      .codex-icon {
        color: var(--text-primary);
        height: 64px;
        margin: 0 auto;
        width: 64px;
      }

      .copy {
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      h1 {
        font-size: 30px;
        font-weight: 600;
        letter-spacing: -0.02em;
        line-height: 36px;
        margin: 0;
      }

      p {
        color: var(--text-secondary);
        font-size: 16px;
        line-height: 24px;
        margin: 0;
      }

      .actions {
        align-items: center;
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      .return-link {
        align-items: center;
        background: var(--button);
        border: 1px solid transparent;
        border-radius: 9999px;
        color: var(--button-text);
        display: inline-flex;
        font-size: 14px;
        font-weight: 500;
        justify-content: center;
        min-height: 44px;
        padding: 0 22px;
        text-decoration: none;
      }

      .return-link:hover {
        background: var(--button-hover);
      }

      .return-link:focus-visible {
        outline: 1.5px solid var(--text-primary);
        outline-offset: 2.5px;
      }

      .close-hint {
        color: var(--text-tertiary);
        font-size: 13px;
        line-height: 18px;
      }

      @media (prefers-color-scheme: dark) {
        :root {
          --background: #212121;
          --surface: #2f2f2f;
          --text-primary: #f2f2f2;
          --text-secondary: rgb(255 255 255 / 70%);
          --text-tertiary: rgb(255 255 255 / 58%);
          --border: rgb(255 255 255 / 5%);
          --button: #f9f9f9;
          --button-hover: #ececec;
          --button-text: #0d0d0d;
          --card-shadow: 0 10px 15px -3px rgb(0 0 0 / 24%), 0 4px 6px -4px rgb(0 0 0 / 24%);
        }
      }

      @media (max-width: 520px) {
        .success-card {
          padding: 24px;
        }

        h1 {
          font-size: 26px;
          line-height: 32px;
        }
      }
    </style>
  </head>
  <body>
    <main class="success-card">
      <svg class="codex-icon" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M13.333 11.418C13.7002 11.418 13.9978 11.7159 13.998 12.083C13.998 12.4503 13.7003 12.748 13.333 12.748H10.833C10.4657 12.748 10.168 12.4503 10.168 12.083C10.1682 11.7159 10.4659 11.418 10.833 11.418H13.333Z" fill="currentColor" />
        <path d="M6.74121 7.34668C7.0561 7.15796 7.46442 7.26036 7.65332 7.5752L8.90332 9.6582C9.02949 9.86874 9.02961 10.1323 8.90332 10.3428L7.65332 12.4258C7.46441 12.7403 7.05597 12.8427 6.74121 12.6543C6.42637 12.4654 6.32396 12.0561 6.5127 11.7412L7.55664 10L6.5127 8.25879C6.324 7.94395 6.4265 7.53562 6.74121 7.34668Z" fill="currentColor" />
        <path fill-rule="evenodd" clip-rule="evenodd" d="M9.00195 1.75C10.1157 1.75021 11.1362 2.15467 11.9238 2.82227C12.1849 2.77516 12.455 2.74903 12.7295 2.74902C15.2262 2.74978 17.2507 4.77449 17.251 7.27148C17.2509 7.54581 17.2238 7.81473 17.1768 8.0752C17.8448 8.86317 18.2499 9.88479 18.25 10.999C18.2496 12.9609 16.9996 14.6284 15.2549 15.2549C14.6285 16.9998 12.9608 18.2497 10.999 18.25C9.88486 18.25 8.86411 17.8448 8.07617 17.1768C7.8155 17.2239 7.54592 17.2509 7.27148 17.251C4.77445 17.2507 2.7504 15.2257 2.75 12.7285C2.75003 12.4539 2.77608 12.1848 2.82324 11.9238C2.20237 11.1913 1.80895 10.2574 1.75684 9.23438L1.75 9.00098C1.75022 7.03932 2.99952 5.36992 4.74414 4.74316C5.37104 2.99851 7.04034 1.75002 9.00195 1.75ZM9.00195 3.07812C7.52474 3.07814 6.27967 4.08156 5.91504 5.44531C5.85362 5.67419 5.67418 5.85363 5.44531 5.91504C4.08208 6.27984 3.07836 7.52408 3.07812 9.00098C3.07826 9.88321 3.43594 10.682 4.01465 11.2607C4.1816 11.4283 4.24663 11.6728 4.18555 11.9014C4.11505 12.1653 4.07719 12.4429 4.07715 12.7285C4.07755 14.4925 5.50753 15.9225 7.27148 15.9229C7.55712 15.9228 7.83548 15.886 8.09961 15.8154L8.18652 15.7979C8.38833 15.7722 8.59297 15.8403 8.73926 15.9863C9.31801 16.5649 10.1168 16.9218 10.999 16.9219C12.4759 16.9216 13.7203 15.9183 14.085 14.5547L14.1133 14.4707C14.1918 14.2821 14.3542 14.1386 14.5547 14.085C15.9181 13.7203 16.9225 12.4758 16.9229 10.999C16.9228 10.1168 16.5648 9.31802 15.9863 8.73926C15.819 8.57175 15.7544 8.3274 15.8154 8.09863C15.886 7.83454 15.9238 7.5568 15.9238 7.27148C15.9235 5.50751 14.4924 4.07762 12.7285 4.07715C12.4424 4.0772 12.164 4.11412 11.9004 4.18457C11.672 4.24541 11.4282 4.18048 11.2607 4.01367C10.7183 3.47141 9.98306 3.12271 9.16699 3.08105L9.00195 3.07812Z" fill="currentColor" />
      </svg>
      <div class="copy">
        <h1>Remote control authorized</h1>
        <p>You can return to ${t} to finish connecting this device.</p>
      </div>
      <div class="actions">
        <a class="return-link" href="${b2}">Return to ${t}</a>
        <p class="close-hint">This tab is safe to close.</p>
      </div>
    </main>
    <script>window.location.href = ${JSON.stringify(b2)};<\/script>
  </body>
</html>`));
}
var M2 = r.a(`remote-connections/window-context`),
  N2 = 250,
  P2 = class {
    remoteConnectionsByHostId = new Map();
    remoteControlRefreshGeneration = 0;
    shouldLoadRemoteControlConnections = !1;
    shouldLoadWslConnections = !1;
    constructor(e, t, n, r, i, a, o, s, c, l, u, d) {
      ((this.appState = e),
        (this.sharedObjectRepository = t),
        (this.owningHostId = n),
        (this.appServerConnectionRegistry = r),
        (this.desktopApiOptions = i),
        (this.desktopAuthAppServerClient = a),
        (this.remoteControlDeviceKeyClient = o),
        (this.getHostConfigForHostId = s),
        (this.getAppServerClientForHostId = c),
        (this.createAndRegisterRemoteConnection = l),
        (this.disposeRemoteConnection = u),
        (this.reconcileAuthoritativeCatalogHosts = d));
    }
    reconcileRemoteConnections(e) {
      let t = this.remoteConnectionsByHostId.size,
        n = 0,
        r = new Map();
      for (let t of e) t.hostId !== this.owningHostId && r.set(t.hostId, t);
      for (let e of r.values()) {
        let t = e.hostId,
          r = this.remoteConnectionsByHostId.get(t),
          i = this.appServerConnectionRegistry.getMaybeConnection(t);
        if (!e.autoConnect) {
          i != null && this.disposeRemoteConnection(t);
          continue;
        }
        if (!(i != null && L2(r, e))) {
          i != null && this.disposeRemoteConnection(t);
          try {
            this.appServerConnectionRegistry.addConnection(
              t,
              this.createAndRegisterRemoteConnection(e),
            );
          } catch (e) {
            ((n += 1),
              M2().warning(`reconcile_create_connection_failed`, {
                safe: {},
                sensitive: { error: e, hostId: t },
              }));
          }
        }
      }
      for (let e of this.remoteConnectionsByHostId.keys())
        r.has(e) || this.disposeRemoteConnection(e);
      this.remoteConnectionsByHostId.clear();
      for (let [e, t] of r) this.remoteConnectionsByHostId.set(e, t);
      M2().info(`reconcile_completed`, {
        safe: {
          nextConnectionCount: r.size,
          previousConnectionCount: t,
          creationFailureCount: n,
        },
        sensitive: {},
      });
    }
    handleLocalRemoteControlIdentityChanged() {
      (this.removeLocalRemoteControlConnection(),
        this.shouldLoadRemoteControlConnections &&
          this.refreshRemoteControlConnections().catch((e) => {
            M2().warning(`refresh_remote_control_for_local_identity_failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }));
    }
    async refreshRemoteConnections() {
      let e = this.remoteConnectionsByHostId.size;
      M2().info(`refresh_started`, {
        safe: { previousConnectionCount: e },
        sensitive: {},
      });
      let t = await this.loadRemoteConnections();
      (this.reconcileAuthoritativeCatalogHosts?.(
        `ssh`,
        new Set(this.loadSavedRemoteConnections().map((e) => e.hostId)),
      ),
        this.sharedObjectRepository.set(`remote_ssh_connections`, t));
      let n = F2(t);
      return (
        M2().info(`refresh_completed`, {
          safe: {
            previousConnectionCount: e,
            nextConnectionCount: t.length,
            discoveredConnectionCount: n.discovered,
            codexManagedConnectionCount: n.codexManaged,
            autoConnectConnectionCount: t.filter((e) => e.autoConnect).length,
          },
          sensitive: {},
        }),
        { remoteConnections: this.getSharedRemoteConnections() }
      );
    }
    async discoverRemoteConnections() {
      return { discoveredRemoteConnections: await t2(z2()) };
    }
    async refreshRemoteControlConnections() {
      let e = ++this.remoteControlRefreshGeneration;
      if (!this.shouldLoadRemoteControlConnections)
        return (
          this.sharedObjectRepository.set(`remote_control_connections`, []),
          this.sharedObjectRepository.set(`remote_control_connections_state`, {
            available: !1,
            accessRequired: !1,
            authRequired: !1,
            clientAuthorized: !1,
          }),
          { remoteControlConnections: [] }
        );
      let t = this.sharedObjectRepository.get(`remote_control_connections`),
        n =
          this.sharedObjectRepository.get(`remote_control_connections_state`)
            ?.clientAuthorized === !0,
        r = t?.length ?? 0;
      M2().info(`refresh_remote_control_started`, {
        safe: { previousConnectionCount: r },
        sensitive: {},
      });
      let i = await this.loadRemoteControlConnections(e);
      if (e !== this.remoteControlRefreshGeneration)
        return {
          remoteControlConnections:
            this.sharedObjectRepository.get(`remote_control_connections`) ?? [],
        };
      let a = this.filterLocalRemoteControlConnection(i).map((e) => ({
        ...e,
        autoConnect: this.getAutoConnectForRemoteConnection(e),
      }));
      this.sharedObjectRepository.set(`remote_control_connections`, a);
      let o =
        this.sharedObjectRepository.get(`remote_control_connections_state`)
          ?.clientAuthorized === !0;
      return (
        o &&
          this.reconcileAuthoritativeCatalogHosts?.(
            `remote-control`,
            new Set(a.map((e) => e.hostId)),
          ),
        o
          ? this.connectRemoteConnectionsAndLogFailures(
              a.filter((e) => e.autoConnect).map((e) => e.hostId),
            )
          : n &&
            (await Promise.all(
              (t ?? []).map((e) => this.disconnectRemoteConnection(e.hostId)),
            )),
        M2().info(`refresh_remote_control_completed`, {
          safe: {
            previousConnectionCount: r,
            nextConnectionCount: a.length,
            autoConnectConnectionCount: a.filter((e) => e.autoConnect).length,
          },
          sensitive: {},
        }),
        { remoteControlConnections: a }
      );
    }
    async setRemoteControlConnectionsEnabled(e) {
      if (this.shouldLoadRemoteControlConnections === e) {
        let e = this.sharedObjectRepository.get(`remote_control_connections`);
        if (e != null) return { remoteControlConnections: e };
      }
      return (
        (this.shouldLoadRemoteControlConnections = e),
        this.refreshRemoteControlConnections()
      );
    }
    async setRemoteWslConnectionsEnabled(e) {
      if (this.shouldLoadWslConnections === e) {
        let e = this.sharedObjectRepository.get(`remote_wsl_connections`);
        if (e != null) return { remoteWslConnections: e };
      }
      return (
        (this.shouldLoadWslConnections = e),
        this.refreshRemoteWslConnections()
      );
    }
    async refreshRemoteWslConnections() {
      let e = this.shouldLoadWslConnections
          ? this.loadRemoteWslConnections()
          : { authoritative: !1, connections: [] },
        t = e.connections;
      return (
        this.shouldLoadWslConnections &&
          e.authoritative &&
          this.reconcileAuthoritativeCatalogHosts?.(
            `wsl`,
            new Set(t.map((e) => e.hostId)),
          ),
        this.sharedObjectRepository.set(`remote_wsl_connections`, t),
        this.connectRemoteConnectionsAndLogFailures(
          t.filter((e) => e.autoConnect).map((e) => e.hostId),
        ),
        M2().info(`refresh_wsl_completed`, {
          safe: {
            enabled: this.shouldLoadWslConnections,
            nextConnectionCount: t.length,
            autoConnectConnectionCount: t.filter((e) => e.autoConnect).length,
          },
          sensitive: {},
        }),
        { remoteWslConnections: t }
      );
    }
    async authorizeRemoteControlConnections() {
      return (
        await yv({
          appServerClient: this.desktopAuthAppServerClient,
          desktopApiOptions: this.desktopApiOptions,
          deviceKeyClient: this.remoteControlDeviceKeyClient,
          globalState: this.appState,
          requestRemoteControlEnrollmentStepUpToken: ({ accountId: e }) =>
            S2({ accountId: e, desktopApiOptions: this.desktopApiOptions }),
        }),
        this.refreshRemoteControlConnections()
      );
    }
    async renameRemoteControlEnvironment({ envId: e, name: t }) {
      let n = t.trim();
      if (n.length === 0)
        throw Error(`Remote control display name cannot be empty.`);
      let r = this.sharedObjectRepository.get(`remote_control_connections`),
        i = r?.find((t) => t.envId === e);
      if (i == null) throw Error(`Remote control environment not found.`);
      let a = await $_({
          appServerClient: this.desktopAuthAppServerClient,
          desktopApiOptions: this.desktopApiOptions,
          envId: e,
          name: n,
        }),
        o =
          this.sharedObjectRepository.get(`remote_control_connections`) ??
          r ??
          [],
        s = o.find((t) => t.envId === e) ?? i,
        c = { ...a, autoConnect: this.getAutoConnectForRemoteConnection(s) },
        l = o.map((t) => (t.envId === e ? c : t));
      return (
        this.sharedObjectRepository.set(`remote_control_connections`, l),
        this.reconcileAuthoritativeCatalogHosts?.(
          `remote-control`,
          new Set(l.map((e) => e.hostId)),
        ),
        { remoteControlConnection: c, remoteControlConnections: l }
      );
    }
    async deleteRemoteControlEnvironment({ envId: e }) {
      let t = this.sharedObjectRepository.get(`remote_control_connections`),
        n = t?.find((t) => t.envId === e);
      if (n == null) throw Error(`Remote control environment not found.`);
      if (n.online)
        throw Error(`Only offline remote control environments can be deleted.`);
      (await ev({
        appServerClient: this.desktopAuthAppServerClient,
        desktopApiOptions: this.desktopApiOptions,
        envId: e,
      }),
        this.clearRemoteConnectionAutoConnect(n.hostId));
      let r = (
        this.sharedObjectRepository.get(`remote_control_connections`) ??
        t ??
        []
      ).filter((t) => t.envId !== e);
      return (
        this.sharedObjectRepository.set(`remote_control_connections`, r),
        this.reconcileAuthoritativeCatalogHosts?.(
          `remote-control`,
          new Set(r.map((e) => e.hostId)),
        ),
        { remoteControlConnections: r }
      );
    }
    async saveCodexManagedRemoteSshConnections(e) {
      let t = this.addConnectionAnalyticsIds(e);
      return (
        this.appState.set(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS, t),
        await this.refreshRemoteConnections(),
        { remoteConnections: t }
      );
    }
    async setRemoteConnectionAutoConnect(e, t) {
      let r = this.getRemoteConnectionAutoConnectByHostId();
      this.appState.set(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, {
        ...r,
        [e]: t,
      });
      let i = await this.refreshRemoteConnectionsForHostId(e);
      return t
        ? {
            remoteConnections: i,
            ...(await this.ensureRemoteConnectionConnected(e)),
          }
        : {
            remoteConnections: i,
            ...(await this.disconnectRemoteConnection(e)),
          };
    }
    async installRemoteCodex(e) {
      let t = Yy(this.getHostConfigForHostId(e));
      if (t == null)
        throw Error(`Installing the Codex CLI requires an SSH connection.`);
      await new qy(t).installCodex();
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      return n?.getConnectionError()?.code === `update-required`
        ? (await n.restart({ killCodexProcess: !0 }),
          { state: n.getConnectionState(), error: n.getConnectionError() })
        : this.ensureRemoteConnectionConnected(e);
    }
    async startRemoteChatgptLoginPortForward(e, t) {
      if (this.getHostConfigForHostId(e).kind === `remote-control`)
        throw Error(
          `Remote control does not support ChatGPT login forwarding yet.`,
        );
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      if (n == null) throw Error(`Connection for host ID ${e} not found`);
      return {
        forwarded: await n.startRemoteChatgptLoginPortForward({ loginId: t }),
      };
    }
    async stopRemoteChatgptLoginPortForward(e, t) {
      if (this.getHostConfigForHostId(e).kind === `remote-control`)
        throw Error(
          `Remote control does not support ChatGPT login forwarding yet.`,
        );
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      return n == null
        ? { stopped: !1 }
        : { stopped: n.stopRemoteChatgptLoginPortForward(t) };
    }
    async appServerConnectionState(e) {
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      return {
        state: t?.getConnectionState() ?? `disconnected`,
        progress: t?.getConnectionProgress() ?? null,
        error: t?.getConnectionError() ?? null,
      };
    }
    async remoteWorkspaceDirectoryEntries(e, t, r = !1) {
      let i = this.getHostConfigForHostId(e),
        a = this.getAppServerClientForHostId(e),
        o = n.ui(i),
        s = await this.getWorkspaceDirectoryPath(a, t),
        c = await this.getParentDirectoryPath(a, s),
        l = await n.ti.readdir(s, { withFileTypes: !0 }, a),
        u = await a.platformPath(),
        d = l
          .map((e) => {
            let t = u.join(s, e.name);
            return {
              name: e.name,
              path: z(t, o),
              type: e.isDirectory() ? `directory` : `file`,
            };
          })
          .filter((e) => !r || e.type === `directory`)
          .sort((e, t) => {
            if (e.type !== t.type) return e.type === `directory` ? -1 : 1;
            let n = e.name.startsWith(`.`);
            return n === t.name.startsWith(`.`)
              ? e.name.localeCompare(t.name)
              : n
                ? 1
                : -1;
          });
      return {
        directoryPath: z(s, o),
        parentPath: c == null ? null : z(c, o),
        entries: d,
      };
    }
    async getWorkspaceDirectoryPath(e, t) {
      let r = t?.trim() || (await this.getDefaultWorkspaceDirectoryPath(e));
      if (!(await n.ti.stat(r, e)).isDirectory())
        throw Error(`Directory not found: ${r}`);
      return r;
    }
    async getDefaultWorkspaceDirectoryPath(e) {
      let t = e.hostConfig;
      if (!n.eo(t)) return (0, l.homedir)();
      let r = await e.platformPath(),
        i = await e.codexHome();
      return r.dirname(i);
    }
    async getParentDirectoryPath(e, t) {
      let n = (await e.platformPath()).dirname(t);
      return n === t ? null : n;
    }
    async connectRemoteConnectionsAndLogFailures(e) {
      let t = 0;
      await Promise.all(
        Array.from(new Set(e), (e) => {
          let n = this.isAutoConnectSshRemoteConnection(e) ? t++ * N2 : 0;
          return this.connectRemoteConnectionAndLogFailure(e, n);
        }),
      );
    }
    isAutoConnectSshRemoteConnection(e) {
      let t = this.remoteConnectionsByHostId.get(e);
      return t != null && t.autoConnect && !n.Qa(t);
    }
    async connectRemoteConnectionAndLogFailure(e, t) {
      if (!(t > 0 && (await R2(t), !this.isAutoConnectSshRemoteConnection(e))))
        try {
          await this.ensureRemoteConnectionConnected(e);
        } catch (t) {
          (M2().warning(`connect_from_config_apply_failed`, {
            safe: {},
            sensitive: { error: t, hostId: e },
          }),
            this.sharedObjectRepository
              .get(`remote_control_connections`)
              ?.some((t) => t.hostId === e) === !0 &&
              (await this.refreshRemoteControlClientAuthorizationState()));
        }
    }
    async ensureRemoteConnectionConnected(e) {
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      return t == null
        ? this.connectRemoteConnection(e)
        : (t.getConnectionState() !== `connected` && (await t.connect()),
          { state: t.getConnectionState(), error: t.getConnectionError() });
    }
    async connectRemoteConnection(e) {
      let t = this.remoteConnectionsByHostId.get(e);
      if (t == null)
        throw Error(`Remote connection for host ID ${e} not found`);
      (this.appServerConnectionRegistry.getMaybeConnection(e) != null &&
        this.disposeRemoteConnection(e),
        M2().info(`connect_requested`, { safe: {}, sensitive: { hostId: e } }));
      let n = null;
      try {
        ((n = this.createAndRegisterRemoteConnection(t)),
          this.appServerConnectionRegistry.addConnection(e, n),
          await n.connect());
      } catch (t) {
        throw (
          M2().warning(`connect_failed`, {
            safe: {},
            sensitive: { error: t, hostId: e },
          }),
          Error(`Failed to connect remote connection.`)
        );
      }
      let r = n?.getConnectionState() ?? `disconnected`,
        i = n?.getConnectionError() ?? null;
      return (
        M2().info(`connect_completed`, {
          safe: { state: r, hasError: i != null },
          sensitive: { hostId: e, connectionError: i },
        }),
        { state: r, error: i }
      );
    }
    async disconnectRemoteConnection(e) {
      M2().info(`disconnect_requested`, { safe: {}, sensitive: { hostId: e } });
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      if (t == null) return { state: `disconnected`, error: null };
      this.disposeRemoteConnection(e);
      let n = t.getConnectionError();
      return (
        M2().info(`disconnect_completed`, {
          safe: { hasError: n != null },
          sensitive: { hostId: e, connectionError: n },
        }),
        { state: `disconnected`, error: n }
      );
    }
    async loadRemoteConnections() {
      let e = await this.restoreSavedAliasConnectionsForExistingRemoteProjects(
          this.loadSavedRemoteConnections(),
        ),
        t = await Promise.all(
          e.flatMap((e) => {
            let t = e.alias?.trim();
            return t
              ? [
                  r2({ entrypointPath: z2(), sshAlias: t }).then((t) =>
                    t == null
                      ? null
                      : {
                          ...t,
                          hostId: e.hostId,
                          connectionAnalyticsId: e.connectionAnalyticsId,
                          displayName: e.displayName,
                          autoConnect: !1,
                        },
                  ),
                ]
              : [];
          }),
        ),
        n = [
          ...e.flatMap((e) =>
            e.hostname == null
              ? []
              : [
                  {
                    hostId: e.hostId,
                    connectionAnalyticsId: e.connectionAnalyticsId,
                    displayName: e.displayName,
                    source: `codex-managed`,
                    autoConnect: !1,
                    sshAlias: null,
                    sshHost: e.hostname,
                    sshPort: e.sshPort,
                    identity: e.identity,
                  },
                ],
          ),
          ...t.filter((e) => e != null),
        ].map((e) => ({
          ...e,
          autoConnect: this.getAutoConnectForRemoteConnection(e),
        })),
        r = this.addConnectionAnalyticsIds(n);
      return (
        M2().info(`load_completed`, {
          safe: {
            savedRemoteConnectionCount: e.length,
            totalConnectionCount: r.length,
          },
          sensitive: {},
        }),
        r
      );
    }
    async restoreSavedAliasConnectionsForExistingRemoteProjects(e) {
      if (
        this.appState.get(n.Gs.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED) ===
        !0
      )
        return e;
      let t = this.appState.get(n.Gs.REMOTE_PROJECTS);
      if (t == null || t.length === 0)
        return (
          M2().info(`remote_project_connection_restore_skipped`, {
            safe: { reason: `no_remote_projects` },
            sensitive: {},
          }),
          this.appState.set(
            n.Gs.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED,
            !0,
          ),
          e
        );
      try {
        let r = await t2(z2()),
          i = I2({
            discoveredRemoteConnections: r,
            remoteProjects: t,
            savedRemoteConnections: e,
          }),
          a = i.length - e.length;
        return (
          (0, af.default)(e, i) ||
            this.appState.set(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS, i),
          M2().info(`remote_project_connection_restore_completed`, {
            safe: {
              discoveredRemoteConnectionCount: r.length,
              remoteProjectCount: t.length,
              restoredConnectionCount: a,
              savedRemoteConnectionCountAfter: i.length,
              savedRemoteConnectionCountBefore: e.length,
            },
            sensitive: {},
          }),
          this.appState.set(
            n.Gs.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED,
            !0,
          ),
          i
        );
      } catch (n) {
        M2().warning(`remote_project_connection_restore_failed`, {
          safe: {
            remoteProjectCount: t.length,
            savedRemoteConnectionCount: e.length,
          },
          sensitive: { error: n },
        });
      }
      return e;
    }
    loadSavedRemoteConnections() {
      let e = this.appState.get(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS) ?? [],
        t = e.map((e) => {
          if (`alias` in e && `hostname` in e)
            return e.alias == null
              ? {
                  hostId: e.hostId,
                  connectionAnalyticsId: e.connectionAnalyticsId,
                  displayName: e.displayName,
                  source: `codex-managed`,
                  alias: null,
                  hostname: e.hostname,
                  sshPort: e.sshPort,
                  identity: e.identity,
                }
              : {
                  hostId: e.hostId,
                  connectionAnalyticsId: e.connectionAnalyticsId,
                  displayName: e.displayName,
                  source: `discovered`,
                  alias: e.alias,
                  hostname: null,
                  sshPort: null,
                  identity: null,
                };
          let t = e,
            n = t.sshAlias?.trim() || null;
          return n == null
            ? {
                hostId: t.hostId,
                connectionAnalyticsId: t.connectionAnalyticsId,
                displayName: t.displayName,
                source: `codex-managed`,
                alias: null,
                hostname: t.sshHost,
                sshPort: t.sshPort,
                identity: t.identity,
              }
            : {
                hostId: t.hostId,
                connectionAnalyticsId: t.connectionAnalyticsId,
                displayName: t.displayName,
                source: `discovered`,
                alias: n,
                hostname: null,
                sshPort: null,
                identity: null,
              };
        });
      return (
        (0, af.default)(e, t) ||
          this.appState.set(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS, t),
        t
      );
    }
    async refreshRemoteConnectionsForHostId(e) {
      return (
        this.sharedObjectRepository
          .get(`remote_control_connections`)
          ?.some((t) => t.hostId === e) === !0
          ? await this.refreshRemoteControlConnections()
          : this.sharedObjectRepository
                .get(`remote_wsl_connections`)
                ?.some((t) => t.hostId === e) === !0
            ? await this.refreshRemoteWslConnections()
            : await this.refreshRemoteConnections(),
        this.getSharedRemoteConnections()
      );
    }
    getSharedRemoteConnections() {
      return [
        ...(this.sharedObjectRepository.get(`remote_ssh_connections`) ?? []),
        ...(this.sharedObjectRepository.get(`remote_wsl_connections`) ?? []),
        ...(this.sharedObjectRepository.get(`remote_control_connections`) ??
          []),
      ];
    }
    loadRemoteWslConnections() {
      let e = n.oi();
      if (!e.available || e.distro == null)
        return { authoritative: e.distro == null, connections: [] };
      let t = {
        hostId: n.Xa(e.distro),
        displayName: `WSL: ${e.distro}`,
        source: `wsl`,
        distro: e.distro,
      };
      return {
        authoritative: !0,
        connections: this.addConnectionAnalyticsIds([
          { ...t, autoConnect: this.getAutoConnectForRemoteConnection(t, !0) },
        ]),
      };
    }
    getAutoConnectForRemoteConnection(e, t = !1) {
      return this.getRemoteConnectionAutoConnectByHostId()[e.hostId] ?? t;
    }
    getRemoteConnectionAutoConnectByHostId() {
      return (
        this.appState.get(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID) ?? {}
      );
    }
    addConnectionAnalyticsIds(e) {
      let t =
          this.appState.get(n.Gs.REMOTE_CONNECTION_ANALYTICS_ID_BY_HOST_ID) ??
          {},
        r = null,
        i = e.map((e) => {
          let n = t[e.hostId] ?? e.connectionAnalyticsId ?? (0, f.randomUUID)();
          return (
            t[e.hostId] !== n && ((r ??= { ...t }), (r[e.hostId] = n)),
            { ...e, connectionAnalyticsId: n }
          );
        });
      return (
        r != null &&
          this.appState.set(n.Gs.REMOTE_CONNECTION_ANALYTICS_ID_BY_HOST_ID, r),
        i
      );
    }
    clearRemoteConnectionAutoConnect(e) {
      let t = this.getRemoteConnectionAutoConnectByHostId();
      if (!(e in t)) return;
      let r = { ...t };
      (delete r[e],
        this.appState.set(n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, r));
    }
    async loadRemoteControlConnections(e) {
      try {
        let t = this.getRemoteControlClientEnrollmentStatus(),
          n = Ke().remoteControlClientScopedEnvironments
            ? t.then(({ clientId: e }) =>
                e == null
                  ? []
                  : Q_({
                      appServerClient: this.desktopAuthAppServerClient,
                      desktopApiOptions: this.desktopApiOptions,
                      clientId: e,
                    }),
              )
            : Q_({
                appServerClient: this.desktopAuthAppServerClient,
                clientId: void 0,
                desktopApiOptions: this.desktopApiOptions,
              }),
          [r, { clientAuthorized: i, clientId: a }] = await Promise.all([n, t]);
        return e === this.remoteControlRefreshGeneration
          ? (this.sharedObjectRepository.set(
              `local_remote_control_client_id`,
              a,
            ),
            this.sharedObjectRepository.set(
              `remote_control_connections_state`,
              {
                available: !0,
                accessRequired: !1,
                authRequired: !1,
                clientAuthorized: i,
              },
            ),
            r)
          : r;
      } catch (t) {
        if (e !== this.remoteControlRefreshGeneration) return [];
        if (t instanceof Y_)
          return (
            this.sharedObjectRepository.set(
              `local_remote_control_client_id`,
              null,
            ),
            this.sharedObjectRepository.set(
              `remote_control_connections_state`,
              {
                available: !1,
                accessRequired: !1,
                authRequired: !1,
                clientAuthorized: !1,
              },
            ),
            []
          );
        if (t instanceof Z_)
          return (
            this.sharedObjectRepository.set(
              `remote_control_connections_state`,
              {
                available: !1,
                accessRequired: !0,
                authRequired: !1,
                clientAuthorized: !1,
              },
            ),
            []
          );
        if (t instanceof X_)
          return (
            this.sharedObjectRepository.set(
              `local_remote_control_client_id`,
              null,
            ),
            this.sharedObjectRepository.set(
              `remote_control_connections_state`,
              {
                available: !0,
                accessRequired: !1,
                authRequired: !0,
                clientAuthorized: !1,
              },
            ),
            M2().warning(`load_remote_control_unauthed`, {
              safe: {},
              sensitive: { error: t },
            }),
            []
          );
        throw (
          this.sharedObjectRepository.set(`remote_control_connections_state`, {
            available: !0,
            accessRequired: !1,
            authRequired: !1,
            clientAuthorized: !1,
          }),
          this.sharedObjectRepository.set(
            `local_remote_control_client_id`,
            null,
          ),
          t
        );
      }
    }
    removeLocalRemoteControlConnection() {
      let e = this.sharedObjectRepository.get(`remote_control_connections`);
      if (e == null) return;
      let t = this.filterLocalRemoteControlConnection(e);
      t.length !== e.length &&
        this.sharedObjectRepository.set(`remote_control_connections`, t);
    }
    filterLocalRemoteControlConnection(e) {
      let t = this.sharedObjectRepository.get(
          `local_remote_control_environment_id`,
        ),
        n = this.sharedObjectRepository.get(
          `local_remote_control_installation_id`,
        );
      return t == null && n == null
        ? e
        : e.filter(
            (e) => e.envId !== t && (n == null || e.installationId !== n),
          );
    }
    async refreshLocalRemoteControlClientId() {
      try {
        let { clientId: e } =
          await this.getRemoteControlClientEnrollmentStatus();
        this.sharedObjectRepository.set(`local_remote_control_client_id`, e);
      } catch (e) {
        M2().warning(`refresh_local_remote_control_client_id_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async refreshRemoteControlClientAuthorizationState() {
      try {
        let { clientAuthorized: e, clientId: t } =
          await this.getRemoteControlClientEnrollmentStatus();
        this.sharedObjectRepository.set(`local_remote_control_client_id`, t);
        let n = this.sharedObjectRepository.get(
          `remote_control_connections_state`,
        );
        if (n == null) return;
        this.sharedObjectRepository.set(`remote_control_connections_state`, {
          ...n,
          clientAuthorized: e,
        });
      } catch (e) {
        M2().warning(
          `refresh_remote_control_client_authorization_state_failed`,
          { safe: {}, sensitive: { error: e } },
        );
      }
    }
    getRemoteControlClientEnrollmentStatus() {
      return bv({
        appServerClient: this.desktopAuthAppServerClient,
        desktopApiOptions: this.desktopApiOptions,
        deviceKeyClient: this.remoteControlDeviceKeyClient,
        globalState: this.appState,
      });
    }
  };
function F2(e) {
  return e.reduce(
    (e, t) => (
      t.source === `codex-managed`
        ? (e.codexManaged += 1)
        : (e.discovered += 1),
      e
    ),
    { codexManaged: 0, discovered: 0 },
  );
}
function I2({
  discoveredRemoteConnections: e,
  remoteProjects: t,
  savedRemoteConnections: n,
}) {
  let r = new Set(
      n.flatMap((e) => {
        let t = e.alias?.trim();
        return t ? [t] : [];
      }),
    ),
    i = new Set(t.map((e) => e.hostId)),
    a = e.flatMap((e) => {
      let t = e.sshAlias?.trim();
      return !t || r.has(t) || !i.has(e.hostId)
        ? []
        : (r.add(t),
          [
            {
              hostId: e.hostId,
              displayName: e.displayName,
              source: `discovered`,
              alias: t,
              hostname: null,
              sshPort: null,
              identity: null,
            },
          ]);
    });
  return a.length === 0 ? n : [...n, ...a];
}
function L2(e, t) {
  return e == null
    ? !1
    : (0, af.default)(e, t) ||
        (!e.autoConnect &&
          t.autoConnect &&
          (0, af.default)({ ...e, autoConnect: t.autoConnect }, t))
      ? !0
      : n.Qa(e) && n.Qa(t) && e.hostId === t.hostId && e.envId === t.envId;
}
async function R2(e) {
  await new Promise((t) => {
    setTimeout(t, e);
  });
}
function z2() {
  return (0, u.join)((0, l.homedir)(), `.ssh`, `config`);
}
var B2 = `config.toml`;
async function V2({ codexHome: e, hostConfig: t, logger: i = r.r() }) {
  if (t.kind === `local`)
    try {
      (await H2(u.default.join(e ?? n.ni({ preferWsl: n.ui(t) }), B2))) &&
        i.info(`Removed remote_control from config before app-server start`);
    } catch (e) {
      i.warning(`Failed to remove remote_control before app-server start`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
async function H2(e) {
  let t;
  try {
    t = await (0, m.readFile)(e, `utf8`);
  } catch (e) {
    if (K2(e)) return !1;
    throw e;
  }
  let r = n.Qr(t, { integersAsBigInt: !1 });
  if (!W2(r)) return !1;
  let i = L(U2(r));
  return (
    await (0, m.writeFile)(
      e,
      i.trim() === ``
        ? ``
        : i.endsWith(`
`)
          ? i
          : `${i}\n`,
      `utf8`,
    ),
    !0
  );
}
function U2(e) {
  let t = { ...e };
  if (
    (Object.hasOwn(t, `remote_control`) && delete t.remote_control,
    !G2(t.features))
  )
    return t;
  let n = { ...t.features };
  return (
    Object.hasOwn(n, `remote_control`) && delete n.remote_control,
    Object.keys(n).length === 0 ? (delete t.features, t) : ((t.features = n), t)
  );
}
function W2(e) {
  return (
    Object.hasOwn(e, `remote_control`) ||
    (G2(e.features) && Object.hasOwn(e.features, `remote_control`))
  );
}
function G2(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function K2(e) {
  return e instanceof Error && `code` in e && e.code === `ENOENT`;
}
var q2 = (0, T.createRequire)(__filename),
  J2 = `remote-control-device-key.node`,
  Y2 = `codex-device-key-sign-payload/v1`,
  X2 = /^[A-Za-z0-9_-]+$/u;
function Z2({ resourcesPath: e }) {
  let t = null,
    n = () => {
      if (process.platform !== `darwin`)
        throw Error(`Remote control device keys are only available on macOS`);
      if (e == null)
        throw Error(`Remote control device keys require resourcesPath`);
      return ((t ??= q2((0, u.join)(e, `native`, J2))), t);
    };
  return {
    createDeviceKey: (e) => n().createDeviceKey(e ?? `hardware_only`),
    deleteDeviceKey: (e) => n().deleteDeviceKey(e),
    getDeviceKeyPublic: (e) => n().getDeviceKeyPublic(e),
    signDeviceKey: async (e, t) => {
      let r = Q2(t);
      return {
        ...(await n().signDeviceKey(e, r)),
        signedPayloadBase64: r.toString(`base64`),
      };
    },
  };
}
function Q2(e) {
  return Buffer.from(JSON.stringify({ domain: Y2, payload: $2(e) }), `utf8`);
}
function $2(e) {
  switch (e.type) {
    case `remoteControlClientConnection`:
      if ((e4(e.nonce), e.audience !== `remote_control_client_websocket`))
        throw Error(`Invalid remote-control device-key connection audience`);
      if (
        e.scopes.length !== 1 ||
        e.scopes[0] !== `remote_control_controller_websocket`
      )
        throw Error(`Invalid remote-control device-key connection scopes`);
      return (
        t4(e.tokenSha256Base64url),
        {
          accountUserId: e.accountUserId,
          audience: e.audience,
          clientId: e.clientId,
          nonce: e.nonce,
          scopes: e.scopes,
          sessionId: e.sessionId,
          targetOrigin: e.targetOrigin,
          targetPath: e.targetPath,
          tokenExpiresAt: e.tokenExpiresAt,
          tokenSha256Base64url: e.tokenSha256Base64url,
          type: e.type,
        }
      );
    case `remoteControlClientEnrollment`:
      if ((e4(e.nonce), e.audience !== `remote_control_client_enrollment`))
        throw Error(`Invalid remote-control device-key enrollment audience`);
      return (
        t4(e.deviceIdentitySha256Base64url),
        {
          accountUserId: e.accountUserId,
          audience: e.audience,
          challengeExpiresAt: e.challengeExpiresAt,
          challengeId: e.challengeId,
          clientId: e.clientId,
          deviceIdentitySha256Base64url: e.deviceIdentitySha256Base64url,
          nonce: e.nonce,
          targetOrigin: e.targetOrigin,
          targetPath: e.targetPath,
          type: e.type,
        }
      );
  }
}
function e4(e) {
  if (!X2.test(e) || Buffer.from(e, `base64url`).length < 32)
    throw Error(`Invalid remote-control device-key nonce`);
}
function t4(e) {
  if (!X2.test(e) || Buffer.from(e, `base64url`).length !== 32)
    throw Error(`Invalid remote-control device-key SHA-256 digest`);
}
var n4 = `(remote) `;
function r4(e) {
  let t = {
    id: e.id,
    display_name: e.displayName,
    kind: `ssh`,
    codex_cli_command: e.codexCliCommand,
    terminal_command: e.terminalCommand,
    default_workspaces: e.defaultWorkspaces ?? [],
    [k_]: {
      sshAlias: e.sshAlias ?? null,
      sshHost: e.sshHost,
      sshPort: e.sshPort,
      identity: e.identity,
    },
  };
  return (e.homeDir && (t.home_dir = e.homeDir), t);
}
function i4(e) {
  let t = n.Za({
    alias: e.sshAlias,
    hostname: e.sshHost,
    sshPort: e.sshPort,
    identity: e.identity,
  });
  return r4({
    id: e.hostId,
    displayName: `${n4}${e.displayName}`,
    sshAlias: e.sshAlias ?? null,
    sshHost: e.sshHost,
    sshPort: e.sshPort,
    identity: e.identity,
    codexCliCommand: [],
    terminalCommand: [`ssh`, ...t],
    defaultWorkspaces: [],
  });
}
function a4(e) {
  return {
    id: e.hostId,
    display_name: e.displayName,
    kind: n.qa,
    codex_cli_command: [],
    terminal_command: [],
    default_workspaces: [],
    env_id: e.envId,
    host_name: e.hostName,
    environment_kind: e.environmentKind,
    online: e.online,
    busy: e.busy,
    os: e.os,
    arch: e.arch,
    app_server_version: e.appServerVersion,
    last_seen_at: e.lastSeenAt,
  };
}
function o4(e) {
  return {
    id: e.hostId,
    display_name: e.displayName,
    kind: `wsl`,
    codex_cli_command: gb(e.distro),
    terminal_command: [`wsl.exe`, `-d`, e.distro],
    default_workspaces: [],
    distro: e.distro,
  };
}
function s4(e) {
  return n.Qa(e) ? a4(e) : n.ro(e) ? o4(e) : i4(e);
}
var c4 = class extends a.P {
    constructor(e, t) {
      (super(), (this.appServerConnectionRegistry = e), (this.surfaceId = t));
    }
    recordConversationActivity({ conversationId: e, hostId: t }) {
      this.appServerConnectionRegistry
        .getConnection(t)
        .requestUserInputAutoResolution.recordConversationActivity(
          e,
          this.surfaceId,
        );
    }
    setConversationPresented({ conversationId: e, hostId: t, presented: n }) {
      this.appServerConnectionRegistry
        .getConnection(t)
        .requestUserInputAutoResolution.setConversationPresented(
          this.surfaceId,
          e,
          n,
        );
    }
    snooze({ conversationId: e, hostId: t, requestId: n }) {
      this.appServerConnectionRegistry
        .getConnection(t)
        .requestUserInputAutoResolution.snoozeRequest(e, n);
    }
  },
  l4 = r.a(`notifications-service`),
  u4 = class extends a.P {
    constructor(e, t, n) {
      (super(),
        (this.windowManager = e),
        (this.origin = t),
        (this.desktopNotificationManager = n));
    }
    #e(e) {
      let t = this.#n(e);
      t != null && this.#t(t, { stealFocus: process.platform === `win32` });
    }
    #t(e, { stealFocus: t }) {
      (e.isMinimized() && e.restore(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!0),
        e.show(),
        e.focus(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!1));
    }
    #n(e) {
      let t =
        this.windowManager.getPrimaryWindow() ??
        c.BrowserWindow.fromWebContents(e);
      return !t || t.isDestroyed() ? null : t;
    }
    show(e, t) {
      let n = t?.dup();
      (l4().info(`[desktop-notifications] forward show`, {
        safe: { notificationId: e.id, kind: e.kind },
        sensitive: {},
      }),
        this.desktopNotificationManager.showNotification(
          e,
          this.origin,
          (e) => {
            try {
              var t = A_();
              let r = t.u(n?.dup());
              (e.actionType === `open` && this.#e(this.origin),
                l4().info(`[desktop-notifications] emit action`, {
                  safe: {
                    notificationId: e.notificationId,
                    actionType: e.actionType,
                    actionId: e.actionId ?? `none`,
                  },
                  sensitive: {},
                }),
                t.u(
                  r?.({
                    id: e.actionId ?? null,
                    type: e.actionType,
                    reply: e.reply ?? null,
                  }),
                ));
            } catch (e) {
              t.e = e;
            } finally {
              t.d();
            }
          },
          () => n?.[Symbol.dispose](),
        ));
    }
    hide(e) {
      e.notificationId == null
        ? (this.desktopNotificationManager.dismissByConversationId(
            e.conversationId ?? null,
          ),
          e.navigationPath != null &&
            this.desktopNotificationManager.dismissByNavigationPath(
              e.navigationPath,
            ))
        : this.desktopNotificationManager.dismissByNotificationId(
            e.notificationId,
          );
    }
  },
  d4 = r.a(`mac-notification-permission`),
  f4 = 7,
  p4 = `/System/Library/Frameworks/UserNotifications.framework/UserNotifications`,
  m4,
  h4 = 0,
  g4;
async function _4() {
  if (process.platform === `darwin`)
    try {
      d4().info(`macOS notification permission request started`, {
        safe: {},
        sensitive: {},
      });
      let e = await v4();
      d4().info(`macOS notification permission request completed`, {
        safe: { granted: e },
        sensitive: {},
      });
    } catch (e) {
      d4().warning(`Failed to request macOS notification authorization`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
async function v4() {
  let { RunLoop: e, typedBlock: t } = await import(`objc-js`),
    n = await b4();
  return new Promise((r, i) => {
    let a = y4(e);
    try {
      n.requestAuthorizationWithOptions$completionHandler$(
        f4,
        t({ returns: `v`, args: [`B`, `@`] }, (e, t) => {
          if ((a(), t != null)) {
            (d4().warning(
              `macOS notification permission request returned an error`,
              {
                safe: { code: t.code(), domain: t.domain().toString() },
                sensitive: {
                  localizedDescription: t.localizedDescription().toString(),
                },
              },
            ),
              i(
                Error(`Failed to request macOS notification authorization`, {
                  cause: t,
                }),
              ));
            return;
          }
          r(e);
        }),
      );
    } catch (e) {
      (a(),
        i(
          Error(`Failed to request macOS notification authorization`, {
            cause: e,
          }),
        ));
    }
  });
}
function y4(e) {
  return (
    h4 === 0 && (g4 = e.run()),
    (h4 += 1),
    () => {
      (--h4, h4 === 0 && (g4?.(), (g4 = void 0)));
    }
  );
}
async function b4() {
  if (m4 == null) {
    let { NobjcLibrary: e } = await import(`objc-js`);
    m4 = new e(p4).UNUserNotificationCenter.currentNotificationCenter();
  }
  return m4;
}
var x4 = r.a(`system-permissions-service`),
  S4 = `x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture`,
  C4 = `x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility`,
  w4 = `x-apple.systempreferences:com.apple.Notifications-Settings.extension`,
  T4 = 2e3,
  E4 = `ms-settings:notifications`,
  D4 = class extends a.P {
    notificationAuthorizationUnavailable;
    constructor(e, t) {
      (super(),
        (this.notificationSettingsAppBundleIdentifier = t),
        (this.notificationAuthorizationUnavailable =
          process.platform === `darwin` &&
          e === i.a.Prod &&
          !c.app.isPackaged));
    }
    async getNotificationPermissionStatus() {
      return this.notificationAuthorizationUnavailable ? null : aP();
    }
    async requestMicrophoneAccess() {
      if (process.platform === `darwin`)
        try {
          await c.systemPreferences.askForMediaAccess(`microphone`);
        } catch (e) {
          x4().error(`Microphone permission request failed`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    async openNotificationSettings() {
      try {
        if (process.platform === `win32`) {
          await c.shell.openExternal(E4);
          return;
        }
        if (this.notificationAuthorizationUnavailable) return;
        let e = await this.getNotificationPermissionStatus();
        ((e == null || e === `not-determined`) &&
          (_4(),
          await new Promise((e) => {
            setTimeout(e, T4);
          })),
          await c.shell.openExternal(
            `${w4}?id=${encodeURIComponent(this.notificationSettingsAppBundleIdentifier)}`,
          ));
      } catch (e) {
        x4().error(`Open notification settings failed`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async openScreenRecordingSettings() {
      if (process.platform === `darwin`)
        try {
          await c.shell.openExternal(S4);
        } catch (e) {
          x4().error(`Open screen recording settings failed`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    async openAccessibilitySettings() {
      if (process.platform === `darwin`)
        try {
          await c.shell.openExternal(C4);
        } catch (e) {
          x4().error(`Open accessibility settings failed`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    async showPermissionSettingsAppInFinder() {
      if (process.platform === `darwin`)
        try {
          c.shell.showItemInFolder(a.s());
        } catch (e) {
          x4().error(`Show permission settings app in Finder failed`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
  },
  O4 = (0, T.createRequire)(__filename),
  k4 = class e {
    static create(t) {
      let { spawn: n } = O4(`node-pty`),
        [r, ...i] = t.command,
        a = n(r, i, { cols: t.cols, rows: t.rows, cwd: t.cwd, env: t.env });
      return (
        a.onData((e) => {
          t.callbacks.onData(e);
        }),
        a.onExit(({ exitCode: e, signal: n }) => {
          t.callbacks.onExit({ code: e, signal: A4(n) });
        }),
        new e(a)
      );
    }
    constructor(e) {
      this.pty = e;
    }
    async write(e) {
      this.pty.write(e);
    }
    async resize(e, t) {
      this.pty.resize(e, t);
    }
    async terminate() {
      this.pty.kill();
    }
    async dispose() {
      this.pty.kill();
    }
  };
function A4(e) {
  return e == null ? null : String(e);
}
var j4 = class {
  decoder = new y.StringDecoder(`utf8`);
  session = null;
  settled = !1;
  disposed = !1;
  pendingInput = ``;
  scheduledWrite = null;
  constructor(e, t) {
    ((this.sessionPromise = e), (this.callbacks = t), this.run());
  }
  async run() {
    let e;
    try {
      e = await this.sessionPromise;
    } catch {
      if (((this.settled = !0), this.disposed)) return;
      this.callbacks.onExit({ code: null, signal: null });
      return;
    }
    ((this.session = e),
      e.response.then(
        (e) => {
          this.settled = !0;
          let t = this.flushDecoder();
          (!this.disposed && t.length > 0 && this.callbacks.onData(t),
            !this.disposed &&
              this.callbacks.onExit({ code: e.exitCode, signal: null }));
        },
        () => {
          this.settled = !0;
          let e = this.flushDecoder();
          (!this.disposed && e.length > 0 && this.callbacks.onData(e),
            !this.disposed &&
              this.callbacks.onExit({ code: null, signal: null }));
        },
      ));
  }
  handleOutputDelta(e) {
    if (this.disposed || this.settled) return;
    let t = this.decoder.write(e.chunk);
    t.length > 0 && this.callbacks.onData(t);
  }
  write(e) {
    if (this.disposed || this.settled) return Promise.resolve();
    if (((this.pendingInput += e), this.scheduledWrite != null))
      return this.scheduledWrite;
    let t = Promise.resolve().then(async () => {
      let e = this.pendingInput;
      ((this.pendingInput = ``),
        (this.scheduledWrite = null),
        await this.writeInput(e));
    });
    return ((this.scheduledWrite = t), t);
  }
  async writeInput(e) {
    let t = await this.getActiveSession();
    t == null ||
      this.disposed ||
      this.settled ||
      (await t.write(Buffer.from(e, `utf8`)));
  }
  async resize(e, t) {
    if (this.disposed || this.settled) return;
    let n = await this.getActiveSession();
    n == null ||
      this.disposed ||
      this.settled ||
      (await n.resize({ cols: e, rows: t }));
  }
  async terminate() {
    if (this.disposed || this.settled) return;
    let e = await this.getActiveSession();
    e == null || this.disposed || this.settled || (await e.terminate());
  }
  async dispose() {
    if (
      this.disposed ||
      ((this.disposed = !0),
      (this.pendingInput = ``),
      this.flushDecoder(),
      this.settled)
    )
      return;
    let e = await this.getActiveSession();
    if (!(e == null || this.settled))
      try {
        await e.terminate();
      } catch {}
  }
  flushDecoder() {
    return this.decoder.end();
  }
  async getActiveSession() {
    if (this.session != null) return this.session;
    try {
      return ((this.session = await this.sessionPromise), this.session);
    } catch {
      return null;
    }
  }
};
function M4(e) {
  let t = ``;
  for (let n = 0; n < e.length; ) {
    if (e[n] !== `\x1B`) {
      ((t += e[n]), (n += 1));
      continue;
    }
    let r = e[n + 1];
    if (r === `[`) {
      let r = n + 2;
      for (; r < e.length; ) {
        let t = e[r];
        if (t >= `@` && t <= `~`) break;
        r += 1;
      }
      if (r >= e.length) {
        t += e.slice(n);
        break;
      }
      if (e[r] === `n`) {
        n = r + 1;
        continue;
      }
      ((t += e.slice(n, r + 1)), (n = r + 1));
      continue;
    }
    if (r === `]`) {
      let r = n + 2,
        i = 0,
        a = -1;
      for (; r < e.length; ) {
        if (e[r] === `\x07`) {
          ((a = r), (i = 1));
          break;
        }
        if (e.startsWith(`\x1B\\`, r)) {
          ((a = r), (i = 2));
          break;
        }
        r += 1;
      }
      if (a === -1) {
        t += e.slice(n);
        break;
      }
      let o = e.slice(n + 2, a);
      if (/^\d+;\?$/.test(o)) {
        n = a + i;
        continue;
      }
      ((t += e.slice(n, a + i)), (n = a + i));
      continue;
    }
    ((t += e[n]), (n += 1));
  }
  return t;
}
var N4 = 16e3,
  P4 = `xterm-256color`,
  F4 = {
    write: () => Promise.resolve(),
    resize: () => Promise.resolve(),
    terminate: () => Promise.resolve(),
    dispose: () => Promise.resolve(),
  };
function I4(e, t) {
  return `${e}:${t}`;
}
var L4 = class {
  sessions = new Map();
  sessionsByWindowConversation = new Map();
  actionRestartPromises = new Map();
  trackedOrigins = new Set();
  terminalCommand;
  constructor(e, t, n, r, i, a, o) {
    ((this.windowManager = e),
      (this.globalState = t),
      (this.getIntegratedTerminalShellPreference = n),
      (this.getWorktreeShellEnvironmentForCwd = i),
      (this.getHostConfigForHostId = a),
      (this.getProcessConnectionForHostId = o),
      (this.terminalCommand = r ?? null));
  }
  track(e) {
    this.trackedOrigins.has(e.id) ||
      (this.trackedOrigins.add(e.id),
      e.once(`destroyed`, () => {
        (this.trackedOrigins.delete(e.id), this.cleanupForOrigin(e.id));
      }));
  }
  async createOrAttach(e, t) {
    let n = this.getExistingSessionId({
      ownerId: e.id,
      allowConversationSessionFallback: t.type === `terminal-attach`,
      ...t,
    });
    return n
      ? this.attach(e, n, {
          nextSessionId: t.sessionId,
          conversationId: t.conversationId,
          conversationTitle: t.conversationTitle,
          hostId: t.hostId,
          cwd: t.cwd,
          forceCwdSync: t.forceCwdSync,
          cols: t.cols,
          rows: t.rows,
          preserveOnOwnerDestroy: t.preserveOnOwnerDestroy,
        })
      : this.create(e, t);
  }
  write(e, t, n) {
    let r = this.sessions.get(t);
    if (!r) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (r.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    this.performSessionAction({
      origin: e,
      session: r,
      sessionId: t,
      action: async () => {
        await r.backend.write(n);
      },
    });
  }
  runAction(e, t, n) {
    let r = this.sessions.get(t);
    if (!r) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (r.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    let i = (this.actionRestartPromises.get(t) ?? Promise.resolve())
      .catch(() => void 0)
      .then(async () => {
        this.sessions.get(t) === r &&
          (await this.performSessionAction({
            origin: e,
            session: r,
            sessionId: t,
            action: () => this.restartSessionForAction(r, t, n),
          }));
      });
    (this.actionRestartPromises.set(t, i),
      i.finally(() => {
        this.actionRestartPromises.get(t) === i &&
          this.actionRestartPromises.delete(t);
      }));
  }
  resize(e, t, n, r) {
    let i = this.sessions.get(t);
    if (!i) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (i.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    this.resizeSession(e, i, t, n, r);
  }
  close(e, t) {
    let n = this.sessions.get(t);
    if (n) {
      if (n.ownerId !== e.id) {
        this.sendError(e, t, `Session owned by another window`);
        return;
      }
      this.destroySession(n, { code: null, signal: null });
    }
  }
  appendOutput(e, t) {
    let n = this.sessions.get(e);
    n &&
      ((n.buffer = `${n.buffer}${t}`.slice(-N4)),
      n.attached &&
        this.send(n.owner, {
          type: `terminal-data`,
          sessionId: n.id,
          data: t,
        }));
  }
  closeSession(e) {
    let t = this.sessions.get(e);
    t && this.destroySession(t, { code: null, signal: null });
  }
  getSnapshotForConversationId(e, t) {
    let n = this.sessionsByWindowConversation.get(I4(t, e));
    if (!n) return null;
    let r = this.sessions.get(n);
    return r
      ? {
          cwd: r.cwd,
          shell: r.shell,
          buffer: r.buffer,
          truncated: r.buffer.length >= N4,
        }
      : null;
  }
  async create(e, t) {
    let r = t.sessionId ?? (0, f.randomUUID)(),
      i = this.resolveRequestedCwd(t.cwd, t.hostId);
    try {
      let n = await this.createTerminalBackend({
          hostId: t.hostId,
          conversationTitle: t.conversationTitle,
          requestedCwd: i,
          cols: t.cols ?? 80,
          rows: t.rows ?? 24,
        }),
        a = {
          id: r,
          backend: n.backend,
          owner: e,
          ownerId: e.id,
          buffer: ``,
          cwd: i,
          shell: n.shell,
          shellKind: n.shellKind,
          size: { cols: t.cols ?? 80, rows: t.rows ?? 24 },
          attached: !1,
          conversationId: t.conversationId,
          hostId: t.hostId,
          conversationTitle: t.conversationTitle,
          preserveOnOwnerDestroy: t.preserveOnOwnerDestroy ?? !1,
        };
      (this.sessions.set(r, a),
        t.conversationId &&
          this.sessionsByWindowConversation.set(
            I4(e.id, String(t.conversationId)),
            r,
          ),
        n.pendingState.buffer.length > 0 &&
          (a.buffer = n.pendingState.buffer.slice(-N4)));
      let o = n.initialShellCwd;
      return o != null &&
        !(await this.performSessionAction({
          origin: e,
          session: a,
          sessionId: r,
          action: async () => {
            await this.seedShellCwd(a, o);
          },
        }))
        ? null
        : (this.flushInit(a),
          this.sendAttached(a),
          n.pendingState.exit != null &&
            this.destroySession(a, n.pendingState.exit),
          r);
    } catch (t) {
      return (this.sendError(e, r, n.Rs(t)), null);
    }
  }
  async attach(e, t, r) {
    let i = this.sessions.get(t);
    if (!i) return (this.sendError(e, t, `Session missing`), null);
    if (i.owner.isDestroyed() || i.ownerId === e.id)
      ((i.owner = e), (i.ownerId = e.id));
    else if (i.ownerId !== e.id)
      return (this.sendError(e, t, `Session owned by another window`), null);
    (r.conversationId &&
      ((i.conversationId = r.conversationId),
      this.sessionsByWindowConversation.set(
        I4(e.id, String(r.conversationId)),
        i.id,
      )),
      r.hostId !== void 0 && (i.hostId = r.hostId),
      r.conversationTitle !== void 0 &&
        (i.conversationTitle = r.conversationTitle));
    let { cols: a, rows: o } = r;
    if (
      a != null &&
      o != null &&
      ((i.size = { cols: a, rows: o }),
      !(await this.performSessionAction({
        origin: e,
        session: i,
        sessionId: t,
        action: async () => {
          await i.backend.resize(a, o);
        },
      })))
    )
      return null;
    if (r.forceCwdSync && r.cwd) {
      let a = this.resolveRequestedCwd(r.cwd, r.hostId),
        o =
          r.hostId == null
            ? null
            : (this.getHostConfigForHostId?.(r.hostId) ?? null);
      if (
        ((o != null && n.eo(o)) ||
          i.shellKind !== `posix` ||
          this.resolveLocalCwd(a) !== process.cwd()) &&
        ((i.cwd = a),
        !(await this.performSessionAction({
          origin: e,
          session: i,
          sessionId: t,
          action: async () => {
            await this.seedShellCwd(i, a);
          },
        })))
      )
        return null;
    }
    return (
      r.nextSessionId &&
        r.nextSessionId !== i.id &&
        this.rekeySession(i, r.nextSessionId),
      r.preserveOnOwnerDestroy != null &&
        (i.preserveOnOwnerDestroy = r.preserveOnOwnerDestroy),
      this.flushInit(i),
      this.sendAttached(i),
      i.id
    );
  }
  destroySession(e, t) {
    if (this.sessions.get(e.id) !== e) return;
    (this.sessions.delete(e.id),
      this.actionRestartPromises.delete(e.id),
      e.conversationId &&
        this.deleteConversationMapping(
          e.ownerId,
          String(e.conversationId),
          e.id,
        ),
      e.backend.dispose().catch((t) => {
        e.owner.isDestroyed() || this.sendError(e.owner, e.id, n.Rs(t));
      }));
    let r = {
      type: `terminal-exit`,
      sessionId: e.id,
      code: t?.code ?? null,
      signal: t?.signal ?? null,
    };
    e.owner.isDestroyed() || this.send(e.owner, r);
  }
  async restartSessionForAction(e, t, n) {
    let r = e.backend;
    if (
      ((e.backend = F4),
      (e.attached = !1),
      await r.dispose(),
      this.sessions.get(t) !== e)
    )
      return;
    let i = await this.createTerminalBackend({
      hostId: e.hostId,
      conversationTitle: e.conversationTitle,
      requestedCwd: n.cwd,
      cols: e.size.cols,
      rows: e.size.rows,
    });
    if (this.sessions.get(t) !== e) {
      await i.backend.dispose();
      return;
    }
    if (
      ((e.backend = i.backend),
      (e.cwd = n.cwd),
      (e.shell = i.shell),
      (e.shellKind = i.shellKind),
      (e.buffer = i.pendingState.buffer.slice(-N4)),
      this.flushInit(e, !0),
      this.sendAttached(e),
      i.pendingState.exit != null)
    ) {
      this.destroySession(e, i.pendingState.exit);
      return;
    }
    await e.backend.write(
      z4(e.shellKind, this.resolveShellCwd(e.shellKind, n.cwd), n.command),
    );
  }
  async performSessionAction(e) {
    try {
      return (await e.action(), !0);
    } catch (t) {
      return (
        this.sendError(e.origin, e.sessionId, n.Rs(t)),
        this.destroySession(e.session, { code: null, signal: null }),
        !1
      );
    }
  }
  resizeSession(e, t, n, r, i) {
    return t.size.cols === r && t.size.rows === i
      ? Promise.resolve(!0)
      : ((t.size = { cols: r, rows: i }),
        this.performSessionAction({
          origin: e,
          session: t,
          sessionId: n,
          action: async () => {
            await t.backend.resize(r, i);
          },
        }));
  }
  flushInit(e, t = !1) {
    if (t || e.buffer.length > 0) {
      let t = e.buffer.length > 0 ? M4(e.buffer) : ``;
      this.send(e.owner, {
        type: `terminal-init-log`,
        sessionId: e.id,
        log: t,
      });
    }
    e.attached = !0;
  }
  sendAttached(e) {
    this.send(e.owner, {
      type: `terminal-attached`,
      sessionId: e.id,
      cwd: e.cwd,
      shell: e.shell,
    });
  }
  send(e, t) {
    this.windowManager.sendMessageToWebContents(e, t);
  }
  sendError(e, t, n) {
    this.windowManager.sendMessageToWebContents(e, {
      type: `terminal-error`,
      sessionId: t,
      message: n,
    });
  }
  resolveRequestedCwd(e, t) {
    if (e) return e;
    let n = rt(this.globalState);
    if (n[0]) return n[0];
    if (t != null) {
      let e = this.getHostConfigForHostId?.(t) ?? null;
      if (e?.home_dir != null && e.home_dir.length > 0) return e.home_dir;
    }
    return process.cwd();
  }
  resolveLocalCwd(e) {
    let t = ne(e);
    return (0, p.existsSync)(t) ? t : (0, p.existsSync)(e) ? e : process.cwd();
  }
  resolveShellCwd(e, t) {
    if (e === `wsl`) return z(t, !0);
    if (e === `posix`) return t;
    let n = ne(t);
    return (0, p.existsSync)(n) ? n : t;
  }
  async seedShellCwd(e, t) {
    await e.backend.write(`cd ${n.Qn(t)}\n`);
  }
  getExistingSessionId(e) {
    if (e.sessionId && this.sessions.has(e.sessionId)) return e.sessionId;
    if (e.allowConversationSessionFallback && e.conversationId) {
      let t = this.sessionsByWindowConversation.get(
        I4(e.ownerId, String(e.conversationId)),
      );
      if (t) return t;
    }
  }
  cleanupForOrigin(e) {
    for (let t of Array.from(this.sessions.values()))
      if (t.ownerId === e) {
        if (t.preserveOnOwnerDestroy) {
          t.attached = !1;
          continue;
        }
        this.destroySession(t, { code: null, signal: null });
      }
  }
  rekeySession(e, t) {
    this.sessions.get(e.id) === e &&
      (this.sessions.delete(e.id),
      (e.id = t),
      this.sessions.set(t, e),
      e.conversationId &&
        this.sessionsByWindowConversation.set(
          I4(e.ownerId, String(e.conversationId)),
          t,
        ));
  }
  deleteConversationMapping(e, t, n) {
    let r = I4(e, t);
    this.sessionsByWindowConversation.get(r) === n &&
      this.sessionsByWindowConversation.delete(r);
  }
  resolveTerminalCommand(e) {
    let t = null;
    return (
      e != null &&
        (t = this.getHostConfigForHostId?.(e)?.terminal_command ?? null),
      t && t.length > 0
        ? t
        : this.terminalCommand && this.terminalCommand.length > 0
          ? this.terminalCommand
          : jP(this.getIntegratedTerminalShellPreference() ?? null)
    );
  }
  async buildTerminalEnv(e, t, n) {
    let i = { ...process.env };
    if (
      (t != null && (i.CODEX_APP_TITLE = t), this.isLocalTerminalSession(n))
    ) {
      let t = await this.getWorktreeShellEnvironmentForCwd(e);
      if (t != null) {
        for (let e of t.exclude) delete i[e];
        Object.assign(i, t.set);
      }
    }
    return (
      process.platform !== `win32` &&
        ((i.TERM = P4), delete i.TERMINFO, delete i.TERMINFO_DIRS),
      r.t(i)
    );
  }
  isLocalTerminalSession(e) {
    return e == null ? !0 : this.getHostConfigForHostId?.(e)?.kind === `local`;
  }
  buildRemoteProcessEnv() {
    return { TERM: P4, TERMINFO: null, TERMINFO_DIRS: null };
  }
  async createTerminalBackend(e) {
    let { hostId: t } = e,
      r = t == null ? null : (this.getHostConfigForHostId?.(t) ?? null),
      i = null,
      a = { buffer: ``, exit: null },
      o = () => (i == null ? null : this.getSessionForBackend(i)),
      s = {
        onData: (e) => {
          let t = o();
          if (t == null) {
            a.buffer = `${a.buffer}${e}`.slice(-N4);
            return;
          }
          this.appendOutput(t.id, e);
        },
        onExit: (e) => {
          let t = o();
          if (t == null) {
            a.exit = e;
            return;
          }
          this.destroySession(t, e);
        },
      },
      c = await (t != null && r && n.eo(r)
        ? this.createRemoteTerminalBackend({
            hostId: t,
            requestedCwd: e.requestedCwd,
            cols: e.cols,
            rows: e.rows,
            callbacks: s,
          })
        : this.createLocalTerminalBackend({
            hostId: e.hostId,
            conversationTitle: e.conversationTitle,
            requestedCwd: e.requestedCwd,
            cols: e.cols,
            rows: e.rows,
            callbacks: s,
          }));
    return ((i = c.backend), { ...c, pendingState: a });
  }
  getSessionForBackend(e) {
    for (let t of this.sessions.values()) if (t.backend === e) return t;
    return null;
  }
  async createLocalTerminalBackend(e) {
    let t = this.resolveLocalCwd(e.requestedCwd),
      n = this.resolveTerminalCommand(e.hostId);
    return {
      backend: k4.create({
        command: n,
        cwd: t,
        env: await this.buildTerminalEnv(t, e.conversationTitle, e.hostId),
        cols: e.cols,
        rows: e.rows,
        callbacks: e.callbacks,
      }),
      shell: MP(n),
      shellKind: R4(n),
      initialShellCwd:
        e.requestedCwd !== t && t === process.cwd() ? e.requestedCwd : void 0,
      pendingState: { buffer: ``, exit: null },
    };
  }
  createRemoteTerminalBackend(e) {
    let t = this.getProcessConnectionForHostId?.(e.hostId) ?? null,
      n = py(),
      r = null;
    return (
      (r = new j4(
        t?.startProcessSession({
          command: n,
          tty: !0,
          size: { cols: e.cols, rows: e.rows },
          streamStdoutStderr: !0,
          outputBytesCap: null,
          timeoutMs: null,
          cwd: e.requestedCwd,
          env: this.buildRemoteProcessEnv(),
          onStdoutDelta: (e) => {
            r?.handleOutputDelta(e);
          },
          onStderrDelta: (e) => {
            r?.handleOutputDelta(e);
          },
        }) ?? Promise.reject(Error(`Remote process connection is unavailable`)),
        e.callbacks,
      )),
      {
        backend: r,
        shell: MP(n),
        shellKind: `posix`,
        pendingState: { buffer: ``, exit: null },
      }
    );
  }
};
function R4(e) {
  if (process.platform !== `win32`) return `posix`;
  let t = G4(e);
  return t === `wsl` || t === `wsl.exe`
    ? `wsl`
    : t === `pwsh` ||
        t === `pwsh.exe` ||
        t === `powershell` ||
        t === `powershell.exe`
      ? `powershell`
      : t === `cmd` || t === `cmd.exe`
        ? `commandPrompt`
        : `posix`;
}
function z4(e, t, n) {
  if (e === `powershell`) {
    let r = n.replace(
      /\r\n|\r|\n/g,
      `\r
`,
    );
    return `${H4(e, t)}; ${r}\r
`;
  }
  if (e === `commandPrompt`) {
    let r = n.replace(
      /\r\n|\r|\n/g,
      `\r
`,
    );
    return `${H4(e, t)} && ${r}\r
`;
  }
  return e === `wsl`
    ? V4(
        t,
        n,
        `
`,
      )
    : B4(t, n);
}
function B4(e, t) {
  return V4(
    e,
    t,
    process.platform === `win32`
      ? `\r
`
      : `
`,
  );
}
function V4(e, t, n) {
  let r = t.replace(/\r\n|\r|\n/g, n);
  return `cd ${JSON.stringify(e)} && ${r}${n}`;
}
function H4(e, t) {
  return e === `powershell`
    ? `Set-Location -LiteralPath ${U4(t)}`
    : e === `commandPrompt`
      ? `cd /d ${W4(t)}`
      : `cd ${JSON.stringify(t)}`;
}
function U4(e) {
  return `'${e.replace(/'/g, `''`)}'`;
}
function W4(e) {
  return `"${e.replace(/"/g, `""`)}"`;
}
function G4(e) {
  let t = (e[0] ?? ``).split(/[/\\]/);
  return t[t.length - 1]?.toLowerCase() ?? ``;
}
var K4 = class extends a.P {
    logger = r.i(`ThreadArchiveService`);
    constructor(e, t, n) {
      (super(),
        (this.executionHostRegistry = e),
        (this.appServerConnectionRegistry = t),
        (this.localThreadCatalogSyncCoordinator = n));
    }
    async archiveInactiveThread({
      hostId: e,
      threadId: t,
      removeCatalogEntryIfMissing: r,
    }) {
      let i = this.executionHostRegistry.get(e);
      if (n.eo(i.hostConfig))
        throw Error(`Archiving inactive tasks is only supported locally`);
      let a = await n.kr({ codexHome: await i.codexHome(), threadId: t }),
        o = a === !0 || (a == null && r === !0);
      if (o) {
        try {
          this.localThreadCatalogSyncCoordinator?.handleNotification({
            method: `thread/archived`,
            params: { threadId: t },
          });
        } catch (e) {
          if (
            (this.logger.warning(
              `Failed to remove archived thread from catalog`,
              { safe: {}, sensitive: { error: e, threadId: t } },
            ),
            a == null)
          )
            return { success: !1 };
        }
        this.appServerConnectionRegistry
          .getConnection(e)
          .handleArchivedThreadSideEffects(t);
      }
      return { success: o };
    }
  },
  q4 = 100,
  J4 = 3e4,
  Y4 = 1e4,
  X4 = 250,
  Z4 = class {
    listeners = new Set();
    inFlight = null;
    inFlightGeneration = null;
    inFlightMode = null;
    lastSuccessAt = null;
    lastFailureAt = null;
    lifecycleGeneration = 0;
    foreground = !0;
    syncEnabled = !1;
    syncRequested = !1;
    disposed = !1;
    pendingColdFullSweep;
    notificationSyncTimer = null;
    coldFullSweepRetryTimer = null;
    notificationSyncGeneration = 0;
    reconciledNotificationSyncGeneration = 0;
    pendingThreadRefreshes = new Map();
    entriesById = new Map();
    syncState;
    materializedSnapshot;
    constructor(e, t, n = {}) {
      ((this.client = e), (this.store = t), (this.options = n));
      let r = t.readSnapshot();
      for (let e of r.entries) this.entriesById.set(e.threadId, e);
      ((this.syncState = r.syncState),
        (this.materializedSnapshot = r),
        (this.pendingColdFullSweep =
          !r.syncState.isComplete || !(n.skipColdFullSweep ?? !1)));
    }
    readSnapshot() {
      return (
        (this.materializedSnapshot ??= {
          entries: Array.from(this.entriesById.values()).sort(e3),
          syncState: this.syncState,
        }),
        this.materializedSnapshot
      );
    }
    subscribe(e, t = !0) {
      return (
        this.listeners.add(e),
        t && e({ type: `snapshot`, snapshot: this.readSnapshot() }),
        {
          dispose: () => {
            this.listeners.delete(e);
          },
        }
      );
    }
    requestSync() {
      return (
        (this.syncEnabled = !0),
        (this.syncRequested = !0),
        this.requestRun(null, !1)
      );
    }
    requestStartupSync() {
      return (
        (this.syncEnabled = !0),
        (this.syncRequested = !0),
        this.requestRun(
          this.pendingColdFullSweep ? `full` : null,
          this.pendingColdFullSweep,
        ).then((e) => (e === `backoff` && this.scheduleColdFullSweepRetry(), e))
      );
    }
    setSyncEnabled(e) {
      this.disposed ||
        (e && this.syncEnabled) ||
        ((this.syncEnabled = e),
        !e &&
          ((this.syncRequested = !1),
          (this.lifecycleGeneration += 1),
          this.clearColdFullSweepRetry(),
          this.pendingThreadRefreshes.clear(),
          this.notificationSyncTimer != null &&
            (clearTimeout(this.notificationSyncTimer),
            (this.notificationSyncTimer = null))));
    }
    handleNotification(e) {
      let t = n.tr(e.params);
      switch (e.method) {
        case `thread/started`: {
          let t = n.er(e.params);
          if (t == null) return;
          (this.pendingThreadRefreshes.delete(t.id), this.observeThread(t));
          return;
        }
        case `thread/name/updated`: {
          if (t == null) return;
          this.pendingThreadRefreshes.delete(t);
          let n = e.params?.threadName;
          if (typeof n == `string`) {
            let e = this.store.applyObservedTitle(t, n);
            if (e.changedThreadIds.length > 0 || this.entriesById.has(t)) {
              (this.publishMutation(e, []), this.scheduleNotificationSync());
              return;
            }
          }
          this.scheduleNotificationSync();
          return;
        }
        case `thread/archived`:
        case `thread/deleted`:
          if (t == null) return;
          (this.pendingThreadRefreshes.delete(t),
            this.publishMutation(this.store.applyAuthoritativeRemoval(t), []));
          return;
        case `thread/unarchived`: {
          if (t == null) return;
          if (!this.syncEnabled) {
            this.scheduleNotificationSync();
            return;
          }
          let e = Symbol();
          (this.pendingThreadRefreshes.set(t, e), this.refreshThread(t, e));
          return;
        }
        case `turn/completed`:
          this.scheduleNotificationSync();
          return;
        default:
          return;
      }
    }
    requestRun(e, t) {
      if (this.disposed || !this.foreground) return Promise.resolve(`stopped`);
      if (this.inFlight != null)
        return this.inFlightGeneration !== this.lifecycleGeneration ||
          (e === `full` && this.inFlightMode !== `full`)
          ? this.inFlight.then(() => this.requestRun(e, t))
          : this.inFlight;
      let n = this.now();
      if (
        !t &&
        this.lastSuccessAt != null &&
        n - this.lastSuccessAt < (this.options.successFreshnessMs ?? J4)
      )
        return Promise.resolve(`fresh`);
      if (
        this.lastFailureAt != null &&
        n - this.lastFailureAt < (this.options.failureBackoffMs ?? Y4)
      )
        return Promise.resolve(`backoff`);
      let r = this.lifecycleGeneration;
      this.inFlightGeneration = r;
      let i =
        e ?? (this.store.readSyncState().isComplete ? `incremental` : `full`);
      this.inFlightMode = i;
      let a = () =>
          this.isCurrent(r) ? this.runSync(r, i) : Promise.resolve(`aborted`),
        o = (this.options.scheduleRun?.(i, a) ?? a())
          .then(
            (e) => (
              i === `full` &&
                e === `failed` &&
                this.scheduleColdFullSweepRetry(),
              e
            ),
          )
          .finally(() => {
            this.inFlight === o &&
              ((this.inFlight = null),
              (this.inFlightGeneration = null),
              (this.inFlightMode = null));
          });
      return ((this.inFlight = o), o);
    }
    setForeground(e) {
      this.disposed ||
        this.foreground === e ||
        ((this.foreground = e),
        (this.lifecycleGeneration += 1),
        e ||
          (this.clearColdFullSweepRetry(),
          this.notificationSyncTimer != null &&
            (clearTimeout(this.notificationSyncTimer),
            (this.notificationSyncTimer = null))),
        e &&
          this.syncEnabled &&
          (this.pendingColdFullSweep && this.syncRequested
            ? this.requestStartupSync()
            : !this.pendingColdFullSweep && this.hasPendingNotificationSync()
              ? this.requestNotificationSync()
              : this.pendingColdFullSweep || this.requestSync()));
    }
    dispose() {
      this.disposed ||
        ((this.disposed = !0),
        (this.foreground = !1),
        (this.lifecycleGeneration += 1),
        this.clearColdFullSweepRetry(),
        this.pendingThreadRefreshes.clear(),
        this.notificationSyncTimer != null &&
          (clearTimeout(this.notificationSyncTimer),
          (this.notificationSyncTimer = null)),
        this.listeners.clear());
    }
    async runSync(e, t) {
      let r = this.store.readSyncState(),
        i = this.store.beginScan(t),
        a = r.sourceUpdatedAtWatermark,
        o = new Set(),
        s = null,
        c = null,
        l = new Map(),
        u = [],
        d = [],
        f = `scan`,
        p = !0,
        m = !1,
        h = !1,
        g = t === `full` ? this.notificationSyncGeneration : null;
      try {
        for (;;) {
          if (!this.isCurrent(e))
            return (this.store.abortScan(i), (p = !1), `aborted`);
          let r = await this.client.listThreads({
            archived: !1,
            cursor: s,
            limit: q4,
            parentThreadId: null,
            sortKey: `updated_at`,
            sortDirection: `desc`,
            sourceKinds: n.bi,
            ...((this.options.useStateDbOnly ?? !0)
              ? { useStateDbOnly: !0 }
              : {}),
          });
          if (!this.isCurrent(e))
            return (this.store.abortScan(i), (p = !1), `aborted`);
          let _ = r.data.flatMap((e) => {
            let t = n.a(e, this.store.hostId);
            return t == null ? [] : [t];
          });
          c ??= Q4(r.data);
          let v = this.store.applyScanPage(i, _);
          t === `full`
            ? v.changedThreadIds.length > 0 &&
              (this.publish(v, _, !m), (h ||= m), (m = !0))
            : this.publishMutation(v, _);
          let y =
              t === `incremental` &&
              a != null &&
              r.data.some(
                (e) => Number.isFinite(e.updatedAt) && e.updatedAt < a,
              ),
            b = f === `scan` && (r.nextCursor == null || y);
          if (
            (f === `scan` &&
              u.length === 0 &&
              r.data.length > 0 &&
              (u = r.data),
            f === `scan` && r.data.length > 0 && (d = r.data),
            b &&
              ((l = $4(
                t === `full` ? u : r.data.length > 0 ? r.data : d,
                t === `incremental` && y ? a : null,
              )),
              l.size > 0))
          ) {
            ((f = `reconcile-head`),
              (s = null),
              (c = null),
              o.clear(),
              (g = this.notificationSyncGeneration),
              await this.yieldBetweenPages());
            continue;
          }
          let x =
            f === `reconcile-head` &&
            r.data.some((e) => l.get(e.id) === e.updatedAt);
          if (
            (f === `scan` && b) ||
            (f === `reconcile-head` && (r.nextCursor == null || x))
          ) {
            let e = this.store.completeScan(
              i,
              c ?? (t === `incremental` ? a : null),
            );
            return (
              (p = !1),
              (this.lastSuccessAt = this.now()),
              (this.lastFailureAt = null),
              g != null &&
                (this.reconciledNotificationSyncGeneration = Math.max(
                  this.reconciledNotificationSyncGeneration,
                  g,
                )),
              t === `full` &&
                ((this.pendingColdFullSweep = !1),
                this.clearColdFullSweepRetry(),
                this.options.onColdFullSweepCompleted?.()),
              this.hasPendingNotificationSync() &&
                this.foreground &&
                this.armNotificationSyncTimer(0),
              t === `full`
                ? (this.publish(e, [], !h), h && this.publishSnapshot())
                : this.publish(e, []),
              `completed`
            );
          }
          let S = r.nextCursor;
          if (S == null)
            throw Error(`Thread list ended before scan completion`);
          if (o.has(S))
            return (
              this.store.abortScan(i),
              (p = !1),
              (this.lastFailureAt = this.now()),
              h && this.publishSnapshot(),
              `failed`
            );
          (o.add(S), (s = S), await this.yieldBetweenPages());
        }
      } catch (t) {
        return (
          p && this.store.abortScan(i),
          this.isCurrent(e)
            ? (this.options.onError?.(t),
              (this.lastFailureAt = this.now()),
              h && this.publishSnapshot(),
              `failed`)
            : `aborted`
        );
      }
    }
    observeThread(e) {
      let t = n.a(e, this.store.hostId);
      if (t == null) {
        this.publishMutation(this.store.applyAuthoritativeRemoval(e.id), []);
        return;
      }
      this.publishMutation(this.store.applyObservedEntry(t), [t]);
    }
    async refreshThread(e, t) {
      try {
        let n = await this.client.readThread(e, { includeTurns: !1 });
        if (this.disposed || this.pendingThreadRefreshes.get(e) !== t) return;
        if ((this.pendingThreadRefreshes.delete(e), n == null)) {
          this.publishMutation(this.store.applyAuthoritativeRemoval(e), []);
          return;
        }
        this.observeThread(n);
      } catch (n) {
        if (this.pendingThreadRefreshes.get(e) !== t) return;
        (this.pendingThreadRefreshes.delete(e),
          this.options.onError?.(n),
          this.scheduleNotificationSync());
      }
    }
    scheduleNotificationSync() {
      this.disposed ||
        ((this.notificationSyncGeneration += 1),
        !(!this.foreground || !this.syncEnabled) &&
          this.armNotificationSyncTimer(
            this.options.notificationDebounceMs ?? X4,
          ));
    }
    armNotificationSyncTimer(e) {
      (this.notificationSyncTimer != null &&
        clearTimeout(this.notificationSyncTimer),
        (this.notificationSyncTimer = setTimeout(() => {
          ((this.notificationSyncTimer = null),
            this.hasPendingNotificationSync() &&
              this.store.readSyncState().isComplete &&
              this.requestNotificationSync());
        }, e)));
    }
    requestNotificationSync(e = this.notificationSyncGeneration) {
      return e <= this.reconciledNotificationSyncGeneration
        ? Promise.resolve(`fresh`)
        : this.disposed || !this.foreground || !this.syncEnabled
          ? Promise.resolve(`stopped`)
          : this.inFlight == null
            ? this.requestRun(
                this.store.readSyncState().isComplete ? `incremental` : null,
                !0,
              ).then(
                (t) => (
                  t === `completed`
                    ? ((this.reconciledNotificationSyncGeneration = Math.max(
                        this.reconciledNotificationSyncGeneration,
                        e,
                      )),
                      this.syncEnabled &&
                        this.foreground &&
                        !this.disposed &&
                        this.hasPendingNotificationSync() &&
                        this.armNotificationSyncTimer(0))
                    : this.syncEnabled &&
                      this.foreground &&
                      !this.disposed &&
                      this.armNotificationSyncTimer(
                        this.options.failureBackoffMs ?? Y4,
                      ),
                  t
                ),
              )
            : this.inFlight.then(() => this.requestNotificationSync(e));
    }
    hasPendingNotificationSync() {
      return (
        this.notificationSyncGeneration >
        this.reconciledNotificationSyncGeneration
      );
    }
    scheduleColdFullSweepRetry() {
      !this.syncEnabled ||
        !this.pendingColdFullSweep ||
        !(this.options.autoRetryColdFullSweep ?? !1) ||
        !this.foreground ||
        this.disposed ||
        this.coldFullSweepRetryTimer != null ||
        (this.coldFullSweepRetryTimer = setTimeout(() => {
          ((this.coldFullSweepRetryTimer = null),
            !(!this.syncEnabled || !this.foreground || this.disposed) &&
              this.requestStartupSync());
        }, this.options.failureBackoffMs ?? Y4));
    }
    clearColdFullSweepRetry() {
      this.coldFullSweepRetryTimer != null &&
        (clearTimeout(this.coldFullSweepRetryTimer),
        (this.coldFullSweepRetryTimer = null));
    }
    publishMutation(e, t) {
      (e.changedThreadIds.length === 0 && e.removedThreadIds.length === 0) ||
        this.publish(e, t);
    }
    publish(e, t, n = !0) {
      let r = this.syncState.isComplete,
        i = new Map(t.map((e) => [e.threadId, e]));
      for (let t of e.changedThreadIds) {
        let e = i.get(t) ?? this.store.readEntry(t);
        e != null && this.entriesById.set(t, e);
      }
      for (let t of e.removedThreadIds) this.entriesById.delete(t);
      if (
        ((this.syncState = this.store.readSyncState()),
        (this.materializedSnapshot = null),
        !n ||
          (e.changedThreadIds.length === 0 &&
            e.removedThreadIds.length === 0 &&
            r === this.syncState.isComplete))
      )
        return;
      let a = {
        type: `delta`,
        entries: e.changedThreadIds.flatMap((e) => {
          let t = this.entriesById.get(e);
          return t == null ? [] : [t];
        }),
        mutation: e,
        syncState: this.syncState,
      };
      for (let e of this.listeners)
        try {
          e(a);
        } catch (e) {
          this.options.onError?.(e);
        }
    }
    publishSnapshot() {
      let e = { type: `snapshot`, snapshot: this.readSnapshot() };
      for (let t of this.listeners)
        try {
          t(e);
        } catch (e) {
          this.options.onError?.(e);
        }
    }
    isCurrent(e) {
      return (
        !this.disposed &&
        this.foreground &&
        this.syncEnabled &&
        e === this.lifecycleGeneration
      );
    }
    now() {
      return this.options.now?.() ?? Date.now();
    }
    yieldBetweenPages() {
      return (
        this.options.yieldBetweenPages?.() ??
        new Promise((e) => setImmediate(e))
      );
    }
  };
function Q4(e) {
  let t = e.map((e) => e.updatedAt).filter((e) => Number.isFinite(e));
  return t.length === 0 ? null : Math.max(...t);
}
function $4(e, t) {
  return new Map(
    e.flatMap((e) =>
      !Number.isFinite(e.updatedAt) || (t != null && e.updatedAt >= t)
        ? []
        : [[e.id, e.updatedAt]],
    ),
  );
}
function e3(e, t) {
  return (
    t.sourceUpdatedAt - e.sourceUpdatedAt ||
    t.sourceCreatedAt - e.sourceCreatedAt ||
    e.threadId.localeCompare(t.threadId)
  );
}
var t3 = class {
  hosts = new Map();
  completedColdSweepHostIds = new Set();
  listeners = new Set();
  headScanWaiters = [];
  foreground = !0;
  syncEnabled = !1;
  startupSyncRequested = !1;
  fullScanQueue = Promise.resolve();
  activeHeadScans = 0;
  entriesByKey = new Map();
  hostsById = new Map();
  aggregateLoaded = !1;
  catalogRevision = 0;
  materializedSnapshot = null;
  disposed = !1;
  constructor(e, t, n) {
    ((this.localHostId = e), (this.createStore = t), (this.onError = n));
  }
  attachHost(e, t, n) {
    if (this.disposed)
      throw Error(`Cannot attach a host to a disposed thread catalog`);
    let r = this.hosts.get(e);
    if (r?.connection === n) return r.coordinator;
    this.detachHost(e);
    let i = new Z4(n, this.createStore(e, t), {
      useStateDbOnly: t === `local`,
      scheduleRun: this.scheduleRun,
      skipColdFullSweep: this.completedColdSweepHostIds.has(e),
      autoRetryColdFullSweep: !0,
      onColdFullSweepCompleted: () => {
        this.completedColdSweepHostIds.add(e);
      },
      onError: (t) => this.onError(e, t),
    });
    (i.setForeground(this.foreground), i.setSyncEnabled(this.syncEnabled));
    let a = {
      connection: n,
      coordinator: i,
      disposeNotificationSubscription: n.registerInternalNotificationHandler(
        (e) => {
          i.handleNotification(e);
        },
      ),
      coordinatorSubscription: null,
    };
    (this.hosts.set(e, a),
      (a.coordinatorSubscription = i.subscribe((t) => {
        this.publishCoordinatorUpdate(e, t);
      }, !1)));
    let o = i.readSnapshot();
    return (
      this.publish({
        type: `delta`,
        delta: {
          revision: o.syncState.catalogRevision,
          changedHosts: [{ hostId: e, isComplete: o.syncState.isComplete }],
          removedHostIds: [],
          changedEntries: [],
          removedEntries: [],
        },
      }),
      this.startupSyncRequested && this.enqueueStartupSync(e, i),
      i
    );
  }
  detachHost(e) {
    let t = this.hosts.get(e);
    t != null &&
      (this.hosts.delete(e),
      t.coordinatorSubscription?.dispose(),
      t.disposeNotificationSubscription(),
      t.coordinator.dispose());
  }
  reconcileAuthoritativeHostKind(e, t) {
    let n = this.createStore(this.localHostId, `local`);
    for (let r of n.listHostIds(e)) {
      if (t.has(r)) continue;
      this.detachHost(r);
      let n = this.createStore(r, e).pruneHost();
      this.publish({
        type: `delta`,
        delta: {
          revision: n.catalogRevision,
          changedHosts: [],
          removedHostIds: [r],
          changedEntries: [],
          removedEntries: n.removedThreadIds.map((e) => ({
            hostId: r,
            threadId: e,
          })),
        },
      });
    }
  }
  readSnapshot() {
    return (
      this.ensureAggregate(),
      (this.materializedSnapshot ??= {
        revision: this.catalogRevision,
        hosts: Array.from(this.hostsById.values()).sort((e, t) =>
          e.hostId.localeCompare(t.hostId),
        ),
        entries: Array.from(this.entriesByKey.values()).sort(r3),
      }),
      this.materializedSnapshot
    );
  }
  ensureAggregate() {
    if (this.aggregateLoaded) return;
    let e = this.loadSnapshot();
    for (let t of e.entries) this.entriesByKey.set(n3(t.hostId, t.threadId), t);
    for (let t of e.hosts) this.hostsById.set(t.hostId, t);
    ((this.aggregateLoaded = !0),
      (this.catalogRevision = e.revision),
      (this.materializedSnapshot = e));
  }
  loadSnapshot() {
    let e = this.createStore(this.localHostId, `local`),
      t = [],
      n = [],
      r = 0;
    for (let i of [`local`, `ssh`, `wsl`, `remote-control`])
      for (let a of e.listHostIds(i)) {
        let e = this.createStore(a, i).readSnapshot();
        (t.push(...e.entries),
          n.push({ hostId: a, isComplete: e.syncState.isComplete }),
          (r = Math.max(r, e.syncState.catalogRevision)));
      }
    return (
      t.sort(r3),
      n.sort((e, t) => e.hostId.localeCompare(t.hostId)),
      { revision: r, hosts: n, entries: t }
    );
  }
  subscribe(e) {
    return (
      this.listeners.add(e),
      this.listeners.size === 1 && this.setSyncEnabled(!0),
      e({ type: `snapshot`, snapshot: this.readSnapshot() }),
      {
        dispose: () => {
          (this.listeners.delete(e),
            this.listeners.size === 0 && this.setSyncEnabled(!1));
        },
      }
    );
  }
  getCoordinator(e) {
    return this.hosts.get(e)?.coordinator ?? null;
  }
  requestStartupSync() {
    if (!this.syncEnabled) return Promise.resolve();
    this.startupSyncRequested = !0;
    let e = Array.from(this.hosts.keys()).sort((e, t) =>
      e === this.localHostId
        ? -1
        : t === this.localHostId
          ? 1
          : e.localeCompare(t),
    );
    return Promise.all(
      e.map((e) => {
        let t = this.hosts.get(e)?.coordinator;
        return t == null ? Promise.resolve() : this.enqueueStartupSync(e, t);
      }),
    ).then(() => void 0);
  }
  async requestSync() {
    this.syncEnabled &&
      (await Promise.all(
        Array.from(this.hosts.values(), ({ coordinator: e }) =>
          e.requestSync(),
        ),
      ));
  }
  setSyncEnabled(e) {
    if (!(this.disposed || this.syncEnabled === e)) {
      ((this.syncEnabled = e), e || (this.startupSyncRequested = !1));
      for (let { coordinator: t } of this.hosts.values()) t.setSyncEnabled(e);
    }
  }
  setForeground(e) {
    if (!(this.disposed || this.foreground === e)) {
      this.foreground = e;
      for (let { coordinator: t } of this.hosts.values()) t.setForeground(e);
    }
  }
  dispose() {
    if (!this.disposed) {
      ((this.disposed = !0), this.listeners.clear());
      for (let e of Array.from(this.hosts.keys())) this.detachHost(e);
    }
  }
  async enqueueStartupSync(e, t) {
    try {
      await t.requestStartupSync();
    } catch (t) {
      this.onError(e, t);
    }
  }
  scheduleRun = (e, t) =>
    e === `full` ? this.runFullScan(t) : this.runHeadScan(t);
  runFullScan(e) {
    let t = this.fullScanQueue.then(e);
    return (
      (this.fullScanQueue = t.then(
        () => void 0,
        () => void 0,
      )),
      t
    );
  }
  async runHeadScan(e) {
    await this.acquireHeadScanSlot();
    try {
      return await e();
    } finally {
      let e = this.headScanWaiters.shift();
      e == null ? --this.activeHeadScans : e();
    }
  }
  acquireHeadScanSlot() {
    return this.activeHeadScans < 2
      ? ((this.activeHeadScans += 1), Promise.resolve())
      : new Promise((e) => this.headScanWaiters.push(e));
  }
  publishCoordinatorUpdate(e, t) {
    if (t.type === `snapshot`) {
      (this.replaceHostSnapshot(e, t.snapshot),
        this.publish({ type: `snapshot`, snapshot: this.readSnapshot() }));
      return;
    }
    this.publish({
      type: `delta`,
      delta: {
        revision: t.mutation.catalogRevision,
        changedHosts: [{ hostId: e, isComplete: t.syncState.isComplete }],
        removedHostIds: [],
        changedEntries: t.entries,
        removedEntries: t.mutation.removedThreadIds.map((t) => ({
          hostId: e,
          threadId: t,
        })),
      },
    });
  }
  publish(e) {
    e.type === `delta` && this.applyDelta(e.delta);
    for (let t of this.listeners)
      try {
        t(e);
      } catch (e) {
        this.onError(this.localHostId, e);
      }
  }
  applyDelta(e) {
    if (this.aggregateLoaded) {
      for (let t of e.removedHostIds) {
        this.hostsById.delete(t);
        for (let [e, n] of this.entriesByKey)
          n.hostId === t && this.entriesByKey.delete(e);
      }
      for (let t of e.changedHosts) this.hostsById.set(t.hostId, t);
      for (let { hostId: t, threadId: n } of e.removedEntries)
        this.entriesByKey.delete(n3(t, n));
      for (let t of e.changedEntries)
        this.entriesByKey.set(n3(t.hostId, t.threadId), t);
      ((this.catalogRevision = Math.max(this.catalogRevision, e.revision)),
        (this.materializedSnapshot = null));
    }
  }
  replaceHostSnapshot(e, t) {
    if (this.aggregateLoaded) {
      for (let [t, n] of this.entriesByKey)
        n.hostId === e && this.entriesByKey.delete(t);
      for (let e of t.entries)
        this.entriesByKey.set(n3(e.hostId, e.threadId), e);
      (this.hostsById.set(e, { hostId: e, isComplete: t.syncState.isComplete }),
        (this.catalogRevision = Math.max(
          this.catalogRevision,
          t.syncState.catalogRevision,
        )),
        (this.materializedSnapshot = null));
    }
  }
};
function n3(e, t) {
  return `${e}\0${t}`;
}
function r3(e, t) {
  return (
    t.sourceUpdatedAt - e.sourceUpdatedAt ||
    t.sourceCreatedAt - e.sourceCreatedAt ||
    e.hostId.localeCompare(t.hostId) ||
    e.threadId.localeCompare(t.threadId)
  );
}
var i3 = class extends a.P {
    logger = r.i(`ThreadMetadataGenerationService`);
    constructor(e) {
      (super(), (this.appServerConnectionRegistry = e));
    }
    async generateTitle({
      hostId: e,
      prompt: t,
      cwd: r,
      threadStartKind: i,
      serviceName: a,
    }) {
      try {
        return await n.n({
          prompt: t,
          cwd: r,
          serviceTier: null,
          buildTitlePrompt: void 0,
          serviceName: a,
          appServerClient: this.appServerConnectionRegistry.getConnection(e),
        });
      } catch (e) {
        return (
          this.logger.warning(`Failed to generate thread title`, {
            safe: {},
            sensitive: { error: e },
          }),
          null
        );
      }
    }
    generateDescription({
      hostId: e,
      threadId: t,
      title: r,
      cwd: i,
      serviceName: a,
    }) {
      return n.t({
        title: r,
        cwd: i,
        serviceTier: null,
        sourceThreadId: t,
        serviceName: a,
        appServerClient: this.appServerConnectionRegistry.getConnection(e),
      });
    }
  },
  a3 = `mcp_app_sandbox`,
  o3 = r.a(`mcp-app-sandbox`);
function s3(e, t, n = {}) {
  o3().info(`${a3}.${e}`, {
    safe: n.safe ?? {},
    sensitive: {
      origin: t.origin,
      ownerWebContentsId: t.ownerWebContents.id,
      sandboxId: t.sandboxId,
      ...n.sensitive,
    },
  });
}
function c3(e) {
  let t = m3(e);
  return t == null
    ? !0
    : ![`about:`, `blob:`, `data:`, `http:`, `https:`].includes(t.protocol);
}
function l3(e, { allowDevtools: t }) {
  let n = m3(e),
    r =
      t && n?.hostname === `localhost` && [`http:`, `ws:`].includes(n.protocol);
  return (
    n != null &&
    ([`about:`, `blob:`, `data:`, `https:`, `wss:`].includes(n.protocol) ||
      (t && n.protocol === `devtools:`) ||
      r)
  );
}
function u3(e) {
  return (
    d3(e.frame) ||
    p3(e.url) ||
    p3(f3(e.requestHeaders, `Origin`)) ||
    p3(f3(e.requestHeaders, `Referer`))
  );
}
function d3(e) {
  return e != null && (p3(e.origin) || p3(e.url));
}
function f3(e, t) {
  let n = Object.keys(e).find((e) => e.toLowerCase() === t.toLowerCase());
  return n == null ? void 0 : e[n];
}
function p3(e) {
  let t = m3(e);
  return t != null && n.Ga(t.hostname);
}
function m3(e) {
  if (e == null || e.length === 0) return null;
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
function h3(e) {
  return m3(e)?.protocol ?? `invalid`;
}
function g3(e, t, n) {
  let r = m3(t.sourceUrl),
    i = (e) => {
      let t = m3(e);
      return (
        t != null &&
        r != null &&
        t.origin === r.origin &&
        t.pathname === r.pathname &&
        t.search === r.search
      );
    },
    a = (e) => (e.isMainFrame === !0 ? !i(e.url) : c3(e.url)),
    o = (e, n) => {
      s3(e, t, {
        safe: {
          isMainFrame: n.isMainFrame,
          reason:
            n.isMainFrame === !0
              ? `main_frame_url_mismatch`
              : `privileged_subframe_url`,
          urlProtocol: h3(n.url),
        },
        sensitive: { guestWebContentsId: n.guestWebContentsId, url: dI(n.url) },
      });
    },
    s = (t, n) => {
      let r = t.url ?? n;
      i(r) ||
        (o(`navigation_blocked`, {
          guestWebContentsId: e.id,
          isMainFrame: !0,
          url: r,
        }),
        t.preventDefault());
    },
    c = (t) => {
      a({ isMainFrame: t.isMainFrame, url: t.url }) &&
        (o(`navigation_blocked`, {
          guestWebContentsId: e.id,
          isMainFrame: t.isMainFrame,
          url: t.url,
        }),
        t.preventDefault());
    },
    l = (t, n) => {
      let r = t.url ?? n;
      a({ isMainFrame: t.isMainFrame, url: r }) &&
        (o(`redirect_blocked`, {
          guestWebContentsId: e.id,
          isMainFrame: t.isMainFrame,
          url: r,
        }),
        t.preventDefault());
    },
    u = (n, r, i, a, o, s, c) => {
      r !== -3 &&
        s3(`guest_load_failed`, t, {
          safe: { errorCode: r, isMainFrame: o, urlProtocol: h3(a) },
          sensitive: {
            errorDescription: i,
            frameProcessId: s,
            frameRoutingId: c,
            guestWebContentsId: e.id,
            validatedURL: dI(a),
          },
        });
    },
    d = (n, r, i) => {
      o3().error(`${a3}.preload_failed`, {
        safe: {},
        sensitive: {
          error: i,
          guestWebContentsId: e.id,
          preloadPath: r,
          sandboxId: t.sandboxId,
        },
      });
    },
    f = (n, r) => {
      r.reason !== `clean-exit` &&
        s3(`render_process_gone`, t, {
          safe: { reason: r.reason },
          sensitive: { exitCode: r.exitCode, guestWebContentsId: e.id },
        });
    },
    p = () => {
      (n(),
        e.removeListener(`will-navigate`, s),
        e.removeListener(`will-frame-navigate`, c),
        e.removeListener(`will-redirect`, l),
        e.removeListener(`did-fail-load`, u),
        e.removeListener(`preload-error`, d),
        e.removeListener(`render-process-gone`, f),
        e.removeListener(`destroyed`, p));
    };
  (e.setWindowOpenHandler(
    ({ url: n }) => (
      s3(`popup_blocked`, t, {
        safe: { urlProtocol: h3(n) },
        sensitive: { guestWebContentsId: e.id, url: dI(n) },
      }),
      { action: `deny` }
    ),
  ),
    e.on(`will-navigate`, s),
    e.on(`will-frame-navigate`, c),
    e.on(`will-redirect`, l),
    e.on(`did-fail-load`, u),
    e.on(`preload-error`, d),
    e.on(`render-process-gone`, f),
    e.on(`destroyed`, p));
}
var _3 = /\b(?:Chrome|Chromium)\/(\d+)\./;
function v3(e, t) {
  return t
    .filter(Boolean)
    .reduce((e, t) => e.replace(RegExp(`\\s${y3(t)}/[^\\s]+`, `g`), ``), e);
}
function y3(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, `\\$&`);
}
function b3(e) {
  let t = _3.exec(e)?.[1];
  return t == null
    ? null
    : `"Chromium";v="${t}", "Google Chrome";v="${t}", "Not=A?Brand";v="24"`;
}
function x3() {
  return process.platform === `darwin`
    ? `"macOS"`
    : process.platform === `win32`
      ? `"Windows"`
      : `"Linux"`;
}
function S3(e, t) {
  let n = { ...e },
    r = v3(t.userAgent, t.excludedUserAgentProducts),
    i = b3(r);
  return (
    C3(n, `User-Agent`, r),
    C3(n, `Accept-Language`, t.acceptLanguage),
    i != null && C3(n, `sec-ch-ua`, i),
    C3(n, `sec-ch-ua-mobile`, `?0`),
    C3(n, `sec-ch-ua-platform`, x3()),
    n
  );
}
function C3(e, t, n) {
  let r = Object.keys(e).find((e) => e.toLowerCase() === t.toLowerCase());
  (r != null && r !== t && delete e[r], (e[t] = n));
}
var w3 = !1,
  T3 = new WeakSet();
function E3() {
  if (w3) return;
  let e = `session` in c ? c.session?.defaultSession : void 0;
  e?.webRequest != null &&
    ((w3 = !0),
    e.webRequest.onBeforeSendHeaders((t, n) => {
      let r = t.requestHeaders;
      n({
        requestHeaders: u3({ frame: t.frame, requestHeaders: r, url: t.url })
          ? S3(r, {
              acceptLanguage: O3(),
              excludedUserAgentProducts: [`Electron`, c.app.getName()],
              userAgent: e.getUserAgent(),
            })
          : r,
      });
    }));
}
function D3(e, { allowDevtools: t }) {
  let r = c.session.fromPartition(n.Ha(e));
  return (
    Yz(r),
    T3.has(r)
      ? r
      : (T3.add(r),
        r.setPermissionRequestHandler((t, n, r) => {
          (o3().info(`${a3}.permission_blocked`, {
            safe: { permission: n },
            sensitive: { guestWebContentsId: t.id, sandboxId: e },
          }),
            r(!1));
        }),
        r.setPermissionCheckHandler(() => !1),
        r.on(`will-download`, (t, n) => {
          (o3().info(`${a3}.download_blocked`, {
            safe: { urlProtocol: h3(n.getURL()) },
            sensitive: { sandboxId: e, url: dI(n.getURL()) },
          }),
            t.preventDefault(),
            n.cancel());
        }),
        r.webRequest.onBeforeRequest((n, r) => {
          let i = l3(n.url, { allowDevtools: t });
          (i ||
            o3().info(`${a3}.request_blocked`, {
              safe: { resourceType: n.resourceType, urlProtocol: h3(n.url) },
              sensitive: {
                guestWebContentsId: n.webContentsId,
                sandboxId: e,
                url: dI(n.url),
              },
            }),
            r({ cancel: !i }));
        }),
        r.webRequest.onBeforeSendHeaders((e, t) => {
          t({
            requestHeaders: S3(e.requestHeaders, {
              acceptLanguage: O3(),
              excludedUserAgentProducts: [`Electron`, c.app.getName()],
              userAgent: r.getUserAgent(),
            }),
          });
        }),
        r)
  );
}
function O3() {
  let e = c.app.getPreferredSystemLanguages();
  return (e.length > 0 ? e : [c.app.getLocale()])
    .map((e, t) =>
      t === 0 ? e : `${e};q=${Math.max(1 - t * 0.1, 0.1).toFixed(1)}`,
    )
    .join(`,`);
}
var k3 = (0, u.join)(__dirname, `sandbox-preload.js`),
  A3 = 3e4,
  j3 = !1,
  M3 = new Map(),
  N3 = new Map();
function P3() {
  (E3(), I3());
}
function F3(e, t, n) {
  n && R3(e, t)?.openDevTools({ mode: `detach` });
}
function I3() {
  j3 ||
    !(`ipcMain` in c) ||
    c.ipcMain?.on == null ||
    ((j3 = !0),
    c.ipcMain.on(Zl, (e, t) => {
      let r = N3.get(e.sender.id);
      if (r == null) {
        o3().warning(`${a3}.init_rejected`, {
          safe: { reason: `unknown_guest` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      let i = L3(t);
      if (i == null) {
        s3(`init_rejected`, r, { safe: { reason: `invalid_message` } });
        return;
      }
      if (r.origin !== i.origin) {
        s3(`init_rejected`, r, {
          safe: { reason: `origin_mismatch` },
          sensitive: {
            guestWebContentsId: e.sender.id,
            receivedOrigin: i.origin,
          },
        });
        return;
      }
      if (r.initId !== i.initId) {
        s3(`init_rejected`, r, {
          safe: { reason: `init_id_mismatch` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (!n.Wa(i.portNames)) {
        s3(`init_rejected`, r, {
          safe: {
            receivedPortNameCount: i.portNames.length,
            reason: `invalid_port_names`,
          },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (e.ports.length !== i.portNames.length + 1) {
        s3(`init_rejected`, r, {
          safe: {
            expectedPortCount: i.portNames.length + 1,
            receivedPortCount: e.ports.length,
            reason: `port_count_mismatch`,
          },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (r.ownerWebContents.isDestroyed()) {
        s3(`init_rejected`, r, {
          safe: { reason: `owner_destroyed` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      r.ownerWebContents.postMessage(
        Ql,
        {
          initId: i.initId,
          origin: i.origin,
          portNames: i.portNames,
          sandboxId: r.sandboxId,
          type: `init`,
        },
        e.ports,
      );
    }));
}
function L3(e) {
  if (typeof e != `object` || !e) return null;
  let { initId: t, origin: n, portNames: r, type: i } = e;
  return i !== `init` ||
    typeof t != `string` ||
    t.length === 0 ||
    typeof n != `string` ||
    !Array.isArray(r)
    ? null
    : { initId: t, origin: n, portNames: r };
}
function R3(e, t) {
  for (let [n, r] of N3) {
    if (r.ownerWebContents.id !== e.id || r.sandboxId !== t) continue;
    let i = c.webContents.fromId(n);
    if (i == null || i.isDestroyed()) {
      N3.delete(n);
      continue;
    }
    return i;
  }
  return null;
}
function z3(e, t, { allowNonMcpAppSandboxWebviews: r = !1 } = {}) {
  let i = (i, a, o) => {
      let s = (t, n = {}) => {
          (o3().warning(`${a3}.attach_rejected`, {
            safe: t,
            sensitive: { ownerWebContentsId: e.id, ...n },
          }),
            i.preventDefault());
        },
        c = n.Ba(o.partition);
      if (c == null) {
        r || s({ reason: `non_mcp_app_sandbox_webview` });
        return;
      }
      let l = o.src,
        u = n.Ua(l, { requireSkybridge: !0 }),
        d = n.Va(l);
      if (!n.Ka(c)) {
        s({ reason: `invalid_sandbox_id` });
        return;
      }
      if (u == null) {
        s(
          { reason: `invalid_source_url`, sourceProtocol: h3(l) },
          { sourceUrl: dI(l) },
        );
        return;
      }
      if (d == null) {
        s({ reason: `invalid_init_id` });
        return;
      }
      let f = D3(c, { allowDevtools: t });
      (B3(f, {
        initId: d,
        origin: u,
        ownerWebContents: e,
        sandboxId: c,
        sourceUrl: l,
      }),
        (a.session = f),
        (a.preload = k3),
        (a.nodeIntegration = !1),
        (a.nodeIntegrationInSubFrames = !1),
        (a.contextIsolation = !0),
        (a.disableDialogs = !0),
        (a.sandbox = !0),
        (a.webSecurity = !0),
        (a.plugins = !1),
        (a.allowRunningInsecureContent = !1),
        (a.devTools = t),
        (a.webviewTag = !1));
    },
    a = (t, r) => {
      let i = V3(r.session, e, n.Va(r.getURL()));
      if (i == null) {
        o3().warning(`${a3}.attach_unmatched`, {
          safe: { urlProtocol: h3(r.getURL()) },
          sensitive: { guestWebContentsId: r.id, url: dI(r.getURL()) },
        });
        return;
      }
      (N3.set(r.id, i),
        g3(r, i, () => {
          N3.delete(r.id);
        }));
    };
  (e.on(`will-attach-webview`, i),
    e.on(`did-attach-webview`, a),
    e.once(`destroyed`, () => {
      (e.removeListener(`will-attach-webview`, i),
        e.removeListener(`did-attach-webview`, a),
        U3(e));
    }));
}
function B3(e, t) {
  let n = setTimeout(() => {
      s3(`attach_expired`, t);
      let n = M3.get(e)?.find((e) => e.state === t);
      n != null && H3(e, n);
    }, A3),
    r = M3.get(e) ?? [];
  (r.push({ state: t, timeout: n }), M3.set(e, r));
}
function V3(e, t, n) {
  let r = M3.get(e),
    i = -1;
  r != null &&
    (i =
      n == null
        ? r.findIndex(({ state: e }) => e.ownerWebContents.id === t.id)
        : r.findIndex(
            ({ state: e }) => e.ownerWebContents.id === t.id && e.initId === n,
          ));
  let a = r != null && i >= 0 ? (r.splice(i, 1)[0] ?? null) : null;
  return (
    a != null && clearTimeout(a.timeout),
    (r == null || r.length === 0) && M3.delete(e),
    a?.state ?? null
  );
}
function H3(e, t) {
  let n = M3.get(e);
  if (n == null) return;
  let r = n.filter((e) => e !== t);
  (clearTimeout(t.timeout),
    r.length === 0 ? M3.delete(e) : r.length !== n.length && M3.set(e, r));
}
function U3(e) {
  for (let [t, n] of M3)
    for (let r of n) r.state.ownerWebContents.id === e.id && H3(t, r);
}
var W3 = 5e6,
  G3 = n
    .xl({
      captureRect: n.xl({
        height: n.bl().finite().positive(),
        width: n.bl().finite().positive(),
        x: n.bl().finite().nonnegative(),
        y: n.bl().finite().nonnegative(),
      }),
      sandboxId: n.wl().regex(/^visualization-[0-9a-f]{16}$/),
      viewportSize: n.xl({
        height: n.bl().finite().positive(),
        width: n.bl().finite().positive(),
      }),
    })
    .refine(
      ({ captureRect: e, viewportSize: t }) =>
        e.x + e.width <= t.width && e.y + e.height <= t.height,
    ),
  K3 = n.xl({
    file: n.wl().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*\.html$/),
    hostId: n.wl().trim().min(1).optional(),
    threadId: n.wl().regex(/^[a-zA-Z0-9_-]+$/),
  }),
  q3 = class extends a.P {
    constructor(e, t) {
      (super(), (this.executionHostRegistry = e), (this.origin = t));
    }
    async copyImage(e) {
      let t = G3.safeParse(e);
      if (!t.success)
        throw Error(`codex-inline-vis-screenshot: invalid request`);
      let { captureRect: n, sandboxId: r, viewportSize: i } = t.data,
        a = R3(this.origin, r);
      if (a == null)
        throw Error(`codex-inline-vis-screenshot: sandbox is unavailable`);
      let o = await a.capturePage(),
        s = bV(o.getSize(), n, i, 0);
      if (s == null)
        throw Error(`codex-inline-vis-screenshot: capture is empty`);
      let l = o.crop(s),
        u =
          process.platform === `win32`
            ? c.nativeImage.createFromBitmap(l.toBitmap(), l.getSize())
            : l;
      c.clipboard.writeImage(u);
    }
    async read(e) {
      let t = K3.safeParse(e);
      if (!t.success) throw Error(`Invalid visualization read request`);
      let { file: r, hostId: i, threadId: a } = t.data,
        o = this.executionHostRegistry.get(i),
        [s, c] = await Promise.all([o.codexHome(), o.platformPath()]),
        l = n.pi(a);
      if (l == null) throw Error(`Invalid visualization read request`);
      let u = c.join(s, `visualizations`, ...l.split(`/`), a, r);
      if (
        (
          await o.stat(u, { bypassCache: !0, followSymlinks: !1 })
        ).isSymbolicLink()
      )
        throw Error(`Invalid visualization read request`);
      let d = Buffer.from(
        await new Response(await o.readFile(u)).arrayBuffer(),
      );
      return d.byteLength <= W3 ? { contents: d.toString(`utf8`) } : null;
    }
  },
  J3 = class extends a.P {
    constructor(e, t) {
      (super(), (this.windowManager = e), (this.origin = t));
    }
    setHistorySwipeNavigationState(e) {
      this.windowManager.setHistorySwipeNavigationState(this.origin, e);
    }
  },
  Y3 = r.i(`WindowContext`),
  X3 = `Codex rebuilt its local database.`;
function Z3(e) {
  if (e.method !== `configWarning`) return null;
  let { params: t } = e;
  return t == null || t.summary !== X3
    ? null
    : n.r(
        [X3, typeof t.details == `string` ? t.details : ``].join(`
`),
      );
}
var Q3 = class {
  appServerConnectionRegistry;
  remoteControlDeviceKeyClient;
  messageHandler;
  remoteConnectionsHandler;
  sharedObjectRepository;
  fetchHandler;
  appState;
  errorReporter;
  localEnvironments;
  workspaceFiles;
  options;
  scopedState;
  durableThreadHostConfig;
  customRuntime;
  conversationExecutor;
  devboxService;
  primaryRuntimeService;
  appServerHistorySnapshots;
  kitchenService;
  localThreadCatalogSyncCoordinator;
  threadCatalogSyncManager;
  keyboardModifierService = new x1();
  restricted;
  initialRemoteConnectionsRefresh;
  sshConnectTimeoutSeconds;
  codexMicroServiceManager;
  disposables = new n.sr();
  registeredWindows = new Map();
  pendingAutomationCapabilityEvents = [];
  executionHostRegistry = new n1();
  fileDragServices = new WeakMap();
  appViewsByWebContentsId = new Map();
  ipcClientsByWebContentsId = new Map();
  constructor(e) {
    ((this.options = e),
      (this.codexMicroServiceManager = new tS(e.windowManager)),
      this.disposables.add(this.codexMicroServiceManager),
      (this.appState = e.appGlobalState),
      (this.errorReporter = e.errorReporter),
      (this.scopedState = e.globalState),
      (this.durableThreadHostConfig = null),
      (this.sharedObjectRepository = new n.l()),
      this.sharedObjectRepository.set(`host_config`, sd),
      this.sharedObjectRepository.set(`remote_ssh_connections`, []),
      this.sharedObjectRepository.set(`remote_control_connections_state`, {
        available: !1,
        accessRequired: !1,
        authRequired: !1,
        clientAuthorized: !1,
      }),
      this.sharedObjectRepository.set(`local_remote_control_client_id`, null),
      this.sharedObjectRepository.set(
        `local_remote_control_environment_id`,
        e.appGlobalState.get(a.b),
      ),
      this.sharedObjectRepository.set(
        `local_remote_control_installation_id`,
        e.appGlobalState.get(a.x),
      ));
    let t = n.Dt();
    t != null &&
      this.sharedObjectRepository.set(`local_remote_control_enabled`, t);
    let r = this.createAppServerConnection(V),
      o = new n.et();
    ((this.remoteControlDeviceKeyClient = Z2({
      resourcesPath: St({
        env: process.env,
        resourcesPath: process.resourcesPath,
      }),
    })),
      this.executionHostRegistry.add(V, o),
      (this.appServerConnectionRegistry = new n.jn()),
      this.appServerConnectionRegistry.addConnection(V, r));
    let s;
    if (
      ((this.appServerHistorySnapshots = new j_(
        () => r.getAuthenticatedPrincipal(),
        () => {
          if (s !== void 0) return s;
          try {
            let e = o_();
            s = e == null ? null : new v_(e);
          } catch {
            (Y3.warning(`Failed to open the disposable history cache`),
              (s = null));
          }
          return s;
        },
        (e) => {
          let t = this.appServerConnectionRegistry.getMaybeConnection(e);
          if (t == null) return null;
          let { hostConfig: n } = t;
          return P_(n, n.kind === `local` ? t6(n) : null);
        },
        (e) =>
          this.appServerConnectionRegistry.subscribe((t) => {
            t.type === `removed` && e(t.hostId);
          }),
        (e) => r.registerInternalAuthenticatedPrincipalChangeHandler(e),
      )),
      (this.conversationExecutor = null),
      (this.customRuntime = null),
      (this.kitchenService = null),
      this.durableThreadHostConfig != null)
    ) {
      let e = this.createAppServerConnection(
        this.durableThreadHostConfig.id,
        this.durableThreadHostConfig,
        !1,
        r,
      );
      (this.executionHostRegistry.add(this.durableThreadHostConfig.id, e),
        this.appServerConnectionRegistry.addConnection(
          this.durableThreadHostConfig.id,
          e,
        ));
    }
    if (
      ((this.threadCatalogSyncManager =
        e.createThreadCatalogStore == null
          ? null
          : new t3(V, e.createThreadCatalogStore, (e, t) => {
              Y3.warning(`Thread catalog sync failed`, {
                safe: {},
                sensitive: { error: t, hostId: e },
              });
            })),
      (this.localThreadCatalogSyncCoordinator =
        this.threadCatalogSyncManager?.attachHost(`local`, `local`, r) ?? null),
      this.threadCatalogSyncManager != null)
    ) {
      let t = this.threadCatalogSyncManager;
      (this.disposables.add(t),
        this.disposables.add(
          e.appEvent.subscribe((e) => {
            e.type === `background`
              ? t.setForeground(!1)
              : e.type === `foreground` && t.setForeground(!0);
          }),
        ));
    }
    ((this.workspaceFiles = new c1(
      (e) => this.getAppServerClient(e),
      this.scopedState,
    )),
      (this.localEnvironments = new V1((e) => this.getAppServerClient(e))));
    let c = new L4(
      e.windowManager,
      this.scopedState,
      () => e.settingsStore.getEffective(n.Bi.integratedTerminalShell.key),
      sd.terminal_command ?? null,
      async (t) => {
        let r = await e.gitManager.getWorktreeRepository(n.oo(t), o);
        return r == null ? null : n.ct(r);
      },
      (e) => this.getHostConfigForHostId(e),
      (e) => this.appServerConnectionRegistry.getMaybeConnection(e),
    );
    ((this.remoteConnectionsHandler = new P2(
      this.appState,
      this.sharedObjectRepository,
      V,
      this.appServerConnectionRegistry,
      {
        desktopOriginator: e.desktopOriginator,
        devApiBaseUrl: e.devApiBaseUrl,
        prodApiBaseUrl: e.prodApiBaseUrl,
      },
      r,
      this.remoteControlDeviceKeyClient,
      this.getHostConfigForHostId.bind(this),
      this.getAppServerClient.bind(this),
      this.createAndRegisterRemoteConnection.bind(this),
      this.disposeRemoteConnection.bind(this),
      (e, t) => {
        this.threadCatalogSyncManager?.reconcileAuthoritativeHostKind(e, t);
      },
    )),
      (this.devboxService = null));
    let l = new j0(
      this.appServerConnectionRegistry,
      e.errorReporter,
      this.scopedState,
      this.sharedObjectRepository,
      e.windowManager,
    );
    ((this.primaryRuntimeService = l),
      this.disposables.add(
        J1({
          captureMessage: e.desktopSentry.captureMessage,
          diagnosePrimaryRuntimeDependencies: () =>
            l.diagnoseDependencies({ hostId: V }),
          getPlatformOs: () => r.platformOs(),
          getPrimaryRuntimeDependencyRoot: n.Kn,
          registerInternalNotificationHandler: (e) =>
            r.registerInternalNotificationHandler(e),
        }),
      ),
      (this.fetchHandler = new DF(
        this.scopedState,
        (e) => this.getRegisteredWindowIpcClient(e),
        this.appServerConnectionRegistry,
        e.gitManager,
        o,
        this.remoteConnectionsHandler,
        e.hotkeyWindowLifecycleManager,
        e.windowManager,
        c,
        l.isWorkspaceDependenciesFeatureEnabled,
        (e) => this.getAppServerClient(e),
        (e) => this.messageHandler.getWorktreeService(e),
        e.requestGitWorker,
        this.sharedObjectRepository,
        e.errorReporter,
        e.repoRoot,
        e.globalDictationHotkeyController,
        async (e) => {
          let t = this.messageHandler.getBrowserSidebarManager();
          (await t.clearBrowsingData(e), t.reloadOpenPages());
        },
        e.refreshApplicationMenu,
        e.requestComputerUseCaptureWorker,
        e.subscribeComputerUseCaptureWorkerEvent,
        () => {
          (e.windowManager.refreshAppShellCommandAccelerators(),
            this.messageHandler
              .getBrowserSidebarManager()
              .refreshOwnerCommandAccelerators());
        },
        e.settingsStore,
        e.updateDockIcon,
        void 0,
        ({ includeBrowserTabs: t, includeDesktopLogs: n }) => [
          ...(n ? e.avatarOverlayManager.getFeedbackLogEntries() : []),
          ...(t
            ? [
                {
                  contents: Buffer.from(
                    JSON.stringify(
                      this.messageHandler
                        .getBrowserSidebarManager()
                        .getFeedbackDiagnostics(),
                    ),
                  ),
                  name: `browser-tabs.json`,
                },
              ]
            : []),
        ],
        void 0,
        void 0,
        e.requestOpenInWorker,
      )),
      this.disposables.add(() => this.fetchHandler.dispose()),
      this.disposables.add(l.startUpdatePolling()));
    let u = aI(this.scopedState);
    this.disposables.add(
      r.registerInternalNotificationHandler((e) => {
        e.method === `account/updated` &&
          e.params?.authMode == null &&
          u.clear();
      }),
    );
    let d = new _I(this.fetchHandler, {
        desktopOriginator: e.desktopOriginator,
        devApiBaseUrl: e.devApiBaseUrl,
        deviceCheckCookieManager: new yS({
          bundleIdentifier: i.r(e.buildFlavor),
          generateToken: () =>
            we({
              resourcesPath: St({
                env: process.env,
                resourcesPath: process.resourcesPath,
              }),
            }),
          url: R_(e, `/devicecheck`),
        }),
        integrityStateStore: u,
        messageChannel: e.messageChannel,
        appServerConnectionRegistry: this.appServerConnectionRegistry,
        prodApiBaseUrl: e.prodApiBaseUrl,
      }),
      f = new Q$({
        buildFlavor: e.buildFlavor,
        gitWorkerInvocationSampler: e.gitWorkerInvocationSampler,
      });
    ((this.messageHandler = new U$(
      V,
      sd,
      o,
      e.windowManager,
      e.avatarOverlayManager,
      this.appServerConnectionRegistry,
      d,
      this.scopedState,
      this.sharedObjectRepository,
      c,
      e.gitManager,
      e.requestGitWorker,
      e.desktopNotificationManager,
      e.errorReporter,
      e.desktopSentry,
      e.updateWorkerSentryUser,
      (e) => this.getRegisteredWindowIpcClient(e),
      e.inboxManager,
      e.datadogLogger,
      e.maxLogLevel,
      e.buildFlavor,
      e.subscribeGitWorkerEvent,
      f,
      (e) => this.getHostConfigForHostId(e),
      e.hasActiveComputerUseTurn,
      e.settingsStore,
    )),
      a.j(`OwlDownloads`) &&
        this.messageHandler
          .getBrowserSidebarManager()
          .downloadStore.setChangeListener(() => {
            this.broadcastDownloadsState();
          }));
    let p = !1;
    (this.disposables.add(
      this.sharedObjectRepository.addSubscriber((e) => {
        if (
          e === `remote_ssh_connections` ||
          e === `remote_wsl_connections` ||
          e === `remote_control_connections`
        ) {
          this.remoteConnectionsHandler.reconcileRemoteConnections(
            this.getSharedRemoteConnections(),
          );
          return;
        }
        if (
          e === `local_remote_control_environment_id` ||
          e === `local_remote_control_installation_id`
        ) {
          let e = this.sharedObjectRepository.get(
              `local_remote_control_environment_id`,
            ),
            t = this.sharedObjectRepository.get(
              `local_remote_control_installation_id`,
            );
          (this.appState.set(a.b, e ?? void 0),
            t != null && this.appState.set(a.x, t),
            p ||
              ((p = !0),
              queueMicrotask(() => {
                ((p = !1),
                  this.remoteConnectionsHandler.handleLocalRemoteControlIdentityChanged());
              })));
        }
      }),
    ),
      (this.initialRemoteConnectionsRefresh = Promise.all([
        this.remoteConnectionsHandler.refreshRemoteConnections(),
        this.remoteConnectionsHandler.refreshLocalRemoteControlClientId(),
      ])
        .then(() => void 0)
        .catch((e) => {
          Y3.warning(`Failed to refresh remote connections`, {
            safe: {},
            sensitive: { error: e },
          });
        })));
  }
  createAppHost(e) {
    let t = this.messageHandler.getBrowserSidebarManager(),
      { downloadStore: r } = t,
      o =
        this.threadCatalogSyncManager == null
          ? void 0
          : new H1(this.threadCatalogSyncManager);
    o != null && e.once(`destroyed`, () => o[Symbol.dispose]());
    let s = this.appServerConnectionRegistry.getConnection(V);
    return new b_({
      customRuntime: void 0,
      appActions: {
        runInPrimaryWindow: async (e) => {
          let t = this.options.windowManager.getPrimaryWindow();
          if (t == null) throw Error(`No primary app window is available`);
          let n = this.appViewsByWebContentsId.get(t.webContents.id);
          if (n == null) throw Error(`The primary app window is not ready`);
          return (await n.services).appActions.run(e);
        },
      },
      appServerHistorySnapshots: this.appServerHistorySnapshots,
      appInfo: new T_(),
      appUpdates: new jb(
        this.options.windowManager,
        e,
        this.options.sparkleManager,
      ),
      browserProfileImport: Ix.create(),
      browserSidebar: new Rx(this.messageHandler, t, e),
      browserSidebarAutocomplete:
        a.j(`OwlHistory`) && a.j(`OwlWebViewEnhancements`)
          ? new Ex(
              this.messageHandler.getBrowserSidebarAutocompleteCoordinator(),
              e,
            )
          : void 0,
      debug: new vS(this.options.windowManager.debugWindowManager, t, e),
      downloads: zx.create(r, e),
      fileDrags: this.getFileDragService(e),
      hotkeyWindowHotkeys: new b1(this.options.hotkeyWindowLifecycleManager, e),
      keyboardModifiers: this.keyboardModifierService,
      libraryFiles: new B1(new N1(s, this.scopedState)),
      localEnvironments: this.localEnvironments,
      localThreadCatalog: o,
      appshotHotkeys: this.options.appshotHotkeysService,
      browserUsePermissions: new ex(n.ni()),
      conversationalOnboarding: new nS(),
      customAvatars: new rS(this.executionHostRegistry),
      fileAttachments: new i1(this.executionHostRegistry),
      workspaceFiles: this.workspaceFiles,
      owlFeatures: new a.A(),
      primaryRuntime: this.primaryRuntimeService,
      quickChatWindow: new M0(this.options.quickChatController, e),
      pluginScheduledTasks: new o0(s),
      chromeNativeHost: new Hx(
        this.executionHostRegistry,
        process.env.NODE_ENV === `development` ? this.options.repoRoot : void 0,
      ),
      performanceLogger: new a0(
        this.options.performanceLogger,
        this.messageHandler,
        e,
      ),
      clipboard: new Gx(),
      projectWritableRoots:
        this.messageHandler.createProjectWritableRootsService(e),
      requestUserInputAutoResolution: new c4(
        this.appServerConnectionRegistry,
        e.id,
      ),
      systemPermissions:
        process.platform === `darwin` || (process.platform === `win32` && iP())
          ? new D4(this.options.buildFlavor, i.r(this.options.buildFlavor))
          : null,
      threadArchive: new K4(
        this.executionHostRegistry,
        this.appServerConnectionRegistry,
        this.localThreadCatalogSyncCoordinator,
      ),
      threadMetadataGeneration: new i3(this.appServerConnectionRegistry),
      threadProjectAssignments: new WP(
        this.scopedState,
        this.options.windowManager,
      ),
      visualizations: new q3(this.executionHostRegistry, e),
      codexMicro: new eS(
        this.codexMicroServiceManager,
        this.options.windowManager,
        e,
      ),
      notifications: new u4(
        this.options.windowManager,
        e,
        this.options.desktopNotificationManager,
      ),
      openInTargets: new VN(
        this.options.settingsStore,
        this.options.requestOpenInWorker,
      ),
      windowNavigation: new J3(this.options.windowManager, e),
    });
  }
  startFileDrag(e, t) {
    return this.getFileDragService(e).startDrag(t);
  }
  getFileDragService(e) {
    let t = this.fileDragServices.get(e);
    if (t != null) return t;
    let n = new y1(this.executionHostRegistry, this.scopedState, e);
    return (this.fileDragServices.set(e, n), n);
  }
  async registerAppView(e, t) {
    let { appUpdates: n, downloads: r } = await t.services;
    e.isDestroyed() ||
      (this.appViewsByWebContentsId.set(e.id, t),
      e.once(`destroyed`, () => {
        this.appViewsByWebContentsId.get(e.id) === t &&
          this.appViewsByWebContentsId.delete(e.id);
      }),
      await n.stateChanged(this.getAppUpdateViewState()),
      a.j(`OwlDownloads`) &&
        (await r.stateChanged(this.getDownloadsSnapshot())));
  }
  broadcastAppUpdateState() {
    let e = this.getAppUpdateViewState();
    for (let t of this.appViewsByWebContentsId.values())
      t.services
        .then(({ appUpdates: t }) => t.stateChanged(e))
        .catch((e) => {
          Y3.warning(`Failed to publish app update state to renderer`, {
            safe: {},
            sensitive: { error: e },
          });
        });
  }
  broadcastDownloadsState() {
    if (!a.j(`OwlDownloads`)) return;
    let e = this.getDownloadsSnapshot();
    for (let t of this.appViewsByWebContentsId.values())
      t.services
        .then(({ downloads: t }) => t.stateChanged(e))
        .catch((e) => {
          Y3.warning(`Failed to publish downloads state to renderer`, {
            safe: {},
            sensitive: { error: e },
          });
        });
  }
  async applyCodexAppConfigFromDisk(e) {
    let t = await dee(e);
    return t.status === `loaded`
      ? {
          status: `applied`,
          configPath: t.configPath,
          result: await this.applyCodexAppConfig(t.config),
        }
      : (t.status === `missing` && (this.sshConnectTimeoutSeconds = void 0), t);
  }
  async applyCodexAppConfig(e) {
    ((this.sshConnectTimeoutSeconds = e.sshConnectTimeoutSeconds),
      await this.initialRemoteConnectionsRefresh);
    let t = Nb({ config: e, globalState: this.appState }),
      { remoteConnections: r } =
        await this.remoteConnectionsHandler.refreshRemoteConnections(),
      i = Pb({
        config: e,
        globalState: this.appState,
        remoteConnections: r.filter(n.to),
      });
    return (
      t &&
        i.changedGlobalStateKeys.unshift(n.Gs.CODEX_MANAGED_REMOTE_CONNECTIONS),
      i.changedGlobalStateKeys.includes(
        n.Gs.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID,
      ) && (await this.remoteConnectionsHandler.refreshRemoteConnections()),
      i.changedGlobalStateKeys.length > 0 &&
        this.options.windowManager.sendMessageToAllWindows({
          type: `global-state-updated`,
          keys: i.changedGlobalStateKeys,
        }),
      i.remoteProjectsChanged &&
        this.options.windowManager.sendMessageToAllWindows({
          type: `workspace-root-options-updated`,
        }),
      this.remoteConnectionsHandler.connectRemoteConnectionsAndLogFailures(
        i.remoteConnectionHostIdsToConnect,
      ),
      i
    );
  }
  registerWindow(e) {
    let { webContents: t } = e,
      n = new RQ(t, e);
    (this.registeredWindows.set(t.id, n),
      t.once(`destroyed`, () => {
        (this.registeredWindows.delete(t.id), Ab(!1, t.id));
      }));
    for (let e of this.appServerConnectionRegistry.getAllHostIds())
      this.appServerConnectionRegistry
        .getConnection(e)
        .registerWebviewWindow(n);
    (this.registerIpcClientForWebContents(e.webContents),
      this.flushPendingAutomationCapabilityEvents());
  }
  handleMessage(e, t) {
    return this.messageHandler.handleMessage(e, t);
  }
  getBrowserSidebarManager() {
    return this.messageHandler.getBrowserSidebarManager();
  }
  getBrowserSessionRegistry() {
    return this.messageHandler.getBrowserSessionRegistry();
  }
  flushTelemetry() {
    this.messageHandler.flushTelemetry();
  }
  dispose() {
    let e = this.appServerConnectionRegistry.getAllHostIds();
    (this.messageHandler.dispose(), this.disposables.dispose());
    for (let t of e)
      (this.appServerConnectionRegistry.getMaybeConnection(t)?.dispose(),
        this.appServerConnectionRegistry.removeConnection(t),
        this.executionHostRegistry.remove(t));
    (this.registeredWindows.clear(), this.appViewsByWebContentsId.clear());
    for (let { dispose: e } of this.ipcClientsByWebContentsId.values()) e();
    this.ipcClientsByWebContentsId.clear();
  }
  getMessageHandler() {
    return this.messageHandler;
  }
  getSharedObject(e) {
    return this.sharedObjectRepository.get(e);
  }
  getAppUpdateViewState() {
    return {
      downloadProgressPercent:
        this.options.sparkleManager.getDownloadProgressPercent(),
      downloadedUpdateAppBrand:
        this.options.sparkleManager.getDownloadedUpdateAppBrand(),
      installProgressPercent:
        this.options.sparkleManager.getInstallProgressPercent(),
      isUpdateReady: this.options.sparkleManager.getIsUpdateReady(),
      lifecycleState: this.options.sparkleManager.getUpdateLifecycleState(),
      relaunchNotice: this.options.sparkleManager.getRelaunchNotice(),
    };
  }
  getDownloadsSnapshot() {
    return this.messageHandler
      .getBrowserSidebarManager()
      .downloadStore.getSnapshot();
  }
  setChronicleConfig(e) {
    this.sharedObjectRepository.set(`codex_chronicle_config`, e);
  }
  getSharedObjectSnapshot() {
    return this.sharedObjectRepository.getSnapshot();
  }
  getHostConfigForHostId(e) {
    if (e === `local`) return sd;
    if (this.durableThreadHostConfig?.id === e)
      return this.durableThreadHostConfig;
    let t = this.getSharedRemoteConnections().find((t) => t.hostId === e);
    if (!t) throw Error(`Host config for host ID ${e} not found`);
    return s4(t);
  }
  getSharedRemoteConnections() {
    return [
      ...(this.sharedObjectRepository.get(`remote_ssh_connections`) ?? []),
      ...(this.sharedObjectRepository.get(`remote_wsl_connections`) ?? []),
      ...(this.sharedObjectRepository.get(`remote_control_connections`) ?? []),
    ];
  }
  createAppServerConnection(e, t = sd, r = !1, a, o) {
    let s = {
        desktopOriginator: this.options.desktopOriginator,
        devApiBaseUrl: this.options.devApiBaseUrl,
        prodApiBaseUrl: this.options.prodApiBaseUrl,
      },
      l = St({ env: process.env, resourcesPath: process.resourcesPath }),
      u;
    if (n.$a(t)) {
      if (a == null)
        throw Error(
          `Remote control transport requires a desktop app-server client for authentication.`,
        );
      if (o == null)
        throw Error(
          `Remote control transport requires the remote-control device-key client.`,
        );
      u = e6({
        transportKind: `remote-control`,
        hostConfig: t,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        desktopApiOptions: s,
        desktopAuthAppServerClient: a,
        remoteControlDeviceKeyClient: o,
        globalState: this.appState,
        getSshConnectTimeoutSeconds: () => this.sshConnectTimeoutSeconds,
      });
    } else
      u = e6({
        transportKind: `standard`,
        hostConfig: t,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        desktopApiOptions: s,
        desktopAuthAppServerClient: a,
        globalState: this.appState,
        getSshConnectTimeoutSeconds: () => this.sshConnectTimeoutSeconds,
      });
    let d = this.options.hostProcess.getVersion(),
      f = new n.Mn(this.options.messageChannel, {
        buildFlavor: this.options.buildFlavor,
        clientInfo: { name: mu, title: `Codex Desktop`, version: d },
        defaultServiceName: hu,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        errorReporter: this.options.errorReporter,
        sharedObjectRepository: this.sharedObjectRepository,
        hostId: e,
        hostConfig: t,
        onAutomationCapabilityEvent: (e) => {
          this.sendAutomationCapabilityEvent(e);
        },
        onArchiveCleanup: async ({
          conversationId: t,
          cwd: n,
          replacementOwnerThreadId: r,
          replacementOwnerCwd: i,
        }) => {
          await this.messageHandler.getWorktreeService(e).handleArchiveCleanup({
            conversationId: t,
            cwd: n,
            replacementOwnerThreadId: r,
            replacementOwnerCwd: i,
          });
        },
        transport: u,
        hostProcess: this.options.hostProcess,
        isOnline: () => c.net.isOnline(),
        supportsAttestationRequests: be,
        isMcpAppsDilEnabled: () => Ke().dil,
        isOpenAIMcpFormElicitationsEnabled: () =>
          Ke().openAIMcpFormElicitations,
        beforeConnect: t.id === `local` ? () => V2({ hostConfig: t }) : void 0,
        prepareRemoteChatgptLoginPortForward: async () => {
          await Oy({ caller: `ssh_connect` });
        },
        ensureAuth: r,
        getRemoteConnectionMaxRetryAttempts: () =>
          this.appState.get(n.Gs.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS) ?? null,
      });
    if (t.id === `local` && this.options.onAppServerSqliteRecovery != null) {
      let e = this.options.onAppServerSqliteRecovery;
      f.registerInternalNotificationHandler((t) => {
        let n = Z3(t);
        n != null &&
          (Y3.info(`App-server SQLite recovery config warning received`, {
            safe: {},
            sensitive: {
              backupFolder: n.backupFolder,
              databasePath: n.databasePath,
            },
          }),
          e(n));
      });
    }
    return (
      f.registerInternalServerRequestHandler({
        methods: [
          `item/commandExecution/requestApproval`,
          `mcpServer/elicitation/request`,
        ],
        handler: (t) => (
          this.messageHandler.revealWindowsReviewRequest(e, t),
          null
        ),
      }),
      f.registerInternalServerRequestHandler({
        methods: [`attestation/generate`],
        handler: Se({
          bundleIdentifier: i.r(this.options.buildFlavor),
          getSignals: Jb,
          resourcesPath: l,
        }),
      }),
      f
    );
  }
  sendAutomationCapabilityEvent(e) {
    let t = this.options.windowManager.getPrimaryWindow();
    if (t == null) {
      this.pendingAutomationCapabilityEvents.push(e);
      return;
    }
    this.options.windowManager.sendMessageToWebContents(t.webContents, {
      type: `ipc-broadcast`,
      method: `automation-capability-event`,
      sourceClientId: `desktop`,
      version: n.fc(`automation-capability-event`),
      params: e,
    });
  }
  flushPendingAutomationCapabilityEvents() {
    let e = this.pendingAutomationCapabilityEvents;
    this.pendingAutomationCapabilityEvents = [];
    for (let t of e) this.sendAutomationCapabilityEvent(t);
  }
  createAndRegisterRemoteConnection(e) {
    let t = s4(e),
      n = this.createAppServerConnection(
        e.hostId,
        t,
        !0,
        this.getLocalAppServerClient(),
        this.remoteControlDeviceKeyClient,
      );
    (this.executionHostRegistry.add(e.hostId, n),
      this.threadCatalogSyncManager?.attachHost(e.hostId, $3(e), n));
    for (let e of this.registeredWindows.values()) n.registerWebviewWindow(e);
    return n;
  }
  disposeRemoteConnection(e) {
    this.threadCatalogSyncManager?.detachHost(e);
    let t = this.appServerConnectionRegistry.getMaybeConnection(e);
    t &&
      (t.dispose(),
      this.appServerConnectionRegistry.removeConnection(e),
      this.executionHostRegistry.remove(e));
  }
  getLocalAppServerClient() {
    return this.appServerConnectionRegistry.getConnection(V);
  }
  getLocalAuthToken({ refreshToken: e }) {
    return this.getLocalAppServerClient().getAuthToken({ refreshToken: e });
  }
  getAppServerClient(e = V) {
    return this.executionHostRegistry.get(e);
  }
  registerIpcClientForWebContents(e) {
    if (
      this.appServerConnectionRegistry
        .getConnection(`local`)
        .getTransportKind() !== `stdio` ||
      this.ipcClientsByWebContentsId.has(e.id)
    )
      return;
    let t = new n.v(`desktop`, this.errorReporter),
      r = new n.sr();
    r.add(
      t.addAnyBroadcastHandler(async (t) => {
        let n = { ...t, type: `ipc-broadcast` };
        this.messageHandler.sendMessageToOrigin(e, n);
      }),
    );
    let i = async (t) =>
      (await this.messageHandler.getThreadRole(e, t.conversationId)) ===
      `owner`;
    (r.add(
      t.addRequestHandler(`thread-follower-start-turn`, i, async (t) =>
        this.messageHandler.handleThreadFollowerStartTurnRequest(e, t),
      ),
    ),
      r.add(
        t.addRequestHandler(
          `thread-follower-load-complete-history`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerLoadCompleteHistoryRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(`thread-follower-compact-thread`, i, async (t) =>
          this.messageHandler.handleThreadFollowerCompactThreadRequest(e, t),
        ),
      ),
      r.add(
        t.addRequestHandler(`thread-follower-steer-turn`, i, async (t) =>
          this.messageHandler.handleThreadFollowerSteerTurnRequest(e, t),
        ),
      ),
      r.add(
        t.addRequestHandler(`thread-follower-interrupt-turn`, i, async (t) =>
          this.messageHandler.handleThreadFollowerInterruptTurnRequest(e, t),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-update-thread-settings`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerUpdateThreadSettingsRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-edit-last-user-turn`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerEditLastUserTurnRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-command-approval-decision`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerCommandApprovalDecisionRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-file-approval-decision`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerFileApprovalDecisionRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-permissions-request-approval-response`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerPermissionsRequestApprovalResponseRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(`thread-follower-submit-user-input`, i, async (t) =>
          this.messageHandler.handleThreadFollowerSubmitUserInputRequest(e, t),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-submit-mcp-server-elicitation-response`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerSubmitMcpServerElicitationRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(
        t.addRequestHandler(
          `thread-follower-set-queued-follow-ups-state`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerSetQueuedFollowUpsStateRequest(
              e,
              t,
            ),
        ),
      ),
      r.add(() => t.dispose()),
      this.ipcClientsByWebContentsId.set(e.id, {
        client: t,
        dispose: () => {
          (r.dispose(),
            this.messageHandler.rejectPendingThreadFollowerActionRequestsForOrigin(
              e,
            ));
        },
      }),
      e.once(`destroyed`, () => {
        this.disposeIpcClientForWebContents(e);
      }));
  }
  disposeIpcClientForWebContents(e) {
    let t = this.ipcClientsByWebContentsId.get(e.id);
    t && (this.ipcClientsByWebContentsId.delete(e.id), t.dispose());
  }
  getRegisteredWindowIpcClient(e) {
    return this.ipcClientsByWebContentsId.get(e.id)?.client ?? null;
  }
};
function $3(e) {
  return n.Qa(e) ? `remote-control` : n.ro(e) ? `wsl` : `ssh`;
}
function e6(e) {
  let t = Yy(e.hostConfig);
  if (t)
    return (
      Y3.info(`[ssh-websocket-v0] selected app-server transport`, {
        safe: { hostId: e.hostConfig.id },
      }),
      new qy({ ...t, getConnectTimeoutSeconds: e.getSshConnectTimeoutSeconds })
    );
  if (e.transportKind === `remote-control`)
    return new vv({
      hostConfig: e.hostConfig,
      appServerClient: e.desktopAuthAppServerClient,
      deviceKeyClient: e.remoteControlDeviceKeyClient,
      desktopApiOptions: e.desktopApiOptions,
      globalState: e.globalState,
    });
  if (n.io(e.hostConfig))
    return new yb({
      hostConfig: e.hostConfig,
      repoRoot: e.repoRoot,
      resourcesPath: e.resourcesPath,
      defaultOriginator: e.defaultOriginator,
    });
  let r = t6(e.hostConfig);
  return r
    ? (e.desktopAuthAppServerClient,
      new n.Fn({
        hostConfig: e.hostConfig,
        websocketUrl: r,
        getWebsocketProtocols: void 0,
      }))
    : new n.Nn({
        hostConfig: e.hostConfig,
        repoRoot: e.repoRoot,
        resourcesPath: e.resourcesPath,
        defaultOriginator: e.defaultOriginator,
      });
}
function t6(e) {
  return process.env.CODEX_APP_SERVER_FORCE_CLI === `1` ? null : n.In(e);
}
function n6({
  app: e,
  globalState: t,
  settingsStore: r,
  windowServices: i,
  repoRoot: a,
  errorReporter: o,
  desktopSentry: s,
  datadogLogger: c,
  performanceLogger: l,
  maxLogLevel: u,
  buildFlavor: d,
  sparkleManager: f,
  avatarOverlayManager: p,
  hotkeyWindowLifecycleManager: m,
  hasActiveComputerUseTurn: h,
  globalDictationHotkeyController: g,
  appshotHotkeysService: _,
  gitManager: v,
  requestComputerUseCaptureWorker: y,
  requestGitWorker: b,
  requestOpenInWorker: x,
  refreshApplicationMenu: S,
  subscribeComputerUseCaptureWorkerEvent: C,
  subscribeGitWorkerEvent: w,
  gitWorkerInvocationSampler: T,
  updateWorkerSentryUser: E,
  appEvent: D,
  onCompletedThreadsChanged: O,
  onAppServerSqliteRecovery: k,
}) {
  let A = new n_(e),
    j = new t_({ platform: process.platform, onCompletedThreadsChanged: O }),
    M,
    N = new r_({
      inboxItemsChanged: (e) => {
        i.sendMessageToAllWindows({ type: e });
      },
    }),
    P = n.Vr(),
    F = P == null ? null : (e, t) => new n.i(P, e, t),
    I = () => {};
  return (
    ft(t, sd.default_workspaces),
    (M = new Q3({
      hostProcess: A,
      appGlobalState: t,
      settingsStore: r,
      windowManager: i.windowManager,
      globalState: t,
      repoRoot: a,
      errorReporter: o,
      desktopSentry: s,
      desktopNotificationManager: j,
      updateWorkerSentryUser: E,
      gitManager: v,
      inboxManager: N,
      datadogLogger: c,
      performanceLogger: l,
      maxLogLevel: u,
      buildFlavor: d,
      sparkleManager: f,
      gitWorkerInvocationSampler: T,
      desktopOriginator: mu,
      devApiBaseUrl: pu,
      prodApiBaseUrl: fu,
      messageChannel: B,
      avatarOverlayManager: p,
      hotkeyWindowLifecycleManager: m,
      hasActiveComputerUseTurn: h,
      globalDictationHotkeyController: g,
      refreshApplicationMenu: S,
      updateDockIcon: i.updateDockIcon,
      appshotHotkeysService: _,
      quickChatController: i.quickChatController,
      requestComputerUseCaptureWorker: y,
      requestGitWorker: b,
      requestOpenInWorker: x,
      subscribeComputerUseCaptureWorkerEvent: C,
      subscribeGitWorkerEvent: w,
      onAppServerSqliteRecovery: k,
      appEvent: D,
      createThreadCatalogStore: F,
    })),
    M.getMessageHandler().setOnBrowserSidebarStateChanged(I),
    M.appServerConnectionRegistry
      .getConnection(V)
      .registerInternalNotificationHandler((e) => {
        if (e.method === `account/login/completed`) {
          i.windowManager.showPrimaryWindow({ stealFocus: !0 });
          return;
        }
        e.method === `turn/completed` && D.emit(`turnComplete`);
      }),
    i.setWindowContext(M),
    {
      desktopNotificationManager: j,
      getWindowContext: () => M,
      getElectronMessageHandlerForWindow: (e) => {
        let t = i.getContextForWebContents(e.webContents);
        return t ? t.getMessageHandler() : null;
      },
      getAppServerClientForHost: ({ hostId: e }) => {
        try {
          return M.getAppServerClient(e);
        } catch {
          return null;
        }
      },
      getWorktreeCleanupInputsForHost: async ({ hostKey: n, threadIds: i }) => {
        let a = M.appServerConnectionRegistry.getMaybeConnection(n);
        return {
          threadMetadataById: a
            ? await di(a, M.getAppServerClient(n), i)
            : Object.fromEntries(
                i.map((e) => [
                  e,
                  {
                    updatedAtMs: Date.now(),
                    isInProgress: !0,
                    cwd: null,
                    readFailed: !0,
                  },
                ]),
              ),
          pinnedThreadIds: fi(t),
          protectPreMigrationOwnerlessWorktrees: vi(t),
          autoCleanupEnabled: _i(r, t, e.isPackaged),
          keepCount: hi(r),
          worktreesRoot: gi(r),
        };
      },
      setOnBrowserSidebarStateChanged: (e) => {
        ((I = e), M.getMessageHandler().setOnBrowserSidebarStateChanged(I));
      },
      flushAndDisposeContext: () => {
        (M.dispose(), M.flushTelemetry());
      },
    }
  );
}
function r6(e) {
  return (
    c.ipcMain.handle(eu, async (t, n) => {
      if (!e(t)) return;
      let r = c.BrowserWindow.fromWebContents(t.sender),
        i = c.Menu.getApplicationMenu()?.getMenuItemById(n.menuId)?.submenu;
      if (i)
        return new Promise((e) => {
          i.popup({
            window: r ?? void 0,
            x: Math.round(n.x),
            y: Math.round(n.y),
            callback: e,
          });
        });
    }),
    () => c.ipcMain.removeHandler(eu)
  );
}
var i6 = 16;
function a6(e) {
  let { width: t, height: n } = e.getSize();
  return !t || !n || (t <= i6 && n <= i6)
    ? e
    : e.resize({ width: i6, height: i6, quality: `best` });
}
function o6(e, t) {
  if (e)
    try {
      if (e.startsWith(`data:`)) return a6(c.nativeImage.createFromDataURL(e));
      if (e.startsWith(`file://`))
        return a6(c.nativeImage.createFromPath((0, s.fileURLToPath)(e)));
      if (e.startsWith(`/`)) return a6(c.nativeImage.createFromPath(e));
      for (let n of t) {
        let t = new URL(e, `file://${n.endsWith(`/`) ? n : `${n}/`}`).pathname;
        if ((0, p.existsSync)(t)) {
          let e = c.nativeImage.createFromPath(t);
          if (!e.isEmpty()) return a6(e);
        }
      }
    } catch {}
}
function s6(e, t) {
  return (
    c.ipcMain.handle($l, async (n, r) => {
      if (!t(n)) return { id: null };
      let i = c.BrowserWindow.fromWebContents(n.sender);
      return new Promise((t) => {
        let n = !1,
          a = (e) => {
            n || ((n = !0), t({ id: e }));
          },
          o = (t) =>
            t.map((t) =>
              t.type === `separator`
                ? { type: `separator` }
                : {
                    id: t.id,
                    label: t.label,
                    role: t.role,
                    enabled: t.enabled ?? !0,
                    icon: o6(t.icon, e),
                    toolTip: t.toolTip,
                    click: () => a(t.id),
                    submenu: t.submenu ? o(t.submenu) : void 0,
                  },
            );
        c.Menu.buildFromTemplate(o(r)).popup({
          window: i ?? void 0,
          callback: () => a(null),
        });
      });
    }),
    () => c.ipcMain.removeHandler($l)
  );
}
function c6(e, t, n) {
  return new a.N(new l6(e), t, n).getRemoteMain();
}
var l6 = class {
    constructor(e) {
      ((this.#e = e),
        e.start(),
        e.on(`message`, (e) => {
          this.#i ||
            (e.data == null
              ? this.#a(Error(`Peer closed MessagePort connection.`))
              : typeof e.data == `string`
                ? this.#t
                  ? (this.#t(e.data), (this.#t = void 0), (this.#n = void 0))
                  : this.#r.push(e.data)
                : this.#a(
                    TypeError(`Received non-string message from MessagePort.`),
                  ));
        }),
        e.on(`close`, () => {
          this.#a(Error(`MessagePort message error.`));
        }));
    }
    #e;
    #t;
    #n;
    #r = [];
    #i;
    async send(e) {
      if (this.#i) throw this.#i;
      this.#e.postMessage(e);
    }
    async receive() {
      if (this.#r.length > 0) {
        let e = this.#r.shift();
        if (e != null) return e;
      }
      if (this.#i) throw this.#i;
      return new Promise((e, t) => {
        ((this.#t = e), (this.#n = t));
      });
    }
    abort(e) {
      try {
        this.#e.postMessage(null);
      } catch {}
      (this.#e.close(), (this.#i ||= e));
    }
    #a(e) {
      this.#i ||
        ((this.#i = e), (this.#n &&= (this.#n(e), (this.#t = void 0), void 0)));
    }
  },
  u6 = 3,
  d6 = 35;
function f6(e) {
  return e.type === `tray-menu-threads-changed`;
}
var p6 = class {
  nativeTrayClickSuppressionReason = null;
  clearNativeTrayClickSuppressionTimeout = null;
  chronicleTrayIconRefreshInterval = null;
  chronicleTrayIconState = `default`;
  isNativeTrayMenuOpen = !1;
  trayMenuThreads = {
    runningThreads: [],
    unreadThreads: [],
    pinnedThreads: [],
    recentThreads: [],
    usageLimits: [],
  };
  constructor(e, t, n, r, i, a, o, s, c, l) {
    if (
      ((this.tray = e),
      (this.onTrayButtonClick = t),
      (this.onTrayMenuOpenMainWindow = n),
      (this.onTrayMenuOpenNewThread = r),
      (this.onTrayMenuOpenRecentThread = i),
      (this.getChronicleSidecarControlState = a),
      (this.toggleChronicleSidecar = o),
      (this.trayIcons = s),
      (this.nativeIntl = c),
      (this.appName = l),
      process.platform === `darwin`)
    ) {
      (this.tray.on(`mouse-down`, () => {
        if (
          (this.clearPendingNativeTrayClickSuppression(),
          this.isNativeTrayMenuOpen)
        ) {
          this.nativeTrayClickSuppressionReason = `close`;
          return;
        }
        ((this.nativeTrayClickSuppressionReason = `open`),
          this.openNativeTrayMenu());
      }),
        this.tray.on(`click`, () => {
          this.consumeIgnoredNativeTrayClick() || this.openNativeTrayMenu();
        }),
        this.tray.on(`right-click`, () => {
          this.consumeIgnoredNativeTrayClick() || this.openNativeTrayMenu();
        }),
        this.getChronicleSidecarControlState != null &&
          this.trayIcons.chronicleRunningIcon != null &&
          (this.updateChronicleTrayIcon(),
          (this.chronicleTrayIconRefreshInterval = setInterval(() => {
            this.updateChronicleTrayIcon();
          }, 1e3)),
          this.chronicleTrayIconRefreshInterval.unref?.()));
      return;
    }
    (this.tray.on(`click`, () => {
      this.onTrayButtonClick();
    }),
      this.tray.on(`right-click`, () => {
        this.openNativeTrayMenu();
      }));
  }
  destroy() {
    (this.chronicleTrayIconRefreshInterval != null &&
      (clearInterval(this.chronicleTrayIconRefreshInterval),
      (this.chronicleTrayIconRefreshInterval = null)),
      this.tray.destroy());
  }
  async handleMessage(e) {
    switch (e.type) {
      case `tray-menu-threads-changed`:
        this.trayMenuThreads = e.trayMenuThreads;
        return;
    }
  }
  consumeIgnoredNativeTrayClick() {
    return this.nativeTrayClickSuppressionReason == null
      ? !1
      : (this.clearNativeTrayClickSuppression(), !0);
  }
  openNativeTrayMenu() {
    this.updateChronicleTrayIcon();
    let e = c.Menu.buildFromTemplate(this.getNativeTrayMenuItems());
    (e.once(`menu-will-show`, () => {
      this.isNativeTrayMenuOpen = !0;
    }),
      e.once(`menu-will-close`, () => {
        ((this.isNativeTrayMenuOpen = !1), this.handleNativeTrayMenuClosed());
      }),
      this.tray.popUpContextMenu(e));
  }
  handleNativeTrayMenuClosed() {
    if (this.nativeTrayClickSuppressionReason !== `close`) {
      this.clearNativeTrayClickSuppression();
      return;
    }
    (this.clearPendingNativeTrayClickSuppression(),
      (this.clearNativeTrayClickSuppressionTimeout = setTimeout(() => {
        this.nativeTrayClickSuppressionReason === `close` &&
          this.clearNativeTrayClickSuppression();
      }, 0)),
      this.clearNativeTrayClickSuppressionTimeout.unref?.());
  }
  clearPendingNativeTrayClickSuppression() {
    this.clearNativeTrayClickSuppressionTimeout != null &&
      (clearTimeout(this.clearNativeTrayClickSuppressionTimeout),
      (this.clearNativeTrayClickSuppressionTimeout = null));
  }
  clearNativeTrayClickSuppression() {
    (this.clearPendingNativeTrayClickSuppression(),
      (this.nativeTrayClickSuppressionReason = null));
  }
  getNativeTrayMenuItems() {
    let {
        pinnedThreads: e,
        recentThreads: t,
        runningThreads: n,
        unreadThreads: r,
        usageLimits: i,
      } = this.trayMenuThreads,
      a = this.nativeIntl.formatMessage({ messageId: $u, defaultMessage: ed }),
      o = this.nativeIntl.formatMessage({ messageId: Zu, defaultMessage: Qu }),
      s = g6({
        label: this.nativeIntl.formatMessage({
          messageId: Uu,
          defaultMessage: Wu,
        }),
        moreLabel: o,
        threads: n,
        projectlessLabel: a,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      l = g6({
        label: this.nativeIntl.formatMessage({
          messageId: qu,
          defaultMessage: Ju,
        }),
        moreLabel: o,
        threads: r,
        projectlessLabel: a,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      u = g6({
        label: this.nativeIntl.formatMessage({
          messageId: Vu,
          defaultMessage: Hu,
        }),
        moreLabel: o,
        threads: e,
        projectlessLabel: a,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      d = g6({
        label: this.nativeIntl.formatMessage({
          messageId: Gu,
          defaultMessage: Ku,
        }),
        moreLabel: o,
        threads: t,
        projectlessLabel: a,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      f =
        process.platform !== `darwin` || i.length === 0
          ? []
          : [
              {
                label: this.nativeIntl.formatMessage({
                  messageId: Yu,
                  defaultMessage: Xu,
                }),
                enabled: !1,
              },
              ...i.map(({ label: e }) => ({ label: e, enabled: !1 })),
            ],
      p = this.getChronicleSidecarControlState?.(),
      m =
        p?.enabled === !0
          ? {
              label: m6(p),
              enabled: h6(p),
              click: async (e) => {
                if (this.toggleChronicleSidecar == null) return;
                e.enabled = !1;
                let t = null;
                try {
                  ((t = await this.toggleChronicleSidecar()),
                    this.updateChronicleTrayIcon(t),
                    (e.label = m6(t)));
                } finally {
                  e.enabled = t == null ? !0 : h6(t);
                }
              },
            }
          : null,
      h = [s, l, u, d, f]
        .filter((e) => e.length > 0)
        .flatMap((e, t) => (t === 0 ? e : [{ type: `separator` }, ...e]));
    return [
      ...h,
      ...(h.length > 0 ? [{ type: `separator` }] : []),
      {
        label: this.nativeIntl.formatMessage({
          messageId: zu,
          defaultMessage: Bu,
        }),
        click: () => {
          this.onTrayMenuOpenNewThread();
        },
      },
      ...(m == null ? [] : [m]),
      { type: `separator` },
      {
        label: this.nativeIntl.formatMessage({
          messageId: Lu,
          defaultMessage: Ru,
          values: { appName: this.appName },
        }),
        click: () => {
          this.onTrayMenuOpenMainWindow();
        },
      },
      { type: `separator` },
      {
        label: v6(this.appName),
        click: () => {
          c.app.quit();
        },
      },
    ];
  }
  updateChronicleTrayIcon(e = this.getChronicleSidecarControlState?.()) {
    if (this.trayIcons.chronicleRunningIcon == null) return;
    let t = e?.running === !0 ? `chronicle-running` : `default`;
    t !== this.chronicleTrayIconState &&
      ((this.chronicleTrayIconState = t),
      this.tray.setImage(
        t === `chronicle-running`
          ? this.trayIcons.chronicleRunningIcon
          : this.trayIcons.defaultIcon,
      ));
  }
};
function m6(e) {
  switch (e.state) {
    case `disabled`:
    case `stopped`:
      return `Resume Chronicle`;
    case `running`:
    case `restarting`:
      return `Pause Chronicle`;
    case `starting`:
      return `Starting Chronicle...`;
    case `stopping`:
      return `Stopping Chronicle...`;
  }
}
function h6(e) {
  return e.state !== `starting` && e.state !== `stopping`;
}
function g6({
  label: e,
  moreLabel: t,
  threads: n,
  projectlessLabel: r,
  onOpenThread: i,
}) {
  if (n.length === 0) return [];
  let a = n.slice(0, u6),
    o = n.slice(u6);
  return [
    { label: e, enabled: !1 },
    ...a.map((e) => _6({ thread: e, projectlessLabel: r, onOpenThread: i })),
    ...(o.length > 0
      ? [
          {
            label: t,
            submenu: o.map((e) =>
              _6({ thread: e, projectlessLabel: r, onOpenThread: i }),
            ),
          },
        ]
      : []),
  ];
}
function _6({ thread: e, projectlessLabel: t, onOpenThread: n }) {
  let r = Array.from(e.title);
  return {
    label:
      r.length <= d6
        ? e.title
        : `${r
            .slice(0, d6 - 1)
            .join(``)
            .trimEnd()}…`,
    sublabel: e.isProjectless ? t : e.projectLabel,
    click: () => {
      n(e.path);
    },
  };
}
function v6(e) {
  let t = c.Menu.buildFromTemplate([{ role: `quit` }]);
  return (Array.isArray(t) ? t : t.items)[0]?.label ?? `Quit ${e}`;
}
var y6 = `desktop.quitConfirmation.title`,
  b6 = `Quit {appName}?`,
  x6 = `desktop.quitConfirmation.activeLocalTasksDetail`,
  S6 = `Active local tasks on this machine will be interrupted`,
  C6 = `desktop.quitConfirmation.scheduledTasksDetail`,
  w6 = `Scheduled tasks won't run while {appName} is closed`,
  T6 = `desktop.quitConfirmation.activeLocalAndScheduledTasksDetail`,
  E6 = `Active local tasks on this machine will be interrupted and scheduled tasks won't run while {appName} is closed`,
  D6 = n.gl(`mode`, [
    n.xl({ mode: n.vl(`create`) }),
    n.xl({ mode: n.vl(`edit`), commentId: n.wl() }),
    n.xl({ mode: n.vl(`design`), groupId: n.wl() }),
  ]),
  O6 = n.xl({ x: n.bl(), y: n.bl(), width: n.bl(), height: n.bl() }),
  k6 = n.xl({ x: n.bl(), y: n.bl() }),
  A6 = n.xl({
    provider: n.vl(`google-docs`),
    kind: n.vl(`google-docs`),
    documentId: n.wl(),
    tabId: n.wl().optional(),
    documentTitle: n.wl().optional(),
    selectedText: n.wl().optional(),
    visibleText: n.wl().optional(),
  }),
  j6 = n.xl({
    pageUrl: n.wl(),
    frameUrl: n.wl().nullable(),
    title: n.wl(),
    elementPath: n.wl(),
    point: n.xl({ xPercent: n.bl(), y: n.bl() }),
    rect: O6,
    isFixed: n.ml(),
    framePath: n.pl(n.wl()),
    documentContext: A6.optional(),
    scrollContainers: n
      .pl(n.xl({ selector: n.wl(), scrollLeft: n.bl(), scrollTop: n.bl() }))
      .optional(),
  }),
  M6 = n.gl(`kind`, [
    j6.extend({
      kind: n.vl(`element`),
      role: n.wl().nullable(),
      name: n.wl().nullable(),
      selector: n.wl().nullable(),
      nearbyText: n.wl().nullable(),
    }),
    j6.extend({
      kind: n.vl(`region`),
      role: n.fl(),
      name: n.fl(),
      selector: n.fl(),
      nearbyText: n.fl(),
    }),
    j6.extend({
      kind: n.vl(`text`),
      role: n.fl(),
      name: n.fl(),
      selector: n.fl(),
      nearbyText: n.fl(),
      selectedText: n.wl().max(n.bc),
      selectionRects: n.pl(O6).max(n.xc),
      textLocator: n.gl(`kind`, [
        n.xl({
          kind: n.vl(`dom`),
          direction: n.dl([`backward`, `forward`]),
          rangeText: n.wl().max(n.bc),
          selector: n.wl().nullable(),
          shadowHosts: n.pl(n.wl()),
          startOffset: n.bl(),
          endOffset: n.bl(),
        }),
        n.xl({
          kind: n.vl(`form-control`),
          direction: n.dl([`backward`, `forward`]),
          selector: n.wl(),
          shadowHosts: n.pl(n.wl()),
          startOffset: n.bl(),
          endOffset: n.bl(),
        }),
      ]),
    }),
  ]),
  N6 = n.xl({
    anchor: M6,
    cardViewportRect: O6.optional(),
    viewportRect: O6.nullable(),
    viewportPoint: k6,
    viewportSize: n.xl({ width: n.bl(), height: n.bl() }).optional(),
  }),
  P6 = n.xl({
    property: n.wl(),
    value: n.wl(),
    previousValue: n.wl(),
    placeholderValue: n.wl().optional(),
  }),
  F6 = n.xl({ value: n.wl(), previousValue: n.wl() }),
  I6 = n.xl({
    id: n.wl(),
    anchor: M6,
    declarations: n.pl(P6),
    draftAttribute: n.wl(),
    markerViewportPoint: k6.optional(),
    provenance: n.pl(
      n.xl({
        property: n.wl(),
        selectorText: n.wl().optional(),
        sourceUrl: n.wl().optional(),
        sourceLine: n.bl().optional(),
        sourceColumn: n.bl().optional(),
      }),
    ),
    selector: n.wl().nullable(),
    targetLabel: n.wl(),
    text: F6.optional(),
    viewportSize: n.xl({ width: n.bl(), height: n.bl() }).optional(),
  }),
  L6 = I6.extend({
    createdAt: n.wl(),
    updatedAt: n.wl(),
    status: n.Tl([n.vl(`current`), n.vl(`queued`)]),
  }),
  R6 = n.gl(`type`, [
    n.xl({
      type: n.vl(`browser-sidebar-runtime-close-comment-preview`),
      commentId: n.wl(),
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-comment-screenshot-ready`),
      commentId: n.wl(),
      annotationViewportRect: O6.nullable().optional(),
      markerViewportPoint: k6.optional(),
      skipScreenshotCapture: n.ml().optional(),
    }),
    n.xl({ type: n.vl(`browser-sidebar-runtime-exit-comment-mode`) }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-design-modifier-state`),
      pressed: n.ml(),
    }),
    n.xl({ type: n.vl(`browser-sidebar-runtime-focus-editor`) }),
    n.xl({ type: n.vl(`browser-sidebar-runtime-image-drag-ended`) }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-image-drag-started`),
      sourceUrl: n.wl(),
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-mouse-navigation`),
      direction: n.Tl([n.vl(`back`), n.vl(`forward`)]),
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-open-comment-preview`),
      commentId: n.wl(),
      anchorState: N6,
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-open-editor`),
      target: D6,
      anchorState: N6,
      designEditorState: I6.optional(),
      wantsScreenshot: n.ml().optional(),
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-open-design-editor`),
      anchorState: N6,
      designEditorState: I6,
      designChange: L6.optional(),
    }),
    n.xl({
      type: n.vl(`browser-sidebar-runtime-update-anchor`),
      target: D6,
      anchorState: N6.nullable(),
    }),
  ]);
function z6() {
  let e = !1,
    t = null,
    n = !1;
  return {
    allowQuitTemporarilyForUpdateInstall: (r) => {
      ((e = r?.allowWithoutPrompt ?? !0),
        (n = r?.skipDrainBeforeQuit ?? !1),
        t && clearTimeout(t),
        (t = setTimeout(() => {
          ((e = !1), (n = !1), (t = null));
        }, 6e4)),
        t.unref());
    },
    canQuitWithoutPrompt: () => e,
    markQuitApproved: () => {
      e = !0;
    },
    shouldSkipDrainBeforeQuit: () => n,
  };
}
function B6({
  hotkeyWindowLifecycleManager: e,
  globalDictationLifecycleManager: t,
  flushAndDisposeContext: n,
  disposables: r,
}) {
  (e.dispose(), t.dispose(), n(), r.dispose());
}
function V6(e) {
  let {
    desktopSentry: t,
    hotkeyWindowLifecycleManager: i,
    globalDictationLifecycleManager: o,
    codexHome: s,
    globalState: l,
    nativeContextMenuIconSearchRoots: u,
    getContextForWebContents: d,
    getPrimaryWindow: f,
    showPrimaryWindow: p,
    sendMessageToWindow: m,
    ensureWindow: h,
    createFreshWindow: g,
    setDesktopFeatureAvailability: _,
    setAvatarOverlayFeedbackDiagnosticsEnabled: v,
    setAutomaticBackgroundDownloadsEnabled: y,
    restoreAvatarOverlayOpenState: b,
    getAvatarOverlayCompositionSurfaceHost: x,
    reloadBundledPlugins: S,
    setMacMenuBarEnabled: C,
    navigateToRoute: w,
    setRemoteHostedPIPActiveThreadID: T,
    setRemoteHostedPIPHiddenThreadIDs: E,
    setRemoteHostedPIPHostLayout: D,
    getDockMenuController: O,
    getTrayController: k,
    isTrustedIpcEvent: A,
  } = e;
  (s6(u, A), r6(A));
  let j = !1;
  (c.ipcMain.handle(Yl, async (e, t) => {
    if (!A(e)) return;
    if (t.type === `electron-avatar-overlay-restore-ready`) {
      j || ((j = !0), await b(e.sender));
      return;
    }
    if (t.type === `remote-hosted-pip-active-thread-changed`) {
      T?.(e.sender, t.conversationId);
      return;
    }
    if (t.type === `remote-hosted-pip-hidden-thread-ids-changed`) {
      E?.(t.hiddenThreadIds);
      return;
    }
    if (t.type === `remote-hosted-pip-host-layout-changed`) {
      D?.(e.sender, t.layout);
      return;
    }
    if (t.type === `electron-avatar-overlay-feedback-diagnostics-changed`) {
      v(t.enabled);
      return;
    }
    if (t.type === `electron-sparkle-gates-changed`) {
      (y(!t.disableSparkleAutodownload), await a.f(l, t.useInternalUpdateCdn));
      return;
    }
    if (t.type === `electron-desktop-features-changed`) {
      (d(e.sender)?.setChronicleConfig(t.codexChronicleConfig),
        _(
          {
            ambientSuggestions: t.ambientSuggestions,
            appshotsEnabled: t.appshotsEnabled,
            cuaPIP: t.cuaPIP,
            inAppBrowserUse: t.inAppBrowserUse,
            inAppBrowserUseAllowed: t.inAppBrowserUseAllowed,
            defaultLinkOpenTargetPreference: t.defaultLinkOpenTargetPreference,
            linksDefaultInAppBrowser: t.linksDefaultInAppBrowser,
            localBackend: t.localBackend,
            browserPane: t.browserPane,
            externalBrowserUse: t.externalBrowserUse,
            externalBrowserUseAllowed: t.externalBrowserUseAllowed,
            findShortcuts: t.findShortcuts,
            computerUse: t.computerUse,
            computerUseNodeRepl: t.computerUseNodeRepl,
            recordAndReplay: t.recordAndReplay,
            sites: t.sites,
            control: t.control,
            dil: t.dil,
            multiBrowserTabs: t.multiBrowserTabs,
            unifiedBrowserSkill: t.unifiedBrowserSkill,
            multiWindow: t.multiWindow,
            openAIMcpFormElicitations: t.openAIMcpFormElicitations,
            processManager: t.processManager,
            quickChat: t.quickChat,
            remoteControlClientScopedEnvironments:
              t.remoteControlClientScopedEnvironments,
            webMcp: t.webMcp,
            deepResearch: t.deepResearch,
            visualize: t.visualize,
          },
          t.bundledPluginEligibilityReasons,
        ));
      return;
    }
    if (t.type === `mac-menu-bar-enabled-changed`) {
      await C(t.enabled);
      return;
    }
    if (t.type === `reload-bundled-plugins`) {
      await S?.();
      return;
    }
    if (t.type === `open-browser-in-main-window`) {
      if (!Ke().browserPane || !n.Ca(t.path)) return;
      i.hide();
      let r =
          d(e.sender)
            ?.getBrowserSidebarManager()
            .getPreferredWindowStateForConversation(
              t.browserPanel.conversationId,
            )?.window ?? f(),
        a = r != null;
      if (((r ??= await g(t.path)), r == null)) return;
      (r.isMinimized() && r.restore(),
        r.show(),
        r.focus(),
        m(r, t.browserPanel),
        a && w(r, t.path));
      return;
    }
    if (
      t.type === `open-current-main-window` ||
      t.type === `open-in-main-window`
    ) {
      let e = t.type === `open-current-main-window` ? null : t.path;
      if (e != null && !n.Ca(e)) return;
      i.hide();
      let r = f(),
        a = r != null || e == null ? await h() : await g(e);
      a &&
        (t.type === `open-current-main-window` && t.stealFocus === !0
          ? p({ focusComposer: t.focusComposer, stealFocus: !0 })
          : (a.isMinimized() && a.restore(), a.show(), a.focus()),
        r != null && e != null && w(a, e));
      return;
    }
    if (t.type === `open-in-new-window`) {
      if (!n.Ca(t.path)) return;
      i.hide();
      let e = await g(t.path);
      e && (e.isMinimized() && e.restore(), e.show(), e.focus());
      return;
    }
    if (t.type === `show-settings`) {
      i.hide();
      let e = await h();
      if (!e) return;
      (e.isMinimized() && e.restore(),
        e.show(),
        e.focus(),
        w(e, `/settings/${t.section}`));
      return;
    }
    if (t.type === `quit-app`) {
      c.app.quit();
      return;
    }
    if (f6(t)) {
      await Promise.all([k()?.handleMessage(t), O?.()?.handleMessage(t)]);
      return;
    }
    if (await o.handleMessage(e, t)) return;
    let s = d(e.sender);
    if (!s) {
      r.r().warning(`Message received for unknown window context`);
      return;
    }
    await s.handleMessage(e.sender, t);
  }),
    c.ipcMain.handle(Xl, async (e, t) => {
      let n = R6.safeParse(t);
      if (!n.success) {
        r.r().warning(`Invalid browser sidebar runtime message received`);
        return;
      }
      let i = c.BrowserWindow.fromWebContents(e.sender);
      if (i == null) {
        r.r().warning(
          `Browser sidebar runtime message received without owner window`,
        );
        return;
      }
      let a = d(i.webContents);
      if (a == null) {
        r.r().warning(
          `Browser sidebar runtime message received for unknown window context`,
        );
        return;
      }
      await a.handleMessage(e.sender, n.data);
    }),
    c.ipcMain.handle(ou, async (e, t) =>
      A(e) ? n.jt({ codexHome: s, params: t }) : null,
    ),
    c.ipcMain.handle(cu, (e) => {
      if (!A(e)) return;
      let n = Error(`Desktop Sentry test error (debug button)`);
      t.captureException(n, { tags: { trigger: `debug-button` } });
    }),
    c.ipcMain.on(lu, (e) => {
      if (!A(e)) return;
      let [t] = e.ports,
        n = d(e.sender),
        i = n?.createAppHost(e.sender),
        a = c6(t, i);
      n?.registerAppView(e.sender, a).catch((e) => {
        r.r().warning(`Failed to register AppView RPC services`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }),
    c.ipcMain.on(n.Ys, (e) => {
      if (!A(e)) return;
      let [t] = e.ports,
        n = x?.(e.sender);
      t != null && n != null && c6(t, n);
    }));
}
function H6({
  buildFlavor: e,
  getContextForWebContents: t,
  isTrustedIpcEvent: n,
  usesOwlAppShell: r,
}) {
  (c.ipcMain.on(tu, (e) => {
    if (!n(e)) {
      e.returnValue = null;
      return;
    }
    e.returnValue = { ...a.O, appVersion: F_() };
  }),
    c.ipcMain.on(nu, (t) => {
      if (!n(t)) {
        t.returnValue = null;
        return;
      }
      t.returnValue = e;
    }),
    c.ipcMain.on(ru, (e) => {
      if (!n(e)) {
        e.returnValue = !1;
        return;
      }
      e.returnValue = r;
    }),
    c.ipcMain.on(au, (e) => {
      if (!n(e)) {
        e.returnValue = {};
        return;
      }
      e.returnValue = t(e.sender)?.getSharedObjectSnapshot() ?? {};
    }),
    c.ipcMain.on(uu, (e, r) => {
      if (!n(e)) {
        e.returnValue = !1;
        return;
      }
      e.returnValue = t(e.sender)?.startFileDrag(e.sender, r) ?? !1;
    }));
}
function U6({
  isWindows: e,
  disableQuitConfirmationPrompt: t,
  quitState: r,
  windows: i,
  applicationMenuManager: o,
  ensureWindow: s,
  hotkeyWindowLifecycleManager: l,
  globalDictationLifecycleManager: u,
  globalState: d,
  settingsStore: f,
  flushAndDisposeContext: p,
  disposables: m,
  appEvent: h,
}) {
  let g = !1,
    _ = !1;
  (c.app.on(`before-quit`, (o) => {
    if (Ka()) {
      o.preventDefault();
      return;
    }
    let s = kb(),
      l = n.Cr().some((e) => e.status === `ACTIVE`);
    if (e || r.canQuitWithoutPrompt() || t || (!s && !l)) {
      ((_ = !0), i.markAppQuitting());
      return;
    }
    let u = c.app.getName(),
      d = a.H(),
      f = d.formatMessage({
        messageId: y6,
        defaultMessage: b6,
        values: { appName: u },
      });
    if (
      c.dialog.showMessageBoxSync({
        type: `warning`,
        buttons: [
          d.formatMessage({
            messageId: `desktop.quitConfirmation.quit`,
            defaultMessage: `Quit`,
          }),
          d.formatMessage({
            messageId: `desktop.quitConfirmation.cancel`,
            defaultMessage: `Cancel`,
          }),
        ],
        defaultId: 0,
        cancelId: 1,
        noLink: !0,
        title: f,
        message: f,
        detail: W6({
          nativeIntl: d,
          appName: u,
          hasInProgressLocalConversation: s,
          hasEnabledAutomations: l,
        }),
      }) !== 0
    ) {
      o.preventDefault();
      return;
    }
    (r.markQuitApproved(), (_ = !0), i.markAppQuitting());
  }),
    c.app.on(`activate`, () => {
      _ || (i.showLastActivePrimaryWindow() || s(), o.refresh());
    }),
    c.app.on(`browser-window-blur`, () => {
      c.BrowserWindow.getFocusedWindow() ?? h.emit(`background`);
    }),
    c.app.on(`browser-window-focus`, () => {
      (h.emit(`foreground`), o.refresh());
    }),
    c.app.on(`will-quit`, (e) => {
      if (((_ = !0), !g)) {
        if (r.shouldSkipDrainBeforeQuit()) {
          B6({
            hotkeyWindowLifecycleManager: l,
            globalDictationLifecycleManager: u,
            flushAndDisposeContext: p,
            disposables: m,
          });
          return;
        }
        (e.preventDefault(),
          (g = !0),
          l.dispose(),
          u.dispose(),
          Promise.all([d.flush(), f.flush()]).finally(() => {
            (p(), m.dispose(), c.app.quit());
          }));
      }
    }));
}
function W6({
  nativeIntl: e,
  appName: t,
  hasInProgressLocalConversation: n,
  hasEnabledAutomations: r,
}) {
  return n && r
    ? e.formatMessage({
        messageId: T6,
        defaultMessage: E6,
        values: { appName: t },
      })
    : n
      ? e.formatMessage({ messageId: x6, defaultMessage: S6 })
      : e.formatMessage({
          messageId: C6,
          defaultMessage: w6,
          values: { appName: t },
        });
}
var G6 = `codex.aboutDialog.title`,
  K6 = `About {appName}`,
  q6 = `codex.aboutDialog.ok`,
  J6 = `codex.aboutDialog.versionLine`,
  Y6 = `Version {version}`,
  X6 = `codex.aboutDialog.versionLineWithDate`,
  Z6 = `Version {version} • Released {releaseDate}`,
  Q6 = `codex.aboutDialog.buildInfoLabel`,
  $6 = `Build information`,
  e8 = 380,
  t8 = 360,
  n8 = 72,
  r8 = `      --background: #202020;
      --border: #3a3a3a;
      --button-background: #2d2d2d;
      --button-background-hover: #383838;
      --button-border: #6b6b6b;
      --button-text: #f5f5f5;
      --footer-background: #252525;
      --muted-text: #c8c8c8;
      --selection: #005a9e;
      --text: #f5f5f5;`,
  i8 = `      --background: #ffffff;
      --border: #d8d8d8;
      --button-background: #f3f3f3;
      --button-background-hover: #e9e9e9;
      --button-border: #8a8a8a;
      --button-text: #1f1f1f;
      --footer-background: #f5f5f5;
      --muted-text: #4f4f4f;
      --selection: #0078d4;
      --text: #1f1f1f;`,
  a8 = `    .footer {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      min-height: 56px;
      padding: 12px 14px 14px;
      border-top: 1px solid var(--border);
      background: var(--footer-background);
    }

    button {
      min-width: 76px;
      height: 28px;
      padding: 0 18px;
      border: 1px solid var(--button-border);
      border-radius: 2px;
      background: var(--button-background);
      color: var(--button-text);
      font: inherit;
      user-select: none;
      -webkit-user-select: none;
    }

    button:hover {
      background: var(--button-background-hover);
    }

    button:focus-visible {
      outline: 2px solid var(--selection);
      outline-offset: 1px;
    }`,
  o8 = null;
async function s8({ buildFlavor: e, parent: t }) {
  if (o8 != null && !o8.isDestroyed()) {
    (o8.isMinimized() && o8.restore(), o8.show(), o8.focus());
    return;
  }
  let r = c.app.getName(),
    o = c.app.getVersion(),
    s = c8(o),
    l = n.Ma(e),
    u = l == null ? o : `${o} • ${l}`,
    d = process.platform === `darwin`,
    f = a.H(),
    p = await p8(e),
    m = f.formatMessage({
      messageId: G6,
      defaultMessage: K6,
      values: { appName: r },
    }),
    h = d ? null : f.formatMessage({ messageId: q6, defaultMessage: `OK` }),
    g =
      s == null
        ? f.formatMessage({
            messageId: J6,
            defaultMessage: Y6,
            values: { version: u },
          })
        : f
            .formatMessage({
              messageId: X6,
              defaultMessage: Z6,
              values: { version: u, releaseDate: s },
            })
            .replace(
              /\s*•\s*(?=[^•]*$)/u,
              `

`,
            ),
    _ = f.formatMessage({ messageId: Q6, defaultMessage: $6 }),
    v = l8(o),
    y = [...(i.o() ? [`Powered by Codex & OWL`] : []), g, ...v].join(`
`),
    b = t != null && !t.isDestroyed() ? t : null,
    x = c.nativeTheme.shouldUseDarkColors,
    S = new c.BrowserWindow({
      width: e8,
      height: t8,
      useContentSize: !0,
      title: m,
      show: !1,
      resizable: !1,
      minimizable: !1,
      maximizable: !1,
      fullscreenable: !1,
      autoHideMenuBar: !0,
      backgroundColor: x ? `#202020` : `#ffffff`,
      ...(p.windowIcon.isEmpty() ? {} : { icon: p.windowIcon }),
      ...(b == null ? {} : { parent: b, ...(d ? {} : { modal: !0 }) }),
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        sandbox: !0,
        spellcheck: !1,
        devTools: !1,
      },
    });
  ((o8 = S),
    S.setMenuBarVisibility(!1),
    process.platform !== `darwin` && S.removeMenu(),
    d8(S, b),
    S.once(`closed`, () => {
      o8 === S && (o8 = null);
    }),
    S.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    S.webContents.on(`will-navigate`, (e) => {
      e.preventDefault();
    }));
  try {
    if (
      (await S.loadURL(
        `data:text/html;charset=utf-8,${encodeURIComponent(m8({ appDisplayName: r, buildInfoLabel: _, buildInfoText: y, iconDataUrl: p.htmlIconDataUrl, isDark: x, okLabel: h, title: m }))}`,
      ),
      S.isDestroyed())
    )
      return;
    (await u8(S), d8(S, b), S.show(), S.focus());
  } catch {
    (o8 === S && (o8 = null), S.isDestroyed() || S.destroy());
  }
}
function c8(e) {
  let t = n.Pc(e);
  return t == null
    ? null
    : new Intl.DateTimeFormat(void 0, { dateStyle: `medium` }).format(t);
}
function l8(e) {
  return [];
}
async function u8(e) {
  let t = await e.webContents.executeJavaScript(
      `Math.ceil(document.querySelector(".dialog")?.getBoundingClientRect().height ?? document.documentElement.scrollHeight)`,
      !0,
    ),
    n = typeof t == `number` && Number.isFinite(t) ? t : t8;
  e.setContentSize(e8, n, !1);
}
function d8(e, t) {
  if (t == null || t.isDestroyed()) {
    e.center();
    return;
  }
  let n = t.getBounds(),
    r = e.getBounds(),
    i = c.screen.getDisplayMatching(n).workArea,
    a = f8(
      Math.round(n.x + (n.width - r.width) / 2),
      i.x,
      i.x + Math.max(0, i.width - r.width),
    ),
    o = f8(
      Math.round(n.y + (n.height - r.height) / 2),
      i.y,
      i.y + Math.max(0, i.height - r.height),
    );
  e.setPosition(a, o, !1);
}
function f8(e, t, n) {
  return Math.min(Math.max(e, t), n);
}
async function p8(e) {
  let t = process.platform === `darwin`,
    n = process.platform === `win32`,
    r = process.execPath;
  t
    ? (r = a.s())
    : n &&
      (r = (0, u.join)(
        c.app.isPackaged
          ? process.resourcesPath
          : (0, u.join)(c.app.getAppPath(), `src`, `icons`),
        `${C_(e, `win32`, a.U())}.ico`,
      ));
  let [i, o] = await Promise.all([
    t ? GD(r) : null,
    n
      ? c.nativeImage.createFromPath(r)
      : c.app.getFileIcon(r, { size: `normal` }),
  ]);
  return {
    htmlIconDataUrl:
      i ??
      (o.isEmpty()
        ? null
        : o.resize({ width: n8, height: n8, quality: `best` }).toDataURL()),
    windowIcon: o,
  };
}
function m8({
  appDisplayName: e,
  buildInfoLabel: t,
  buildInfoText: n,
  iconDataUrl: r,
  isDark: i,
  okLabel: a,
  title: o,
}) {
  let s =
      r == null
        ? ``
        : `<img class="app-icon" src="${(0, bH.default)(r)}" alt="">`,
    c =
      a == null
        ? ``
        : `    <footer class="footer">
      <button id="ok" type="button" autofocus>${(0, bH.default)(a)}</button>
    </footer>`,
    l =
      a == null
        ? ``
        : `
    document.getElementById("ok").addEventListener("click", closeDialog);`;
  return `<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'">
  <title>${(0, bH.default)(o)}</title>
  <style>
    :root {
      color-scheme: ${i ? `dark` : `light`};
${i ? r8 : i8}
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      width: 100vw;
      overflow: hidden;
      background: var(--background);
      color: var(--text);
      font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 12px;
    }

    ::selection {
      background: var(--selection);
      color: #ffffff;
    }

    .dialog {
      display: flex;
      flex-direction: column;
    }

    .content {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: ${a == null ? `32px` : `32px 32px 22px`};
      text-align: center;
    }

    .app-icon {
      width: ${n8}px;
      height: ${n8}px;
      object-fit: contain;
      margin-bottom: 14px;
      user-select: none;
      -webkit-user-drag: none;
    }

    .app-name {
      margin-bottom: 18px;
      font-size: 17px;
      line-height: 1.25;
      font-weight: 600;
    }

    .build-info {
      width: 100%;
      margin: 0;
      line-height: 1.45;
      color: var(--muted-text);
      white-space: pre-wrap;
      overflow-wrap: anywhere;
      border: 0;
      background: transparent;
      font: inherit;
    }

    .app-name,
    .build-info,
    .copyright {
      cursor: text;
      user-select: text;
      -webkit-user-select: text;
    }

    .copyright {
      margin-top: 14px;
      color: var(--muted-text);
    }

${a == null ? `` : a8}
  </style>
</head>
<body>
  <main class="dialog">
    <section class="content" aria-labelledby="app-name">
      ${s}
      <div class="app-name" id="app-name">${(0, bH.default)(e)}</div>
      <pre class="build-info" aria-label="${(0, bH.default)(t)}">${(0, bH.default)(n)}</pre>
      <div class="copyright">© OpenAI</div>
    </section>
${c}
  </main>
  <script>
    const closeDialog = () => window.close();
${l}
    window.addEventListener("keydown", (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        closeDialog();
      }
    });
  <\/script>
</body>
</html>`;
}
var h8 = r.a(`sparkle`),
  g8 = `/`,
  _8 = `/settings/general-settings`;
function v8({
  buildFlavor: e,
  isMacOS: t,
  allowDevtools: r,
  allowDebugMenu: i,
  allowWindowReload: o,
  enableSparkle: s,
  desktopFeatureAvailability: l,
  sparkleManager: u,
  ensurePrimaryWindowVisible: d,
  createFreshWindow: f,
  queueCodexDeepLinkUrl: p,
  navigateToRoute: m,
  collapseHotkeyWindowToHome: h,
  getElectronMessageHandlerForWindow: g,
  windowManager: _,
  traceRecordingUploadOptions: v,
}) {
  let y = l.findShortcuts === !0,
    b = t
      ? [`Command+F`, `Command+G`, `Command+Shift+G`]
      : [`Ctrl+F`, `Ctrl+G`, `Shift+F3`],
    x = new Set(b.map((e) => y8(e, t))),
    S = n.qt(),
    C = (e) => n.Qt({ commandId: e, keymapState: S, isMacOS: t }),
    w = (e) => !e.includes(` `) && !n.tn(e),
    T = (e) => C(e).filter((e) => w(e) && !(y && x.has(y8(e, t)))),
    E = async () => {
      let e = c.BrowserWindow.getFocusedWindow();
      return e && !e.isDestroyed() ? e : d();
    },
    D = (e, r) => {
      let i = n.en({ commandId: e });
      if (i == null) throw Error(`Electron menu command '${e}' is not defined`);
      return {
        label: a.H().formatMessage({
          messageId: i.menuTitleIntlId,
          defaultMessage: i.menuTitle,
        }),
        accelerator: n.Gt({ commandId: e, isMacOS: t }).filter(w)[r ?? 0],
      };
    },
    O = new Set(
      n.nn
        .filter(
          (e) =>
            `electron` in e &&
            e.electron != null &&
            `isOverridableByBrowserWebpage` in e.electron &&
            e.electron.isOverridableByBrowserWebpage === !0,
        )
        .map(({ id: e }) => e),
    ),
    k = new Set();
  for (let e of n.nn) if (!O.has(e.id)) for (let n of T(e.id)) k.add(y8(n, t));
  let A = (e) => e.filter((e) => w(e) && !k.has(y8(e, t))),
    j = (e) => A(C(e)),
    M = ({ accelerators: e, click: t, enabled: n, label: r }) =>
      e.map((e) => ({
        label: r,
        accelerator: e,
        acceleratorWorksWhenHidden: !0,
        enabled: n,
        visible: !1,
        click: t,
      })),
    N = {
      ...D(`settings`),
      click: async () => {
        let e = await d();
        e && m(e, _8);
      },
    },
    P = {
      ...D(`newTask`),
      click: async () => {
        let e = await E();
        if (e) {
          if (_.isHotkeyWindowThread(e)) {
            await h?.();
            return;
          }
          (e.webContents.focus(),
            _.sendMessageToWindow(e, { type: `run-command`, id: `newTask` }));
        }
      },
    },
    F = {
      ...D(`newTask`, 1),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: P.click,
    },
    I = {
      ...D(`newProjectlessTask`),
      click: async () => {
        let e = await d();
        e &&
          _.sendMessageToWindow(e, {
            type: `run-command`,
            id: `newProjectlessTask`,
          });
      },
    },
    ee = {
      ...D(`newWindow`),
      click: async () => {
        let e = await f(g8);
        e && (e.isMinimized() && e.restore(), e.show(), e.focus());
      },
    },
    L = D(`closeWindow`),
    R = (e) => {
      if (
        e != null &&
        n.Gt({ commandId: `closeTab`, isMacOS: t }).includes(e)
      ) {
        let e = c.webContents.getFocusedWebContents(),
          t = c.BrowserWindow.getFocusedWindow();
        if (t != null && !t.isDestroyed()) {
          let e = Ve(t);
          if (e != null) {
            _.sendMessageToWindow(t, e);
            return;
          }
        }
        if (e != null) {
          let n =
            t != null && !t.isDestroyed()
              ? [t]
              : c.BrowserWindow.getAllWindows();
          for (let t of n)
            if (!t.isDestroyed() && ze(t)?.closeFocusedVisibleBrowserTab(t, e))
              return;
        }
        if (t != null && !t.isDestroyed()) {
          let e = QI(_.getAppShellShortcutState(t.webContents.id));
          if (e != null) {
            _.sendMessageToWindow(t, e);
            return;
          }
        }
      }
      let r = c.BrowserWindow.getFocusedWindow();
      r == null || r.isDestroyed() || r.close();
    },
    z = {
      ...L,
      click: () => {
        R(L.accelerator);
      },
    },
    te = D(`closeWindow`, 1),
    ne = {
      ...te,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: () => {
        R(te.accelerator);
      },
    },
    re = {
      ...D(`openFolder`),
      click: async () => {
        let e = await E();
        if (!e) return;
        let t = g(e);
        t && (await t.addWorkspaceRootOption(e.webContents));
      },
    },
    ie = {
      label: `Log Out`,
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `log-out` });
      },
    },
    ae = async (e) => {
      let t = await E();
      t && _.sendMessageToWindow(t, { type: `command-menu`, query: e });
    },
    oe = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `file-search-command-menu` });
    },
    se = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `chat-search-command-menu` });
    },
    ce = async (e, { allowAppShellTabShortcut: t = !1 } = {}) => {
      let n = await E();
      if (!n) return;
      let r = e === `focusBrowserAddressBar` || (t && qe(e)) ? Ge(n) : null;
      _.sendMessageToWindow(n, {
        type: `run-command`,
        id: e,
        ...(t ? { allowAppShellTabShortcut: t } : {}),
        originPanel: r ?? void 0,
      });
    },
    le = async () => {
      await ce(`feedback`);
    },
    ue = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `copy-conversation-path` });
    },
    de = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `toggle-thread-pin` });
    },
    fe = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `rename-thread` });
    },
    pe = async () => {
      let e = await E();
      e &&
        _.sendMessageToWindow(e, {
          type: `archive-thread`,
          source: `native_accelerator`,
        });
    },
    me = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `copy-working-directory` });
    },
    he = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `copy-session-id` });
    },
    ge = async () => {
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `copy-deeplink` });
    },
    _e = {
      ...D(`openCommandMenu`, 1),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await ae(``);
      },
    },
    ve = {
      ...D(`openCommandMenu`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await ae();
      },
    },
    ye = {
      ...D(`searchFiles`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: oe,
    },
    be = D(`searchChats`),
    xe = {
      ...be,
      accelerator: y ? T(`searchChats`)[0] : be.accelerator,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: se,
    },
    Se = {
      ...D(`openProcessManager`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await ce(`openProcessManager`);
      },
    },
    Ce = n.Xt.map((e) => ({
      ...D(e),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await ce(e);
      },
    })),
    we = {
      ...D(`copyConversationPath`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: ue,
    },
    Te = {
      ...D(`toggleThreadPin`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: de,
    },
    Ee = {
      ...D(`renameThread`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: fe,
    },
    De = {
      ...D(`archiveThread`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: pe,
    },
    Oe = {
      ...D(`copyWorkingDirectory`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: me,
    },
    ke = {
      ...D(`copySessionId`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: he,
    },
    Ae = {
      ...D(`copyDeeplink`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: ge,
    },
    je = {
      label: a.H().formatMessage({
        messageId: G6,
        defaultMessage: K6,
        values: { appName: c.app.getName() },
      }),
      click: (t, n) => {
        s8({
          buildFlavor: e,
          parent: n instanceof c.BrowserWindow && !n.isDestroyed() ? n : null,
        });
      },
    },
    Me = {
      ...D(`toggleSidebar`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-sidebar` });
      },
    },
    Ne = {
      ...D(`toggleBottomPanel`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-bottom-panel` });
      },
    },
    Pe = {
      ...D(`togglePinnedSummary`),
      click: () => {
        ce(`togglePinnedSummary`);
      },
    },
    Fe = {
      ...D(`toggleTerminal`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-terminal` });
      },
    },
    Ie = {
      ...D(`toggleFileTreePanel`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-file-tree-panel` });
      },
    },
    Le = {
      ...D(`toggleBrowserPanel`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        let e = await E();
        e &&
          _.sendMessageToWindow(e, {
            type: `toggle-browser-panel`,
            source: `manual`,
            initiator: `app_menu`,
          });
      },
    },
    Re = {
      ...D(`openBrowserTab`),
      click: async () => {
        let e = await E();
        e &&
          _.sendMessageToWindow(e, {
            type: `open-browser-tab`,
            source: `manual`,
            initiator: `app_menu`,
          });
      },
    },
    ze = (e) => g(e)?.getBrowserSidebarManager?.(),
    Be = (e) => {
      let t = c.webContents.getFocusedWebContents();
      if (t == null) return !1;
      let n = c.BrowserWindow.getFocusedWindow();
      return (
        n != null && !n.isDestroyed() ? [n] : c.BrowserWindow.getAllWindows()
      ).some(
        (n) =>
          !n.isDestroyed() &&
          ze(n)?.runFocusedVisiblePageCommand(n, t, e) === !0,
      );
    },
    Ve = (e) => {
      let t = _.getAppShellShortcutState(e.webContents.id);
      return t?.focusArea === `main` ? null : QI(t);
    },
    He = async () => {
      await ce(`focusBrowserAddressBar`);
    },
    Ue = { ...D(`focusBrowserAddressBar`), accelerator: void 0, click: He },
    We = [
      Ue,
      ...M({
        accelerators: j(`focusBrowserAddressBar`),
        click: () => {
          Be({ type: `focus-address-bar` });
        },
        label: Ue.label ?? `Focus Browser Address Bar`,
      }),
    ],
    Ge = (e) => {
      let t = _.getAppShellShortcutState(e.webContents.id);
      if (t?.focusArea === `bottom-panel` && t.bottomPanelBrowserTabId == null)
        return null;
      let n =
        ze(e)?.getFocusedVisibleBrowserTabPanel(
          e,
          c.webContents.getFocusedWebContents(),
        ) ?? null;
      if (n != null) return n;
      switch (t?.focusArea) {
        case `bottom-panel`:
          return `bottom`;
        case `right-panel`:
          return `right`;
        case `main`:
        case void 0:
          break;
      }
      return $I(t);
    },
    Ke = (e) =>
      e === `nextRecentThread` || e === `nextThread`
        ? `nextTab`
        : e === `previousRecentThread` || e === `previousThread`
          ? `previousTab`
          : null,
    qe = (e) => Ke(e) != null,
    Je = (e, r) => {
      let i = Ke(e);
      return i != null && n.Gt({ commandId: i, isMacOS: t }).includes(r);
    },
    Ye = (e, t, n) => n?.triggeredByAccelerator === !0 && t != null && Je(e, t),
    Xe = c.BrowserWindow.getFocusedWindow(),
    Ze = c.webContents.getFocusedWebContents(),
    Qe =
      Xe != null &&
      !Xe.isDestroyed() &&
      !!ze(Xe)?.canReloadActiveVisiblePage(Xe, Ze),
    $e = async (e = !1) => {
      let t = await E();
      if (!t) return;
      let n = ze(t);
      if (n == null) return;
      let r = c.webContents.getFocusedWebContents();
      if (e) {
        n.reloadActiveVisiblePageWithOptions(t, { ignoreCache: !0 }, r);
        return;
      }
      n.reloadActiveVisiblePage(t, r);
    },
    et = {
      ...D(`reloadBrowserPage`),
      accelerator: j(`reloadBrowserPage`)[0],
      enabled: Qe,
      click: async () => {
        await $e();
      },
    },
    tt = [
      et,
      ...M({
        accelerators: j(`reloadBrowserPage`).slice(1),
        click: () => $e(),
        enabled: Qe,
        label: et.label ?? `Reload Browser Page`,
      }),
    ],
    nt = {
      ...D(`hardReloadBrowserPage`),
      accelerator: j(`hardReloadBrowserPage`)[0],
      acceleratorWorksWhenHidden: !0,
      enabled: Qe,
      visible: !1,
      click: async () => {
        await $e(!0);
      },
    },
    rt = [
      nt,
      ...M({
        accelerators: j(`hardReloadBrowserPage`).slice(1),
        click: () => $e(!0),
        enabled: Qe,
        label: nt.label ?? `Force Reload Browser Page`,
      }),
    ],
    it = {
      label: `Reload Window`,
      accelerator: `Command+Control+R`,
      click: async () => {
        let e = await E();
        e && e.reload();
      },
    },
    at = {
      ...D(`toggleSidePanel`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-diff-panel` });
      },
    },
    ot = h$({
      allowDevtools: r,
      accelerator: D(`toggleTraceRecording`).accelerator,
      windowManager: _,
      traceRecordingUploadOptions: v,
    }),
    st = async () => {
      if (Be({ type: `open-find` })) return;
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `find-in-thread` });
    },
    ct = {
      ...D(`findInThread`),
      accelerator: y ? b[0] : j(`findInThread`)[0],
      click: st,
    },
    lt = [
      ct,
      ...M({
        accelerators: y ? [] : j(`findInThread`).slice(1),
        click: st,
        label: ct.label ?? `Find`,
      }),
    ],
    ut = {
      label: `Find Next`,
      accelerator: t ? `Command+G` : `Ctrl+G`,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        if (Be({ type: `find-next` })) return;
        let e = await E();
        e != null && _.sendMessageToWindow(e, { type: `find-next-in-thread` });
      },
    },
    dt = {
      label: `Find Previous`,
      accelerator: t ? `Command+Shift+G` : `Shift+F3`,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        if (Be({ type: `find-previous` })) return;
        let e = await E();
        e != null &&
          _.sendMessageToWindow(e, { type: `find-previous-in-thread` });
      },
    },
    ft = D(`previousThread`),
    pt = {
      ...ft,
      click: async (e, t, n) => {
        await ce(`previousThread`, {
          allowAppShellTabShortcut: Ye(`previousThread`, ft.accelerator, n),
        });
      },
    },
    mt = D(`previousThread`, 1),
    ht = {
      ...mt,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async (e, t, n) => {
        await ce(`previousThread`, {
          allowAppShellTabShortcut: Ye(`previousThread`, mt.accelerator, n),
        });
      },
    },
    gt = D(`nextThread`),
    _t = {
      ...gt,
      click: async (e, t, n) => {
        await ce(`nextThread`, {
          allowAppShellTabShortcut: Ye(`nextThread`, gt.accelerator, n),
        });
      },
    },
    vt = D(`nextThread`, 1),
    yt = {
      ...vt,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async (e, t, n) => {
        await ce(`nextThread`, {
          allowAppShellTabShortcut: Ye(`nextThread`, vt.accelerator, n),
        });
      },
    },
    bt = {
      label: `Toggle Debug Menu`,
      accelerator: `Alt+D`,
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-debug-modal` });
      },
    },
    xt = {
      label: `Open Deeplink from Clipboard`,
      click: () => {
        p(c.clipboard.readText().trim()) ||
          c.dialog.showMessageBox({
            type: `info`,
            title: `Invalid Deeplink`,
            message: `Clipboard does not contain a valid codex:// deeplink.`,
            detail: `Copy a codex:// URL to the clipboard and try again.`,
          });
      },
    },
    St = {
      label: `Toggle Query Devtools`,
      accelerator: `CmdOrCtrl+Alt+Y`,
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-query-devtools` });
      },
    },
    Ct = {
      label: `Toggle React Scan`,
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `toggle-react-scan` });
      },
    },
    wt = async (e) => {
      if (Be({ type: `step-zoom`, delta: e })) return;
      let t = await E();
      t && _.sendMessageToWindow(t, { type: `step-window-zoom`, delta: e });
    },
    Tt = async () => {
      if (Be({ type: `reset-zoom` })) return;
      let e = await E();
      e && _.sendMessageToWindow(e, { type: `reset-window-zoom` });
    },
    Et = () => {
      let e = c.BrowserWindow.getFocusedWindow();
      !e || e.isDestroyed() || e.setFullScreen(!e.isFullScreen());
    },
    Dt = {
      ...D(`navigateBack`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `navigate-back` });
      },
    },
    Ot = {
      ...D(`navigateForward`),
      click: async () => {
        let e = await E();
        e && _.sendMessageToWindow(e, { type: `navigate-forward` });
      },
    },
    kt = [
      je,
      { type: `separator` },
      N,
      ...(s
        ? [
            {
              label: `Check for Updates…`,
              enabled: !0,
              click: () => {
                if (
                  (h8().info(`Check for updates requested via menu.`),
                  !u.hasUpdater())
                ) {
                  let e = u.getUnavailableReason() ?? `unknown`;
                  (h8().warning(
                    `Desktop updater unavailable; init likely skipped.`,
                    { safe: { reason: e }, sensitive: {} },
                  ),
                    c.dialog.showMessageBox({
                      type: `info`,
                      title: `Updates Unavailable`,
                      message: `Automatic updates are unavailable right now.`,
                      detail: `Updater initialization skipped: ${e}`,
                    }));
                  return;
                }
                u.checkForUpdates();
              },
            },
          ]
        : []),
      ie,
      { type: `separator` },
      { role: `services` },
      { type: `separator` },
      { role: `hide` },
      { role: `hideOthers` },
      { role: `unhide` },
      { type: `separator` },
      { role: `quit` },
    ],
    At = { label: `File`, id: n.To.file, submenu: [] },
    jt = { role: `appMenu`, submenu: kt },
    Mt = { label: `View`, id: n.To.view, submenu: [] },
    Nt = { role: `windowMenu`, id: n.To.window },
    Pt = [
      ...M({
        accelerators: j(`navigateBrowserBack`),
        click: () => {
          Be({ type: `go-back` });
        },
        label: `Browser Back`,
      }),
      ...M({
        accelerators: j(`navigateBrowserForward`),
        click: () => {
          Be({ type: `go-forward` });
        },
        label: `Browser Forward`,
      }),
    ],
    Ft = [
      Me,
      Ne,
      Pe,
      Fe,
      Ie,
      ...(l.browserPane ? [Le] : []),
      at,
      ...(l.browserPane
        ? [{ type: `separator` }, Re, ...We, ...tt, ...rt, ...Pt]
        : []),
      { type: `separator` },
      ...lt,
      ...(y ? [ut, dt] : []),
      { type: `separator` },
      pt,
      ht,
      _t,
      yt,
      Dt,
      Ot,
      ...(o ? [it] : []),
    ],
    It = [
      ...(i ? [bt, xt] : []),
      ...(r ? [{ role: `toggleDevTools` }] : []),
      ...[],
      ...(r && n.qs.supportsReactScan(e) ? [Ct, ...[]] : []),
      ...(n.Ea({ buildFlavor: e, isDev: !1 }) ? [St] : []),
    ],
    Lt = [
      {
        label: `Zoom In`,
        accelerator: `CmdOrCtrl+Plus`,
        click: async () => {
          await wt(1);
        },
      },
      ...(t
        ? []
        : [
            {
              label: `Zoom In`,
              accelerator: `Ctrl+=`,
              acceleratorWorksWhenHidden: !0,
              visible: !1,
              click: async () => {
                await wt(1);
              },
            },
          ]),
      {
        label: `Zoom Out`,
        accelerator: `CmdOrCtrl+-`,
        click: async () => {
          await wt(-1);
        },
      },
      {
        label: `Actual Size`,
        accelerator: `CmdOrCtrl+0`,
        click: async () => {
          await Tt();
        },
      },
      ...M({
        accelerators: A([`CmdOrCtrl+numadd`]),
        click: () => {
          Be({ type: `step-zoom`, delta: 1 });
        },
        label: `Zoom In`,
      }),
      ...M({
        accelerators: A([`CmdOrCtrl+numsub`]),
        click: () => {
          Be({ type: `step-zoom`, delta: -1 });
        },
        label: `Zoom Out`,
      }),
      ...M({
        accelerators: A([`CmdOrCtrl+num0`]),
        click: () => {
          Be({ type: `reset-zoom` });
        },
        label: `Actual Size`,
      }),
    ];
  (Ft.push({ type: `separator` }, ...Lt),
    It.length > 0 && Ft.push({ type: `separator` }, ...It),
    t
      ? Ft.push(
          { type: `separator` },
          {
            label: `Toggle Full Screen`,
            accelerator: `Command+Control+F`,
            visible: !1,
            acceleratorWorksWhenHidden: !0,
            click: Et,
          },
        )
      : Ft.push(
          { type: `separator` },
          { label: `Toggle Full Screen`, accelerator: `F11`, click: Et },
        ),
    Ft.push(
      _e,
      ve,
      xe,
      ye,
      ...(l.processManager ? [Se] : []),
      ...Ce,
      ...(t ? [we] : []),
      Te,
      Ee,
      De,
      Oe,
      ke,
      Ae,
    ),
    (Mt.submenu = Ft));
  let Rt = [],
    zt = [
      ...(t ? [jt, At] : [At]),
      { role: `editMenu`, id: n.To.edit },
      Mt,
      ...(t ? [Nt] : []),
      {
        role: `help`,
        id: n.To.help,
        submenu: [
          {
            label: `Documentation`,
            click: () => {
              c.shell.openExternal(`https://developers.openai.com/codex/app`);
            },
          },
          {
            ...D(`showKeyboardShortcuts`),
            click: async () => {
              await ce(`showKeyboardShortcuts`);
            },
          },
          {
            label: `What's New`,
            click: () => {
              c.shell.openExternal(
                `https://developers.openai.com/codex/changelog`,
              );
            },
          },
          { type: `separator` },
          {
            label: `Troubleshooting`,
            click: () => {
              c.shell.openExternal(
                `https://developers.openai.com/codex/app/troubleshooting`,
              );
            },
          },
          {
            label: a.H().formatMessage({
              messageId: `electron.appMenu.help.systemStatus`,
              defaultMessage: `System Status`,
            }),
            click: () => {
              c.shell.openExternal(`https://status.openai.com`);
            },
          },
          { label: `Send Feedback`, click: le },
          { type: `separator` },
          ot,
          ...Rt,
          ...(t ? [] : [{ type: `separator` }, je]),
        ],
      },
    ],
    Bt = c.Menu.buildFromTemplate(zt);
  if (t) {
    let e = Bt.getMenuItemById(n.To.edit)?.submenu,
      t = e?.items.find((e) => e.role?.toLowerCase() === `pasteandmatchstyle`);
    e &&
      t &&
      ((t.visible = !1),
      e.insert(
        e.items.indexOf(t),
        new c.MenuItem({
          role: `pasteAndMatchStyle`,
          accelerator: `Command+Shift+V`,
        }),
      ));
  }
  let Vt = Bt.getMenuItemById(n.To.file)?.submenu;
  if (Vt) {
    let e = 0;
    (l.multiWindow && Vt.insert(e++, new c.MenuItem(ee)),
      Vt.insert(e++, new c.MenuItem(P)),
      Vt.insert(e++, new c.MenuItem(I)),
      Vt.insert(e++, new c.MenuItem(F)),
      Vt.insert(e++, new c.MenuItem({ type: `separator` })),
      Vt.insert(e++, new c.MenuItem(re)),
      Vt.insert(e++, new c.MenuItem({ type: `separator` })),
      Vt.insert(e++, new c.MenuItem(z)),
      ne.accelerator != null && Vt.insert(e++, new c.MenuItem(ne)),
      t ||
        (Vt.insert(e, new c.MenuItem({ type: `separator` })),
        Vt.append(new c.MenuItem(N)),
        Vt.append(new c.MenuItem({ type: `separator` })),
        Vt.append(new c.MenuItem(ie)),
        Vt.append(new c.MenuItem({ role: `quit`, accelerator: `Ctrl+Q` }))));
  }
  (c.Menu.setApplicationMenu(Bt), g$(_));
}
function y8(e, t) {
  return e
    .split(`+`)
    .map((e) => {
      switch (e.toLowerCase()) {
        case `cmdorctrl`:
        case `commandorcontrol`:
          return t ? `command` : `control`;
        case `cmd`:
        case `command`:
          return `command`;
        case `control`:
        case `ctrl`:
          return `control`;
        case `option`:
          return t ? `alt` : `option`;
        case `alt`:
          return `alt`;
        case `meta`:
        case `super`:
          return t ? `command` : `super`;
        case `enter`:
        case `return`:
          return `enter`;
        case `esc`:
        case `escape`:
          return `escape`;
        case `plus`:
          return t ? `=` : `plus`;
        default:
          return e.toLowerCase();
      }
    })
    .sort()
    .join(`+`);
}
var b8 = class {
    buildMenu;
    constructor({ buildMenu: e }) {
      this.buildMenu = e;
    }
    refresh() {
      this.buildMenu();
    }
  },
  x8 = r.i(`codex-deep-link-navigation`);
async function S8({
  window: e,
  route: t,
  globalState: r,
  gitManager: i,
  appServerClient: a,
  listPlugins: o,
  windowManager: s,
  navigateToRoute: c,
}) {
  switch (t.kind) {
    case `applyCodexAppConfig`:
      return;
    case `pluginInstall`: {
      let r;
      try {
        r = await C8({ listPlugins: o, route: t });
      } catch (n) {
        (x8.warning(`plugin_install_marketplace_lookup_failed`, {
          safe: {
            marketplaceName: t.marketplaceName,
            pluginName: t.pluginName,
          },
          sensitive: { error: n },
        }),
          c(e, `/skills`, { initialTab: `plugins` }));
        return;
      }
      if (r == null) {
        c(e, `/skills`, { initialTab: `plugins` });
        return;
      }
      c(
        e,
        n.va({
          marketplacePath: r,
          pluginId: t.pluginName,
          pluginName: t.pluginName,
        }),
      );
      return;
    }
    case `petInstall`:
      (s.showPrimaryWindow({ stealFocus: !0 }),
        s.sendMessageToWindow(e, {
          type: `open-pet-install-modal`,
          name: t.name,
          description: t.description,
          imageUrl: t.imageUrl,
          spriteVersionNumber: t.spriteVersionNumber,
        }));
      return;
    case `pluginDetail`:
      c(e, n.va(t));
      return;
    case `localPluginDetail`:
      c(
        e,
        n.va({
          hostId: t.hostId ?? `local`,
          marketplacePath: t.marketplacePath,
          pluginId: t.pluginName,
          pluginName: t.pluginName,
        }),
        { pluginDetailDeepLinkMode: t.openShareDialog ? `share` : `view` },
      );
      return;
    case `automations`:
      c(e, `/automations`, { automationMode: `create` });
      return;
    case `connectorOAuthCallback`:
      (s.showPrimaryWindow({ stealFocus: !0 }),
        s.sendMessageToWindow(e, {
          type: `connector-oauth-callback`,
          fullRedirectUrl: t.fullRedirectUrl,
          returnTo: t.returnTo ?? void 0,
        }));
      return;
    case `launch`:
      return;
    case `localConversation`: {
      let r;
      (t.openReview && t.reviewDiffFilter != null
        ? (r = {
            openReview: !0,
            reviewDiffFilter: t.reviewDiffFilter,
            reviewPath: t.reviewPath,
          })
        : t.openReview && (r = { openReview: !0 }),
        c(e, n._a(t.conversationId), r));
      return;
    }
    case `newThread`: {
      let n = await w8({
        route: t,
        globalState: r,
        gitManager: i,
        appServerClient: a,
      });
      (n != null && A8({ globalState: r, workspaceRoot: n, windowManager: s }),
        c(e, `/`, {
          focusComposerNonce: Date.now(),
          prefillPrompt: t.prompt,
          prefillCwd: n ?? void 0,
        }));
      return;
    }
    case `settings`:
      c(
        e,
        `${t.section == null ? `/settings` : `/settings/${t.section}`}${t.search ?? ``}`,
      );
      return;
    case `skills`:
      c(e, `/skills`);
      return;
  }
}
async function C8({ listPlugins: e, route: t }) {
  if (e == null) return null;
  let { marketplaces: n } = await e({});
  return (
    n.find((e) =>
      e.path == null || e.name !== t.marketplaceName
        ? !1
        : e.plugins.some((e) => e.name === t.pluginName),
    )?.path ?? null
  );
}
async function w8({
  route: e,
  globalState: t,
  gitManager: n,
  appServerClient: r,
}) {
  if (e.path != null) {
    let t = await T8(e.path);
    if (t != null) return t;
  }
  return e.originUrl == null
    ? null
    : E8({
        targetOriginUrl: e.originUrl,
        globalState: t,
        gitManager: n,
        appServerClient: r,
      });
}
async function T8(e) {
  let t = (0, u.resolve)(e);
  try {
    return (await (0, m.stat)(t)).isDirectory() ? t : null;
  } catch {
    return null;
  }
}
async function E8({
  targetOriginUrl: e,
  globalState: t,
  gitManager: r,
  appServerClient: i,
}) {
  let a = D8(t);
  return a.length === 0
    ? null
    : O8({
        targetOriginUrl: e,
        workspaceRoots: a,
        origins: await n.Q(a, r, i),
      });
}
function D8(e) {
  let t = it(e);
  return t.length > 0 ? t : rt(e);
}
function O8({ targetOriginUrl: e, workspaceRoots: t, origins: r }) {
  let i = n.Fa(e),
    a = new Map();
  for (let e of r)
    e.originUrl != null &&
      (a.has(e.dir) || a.set(e.dir, e.originUrl),
      a.has(e.root) || a.set(e.root, e.originUrl));
  for (let n of t) {
    let t = a.get(n);
    if (
      t != null &&
      k8({ targetOriginUrl: e, candidateOriginUrl: t, targetRepo: i })
    )
      return n;
  }
  return null;
}
function k8({ targetOriginUrl: e, candidateOriginUrl: t, targetRepo: r }) {
  if (t === e) return !0;
  if (r == null) return !1;
  let i = n.Fa(t);
  return i == null
    ? !1
    : i.host === r.host && i.owner === r.owner && i.repo === r.repo;
}
function A8({ globalState: e, workspaceRoot: t, windowManager: n }) {
  let r = it(e);
  r.includes(t) ||
    (ct(e, [t, ...r]),
    n.sendMessageToAllWindows({ type: `workspace-root-options-updated` }));
  let i = rt(e);
  (i.length === 1 && i[0] === t) ||
    (st(e, [t]),
    n.sendMessageToAllWindows({ type: `active-workspace-roots-updated` }));
}
function j8({
  buildFlavor: e,
  globalState: t,
  errorReporter: n,
  sparkleManager: r,
  gitManager: o,
  getLocalAppServerClient: s,
  applyCodexAppConfig: l,
  getLocalAppServerConnection: u,
  windowServices: d,
  createFreshWindow: f,
  ensureWindow: p,
  getElectronMessageHandlerForWindow: m,
  allowDevtools: h,
  allowDebugMenu: g,
  enableSparkle: _,
  isMacOS: v,
  appVersion: y,
}) {
  let b = (e, t, n) => {
      d.sendMessageToWindow(e, {
        type: `navigate-to-route`,
        path: t,
        state: n,
      });
    },
    x = async () => p(),
    S = a.c({
      app: c.app,
      isMacOS: v,
      ensurePrimaryWindowVisible: x,
      navigateToRoute: async (e, n) => {
        if (n.kind === `applyCodexAppConfig`) {
          await l(e);
          return;
        }
        return S8({
          window: e,
          route: n,
          globalState: t,
          gitManager: o,
          appServerClient: s(),
          listPlugins:
            n.kind === `pluginInstall` ? (e) => u().listPlugins(e) : void 0,
          windowManager: d.windowManager,
          navigateToRoute: b,
        });
      },
      initialArgv: process.argv,
      errorReporter: n,
    }),
    C = new b8({
      buildMenu: () => {
        v8({
          buildFlavor: e,
          isMacOS: v,
          allowDevtools: h,
          allowDebugMenu: g,
          allowWindowReload: e !== i.a.Prod,
          enableSparkle: _,
          desktopFeatureAvailability: Ke(),
          sparkleManager: r,
          ensurePrimaryWindowVisible: x,
          createFreshWindow: f,
          queueCodexDeepLinkUrl: S.queueCodexDeepLinkUrl,
          navigateToRoute: b,
          collapseHotkeyWindowToHome: () =>
            d.hotkeyWindowLifecycleManager.collapseToHome(),
          getElectronMessageHandlerForWindow: m,
          windowManager: d.windowManager,
          traceRecordingUploadOptions: {
            appVersion: y,
            buildFlavor: e,
            buildNumber: a.k.value,
            uploadFeedback: (e) =>
              u().sendAppServerRequest(`feedback/upload`, e),
          },
        });
      },
    }),
    w = () => {
      C.refresh();
    };
  return {
    applicationMenuManager: C,
    deepLinks: S,
    navigateToRoute: b,
    refreshApplicationMenu: w,
    setDesktopFeatureAvailability: (e) => {
      Ye(e) && w();
    },
  };
}
var M8 = `codex.sqliteBackfillProgress.title`,
  N8 = `Rebuilding conversation history`,
  P8 = `codex.sqliteBackfillProgress.detail`,
  F8 = `This may take several minutes — keep the app open`;
async function I8({ onQuit: e }) {
  let t = a.H(),
    n = t.formatMessage({ messageId: M8, defaultMessage: N8 }),
    r = t.formatMessage({ messageId: P8, defaultMessage: F8 }),
    i = !1,
    o = !1,
    s = new c.BrowserWindow({
      width: 420,
      height: 176,
      resizable: !1,
      maximizable: !1,
      fullscreenable: !1,
      show: !1,
      title: n,
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        sandbox: !0,
        spellcheck: !1,
        devTools: !1,
      },
    });
  (s.setMenuBarVisibility(!1),
    s.on(`close`, (t) => {
      if (!(i || o)) {
        (t.preventDefault(), (o = !0));
        try {
          e();
        } finally {
          o = !1;
        }
      }
    }),
    s.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    s.webContents.on(`will-navigate`, (e) => {
      e.preventDefault();
    }));
  try {
    await s.loadURL(
      `data:text/html;charset=utf-8,${encodeURIComponent(L8(n, r))}`,
    );
  } catch (e) {
    throw ((i = !0), s.isDestroyed() || s.destroy(), e);
  }
  return (
    s.isDestroyed() || (s.center(), s.show(), s.focus()),
    () => {
      ((i = !0), s.isDestroyed() || s.destroy());
    }
  );
}
function L8(e, t) {
  let n = (0, bH.default)(e);
  return `<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    :root { color-scheme: light dark; }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      background: Canvas;
      color: CanvasText;
      font-family: system-ui, sans-serif;
    }
    main { width: 100%; padding: 28px 32px; }
    h1 { margin: 0; font-size: 17px; font-weight: 600; }
    p { margin: 8px 0 0; color: GrayText; font-size: 13px; line-height: 1.45; }
    progress { width: 100%; margin-top: 22px; }
  </style>
</head>
<body>
  <main>
    <h1>${n}</h1>
    <p>${(0, bH.default)(t)}</p>
    <progress aria-label="${n}"></progress>
  </main>
</body>
</html>`;
}
var R8 = `DoubleCommand`,
  z8 = 6e4,
  B8 = class extends a.P {
    configuredHotkey;
    registration = null;
    dispose = () => {
      (this.registration?.unregister(), (this.registration = null));
    };
    constructor(e, t, n, i) {
      (super(),
        (this.globalState = e),
        (this.settingsStore = t),
        (this.windowManager = n),
        (this.enabled = i));
      let a = e.getStored(`appshotHotkey`);
      this.configuredHotkey = a === void 0 ? R8 : a;
      try {
        this.#e();
      } catch (e) {
        r.r().warning(`Failed to register appshot hotkey`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    getState() {
      return {
        supported: this.enabled && process.platform === `darwin`,
        configuredHotkey: this.configuredHotkey,
        isActive: this.registration != null,
      };
    }
    setHotkey(e) {
      if (!this.enabled || process.platform !== `darwin`)
        return { success: !1, error: `Not supported.`, state: this.getState() };
      if (e != null) {
        let t = nA(e);
        if (t != null) return { success: !1, error: t, state: this.getState() };
      }
      let t = this.configuredHotkey;
      this.configuredHotkey = e;
      try {
        this.#e();
      } catch (e) {
        this.configuredHotkey = t;
        try {
          this.#e();
        } catch (e) {
          r.r().warning(
            `Failed to restore appshot hotkey after update failure`,
            { safe: {}, sensitive: { rollbackError: e } },
          );
        }
        return {
          success: !1,
          error: e instanceof Error ? e.message : String(e),
          state: this.getState(),
        };
      }
      return (
        this.globalState.set(`appshotHotkey`, this.configuredHotkey),
        { success: !0, state: this.getState() }
      );
    }
    #e() {
      if (
        (this.registration?.unregister(),
        (this.registration = null),
        !this.enabled ||
          process.platform !== `darwin` ||
          this.configuredHotkey == null)
      ) {
        r.r().info(`Appshot hotkey inactive`, {
          safe: {
            enabled: this.enabled,
            platform: process.platform,
            configured: this.configuredHotkey != null,
          },
          sensitive: {},
        });
        return;
      }
      if (
        (r.r().info(`Registering appshot hotkey`, {
          safe: { hotkey: this.configuredHotkey },
          sensitive: {},
        }),
        rA(this.configuredHotkey, { bareModifierTrigger: `immediatePress` }),
        (this.registration = eA(
          this.configuredHotkey,
          { onPressed: () => this.#t() },
          { bareModifierTrigger: `immediatePress` },
        )),
        this.registration == null)
      )
        throw Error(`Unable to register shortcut: ${this.configuredHotkey}`);
      r.r().info(`Registered appshot hotkey`, {
        safe: { hotkey: this.configuredHotkey },
        sensitive: {},
      });
    }
    #t() {
      r.r().info(`Appshot hotkey pressed`, {
        safe: { hotkey: this.configuredHotkey },
        sensitive: {},
      });
      let e = this.windowManager.getPrimaryWindow();
      if (e == null || e.isDestroyed()) {
        r.r().info(`Appshot hotkey skipped`, {
          safe: {
            reason:
              e == null ? `no-primary-window` : `primary-window-destroyed`,
          },
          sensitive: {},
        });
        return;
      }
      let t =
          this.settingsStore.getEffective(n.Wi.destination.key) ?? `automatic`,
        i = this.windowManager.getAppShellShortcutState(e.webContents.id),
        a = !1;
      if (i?.canAcceptAppshotShortcut === !0)
        switch (t) {
          case `automatic`:
            a = this.windowManager.wasPrimaryWindowFocusedWithin(e, z8);
            break;
          case `last-chat`:
            a = !0;
            break;
          case `new-chat`:
            a = i.isNewChatRoute === !0;
            break;
        }
      (a ||
        this.windowManager.sendMessageToWindow(e, {
          type: `navigate-to-route`,
          path: `/`,
          state: { focusComposerNonce: Date.now() },
        }),
        r.r().info(`Dispatching appshot hotkey`, {
          safe: {
            target: a ? `current-route` : `new-chat`,
            destination: t,
            windowId: e.id,
          },
          sensitive: {},
        }),
        this.windowManager.sendMessageToWindow(e, {
          type: `appshot-shortcut`,
        }));
    }
  },
  V8 = `avatar-overlay.node`,
  H8 = `avatar_overlay.node`,
  U8 = (0, T.createRequire)(__filename);
function W8(e) {
  let t = G8(e).find(p.existsSync);
  if (t == null) throw Error(`Avatar overlay native addon not found`);
  return q8(t);
}
function G8(e) {
  let t = K8(e, H8),
    n = St({ env: process.env, resourcesPath: process.resourcesPath });
  return n == null ? [t] : [u.default.join(n, `native`, V8), t];
}
function K8(e, t) {
  return u.default.join(
    e,
    `native`,
    `avatar-overlay-addon`,
    `artifacts`,
    `Release-${process.arch}`,
    t,
  );
}
function q8(e) {
  return U8(e);
}
var J8 = 200;
function Y8({ devAppPath: e, platform: t = process.platform } = {}) {
  if (t !== `darwin`) return null;
  let n = null,
    i = !1,
    a = !1,
    o = 0,
    s = [],
    l = (e, t) => {
      if (!t) return;
      o += 1;
      let n = { details: t, name: e, sequence: o, timestampMs: Date.now() };
      (s.push(n), s.length > J8 && s.shift());
    };
  return {
    animateSurface(e) {
      (l(`animate-surface`, a && { ...e, nativeAttached: n != null }),
        n?.animateCompositionSurface(e));
    },
    attachMaterial({
      activityStackDurationMs: t,
      activityStackOpacity: o,
      activityStackRect: s,
      mascotBadgeTint: u,
      mascotRect: d,
      rootWindow: f,
      surfaces: p,
    }) {
      i = !1;
      try {
        n ??= W8(e ?? c.app.getAppPath());
        let i = n.attachCompositionMaterial({
            activityStackDurationMs: t,
            activityStackOpacity: o,
            activityStackRect: s,
            mascotBadgeTint: u,
            mascotRect: d,
            root: X8(f),
            surfaces: p.map(({ cornerRadius: e, id: t, window: n }) => ({
              ...X8(n),
              cornerRadius: e,
              id: t,
            })),
          }),
          m = null;
        if (!i)
          try {
            let e = JSON.parse(n.dumpCompositionHierarchy());
            typeof e == `object` &&
              e &&
              `lastAttachmentFailureReason` in e &&
              typeof e.lastAttachmentFailureReason == `string` &&
              (m = e.lastAttachmentFailureReason);
          } catch {
            m = `diagnostics-unavailable`;
          }
        return (
          l(
            `attach-material`,
            a && {
              activityStackDurationMs: t,
              activityStackOpacity: o,
              activityStackRect: s,
              mascotBadgeTint: u,
              attached: i,
              failureReason: m,
              mascotRect: d,
              rootBounds: f.getContentBounds(),
              surfaces: p.map(({ cornerRadius: e, id: t, window: n }) => ({
                bounds: n.getContentBounds(),
                cornerRadius: e,
                id: t,
              })),
            },
          ),
          r.r().info(`Native pet material attachment completed`, {
            safe: { attached: i, failureReason: m, surfaceCount: p.length },
          }),
          i || n.detachCompositionMaterial(),
          i
        );
      } catch (e) {
        return (
          l(
            `attach-material`,
            a && {
              activityStackDurationMs: t,
              activityStackOpacity: o,
              activityStackRect: s,
              mascotBadgeTint: u,
              attached: !1,
              failure: `exception`,
              mascotRect: d,
              rootBounds: f.getContentBounds(),
              surfaceCount: p.length,
            },
          ),
          n?.detachCompositionMaterial(),
          (n = null),
          (i = !0),
          r.r().warning(`Failed to attach native pet material`, {
            safe: {},
            sensitive: { error: e },
          }),
          !1
        );
      }
    },
    consumeActivityStackScrollDeltaY() {
      return n?.consumeCompositionActivityStackScrollDeltaY() ?? 0;
    },
    consumeScreensDidWake() {
      if (i) return !1;
      try {
        return (
          (n ??= W8(e ?? c.app.getAppPath())),
          n.consumeCompositionScreensDidWake()
        );
      } catch {
        return ((i = !0), !1);
      }
    },
    consumePointerDownSurfaceId() {
      return n?.consumeCompositionPointerDownSurfaceId() ?? null;
    },
    consumePointerUpSurfaceId() {
      return n?.consumeCompositionPointerUpSurfaceId() ?? null;
    },
    detach() {
      l(`detach-material`, a && { hadAddon: n != null });
      try {
        (n?.consumeCompositionScreensDidWake(), n?.detachCompositionMaterial());
      } catch (e) {
        (l(
          `detach-material`,
          a && { failure: `exception`, hadAddon: n != null },
        ),
          r.r().warning(`Failed to detach native pet material`, {
            safe: {},
            sensitive: { error: e },
          }));
      } finally {
        ((n = null), (i = !1));
      }
    },
    getCursorPosition() {
      if (i) return null;
      try {
        return ((n ??= W8(e ?? c.app.getAppPath())), n.getCursorPosition());
      } catch {
        return ((i = !0), null);
      }
    },
    getDiagnosticEvents() {
      return [...s];
    },
    dumpHierarchy() {
      try {
        return n?.dumpCompositionHierarchy() ?? null;
      } catch (e) {
        return (
          l(`dump-hierarchy`, a && { failure: `exception` }),
          r.r().warning(`Failed to dump native pet hierarchy`, {
            safe: {},
            sensitive: { error: e },
          }),
          null
        );
      }
    },
    getPresentations() {
      return n?.getCompositionSurfacePresentations() ?? [];
    },
    getPointerSurfaceId() {
      return n?.getCompositionPointerSurfaceId() ?? null;
    },
    isAnimating() {
      return n?.isCompositionAnimating() ?? !1;
    },
    isPrimaryMouseButtonPressed() {
      if (i) return !1;
      try {
        return (
          (n ??= W8(e ?? c.app.getAppPath())),
          n.isPrimaryMouseButtonPressed()
        );
      } catch {
        return ((i = !0), !1);
      }
    },
    orderSurfaces(e) {
      (l(`order-surfaces`, a && { ids: e }), n?.orderCompositionSurfaces(e));
    },
    performWindowDrag(t) {
      if (i) return !1;
      try {
        n ??= W8(e ?? c.app.getAppPath());
        let r = n.performOverlayWindowDrag(X8(t));
        return (l(`perform-window-drag`, a && { started: r }), r);
      } catch {
        return ((i = !0), !1);
      }
    },
    presentNotificationStack(e) {
      (l(`present-notification-stack`, a && { durationMs: e }),
        n?.presentNotificationStack(e));
    },
    setFeedbackDiagnosticsEnabled(e) {
      ((a = e), e || ((s.length = 0), (o = 0)));
    },
    setNotificationStackExpanded(e) {
      (l(`set-notification-stack-expanded`, a && { isExpanded: e }),
        n?.setNotificationStackExpanded(e));
    },
    setOverlayWindowPosition(t, o) {
      if (i) return !1;
      try {
        n ??= W8(e ?? c.app.getAppPath());
        let r = n.setOverlayWindowPosition({ position: o, root: X8(t) });
        return (
          l(
            `set-overlay-window-position`,
            a && {
              position: o,
              positioned: r,
              rootBounds: t.getContentBounds(),
            },
          ),
          r
        );
      } catch (e) {
        return (
          l(
            `set-overlay-window-position`,
            a && {
              failure: `exception`,
              position: o,
              positioned: !1,
              rootBounds: t.getContentBounds(),
            },
          ),
          (i = !0),
          r.r().warning(`Failed to position native pet window`, {
            safe: {},
            sensitive: { error: e },
          }),
          !1
        );
      }
    },
    updateMaterial(e) {
      if (n == null) return !1;
      try {
        return (
          n.updateCompositionMaterial(e),
          l(`update-material`, a && { ...e, updated: !0 }),
          !0
        );
      } catch (t) {
        return (
          l(
            `update-material`,
            a && { ...e, failure: `exception`, updated: !1 },
          ),
          n.detachCompositionMaterial(),
          (n = null),
          r.r().warning(`Failed to update native pet material`, {
            safe: {},
            sensitive: { error: t },
          }),
          !1
        );
      }
    },
  };
}
function X8(e) {
  return {
    contentBounds: e.getContentBounds(),
    mediaSourceId:
      typeof e.getMediaSourceId == `function` ? e.getMediaSourceId() : null,
    nativeWindowHandle:
      typeof e.getNativeWindowHandle == `function`
        ? e.getNativeWindowHandle()
        : null,
    title: e.getTitle(),
  };
}
var Z8 = 720,
  Q8 = 72,
  $8 = 48e3,
  e5 = 0.2;
function t5({ current: e, next: t, target: n }) {
  let r = n.x - e.x,
    i = n.y - e.y,
    a = Math.hypot(r, i);
  if (a >= Z8) return { acceleration: { x: 0, y: 0 }, shouldDock: !1 };
  let o = t.x - e.x,
    s = t.y - e.y,
    c = Math.hypot(o, s);
  if (c === 0) return { acceleration: { x: 0, y: 0 }, shouldDock: a <= Q8 };
  if ((a === 0 ? 0 : (r * o + i * s) / (a * c)) < e5)
    return { acceleration: { x: 0, y: 0 }, shouldDock: !1 };
  let l = $8 * (1 - a / Z8),
    u = a === 0 ? 0 : l / a;
  return {
    acceleration: { x: r * u, y: i * u },
    shouldDock: n5(n, e, t) <= Q8,
  };
}
function n5(e, t, n) {
  let r = n.x - t.x,
    i = n.y - t.y,
    a = r * r + i * i;
  if (a === 0) return Math.hypot(e.x - t.x, e.y - t.y);
  let o = Math.min(1, Math.max(0, ((e.x - t.x) * r + (e.y - t.y) * i) / a));
  return Math.hypot(e.x - (t.x + r * o), e.y - (t.y + i * o));
}
var r5 = class {
  crossedDisplay = !1;
  hasMovementIntent = !1;
  hasMoved = !1;
  displayBounds;
  constructor(e, t, n, r, i = !1) {
    ((this.cursorSource = e),
      (this.pointerAnchorX = t),
      (this.pointerAnchorY = n),
      (this.usesOrbPhysics = i),
      (this.displayBounds = r));
  }
  getCursorPointForSource({ native: e, renderer: t }) {
    return this.cursorSource === `native` ? e : t;
  }
  recordMove(e) {
    ((this.hasMovementIntent = !0),
      (this.hasMoved = !0),
      this.updateDisplayBounds(e));
  }
  recordMovementIntent() {
    this.hasMovementIntent = !0;
  }
  updateDisplayBounds(e) {
    ((this.crossedDisplay ||= !i5(this.displayBounds, e)),
      (this.displayBounds = e));
  }
  shouldSuppressRendererThrow() {
    return (
      this.cursorSource === `native` && (!this.hasMoved || this.crossedDisplay)
    );
  }
};
function i5(e, t) {
  return (
    e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height
  );
}
var a5 = 5e3,
  o5 = 250,
  s5 = 16,
  c5 = class {
    applying = !1;
    target = null;
    timer = null;
    constructor(e) {
      this.options = e;
    }
    clear() {
      (this.timer != null && (clearTimeout(this.timer), (this.timer = null)),
        (this.target = null));
    }
    position(e, t) {
      if ((this.clear(), this.options.shouldCancelWindowMoveResync())) return;
      let n = {
        bounds: { ...t },
        retryUntilMs: Date.now() + (e.isVisible() ? o5 : a5),
        window: e,
      };
      this.target = n;
      let r = this.apply(n);
      if (!this.options.retryEnabled) {
        this.clear();
        return;
      }
      r || this.schedule(n, s5);
    }
    handleWindowMove(e) {
      let t = this.target;
      return t == null || t.window !== e
        ? !1
        : this.applying
          ? !0
          : this.options.shouldCancelWindowMoveResync()
            ? (this.clear(), !1)
            : ((t.retryUntilMs = Date.now() + o5), this.schedule(t, 0), !0);
    }
    handleWindowShow(e) {
      let t = this.target;
      t != null && t.window === e && this.position(e, t.bounds);
    }
    apply(e) {
      this.applying = !0;
      try {
        return this.options.setPosition(e.bounds);
      } finally {
        this.applying = !1;
      }
    }
    schedule(e, t) {
      if (this.target !== e) return;
      this.timer != null && clearTimeout(this.timer);
      let n = e.retryUntilMs - Date.now();
      if (n <= 0) {
        this.timer = null;
        return;
      }
      this.timer = setTimeout(
        () => {
          if (
            ((this.timer = null),
            this.target !== e ||
              e.window !== this.options.getCurrentWindow() ||
              e.window.isDestroyed())
          ) {
            this.target === e && (this.target = null);
            return;
          }
          Date.now() >= e.retryUntilMs || this.apply(e) || this.schedule(e, s5);
        },
        Math.min(t, n),
      );
    }
  },
  l5 = `/avatar-overlay`,
  u5 = 24,
  d5 = 24,
  f5 = { width: 112, height: 121 },
  p5 = 80,
  m5 = 224,
  h5 = 192 / 208,
  g5 = { width: 345, height: 131 },
  _5 = { width: 345, height: 141 },
  v5 = 0.01,
  y5 = 8,
  b5 = 16,
  x5 = 32,
  S5 = 0.88,
  C5 = 0.7,
  w5 = 65,
  T5 = 900,
  E5 = 360,
  D5 = 26,
  O5 = 220,
  k5 = 3,
  A5 = 80,
  j5 = 8,
  M5 = 0.08,
  N5 = 100,
  P5 = 1e4,
  F5 = 1e3,
  I5 = r.a(`avatar-overlay`),
  L5 = class {
    window = null;
    openingWindowPromise = null;
    anchor = R5({ x: 0, y: 0, ...of }, f5);
    displayBounds = null;
    displayId = null;
    detachedDisplay = null;
    detachedDisplayRestoreTimer = null;
    dragState = null;
    windowServerDragActive = !1;
    windowServerDragDirection = null;
    windowServerDragWindowX = null;
    layout = null;
    layoutMode = `legacy`;
    hasDeferredLayout = !1;
    mascotSize = f5;
    momentumActive = !1;
    momentumTimer = null;
    pendingPresentation = null;
    presentationOffset = { x: 0, y: 0 };
    presentationMotionTarget = null;
    presentationVisibility = null;
    startupPresentationVisibility = null;
    dockRestoreAnchor = null;
    dockTarget = null;
    orbDragFollowTarget = null;
    orbDragFollowTimer = null;
    suppressNextRendererThrow = !1;
    movedWindowPersistTimer = null;
    mousePassthroughEnabled = !1;
    placement = `top-end`;
    pointerInteractive = !1;
    rendererReady = !1;
    lastSentRendererState = null;
    mascotResizeState = null;
    ignoreMascotElementSizeUntilMs = 0;
    expectedMascotSizeAfterResize = null;
    feedbackDiagnosticsEnabled = !1;
    computerUseCursorLocation = null;
    computerUseCursorPoint = null;
    pendingElementSizeRevision = null;
    isSuspended = !1;
    isAppQuitting = !1;
    applicationHideCheckTimer = null;
    compositionTuckedForApplicationHide = !1;
    initialPresentationState = `waiting-for-layout`;
    petOpenIntent = !1;
    windowVisibilitySequence = 0;
    windowStagedForNativePresentation = !1;
    resolutionKey = null;
    traySize = null;
    compositionHost;
    nativePositionController;
    nativeCompositionSupported;
    nativeCompositionEnabled = !0;
    restrictedController;
    constructor(e, t, n = null, r = () => Ke().cuaPIP) {
      ((this.windowManager = e),
        (this.globalState = t),
        (this.nativeCompositionSupported = n != null),
        (this.compositionHost = new dp(
          e,
          n,
          () => {
            let e = this.window;
            e != null &&
              (this.initialPresentationState === `waiting-for-native` &&
                (this.initialPresentationState = `ready`),
              this.applyLayout(e),
              this.showWindowIfReady(e));
          },
          r,
        )),
        (this.nativePositionController = new c5({
          getCurrentWindow: () => this.window,
          retryEnabled: this.nativeCompositionSupported,
          setPosition: (e) => this.compositionHost.setOverlayWindowPosition(e),
          shouldCancelWindowMoveResync: () =>
            this.layoutMode === `native` &&
            this.isOverlayWindowPointerDragActive(),
        })),
        io((e) => {
          this.setComputerUseCursorLocation(e);
        }));
    }
    isOpen() {
      let e = this.window;
      return (
        e != null &&
        !e.isDestroyed() &&
        e.isVisible() &&
        !this.windowStagedForNativePresentation
      );
    }
    subscribePresentationFocus(e) {
      let t = () => {
        let t = c.BrowserWindow.getFocusedWindow();
        for (; t != null && t !== this.window; ) t = t.getParentWindow();
        e(t != null);
      };
      return (
        c.app.on(`browser-window-blur`, t),
        c.app.on(`browser-window-focus`, t),
        t(),
        () => {
          (c.app.off(`browser-window-blur`, t),
            c.app.off(`browser-window-focus`, t));
        }
      );
    }
    getVisibleWebContents() {
      return this.isOpen() ? (this.window?.webContents ?? null) : null;
    }
    preparePresentation(e, t) {
      (this.cancelMomentum(),
        this.restoreDockAnchor(),
        (this.pendingPresentation = {
          origin: e,
          target: { ...this.anchor },
          velocity: t,
        }));
      let n = this.window;
      n != null && !n.isDestroyed() && this.positionPresentationAtOrigin(n, e);
    }
    dockPresentation(e, t, n) {
      this.pendingPresentation = null;
      let r = this.window;
      if (r == null || r.isDestroyed()) {
        t();
        return;
      }
      this.dockRestoreAnchor ??= { ...this.anchor };
      let i = X5(e, this.mascotSize, this.presentationOffset);
      this.animatePresentationTo(i, n ?? Z5(this.anchor, i), t);
    }
    cancelPresentationDock() {
      let e = this.pendingPresentation;
      if (
        ((this.pendingPresentation = null),
        e != null && this.dockRestoreAnchor == null)
      ) {
        ((this.dockRestoreAnchor = e.target), this.restoreDockAnchor());
        return;
      }
      let t = this.dockRestoreAnchor;
      t != null &&
        this.animatePresentationTo(t, Z5(this.anchor, t), () => {
          this.dockRestoreAnchor === t && (this.dockRestoreAnchor = null);
        });
    }
    updatePresentationOffset(e) {
      let t = this.presentationOffset,
        n = t.x - e.x,
        r = t.y - e.y;
      if (n === 0 && r === 0) return;
      this.presentationOffset = e;
      let i = new Set([
        this.pendingPresentation?.target,
        this.dockRestoreAnchor,
        this.presentationMotionTarget,
      ]);
      for (let e of i) e != null && ((e.x += n), (e.y += r));
      let a = this.window;
      a == null ||
        a.isDestroyed() ||
        ((this.anchor = {
          ...this.anchor,
          x: this.anchor.x + n,
          y: this.anchor.y + r,
        }),
        this.applyLayout(a, c.screen.getDisplayNearestPoint(q5(this.anchor))));
    }
    setPresentationDockTarget(e, t) {
      this.dockTarget =
        e == null || t == null ? null : { anchor: e, onDock: t };
    }
    configureDevelopmentWindow({ mascotWidth: e, placement: t }) {
      let n = this.window;
      if (n == null || n.isDestroyed()) return;
      let r = this.getCurrentDisplay(),
        i = this.layoutMode === `native` ? r.workArea : r.bounds,
        a = t7(e);
      ((this.mascotSize = a),
        (this.placement = t),
        (this.anchor = {
          x: t.endsWith(`start`) ? i.x + u5 : i.x + i.width - a.width - u5,
          y: t.startsWith(`bottom`) ? i.y + u5 : i.y + i.height - a.height - u5,
          ...a,
        }),
        this.applyLayout(n, r));
    }
    getDevelopmentDiagnostics(e = `include`) {
      return {
        ...this.compositionHost.getDevelopmentDiagnostics(e),
        layout: this.layout == null ? null : V5(this.layout),
      };
    }
    getFeedbackLogEntries() {
      if (!this.feedbackDiagnosticsEnabled) return [];
      try {
        let {
            diagnosticEvents: e,
            hierarchy: t,
            ...n
          } = this.compositionHost.getDevelopmentDiagnostics(),
          r = this.window;
        return [
          {
            contents: Buffer.from(
              JSON.stringify(
                {
                  ...n,
                  anchor: this.anchor,
                  displayBounds: this.displayBounds,
                  displayId: this.displayId,
                  displays: c.screen.getAllDisplays().map((e) => ({
                    bounds: e.bounds,
                    id: e.id,
                    rotation: e.rotation,
                    scaleFactor: e.scaleFactor,
                    workArea: e.workArea,
                  })),
                  expectedMascotSizeAfterResize:
                    this.expectedMascotSizeAfterResize,
                  initialPresentationState: this.initialPresentationState,
                  isSuspended: this.isSuspended,
                  layout: this.layout,
                  layoutMode: this.layoutMode,
                  mascotSize: this.mascotSize,
                  nativeCompositionSupported: this.nativeCompositionSupported,
                  pendingElementSizeRevision: this.pendingElementSizeRevision,
                  rendererReady: this.rendererReady,
                  resolutionKey: this.resolutionKey,
                  traySize: this.traySize,
                  window:
                    r == null
                      ? null
                      : r.isDestroyed()
                        ? { destroyed: !0 }
                        : {
                            bounds: r.getBounds(),
                            contentBounds: r.getContentBounds(),
                            destroyed: !1,
                            visible: r.isVisible(),
                          },
                  windowStagedForNativePresentation:
                    this.windowStagedForNativePresentation,
                },
                null,
                2,
              ),
            ),
            name: `native-pet-state.json`,
          },
          {
            contents: Buffer.from(
              e.map((e) => JSON.stringify(e)).join(`
`),
            ),
            name: `native-pet-events.jsonl`,
          },
          ...(t == null
            ? []
            : [
                { contents: Buffer.from(t), name: `native-pet-hierarchy.json` },
              ]),
        ];
      } catch (e) {
        return (
          I5().warning(`Failed to capture native pet diagnostics`, {
            safe: {},
            sensitive: { error: e },
          }),
          []
        );
      }
    }
    setFeedbackDiagnosticsEnabled(e) {
      ((this.feedbackDiagnosticsEnabled = e),
        this.compositionHost.setFeedbackDiagnosticsEnabled(e));
    }
    reconcileRemoteHostedPIPContentHost() {
      this.compositionHost.publishRemoteHostedPIPContentHost();
    }
    async toggle(e) {
      let t = this.window;
      if (t != null && !t.isDestroyed() && this.shouldPresentWindow()) {
        this.hide();
        return;
      }
      await this.wake(e);
    }
    async wake(e = null) {
      let t = this.window;
      if (
        this.petOpenIntent &&
        t != null &&
        !t.isDestroyed() &&
        this.shouldPresentWindow()
      ) {
        this.compositionHost.publishRemoteHostedPIPContentHost();
        return;
      }
      await this.open(e, { forcePetView: !0 });
    }
    close() {
      this.hide();
    }
    hide() {
      (this.globalState.set(a.h, !1),
        (this.petOpenIntent = !1),
        this.reconcileWindowVisibility());
    }
    setPresentationVisibility(e) {
      (e == null
        ? this.updatePresentationOffset({ x: 0, y: 0 })
        : (this.startupPresentationVisibility = null),
        (this.presentationVisibility = e),
        this.reconcileWindowVisibility());
    }
    markAppQuitting() {
      ((this.isAppQuitting = !0),
        this.updatePresentationOffset({ x: 0, y: 0 }),
        (this.presentationVisibility = null),
        (this.startupPresentationVisibility = null),
        (this.windowVisibilitySequence += 1));
    }
    async open(e, { forcePetView: t = !1, persistPetIntent: n = !0 } = {}) {
      n && (this.globalState.set(a.h, !0), (this.petOpenIntent = !0));
      let r = ++this.windowVisibilitySequence,
        i = await this.ensureWindow();
      r === this.windowVisibilitySequence &&
        (this.positionWindow(i, e),
        this.pendingPresentation != null &&
          ((this.pendingPresentation.target = { ...this.anchor }),
          this.positionPresentationAtOrigin(
            i,
            this.pendingPresentation.origin,
          )),
        this.presentWindow(i),
        t &&
          this.windowManager.sendMessageToWebContents(i.webContents, {
            type: `avatar-overlay-explicit-pet-opened`,
          }));
    }
    presentWindow(e) {
      this.compositionTuckedForApplicationHide ||
        (this.nativeCompositionSupported &&
          this.compositionHost.isEnabled() &&
          (this.initialPresentationState = `waiting-for-native`),
        this.stageWindowForNativePresentation(e),
        this.compositionHost.wake(),
        this.showWindowIfReady(e));
    }
    prewarm() {
      this.ensureWindow().catch((e) => {
        I5().warning(`Failed to prewarm avatar overlay`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }
    async prepareRealtimePresentation(e, t) {
      this.presentationVisibility ?? (this.startupPresentationVisibility = t);
      let n = ++this.windowVisibilitySequence,
        r = await this.ensureWindow();
      n === this.windowVisibilitySequence &&
        ((this.presentationVisibility ?? this.startupPresentationVisibility) ===
          !0 &&
          (this.positionWindow(r, e),
          this.pendingPresentation != null &&
            ((this.pendingPresentation.target = { ...this.anchor }),
            this.positionPresentationAtOrigin(
              r,
              this.pendingPresentation.origin,
            ))),
        this.reconcileWindowVisibility());
    }
    async restoreOpenState(e) {
      this.globalState.get(`electron-avatar-overlay-open`) === !0 &&
        (await this.open(e));
    }
    handleRendererReady(e) {
      let t = this.window;
      t == null ||
        t.isDestroyed() ||
        t.webContents.id !== e ||
        ((this.rendererReady = !0),
        (this.lastSentRendererState = null),
        this.sendLayoutToRenderer(t),
        this.sendComputerUseCursorLocationToRenderer(t),
        this.stageWindowForNativePresentation(t),
        this.showWindowIfReady(t));
    }
    setPointerInteraction(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        ((this.pointerInteractive = t),
        this.movedWindowPersistTimer ?? this.applyPointerInteractivityPolicy());
    }
    setKeyboardInteraction(e, t) {
      let n = this.window;
      if (!(n == null || n.isDestroyed() || n.webContents.id !== e)) {
        if ((this.applyPointerInteractivityPolicy(), !t)) {
          n.setFocusable(!1);
          return;
        }
        (n.setFocusable(!0),
          n.show(),
          process.platform === `darwin` && c.app.focus({ steal: !0 }),
          n.focus(),
          n.webContents.focus(),
          this.windowManager.sendMessageToWebContents(n.webContents, {
            type: `avatar-overlay-keyboard-interaction-ready`,
          }));
      }
    }
    startDrag(e, t, n = !1) {
      let r = this.window;
      if (r == null || r.isDestroyed() || r.webContents.id !== e) return;
      (this.cancelMomentum(),
        (this.dockRestoreAnchor = null),
        this.cancelOrbDragFollow(),
        (this.windowServerDragActive = !1),
        (this.windowServerDragDirection = null),
        (this.windowServerDragWindowX = r.getContentBounds().x),
        (this.suppressNextRendererThrow = !1),
        this.clearDetachedDisplayRestore());
      let i = this.getLayout(r);
      this.nativePositionController.clear();
      let a = z5(this.compositionHost.getCursorPosition()),
        o =
          t.pointerScreenX != null && t.pointerScreenY != null
            ? { x: t.pointerScreenX, y: t.pointerScreenY }
            : c.screen.getCursorScreenPoint(),
        s = a ?? o,
        l = t.pointerWindowX - i.mascot.left,
        u = t.pointerWindowY - i.mascot.top;
      ((this.dragState = new r5(
        a == null ? `renderer` : `native`,
        l,
        u,
        c.screen.getDisplayNearestPoint(s).bounds,
        n,
      )),
        (this.windowServerDragActive =
          this.layoutMode === `native` &&
          !n &&
          this.compositionHost.performWindowDrag()),
        this.windowServerDragActive || (this.windowServerDragWindowX = null));
    }
    moveDrag(e, t) {
      let n = this.window;
      if (
        n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.dragState == null
      )
        return;
      this.cancelMomentum();
      let r = this.dragState;
      if ((r.recordMovementIntent(), this.windowServerDragActive)) return;
      let i = c.screen.getCursorScreenPoint(),
        a = r.getCursorPointForSource({
          native:
            r.cursorSource === `native`
              ? z5(this.compositionHost.getCursorPosition())
              : null,
          renderer: {
            x: t?.pointerScreenX ?? i.x,
            y: t?.pointerScreenY ?? i.y,
          },
        });
      a != null &&
        (r.usesOrbPhysics
          ? ((this.orbDragFollowTarget = a), this.scheduleOrbDragFollow(n))
          : this.moveDragToPointer(n, a));
    }
    endDrag(e, t) {
      let n = this.window;
      if (n == null || n.isDestroyed() || n.webContents.id !== e) return;
      this.cancelOrbDragFollow();
      let r = this.dragState,
        i = this.windowServerDragActive,
        a = null;
      if (i && r != null) {
        let e =
            z5(this.compositionHost.getCursorPosition()) ??
            (t == null
              ? c.screen.getCursorScreenPoint()
              : { x: t.pointerScreenX, y: t.pointerScreenY }),
          i = c.screen.getDisplayNearestPoint(e),
          o = {
            ...this.anchor,
            x: e.x - r.pointerAnchorX,
            y: e.y - r.pointerAnchorY,
          };
        ((a = i),
          this.updateWindowServerDragPlacement(n, n.getContentBounds(), o, i),
          this.sendWindowServerDragDirection(n, null));
      } else if (r?.hasMovementIntent) {
        let e = c.screen.getCursorScreenPoint(),
          i = r.getCursorPointForSource({
            native:
              r.cursorSource === `native`
                ? z5(this.compositionHost.getCursorPosition())
                : null,
            renderer: {
              x: t?.pointerScreenX ?? e.x,
              y: t?.pointerScreenY ?? e.y,
            },
          });
        i != null && this.moveDragToPointer(n, i);
      }
      ((this.suppressNextRendererThrow =
        r?.shouldSuppressRendererThrow() ?? !1),
        (this.dragState = null),
        (this.windowServerDragActive = !1),
        (this.windowServerDragWindowX = null),
        i
          ? this.persistWindowBounds(n, a ?? this.getCurrentDisplay())
          : this.reclampWindowToVisibleDisplay({ shouldPersist: !0 }));
      let o = this.dockTarget,
        s = J5(this.anchor, this.presentationOffset);
      o != null &&
        t5({
          current: s,
          next: s,
          target: { x: o.anchor.centerX, y: o.anchor.centerY },
        }).shouldDock &&
        this.dockPresentation(o.anchor, o.onDock);
    }
    throwWithVelocity(e, t, n, r = !1) {
      let i = this.window;
      if (
        i == null ||
        i.isDestroyed() ||
        i.webContents.id !== e ||
        !Number.isFinite(t) ||
        !Number.isFinite(n) ||
        (t === 0 && n === 0)
      )
        return;
      let a = this.suppressNextRendererThrow;
      ((this.suppressNextRendererThrow = !1),
        !(a && !r) && this.startMomentum(t, n, r));
    }
    startMomentum(e, t, n) {
      let r = this.window;
      if (r == null || r.isDestroyed()) return;
      (this.cancelMomentum(),
        this.dockTarget != null &&
          (this.dockRestoreAnchor ??= { ...this.anchor }),
        (this.momentumActive = !0));
      let i = e,
        a = t,
        o = r.webContents.id,
        s = Date.now(),
        l = s,
        u = () => {
          let e = this.window;
          if (e == null || e.isDestroyed() || e.webContents.id !== o) {
            this.cancelMomentum();
            return;
          }
          this.momentumTimer = null;
          let t = Date.now(),
            r = Math.min(Math.max(0, t - l), x5);
          l = t;
          let d = r / 1e3,
            f = q5(this.anchor),
            p = { x: f.x + i * d, y: f.y + a * d },
            m = this.dockTarget;
          if (m != null) {
            let e = { x: m.anchor.centerX, y: m.anchor.centerY },
              t = t5({
                current: Y5(f, this.presentationOffset),
                next: Y5(p, this.presentationOffset),
                target: e,
              });
            if (
              ((i += t.acceleration.x * d),
              (a += t.acceleration.y * d),
              (p = { x: f.x + i * d, y: f.y + a * d }),
              t5({
                current: Y5(f, this.presentationOffset),
                next: Y5(p, this.presentationOffset),
                target: e,
              }).shouldDock)
            ) {
              ((this.momentumActive = !1),
                this.dockPresentation(m.anchor, m.onDock, { x: i, y: a }));
              return;
            }
          }
          let h = {
              ...this.anchor,
              x: p.x - this.anchor.width / 2,
              y: p.y - this.anchor.height / 2,
            },
            g = n
              ? this.getCurrentDisplay()
              : c.screen.getDisplayNearestPoint(q5(h));
          ((this.anchor = h),
            this.applyLayout(e, g),
            this.anchor.x !== Math.round(h.x) && (i = n ? -i * C5 : 0),
            this.anchor.y !== Math.round(h.y) && (a = n ? -a * C5 : 0));
          let _ = S5 ** +(r / b5);
          if (((i *= _), (a *= _), t - s >= T5 || Math.hypot(i, a) < w5)) {
            ((this.momentumActive = !1),
              (this.dockRestoreAnchor = null),
              this.persistWindowBounds(e));
            return;
          }
          this.momentumTimer = setTimeout(u, y5);
        };
      this.momentumTimer = setTimeout(u, y5);
    }
    setCompositionState(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        (t != null && !this.nativeCompositionEnabled) ||
        (t != null &&
          this.layout != null &&
          !(0, af.default)(t.contentState.layout, V5(this.layout))) ||
        (this.compositionHost.setState(e, t),
        t ?? this.sendLayoutToRenderer(n));
    }
    handleCompositionSurfaceAction(e, t) {
      this.compositionHost.handleSurfaceAction(e, t);
    }
    handleCompositionSurfaceReady(e, t) {
      this.compositionHost.handleSurfaceReady(e, t);
    }
    getCompositionSurfaceHost(e) {
      return this.compositionHost.getSurfaceHost(e);
    }
    startMascotResize(e, t) {
      let n = this.window;
      if (
        n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.layoutMode !== `legacy`
      )
        return;
      (this.cancelMomentum(), this.clearDetachedDisplayRestore());
      let r = z5(this.compositionHost.getCursorPosition()),
        i = r ?? c.screen.getCursorScreenPoint(),
        a = t7(t ?? this.mascotSize.width);
      ((this.mascotResizeState = {
        cursorSource: r == null ? `electron` : `native`,
        displayBounds: c.screen.getDisplayNearestPoint(i).bounds,
        startAnchor: { ...this.anchor },
        startCursorScreenX: i.x,
        startMascotSize: a,
      }),
        (this.ignoreMascotElementSizeUntilMs = 0),
        (this.expectedMascotSizeAfterResize = null));
    }
    moveMascotResize(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.mascotResizeState == null ||
        this.resizeMascotToCurrentCursor(n, t);
    }
    endMascotResize(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.mascotResizeState == null ||
        (this.resizeMascotToCurrentCursor(n, t),
        (this.mascotResizeState = null),
        (this.ignoreMascotElementSizeUntilMs = Date.now() + F5),
        (this.expectedMascotSizeAfterResize = this.mascotSize),
        this.persistWindowBounds(n));
    }
    setElementSize(
      e,
      {
        elementSizeRevision: t,
        isTrayVisible: n,
        mascot: r,
        nativeCompositionEnabled: i,
        tray: a,
      },
    ) {
      let o = this.window;
      if (o == null || o.isDestroyed() || o.webContents.id !== e) return;
      this.pendingElementSizeRevision = t ?? null;
      let s = n == null ? `native` : `legacy`,
        c = s === `native` && i !== !1;
      if (
        ((s !== this.layoutMode ||
          !n7(r, this.mascotSize) ||
          !(0, af.default)(a, this.traySize)) &&
          this.cancelMomentum(),
        (this.layoutMode = s),
        (this.nativeCompositionEnabled = c),
        (this.initialPresentationState === `waiting-for-layout` ||
          (this.initialPresentationState === `waiting-for-native` &&
            !this.nativeCompositionEnabled)) &&
          (this.initialPresentationState =
            this.nativeCompositionEnabled && this.nativeCompositionSupported
              ? `waiting-for-native`
              : `ready`),
        this.nativeCompositionEnabled || this.compositionHost.setState(e, null),
        this.mascotResizeState != null)
      ) {
        this.traySize = a;
        return;
      }
      if (this.expectedMascotSizeAfterResize != null)
        if (n7(r, this.expectedMascotSizeAfterResize))
          ((this.expectedMascotSizeAfterResize = null),
            (this.ignoreMascotElementSizeUntilMs = 0));
        else if (Date.now() < this.ignoreMascotElementSizeUntilMs) {
          this.traySize = a;
          return;
        } else
          ((this.expectedMascotSizeAfterResize = null),
            (this.ignoreMascotElementSizeUntilMs = 0));
      if (
        Date.now() < this.ignoreMascotElementSizeUntilMs &&
        !n7(r, this.mascotSize)
      ) {
        this.traySize = a;
        return;
      }
      if (
        ((this.ignoreMascotElementSizeUntilMs = 0),
        (this.mascotSize = r),
        (this.traySize = a),
        this.movedWindowPersistTimer != null)
      ) {
        ((this.hasDeferredLayout = !0),
          this.layout != null &&
            this.compositionHost.updateMascotRect({
              ...this.layout.mascot,
              ...r,
            }));
        return;
      }
      (this.applyLatestElementSizes(o),
        this.stageWindowForNativePresentation(o),
        this.showWindowIfReady(o));
    }
    async ensureWindow() {
      let e = this.window;
      return e != null && !e.isDestroyed()
        ? e
        : ((this.openingWindowPromise ??= this.createWindow().finally(() => {
            this.openingWindowPromise = null;
          })),
          this.openingWindowPromise);
    }
    async createWindow() {
      let e = await this.windowManager.createWindow({
        title: c.app.getName(),
        width: of.width,
        height: of.height,
        appearance: `avatarOverlay`,
        focusable: !1,
        show: !1,
        initialRoute: l5,
      });
      return (
        (this.window = e),
        (this.lastSentRendererState = null),
        this.compositionHost.setOverlayWindow(e),
        (this.rendererReady = this.windowManager.isWebContentsReady(
          e.webContents.id,
        )),
        this.clearDetachedDisplayRestore(),
        (this.displayBounds = null),
        (this.displayId = null),
        (this.dragState = null),
        (this.windowServerDragActive = !1),
        (this.windowServerDragDirection = null),
        (this.windowServerDragWindowX = null),
        (this.hasDeferredLayout = !1),
        (this.layout = null),
        (this.layoutMode = `legacy`),
        (this.nativeCompositionEnabled = !0),
        (this.initialPresentationState = `waiting-for-layout`),
        (this.windowStagedForNativePresentation = !1),
        (this.compositionTuckedForApplicationHide = !1),
        (this.mascotSize = f5),
        (this.mascotResizeState = null),
        (this.ignoreMascotElementSizeUntilMs = 0),
        (this.expectedMascotSizeAfterResize = null),
        (this.isSuspended = !1),
        (this.mousePassthroughEnabled = !1),
        (this.placement = `top-end`),
        (this.pointerInteractive = !1),
        (this.resolutionKey = null),
        (this.traySize = null),
        process.platform === `darwin`
          ? e.setVisibleOnAllWorkspaces(!0, {
              visibleOnFullScreen: !0,
              skipTransformProcessType: !0,
            })
          : e.setVisibleOnAllWorkspaces(!0),
        e.setAlwaysOnTop(!0, `floating`),
        e.setMenuBarVisibility(!1),
        this.addDisplayChangeListeners(),
        this.addApplicationLifecycleListeners(),
        this.addPowerMonitorListeners(),
        e.once(`ready-to-show`, () => {
          this.showWindowIfReady(e);
        }),
        e.on(`close`, () => {
          this.window === e &&
            (this.cancelMomentum(),
            this.cancelOrbDragFollow(),
            this.clearMovedWindowPersist(),
            this.nativePositionController.clear(),
            this.persistWindowBounds(e));
        }),
        e.on(`move`, () => {
          if (!this.nativePositionController.handleWindowMove(e)) {
            if (this.windowServerDragActive) {
              this.handleWindowServerDragMove(e);
              return;
            }
            this.dragState ??
              (this.compositionHost.isNativeMaterialAttached() ||
                this.compositionHost.moveBackingCanvases(),
              this.layoutMode === `native` && this.rememberMovedWindow(e));
          }
        }),
        e.on(`will-move`, () => {
          this.momentumActive ||
            (this.cancelMomentum(),
            this.clearDetachedDisplayRestore(),
            this.nativePositionController.clear());
        }),
        e.on(`show`, () => {
          this.nativePositionController.handleWindowShow(e);
        }),
        e.on(`hide`, () => {
          if (
            process.platform === `darwin` &&
            this.window === e &&
            this.shouldPresentWindow() &&
            !this.compositionTuckedForApplicationHide &&
            c.app.isHidden()
          ) {
            ((this.compositionTuckedForApplicationHide = !0),
              this.compositionHost.tuck());
            return;
          }
          this.window === e &&
            (this.presentationVisibility ??
              this.startupPresentationVisibility) === !0 &&
            this.reconcileWindowVisibility();
        }),
        e.on(`closed`, () => {
          if (this.window !== e) return;
          let t =
            this.presentationVisibility != null ||
            this.startupPresentationVisibility != null;
          (this.cancelMomentum(),
            this.clearMovedWindowPersist(),
            this.nativePositionController.clear(),
            (this.window = null),
            this.removeDisplayChangeListeners(),
            this.removeApplicationLifecycleListeners(),
            this.removePowerMonitorListeners(),
            (this.displayBounds = null),
            (this.displayId = null),
            (this.dragState = null),
            (this.windowServerDragActive = !1),
            (this.windowServerDragDirection = null),
            (this.windowServerDragWindowX = null),
            this.clearDetachedDisplayRestore(),
            (this.hasDeferredLayout = !1),
            (this.layout = null),
            (this.mascotResizeState = null),
            (this.ignoreMascotElementSizeUntilMs = 0),
            (this.expectedMascotSizeAfterResize = null),
            (this.isSuspended = !1),
            (this.rendererReady = !1),
            (this.lastSentRendererState = null),
            t || (this.petOpenIntent = !1),
            (this.windowStagedForNativePresentation = !1),
            (this.compositionTuckedForApplicationHide = !1),
            (this.pointerInteractive = !1),
            (this.mousePassthroughEnabled = !1),
            this.compositionHost.setOverlayWindow(null),
            this.broadcastOpenState(),
            this.reconcileWindowVisibility());
        }),
        e
      );
    }
    positionWindow(e, t) {
      if (e.isDestroyed()) return;
      let n = this.globalState.get(a.m),
        r = this.getInitialDisplay(e, t, n),
        i = W5(n, r);
      if (i != null) {
        (this.restoreBoundsForDisplay(i, r),
          this.applyLayout(e, r),
          (n?.byDisplayId == null || n.byResolution == null) &&
            this.persistWindowBounds(e, r));
        return;
      }
      (this.resetToDefaultAnchor(r), this.applyLayout(e, r));
    }
    getInitialDisplay(e, t, n) {
      let r = n?.displayId;
      if (r != null) {
        let e = H5(r);
        if (e != null) return e;
      }
      let i = n?.displayBounds;
      if (i != null) {
        let e = c.screen.getDisplayMatching(i);
        if (r7(e.bounds, i)) return e;
      }
      let a = t == null ? null : c.BrowserWindow.fromWebContents(t);
      return a == null || a.isDestroyed() || a === e
        ? c.screen.getDisplayNearestPoint(c.screen.getCursorScreenPoint())
        : c.screen.getDisplayMatching(a.getBounds());
    }
    restoreBoundsForDisplay(e, t) {
      let n = G5(e, t.bounds);
      ((this.anchor = {
        ...n,
        x: n.x - this.presentationOffset.x,
        y: n.y - this.presentationOffset.y,
      }),
        (this.mascotSize = { width: n.width, height: n.height }),
        (this.placement = e.placement ?? this.placement));
    }
    resetToDefaultAnchor(e) {
      let t = R5(
        this.layoutMode === `native` ? e.workArea : e.bounds,
        this.mascotSize,
      );
      this.anchor = {
        ...t,
        x: t.x - this.presentationOffset.x,
        y: t.y - this.presentationOffset.y,
      };
    }
    switchToDisplay(e, t, n = !1) {
      let r = W5(this.globalState.get(a.m), t);
      (r == null
        ? this.resetToDefaultAnchor(t)
        : this.restoreBoundsForDisplay(r, t),
        this.applyLayout(e, t, n));
    }
    getCurrentDisplay() {
      if (this.displayId != null) {
        let e = H5(this.displayId);
        if (e != null) return e;
      }
      return c.screen.getDisplayNearestPoint(q5(this.anchor));
    }
    addDisplayChangeListeners() {
      (c.screen.on(`display-added`, this.handleDisplayChanged),
        c.screen.on(`display-removed`, this.handleDisplayChanged),
        c.screen.on(`display-metrics-changed`, this.handleDisplayChanged));
    }
    removeDisplayChangeListeners() {
      (c.screen.off(`display-added`, this.handleDisplayChanged),
        c.screen.off(`display-removed`, this.handleDisplayChanged),
        c.screen.off(`display-metrics-changed`, this.handleDisplayChanged));
    }
    addPowerMonitorListeners() {
      (c.powerMonitor.on(`suspend`, this.handleSuspend),
        c.powerMonitor.on(`resume`, this.handleResume));
    }
    removePowerMonitorListeners() {
      (c.powerMonitor.off(`suspend`, this.handleSuspend),
        c.powerMonitor.off(`resume`, this.handleResume));
    }
    addApplicationLifecycleListeners() {
      (c.app.on(`did-resign-active`, this.handleApplicationDidResignActive),
        c.app.on(`did-become-active`, this.handleApplicationDidBecomeActive));
    }
    removeApplicationLifecycleListeners() {
      (c.app.off(`did-resign-active`, this.handleApplicationDidResignActive),
        c.app.off(`did-become-active`, this.handleApplicationDidBecomeActive),
        this.clearApplicationHideCheck());
    }
    clearApplicationHideCheck() {
      this.applicationHideCheckTimer != null &&
        (clearTimeout(this.applicationHideCheckTimer),
        (this.applicationHideCheckTimer = null));
    }
    handleApplicationDidResignActive = () => {
      (this.clearApplicationHideCheck(),
        (this.applicationHideCheckTimer = setTimeout(() => {
          this.applicationHideCheckTimer = null;
          let e = this.window;
          !c.app.isHidden() ||
            this.compositionTuckedForApplicationHide ||
            e == null ||
            e.isDestroyed() ||
            !this.shouldPresentWindow() ||
            ((this.compositionTuckedForApplicationHide = !0),
            this.compositionHost.tuck());
        }, 0)));
    };
    handleApplicationDidBecomeActive = () => {
      if (
        (this.clearApplicationHideCheck(),
        !this.compositionTuckedForApplicationHide)
      )
        return;
      this.compositionTuckedForApplicationHide = !1;
      let e = this.window;
      e != null &&
        !e.isDestroyed() &&
        this.shouldPresentWindow() &&
        this.presentWindow(e);
    };
    handleDisplayChanged = () => {
      if (this.isSuspended) return;
      if (this.dragState != null) {
        let e =
          this.dragState.cursorSource === `native`
            ? z5(this.compositionHost.getCursorPosition())
            : c.screen.getCursorScreenPoint();
        e != null &&
          this.dragState.updateDisplayBounds(
            c.screen.getDisplayNearestPoint(e).bounds,
          );
        return;
      }
      this.cancelMomentum();
      let e = this.window;
      if (e == null || e.isDestroyed() || this.restoreDetachedDisplay(e))
        return;
      this.rememberDetachedDisplay();
      let t = this.getCurrentDisplay();
      if (
        B5(t.bounds) !== this.resolutionKey ||
        this.displayBounds == null ||
        !r7(t.bounds, this.displayBounds)
      ) {
        (this.switchToDisplay(e, t, !0), this.persistWindowBounds(e, t));
        return;
      }
      this.reclampWindowToVisibleDisplay({ shouldPersist: !0 });
    };
    handleSuspend = () => {
      let e = this.window;
      if (e == null || e.isDestroyed() || this.displayBounds == null) return;
      (this.cancelMomentum(), (this.isSuspended = !0));
      let t = this.getCurrentDisplay();
      this.persistWindowBounds(e, t);
    };
    handleResume = () => {
      this.isSuspended = !1;
      let e = this.window;
      if (e == null || e.isDestroyed() || this.restoreDetachedDisplay(e))
        return;
      let t = this.displayId;
      if (t == null) return;
      let n = H5(t);
      if (n == null) {
        this.rememberDetachedDisplay();
        let t = this.getCurrentDisplay();
        (this.switchToDisplay(e, t, !0), this.persistWindowBounds(e, t));
        return;
      }
      (this.switchToDisplay(e, n, !0), this.persistWindowBounds(e, n));
    };
    rememberDetachedDisplay() {
      let e = this.displayId,
        t = this.displayBounds;
      e == null ||
        t == null ||
        H5(e) != null ||
        this.detachedDisplay?.id === e ||
        ((this.detachedDisplay = { bounds: t, id: e }),
        this.detachedDisplayRestoreTimer != null &&
          clearTimeout(this.detachedDisplayRestoreTimer),
        (this.detachedDisplayRestoreTimer = setTimeout(() => {
          ((this.detachedDisplay = null),
            (this.detachedDisplayRestoreTimer = null));
        }, P5)));
    }
    restoreDetachedDisplay(e) {
      let t = this.detachedDisplay;
      if (t == null) return !1;
      let n = H5(t.id) ?? U5(t.bounds);
      if (n == null) return !1;
      let r = this.globalState.get(a.m)?.byDisplayId?.[String(t.id)];
      return (
        this.clearDetachedDisplayRestore(),
        r == null
          ? this.switchToDisplay(e, n, !0)
          : (this.restoreBoundsForDisplay(r, n), this.applyLayout(e, n, !0)),
        this.persistWindowBounds(e, n),
        !0
      );
    }
    clearDetachedDisplayRestore() {
      (this.detachedDisplayRestoreTimer != null &&
        (clearTimeout(this.detachedDisplayRestoreTimer),
        (this.detachedDisplayRestoreTimer = null)),
        (this.detachedDisplay = null));
    }
    reclampWindowToVisibleDisplay({ shouldPersist: e }) {
      let t = this.window;
      if (t == null || t.isDestroyed()) return;
      let n = this.getCurrentDisplay();
      (this.applyLayout(t, n), e && this.persistWindowBounds(t, n));
    }
    moveDragToPointer(e, t) {
      let n = this.dragState;
      if (n == null) return;
      let r = K5(t, n.displayBounds),
        i = c.screen.getDisplayMatching(r);
      n.recordMove(i.bounds);
      let a = t.x - n.pointerAnchorX,
        o = t.y - n.pointerAnchorY,
        s = n.usesOrbPhysics ? M5 : 1;
      ((this.anchor = {
        ...this.anchor,
        x: this.anchor.x + (a - this.anchor.x) * s,
        y: this.anchor.y + (o - this.anchor.y) * s,
      }),
        this.applyLayout(e, i, !1, !1));
    }
    resizeMascotToCurrentCursor(e, t) {
      let n = this.mascotResizeState;
      if (n == null) return;
      let r =
        n.cursorSource === `native`
          ? z5(this.compositionHost.getCursorPosition())
          : c.screen.getCursorScreenPoint();
      if (r == null && t == null) return;
      let i =
        r == null
          ? c.screen.getDisplayMatching(n.displayBounds)
          : c.screen.getDisplayMatching(K5(r, n.displayBounds));
      n.displayBounds = i.bounds;
      let a = t7(
        t ??
          n.startMascotSize.width +
            (r?.x ?? n.startCursorScreenX) -
            n.startCursorScreenX,
      );
      ((this.mascotSize = a),
        (this.anchor = { ...n.startAnchor, ...a }),
        this.applyLayout(e, i));
    }
    positionPresentationAtOrigin(e, t) {
      ((this.anchor = X5(t, this.mascotSize, this.presentationOffset)),
        this.applyLayout(e, c.screen.getDisplayNearestPoint(q5(this.anchor))));
    }
    animatePresentationTo(e, t, n) {
      let r = this.window;
      if (r == null || r.isDestroyed()) {
        n?.();
        return;
      }
      (this.cancelMomentum(),
        (this.presentationMotionTarget = e),
        (this.momentumActive = !0));
      let i = t.x,
        a = t.y,
        o = r.webContents.id,
        s = Date.now(),
        l = s,
        u = () => {
          let t = this.window;
          if (t == null || t.isDestroyed() || t.webContents.id !== o) {
            this.cancelMomentum();
            return;
          }
          this.momentumTimer = null;
          let r = Date.now(),
            d = Math.min(Math.max(0, r - l), x5) / 1e3;
          l = r;
          let f = O5 * (e.x - this.anchor.x) - D5 * i,
            p = O5 * (e.y - this.anchor.y) - D5 * a;
          if (
            ((i += f * d),
            (a += p * d),
            (this.anchor = {
              ...this.anchor,
              x: this.anchor.x + i * d,
              y: this.anchor.y + a * d,
            }),
            this.applyLayout(
              t,
              c.screen.getDisplayNearestPoint(q5(this.anchor)),
            ),
            r - s >= E5 ||
              (Math.hypot(e.x - this.anchor.x, e.y - this.anchor.y) <= k5 &&
                Math.hypot(i, a) < A5))
          ) {
            ((this.anchor = e),
              this.applyLayout(t, c.screen.getDisplayNearestPoint(q5(e))),
              (this.momentumActive = !1),
              (this.presentationMotionTarget = null),
              n?.());
            return;
          }
          this.momentumTimer = setTimeout(u, y5);
        };
      this.momentumTimer = setTimeout(u, y5);
    }
    restoreDockAnchor() {
      let e = this.dockRestoreAnchor;
      if (e == null) return;
      (this.cancelMomentum(),
        (this.dockRestoreAnchor = null),
        (this.anchor = e));
      let t = this.window;
      t != null &&
        !t.isDestroyed() &&
        this.applyLayout(t, c.screen.getDisplayNearestPoint(q5(e)));
    }
    cancelMomentum() {
      (this.momentumTimer != null &&
        (clearTimeout(this.momentumTimer), (this.momentumTimer = null)),
        (this.presentationMotionTarget = null),
        (this.momentumActive = !1));
    }
    cancelOrbDragFollow() {
      (this.orbDragFollowTimer != null &&
        (clearTimeout(this.orbDragFollowTimer),
        (this.orbDragFollowTimer = null)),
        (this.orbDragFollowTarget = null));
    }
    scheduleOrbDragFollow(e) {
      this.orbDragFollowTimer ??= setTimeout(() => {
        this.orbDragFollowTimer = null;
        let t = this.dragState,
          n = this.orbDragFollowTarget;
        if (t == null || !t.usesOrbPhysics || n == null || e.isDestroyed()) {
          this.orbDragFollowTarget = null;
          return;
        }
        let r = this.anchor.x,
          i = this.anchor.y;
        (this.moveDragToPointer(e, n),
          (this.anchor.x !== r || this.anchor.y !== i) &&
            this.scheduleOrbDragFollow(e));
      }, j5);
    }
    clearMovedWindowPersist() {
      this.movedWindowPersistTimer != null &&
        (clearTimeout(this.movedWindowPersistTimer),
        (this.movedWindowPersistTimer = null));
    }
    handleWindowServerDragMove(e) {
      let t = e.getContentBounds(),
        n = t.x,
        r = this.windowServerDragWindowX;
      ((this.windowServerDragWindowX = n),
        r != null &&
          n !== r &&
          this.sendWindowServerDragDirection(e, n < r ? `left` : `right`));
      let i = this.layout;
      if (i == null) return;
      let a = { ...this.anchor, x: t.x + i.mascot.left, y: t.y + i.mascot.top },
        o = c.screen.getDisplayNearestPoint(q5(a));
      this.updateWindowServerDragPlacement(e, t, a, o);
    }
    updateWindowServerDragPlacement(e, t, n, r) {
      let i = mf(n, r.workArea);
      if (((this.anchor = n), i === this.placement)) return;
      this.placement = i;
      let a = this.getLayoutForDisplay(r);
      ((this.anchor = a.anchor),
        (this.layout = {
          ...a,
          mascot: {
            ...a.mascot,
            left: a.anchor.x - t.x,
            top: a.anchor.y - t.y,
          },
          tray:
            a.tray == null
              ? null
              : {
                  ...a.tray,
                  left: a.windowBounds.x + a.tray.left - t.x,
                  top: a.windowBounds.y + a.tray.top - t.y,
                },
          windowBounds: t,
        }),
        (this.placement = a.placement),
        (this.displayBounds = r.bounds),
        (this.displayId = r.id),
        (this.resolutionKey = B5(r.bounds)),
        this.compositionHost.updateMascotRect(this.layout.mascot),
        this.sendLayoutToRenderer(e));
    }
    sendWindowServerDragDirection(e, t) {
      this.windowServerDragDirection !== t &&
        ((this.windowServerDragDirection = t),
        this.windowManager.sendMessageToWebContents(e.webContents, {
          type: `avatar-overlay-window-drag-state-changed`,
          direction: t,
        }));
    }
    rememberMovedWindow(e) {
      if (e.isDestroyed() || this.layout == null) return;
      let t = e.getContentBounds();
      this.anchor = {
        ...this.anchor,
        x: t.x + this.layout.mascot.left,
        y: t.y + this.layout.mascot.top,
      };
      let n = c.screen.getDisplayNearestPoint(q5(this.anchor));
      ((this.displayBounds = n.bounds),
        (this.displayId = n.id),
        (this.resolutionKey = B5(n.bounds)),
        this.isOverlayWindowPointerDragActive() &&
          this.clearDetachedDisplayRestore(),
        this.clearMovedWindowPersist(),
        (this.movedWindowPersistTimer = setTimeout(() => {
          if (
            ((this.movedWindowPersistTimer = null),
            this.isOverlayWindowPointerDragActive())
          ) {
            this.rememberMovedWindow(e);
            return;
          }
          (this.hasDeferredLayout &&
            ((this.hasDeferredLayout = !1), this.applyLatestElementSizes(e)),
            this.applyPointerInteractivityPolicy(),
            this.reclampWindowToVisibleDisplay({ shouldPersist: !0 }));
        }, N5)));
    }
    isOverlayWindowPointerDragActive() {
      let e = this.window;
      return e == null ||
        e.isDestroyed() ||
        !this.pointerInteractive ||
        !this.compositionHost.isPrimaryMouseButtonPressed()
        ? !1
        : Q5(
            z5(this.compositionHost.getCursorPosition()) ??
              c.screen.getCursorScreenPoint(),
            e.getContentBounds(),
          );
    }
    applyLatestElementSizes(e) {
      this.anchor = {
        ...this.anchor,
        width: this.mascotSize.width,
        height: this.mascotSize.height,
      };
      let t = this.pendingElementSizeRevision;
      ((this.pendingElementSizeRevision = null),
        this.applyLayout(e, this.getCurrentDisplay(), !1, !0, t));
    }
    applyLayout(e, t = this.getCurrentDisplay(), n = !1, r = !0, i = null) {
      if (e.isDestroyed()) return;
      let a = this.getLayoutForDisplay(t);
      ((this.displayId = t.id),
        (this.resolutionKey = B5(t.bounds)),
        (this.displayBounds = t.bounds),
        (this.anchor = a.anchor),
        (this.layout = a),
        (this.placement = a.placement),
        this.setWindowBounds(e, a.windowBounds, n, r),
        this.compositionHost.updateMascotRect(a.mascot),
        this.sendLayoutToRenderer(e, i),
        this.computerUseCursorLocation != null &&
          this.dragState == null &&
          this.sendComputerUseCursorLocationToRenderer(e));
    }
    getLayoutForDisplay(e) {
      return pf({
        anchor: this.anchor,
        displayBounds: this.layoutMode === `native` ? e.workArea : e.bounds,
        mode: this.layoutMode,
        mascotSize: this.mascotSize,
        nativeMaterialAttached: this.compositionHost.isNativeMaterialAttached(),
        previousPlacement: this.placement,
        traySize: this.traySize ?? (this.layoutMode === `native` ? _5 : g5),
      });
    }
    getLayout(e) {
      if ((this.layout ?? this.applyLayout(e), this.layout == null))
        throw Error(`Expected avatar overlay layout`);
      return this.layout;
    }
    setWindowBounds(e, t, n, r) {
      if (e.isDestroyed()) return;
      let i = e.getContentBounds();
      if (r7(i, t)) {
        this.nativePositionController.position(e, t);
        return;
      }
      let a = i.width === t.width && i.height === t.height;
      (e.setContentBounds(t, n && !this.nativeCompositionSupported),
        this.nativePositionController.position(e, t),
        (r || !a) && this.compositionHost.moveBackingCanvases());
    }
    sendLayoutToRenderer(e, t = null) {
      if (e.isDestroyed() || this.layout == null || !this.rendererReady) return;
      let n = V5(this.layout),
        r = this.compositionHost.isNativeMaterialAttached();
      (t == null &&
        this.lastSentRendererState != null &&
        r === this.lastSentRendererState.nativeMaterialAttached &&
        (0, af.default)(n, this.lastSentRendererState.layout)) ||
        ((this.lastSentRendererState = {
          layout: n,
          nativeMaterialAttached: r,
        }),
        this.windowManager.sendMessageToWebContents(e.webContents, {
          type: `avatar-overlay-layout-changed`,
          ...(t == null ? {} : { elementSizeRevision: t }),
          layout: n,
          nativeMaterialAttached: r,
        }));
    }
    persistWindowBounds(e, t = this.getCurrentDisplay()) {
      if (e.isDestroyed()) return;
      let n = t.bounds,
        r = {
          ...e.getContentBounds(),
          anchor: {
            ...this.anchor,
            x: this.anchor.x + this.presentationOffset.x,
            y: this.anchor.y + this.presentationOffset.y,
          },
          displayBounds: n,
          displayId: t.id,
          mascot: this.layout?.mascot,
          placement: this.placement,
          tray: this.layout?.tray,
        },
        i = this.globalState.get(a.m),
        o = B5(n);
      ((this.displayId = t.id),
        (this.resolutionKey = o),
        this.globalState.set(a.m, {
          ...r,
          byDisplayId: { ...i?.byDisplayId, [String(t.id)]: r },
          byResolution: { ...i?.byResolution, [o]: r },
        }));
    }
    showWindow(e) {
      if (e.isDestroyed()) return;
      let t = this.isOpen();
      ((this.windowStagedForNativePresentation &&= (e.setOpacity(1), !1)),
        e.moveTop(),
        e.showInactive(),
        this.compositionHost.publishRemoteHostedPIPContentHost(),
        !t && this.isOpen() && this.broadcastOpenState());
      let n = this.pendingPresentation;
      n != null &&
        ((this.pendingPresentation = null),
        n.velocity == null
          ? ((this.dockRestoreAnchor = n.target),
            this.animatePresentationTo(
              n.target,
              Z5(this.anchor, n.target),
              () => {
                this.dockRestoreAnchor === n.target &&
                  (this.dockRestoreAnchor = null);
              },
            ))
          : this.startMomentum(n.velocity.x, n.velocity.y, !0));
    }
    reconcileWindowVisibility() {
      if (this.isAppQuitting) return;
      if (!this.shouldPresentWindow()) {
        this.hideWindow();
        return;
      }
      let e = this.window;
      if (e != null && !e.isDestroyed()) {
        this.presentWindow(e);
        return;
      }
      this.open(null, { persistPetIntent: !1 }).catch((e) => {
        I5().warning(`Failed to show avatar overlay presentation`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }
    hideWindow() {
      this.windowVisibilitySequence += 1;
      let e = this.window;
      (e != null && !e.isDestroyed() && (e.hide(), this.restoreDockAnchor()),
        this.compositionHost.tuck(),
        this.broadcastOpenState());
    }
    showWindowIfReady(e) {
      !this.shouldPresentWindow() ||
        this.compositionTuckedForApplicationHide ||
        !this.rendererReady ||
        this.initialPresentationState !== `ready` ||
        (this.showWindow(e), this.applyPointerInteractivityPolicy());
    }
    stageWindowForNativePresentation(e) {
      e.isDestroyed() ||
        !this.shouldPresentWindow() ||
        !this.rendererReady ||
        this.initialPresentationState !== `waiting-for-native` ||
        this.windowStagedForNativePresentation ||
        ((this.windowStagedForNativePresentation = !0),
        e.setOpacity(v5),
        e.moveTop(),
        e.showInactive(),
        this.applyPointerInteractivityPolicy());
    }
    shouldPresentWindow() {
      return (
        this.presentationVisibility ??
        this.startupPresentationVisibility ??
        this.petOpenIntent
      );
    }
    broadcastOpenState() {
      this.windowManager.sendMessageToAllRegisteredWindows({
        type: `avatar-overlay-open-state-changed`,
        isOpen: this.isOpen(),
      });
    }
    applyPointerInteractivityPolicy() {
      let e = this.window;
      if (e == null || e.isDestroyed()) {
        this.mousePassthroughEnabled = !1;
        return;
      }
      let t = !this.pointerInteractive;
      if (this.mousePassthroughEnabled !== t) {
        if (((this.mousePassthroughEnabled = t), t)) {
          e.setIgnoreMouseEvents(!0, { forward: !0 });
          return;
        }
        (e.setIgnoreMouseEvents(!1),
          this.refreshCursorAtCurrentMousePosition(e));
      }
    }
    setComputerUseCursorLocation(e) {
      ((this.computerUseCursorLocation = e),
        (this.computerUseCursorPoint = e.isActive ? { x: e.x, y: e.y } : null));
      let t = this.window;
      if (
        !(t == null || t.isDestroyed()) &&
        (this.sendComputerUseCursorLocationToRenderer(t),
        this.dragState == null)
      ) {
        if (this.computerUseCursorPoint != null) {
          this.sendCursorPointToAvatarOverlay(
            t,
            this.computerUseCursorPoint,
            !0,
          );
          return;
        }
        this.refreshCursorAtCurrentMousePosition(t) ||
          t.webContents.sendInputEvent({ type: `mouseLeave`, x: -1, y: -1 });
      }
    }
    sendComputerUseCursorLocationToRenderer(e) {
      if (e.isDestroyed() || !this.rendererReady) return;
      let t = e.getContentBounds();
      this.windowManager.sendMessageToWebContents(e.webContents, {
        type: `avatar-overlay-computer-use-cursor-changed`,
        point:
          this.computerUseCursorPoint == null
            ? null
            : {
                x: this.computerUseCursorPoint.x - t.x,
                y: this.computerUseCursorPoint.y - t.y,
              },
      });
    }
    refreshCursorAtCurrentMousePosition(e) {
      if (e.isDestroyed()) return !1;
      if (this.computerUseCursorPoint != null)
        return this.sendCursorPointToAvatarOverlay(
          e,
          this.computerUseCursorPoint,
          !0,
        );
      let t =
        z5(this.compositionHost.getCursorPosition()) ??
        c.screen.getCursorScreenPoint();
      return this.sendCursorPointToAvatarOverlay(e, t, !1);
    }
    sendCursorPointToAvatarOverlay(e, t, n) {
      let r = e.getContentBounds(),
        i = t.x - r.x,
        a = t.y - r.y;
      return !n && (i < 0 || a < 0 || i > r.width || a > r.height)
        ? !1
        : (e.webContents.sendInputEvent({
            type: `mouseMove`,
            x: i,
            y: a,
            movementX: 0,
            movementY: 0,
          }),
          !0);
    }
  };
function R5(e, t) {
  return {
    x: e.x + e.width - t.width - u5,
    y: e.y + e.height - t.height - u5,
    width: t.width,
    height: t.height,
  };
}
function z5(e) {
  return e == null ? null : { x: e.pointerScreenX, y: e.pointerScreenY };
}
function B5(e) {
  return `${e.width}x${e.height}`;
}
function V5(e) {
  return {
    mascot: e.mascot,
    placement: e.placement,
    tray: e.tray,
    viewport: e.viewport,
  };
}
function H5(e) {
  return c.screen.getAllDisplays().find((t) => t.id === e) ?? null;
}
function U5(e) {
  let t = c.screen.getDisplayMatching(e);
  return r7(t.bounds, e) ? t : null;
}
function W5(e, t) {
  return e == null
    ? null
    : (e.byDisplayId?.[String(t.id)] ??
        e.byResolution?.[B5(t.bounds)] ??
        (e.byDisplayId == null && e.byResolution == null ? e : null));
}
function G5(e, t) {
  let n = e.mascot,
    r =
      e.anchor ??
      (n == null
        ? { x: e.x, y: e.y, width: e.width, height: e.height }
        : {
            x: e.x + n.left,
            y: e.y + n.top,
            width: n.width,
            height: n.height,
          }),
    i = e.displayBounds;
  return i == null ? r : { ...r, x: r.x + t.x - i.x, y: r.y + t.y - i.y };
}
function K5(e, t) {
  let n = c.screen.getDisplayNearestPoint(e).bounds;
  return r7(n, t) ? n : Q5(e, $5(t, d5)) ? t : n;
}
function q5(e) {
  return { x: e.x + e.width / 2, y: e.y + e.height / 2 };
}
function J5(e, t) {
  return Y5(q5(e), t);
}
function Y5(e, t) {
  return { x: e.x + t.x, y: e.y + t.y };
}
function X5(e, t, n) {
  return {
    x: e.centerX - n.x - t.width / 2,
    y: e.centerY - n.y - t.height / 2,
    ...t,
  };
}
function Z5(e, t) {
  let n = t.x - e.x,
    r = t.y - e.y;
  return { x: n * 2.8 - r * 0.16, y: r * 2.8 + n * 0.16 };
}
function Q5(e, t) {
  return (
    e.x >= t.x && e.x < t.x + t.width && e.y >= t.y && e.y < t.y + t.height
  );
}
function $5(e, t) {
  return {
    x: e.x - t,
    y: e.y - t,
    width: e.width + t * 2,
    height: e.height + t * 2,
  };
}
function e7(e) {
  return Number.isFinite(e)
    ? Math.round(Math.min(m5, Math.max(p5, e)))
    : f5.width;
}
function t7(e) {
  let t = e7(e);
  return { width: t, height: Math.ceil(t / h5) };
}
function n7(e, t) {
  return e.width === t.width && e.height === t.height;
}
function r7(e, t) {
  return (
    e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height
  );
}
var i7 = `transcription-history.jsonl`,
  a7 = n.xl({ id: n.wl(), createdAtMs: n.bl(), text: n.wl() });
function o7() {
  let e = c7();
  if (!(0, p.existsSync)(e)) return [];
  try {
    let t = [];
    for (let n of (0, p.readFileSync)(e, `utf8`).split(/\r?\n/)) {
      let e = n.trim();
      if (e.length !== 0)
        try {
          let n = a7.safeParse(JSON.parse(e));
          n.success && t.push(n.data);
        } catch {}
    }
    return t.slice(-10).reverse();
  } catch (e) {
    return (
      r.r().warning(`Failed to read global dictation history`, {
        safe: {},
        sensitive: { error: e },
      }),
      []
    );
  }
}
function s7(e) {
  try {
    let t = c7();
    ((0, p.mkdirSync)((0, u.dirname)(t), { recursive: !0 }),
      (0, p.appendFileSync)(t, `${JSON.stringify(e)}\n`, `utf8`));
  } catch (e) {
    r.r().warning(`Failed to write global dictation history`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
function c7() {
  return (0, u.join)(n.ni(), i7);
}
var l7 = 1e3,
  u7 = `codex-global-dictation-window.lock`,
  d7 = `owner.json`,
  f7 = 5e3,
  p7 = n.xl({ pid: n.bl().int().positive(), token: n.wl().min(1) });
function m7({
  force: e,
  lockPath: t = (0, u.join)((0, l.tmpdir)(), u7),
  onLost: n,
}) {
  let r = { pid: process.pid, token: (0, f.randomUUID)() };
  if (!h7(t, e)) return null;
  g7(t, r);
  let i = setInterval(() => {
    v7(t, r.token) || (clearInterval(i), n());
  }, l7);
  return (
    i.unref?.(),
    {
      dispose: () => {
        (clearInterval(i), _7(t, r.token));
      },
    }
  );
}
function h7(e, t) {
  try {
    return ((0, p.mkdirSync)(e, { recursive: !1 }), !0);
  } catch (e) {
    if (!w7(e)) throw e;
  }
  if (!t && !y7(e)) return !1;
  (0, p.rmSync)(e, { force: !0, recursive: !0 });
  try {
    return ((0, p.mkdirSync)(e, { recursive: !1 }), !0);
  } catch (e) {
    if (w7(e)) return !1;
    throw e;
  }
}
function g7(e, t) {
  (0, p.writeFileSync)(x7(e), `${JSON.stringify(t, null, 2)}\n`);
}
function _7(e, t) {
  v7(e, t) && (0, p.rmSync)(e, { force: !0, recursive: !0 });
}
function v7(e, t) {
  return b7(e)?.token === t;
}
function y7(e) {
  let t = b7(e);
  return t == null ? !S7(e) : !C7(t.pid);
}
function b7(e) {
  try {
    let t = (0, p.readFileSync)(x7(e), `utf8`);
    return p7.parse(JSON.parse(t));
  } catch {
    return null;
  }
}
function x7(e) {
  return (0, u.join)(e, d7);
}
function S7(e) {
  try {
    let t = (0, p.statSync)(e);
    return Date.now() - t.mtimeMs < f7;
  } catch {
    return !1;
  }
}
function C7(e) {
  try {
    return (process.kill(e, 0), !0);
  } catch (e) {
    return !T7(e);
  }
}
function w7(e) {
  return E7(e) === `EEXIST`;
}
function T7(e) {
  return E7(e) === `ESRCH`;
}
function E7(e) {
  return typeof e == `object` && e && `code` in e ? e.code : null;
}
var D7 = 150,
  O7 = 700,
  k7 = (0, d.promisify)(h.execFile);
async function A7(e) {
  let t = j7();
  c.clipboard.writeText(e);
  try {
    (await (0, v.setTimeout)(D7), await P7(), await (0, v.setTimeout)(O7));
  } finally {
    M7(t, e);
  }
}
function j7() {
  return {
    data: {
      text: c.clipboard.readText(),
      html: c.clipboard.readHTML(),
      rtf: c.clipboard.readRTF(),
      image: c.clipboard.readImage(),
      bookmark: c.clipboard.readBookmark().title,
    },
    formats: c.clipboard
      .availableFormats()
      .map((e) => ({ format: e, data: c.clipboard.readBuffer(e) })),
  };
}
function M7(e, t) {
  return c.clipboard.readText() === t ? (N7(e), !0) : !1;
}
function N7(e) {
  c.clipboard.write(e.data);
  let t = new Set(c.clipboard.availableFormats());
  for (let { format: n, data: r } of e.formats)
    t.has(n) || c.clipboard.writeBuffer(n, r);
}
async function P7() {
  switch (process.platform) {
    case `darwin`:
      if (!c.systemPreferences.isTrustedAccessibilityClient(!1))
        throw Error(
          `Accessibility permission is required to paste global dictation.`,
        );
      await k7(`/usr/bin/osascript`, [
        `-e`,
        `tell application "System Events" to keystroke "v" using command down`,
      ]);
      return;
    case `win32`:
      await k7(`powershell.exe`, [
        `-STA`,
        `-NoProfile`,
        `-NonInteractive`,
        `-ExecutionPolicy`,
        `Bypass`,
        `-Command`,
        `Add-Type -AssemblyName System.Windows.Forms; [System.Windows.Forms.SendKeys]::SendWait('^v')`,
      ]);
      return;
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      throw Error(`Global dictation paste is not supported on this OS.`);
  }
}
var F7 = class {
  entry = null;
  disposed = !1;
  constructor(e) {
    this.createWindow = e;
  }
  async prewarm() {
    await this.get();
  }
  async get() {
    if (this.disposed) return null;
    let e = this.entry ?? this.createEntry(),
      t = await e.promise;
    return this.disposed || e.cancelled || t == null
      ? null
      : this.entry === e
        ? t.isDestroyed()
          ? ((this.entry = null), this.get())
          : t
        : this.entry == null
          ? null
          : this.get();
  }
  async take() {
    if (this.disposed) return null;
    let e = this.entry ?? this.createEntry();
    this.entry = null;
    let t = await e.promise;
    return this.disposed
      ? (I7(t), null)
      : t == null
        ? null
        : t.isDestroyed()
          ? this.createTakenWindow()
          : t;
  }
  clear() {
    let e = this.entry;
    ((this.entry = null),
      e != null && ((e.cancelled = !0), I7(e.window), (e.window = null)));
  }
  dispose() {
    ((this.disposed = !0), this.clear());
  }
  peek() {
    let e = this.entry?.window ?? null;
    return e != null && e.isDestroyed() ? ((this.entry = null), null) : e;
  }
  runWhenAvailable(e) {
    let t = this.entry;
    t?.promise
      .then((n) => {
        this.entry === t && n != null && !n.isDestroyed() && e(n);
      })
      .catch(() => void 0);
  }
  createEntry() {
    let e;
    return (
      (e = {
        cancelled: !1,
        promise: this.createWindow()
          .then((t) =>
            t == null
              ? (this.entry === e && (this.entry = null), null)
              : e.cancelled || this.disposed
                ? (I7(t), null)
                : ((e.window = t), t),
          )
          .catch((t) => {
            throw (this.entry === e && (this.entry = null), t);
          }),
        window: null,
      }),
      (this.entry = e),
      e
    );
  }
  async createTakenWindow() {
    if (this.disposed) return null;
    let e = await this.createWindow();
    return e == null
      ? null
      : this.disposed
        ? (I7(e), null)
        : e.isDestroyed()
          ? this.createTakenWindow()
          : e;
  }
};
function I7(e) {
  e != null && !e.isDestroyed() && e.close();
}
var L7 = 16,
  R7 = { width: 720, height: 84 },
  z7 = class {
    recorderWindow = new F7(() => this.createWindow());
    rendererReadyPromise = null;
    rendererReadyResolve = null;
    rendererReadyWebContentsId = null;
    constructor(e) {
      this.windowManager = e;
    }
    prewarm() {
      this.recorderWindow.prewarm();
    }
    async showIdle(e, t) {
      let n = await this.recorderWindow.get();
      return n == null || (await this.waitForRenderer(n), n.isDestroyed())
        ? !1
        : (this.positionWindow(n),
          n.setAlwaysOnTop(!0, `floating`),
          this.windowManager.sendMessageToWebContents(n.webContents, {
            type: `global-dictation-idle`,
            configuredHotkey: e,
            configuredToggleHotkey: t,
          }),
          n.showInactive(),
          this.applyPointerInteractivity(n, !0),
          !0);
    }
    async showAndStart(e) {
      let t = await this.recorderWindow.get();
      return t == null || (await this.waitForRenderer(t), t.isDestroyed())
        ? !1
        : (this.positionWindow(t),
          t.setAlwaysOnTop(!0, `floating`),
          this.windowManager.sendMessageToWebContents(t.webContents, {
            type: `global-dictation-start`,
            sessionId: e,
          }),
          t.showInactive(),
          !0);
    }
    sendStop(e) {
      let t = this.recorderWindow.peek();
      if (t != null) {
        if (this.rendererReadyWebContentsId === t.webContents.id) {
          this.windowManager.sendMessageToWebContents(t.webContents, {
            type: `global-dictation-stop`,
            sessionId: e,
          });
          return;
        }
        this.waitForRenderer(t).then(() => {
          t.isDestroyed() ||
            this.windowManager.sendMessageToWebContents(t.webContents, {
              type: `global-dictation-stop`,
              sessionId: e,
            });
        });
      }
    }
    setPointerInteractive(e, t) {
      let n = this.recorderWindow.peek();
      n == null ||
        n.webContents.id !== e ||
        this.applyPointerInteractivity(n, t);
    }
    setRendererReady(e) {
      let t = this.recorderWindow.peek();
      if (t == null) {
        this.recorderWindow.runWhenAvailable((t) => {
          t.webContents.id === e && this.markRendererReady(e);
        });
        return;
      }
      t.webContents.id === e && this.markRendererReady(e);
    }
    hide() {
      let e = this.recorderWindow.peek();
      if (e != null) {
        e.hide();
        return;
      }
      this.recorderWindow.runWhenAvailable((e) => {
        e.hide();
      });
    }
    close() {
      ((this.rendererReadyWebContentsId = null),
        this.rendererReadyResolve?.(),
        (this.rendererReadyResolve = null),
        (this.rendererReadyPromise = null),
        this.recorderWindow.clear());
    }
    getFocusedInAppWindow() {
      let e = c.BrowserWindow.getFocusedWindow();
      return e == null || e.isDestroyed() || e === this.recorderWindow.peek()
        ? null
        : e;
    }
    positionWindow(e) {
      let t = c.screen.getDisplayNearestPoint(
        c.screen.getCursorScreenPoint(),
      ).workArea;
      e.setBounds(
        {
          ...R7,
          x: Math.round(t.x + (t.width - R7.width) / 2),
          y: Math.max(t.y, t.y + t.height - R7.height - L7),
        },
        !1,
      );
    }
    async createWindow() {
      try {
        let e = await this.windowManager.createWindow({
          title: `Dictation`,
          ...R7,
          appearance: `globalDictation`,
          show: !1,
          initialRoute: n.fa,
          focusable: !1,
        });
        return (
          (this.rendererReadyPromise = new Promise((e) => {
            this.rendererReadyResolve = e;
          })),
          e.setAlwaysOnTop(!0, `floating`),
          this.positionWindow(e),
          this.applyPointerInteractivity(e, !1),
          e
        );
      } catch (e) {
        return (
          r.r().warning(`Failed to create global dictation window`, {
            safe: {},
            sensitive: { error: e },
          }),
          null
        );
      }
    }
    async waitForRenderer(e) {
      this.rendererReadyWebContentsId !== e.webContents.id &&
        (await this.rendererReadyPromise);
    }
    markRendererReady(e) {
      ((this.rendererReadyWebContentsId = e),
        this.rendererReadyResolve?.(),
        (this.rendererReadyResolve = null));
    }
    applyPointerInteractivity(e, t) {
      if (t) {
        e.setIgnoreMouseEvents(!1);
        return;
      }
      e.setIgnoreMouseEvents(!0, { forward: !0 });
    }
  },
  B7 = {
    "global-dictation-enabled-changed": !0,
    "global-dictation-force-lock-changed": !0,
    "global-dictation-pet-renderer-ready": !0,
    "global-dictation-renderer-ready": !0,
    "global-dictation-stop-requested": !0,
    "global-dictation-pointer-interaction-changed": !0,
    "global-dictation-recording-stopped": !0,
    "global-dictation-dismiss": !0,
    "global-dictation-close": !0,
    "global-dictation-completed": !0,
    "global-dictation-record-history-item": !0,
    "global-dictation-failed": !0,
    "global-dictation-in-app-started": !0,
  },
  V7 = 150,
  H7 = class {
    isGateEnabled = !1;
    forceGlobalDictationLock;
    configuredHotkey;
    configuredToggleHotkey;
    keepVisible;
    registeredHotkey = null;
    registeredHotkeyRegistration = null;
    registeredToggleHotkey = null;
    registeredToggleHotkeyRegistration = null;
    activeSession = null;
    activeHotkeyReleaseWatcher = null;
    pendingInAppDictationStart = null;
    lifecycleLock = null;
    nativePetRendererWebContentsIds = new Set();
    windowController;
    history = [];
    hotkeyController = {
      getState: () => this.getHotkeyState(),
      setHotkey: (e) => this.setHotkey(e),
      setKeepVisible: (e) => this.setKeepVisible(e),
      setToggleHotkey: (e) => this.setToggleHotkey(e),
      syncCommandKeybindings: (e) => {
        this.syncCommandKeybindings(e);
      },
      getHistory: () => [...this.history],
      copyHistoryItem: (e) => this.copyHistoryItem(e),
    };
    constructor(e) {
      ((this.options = e),
        (this.windowController = new z7(e.windowManager)),
        (this.forceGlobalDictationLock =
          e.canForceGlobalDictationLock === !0 &&
          (e.forceGlobalDictationLock === !0 ||
            e.globalState.get(
              n.Gs.GLOBAL_DICTATION_FORCE_LOCK_DEBUG_ENABLED,
            ) === !0)),
        this.history.push(...o7()));
      let t = e.globalState.get(`globalDictationHotkey`),
        r = t != null && fA(t) == null ? t : null,
        i = e.globalState.get(`globalDictationToggleHotkey`),
        a = i != null && fA(i) == null ? i : null,
        o = $N(`globalDictationHold`),
        s = $N(`globalDictationToggle`);
      ((this.configuredHotkey = rP({ keymapHotkey: o, legacyHotkey: r })),
        (this.configuredToggleHotkey = rP({
          keymapHotkey: s,
          legacyHotkey: a,
        })),
        o.hasBinding &&
          e.globalState.set(
            `globalDictationHotkey`,
            this.configuredHotkey ?? void 0,
          ),
        s.hasBinding &&
          e.globalState.set(
            `globalDictationToggleHotkey`,
            this.configuredToggleHotkey ?? void 0,
          ),
        U7(this.configuredHotkey, this.configuredToggleHotkey) &&
          ((this.configuredToggleHotkey = null),
          e.globalState.set(`globalDictationToggleHotkey`, void 0)),
        (this.keepVisible =
          e.globalState.getStored(n.Gs.GLOBAL_DICTATION_KEEP_VISIBLE) ??
          this.hasConfiguredHotkey()),
        this.applyLifecycleWithWarning(`startup`));
    }
    getHotkeyController() {
      return this.hotkeyController;
    }
    async handleMessage(e, t) {
      if (!K7(t)) return !1;
      if (t.type === `global-dictation-enabled-changed`)
        return (this.setGateEnabled(t.enabled), !0);
      if (t.type === `global-dictation-force-lock-changed`)
        return (this.setForceGlobalDictationLock(t.enabled), !0);
      if (t.type === `global-dictation-pet-renderer-ready`)
        return (this.setNativePetRendererReady(e.sender.id, t.ready), !0);
      if (t.type === `global-dictation-renderer-ready`)
        return (this.windowController.setRendererReady(e.sender.id), !0);
      if (!this.isGateEnabled) return !0;
      let n = this.activeSession,
        r = n?.id ?? null;
      switch (t.type) {
        case `global-dictation-pointer-interaction-changed`:
          return (
            this.windowController.setPointerInteractive(
              e.sender.id,
              t.isInteractive,
            ),
            !0
          );
        case `global-dictation-stop-requested`:
          return (
            t.sessionId === r &&
              (n?.type === `overlay` ||
                (n?.type === `pet` && n.webContentsId === e.sender.id)) &&
              this.stopRecording(t.sessionId),
            !0
          );
        case `global-dictation-recording-stopped`:
          return (t.sessionId === r && this.clearHotkeyReleaseWatcher(), !0);
        case `global-dictation-dismiss`:
          return (
            t.sessionId === r &&
              (this.clearActiveSession(),
              this.clearHotkeyReleaseWatcher(),
              this.showIdleOrHide()),
            !0
          );
        case `global-dictation-close`:
          return (
            t.sessionId === r &&
              (this.clearActiveSession(),
              this.clearHotkeyReleaseWatcher(),
              this.windowController.close()),
            !0
          );
        case `global-dictation-completed`:
          return (
            t.sessionId === r &&
              (await this.handleTranscript(t.sessionId, t.text)),
            !0
          );
        case `global-dictation-record-history-item`:
          return (this.recordHistoryText((0, f.randomUUID)(), t.text), !0);
        case `global-dictation-failed`:
          return t.sessionId !== r ||
            (this.clearHotkeyReleaseWatcher(), t.stage === `transcription`)
            ? !0
            : (this.clearActiveSession(), this.showIdleOrHide(), !0);
        case `global-dictation-in-app-started`:
          return (
            t.sessionId === this.pendingInAppDictationStart?.sessionId &&
              this.pendingInAppDictationStart.complete(!0),
            !0
          );
      }
    }
    dispose() {
      this.deactivateLifecycle();
    }
    getHotkeyState() {
      let e = W7() && this.isGateEnabled;
      return {
        supported: e,
        configuredHotkey: e ? this.configuredHotkey : null,
        configuredToggleHotkey: e ? this.configuredToggleHotkey : null,
        keepVisible: this.keepVisible,
      };
    }
    setKeepVisible(e) {
      let t = e && this.hasConfiguredHotkey();
      return this.keepVisible === t
        ? this.getHotkeyState()
        : ((this.keepVisible = t),
          this.options.globalState.set(n.Gs.GLOBAL_DICTATION_KEEP_VISIBLE, t),
          t
            ? (this.applyLifecycleWithWarning(`keep-visible-change`),
              this.getHotkeyState())
            : (this.activeSession?.type !== `overlay` &&
                this.windowController.hide(),
              this.getHotkeyState()));
    }
    setHotkey(e) {
      return this.setHotkeyForMode(`hold`, e);
    }
    setToggleHotkey(e) {
      return this.setHotkeyForMode(`toggle`, e);
    }
    setHotkeyForMode(e, t) {
      if (!W7() || !this.isGateEnabled)
        return this.createMutationFailure(`Not supported.`);
      if (t != null) {
        let e = fA(t);
        if (e != null) return this.createMutationFailure(e);
        (iA(t), G7());
      }
      if (
        (e === `hold` && U7(t, this.configuredToggleHotkey)) ||
        (e === `toggle` && U7(this.configuredHotkey, t))
      )
        return this.createMutationFailure(
          `Hold and toggle dictation hotkeys must be different.`,
        );
      let r = this.configuredHotkey,
        i = this.configuredToggleHotkey,
        a = this.keepVisible,
        o = this.hasConfiguredHotkey();
      e === `hold`
        ? (this.configuredHotkey = t)
        : (this.configuredToggleHotkey = t);
      let s = this.hasConfiguredHotkey();
      !o && s ? (this.keepVisible = !0) : s || (this.keepVisible = !1);
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.configuredHotkey = r),
          (this.configuredToggleHotkey = i),
          (this.keepVisible = a),
          this.applyLifecycleWithWarning(`hotkey-rollback`),
          this.createMutationFailure(e instanceof Error ? e.message : String(e))
        );
      }
      return (
        this.options.globalState.set(
          `globalDictationHotkey`,
          this.configuredHotkey ?? void 0,
        ),
        this.options.globalState.set(
          `globalDictationToggleHotkey`,
          this.configuredToggleHotkey ?? void 0,
        ),
        this.keepVisible !== a &&
          this.options.globalState.set(
            n.Gs.GLOBAL_DICTATION_KEEP_VISIBLE,
            this.keepVisible,
          ),
        { success: !0, state: this.getHotkeyState() }
      );
    }
    createMutationFailure(e) {
      return { success: !1, error: e, state: this.getHotkeyState() };
    }
    applyLifecycleOrThrow() {
      if (!this.isGateEnabled || !W7()) {
        this.deactivateLifecycle();
        return;
      }
      if (
        this.configuredHotkey == null &&
        this.configuredToggleHotkey == null
      ) {
        this.deactivateLifecycle();
        return;
      }
      if (!this.ensureLifecycleLock())
        throw Error(
          `Global dictation is already active in another instance of ChatGPT.`,
        );
      (this.configuredHotkey == null
        ? this.unregisterHotkey()
        : this.registerHotkeyOrThrow(this.configuredHotkey),
        this.configuredToggleHotkey == null
          ? this.unregisterToggleHotkey()
          : this.registerToggleHotkeyOrThrow(this.configuredToggleHotkey),
        this.keepVisible && this.activeSession?.type !== `overlay`
          ? this.showIdleWindow()
          : this.windowController.prewarm());
    }
    applyLifecycleWithWarning(e) {
      try {
        this.applyLifecycleOrThrow();
      } catch (t) {
        (r.r().warning(`Failed to apply global dictation runtime state`, {
          safe: { reason: e },
          sensitive: { error: t },
        }),
          this.deactivateLifecycle());
      }
    }
    setGateEnabled(e) {
      this.isGateEnabled !== e &&
        ((this.isGateEnabled = e),
        this.applyLifecycleWithWarning(`gate-change`));
    }
    setForceGlobalDictationLock(e) {
      let t = this.options.canForceGlobalDictationLock === !0 && e;
      this.forceGlobalDictationLock !== t &&
        ((this.forceGlobalDictationLock = t),
        t || this.releaseLifecycleLock(),
        this.applyLifecycleWithWarning(`force-lock-change`));
    }
    syncCommandKeybindings({ holdHotkey: e, toggleHotkey: t }) {
      let n =
        e != null && e !== this.configuredHotkey
          ? e
          : t != null && t !== this.configuredToggleHotkey
            ? t
            : null;
      (n != null && (iA(n), G7()),
        this.options.globalState.set(`globalDictationHotkey`, e ?? void 0),
        this.options.globalState.set(
          `globalDictationToggleHotkey`,
          t ?? void 0,
        ),
        this.syncConfiguredHotkeys({ holdHotkey: e, toggleHotkey: t }));
    }
    syncConfiguredHotkeys({ holdHotkey: e, toggleHotkey: t }) {
      let r = this.hasConfiguredHotkey(),
        i = this.keepVisible;
      ((this.configuredHotkey = e),
        (this.configuredToggleHotkey = U7(e, t) ? null : t));
      let a = this.hasConfiguredHotkey();
      (!r && a ? (this.keepVisible = !0) : a || (this.keepVisible = !1),
        this.keepVisible !== i &&
          this.options.globalState.set(
            n.Gs.GLOBAL_DICTATION_KEEP_VISIBLE,
            this.keepVisible,
          ),
        this.applyLifecycleWithWarning(`command-keybinding-sync`));
    }
    hasConfiguredHotkey() {
      return (
        this.configuredHotkey != null || this.configuredToggleHotkey != null
      );
    }
    deactivateLifecycle() {
      (this.unregisterHotkey(),
        this.unregisterToggleHotkey(),
        this.activeSession != null && this.stopRecording(this.activeSession.id),
        this.clearHotkeyReleaseWatcher(),
        this.pendingInAppDictationStart?.complete(!1),
        this.clearActiveSession(),
        this.windowController.close(),
        this.releaseLifecycleLock());
    }
    ensureLifecycleLock() {
      return this.lifecycleLock == null
        ? ((this.lifecycleLock = m7({
            force: this.forceGlobalDictationLock,
            onLost: () => {
              ((this.lifecycleLock = null), this.deactivateLifecycle());
            },
          })),
          this.lifecycleLock != null)
        : !0;
    }
    releaseLifecycleLock() {
      (this.lifecycleLock?.dispose(), (this.lifecycleLock = null));
    }
    registerHotkeyOrThrow(e) {
      if (this.registeredHotkey === e) return;
      let t = this.registeredHotkey,
        n = eA(e, {
          onPressed: () => {
            this.handleHoldHotkeyPressed();
          },
          onReleased: () => {
            this.handleHoldHotkeyReleased();
          },
        });
      if (n == null)
        throw Error(`Unable to register global dictation hotkey: ${e}`);
      (t != null && this.registeredHotkeyRegistration?.unregister(),
        (this.registeredHotkey = e),
        (this.registeredHotkeyRegistration = n));
    }
    unregisterHotkey() {
      this.registeredHotkey != null &&
        (this.registeredHotkeyRegistration?.unregister(),
        (this.registeredHotkey = null),
        (this.registeredHotkeyRegistration = null));
    }
    registerToggleHotkeyOrThrow(e) {
      if (this.registeredToggleHotkey === e) return;
      let t = this.registeredToggleHotkey,
        n = eA(
          e,
          {
            onPressed: () => {
              this.handleToggleHotkeyPressed();
            },
          },
          { bareModifierTrigger: `release` },
        );
      if (n == null)
        throw Error(`Unable to register global dictation toggle hotkey: ${e}`);
      (t != null && this.registeredToggleHotkeyRegistration?.unregister(),
        (this.registeredToggleHotkey = e),
        (this.registeredToggleHotkeyRegistration = n));
    }
    unregisterToggleHotkey() {
      this.registeredToggleHotkey != null &&
        (this.registeredToggleHotkeyRegistration?.unregister(),
        (this.registeredToggleHotkey = null),
        (this.registeredToggleHotkeyRegistration = null));
    }
    async handleHoldHotkeyPressed() {
      if (this.activeSession != null) return;
      let e = this.windowController.getFocusedInAppWindow();
      (e != null && (await this.tryStartInAppSession(e, `hold`))) ||
        (await this.startSession(`hold`));
    }
    async handleToggleHotkeyPressed() {
      let e = this.activeSession;
      if (e != null) {
        e.mode === `toggle` && this.stopRecording(e.id);
        return;
      }
      let t = this.windowController.getFocusedInAppWindow();
      (t != null && (await this.tryStartInAppSession(t, `toggle`))) ||
        (await this.startSession(`toggle`));
    }
    async startSession(e) {
      if (this.activeSession != null) return;
      let t = (0, f.randomUUID)(),
        n = this.getVisibleNativePetWebContents();
      if (n != null) {
        ((this.activeSession = {
          type: `pet`,
          id: t,
          webContentsId: n.id,
          mode: e,
        }),
          e === `hold` && this.watchHoldHotkeyRelease(t),
          this.windowController.hide(),
          this.options.windowManager.sendMessageToWebContents(n, {
            type: `global-dictation-start`,
            sessionId: t,
          }));
        return;
      }
      this.activeSession = { type: `overlay`, id: t, mode: e };
      try {
        if (
          (e === `hold` && this.watchHoldHotkeyRelease(t),
          !(await this.windowController.showAndStart(t)))
        ) {
          (this.clearHotkeyReleaseWatcher(), this.clearActiveSession());
          return;
        }
        if (this.activeSession?.id !== t) return;
      } catch (e) {
        (this.clearHotkeyReleaseWatcher(),
          this.clearActiveSession(),
          r.r().warning(`Failed to open global dictation window`, {
            safe: {},
            sensitive: { error: e },
          }));
      }
    }
    async tryStartInAppSession(e, t) {
      let n = (0, f.randomUUID)();
      return (
        (this.activeSession = {
          type: `inApp`,
          id: n,
          webContentsId: e.webContents.id,
          mode: t,
        }),
        this.options.windowManager.sendMessageToWebContents(e.webContents, {
          type: `global-dictation-in-app-start`,
          sessionId: n,
        }),
        (await this.waitForInAppDictationStart(n))
          ? this.activeSession?.id === n
            ? (t === `hold` && this.watchHoldHotkeyRelease(n), !0)
            : !1
          : (this.activeSession?.id === n && this.clearActiveSession(), !1)
      );
    }
    waitForInAppDictationStart(e) {
      return new Promise((t) => {
        let n = setTimeout(() => {
          this.pendingInAppDictationStart?.complete(!1);
        }, V7);
        this.pendingInAppDictationStart = {
          sessionId: e,
          complete: (r) => {
            (this.pendingInAppDictationStart?.sessionId === e &&
              (this.pendingInAppDictationStart = null),
              clearTimeout(n),
              t(r));
          },
        };
      });
    }
    watchHoldHotkeyRelease(e) {
      if (this.registeredHotkeyRegistration?.handlesRelease) return;
      let t = this.registeredHotkey;
      if (t != null) {
        this.clearHotkeyReleaseWatcher();
        try {
          this.activeHotkeyReleaseWatcher = pA(t, () => {
            this.activeSession?.id === e && this.stopRecording(e);
          });
        } catch (t) {
          (r.r().warning(`Failed to watch global dictation hotkey release`, {
            safe: {},
            sensitive: { error: t },
          }),
            this.stopRecording(e));
        }
      }
    }
    stopRecording(e) {
      this.clearHotkeyReleaseWatcher();
      let t = this.activeSession;
      if (t?.type === `inApp`) {
        (this.options.windowManager.sendMessageToWebContentsId(
          t.webContentsId,
          { type: `global-dictation-in-app-stop`, sessionId: e },
        ),
          this.clearActiveSession());
        return;
      }
      if (t?.type === `pet`) {
        this.options.windowManager.sendMessageToWebContentsId(t.webContentsId, {
          type: `global-dictation-stop`,
          sessionId: e,
        });
        return;
      }
      this.windowController.sendStop(e);
    }
    handleHoldHotkeyReleased() {
      let e = this.activeSession;
      e == null || e.mode !== `hold` || this.stopRecording(e.id);
    }
    clearHotkeyReleaseWatcher() {
      this.activeHotkeyReleaseWatcher != null &&
        (this.activeHotkeyReleaseWatcher.dispose(),
        (this.activeHotkeyReleaseWatcher = null));
    }
    clearActiveSession() {
      this.activeSession = null;
    }
    async showIdleWindow() {
      this.showNativePetIdleIfAvailable() ||
        (await this.windowController.showIdle(
          this.configuredHotkey,
          this.configuredToggleHotkey,
        ));
    }
    showIdleOrHide() {
      if (!this.showNativePetIdleIfAvailable()) {
        if (this.keepVisible) {
          this.showIdleWindow();
          return;
        }
        this.windowController.hide();
      }
    }
    showNativePetIdleIfAvailable() {
      let e = this.getVisibleNativePetWebContents();
      return e == null
        ? !1
        : (this.options.windowManager.sendMessageToWebContents(e, {
            type: `global-dictation-idle`,
            configuredHotkey: this.configuredHotkey,
            configuredToggleHotkey: this.configuredToggleHotkey,
          }),
          this.windowController.hide(),
          !0);
    }
    setNativePetRendererReady(e, t) {
      (t
        ? this.nativePetRendererWebContentsIds.add(e)
        : this.nativePetRendererWebContentsIds.delete(e),
        this.activeSession ?? this.showIdleOrHide());
    }
    getVisibleNativePetWebContents() {
      let e = this.options.getVisibleNativePetWebContents?.() ?? null;
      return e != null && this.nativePetRendererWebContentsIds.has(e.id)
        ? e
        : null;
    }
    async handleTranscript(e, t) {
      (this.clearHotkeyReleaseWatcher(),
        this.clearActiveSession(),
        this.showIdleOrHide());
      let n = this.recordHistoryText(e, t);
      if (n != null)
        try {
          await A7(`${n} `);
        } catch (e) {
          r.r().warning(`Global dictation paste failed`, {
            safe: {},
            sensitive: { error: e },
          });
        }
    }
    recordHistoryText(e, t) {
      let n = t.trim();
      return n
        ? (this.recordHistory({ id: e, createdAtMs: Date.now(), text: n }), n)
        : null;
    }
    recordHistory(e) {
      (this.history.unshift(e), this.history.splice(10), s7(e));
    }
    copyHistoryItem(e) {
      let t = this.history.find((t) => t.id === e);
      return t == null || t.text.length === 0
        ? !1
        : (c.clipboard.writeText(t.text), !0);
    }
  };
function U7(e, t) {
  return e != null && t != null && e === t;
}
function W7() {
  return process.platform === `darwin` || process.platform === `win32`;
}
function G7() {
  process.platform === `darwin` &&
    c.systemPreferences.isTrustedAccessibilityClient(!0);
}
function K7(e) {
  return e.type in B7;
}
function q7({
  currentBounds: e,
  desiredComposerTopOnScreenPx: t,
  minimumComposerTopInsetPx: n,
  restingComposerTopInsetPx: r,
  workArea: i,
}) {
  let a = Math.min(n, r),
    o = i.y,
    s = i.y + i.height - e.height,
    c = J7(t, o + a, s + r),
    l = J7(r, Math.max(a, c - s), Math.min(r, c - o));
  return { composerTopInsetPx: l, windowBounds: { ...e, y: c - l } };
}
function J7(e, t, n) {
  return Math.max(t, Math.min(e, n));
}
var Y7 = 470,
  X7 = Y7,
  Z7 = 290,
  ste = Y7,
  cte = 640,
  Q7 = 400,
  $7 = 400,
  lte = 52,
  ute = 110,
  e9 = 1200,
  dte = class {
    logger = r.i(`hotkey-window-controller`);
    configuredWindowIds = new Set();
    mode = `hidden`;
    isDisposed = !1;
    lastVisibleSurface = `home`;
    homeWindow = null;
    homeWindowPromise = null;
    threadWindow = null;
    lastDetailRoute = null;
    lastDetailRouteState = void 0;
    threadWindowPromise = null;
    threadSize = { width: ste, height: cte };
    transitionInFlight = null;
    windowGeneration = 0;
    homePointerInteractive = !0;
    homeMousePassthroughEnabled = !1;
    homeComposerTopInsetPx = null;
    homeLayoutMetrics = null;
    homeDragState = null;
    primaryWindowMayBeDismissFocusFallback = !1;
    constructor(e, t = () => !0, n = {}) {
      ((this.windowManager = e),
        (this.shouldRetainWindows = t),
        (this.callbacks = n));
    }
    async toggleHotkey() {
      if (this.transitionInFlight) return;
      if (this.mode === `hidden`) {
        await this.showLastVisibleSurface();
        return;
      }
      let e = this.getWindowForCurrentMode();
      if (e != null && !e.isFocused()) {
        let t = this.lastDetailRoute;
        (this.mode === `threadVisible` &&
          t != null &&
          e === this.threadWindow &&
          this.navigateToDetailRoute(e, t, fte(t, this.lastDetailRouteState)),
          this.showAndFocusFromCurrentFocus(e));
        return;
      }
      this.hideAll();
    }
    async show() {
      if (this.transitionInFlight) return;
      let e = this.getWindowForCurrentMode();
      if (e != null) {
        this.showAndFocusFromCurrentFocus(e);
        return;
      }
      await this.showLastVisibleSurface();
    }
    prewarm() {
      this.prewarmWindows();
    }
    async openThread(e) {
      this.transitionInFlight || (await this.openDetailRoute(n.ga(e)));
    }
    async openDetailRoute(e, t) {
      if (!this.transitionInFlight) {
        if (
          ((this.lastDetailRoute = e),
          (this.lastDetailRouteState = t),
          this.mode === `hidden`)
        ) {
          await this.showThread(e, t);
          return;
        }
        if (this.mode === `threadVisible`) {
          let n = this.windowGeneration,
            r = await this.ensureThreadWindow(e);
          if (!this.isWindowGenerationCurrent(n)) return;
          (this.navigateToDetailRoute(r, e, t),
            this.showAndFocusFromCurrentFocus(r),
            (this.lastVisibleSurface = `thread`));
          return;
        }
        this.mode === `homeVisible` && (await this.startExpandTransition(e, t));
      }
    }
    async openHome(e = null) {
      if (!this.transitionInFlight) {
        if (this.mode === `hidden`) {
          await this.showHome(e);
          return;
        }
        if (this.mode === `homeVisible`) {
          let t = this.windowGeneration,
            r = await this.ensureHomeWindow();
          if (!this.isWindowGenerationCurrent(t)) return;
          (this.applyHotkeyWindowWindowPolicy(r),
            this.navigateToRoute(r, n.pa, n9(e)),
            this.showAndFocusFromCurrentFocus(r),
            (this.lastVisibleSurface = `home`),
            this.applyHomeInteractivityPolicy());
          return;
        }
        this.mode === `threadVisible` &&
          (await this.startCollapseTransition(e));
      }
    }
    async collapseToHome() {
      if (!this.transitionInFlight) {
        if (this.mode === `hidden`) {
          await this.showHome();
          return;
        }
        this.mode === `threadVisible` && (await this.startCollapseTransition());
      }
    }
    setHomePointerInteraction(e, t) {
      this.isHomeOrigin(e) &&
        (this.transitionInFlight ||
          (this.homePointerInteractive !== t.isInteractive &&
            ((this.homePointerInteractive = t.isInteractive),
            this.applyHomeInteractivityPolicy())));
    }
    setHomeLayout(e, t) {
      if (!this.isHomeOrigin(e) || !this.homeWindow) return;
      let n = {
        minimumComposerTopInsetPx: Math.max(
          0,
          Math.round(t.minimumComposerTopInsetPx),
        ),
        restingComposerTopInsetPx: Math.max(
          0,
          Math.round(t.restingComposerTopInsetPx),
        ),
      };
      if (
        this.homeLayoutMetrics?.minimumComposerTopInsetPx ===
          n.minimumComposerTopInsetPx &&
        this.homeLayoutMetrics.restingComposerTopInsetPx ===
          n.restingComposerTopInsetPx
      )
        return;
      let r = this.getWindowLayoutBounds(this.homeWindow),
        i = this.homeComposerTopInsetPx ?? n.restingComposerTopInsetPx;
      ((this.homeLayoutMetrics = n),
        this.layoutHomeForComposerTop(
          r.y + i,
          c.screen.getDisplayMatching(r).workArea,
        ));
    }
    startHomeDrag(e, t) {
      if (!this.isHomeOrigin(e) || !this.homeWindow) return;
      let n = this.homeLayoutMetrics;
      if (n == null) return;
      let r = this.homeComposerTopInsetPx ?? n.restingComposerTopInsetPx;
      this.homeDragState = {
        pointerComposerY: t.pointerWindowY - r,
        pointerWindowX: t.pointerWindowX,
      };
    }
    moveHomeDrag(e) {
      if (
        !this.isHomeOrigin(e) ||
        !this.homeWindow ||
        this.homeDragState == null
      )
        return;
      let t = c.screen.getCursorScreenPoint(),
        n = c.screen.getDisplayNearestPoint(t).workArea;
      this.layoutHomeForComposerTop(
        Math.round(t.y - this.homeDragState.pointerComposerY),
        n,
        Math.round(t.x - this.homeDragState.pointerWindowX),
      );
    }
    endHomeDrag(e) {
      this.isHomeOrigin(e) &&
        ((this.homeDragState = null), this.applyHomeInteractivityPolicy());
    }
    layoutHomeForComposerTop(e, t, n) {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) return;
      let r = this.homeLayoutMetrics;
      if (r == null) return;
      let i = this.getWindowLayoutBounds(this.homeWindow),
        { composerTopInsetPx: a, windowBounds: o } = q7({
          currentBounds: n == null ? i : { ...i, x: n },
          desiredComposerTopOnScreenPx: e,
          minimumComposerTopInsetPx: r.minimumComposerTopInsetPx,
          restingComposerTopInsetPx: r.restingComposerTopInsetPx,
          workArea: t,
        });
      (pte(i, o) || this.setWindowLayoutBounds(this.homeWindow, o),
        this.homeComposerTopInsetPx !== a &&
          ((this.homeComposerTopInsetPx = a),
          this.windowManager.sendMessageToWindow(this.homeWindow, {
            type: `hotkey-window-home-composer-position-changed`,
            topInsetPx: a,
          })));
    }
    handleTransitionDone(e, t) {
      let n = this.transitionInFlight;
      if (n && n.id === t.transitionId) {
        if (n.stage === `awaiting-source-raised`) {
          if (t.step !== `raised` || e.id !== n.sourceWebContentsId) return;
          (this.clearTransitionTimeout(),
            this.swapWindowsAfterRaise(n),
            (n.stage = `awaiting-destination-lowered`),
            this.startTransitionTimeout(n));
          let r = this.getWindowByWebContentsId(n.destinationWebContentsId);
          r && this.sendTransition(r, n.id, `lower-curtain`);
          return;
        }
        n.stage === `awaiting-destination-lowered` &&
          t.step === `lowered` &&
          e.id === n.destinationWebContentsId &&
          this.finishTransition(n);
      }
    }
    hideAll() {
      (this.clearTransitionTimeout(),
        (this.transitionInFlight = null),
        this.resetHomeInteractivity());
      let e =
        process.platform === `darwin` &&
        !this.primaryWindowMayBeDismissFocusFallback
          ? this.windowManager.getPrimaryWindows()
          : [];
      for (let t of e) t.setFocusable(!1);
      try {
        (this.homeWindow &&
          !this.homeWindow.isDestroyed() &&
          this.homeWindow.hide(),
          this.threadWindow &&
            !this.threadWindow.isDestroyed() &&
            this.threadWindow.hide(),
          (this.mode = `hidden`));
      } finally {
        for (let t of e) t.isDestroyed() || t.setFocusable(!0);
      }
    }
    closeAllWindows() {
      (this.clearTransitionTimeout(),
        (this.transitionInFlight = null),
        this.resetHomeInteractivity(),
        (this.windowGeneration += 1));
      let e = this.homeWindow,
        t = this.threadWindow;
      ((this.homeWindow = null),
        (this.homeWindowPromise = null),
        (this.homeComposerTopInsetPx = null),
        (this.homeLayoutMetrics = null),
        (this.homeDragState = null),
        (this.threadWindow = null),
        (this.threadWindowPromise = null),
        (this.mode = `hidden`),
        this.closeWindow(e),
        this.closeWindow(t));
    }
    dispose() {
      ((this.isDisposed = !0),
        this.closeAllWindows(),
        (this.lastDetailRoute = null),
        (this.lastDetailRouteState = void 0));
    }
    async prewarmWindows() {
      let e = this.windowGeneration,
        t = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(e)) return;
      let n = await this.ensureThreadWindow();
      this.isWindowGenerationCurrent(e) &&
        (t.isDestroyed() || t.hide(), n.isDestroyed() || n.hide());
    }
    async showHome(e = null, t = !1) {
      let r = this.windowGeneration;
      this.homePointerInteractive = !0;
      let i = this.homeWindow != null && !this.homeWindow.isDestroyed(),
        a = this.threadWindow != null && !this.threadWindow.isDestroyed(),
        o = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(r)) return;
      if (a && this.lastVisibleSurface === `thread`) this.alignHomeToThread();
      else if (!i) {
        let e = this.computeHomeBounds(this.getTargetDisplay());
        this.setWindowLayoutBounds(o, e);
      }
      (this.applyHotkeyWindowWindowPolicy(o),
        (!t || !i) && this.navigateToRoute(o, n.pa, n9(e)),
        this.showAndFocusFromCurrentFocus(o),
        (this.mode = `homeVisible`),
        (this.lastVisibleSurface = `home`),
        this.applyHomeInteractivityPolicy());
      let s = await this.ensureThreadWindow();
      this.isWindowGenerationCurrent(r) && (this.alignThreadToHome(), s.hide());
    }
    async showThread(e, t) {
      let n = this.windowGeneration,
        r = e ?? this.lastDetailRoute,
        i = t ?? this.lastDetailRouteState;
      if (!r) return !1;
      let a = this.threadWindow != null && !this.threadWindow.isDestroyed(),
        o = await this.ensureThreadWindow(r);
      return this.isWindowGenerationCurrent(n)
        ? (a ||
            this.setWindowLayoutBounds(
              o,
              this.computeThreadBounds(this.getTargetDisplay()),
            ),
          this.homeWindow &&
            !this.homeWindow.isDestroyed() &&
            this.lastVisibleSurface === `home` &&
            this.alignThreadToHome(),
          this.applyHotkeyWindowWindowPolicy(o),
          this.navigateToDetailRoute(o, r, i),
          this.showAndFocusFromCurrentFocus(o),
          this.homeWindow &&
            !this.homeWindow.isDestroyed() &&
            this.homeWindow.hide(),
          this.resetHomeInteractivity(),
          (this.mode = `threadVisible`),
          (this.lastVisibleSurface = `thread`),
          !0)
        : !1;
    }
    async showLastVisibleSurface() {
      (this.lastVisibleSurface === `thread` && (await this.showThread())) ||
        (await this.showHome(null, !0));
    }
    async startExpandTransition(e, t) {
      let n = this.windowGeneration,
        r = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      let i = await this.ensureThreadWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      ((this.lastDetailRoute = e),
        (this.lastDetailRouteState = t),
        this.alignThreadToHome(),
        this.applyHotkeyWindowWindowPolicy(r),
        this.applyHotkeyWindowWindowPolicy(i),
        this.navigateToDetailRoute(i, e, t),
        this.resetHomeInteractivity());
      let a = this.beginTransition({
        kind: `expand`,
        sourceWindow: r,
        destinationWindow: i,
      });
      (this.sendTransition(i, a, `raise-curtain`),
        this.sendTransition(r, a, `raise-curtain`));
    }
    async startCollapseTransition(e = null) {
      let t = this.windowGeneration,
        r = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(t)) return;
      let i = await this.ensureThreadWindow();
      if (!this.isWindowGenerationCurrent(t)) return;
      (this.alignHomeToThread(),
        this.applyHotkeyWindowWindowPolicy(r),
        this.applyHotkeyWindowWindowPolicy(i),
        this.navigateToRoute(r, n.pa, n9(e)),
        this.resetHomeInteractivity());
      let a = this.beginTransition({
        kind: `collapse`,
        sourceWindow: i,
        destinationWindow: r,
      });
      (this.sendTransition(r, a, `raise-curtain`),
        this.sendTransition(i, a, `raise-curtain`));
    }
    beginTransition({ kind: e, sourceWindow: t, destinationWindow: n }) {
      let r = (0, f.randomUUID)(),
        i = setTimeout(() => {
          this.handleTransitionTimeout(r);
        }, e9);
      return (
        i.unref(),
        (this.transitionInFlight = {
          id: r,
          kind: e,
          stage: `awaiting-source-raised`,
          sourceWebContentsId: t.webContents.id,
          destinationWebContentsId: n.webContents.id,
          timeout: i,
        }),
        r
      );
    }
    handleTransitionTimeout(e) {
      let t = this.transitionInFlight;
      !t ||
        t.id !== e ||
        (this.logger.warning(
          `Hotkey Window transition timed out; forcing completion`,
          {
            safe: { transitionId: e, stage: t.stage, kind: t.kind },
            sensitive: {},
          },
        ),
        this.swapWindowsAfterRaise(t),
        this.finishTransition(t));
    }
    startTransitionTimeout(e) {
      (this.clearTransitionTimeout(),
        (e.timeout = setTimeout(() => {
          this.handleTransitionTimeout(e.id);
        }, e9)),
        e.timeout.unref());
    }
    clearTransitionTimeout() {
      this.transitionInFlight && clearTimeout(this.transitionInFlight.timeout);
    }
    swapWindowsAfterRaise(e) {
      let t = this.getWindowByWebContentsId(e.sourceWebContentsId),
        n = this.getWindowByWebContentsId(e.destinationWebContentsId);
      n &&
        (this.applyHotkeyWindowWindowPolicy(n),
        this.showAndFocus(n),
        t && !t.isDestroyed() && t.hide());
    }
    finishTransition(e) {
      this.clearTransitionTimeout();
      let t = this.getWindowByWebContentsId(e.sourceWebContentsId),
        n = this.getWindowByWebContentsId(e.destinationWebContentsId);
      (t && this.sendTransition(t, e.id, `commit`),
        n && this.sendTransition(n, e.id, `commit`),
        (this.transitionInFlight = null),
        (this.mode = e.kind === `expand` ? `threadVisible` : `homeVisible`),
        (this.lastVisibleSurface = e.kind === `expand` ? `thread` : `home`),
        this.applyHomeInteractivityPolicy());
    }
    async ensureHomeWindow() {
      if (this.homeWindow && !this.homeWindow.isDestroyed())
        return this.homeWindow;
      if (this.homeWindowPromise != null) return this.homeWindowPromise;
      let e = this.windowGeneration,
        t = this.windowManager
          .createWindow({
            title: c.app.getName(),
            width: X7,
            height: Z7,
            appearance: `hotkeyWindowHome`,
            show: !1,
            initialRoute: `/hotkey-window`,
          })
          .then((t) =>
            this.isDisposed || e !== this.windowGeneration
              ? (this.closeWindow(t), t)
              : (t.setMenuBarVisibility(!1),
                t.on(`focus`, () => {
                  this.callbacks.onWindowFocused?.(t.webContents.id);
                }),
                t.on(`blur`, () => {
                  this.handleHotkeyWindowBlurred(t.webContents.id);
                }),
                t.on(`closed`, () => {
                  if (
                    (this.configuredWindowIds.delete(t.id),
                    (this.homeMousePassthroughEnabled = !1),
                    (this.homePointerInteractive = !0),
                    this.homeWindow === t &&
                      ((this.homeWindow = null),
                      (this.homeComposerTopInsetPx = null),
                      (this.homeLayoutMetrics = null),
                      (this.homeDragState = null)),
                    !this.shouldRetainWindows())
                  ) {
                    this.closeAllWindows();
                    return;
                  }
                  this.mode !== `threadVisible` && (this.mode = `hidden`);
                }),
                (this.homeWindow = t),
                t),
          )
          .finally(() => {
            this.homeWindowPromise === t && (this.homeWindowPromise = null);
          });
      return ((this.homeWindowPromise = t), t);
    }
    async ensureThreadWindow(e) {
      if (this.threadWindow && !this.threadWindow.isDestroyed())
        return this.threadWindow;
      if (this.threadWindowPromise != null) return this.threadWindowPromise;
      let t = this.windowGeneration,
        n = this.windowManager
          .createWindow({
            title: c.app.getName(),
            width: this.threadSize.width,
            height: this.threadSize.height,
            appearance: `hotkeyWindowThread`,
            show: !1,
            initialRoute: e ?? `/hotkey-window`,
          })
          .then((e) =>
            this.isDisposed || t !== this.windowGeneration
              ? (this.closeWindow(e), e)
              : (e.setMenuBarVisibility(!1),
                this.setWindowLayoutBounds(
                  e,
                  this.computeThreadBounds(this.getTargetDisplay()),
                ),
                e.setMinimumSize(Q7, $7),
                e.on(`resize`, () => {
                  if (e.isDestroyed()) return;
                  let t = this.getWindowLayoutBounds(e);
                  this.threadSize = {
                    width: Math.max(t.width, Q7),
                    height: Math.max(t.height, $7),
                  };
                }),
                e.on(`focus`, () => {
                  this.callbacks.onWindowFocused?.(e.webContents.id);
                }),
                e.on(`blur`, () => {
                  this.handleHotkeyWindowBlurred(e.webContents.id);
                }),
                e.on(`closed`, () => {
                  if (
                    (this.configuredWindowIds.delete(e.id),
                    this.threadWindow === e && (this.threadWindow = null),
                    !this.shouldRetainWindows())
                  ) {
                    this.closeAllWindows();
                    return;
                  }
                  this.mode !== `homeVisible` && (this.mode = `hidden`);
                }),
                (this.threadWindow = e),
                e),
          )
          .finally(() => {
            this.threadWindowPromise === n && (this.threadWindowPromise = null);
          });
      return ((this.threadWindowPromise = n), n);
    }
    applyHotkeyWindowWindowPolicy(e) {
      e.isDestroyed() ||
        (this.configuredWindowIds.has(e.id) ||
          (this.configuredWindowIds.add(e.id),
          process.platform === `darwin`
            ? e.setVisibleOnAllWorkspaces(!0, {
                visibleOnFullScreen: !0,
                skipTransformProcessType: !0,
              })
            : e.setVisibleOnAllWorkspaces(!0)),
        e.moveTop());
    }
    showAndFocus(e) {
      e.isDestroyed() ||
        (e.isMinimized() && e.restore(),
        this.applyHotkeyWindowWindowPolicy(e),
        e.show(),
        process.platform === `darwin` && c.app.focus({ steal: !0 }),
        e.focus());
    }
    showAndFocusFromCurrentFocus(e) {
      ((this.primaryWindowMayBeDismissFocusFallback =
        this.primaryWindowIsFocused()),
        this.showAndFocus(e));
    }
    handleHotkeyWindowBlurred(e) {
      (this.transitionInFlight ||
        (this.primaryWindowMayBeDismissFocusFallback =
          this.primaryWindowIsFocused()),
        this.callbacks.onWindowBlurred?.(e));
    }
    primaryWindowIsFocused() {
      return this.windowManager.getPrimaryWindows().some((e) => e.isFocused());
    }
    getVisibleWindowWebContentsId() {
      return this.getWindowForCurrentMode()?.webContents.id ?? null;
    }
    getTargetDisplay() {
      let e = c.screen.getCursorScreenPoint();
      return c.screen.getDisplayNearestPoint(e);
    }
    computeHomeBounds(e) {
      let t = e.workArea,
        n = Math.min(X7, t.width),
        r = Math.min(Z7, t.height),
        i = this.computeThreadBounds(e);
      return {
        x: Math.round(t.x + (t.width - n) / 2),
        y: r9(Math.round(i.y + i.height - r), t.y, t.y + t.height - r),
        width: n,
        height: r,
      };
    }
    computeThreadBounds(e) {
      let t = e.workArea,
        n = Math.min(Math.max(this.threadSize.width, Q7), t.width),
        r = Math.min(this.threadSize.height, t.height),
        i = Math.round(t.x + (t.width - n) / 2),
        a = Math.round(t.y + lte);
      return {
        x: r9(i, t.x, t.x + t.width - n),
        y: r9(a, t.y, t.y + t.height - r),
        width: n,
        height: r,
      };
    }
    alignThreadToHome() {
      if (
        !this.homeWindow ||
        this.homeWindow.isDestroyed() ||
        !this.threadWindow ||
        this.threadWindow.isDestroyed()
      )
        return;
      let e = this.getWindowLayoutBounds(this.homeWindow),
        t = this.getWindowLayoutBounds(this.threadWindow),
        n = c.screen.getDisplayMatching(e).workArea,
        r = Math.round(e.x + (e.width - t.width) / 2),
        i = Math.round(e.y + e.height - t.height),
        a = r9(r, n.x, n.x + n.width - t.width),
        o = r9(i, n.y, n.y + n.height - t.height);
      this.setWindowLayoutPosition(this.threadWindow, a, o);
    }
    alignHomeToThread() {
      if (
        !this.homeWindow ||
        this.homeWindow.isDestroyed() ||
        !this.threadWindow ||
        this.threadWindow.isDestroyed()
      )
        return;
      let e = this.getWindowLayoutBounds(this.homeWindow),
        t = this.getWindowLayoutBounds(this.threadWindow),
        n = Math.max(t.width, Q7),
        r = Math.round(t.x + (t.width - n) / 2),
        i = Math.round(t.y + t.height - e.height);
      (e.x === r && e.y === i && e.width === n) ||
        this.setWindowLayoutBounds(this.homeWindow, {
          ...e,
          x: r,
          y: i,
          width: n,
        });
    }
    getWindowLayoutBounds(e) {
      return process.platform === `win32`
        ? e.getContentBounds()
        : e.getBounds();
    }
    setWindowLayoutBounds(e, t) {
      if (!e.isDestroyed()) {
        if (process.platform === `win32`) {
          e.setContentBounds(t, !1);
          return;
        }
        e.setBounds(t, !1);
      }
    }
    setWindowLayoutPosition(e, t, n) {
      if (e.isDestroyed()) return;
      let r = this.getWindowLayoutBounds(e);
      (r.x === t && r.y === n) ||
        this.setWindowLayoutBounds(e, { ...r, x: t, y: n });
    }
    closeWindow(e) {
      !e || e.isDestroyed() || e.close();
    }
    isWindowGenerationCurrent(e) {
      return !this.isDisposed && e === this.windowGeneration;
    }
    resetHomeInteractivity() {
      ((this.homeDragState = null),
        (this.homePointerInteractive = !0),
        this.disableHomeMousePassthrough());
    }
    disableHomeMousePassthrough() {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) {
        this.homeMousePassthroughEnabled = !1;
        return;
      }
      this.homeMousePassthroughEnabled &&
        ((this.homeMousePassthroughEnabled = !1),
        this.homeWindow.setIgnoreMouseEvents(!1));
    }
    applyHomeInteractivityPolicy() {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) {
        this.homeMousePassthroughEnabled = !1;
        return;
      }
      if (this.transitionInFlight || this.mode !== `homeVisible`) {
        this.disableHomeMousePassthrough();
        return;
      }
      let e = !this.homePointerInteractive && this.homeDragState == null;
      if (this.homeMousePassthroughEnabled !== e) {
        if (((this.homeMousePassthroughEnabled = e), e)) {
          this.homeWindow.setIgnoreMouseEvents(!0, { forward: !0 });
          return;
        }
        this.homeWindow.setIgnoreMouseEvents(!1);
      }
    }
    getWindowByWebContentsId(e) {
      return this.homeWindow &&
        !this.homeWindow.isDestroyed() &&
        this.homeWindow.webContents.id === e
        ? this.homeWindow
        : this.threadWindow &&
            !this.threadWindow.isDestroyed() &&
            this.threadWindow.webContents.id === e
          ? this.threadWindow
          : null;
    }
    navigateToRoute(e, t, n) {
      this.windowManager.sendMessageToWindow(e, {
        type: `navigate-to-route`,
        path: t,
        state: n,
      });
    }
    navigateToDetailRoute(e, t, r) {
      this.navigateToRoute(e, t, r ?? (n.ba(t) == null ? void 0 : t9()));
    }
    sendTransition(e, t, n) {
      this.windowManager.sendMessageToWindow(e, {
        type: `hotkey-window-transition`,
        transitionId: t,
        step: n,
        durationMs: ute,
      });
    }
    isHomeOrigin(e) {
      return (
        this.homeWindow != null &&
        !this.homeWindow.isDestroyed() &&
        this.homeWindow.webContents.id === e.id
      );
    }
    getWindowForCurrentMode() {
      let e =
        this.mode === `homeVisible`
          ? this.homeWindow
          : this.mode === `threadVisible`
            ? this.threadWindow
            : null;
      return e == null || e.isDestroyed() ? null : e;
    }
  };
function t9() {
  return { focusComposerNonce: Date.now() };
}
function n9(e) {
  return { focusComposerNonce: Date.now(), prefillCwd: e ?? void 0 };
}
function fte(e, t) {
  return n.ba(e) == null
    ? e === n.ma
      ? n9(
          typeof t == `object` &&
            t &&
            `prefillCwd` in t &&
            typeof t.prefillCwd == `string`
            ? t.prefillCwd
            : null,
        )
      : t
    : t9();
}
function r9(e, t, n) {
  return e < t ? t : e > n ? n : e;
}
function pte(e, t) {
  return (
    e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height
  );
}
var mte = 300,
  hte = class {
    hotkeyWindowController = null;
    isHotkeyWindowGateEnabled = !1;
    isHotkeyWindowActive = !1;
    devHotkeyWindowHotkeyOverrideEnabled = !1;
    registeredHotkeyWindowHotkey = null;
    registeredHotkeyWindowHotkeyRegistration = null;
    configuredHotkeyWindowHotkey;
    lastBlurredWindowWebContentsId = null;
    lastBlurredAtMs = 0;
    constructor(e) {
      this.options = e;
      let t = e.globalState.get(`hotkeyWindowHotkey`),
        n = t != null && nA(t) == null ? t : null,
        r = $N(`hotkeyWindow`);
      ((this.configuredHotkeyWindowHotkey = rP({
        keymapHotkey: r,
        legacyHotkey: n,
      })),
        r.hasBinding &&
          e.globalState.set(
            `hotkeyWindowHotkey`,
            this.configuredHotkeyWindowHotkey ?? void 0,
          ));
    }
    openFromRenderer(e, t) {
      return (
        r.r().info(`hotkey_window_open_requested`, {
          safe: {
            threadId: n.ba(t.path),
            isGateEnabled: this.isHotkeyWindowGateEnabled,
            isValidRoute: n.Sa(t.path),
            ...this.options.windowManager.getRendererWindowLogFields(e),
          },
          sensitive: { routePath: t.path },
        }),
        this.open(t)
      );
    }
    handleTransitionDone(e, t) {
      this.hotkeyWindowController?.handleTransitionDone(e, t);
    }
    handleHomePointerInteractionChanged(e, t) {
      this.hotkeyWindowController?.setHomePointerInteraction(e, t);
    }
    handleHomeLayoutChanged(e, t) {
      this.hotkeyWindowController?.setHomeLayout(e, t);
    }
    handleHomeDragStart(e, t) {
      this.hotkeyWindowController?.startHomeDrag(e, t);
    }
    handleHomeDragMove(e) {
      this.hotkeyWindowController?.moveHomeDrag(e);
    }
    handleHomeDragEnd(e) {
      this.hotkeyWindowController?.endHomeDrag(e);
    }
    getState() {
      return this.getHotkeyWindowHotkeyState();
    }
    async open({ path: e, prefillCwd: t }) {
      if (!(!this.isHotkeyWindowGateEnabled || !n.Sa(e))) {
        if (e === `/hotkey-window`) {
          if (this.configuredHotkeyWindowHotkey == null) {
            await this.ensureHotkeyWindowController().openDetailRoute(
              n.ya(!1),
              i9(t ?? null),
            );
            return;
          }
          await this.ensureHotkeyWindowController().openHome(t ?? null);
          return;
        }
        await this.ensureHotkeyWindowController().openDetailRoute(
          e,
          e === n.ya(!1) ? i9(t ?? null) : void 0,
        );
      }
    }
    async collapseToHome() {
      if (this.isHotkeyWindowGateEnabled) {
        if (this.configuredHotkeyWindowHotkey == null) {
          await this.getHotkeyWindowController()?.openDetailRoute(
            n.ya(!1),
            i9(null),
          );
          return;
        }
        await this.getHotkeyWindowController()?.collapseToHome();
      }
    }
    dismiss() {
      this.getHotkeyWindowController()?.hideAll();
    }
    async setHotkey(e) {
      let t = this.setHotkeyWindowHotkey(e);
      return (
        t.success &&
          (await n.Yt({
            commandId: `hotkeyWindow`,
            update:
              e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        t
      );
    }
    setDevOverrideEnabled(e) {
      return this.setHotkeyWindowDevHotkeyOverride(e);
    }
    syncCommandKeybinding(e) {
      (e != null && e !== this.configuredHotkeyWindowHotkey && rA(e),
        (this.configuredHotkeyWindowHotkey = e),
        this.options.globalState.set(`hotkeyWindowHotkey`, e ?? void 0),
        this.applyLifecycleWithWarning(`command-keybinding-sync`));
    }
    dispose() {
      this.deactivateLifecycle();
    }
    hide() {
      this.getHotkeyWindowController()?.hideAll();
    }
    toggleVisibility() {
      if (!this.isHotkeyWindowGateEnabled) return !1;
      let e = this.ensureHotkeyWindowController();
      return this.shouldHideHotkeyWindowOnToggle(e)
        ? (e.hideAll(), !0)
        : (e.toggleHotkey(), !0);
    }
    show() {
      return this.isHotkeyWindowGateEnabled
        ? (this.ensureHotkeyWindowController().show(), !0)
        : !1;
    }
    ensureHotkeyWindowController() {
      return (
        (this.hotkeyWindowController ??= new dte(
          this.options.windowManager,
          () => this.configuredHotkeyWindowHotkey != null,
          {
            onWindowFocused: () => {
              this.clearRecentBlur();
            },
            onWindowBlurred: (e) => {
              this.recordBlur(e);
            },
          },
        )),
        this.hotkeyWindowController
      );
    }
    isHotkeyWindowEffectivelyEnabled() {
      return (
        this.isHotkeyWindowGateEnabled &&
        this.configuredHotkeyWindowHotkey != null &&
        (!this.options.isDevMode || this.devHotkeyWindowHotkeyOverrideEnabled)
      );
    }
    getHotkeyWindowHotkeyState() {
      return {
        supported: !0,
        configuredHotkey: this.configuredHotkeyWindowHotkey,
        isGateEnabled: this.isHotkeyWindowGateEnabled,
        isDevMode: this.options.isDevMode,
        isDevOverrideEnabled: this.devHotkeyWindowHotkeyOverrideEnabled,
        isActive: this.isHotkeyWindowActive,
      };
    }
    createHotkeyWindowMutationFailure(e) {
      return {
        success: !1,
        error: e,
        state: this.getHotkeyWindowHotkeyState(),
      };
    }
    handleHotkeyWindowHotkeyPressed() {
      this.isHotkeyWindowActive &&
        this.ensureHotkeyWindowController().toggleHotkey();
    }
    registerHotkeyWindowHotkeyOrThrow(e) {
      if (this.registeredHotkeyWindowHotkey === e) return;
      let t = this.registeredHotkeyWindowHotkey,
        n = eA(e, {
          onPressed: () => {
            this.handleHotkeyWindowHotkeyPressed();
          },
        });
      if (n == null)
        throw Error(`Unable to register hotkey window hotkey: ${e}`);
      (t != null && this.registeredHotkeyWindowHotkeyRegistration?.unregister(),
        (this.registeredHotkeyWindowHotkey = e),
        (this.registeredHotkeyWindowHotkeyRegistration = n));
    }
    unregisterHotkeyWindowHotkey() {
      this.registeredHotkeyWindowHotkey &&
        (this.registeredHotkeyWindowHotkeyRegistration?.unregister(),
        (this.registeredHotkeyWindowHotkey = null),
        (this.registeredHotkeyWindowHotkeyRegistration = null));
    }
    deactivateLifecycle() {
      (this.unregisterHotkeyWindowHotkey(),
        this.hotkeyWindowController?.dispose(),
        (this.hotkeyWindowController = null),
        (this.isHotkeyWindowActive = !1));
    }
    applyLifecycleOrThrow() {
      if (!this.isHotkeyWindowEffectivelyEnabled()) {
        this.deactivateLifecycle();
        return;
      }
      if (this.configuredHotkeyWindowHotkey == null)
        throw Error(`Hotkey Window hotkey is not configured.`);
      (this.registerHotkeyWindowHotkeyOrThrow(
        this.configuredHotkeyWindowHotkey,
      ),
        (this.isHotkeyWindowActive = !0),
        this.ensureHotkeyWindowController().prewarm());
    }
    applyLifecycleWithWarning(e) {
      try {
        this.applyLifecycleOrThrow();
      } catch (t) {
        (r.r().warning(`Failed to apply hotkey window runtime state`, {
          safe: { reason: e },
          sensitive: { error: t },
        }),
          this.deactivateLifecycle());
      }
    }
    setGateEnabled(e) {
      this.isHotkeyWindowGateEnabled !== e &&
        ((this.isHotkeyWindowGateEnabled = e),
        this.applyLifecycleWithWarning(`gate-change`));
    }
    setHotkeyWindowHotkey(e) {
      if (e != null) {
        let t = nA(e);
        if (t != null) return this.createHotkeyWindowMutationFailure(t);
        rA(e);
      }
      let t = this.configuredHotkeyWindowHotkey;
      this.configuredHotkeyWindowHotkey = e;
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.configuredHotkeyWindowHotkey = t),
          this.applyLifecycleWithWarning(`hotkey-rollback`),
          this.createHotkeyWindowMutationFailure(
            e instanceof Error ? e.message : String(e),
          )
        );
      }
      return (
        this.options.globalState.set(
          `hotkeyWindowHotkey`,
          this.configuredHotkeyWindowHotkey ?? void 0,
        ),
        { success: !0, state: this.getHotkeyWindowHotkeyState() }
      );
    }
    setHotkeyWindowDevHotkeyOverride(e) {
      if (!this.options.isDevMode)
        return this.createHotkeyWindowMutationFailure(
          `Dev override is only available in unpackaged builds.`,
        );
      if (this.devHotkeyWindowHotkeyOverrideEnabled === e)
        return { success: !0, state: this.getHotkeyWindowHotkeyState() };
      let t = this.devHotkeyWindowHotkeyOverrideEnabled;
      this.devHotkeyWindowHotkeyOverrideEnabled = e;
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.devHotkeyWindowHotkeyOverrideEnabled = t),
          this.applyLifecycleWithWarning(`dev-override-rollback`),
          this.createHotkeyWindowMutationFailure(
            e instanceof Error ? e.message : String(e),
          )
        );
      }
      return { success: !0, state: this.getHotkeyWindowHotkeyState() };
    }
    getHotkeyWindowController() {
      return this.hotkeyWindowController;
    }
    shouldHideHotkeyWindowOnToggle(e) {
      let t = e.getVisibleWindowWebContentsId();
      return t == null || this.lastBlurredWindowWebContentsId !== t
        ? !1
        : Date.now() - this.lastBlurredAtMs <= mte;
    }
    recordBlur(e) {
      ((this.lastBlurredWindowWebContentsId = e),
        (this.lastBlurredAtMs = Date.now()));
    }
    clearRecentBlur() {
      ((this.lastBlurredWindowWebContentsId = null),
        (this.lastBlurredAtMs = 0));
    }
  };
function i9(e) {
  return { focusComposerNonce: Date.now(), prefillCwd: e ?? void 0 };
}
var gte = 15e3,
  a9 = 700,
  _te = 5e3,
  o9 = 560,
  vte = class {
    claimedPrewarms = new Set();
    disposed = !1;
    pendingWindows = new Map();
    prewarmedRendererReadyIds = new Set();
    prewarms = new Map();
    constructor(e) {
      this.windowManager = e;
    }
    addToComposer(e) {
      let t = this.windowManager.getPrimaryWindow();
      if (t == null) throw Error(`No primary app window is available`);
      return (
        this.windowManager.sendMessageToWindow(t, {
          type: `add-quick-chat-to-codex`,
          ...e,
        }),
        Promise.resolve()
      );
    }
    prewarm(e) {
      this.disposed ||
        this.getPrewarm(e)
          ?.prewarmedWindow.prewarm()
          .catch((e) => {
            r.r().warning(`Failed to prewarm Quick Chat window`, {
              safe: {},
              sensitive: { error: e },
            });
          });
    }
    clearPrewarm(e) {
      let t = this.prewarms.get(e.id);
      t?.origin === e && this.disposePrewarm(t);
    }
    async open(e, t, r) {
      let i = this.getPrewarm(e);
      if (i == null) return;
      (this.prewarms.delete(e.id), this.claimedPrewarms.add(i));
      let a = !1;
      try {
        let o = await i.prewarmedWindow.take();
        if (o == null) return;
        ((i.claimedWindow = o),
          this.prewarmedRendererReadyIds.delete(o.webContents.id),
          this.positionWindow(o, e, r));
        let s = this.waitForRoute(o, t, _te);
        if (
          (this.windowManager.sendMessageToWindow(o, {
            type: `navigate-to-route`,
            path: n.ha(t),
            persistForReload: !0,
          }),
          !(await s) || o.isDestroyed())
        )
          return;
        (o.isMinimized() && o.restore(), o.show(), o.focus(), (a = !0));
      } finally {
        a ? this.completePrewarmClaim(i) : this.disposePrewarm(i);
      }
    }
    handleRendererReady(e, t) {
      if (!this.disposed) {
        if (this.pendingWindows.get(e.id)?.conversationId === t) {
          this.finishPendingWindow(e.id, !0);
          return;
        }
        t == null
          ? this.prewarmedRendererReadyIds.add(e.id)
          : this.prewarmedRendererReadyIds.delete(e.id);
      }
    }
    dispose() {
      if (!this.disposed) {
        this.disposed = !0;
        for (let e of this.prewarms.values()) this.disposePrewarm(e);
        for (let e of this.claimedPrewarms) this.disposePrewarm(e);
        for (let [e, t] of this.pendingWindows)
          (this.finishPendingWindow(e, !1),
            t.window.isDestroyed() || t.window.close());
        this.prewarmedRendererReadyIds.clear();
      }
    }
    getPrewarm(e) {
      if (this.disposed) return null;
      let t = this.prewarms.get(e.id);
      if (t?.origin === e) return t;
      if ((t != null && this.disposePrewarm(t), e.isDestroyed())) return null;
      let r,
        i = () => {
          this.disposePrewarm(r);
        };
      return (
        (r = {
          claimedWindow: null,
          handleOriginDestroyed: i,
          loadingWindow: null,
          origin: e,
          prewarmedWindow: new F7(async () => {
            let e = await this.windowManager.createWindow({
              title: `ChatGPT`,
              width: o9,
              height: a9,
              appearance: `quickChat`,
              show: !1,
              initialRoute: n.ua,
              lockTitle: !0,
            });
            if (r.released)
              return (
                this.prewarmedRendererReadyIds.delete(e.webContents.id),
                e.isDestroyed() || e.close(),
                null
              );
            r.loadingWindow = e;
            let t = await this.waitForRoute(e, null, gte);
            return ((r.loadingWindow = null), t ? e : null);
          }),
          released: !1,
        }),
        this.prewarms.set(e.id, r),
        e.once(`destroyed`, i),
        r
      );
    }
    disposePrewarm(e) {
      this.prewarms.get(e.origin.id) === e && this.prewarms.delete(e.origin.id);
      let t = e.claimedWindow ?? e.loadingWindow ?? e.prewarmedWindow.peek();
      (this.completePrewarmClaim(e),
        t != null &&
          (this.finishPendingWindow(t.webContents.id, !1),
          t.isDestroyed() || t.close()));
    }
    completePrewarmClaim(e) {
      (this.claimedPrewarms.delete(e),
        (e.released = !0),
        e.origin.removeListener(`destroyed`, e.handleOriginDestroyed),
        e.prewarmedWindow.dispose(),
        (e.claimedWindow = null),
        (e.loadingWindow = null));
    }
    waitForRoute(e, t, n) {
      let r = e.webContents.id;
      return t == null && this.prewarmedRendererReadyIds.delete(r)
        ? Promise.resolve(!0)
        : new Promise((i) => {
            let a = () => {
                this.finishPendingWindow(r, !1);
              },
              o = setTimeout(() => {
                let e = this.finishPendingWindow(r, !1);
                e != null && !e.window.isDestroyed() && e.window.close();
              }, n);
            (o.unref(),
              e.once(`closed`, a),
              this.pendingWindows.set(r, {
                conversationId: t,
                handleClosed: a,
                readyTimeout: o,
                resolve: i,
                window: e,
              }));
          });
    }
    finishPendingWindow(e, t) {
      this.prewarmedRendererReadyIds.delete(e);
      let n = this.pendingWindows.get(e);
      return n == null
        ? null
        : (this.pendingWindows.delete(e),
          clearTimeout(n.readyTimeout),
          n.window.isDestroyed() ||
            n.window.removeListener(`closed`, n.handleClosed),
          n.resolve(t),
          n);
    }
    positionWindow(e, t, n) {
      let r = c.BrowserWindow.fromWebContents(t);
      if (r == null || r.isDestroyed()) return;
      let i = r.getContentBounds(),
        a = { height: n.height, width: n.width, x: i.x + n.x, y: i.y + n.y },
        o = c.screen.getDisplayMatching(a).workArea,
        s = Math.max(400, Math.min(o9, o.width)),
        l = Math.max(400, Math.min(a9, o.height));
      (s !== o9 || l !== a9) && e.setSize(s, l, !1);
      let u = Math.round(a.x + a.width - s),
        d = Math.round(a.y + a.height - l);
      e.setPosition(
        Math.max(o.x, Math.min(u, o.x + o.width - s)),
        Math.max(o.y, Math.min(d, o.y + o.height - l)),
        !1,
      );
    }
  };
function yte(e) {
  return Number.isInteger(e)
    ? e >= 4352 &&
        (e <= 4447 ||
          e === 9001 ||
          e === 9002 ||
          (11904 <= e && e <= 12871 && e !== 12351) ||
          (12880 <= e && e <= 19903) ||
          (19968 <= e && e <= 42182) ||
          (43360 <= e && e <= 43388) ||
          (44032 <= e && e <= 55203) ||
          (63744 <= e && e <= 64255) ||
          (65040 <= e && e <= 65049) ||
          (65072 <= e && e <= 65131) ||
          (65281 <= e && e <= 65376) ||
          (65504 <= e && e <= 65510) ||
          (110592 <= e && e <= 110593) ||
          (127488 <= e && e <= 127569) ||
          (131072 <= e && e <= 262141))
    : !1;
}
var s9 = 10,
  c9 =
    (e = 0) =>
    (t) =>
      `\u001B[${t + e}m`,
  l9 =
    (e = 0) =>
    (t) =>
      `\u001B[${38 + e};5;${t}m`,
  u9 =
    (e = 0) =>
    (t, n, r) =>
      `\u001B[${38 + e};2;${t};${n};${r}m`,
  d9 = {
    modifier: {
      reset: [0, 0],
      bold: [1, 22],
      dim: [2, 22],
      italic: [3, 23],
      underline: [4, 24],
      overline: [53, 55],
      inverse: [7, 27],
      hidden: [8, 28],
      strikethrough: [9, 29],
    },
    color: {
      black: [30, 39],
      red: [31, 39],
      green: [32, 39],
      yellow: [33, 39],
      blue: [34, 39],
      magenta: [35, 39],
      cyan: [36, 39],
      white: [37, 39],
      blackBright: [90, 39],
      gray: [90, 39],
      grey: [90, 39],
      redBright: [91, 39],
      greenBright: [92, 39],
      yellowBright: [93, 39],
      blueBright: [94, 39],
      magentaBright: [95, 39],
      cyanBright: [96, 39],
      whiteBright: [97, 39],
    },
    bgColor: {
      bgBlack: [40, 49],
      bgRed: [41, 49],
      bgGreen: [42, 49],
      bgYellow: [43, 49],
      bgBlue: [44, 49],
      bgMagenta: [45, 49],
      bgCyan: [46, 49],
      bgWhite: [47, 49],
      bgBlackBright: [100, 49],
      bgGray: [100, 49],
      bgGrey: [100, 49],
      bgRedBright: [101, 49],
      bgGreenBright: [102, 49],
      bgYellowBright: [103, 49],
      bgBlueBright: [104, 49],
      bgMagentaBright: [105, 49],
      bgCyanBright: [106, 49],
      bgWhiteBright: [107, 49],
    },
  };
Object.keys(d9.modifier);
var bte = Object.keys(d9.color),
  xte = Object.keys(d9.bgColor);
[...bte, ...xte];
function Ste() {
  let e = new Map();
  for (let [t, n] of Object.entries(d9)) {
    for (let [t, r] of Object.entries(n))
      ((d9[t] = { open: `\u001B[${r[0]}m`, close: `\u001B[${r[1]}m` }),
        (n[t] = d9[t]),
        e.set(r[0], r[1]));
    Object.defineProperty(d9, t, { value: n, enumerable: !1 });
  }
  return (
    Object.defineProperty(d9, `codes`, { value: e, enumerable: !1 }),
    (d9.color.close = `\x1B[39m`),
    (d9.bgColor.close = `\x1B[49m`),
    (d9.color.ansi = c9()),
    (d9.color.ansi256 = l9()),
    (d9.color.ansi16m = u9()),
    (d9.bgColor.ansi = c9(s9)),
    (d9.bgColor.ansi256 = l9(s9)),
    (d9.bgColor.ansi16m = u9(s9)),
    Object.defineProperties(d9, {
      rgbToAnsi256: {
        value(e, t, n) {
          return e === t && t === n
            ? e < 8
              ? 16
              : e > 248
                ? 231
                : Math.round(((e - 8) / 247) * 24) + 232
            : 16 +
                36 * Math.round((e / 255) * 5) +
                6 * Math.round((t / 255) * 5) +
                Math.round((n / 255) * 5);
        },
        enumerable: !1,
      },
      hexToRgb: {
        value(e) {
          let t = /[a-f\d]{6}|[a-f\d]{3}/i.exec(e.toString(16));
          if (!t) return [0, 0, 0];
          let [n] = t;
          n.length === 3 && (n = [...n].map((e) => e + e).join(``));
          let r = Number.parseInt(n, 16);
          return [(r >> 16) & 255, (r >> 8) & 255, r & 255];
        },
        enumerable: !1,
      },
      hexToAnsi256: {
        value: (e) => d9.rgbToAnsi256(...d9.hexToRgb(e)),
        enumerable: !1,
      },
      ansi256ToAnsi: {
        value(e) {
          if (e < 8) return 30 + e;
          if (e < 16) return 90 + (e - 8);
          let t, n, r;
          if (e >= 232) ((t = ((e - 232) * 10 + 8) / 255), (n = t), (r = t));
          else {
            e -= 16;
            let i = e % 36;
            ((t = Math.floor(e / 36) / 5),
              (n = Math.floor(i / 6) / 5),
              (r = (i % 6) / 5));
          }
          let i = Math.max(t, n, r) * 2;
          if (i === 0) return 30;
          let a =
            30 + ((Math.round(r) << 2) | (Math.round(n) << 1) | Math.round(t));
          return (i === 2 && (a += 60), a);
        },
        enumerable: !1,
      },
      rgbToAnsi: {
        value: (e, t, n) => d9.ansi256ToAnsi(d9.rgbToAnsi256(e, t, n)),
        enumerable: !1,
      },
      hexToAnsi: {
        value: (e) => d9.ansi256ToAnsi(d9.hexToAnsi256(e)),
        enumerable: !1,
      },
    }),
    d9
  );
}
var f9 = Ste(),
  Cte = /^[\uD800-\uDBFF][\uDC00-\uDFFF]$/,
  p9 = [`\x1B`, ``],
  m9 = (e) => `${p9[0]}[${e}m`,
  h9 = (e, t, n) => {
    let r = [];
    e = [...e];
    for (let n of e) {
      let i = n;
      n.includes(`;`) && (n = n.split(`;`)[0][0] + `0`);
      let a = f9.codes.get(Number.parseInt(n, 10));
      if (a) {
        let n = e.indexOf(a.toString());
        n === -1 ? r.push(m9(t ? a : i)) : e.splice(n, 1);
      } else if (t) {
        r.push(m9(0));
        break;
      } else r.push(m9(i));
    }
    if (t && ((r = r.filter((e, t) => r.indexOf(e) === t)), n !== void 0)) {
      let e = m9(f9.codes.get(Number.parseInt(n, 10)));
      r = r.reduce((t, n) => (n === e ? [n, ...t] : [...t, n]), []);
    }
    return r.join(``);
  };
function g9(e, t, n) {
  let r = [...e],
    i = [],
    a = typeof n == `number` ? n : r.length,
    o = !1,
    s,
    c = 0,
    l = ``;
  for (let [u, d] of r.entries()) {
    let r = !1;
    if (p9.includes(d)) {
      let t = /\d[^m]*/.exec(e.slice(u, u + 18));
      ((s = t && t.length > 0 ? t[0] : void 0),
        c < a && ((o = !0), s !== void 0 && i.push(s)));
    } else o && d === `m` && ((o = !1), (r = !0));
    if (
      (!o && !r && c++,
      !Cte.test(d) &&
        yte(d.codePointAt()) &&
        (c++, typeof n != `number` && a++),
      c > t && c <= a)
    )
      l += d;
    else if (c === t && !o && s !== void 0) l = h9(i);
    else if (c >= a) {
      l += h9(i, !0, s);
      break;
    }
  }
  return l;
}
function wte({ onlyFirst: e = !1 } = {}) {
  return RegExp(
    `(?:\\u001B\\][\\s\\S]*?(?:\\u0007|\\u001B\\u005C|\\u009C))|[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]`,
    e ? void 0 : `g`,
  );
}
var Tte = wte();
function Ete(e) {
  if (typeof e != `string`)
    throw TypeError(`Expected a \`string\`, got \`${typeof e}\``);
  return e.replace(Tte, ``);
}
function Dte(e) {
  return (
    e === 161 ||
    e === 164 ||
    e === 167 ||
    e === 168 ||
    e === 170 ||
    e === 173 ||
    e === 174 ||
    (e >= 176 && e <= 180) ||
    (e >= 182 && e <= 186) ||
    (e >= 188 && e <= 191) ||
    e === 198 ||
    e === 208 ||
    e === 215 ||
    e === 216 ||
    (e >= 222 && e <= 225) ||
    e === 230 ||
    (e >= 232 && e <= 234) ||
    e === 236 ||
    e === 237 ||
    e === 240 ||
    e === 242 ||
    e === 243 ||
    (e >= 247 && e <= 250) ||
    e === 252 ||
    e === 254 ||
    e === 257 ||
    e === 273 ||
    e === 275 ||
    e === 283 ||
    e === 294 ||
    e === 295 ||
    e === 299 ||
    (e >= 305 && e <= 307) ||
    e === 312 ||
    (e >= 319 && e <= 322) ||
    e === 324 ||
    (e >= 328 && e <= 331) ||
    e === 333 ||
    e === 338 ||
    e === 339 ||
    e === 358 ||
    e === 359 ||
    e === 363 ||
    e === 462 ||
    e === 464 ||
    e === 466 ||
    e === 468 ||
    e === 470 ||
    e === 472 ||
    e === 474 ||
    e === 476 ||
    e === 593 ||
    e === 609 ||
    e === 708 ||
    e === 711 ||
    (e >= 713 && e <= 715) ||
    e === 717 ||
    e === 720 ||
    (e >= 728 && e <= 731) ||
    e === 733 ||
    e === 735 ||
    (e >= 768 && e <= 879) ||
    (e >= 913 && e <= 929) ||
    (e >= 931 && e <= 937) ||
    (e >= 945 && e <= 961) ||
    (e >= 963 && e <= 969) ||
    e === 1025 ||
    (e >= 1040 && e <= 1103) ||
    e === 1105 ||
    e === 8208 ||
    (e >= 8211 && e <= 8214) ||
    e === 8216 ||
    e === 8217 ||
    e === 8220 ||
    e === 8221 ||
    (e >= 8224 && e <= 8226) ||
    (e >= 8228 && e <= 8231) ||
    e === 8240 ||
    e === 8242 ||
    e === 8243 ||
    e === 8245 ||
    e === 8251 ||
    e === 8254 ||
    e === 8308 ||
    e === 8319 ||
    (e >= 8321 && e <= 8324) ||
    e === 8364 ||
    e === 8451 ||
    e === 8453 ||
    e === 8457 ||
    e === 8467 ||
    e === 8470 ||
    e === 8481 ||
    e === 8482 ||
    e === 8486 ||
    e === 8491 ||
    e === 8531 ||
    e === 8532 ||
    (e >= 8539 && e <= 8542) ||
    (e >= 8544 && e <= 8555) ||
    (e >= 8560 && e <= 8569) ||
    e === 8585 ||
    (e >= 8592 && e <= 8601) ||
    e === 8632 ||
    e === 8633 ||
    e === 8658 ||
    e === 8660 ||
    e === 8679 ||
    e === 8704 ||
    e === 8706 ||
    e === 8707 ||
    e === 8711 ||
    e === 8712 ||
    e === 8715 ||
    e === 8719 ||
    e === 8721 ||
    e === 8725 ||
    e === 8730 ||
    (e >= 8733 && e <= 8736) ||
    e === 8739 ||
    e === 8741 ||
    (e >= 8743 && e <= 8748) ||
    e === 8750 ||
    (e >= 8756 && e <= 8759) ||
    e === 8764 ||
    e === 8765 ||
    e === 8776 ||
    e === 8780 ||
    e === 8786 ||
    e === 8800 ||
    e === 8801 ||
    (e >= 8804 && e <= 8807) ||
    e === 8810 ||
    e === 8811 ||
    e === 8814 ||
    e === 8815 ||
    e === 8834 ||
    e === 8835 ||
    e === 8838 ||
    e === 8839 ||
    e === 8853 ||
    e === 8857 ||
    e === 8869 ||
    e === 8895 ||
    e === 8978 ||
    (e >= 9312 && e <= 9449) ||
    (e >= 9451 && e <= 9547) ||
    (e >= 9552 && e <= 9587) ||
    (e >= 9600 && e <= 9615) ||
    (e >= 9618 && e <= 9621) ||
    e === 9632 ||
    e === 9633 ||
    (e >= 9635 && e <= 9641) ||
    e === 9650 ||
    e === 9651 ||
    e === 9654 ||
    e === 9655 ||
    e === 9660 ||
    e === 9661 ||
    e === 9664 ||
    e === 9665 ||
    (e >= 9670 && e <= 9672) ||
    e === 9675 ||
    (e >= 9678 && e <= 9681) ||
    (e >= 9698 && e <= 9701) ||
    e === 9711 ||
    e === 9733 ||
    e === 9734 ||
    e === 9737 ||
    e === 9742 ||
    e === 9743 ||
    e === 9756 ||
    e === 9758 ||
    e === 9792 ||
    e === 9794 ||
    e === 9824 ||
    e === 9825 ||
    (e >= 9827 && e <= 9829) ||
    (e >= 9831 && e <= 9834) ||
    e === 9836 ||
    e === 9837 ||
    e === 9839 ||
    e === 9886 ||
    e === 9887 ||
    e === 9919 ||
    (e >= 9926 && e <= 9933) ||
    (e >= 9935 && e <= 9939) ||
    (e >= 9941 && e <= 9953) ||
    e === 9955 ||
    e === 9960 ||
    e === 9961 ||
    (e >= 9963 && e <= 9969) ||
    e === 9972 ||
    (e >= 9974 && e <= 9977) ||
    e === 9979 ||
    e === 9980 ||
    e === 9982 ||
    e === 9983 ||
    e === 10045 ||
    (e >= 10102 && e <= 10111) ||
    (e >= 11094 && e <= 11097) ||
    (e >= 12872 && e <= 12879) ||
    (e >= 57344 && e <= 63743) ||
    (e >= 65024 && e <= 65039) ||
    e === 65533 ||
    (e >= 127232 && e <= 127242) ||
    (e >= 127248 && e <= 127277) ||
    (e >= 127280 && e <= 127337) ||
    (e >= 127344 && e <= 127373) ||
    e === 127375 ||
    e === 127376 ||
    (e >= 127387 && e <= 127404) ||
    (e >= 917760 && e <= 917999) ||
    (e >= 983040 && e <= 1048573) ||
    (e >= 1048576 && e <= 1114109)
  );
}
function Ote(e) {
  return (
    e === 12288 || (e >= 65281 && e <= 65376) || (e >= 65504 && e <= 65510)
  );
}
function kte(e) {
  return (
    (e >= 4352 && e <= 4447) ||
    e === 8986 ||
    e === 8987 ||
    e === 9001 ||
    e === 9002 ||
    (e >= 9193 && e <= 9196) ||
    e === 9200 ||
    e === 9203 ||
    e === 9725 ||
    e === 9726 ||
    e === 9748 ||
    e === 9749 ||
    (e >= 9776 && e <= 9783) ||
    (e >= 9800 && e <= 9811) ||
    e === 9855 ||
    (e >= 9866 && e <= 9871) ||
    e === 9875 ||
    e === 9889 ||
    e === 9898 ||
    e === 9899 ||
    e === 9917 ||
    e === 9918 ||
    e === 9924 ||
    e === 9925 ||
    e === 9934 ||
    e === 9940 ||
    e === 9962 ||
    e === 9970 ||
    e === 9971 ||
    e === 9973 ||
    e === 9978 ||
    e === 9981 ||
    e === 9989 ||
    e === 9994 ||
    e === 9995 ||
    e === 10024 ||
    e === 10060 ||
    e === 10062 ||
    (e >= 10067 && e <= 10069) ||
    e === 10071 ||
    (e >= 10133 && e <= 10135) ||
    e === 10160 ||
    e === 10175 ||
    e === 11035 ||
    e === 11036 ||
    e === 11088 ||
    e === 11093 ||
    (e >= 11904 && e <= 11929) ||
    (e >= 11931 && e <= 12019) ||
    (e >= 12032 && e <= 12245) ||
    (e >= 12272 && e <= 12287) ||
    (e >= 12289 && e <= 12350) ||
    (e >= 12353 && e <= 12438) ||
    (e >= 12441 && e <= 12543) ||
    (e >= 12549 && e <= 12591) ||
    (e >= 12593 && e <= 12686) ||
    (e >= 12688 && e <= 12773) ||
    (e >= 12783 && e <= 12830) ||
    (e >= 12832 && e <= 12871) ||
    (e >= 12880 && e <= 42124) ||
    (e >= 42128 && e <= 42182) ||
    (e >= 43360 && e <= 43388) ||
    (e >= 44032 && e <= 55203) ||
    (e >= 63744 && e <= 64255) ||
    (e >= 65040 && e <= 65049) ||
    (e >= 65072 && e <= 65106) ||
    (e >= 65108 && e <= 65126) ||
    (e >= 65128 && e <= 65131) ||
    (e >= 94176 && e <= 94180) ||
    (e >= 94192 && e <= 94198) ||
    (e >= 94208 && e <= 101589) ||
    (e >= 101631 && e <= 101662) ||
    (e >= 101760 && e <= 101874) ||
    (e >= 110576 && e <= 110579) ||
    (e >= 110581 && e <= 110587) ||
    e === 110589 ||
    e === 110590 ||
    (e >= 110592 && e <= 110882) ||
    e === 110898 ||
    (e >= 110928 && e <= 110930) ||
    e === 110933 ||
    (e >= 110948 && e <= 110951) ||
    (e >= 110960 && e <= 111355) ||
    (e >= 119552 && e <= 119638) ||
    (e >= 119648 && e <= 119670) ||
    e === 126980 ||
    e === 127183 ||
    e === 127374 ||
    (e >= 127377 && e <= 127386) ||
    (e >= 127488 && e <= 127490) ||
    (e >= 127504 && e <= 127547) ||
    (e >= 127552 && e <= 127560) ||
    e === 127568 ||
    e === 127569 ||
    (e >= 127584 && e <= 127589) ||
    (e >= 127744 && e <= 127776) ||
    (e >= 127789 && e <= 127797) ||
    (e >= 127799 && e <= 127868) ||
    (e >= 127870 && e <= 127891) ||
    (e >= 127904 && e <= 127946) ||
    (e >= 127951 && e <= 127955) ||
    (e >= 127968 && e <= 127984) ||
    e === 127988 ||
    (e >= 127992 && e <= 128062) ||
    e === 128064 ||
    (e >= 128066 && e <= 128252) ||
    (e >= 128255 && e <= 128317) ||
    (e >= 128331 && e <= 128334) ||
    (e >= 128336 && e <= 128359) ||
    e === 128378 ||
    e === 128405 ||
    e === 128406 ||
    e === 128420 ||
    (e >= 128507 && e <= 128591) ||
    (e >= 128640 && e <= 128709) ||
    e === 128716 ||
    (e >= 128720 && e <= 128722) ||
    (e >= 128725 && e <= 128728) ||
    (e >= 128732 && e <= 128735) ||
    e === 128747 ||
    e === 128748 ||
    (e >= 128756 && e <= 128764) ||
    (e >= 128992 && e <= 129003) ||
    e === 129008 ||
    (e >= 129292 && e <= 129338) ||
    (e >= 129340 && e <= 129349) ||
    (e >= 129351 && e <= 129535) ||
    (e >= 129648 && e <= 129660) ||
    (e >= 129664 && e <= 129674) ||
    (e >= 129678 && e <= 129734) ||
    e === 129736 ||
    (e >= 129741 && e <= 129756) ||
    (e >= 129759 && e <= 129770) ||
    (e >= 129775 && e <= 129784) ||
    (e >= 131072 && e <= 196605) ||
    (e >= 196608 && e <= 262141)
  );
}
function Ate(e) {
  if (!Number.isSafeInteger(e))
    throw TypeError(`Expected a code point, got \`${typeof e}\`.`);
}
function jte(e, { ambiguousAsWide: t = !1 } = {}) {
  return (Ate(e), Ote(e) || kte(e) || (t && Dte(e)) ? 2 : 1);
}
var Mte = () =>
    /[#*0-9]\uFE0F?\u20E3|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26AA\u26B0\u26B1\u26BD\u26BE\u26C4\u26C8\u26CF\u26D1\u26E9\u26F0-\u26F5\u26F7\u26F8\u26FA\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2757\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B55\u3030\u303D\u3297\u3299]\uFE0F?|[\u261D\u270C\u270D](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\u270A\u270B](?:\uD83C[\uDFFB-\uDFFF])?|[\u23E9-\u23EC\u23F0\u23F3\u25FD\u2693\u26A1\u26AB\u26C5\u26CE\u26D4\u26EA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2795-\u2797\u27B0\u27BF\u2B50]|\u26D3\uFE0F?(?:\u200D\uD83D\uDCA5)?|\u26F9(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\u2764\uFE0F?(?:\u200D(?:\uD83D\uDD25|\uD83E\uDE79))?|\uD83C(?:[\uDC04\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]\uFE0F?|[\uDF85\uDFC2\uDFC7](?:\uD83C[\uDFFB-\uDFFF])?|[\uDFC4\uDFCA](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDFCB\uDFCC](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF43\uDF45-\uDF4A\uDF4C-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uDDE6\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF]|\uDDE7\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF]|\uDDE8\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF7\uDDFA-\uDDFF]|\uDDE9\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF]|\uDDEA\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA]|\uDDEB\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7]|\uDDEC\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE]|\uDDED\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA]|\uDDEE\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9]|\uDDEF\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5]|\uDDF0\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF]|\uDDF1\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE]|\uDDF2\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF]|\uDDF3\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF]|\uDDF4\uD83C\uDDF2|\uDDF5\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE]|\uDDF6\uD83C\uDDE6|\uDDF7\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC]|\uDDF8\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF]|\uDDF9\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF]|\uDDFA\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF]|\uDDFB\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA]|\uDDFC\uD83C[\uDDEB\uDDF8]|\uDDFD\uD83C\uDDF0|\uDDFE\uD83C[\uDDEA\uDDF9]|\uDDFF\uD83C[\uDDE6\uDDF2\uDDFC]|\uDF44(?:\u200D\uD83D\uDFEB)?|\uDF4B(?:\u200D\uD83D\uDFE9)?|\uDFC3(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDFF3\uFE0F?(?:\u200D(?:\u26A7\uFE0F?|\uD83C\uDF08))?|\uDFF4(?:\u200D\u2620\uFE0F?|\uDB40\uDC67\uDB40\uDC62\uDB40(?:\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDC73\uDB40\uDC63\uDB40\uDC74|\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F)?)|\uD83D(?:[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3]\uFE0F?|[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC](?:\uD83C[\uDFFB-\uDFFF])?|[\uDC6E-\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4\uDEB5](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD74\uDD90](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC25\uDC27-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE41\uDE43\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED8\uDEDC-\uDEDF\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB\uDFF0]|\uDC08(?:\u200D\u2B1B)?|\uDC15(?:\u200D\uD83E\uDDBA)?|\uDC26(?:\u200D(?:\u2B1B|\uD83D\uDD25))?|\uDC3B(?:\u200D\u2744\uFE0F?)?|\uDC41\uFE0F?(?:\u200D\uD83D\uDDE8\uFE0F?)?|\uDC68(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDC68\uDC69]\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?))?|\uDC69(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?[\uDC68\uDC69]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?|\uDC69\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?))|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFC-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFD-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFD\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFE]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])))?))?|\uDD75(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDE2E(?:\u200D\uD83D\uDCA8)?|\uDE35(?:\u200D\uD83D\uDCAB)?|\uDE36(?:\u200D\uD83C\uDF2B\uFE0F?)?|\uDE42(?:\u200D[\u2194\u2195]\uFE0F?)?|\uDEB6(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?)|\uD83E(?:[\uDD0C\uDD0F\uDD18-\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5\uDEC3-\uDEC5\uDEF0\uDEF2-\uDEF8](?:\uD83C[\uDFFB-\uDFFF])?|[\uDD26\uDD35\uDD37-\uDD39\uDD3C-\uDD3E\uDDB8\uDDB9\uDDCD\uDDCF\uDDD4\uDDD6-\uDDDD](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDDDE\uDDDF](?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD0D\uDD0E\uDD10-\uDD17\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCC\uDDD0\uDDE0-\uDDFF\uDE70-\uDE7C\uDE80-\uDE8A\uDE8E-\uDEC2\uDEC6\uDEC8\uDECD-\uDEDC\uDEDF-\uDEEA\uDEEF]|\uDDCE(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDDD1(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1|\uDDD1\u200D\uD83E\uDDD2(?:\u200D\uD83E\uDDD2)?|\uDDD2(?:\u200D\uD83E\uDDD2)?))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])))?))?|\uDEF1(?:\uD83C(?:\uDFFB(?:\u200D\uD83E\uDEF2\uD83C[\uDFFC-\uDFFF])?|\uDFFC(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFD-\uDFFF])?|\uDFFD(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])?|\uDFFE(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFD\uDFFF])?|\uDFFF(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFE])?))?)/g,
  Nte = new Intl.Segmenter(),
  Pte = /^\p{Default_Ignorable_Code_Point}$/u;
function _9(e, t = {}) {
  if (typeof e != `string` || e.length === 0) return 0;
  let { ambiguousIsNarrow: n = !0, countAnsiEscapeCodes: r = !1 } = t;
  if ((r || (e = Ete(e)), e.length === 0)) return 0;
  let i = 0,
    a = { ambiguousAsWide: !n };
  for (let { segment: t } of Nte.segment(e)) {
    let e = t.codePointAt(0);
    if (
      !(e <= 31 || (e >= 127 && e <= 159)) &&
      !((e >= 8203 && e <= 8207) || e === 65279) &&
      !(
        (e >= 768 && e <= 879) ||
        (e >= 6832 && e <= 6911) ||
        (e >= 7616 && e <= 7679) ||
        (e >= 8400 && e <= 8447) ||
        (e >= 65056 && e <= 65071)
      ) &&
      !(e >= 55296 && e <= 57343) &&
      !(e >= 65024 && e <= 65039) &&
      !Pte.test(t)
    ) {
      if (Mte().test(t)) {
        i += 2;
        continue;
      }
      i += jte(e, a);
    }
  }
  return i;
}
function v9(e, t, n) {
  if (e.charAt(t) === ` `) return t;
  let r = n ? 1 : -1;
  for (let n = 0; n <= 3; n++) {
    let i = t + n * r;
    if (e.charAt(i) === ` `) return i;
  }
  return t;
}
function Fte(e, t, n = {}) {
  let {
      position: r = `end`,
      space: i = !1,
      preferTruncationOnSpace: a = !1,
    } = n,
    { truncationCharacter: o = `…` } = n;
  if (typeof e != `string`)
    throw TypeError(`Expected \`input\` to be a string, got ${typeof e}`);
  if (typeof t != `number`)
    throw TypeError(`Expected \`columns\` to be a number, got ${typeof t}`);
  if (t < 1) return ``;
  if (t === 1) return o;
  let s = _9(e);
  if (s <= t) return e;
  if (r === `start`) {
    if (a) {
      let n = v9(e, s - t + 1, !0);
      return o + g9(e, n, s).trim();
    }
    return (i === !0 && (o += ` `), o + g9(e, s - t + _9(o), s));
  }
  if (r === `middle`) {
    i === !0 && (o = ` ${o} `);
    let n = Math.floor(t / 2);
    if (a) {
      let r = v9(e, n),
        i = v9(e, s - (t - n) + 1, !0);
      return g9(e, 0, r) + o + g9(e, i, s).trim();
    }
    return g9(e, 0, n) + o + g9(e, s - (t - n) + _9(o), s);
  }
  if (r === `end`)
    return a
      ? g9(e, 0, v9(e, t - 1)) + o
      : (i === !0 && (o = ` ${o}`), g9(e, 0, t - _9(o)) + o);
  throw Error(
    `Expected \`options.position\` to be either \`start\`, \`middle\` or \`end\`, got ${r}`,
  );
}
function Ite(e) {
  try {
    return (p.default.accessSync(e), !0);
  } catch {
    return !1;
  }
}
var Lte = class extends Error {
    constructor(e, t) {
      (super(`Max tries reached.`),
        (this.originalPath = e),
        (this.lastTriedPath = t));
    }
  },
  Rte = (e, t) => {
    let n = e.match(/^(?<filename>.*)\((?<index>\d+)\)$/),
      { filename: r, index: i } = n ? n.groups : { filename: e, index: 0 };
    return ((r = r.trim()), [`${r}${t}`, `${r} (${++i})${t}`]);
  },
  y9 = (e, t) => {
    let n = u.default.extname(e),
      r = u.default.dirname(e),
      [i, a] = t(u.default.basename(e, n), n);
    return [u.default.join(r, i), u.default.join(r, a)];
  };
function zte(e, { incrementer: t = Rte, maxTries: n = 1 / 0 } = {}) {
  let r = 0,
    [i] = y9(e, t),
    a = e;
  for (;;) {
    if (!Ite(a)) return a;
    if (++r > n) throw new Lte(i, a);
    [i, a] = y9(a, t);
  }
}
var b9 = (e) =>
  e
    .replace(/&/g, `&amp;`)
    .replace(/"/g, `&quot;`)
    .replace(/'/g, `&#39;`)
    .replace(/</g, `&lt;`)
    .replace(/>/g, `&gt;`);
function Bte(e, ...t) {
  if (typeof e == `string`) return b9(e);
  let n = e[0];
  for (let [r, i] of t.entries()) n = n + b9(String(i)) + e[r + 1];
  return n;
}
var Vte = class extends Error {
    constructor(e) {
      (super(
        `Missing a value for ${e ? `the placeholder: ${e}` : `a placeholder`}`,
        e,
      ),
        (this.name = `MissingValueError`),
        (this.key = e));
    }
  },
  Hte = class extends Error {
    constructor(e) {
      (super(`Missing filter: ${e}`),
        (this.name = `MissingFilterError`),
        (this.filterName = e));
    }
  };
function Ute(
  e,
  t,
  {
    ignoreMissing: n = !1,
    transform: r = ({ value: e }) => e,
    filters: i = {},
  } = {},
) {
  if (typeof e != `string`)
    throw TypeError(
      `Expected a \`string\` in the first argument, got \`${typeof e}\``,
    );
  if (typeof t != `object`)
    throw TypeError(
      `Expected an \`object\` or \`Array\` in the second argument, got \`${typeof t}\``,
    );
  let a = ``,
    o = ``;
  ((e = e.replace(/\\{/g, a)), (e = e.replace(/\\}/g, o)));
  let s = (e) => {
      let t = [],
        n = ``;
      for (let r = 0; r < e.length; r++)
        e[r] === `\\` && e[r + 1] === `.`
          ? ((n += `.`), r++)
          : e[r] === `.`
            ? (t.push(n), (n = ``))
            : (n += e[r]);
      return (t.push(n), t);
    },
    c = (e, a) => {
      let o = a.split(`|`).map((e) => e.trim()),
        c = o[0],
        l = o.slice(1),
        u = s(c),
        d = t;
      for (let e of u) d &&= d[e];
      for (let t of l) {
        let r = i[t];
        if (!r) {
          if (n) return e;
          throw new Hte(t);
        }
        d !== void 0 && (d = r(d));
      }
      let f = r({ value: d, key: c });
      if (f === void 0) {
        if (n) return e;
        throw new Vte(c);
      }
      return String(f);
    },
    l = `((\\d+|[a-z$_][\\w\\-.$\\\\]*)\\s*(?:\\|\\s*[a-z$_][\\w$]*\\s*)*)`,
    u = RegExp(`{{${l}}}`, `gi`),
    d = RegExp(`{${l}}`, `gi`);
  return (
    (e = e.replace(u, (...e) => Bte(c(...e)))),
    (e = e.replace(d, c)),
    (e = e.replace(new RegExp(a, `g`), `{`)),
    (e = e.replace(new RegExp(o, `g`), `}`)),
    e
  );
}
var Wte = e.i({ default: () => x9 }),
  x9,
  Gte = e.r(() => {
    x9 = {
      "application/1d-interleaved-parityfec": { source: `iana` },
      "application/3gpdash-qoe-report+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/3gpp-ims+xml": { source: `iana`, compressible: !0 },
      "application/3gpphal+json": { source: `iana`, compressible: !0 },
      "application/3gpphalforms+json": { source: `iana`, compressible: !0 },
      "application/a2l": { source: `iana` },
      "application/ace+cbor": { source: `iana` },
      "application/ace+json": { source: `iana`, compressible: !0 },
      "application/ace-groupcomm+cbor": { source: `iana` },
      "application/ace-trl+cbor": { source: `iana` },
      "application/activemessage": { source: `iana` },
      "application/activity+json": { source: `iana`, compressible: !0 },
      "application/aif+cbor": { source: `iana` },
      "application/aif+json": { source: `iana`, compressible: !0 },
      "application/alto-cdni+json": { source: `iana`, compressible: !0 },
      "application/alto-cdnifilter+json": { source: `iana`, compressible: !0 },
      "application/alto-costmap+json": { source: `iana`, compressible: !0 },
      "application/alto-costmapfilter+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-directory+json": { source: `iana`, compressible: !0 },
      "application/alto-endpointcost+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-endpointcostparams+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-endpointprop+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-endpointpropparams+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-error+json": { source: `iana`, compressible: !0 },
      "application/alto-networkmap+json": { source: `iana`, compressible: !0 },
      "application/alto-networkmapfilter+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-propmap+json": { source: `iana`, compressible: !0 },
      "application/alto-propmapparams+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-tips+json": { source: `iana`, compressible: !0 },
      "application/alto-tipsparams+json": { source: `iana`, compressible: !0 },
      "application/alto-updatestreamcontrol+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/alto-updatestreamparams+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/aml": { source: `iana` },
      "application/andrew-inset": { source: `iana`, extensions: [`ez`] },
      "application/appinstaller": {
        compressible: !1,
        extensions: [`appinstaller`],
      },
      "application/applefile": { source: `iana` },
      "application/applixware": { source: `apache`, extensions: [`aw`] },
      "application/appx": { compressible: !1, extensions: [`appx`] },
      "application/appxbundle": {
        compressible: !1,
        extensions: [`appxbundle`],
      },
      "application/at+jwt": { source: `iana` },
      "application/atf": { source: `iana` },
      "application/atfx": { source: `iana` },
      "application/atom+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`atom`],
      },
      "application/atomcat+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`atomcat`],
      },
      "application/atomdeleted+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`atomdeleted`],
      },
      "application/atomicmail": { source: `iana` },
      "application/atomsvc+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`atomsvc`],
      },
      "application/atsc-dwd+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`dwd`],
      },
      "application/atsc-dynamic-event-message": { source: `iana` },
      "application/atsc-held+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`held`],
      },
      "application/atsc-rdt+json": { source: `iana`, compressible: !0 },
      "application/atsc-rsat+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rsat`],
      },
      "application/atxml": { source: `iana` },
      "application/auth-policy+xml": { source: `iana`, compressible: !0 },
      "application/automationml-aml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`aml`],
      },
      "application/automationml-amlx+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`amlx`],
      },
      "application/bacnet-xdd+zip": { source: `iana`, compressible: !1 },
      "application/batch-smtp": { source: `iana` },
      "application/bdoc": { compressible: !1, extensions: [`bdoc`] },
      "application/beep+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/bufr": { source: `iana` },
      "application/c2pa": { source: `iana` },
      "application/calendar+json": { source: `iana`, compressible: !0 },
      "application/calendar+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xcs`],
      },
      "application/call-completion": { source: `iana` },
      "application/cals-1840": { source: `iana` },
      "application/captive+json": { source: `iana`, compressible: !0 },
      "application/cbor": { source: `iana` },
      "application/cbor-seq": { source: `iana` },
      "application/cccex": { source: `iana` },
      "application/ccmp+xml": { source: `iana`, compressible: !0 },
      "application/ccxml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ccxml`],
      },
      "application/cda+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/cdfx+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`cdfx`],
      },
      "application/cdmi-capability": { source: `iana`, extensions: [`cdmia`] },
      "application/cdmi-container": { source: `iana`, extensions: [`cdmic`] },
      "application/cdmi-domain": { source: `iana`, extensions: [`cdmid`] },
      "application/cdmi-object": { source: `iana`, extensions: [`cdmio`] },
      "application/cdmi-queue": { source: `iana`, extensions: [`cdmiq`] },
      "application/cdni": { source: `iana` },
      "application/ce+cbor": { source: `iana` },
      "application/cea": { source: `iana` },
      "application/cea-2018+xml": { source: `iana`, compressible: !0 },
      "application/cellml+xml": { source: `iana`, compressible: !0 },
      "application/cfw": { source: `iana` },
      "application/cid-edhoc+cbor-seq": { source: `iana` },
      "application/city+json": { source: `iana`, compressible: !0 },
      "application/city+json-seq": { source: `iana` },
      "application/clr": { source: `iana` },
      "application/clue+xml": { source: `iana`, compressible: !0 },
      "application/clue_info+xml": { source: `iana`, compressible: !0 },
      "application/cms": { source: `iana` },
      "application/cnrp+xml": { source: `iana`, compressible: !0 },
      "application/coap-eap": { source: `iana` },
      "application/coap-group+json": { source: `iana`, compressible: !0 },
      "application/coap-payload": { source: `iana` },
      "application/commonground": { source: `iana` },
      "application/concise-problem-details+cbor": { source: `iana` },
      "application/conference-info+xml": { source: `iana`, compressible: !0 },
      "application/cose": { source: `iana` },
      "application/cose-key": { source: `iana` },
      "application/cose-key-set": { source: `iana` },
      "application/cose-x509": { source: `iana` },
      "application/cpl+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`cpl`],
      },
      "application/csrattrs": { source: `iana` },
      "application/csta+xml": { source: `iana`, compressible: !0 },
      "application/cstadata+xml": { source: `iana`, compressible: !0 },
      "application/csvm+json": { source: `iana`, compressible: !0 },
      "application/cu-seeme": { source: `apache`, extensions: [`cu`] },
      "application/cwl": { source: `iana`, extensions: [`cwl`] },
      "application/cwl+json": { source: `iana`, compressible: !0 },
      "application/cwl+yaml": { source: `iana` },
      "application/cwt": { source: `iana` },
      "application/cybercash": { source: `iana` },
      "application/dart": { compressible: !0 },
      "application/dash+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpd`],
      },
      "application/dash-patch+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpp`],
      },
      "application/dashdelta": { source: `iana` },
      "application/davmount+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`davmount`],
      },
      "application/dca-rft": { source: `iana` },
      "application/dcd": { source: `iana` },
      "application/dec-dx": { source: `iana` },
      "application/dialog-info+xml": { source: `iana`, compressible: !0 },
      "application/dicom": { source: `iana`, extensions: [`dcm`] },
      "application/dicom+json": { source: `iana`, compressible: !0 },
      "application/dicom+xml": { source: `iana`, compressible: !0 },
      "application/dii": { source: `iana` },
      "application/dit": { source: `iana` },
      "application/dns": { source: `iana` },
      "application/dns+json": { source: `iana`, compressible: !0 },
      "application/dns-message": { source: `iana` },
      "application/docbook+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`dbk`],
      },
      "application/dots+cbor": { source: `iana` },
      "application/dpop+jwt": { source: `iana` },
      "application/dskpp+xml": { source: `iana`, compressible: !0 },
      "application/dssc+der": { source: `iana`, extensions: [`dssc`] },
      "application/dssc+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xdssc`],
      },
      "application/dvcs": { source: `iana` },
      "application/eat+cwt": { source: `iana` },
      "application/eat+jwt": { source: `iana` },
      "application/eat-bun+cbor": { source: `iana` },
      "application/eat-bun+json": { source: `iana`, compressible: !0 },
      "application/eat-ucs+cbor": { source: `iana` },
      "application/eat-ucs+json": { source: `iana`, compressible: !0 },
      "application/ecmascript": {
        source: `apache`,
        compressible: !0,
        extensions: [`ecma`],
      },
      "application/edhoc+cbor-seq": { source: `iana` },
      "application/edi-consent": { source: `iana` },
      "application/edi-x12": { source: `iana`, compressible: !1 },
      "application/edifact": { source: `iana`, compressible: !1 },
      "application/efi": { source: `iana` },
      "application/elm+json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/elm+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.cap+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/emergencycalldata.comment+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.control+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.deviceinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.ecall.msd": { source: `iana` },
      "application/emergencycalldata.legacyesn+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.providerinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.serviceinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.subscriberinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emergencycalldata.veds+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/emma+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`emma`],
      },
      "application/emotionml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`emotionml`],
      },
      "application/encaprtp": { source: `iana` },
      "application/entity-statement+jwt": { source: `iana` },
      "application/epp+xml": { source: `iana`, compressible: !0 },
      "application/epub+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`epub`],
      },
      "application/eshop": { source: `iana` },
      "application/exi": { source: `iana`, extensions: [`exi`] },
      "application/expect-ct-report+json": { source: `iana`, compressible: !0 },
      "application/express": { source: `iana`, extensions: [`exp`] },
      "application/fastinfoset": { source: `iana` },
      "application/fastsoap": { source: `iana` },
      "application/fdf": { source: `iana`, extensions: [`fdf`] },
      "application/fdt+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`fdt`],
      },
      "application/fhir+json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/fhir+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/fido.trusted-apps+json": { compressible: !0 },
      "application/fits": { source: `iana` },
      "application/flexfec": { source: `iana` },
      "application/font-sfnt": { source: `iana` },
      "application/font-tdpfr": { source: `iana`, extensions: [`pfr`] },
      "application/font-woff": { source: `iana`, compressible: !1 },
      "application/framework-attributes+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/geo+json": {
        source: `iana`,
        compressible: !0,
        extensions: [`geojson`],
      },
      "application/geo+json-seq": { source: `iana` },
      "application/geopackage+sqlite3": { source: `iana` },
      "application/geopose+json": { source: `iana`, compressible: !0 },
      "application/geoxacml+json": { source: `iana`, compressible: !0 },
      "application/geoxacml+xml": { source: `iana`, compressible: !0 },
      "application/gltf-buffer": { source: `iana` },
      "application/gml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`gml`],
      },
      "application/gnap-binding-jws": { source: `iana` },
      "application/gnap-binding-jwsd": { source: `iana` },
      "application/gnap-binding-rotation-jws": { source: `iana` },
      "application/gnap-binding-rotation-jwsd": { source: `iana` },
      "application/gpx+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`gpx`],
      },
      "application/grib": { source: `iana` },
      "application/gxf": { source: `apache`, extensions: [`gxf`] },
      "application/gzip": {
        source: `iana`,
        compressible: !1,
        extensions: [`gz`],
      },
      "application/h224": { source: `iana` },
      "application/held+xml": { source: `iana`, compressible: !0 },
      "application/hjson": { extensions: [`hjson`] },
      "application/hl7v2+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/http": { source: `iana` },
      "application/hyperstudio": { source: `iana`, extensions: [`stk`] },
      "application/ibe-key-request+xml": { source: `iana`, compressible: !0 },
      "application/ibe-pkg-reply+xml": { source: `iana`, compressible: !0 },
      "application/ibe-pp-data": { source: `iana` },
      "application/iges": { source: `iana` },
      "application/im-iscomposing+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/index": { source: `iana` },
      "application/index.cmd": { source: `iana` },
      "application/index.obj": { source: `iana` },
      "application/index.response": { source: `iana` },
      "application/index.vnd": { source: `iana` },
      "application/inkml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ink`, `inkml`],
      },
      "application/iotp": { source: `iana` },
      "application/ipfix": { source: `iana`, extensions: [`ipfix`] },
      "application/ipp": { source: `iana` },
      "application/isup": { source: `iana` },
      "application/its+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`its`],
      },
      "application/java-archive": {
        source: `iana`,
        compressible: !1,
        extensions: [`jar`, `war`, `ear`],
      },
      "application/java-serialized-object": {
        source: `apache`,
        compressible: !1,
        extensions: [`ser`],
      },
      "application/java-vm": {
        source: `apache`,
        compressible: !1,
        extensions: [`class`],
      },
      "application/javascript": {
        source: `apache`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`js`],
      },
      "application/jf2feed+json": { source: `iana`, compressible: !0 },
      "application/jose": { source: `iana` },
      "application/jose+json": { source: `iana`, compressible: !0 },
      "application/jrd+json": { source: `iana`, compressible: !0 },
      "application/jscalendar+json": { source: `iana`, compressible: !0 },
      "application/jscontact+json": { source: `iana`, compressible: !0 },
      "application/json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`json`, `map`],
      },
      "application/json-patch+json": { source: `iana`, compressible: !0 },
      "application/json-seq": { source: `iana` },
      "application/json5": { extensions: [`json5`] },
      "application/jsonml+json": {
        source: `apache`,
        compressible: !0,
        extensions: [`jsonml`],
      },
      "application/jsonpath": { source: `iana` },
      "application/jwk+json": { source: `iana`, compressible: !0 },
      "application/jwk-set+json": { source: `iana`, compressible: !0 },
      "application/jwk-set+jwt": { source: `iana` },
      "application/jwt": { source: `iana` },
      "application/kpml-request+xml": { source: `iana`, compressible: !0 },
      "application/kpml-response+xml": { source: `iana`, compressible: !0 },
      "application/ld+json": {
        source: `iana`,
        compressible: !0,
        extensions: [`jsonld`],
      },
      "application/lgr+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`lgr`],
      },
      "application/link-format": { source: `iana` },
      "application/linkset": { source: `iana` },
      "application/linkset+json": { source: `iana`, compressible: !0 },
      "application/load-control+xml": { source: `iana`, compressible: !0 },
      "application/logout+jwt": { source: `iana` },
      "application/lost+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`lostxml`],
      },
      "application/lostsync+xml": { source: `iana`, compressible: !0 },
      "application/lpf+zip": { source: `iana`, compressible: !1 },
      "application/lxf": { source: `iana` },
      "application/mac-binhex40": { source: `iana`, extensions: [`hqx`] },
      "application/mac-compactpro": { source: `apache`, extensions: [`cpt`] },
      "application/macwriteii": { source: `iana` },
      "application/mads+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mads`],
      },
      "application/manifest+json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`webmanifest`],
      },
      "application/marc": { source: `iana`, extensions: [`mrc`] },
      "application/marcxml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mrcx`],
      },
      "application/mathematica": {
        source: `iana`,
        extensions: [`ma`, `nb`, `mb`],
      },
      "application/mathml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mathml`],
      },
      "application/mathml-content+xml": { source: `iana`, compressible: !0 },
      "application/mathml-presentation+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbms-associated-procedure-description+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbms-deregister+xml": { source: `iana`, compressible: !0 },
      "application/mbms-envelope+xml": { source: `iana`, compressible: !0 },
      "application/mbms-msk+xml": { source: `iana`, compressible: !0 },
      "application/mbms-msk-response+xml": { source: `iana`, compressible: !0 },
      "application/mbms-protection-description+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbms-reception-report+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbms-register+xml": { source: `iana`, compressible: !0 },
      "application/mbms-register-response+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbms-schedule+xml": { source: `iana`, compressible: !0 },
      "application/mbms-user-service-description+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/mbox": { source: `iana`, extensions: [`mbox`] },
      "application/media-policy-dataset+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpf`],
      },
      "application/media_control+xml": { source: `iana`, compressible: !0 },
      "application/mediaservercontrol+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mscml`],
      },
      "application/merge-patch+json": { source: `iana`, compressible: !0 },
      "application/metalink+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`metalink`],
      },
      "application/metalink4+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`meta4`],
      },
      "application/mets+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mets`],
      },
      "application/mf4": { source: `iana` },
      "application/mikey": { source: `iana` },
      "application/mipc": { source: `iana` },
      "application/missing-blocks+cbor-seq": { source: `iana` },
      "application/mmt-aei+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`maei`],
      },
      "application/mmt-usd+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`musd`],
      },
      "application/mods+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mods`],
      },
      "application/moss-keys": { source: `iana` },
      "application/moss-signature": { source: `iana` },
      "application/mosskey-data": { source: `iana` },
      "application/mosskey-request": { source: `iana` },
      "application/mp21": { source: `iana`, extensions: [`m21`, `mp21`] },
      "application/mp4": {
        source: `iana`,
        extensions: [`mp4`, `mpg4`, `mp4s`, `m4p`],
      },
      "application/mpeg4-generic": { source: `iana` },
      "application/mpeg4-iod": { source: `iana` },
      "application/mpeg4-iod-xmt": { source: `iana` },
      "application/mrb-consumer+xml": { source: `iana`, compressible: !0 },
      "application/mrb-publish+xml": { source: `iana`, compressible: !0 },
      "application/msc-ivr+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/msc-mixer+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/msix": { compressible: !1, extensions: [`msix`] },
      "application/msixbundle": {
        compressible: !1,
        extensions: [`msixbundle`],
      },
      "application/msword": {
        source: `iana`,
        compressible: !1,
        extensions: [`doc`, `dot`],
      },
      "application/mud+json": { source: `iana`, compressible: !0 },
      "application/multipart-core": { source: `iana` },
      "application/mxf": { source: `iana`, extensions: [`mxf`] },
      "application/n-quads": { source: `iana`, extensions: [`nq`] },
      "application/n-triples": { source: `iana`, extensions: [`nt`] },
      "application/nasdata": { source: `iana` },
      "application/news-checkgroups": { source: `iana`, charset: `US-ASCII` },
      "application/news-groupinfo": { source: `iana`, charset: `US-ASCII` },
      "application/news-transmission": { source: `iana` },
      "application/nlsml+xml": { source: `iana`, compressible: !0 },
      "application/node": { source: `iana`, extensions: [`cjs`] },
      "application/nss": { source: `iana` },
      "application/oauth-authz-req+jwt": { source: `iana` },
      "application/oblivious-dns-message": { source: `iana` },
      "application/ocsp-request": { source: `iana` },
      "application/ocsp-response": { source: `iana` },
      "application/octet-stream": {
        source: `iana`,
        compressible: !0,
        extensions: [
          `bin`,
          `dms`,
          `lrf`,
          `mar`,
          `so`,
          `dist`,
          `distz`,
          `pkg`,
          `bpk`,
          `dump`,
          `elc`,
          `deploy`,
          `exe`,
          `dll`,
          `deb`,
          `dmg`,
          `iso`,
          `img`,
          `msi`,
          `msp`,
          `msm`,
          `buffer`,
        ],
      },
      "application/oda": { source: `iana`, extensions: [`oda`] },
      "application/odm+xml": { source: `iana`, compressible: !0 },
      "application/odx": { source: `iana` },
      "application/oebps-package+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`opf`],
      },
      "application/ogg": {
        source: `iana`,
        compressible: !1,
        extensions: [`ogx`],
      },
      "application/ohttp-keys": { source: `iana` },
      "application/omdoc+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`omdoc`],
      },
      "application/onenote": {
        source: `apache`,
        extensions: [`onetoc`, `onetoc2`, `onetmp`, `onepkg`, `one`, `onea`],
      },
      "application/opc-nodeset+xml": { source: `iana`, compressible: !0 },
      "application/oscore": { source: `iana` },
      "application/oxps": { source: `iana`, extensions: [`oxps`] },
      "application/p21": { source: `iana` },
      "application/p21+zip": { source: `iana`, compressible: !1 },
      "application/p2p-overlay+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`relo`],
      },
      "application/parityfec": { source: `iana` },
      "application/passport": { source: `iana` },
      "application/patch-ops-error+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xer`],
      },
      "application/pdf": {
        source: `iana`,
        compressible: !1,
        extensions: [`pdf`],
      },
      "application/pdx": { source: `iana` },
      "application/pem-certificate-chain": { source: `iana` },
      "application/pgp-encrypted": {
        source: `iana`,
        compressible: !1,
        extensions: [`pgp`],
      },
      "application/pgp-keys": { source: `iana`, extensions: [`asc`] },
      "application/pgp-signature": {
        source: `iana`,
        extensions: [`sig`, `asc`],
      },
      "application/pics-rules": { source: `apache`, extensions: [`prf`] },
      "application/pidf+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/pidf-diff+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/pkcs10": { source: `iana`, extensions: [`p10`] },
      "application/pkcs12": { source: `iana` },
      "application/pkcs7-mime": { source: `iana`, extensions: [`p7m`, `p7c`] },
      "application/pkcs7-signature": { source: `iana`, extensions: [`p7s`] },
      "application/pkcs8": { source: `iana`, extensions: [`p8`] },
      "application/pkcs8-encrypted": { source: `iana` },
      "application/pkix-attr-cert": { source: `iana`, extensions: [`ac`] },
      "application/pkix-cert": { source: `iana`, extensions: [`cer`] },
      "application/pkix-crl": { source: `iana`, extensions: [`crl`] },
      "application/pkix-pkipath": { source: `iana`, extensions: [`pkipath`] },
      "application/pkixcmp": { source: `iana`, extensions: [`pki`] },
      "application/pls+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`pls`],
      },
      "application/poc-settings+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/postscript": {
        source: `iana`,
        compressible: !0,
        extensions: [`ai`, `eps`, `ps`],
      },
      "application/ppsp-tracker+json": { source: `iana`, compressible: !0 },
      "application/private-token-issuer-directory": { source: `iana` },
      "application/private-token-request": { source: `iana` },
      "application/private-token-response": { source: `iana` },
      "application/problem+json": { source: `iana`, compressible: !0 },
      "application/problem+xml": { source: `iana`, compressible: !0 },
      "application/provenance+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`provx`],
      },
      "application/provided-claims+jwt": { source: `iana` },
      "application/prs.alvestrand.titrax-sheet": { source: `iana` },
      "application/prs.cww": { source: `iana`, extensions: [`cww`] },
      "application/prs.cyn": { source: `iana`, charset: `7-BIT` },
      "application/prs.hpub+zip": { source: `iana`, compressible: !1 },
      "application/prs.implied-document+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/prs.implied-executable": { source: `iana` },
      "application/prs.implied-object+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/prs.implied-object+json-seq": { source: `iana` },
      "application/prs.implied-object+yaml": { source: `iana` },
      "application/prs.implied-structure": { source: `iana` },
      "application/prs.mayfile": { source: `iana` },
      "application/prs.nprend": { source: `iana` },
      "application/prs.plucker": { source: `iana` },
      "application/prs.rdf-xml-crypt": { source: `iana` },
      "application/prs.vcfbzip2": { source: `iana` },
      "application/prs.xsf+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xsf`],
      },
      "application/pskc+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`pskcxml`],
      },
      "application/pvd+json": { source: `iana`, compressible: !0 },
      "application/qsig": { source: `iana` },
      "application/raml+yaml": { compressible: !0, extensions: [`raml`] },
      "application/raptorfec": { source: `iana` },
      "application/rdap+json": { source: `iana`, compressible: !0 },
      "application/rdf+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rdf`, `owl`],
      },
      "application/reginfo+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rif`],
      },
      "application/relax-ng-compact-syntax": {
        source: `iana`,
        extensions: [`rnc`],
      },
      "application/remote-printing": { source: `apache` },
      "application/reputon+json": { source: `iana`, compressible: !0 },
      "application/resolve-response+jwt": { source: `iana` },
      "application/resource-lists+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rl`],
      },
      "application/resource-lists-diff+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rld`],
      },
      "application/rfc+xml": { source: `iana`, compressible: !0 },
      "application/riscos": { source: `iana` },
      "application/rlmi+xml": { source: `iana`, compressible: !0 },
      "application/rls-services+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rs`],
      },
      "application/route-apd+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rapd`],
      },
      "application/route-s-tsid+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sls`],
      },
      "application/route-usd+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rusd`],
      },
      "application/rpki-checklist": { source: `iana` },
      "application/rpki-ghostbusters": { source: `iana`, extensions: [`gbr`] },
      "application/rpki-manifest": { source: `iana`, extensions: [`mft`] },
      "application/rpki-publication": { source: `iana` },
      "application/rpki-roa": { source: `iana`, extensions: [`roa`] },
      "application/rpki-signed-tal": { source: `iana` },
      "application/rpki-updown": { source: `iana` },
      "application/rsd+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`rsd`],
      },
      "application/rss+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`rss`],
      },
      "application/rtf": {
        source: `iana`,
        compressible: !0,
        extensions: [`rtf`],
      },
      "application/rtploopback": { source: `iana` },
      "application/rtx": { source: `iana` },
      "application/samlassertion+xml": { source: `iana`, compressible: !0 },
      "application/samlmetadata+xml": { source: `iana`, compressible: !0 },
      "application/sarif+json": { source: `iana`, compressible: !0 },
      "application/sarif-external-properties+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/sbe": { source: `iana` },
      "application/sbml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sbml`],
      },
      "application/scaip+xml": { source: `iana`, compressible: !0 },
      "application/scim+json": { source: `iana`, compressible: !0 },
      "application/scvp-cv-request": { source: `iana`, extensions: [`scq`] },
      "application/scvp-cv-response": { source: `iana`, extensions: [`scs`] },
      "application/scvp-vp-request": { source: `iana`, extensions: [`spq`] },
      "application/scvp-vp-response": { source: `iana`, extensions: [`spp`] },
      "application/sdp": { source: `iana`, extensions: [`sdp`] },
      "application/secevent+jwt": { source: `iana` },
      "application/senml+cbor": { source: `iana` },
      "application/senml+json": { source: `iana`, compressible: !0 },
      "application/senml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`senmlx`],
      },
      "application/senml-etch+cbor": { source: `iana` },
      "application/senml-etch+json": { source: `iana`, compressible: !0 },
      "application/senml-exi": { source: `iana` },
      "application/sensml+cbor": { source: `iana` },
      "application/sensml+json": { source: `iana`, compressible: !0 },
      "application/sensml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sensmlx`],
      },
      "application/sensml-exi": { source: `iana` },
      "application/sep+xml": { source: `iana`, compressible: !0 },
      "application/sep-exi": { source: `iana` },
      "application/session-info": { source: `iana` },
      "application/set-payment": { source: `iana` },
      "application/set-payment-initiation": {
        source: `iana`,
        extensions: [`setpay`],
      },
      "application/set-registration": { source: `iana` },
      "application/set-registration-initiation": {
        source: `iana`,
        extensions: [`setreg`],
      },
      "application/sgml": { source: `iana` },
      "application/sgml-open-catalog": { source: `iana` },
      "application/shf+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`shf`],
      },
      "application/sieve": { source: `iana`, extensions: [`siv`, `sieve`] },
      "application/simple-filter+xml": { source: `iana`, compressible: !0 },
      "application/simple-message-summary": { source: `iana` },
      "application/simplesymbolcontainer": { source: `iana` },
      "application/sipc": { source: `iana` },
      "application/slate": { source: `iana` },
      "application/smil": { source: `apache` },
      "application/smil+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`smi`, `smil`],
      },
      "application/smpte336m": { source: `iana` },
      "application/soap+fastinfoset": { source: `iana` },
      "application/soap+xml": { source: `iana`, compressible: !0 },
      "application/sparql-query": { source: `iana`, extensions: [`rq`] },
      "application/sparql-results+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`srx`],
      },
      "application/spdx+json": { source: `iana`, compressible: !0 },
      "application/spirits-event+xml": { source: `iana`, compressible: !0 },
      "application/sql": { source: `iana`, extensions: [`sql`] },
      "application/srgs": { source: `iana`, extensions: [`gram`] },
      "application/srgs+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`grxml`],
      },
      "application/sru+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sru`],
      },
      "application/ssdl+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`ssdl`],
      },
      "application/sslkeylogfile": { source: `iana` },
      "application/ssml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ssml`],
      },
      "application/st2110-41": { source: `iana` },
      "application/stix+json": { source: `iana`, compressible: !0 },
      "application/stratum": { source: `iana` },
      "application/swid+cbor": { source: `iana` },
      "application/swid+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`swidtag`],
      },
      "application/tamp-apex-update": { source: `iana` },
      "application/tamp-apex-update-confirm": { source: `iana` },
      "application/tamp-community-update": { source: `iana` },
      "application/tamp-community-update-confirm": { source: `iana` },
      "application/tamp-error": { source: `iana` },
      "application/tamp-sequence-adjust": { source: `iana` },
      "application/tamp-sequence-adjust-confirm": { source: `iana` },
      "application/tamp-status-query": { source: `iana` },
      "application/tamp-status-response": { source: `iana` },
      "application/tamp-update": { source: `iana` },
      "application/tamp-update-confirm": { source: `iana` },
      "application/tar": { compressible: !0 },
      "application/taxii+json": { source: `iana`, compressible: !0 },
      "application/td+json": { source: `iana`, compressible: !0 },
      "application/tei+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`tei`, `teicorpus`],
      },
      "application/tetra_isi": { source: `iana` },
      "application/thraud+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`tfi`],
      },
      "application/timestamp-query": { source: `iana` },
      "application/timestamp-reply": { source: `iana` },
      "application/timestamped-data": { source: `iana`, extensions: [`tsd`] },
      "application/tlsrpt+gzip": { source: `iana` },
      "application/tlsrpt+json": { source: `iana`, compressible: !0 },
      "application/tm+json": { source: `iana`, compressible: !0 },
      "application/tnauthlist": { source: `iana` },
      "application/toc+cbor": { source: `iana` },
      "application/token-introspection+jwt": { source: `iana` },
      "application/toml": {
        source: `iana`,
        compressible: !0,
        extensions: [`toml`],
      },
      "application/trickle-ice-sdpfrag": { source: `iana` },
      "application/trig": { source: `iana`, extensions: [`trig`] },
      "application/trust-chain+json": { source: `iana`, compressible: !0 },
      "application/trust-mark+jwt": { source: `iana` },
      "application/trust-mark-delegation+jwt": { source: `iana` },
      "application/ttml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ttml`],
      },
      "application/tve-trigger": { source: `iana` },
      "application/tzif": { source: `iana` },
      "application/tzif-leap": { source: `iana` },
      "application/ubjson": { compressible: !1, extensions: [`ubj`] },
      "application/uccs+cbor": { source: `iana` },
      "application/ujcs+json": { source: `iana`, compressible: !0 },
      "application/ulpfec": { source: `iana` },
      "application/urc-grpsheet+xml": { source: `iana`, compressible: !0 },
      "application/urc-ressheet+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rsheet`],
      },
      "application/urc-targetdesc+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`td`],
      },
      "application/urc-uisocketdesc+xml": { source: `iana`, compressible: !0 },
      "application/vc": { source: `iana` },
      "application/vc+cose": { source: `iana` },
      "application/vc+jwt": { source: `iana` },
      "application/vcard+json": { source: `iana`, compressible: !0 },
      "application/vcard+xml": { source: `iana`, compressible: !0 },
      "application/vemmi": { source: `iana` },
      "application/vividence.scriptfile": { source: `apache` },
      "application/vnd.1000minds.decision-model+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`1km`],
      },
      "application/vnd.1ob": { source: `iana` },
      "application/vnd.3gpp-prose+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-prose-pc3a+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp-prose-pc3ach+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp-prose-pc3ch+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp-prose-pc8+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp-v2x-local-service-information": { source: `iana` },
      "application/vnd.3gpp.5gnas": { source: `iana` },
      "application/vnd.3gpp.5gsa2x": { source: `iana` },
      "application/vnd.3gpp.5gsa2x-local-service-information": {
        source: `iana`,
      },
      "application/vnd.3gpp.5gsv2x": { source: `iana` },
      "application/vnd.3gpp.5gsv2x-local-service-information": {
        source: `iana`,
      },
      "application/vnd.3gpp.access-transfer-events+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.bsf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.crs+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.current-location-discovery+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.gmop+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.gtpc": { source: `iana` },
      "application/vnd.3gpp.interworking-data": { source: `iana` },
      "application/vnd.3gpp.lpp": { source: `iana` },
      "application/vnd.3gpp.mc-signalling-ear": { source: `iana` },
      "application/vnd.3gpp.mcdata-affiliation-command+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-msgstore-ctrl-request+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-payload": { source: `iana` },
      "application/vnd.3gpp.mcdata-regroup+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-service-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-signalling": { source: `iana` },
      "application/vnd.3gpp.mcdata-ue-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcdata-user-profile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-affiliation-command+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-floor-request+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-location-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-mbms-usage-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-regroup+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-service-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-signed+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-ue-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-ue-init-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcptt-user-profile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-affiliation-command+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-location-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-mbms-usage-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-regroup+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-service-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-transmission-request+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-ue-config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mcvideo-user-profile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.mid-call+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.ngap": { source: `iana` },
      "application/vnd.3gpp.pfcp": { source: `iana` },
      "application/vnd.3gpp.pic-bw-large": {
        source: `iana`,
        extensions: [`plb`],
      },
      "application/vnd.3gpp.pic-bw-small": {
        source: `iana`,
        extensions: [`psb`],
      },
      "application/vnd.3gpp.pic-bw-var": {
        source: `iana`,
        extensions: [`pvb`],
      },
      "application/vnd.3gpp.pinapp-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.s1ap": { source: `iana` },
      "application/vnd.3gpp.seal-group-doc+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-location-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-mbms-usage-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-network-qos-management-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-ue-config-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-unicast-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-user-profile-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.sms": { source: `iana` },
      "application/vnd.3gpp.sms+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.srvcc-ext+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.srvcc-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.state-and-event-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.ussd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.v2x": { source: `iana` },
      "application/vnd.3gpp.vae-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp2.bcmcsinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp2.sms": { source: `iana` },
      "application/vnd.3gpp2.tcap": { source: `iana`, extensions: [`tcap`] },
      "application/vnd.3lightssoftware.imagescal": { source: `iana` },
      "application/vnd.3m.post-it-notes": {
        source: `iana`,
        extensions: [`pwn`],
      },
      "application/vnd.accpac.simply.aso": {
        source: `iana`,
        extensions: [`aso`],
      },
      "application/vnd.accpac.simply.imp": {
        source: `iana`,
        extensions: [`imp`],
      },
      "application/vnd.acm.addressxfer+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.acm.chatbot+json": { source: `iana`, compressible: !0 },
      "application/vnd.acucobol": { source: `iana`, extensions: [`acu`] },
      "application/vnd.acucorp": {
        source: `iana`,
        extensions: [`atc`, `acutc`],
      },
      "application/vnd.adobe.air-application-installer-package+zip": {
        source: `apache`,
        compressible: !1,
        extensions: [`air`],
      },
      "application/vnd.adobe.flash.movie": { source: `iana` },
      "application/vnd.adobe.formscentral.fcdt": {
        source: `iana`,
        extensions: [`fcdt`],
      },
      "application/vnd.adobe.fxp": {
        source: `iana`,
        extensions: [`fxp`, `fxpl`],
      },
      "application/vnd.adobe.partial-upload": { source: `iana` },
      "application/vnd.adobe.xdp+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xdp`],
      },
      "application/vnd.adobe.xfdf": { source: `apache`, extensions: [`xfdf`] },
      "application/vnd.aether.imp": { source: `iana` },
      "application/vnd.afpc.afplinedata": { source: `iana` },
      "application/vnd.afpc.afplinedata-pagedef": { source: `iana` },
      "application/vnd.afpc.cmoca-cmresource": { source: `iana` },
      "application/vnd.afpc.foca-charset": { source: `iana` },
      "application/vnd.afpc.foca-codedfont": { source: `iana` },
      "application/vnd.afpc.foca-codepage": { source: `iana` },
      "application/vnd.afpc.modca": { source: `iana` },
      "application/vnd.afpc.modca-cmtable": { source: `iana` },
      "application/vnd.afpc.modca-formdef": { source: `iana` },
      "application/vnd.afpc.modca-mediummap": { source: `iana` },
      "application/vnd.afpc.modca-objectcontainer": { source: `iana` },
      "application/vnd.afpc.modca-overlay": { source: `iana` },
      "application/vnd.afpc.modca-pagesegment": { source: `iana` },
      "application/vnd.age": { source: `iana`, extensions: [`age`] },
      "application/vnd.ah-barcode": { source: `apache` },
      "application/vnd.ahead.space": { source: `iana`, extensions: [`ahead`] },
      "application/vnd.airzip.filesecure.azf": {
        source: `iana`,
        extensions: [`azf`],
      },
      "application/vnd.airzip.filesecure.azs": {
        source: `iana`,
        extensions: [`azs`],
      },
      "application/vnd.amadeus+json": { source: `iana`, compressible: !0 },
      "application/vnd.amazon.ebook": { source: `apache`, extensions: [`azw`] },
      "application/vnd.amazon.mobi8-ebook": { source: `iana` },
      "application/vnd.americandynamics.acc": {
        source: `iana`,
        extensions: [`acc`],
      },
      "application/vnd.amiga.ami": { source: `iana`, extensions: [`ami`] },
      "application/vnd.amundsen.maze+xml": { source: `iana`, compressible: !0 },
      "application/vnd.android.ota": { source: `iana` },
      "application/vnd.android.package-archive": {
        source: `apache`,
        compressible: !1,
        extensions: [`apk`],
      },
      "application/vnd.anki": { source: `iana` },
      "application/vnd.anser-web-certificate-issue-initiation": {
        source: `iana`,
        extensions: [`cii`],
      },
      "application/vnd.anser-web-funds-transfer-initiation": {
        source: `apache`,
        extensions: [`fti`],
      },
      "application/vnd.antix.game-component": {
        source: `iana`,
        extensions: [`atx`],
      },
      "application/vnd.apache.arrow.file": { source: `iana` },
      "application/vnd.apache.arrow.stream": { source: `iana` },
      "application/vnd.apache.parquet": { source: `iana` },
      "application/vnd.apache.thrift.binary": { source: `iana` },
      "application/vnd.apache.thrift.compact": { source: `iana` },
      "application/vnd.apache.thrift.json": { source: `iana` },
      "application/vnd.apexlang": { source: `iana` },
      "application/vnd.api+json": { source: `iana`, compressible: !0 },
      "application/vnd.aplextor.warrp+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.apothekende.reservation+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.apple.installer+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpkg`],
      },
      "application/vnd.apple.keynote": { source: `iana`, extensions: [`key`] },
      "application/vnd.apple.mpegurl": { source: `iana`, extensions: [`m3u8`] },
      "application/vnd.apple.numbers": {
        source: `iana`,
        extensions: [`numbers`],
      },
      "application/vnd.apple.pages": { source: `iana`, extensions: [`pages`] },
      "application/vnd.apple.pkpass": {
        compressible: !1,
        extensions: [`pkpass`],
      },
      "application/vnd.arastra.swi": { source: `apache` },
      "application/vnd.aristanetworks.swi": {
        source: `iana`,
        extensions: [`swi`],
      },
      "application/vnd.artisan+json": { source: `iana`, compressible: !0 },
      "application/vnd.artsquare": { source: `iana` },
      "application/vnd.astraea-software.iota": {
        source: `iana`,
        extensions: [`iota`],
      },
      "application/vnd.audiograph": { source: `iana`, extensions: [`aep`] },
      "application/vnd.autodesk.fbx": { extensions: [`fbx`] },
      "application/vnd.autopackage": { source: `iana` },
      "application/vnd.avalon+json": { source: `iana`, compressible: !0 },
      "application/vnd.avistar+xml": { source: `iana`, compressible: !0 },
      "application/vnd.balsamiq.bmml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`bmml`],
      },
      "application/vnd.balsamiq.bmpr": { source: `iana` },
      "application/vnd.banana-accounting": { source: `iana` },
      "application/vnd.bbf.usp.error": { source: `iana` },
      "application/vnd.bbf.usp.msg": { source: `iana` },
      "application/vnd.bbf.usp.msg+json": { source: `iana`, compressible: !0 },
      "application/vnd.bekitzur-stech+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.belightsoft.lhzd+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.belightsoft.lhzl+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.bint.med-content": { source: `iana` },
      "application/vnd.biopax.rdf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.blink-idb-value-wrapper": { source: `iana` },
      "application/vnd.blueice.multipass": {
        source: `iana`,
        extensions: [`mpm`],
      },
      "application/vnd.bluetooth.ep.oob": { source: `iana` },
      "application/vnd.bluetooth.le.oob": { source: `iana` },
      "application/vnd.bmi": { source: `iana`, extensions: [`bmi`] },
      "application/vnd.bpf": { source: `iana` },
      "application/vnd.bpf3": { source: `iana` },
      "application/vnd.businessobjects": {
        source: `iana`,
        extensions: [`rep`],
      },
      "application/vnd.byu.uapi+json": { source: `iana`, compressible: !0 },
      "application/vnd.bzip3": { source: `iana` },
      "application/vnd.c3voc.schedule+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.cab-jscript": { source: `iana` },
      "application/vnd.canon-cpdl": { source: `iana` },
      "application/vnd.canon-lips": { source: `iana` },
      "application/vnd.capasystems-pg+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.cendio.thinlinc.clientconf": { source: `iana` },
      "application/vnd.century-systems.tcp_stream": { source: `iana` },
      "application/vnd.chemdraw+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`cdxml`],
      },
      "application/vnd.chess-pgn": { source: `iana` },
      "application/vnd.chipnuts.karaoke-mmd": {
        source: `iana`,
        extensions: [`mmd`],
      },
      "application/vnd.ciedi": { source: `iana` },
      "application/vnd.cinderella": { source: `iana`, extensions: [`cdy`] },
      "application/vnd.cirpack.isdn-ext": { source: `iana` },
      "application/vnd.citationstyles.style+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`csl`],
      },
      "application/vnd.claymore": { source: `iana`, extensions: [`cla`] },
      "application/vnd.cloanto.rp9": { source: `iana`, extensions: [`rp9`] },
      "application/vnd.clonk.c4group": {
        source: `iana`,
        extensions: [`c4g`, `c4d`, `c4f`, `c4p`, `c4u`],
      },
      "application/vnd.cluetrust.cartomobile-config": {
        source: `iana`,
        extensions: [`c11amc`],
      },
      "application/vnd.cluetrust.cartomobile-config-pkg": {
        source: `iana`,
        extensions: [`c11amz`],
      },
      "application/vnd.cncf.helm.chart.content.v1.tar+gzip": { source: `iana` },
      "application/vnd.cncf.helm.chart.provenance.v1.prov": { source: `iana` },
      "application/vnd.cncf.helm.config.v1+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.coffeescript": { source: `iana` },
      "application/vnd.collabio.xodocuments.document": { source: `iana` },
      "application/vnd.collabio.xodocuments.document-template": {
        source: `iana`,
      },
      "application/vnd.collabio.xodocuments.presentation": { source: `iana` },
      "application/vnd.collabio.xodocuments.presentation-template": {
        source: `iana`,
      },
      "application/vnd.collabio.xodocuments.spreadsheet": { source: `iana` },
      "application/vnd.collabio.xodocuments.spreadsheet-template": {
        source: `iana`,
      },
      "application/vnd.collection+json": { source: `iana`, compressible: !0 },
      "application/vnd.collection.doc+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.collection.next+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.comicbook+zip": { source: `iana`, compressible: !1 },
      "application/vnd.comicbook-rar": { source: `iana` },
      "application/vnd.commerce-battelle": { source: `iana` },
      "application/vnd.commonspace": { source: `iana`, extensions: [`csp`] },
      "application/vnd.contact.cmsg": {
        source: `iana`,
        extensions: [`cdbcmsg`],
      },
      "application/vnd.coreos.ignition+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.cosmocaller": { source: `iana`, extensions: [`cmc`] },
      "application/vnd.crick.clicker": { source: `iana`, extensions: [`clkx`] },
      "application/vnd.crick.clicker.keyboard": {
        source: `iana`,
        extensions: [`clkk`],
      },
      "application/vnd.crick.clicker.palette": {
        source: `iana`,
        extensions: [`clkp`],
      },
      "application/vnd.crick.clicker.template": {
        source: `iana`,
        extensions: [`clkt`],
      },
      "application/vnd.crick.clicker.wordbank": {
        source: `iana`,
        extensions: [`clkw`],
      },
      "application/vnd.criticaltools.wbs+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wbs`],
      },
      "application/vnd.cryptii.pipe+json": { source: `iana`, compressible: !0 },
      "application/vnd.crypto-shade-file": { source: `iana` },
      "application/vnd.cryptomator.encrypted": { source: `iana` },
      "application/vnd.cryptomator.vault": { source: `iana` },
      "application/vnd.ctc-posml": { source: `iana`, extensions: [`pml`] },
      "application/vnd.ctct.ws+xml": { source: `iana`, compressible: !0 },
      "application/vnd.cups-pdf": { source: `iana` },
      "application/vnd.cups-postscript": { source: `iana` },
      "application/vnd.cups-ppd": { source: `iana`, extensions: [`ppd`] },
      "application/vnd.cups-raster": { source: `iana` },
      "application/vnd.cups-raw": { source: `iana` },
      "application/vnd.curl": { source: `iana` },
      "application/vnd.curl.car": { source: `apache`, extensions: [`car`] },
      "application/vnd.curl.pcurl": { source: `apache`, extensions: [`pcurl`] },
      "application/vnd.cyan.dean.root+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.cybank": { source: `iana` },
      "application/vnd.cyclonedx+json": { source: `iana`, compressible: !0 },
      "application/vnd.cyclonedx+xml": { source: `iana`, compressible: !0 },
      "application/vnd.d2l.coursepackage1p0+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.d3m-dataset": { source: `iana` },
      "application/vnd.d3m-problem": { source: `iana` },
      "application/vnd.dart": {
        source: `iana`,
        compressible: !0,
        extensions: [`dart`],
      },
      "application/vnd.data-vision.rdz": {
        source: `iana`,
        extensions: [`rdz`],
      },
      "application/vnd.datalog": { source: `iana` },
      "application/vnd.datapackage+json": { source: `iana`, compressible: !0 },
      "application/vnd.dataresource+json": { source: `iana`, compressible: !0 },
      "application/vnd.dbf": { source: `iana`, extensions: [`dbf`] },
      "application/vnd.dcmp+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`dcmp`],
      },
      "application/vnd.debian.binary-package": { source: `iana` },
      "application/vnd.dece.data": {
        source: `iana`,
        extensions: [`uvf`, `uvvf`, `uvd`, `uvvd`],
      },
      "application/vnd.dece.ttml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`uvt`, `uvvt`],
      },
      "application/vnd.dece.unspecified": {
        source: `iana`,
        extensions: [`uvx`, `uvvx`],
      },
      "application/vnd.dece.zip": {
        source: `iana`,
        extensions: [`uvz`, `uvvz`],
      },
      "application/vnd.denovo.fcselayout-link": {
        source: `iana`,
        extensions: [`fe_launch`],
      },
      "application/vnd.desmume.movie": { source: `iana` },
      "application/vnd.dir-bi.plate-dl-nosuffix": { source: `iana` },
      "application/vnd.dm.delegation+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dna": { source: `iana`, extensions: [`dna`] },
      "application/vnd.document+json": { source: `iana`, compressible: !0 },
      "application/vnd.dolby.mlp": { source: `apache`, extensions: [`mlp`] },
      "application/vnd.dolby.mobile.1": { source: `iana` },
      "application/vnd.dolby.mobile.2": { source: `iana` },
      "application/vnd.doremir.scorecloud-binary-document": { source: `iana` },
      "application/vnd.dpgraph": { source: `iana`, extensions: [`dpg`] },
      "application/vnd.dreamfactory": { source: `iana`, extensions: [`dfac`] },
      "application/vnd.drive+json": { source: `iana`, compressible: !0 },
      "application/vnd.ds-keypoint": { source: `apache`, extensions: [`kpxx`] },
      "application/vnd.dtg.local": { source: `iana` },
      "application/vnd.dtg.local.flash": { source: `iana` },
      "application/vnd.dtg.local.html": { source: `iana` },
      "application/vnd.dvb.ait": { source: `iana`, extensions: [`ait`] },
      "application/vnd.dvb.dvbisl+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.dvbj": { source: `iana` },
      "application/vnd.dvb.esgcontainer": { source: `iana` },
      "application/vnd.dvb.ipdcdftnotifaccess": { source: `iana` },
      "application/vnd.dvb.ipdcesgaccess": { source: `iana` },
      "application/vnd.dvb.ipdcesgaccess2": { source: `iana` },
      "application/vnd.dvb.ipdcesgpdd": { source: `iana` },
      "application/vnd.dvb.ipdcroaming": { source: `iana` },
      "application/vnd.dvb.iptv.alfec-base": { source: `iana` },
      "application/vnd.dvb.iptv.alfec-enhancement": { source: `iana` },
      "application/vnd.dvb.notif-aggregate-root+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-container+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-generic+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-ia-msglist+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-ia-registration-request+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-ia-registration-response+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-init+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.pfr": { source: `iana` },
      "application/vnd.dvb.service": { source: `iana`, extensions: [`svc`] },
      "application/vnd.dxr": { source: `iana` },
      "application/vnd.dynageo": { source: `iana`, extensions: [`geo`] },
      "application/vnd.dzr": { source: `iana` },
      "application/vnd.easykaraoke.cdgdownload": { source: `iana` },
      "application/vnd.ecdis-update": { source: `iana` },
      "application/vnd.ecip.rlp": { source: `iana` },
      "application/vnd.eclipse.ditto+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ecowin.chart": { source: `iana`, extensions: [`mag`] },
      "application/vnd.ecowin.filerequest": { source: `iana` },
      "application/vnd.ecowin.fileupdate": { source: `iana` },
      "application/vnd.ecowin.series": { source: `iana` },
      "application/vnd.ecowin.seriesrequest": { source: `iana` },
      "application/vnd.ecowin.seriesupdate": { source: `iana` },
      "application/vnd.efi.img": { source: `iana` },
      "application/vnd.efi.iso": { source: `iana` },
      "application/vnd.eln+zip": { source: `iana`, compressible: !1 },
      "application/vnd.emclient.accessrequest+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.enliven": { source: `iana`, extensions: [`nml`] },
      "application/vnd.enphase.envoy": { source: `iana` },
      "application/vnd.eprints.data+xml": { source: `iana`, compressible: !0 },
      "application/vnd.epson.esf": { source: `iana`, extensions: [`esf`] },
      "application/vnd.epson.msf": { source: `iana`, extensions: [`msf`] },
      "application/vnd.epson.quickanime": {
        source: `iana`,
        extensions: [`qam`],
      },
      "application/vnd.epson.salt": { source: `iana`, extensions: [`slt`] },
      "application/vnd.epson.ssf": { source: `iana`, extensions: [`ssf`] },
      "application/vnd.ericsson.quickcall": { source: `iana` },
      "application/vnd.erofs": { source: `iana` },
      "application/vnd.espass-espass+zip": { source: `iana`, compressible: !1 },
      "application/vnd.eszigno3+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`es3`, `et3`],
      },
      "application/vnd.etsi.aoc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.asic-e+zip": { source: `iana`, compressible: !1 },
      "application/vnd.etsi.asic-s+zip": { source: `iana`, compressible: !1 },
      "application/vnd.etsi.cug+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvcommand+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvdiscovery+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvprofile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvsad-bc+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvsad-cod+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvsad-npvr+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvservice+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.iptvsync+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvueprofile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.mcid+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.mheg5": { source: `iana` },
      "application/vnd.etsi.overload-control-policy-dataset+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.pstn+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.sci+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.simservs+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.timestamp-token": { source: `iana` },
      "application/vnd.etsi.tsl+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.tsl.der": { source: `iana` },
      "application/vnd.eu.kasparian.car+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.eudora.data": { source: `iana` },
      "application/vnd.evolv.ecig.profile": { source: `iana` },
      "application/vnd.evolv.ecig.settings": { source: `iana` },
      "application/vnd.evolv.ecig.theme": { source: `iana` },
      "application/vnd.exstream-empower+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.exstream-package": { source: `iana` },
      "application/vnd.ezpix-album": { source: `iana`, extensions: [`ez2`] },
      "application/vnd.ezpix-package": { source: `iana`, extensions: [`ez3`] },
      "application/vnd.f-secure.mobile": { source: `iana` },
      "application/vnd.familysearch.gedcom+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.fastcopy-disk-image": { source: `iana` },
      "application/vnd.fdf": { source: `apache`, extensions: [`fdf`] },
      "application/vnd.fdsn.mseed": { source: `iana`, extensions: [`mseed`] },
      "application/vnd.fdsn.seed": {
        source: `iana`,
        extensions: [`seed`, `dataless`],
      },
      "application/vnd.fdsn.stationxml+xml": {
        source: `iana`,
        charset: `XML-BASED`,
        compressible: !0,
      },
      "application/vnd.ffsns": { source: `iana` },
      "application/vnd.ficlab.flb+zip": { source: `iana`, compressible: !1 },
      "application/vnd.filmit.zfc": { source: `iana` },
      "application/vnd.fints": { source: `iana` },
      "application/vnd.firemonkeys.cloudcell": { source: `iana` },
      "application/vnd.flographit": { source: `iana`, extensions: [`gph`] },
      "application/vnd.fluxtime.clip": { source: `iana`, extensions: [`ftc`] },
      "application/vnd.font-fontforge-sfd": { source: `iana` },
      "application/vnd.framemaker": {
        source: `iana`,
        extensions: [`fm`, `frame`, `maker`, `book`],
      },
      "application/vnd.freelog.comic": { source: `iana` },
      "application/vnd.frogans.fnc": { source: `apache`, extensions: [`fnc`] },
      "application/vnd.frogans.ltf": { source: `apache`, extensions: [`ltf`] },
      "application/vnd.fsc.weblaunch": { source: `iana`, extensions: [`fsc`] },
      "application/vnd.fujifilm.fb.docuworks": { source: `iana` },
      "application/vnd.fujifilm.fb.docuworks.binder": { source: `iana` },
      "application/vnd.fujifilm.fb.docuworks.container": { source: `iana` },
      "application/vnd.fujifilm.fb.jfi+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.fujitsu.oasys": { source: `iana`, extensions: [`oas`] },
      "application/vnd.fujitsu.oasys2": { source: `iana`, extensions: [`oa2`] },
      "application/vnd.fujitsu.oasys3": { source: `iana`, extensions: [`oa3`] },
      "application/vnd.fujitsu.oasysgp": {
        source: `iana`,
        extensions: [`fg5`],
      },
      "application/vnd.fujitsu.oasysprs": {
        source: `iana`,
        extensions: [`bh2`],
      },
      "application/vnd.fujixerox.art-ex": { source: `iana` },
      "application/vnd.fujixerox.art4": { source: `iana` },
      "application/vnd.fujixerox.ddd": { source: `iana`, extensions: [`ddd`] },
      "application/vnd.fujixerox.docuworks": {
        source: `iana`,
        extensions: [`xdw`],
      },
      "application/vnd.fujixerox.docuworks.binder": {
        source: `iana`,
        extensions: [`xbd`],
      },
      "application/vnd.fujixerox.docuworks.container": { source: `iana` },
      "application/vnd.fujixerox.hbpl": { source: `iana` },
      "application/vnd.fut-misnet": { source: `iana` },
      "application/vnd.futoin+cbor": { source: `iana` },
      "application/vnd.futoin+json": { source: `iana`, compressible: !0 },
      "application/vnd.fuzzysheet": { source: `iana`, extensions: [`fzs`] },
      "application/vnd.ga4gh.passport+jwt": { source: `iana` },
      "application/vnd.genomatix.tuxedo": {
        source: `iana`,
        extensions: [`txd`],
      },
      "application/vnd.genozip": { source: `iana` },
      "application/vnd.gentics.grd+json": { source: `iana`, compressible: !0 },
      "application/vnd.gentoo.catmetadata+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.gentoo.ebuild": { source: `iana` },
      "application/vnd.gentoo.eclass": { source: `iana` },
      "application/vnd.gentoo.gpkg": { source: `iana` },
      "application/vnd.gentoo.manifest": { source: `iana` },
      "application/vnd.gentoo.pkgmetadata+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.gentoo.xpak": { source: `iana` },
      "application/vnd.geo+json": { source: `apache`, compressible: !0 },
      "application/vnd.geocube+xml": { source: `apache`, compressible: !0 },
      "application/vnd.geogebra.file": { source: `iana`, extensions: [`ggb`] },
      "application/vnd.geogebra.pinboard": { source: `iana` },
      "application/vnd.geogebra.slides": {
        source: `iana`,
        extensions: [`ggs`],
      },
      "application/vnd.geogebra.tool": { source: `iana`, extensions: [`ggt`] },
      "application/vnd.geometry-explorer": {
        source: `iana`,
        extensions: [`gex`, `gre`],
      },
      "application/vnd.geonext": { source: `iana`, extensions: [`gxt`] },
      "application/vnd.geoplan": { source: `iana`, extensions: [`g2w`] },
      "application/vnd.geospace": { source: `iana`, extensions: [`g3w`] },
      "application/vnd.gerber": { source: `iana` },
      "application/vnd.globalplatform.card-content-mgt": { source: `iana` },
      "application/vnd.globalplatform.card-content-mgt-response": {
        source: `iana`,
      },
      "application/vnd.gmx": { source: `iana`, extensions: [`gmx`] },
      "application/vnd.gnu.taler.exchange+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.gnu.taler.merchant+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.google-apps.audio": {},
      "application/vnd.google-apps.document": {
        compressible: !1,
        extensions: [`gdoc`],
      },
      "application/vnd.google-apps.drawing": {
        compressible: !1,
        extensions: [`gdraw`],
      },
      "application/vnd.google-apps.drive-sdk": { compressible: !1 },
      "application/vnd.google-apps.file": {},
      "application/vnd.google-apps.folder": { compressible: !1 },
      "application/vnd.google-apps.form": {
        compressible: !1,
        extensions: [`gform`],
      },
      "application/vnd.google-apps.fusiontable": {},
      "application/vnd.google-apps.jam": {
        compressible: !1,
        extensions: [`gjam`],
      },
      "application/vnd.google-apps.mail-layout": {},
      "application/vnd.google-apps.map": {
        compressible: !1,
        extensions: [`gmap`],
      },
      "application/vnd.google-apps.photo": {},
      "application/vnd.google-apps.presentation": {
        compressible: !1,
        extensions: [`gslides`],
      },
      "application/vnd.google-apps.script": {
        compressible: !1,
        extensions: [`gscript`],
      },
      "application/vnd.google-apps.shortcut": {},
      "application/vnd.google-apps.site": {
        compressible: !1,
        extensions: [`gsite`],
      },
      "application/vnd.google-apps.spreadsheet": {
        compressible: !1,
        extensions: [`gsheet`],
      },
      "application/vnd.google-apps.unknown": {},
      "application/vnd.google-apps.video": {},
      "application/vnd.google-earth.kml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`kml`],
      },
      "application/vnd.google-earth.kmz": {
        source: `iana`,
        compressible: !1,
        extensions: [`kmz`],
      },
      "application/vnd.gov.sk.e-form+xml": {
        source: `apache`,
        compressible: !0,
      },
      "application/vnd.gov.sk.e-form+zip": { source: `iana`, compressible: !1 },
      "application/vnd.gov.sk.xmldatacontainer+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xdcf`],
      },
      "application/vnd.gpxsee.map+xml": { source: `iana`, compressible: !0 },
      "application/vnd.grafeq": { source: `iana`, extensions: [`gqf`, `gqs`] },
      "application/vnd.gridmp": { source: `iana` },
      "application/vnd.groove-account": { source: `iana`, extensions: [`gac`] },
      "application/vnd.groove-help": { source: `iana`, extensions: [`ghf`] },
      "application/vnd.groove-identity-message": {
        source: `iana`,
        extensions: [`gim`],
      },
      "application/vnd.groove-injector": {
        source: `iana`,
        extensions: [`grv`],
      },
      "application/vnd.groove-tool-message": {
        source: `iana`,
        extensions: [`gtm`],
      },
      "application/vnd.groove-tool-template": {
        source: `iana`,
        extensions: [`tpl`],
      },
      "application/vnd.groove-vcard": { source: `iana`, extensions: [`vcg`] },
      "application/vnd.hal+json": { source: `iana`, compressible: !0 },
      "application/vnd.hal+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`hal`],
      },
      "application/vnd.handheld-entertainment+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`zmm`],
      },
      "application/vnd.hbci": { source: `iana`, extensions: [`hbci`] },
      "application/vnd.hc+json": { source: `iana`, compressible: !0 },
      "application/vnd.hcl-bireports": { source: `iana` },
      "application/vnd.hdt": { source: `iana` },
      "application/vnd.heroku+json": { source: `iana`, compressible: !0 },
      "application/vnd.hhe.lesson-player": {
        source: `iana`,
        extensions: [`les`],
      },
      "application/vnd.hp-hpgl": { source: `iana`, extensions: [`hpgl`] },
      "application/vnd.hp-hpid": { source: `iana`, extensions: [`hpid`] },
      "application/vnd.hp-hps": { source: `iana`, extensions: [`hps`] },
      "application/vnd.hp-jlyt": { source: `iana`, extensions: [`jlt`] },
      "application/vnd.hp-pcl": { source: `iana`, extensions: [`pcl`] },
      "application/vnd.hp-pclxl": { source: `iana`, extensions: [`pclxl`] },
      "application/vnd.hsl": { source: `iana` },
      "application/vnd.httphone": { source: `iana` },
      "application/vnd.hydrostatix.sof-data": {
        source: `iana`,
        extensions: [`sfd-hdstx`],
      },
      "application/vnd.hyper+json": { source: `iana`, compressible: !0 },
      "application/vnd.hyper-item+json": { source: `iana`, compressible: !0 },
      "application/vnd.hyperdrive+json": { source: `iana`, compressible: !0 },
      "application/vnd.hzn-3d-crossword": { source: `iana` },
      "application/vnd.ibm.afplinedata": { source: `apache` },
      "application/vnd.ibm.electronic-media": { source: `iana` },
      "application/vnd.ibm.minipay": { source: `iana`, extensions: [`mpy`] },
      "application/vnd.ibm.modcap": {
        source: `apache`,
        extensions: [`afp`, `listafp`, `list3820`],
      },
      "application/vnd.ibm.rights-management": {
        source: `iana`,
        extensions: [`irm`],
      },
      "application/vnd.ibm.secure-container": {
        source: `iana`,
        extensions: [`sc`],
      },
      "application/vnd.iccprofile": {
        source: `iana`,
        extensions: [`icc`, `icm`],
      },
      "application/vnd.ieee.1905": { source: `iana` },
      "application/vnd.igloader": { source: `iana`, extensions: [`igl`] },
      "application/vnd.imagemeter.folder+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.imagemeter.image+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.immervision-ivp": {
        source: `iana`,
        extensions: [`ivp`],
      },
      "application/vnd.immervision-ivu": {
        source: `iana`,
        extensions: [`ivu`],
      },
      "application/vnd.ims.imsccv1p1": { source: `iana` },
      "application/vnd.ims.imsccv1p2": { source: `iana` },
      "application/vnd.ims.imsccv1p3": { source: `iana` },
      "application/vnd.ims.lis.v2.result+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ims.lti.v2.toolconsumerprofile+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ims.lti.v2.toolproxy+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ims.lti.v2.toolproxy.id+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ims.lti.v2.toolsettings+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ims.lti.v2.toolsettings.simple+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.informedcontrol.rms+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.informix-visionary": { source: `apache` },
      "application/vnd.infotech.project": { source: `iana` },
      "application/vnd.infotech.project+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.innopath.wamp.notification": { source: `iana` },
      "application/vnd.insors.igm": { source: `iana`, extensions: [`igm`] },
      "application/vnd.intercon.formnet": {
        source: `iana`,
        extensions: [`xpw`, `xpx`],
      },
      "application/vnd.intergeo": { source: `iana`, extensions: [`i2g`] },
      "application/vnd.intertrust.digibox": { source: `iana` },
      "application/vnd.intertrust.nncp": { source: `iana` },
      "application/vnd.intu.qbo": { source: `iana`, extensions: [`qbo`] },
      "application/vnd.intu.qfx": { source: `iana`, extensions: [`qfx`] },
      "application/vnd.ipfs.ipns-record": { source: `iana` },
      "application/vnd.ipld.car": { source: `iana` },
      "application/vnd.ipld.dag-cbor": { source: `iana` },
      "application/vnd.ipld.dag-json": { source: `iana` },
      "application/vnd.ipld.raw": { source: `iana` },
      "application/vnd.iptc.g2.catalogitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.conceptitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.knowledgeitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.newsitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.newsmessage+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.packageitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.iptc.g2.planningitem+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ipunplugged.rcprofile": {
        source: `iana`,
        extensions: [`rcprofile`],
      },
      "application/vnd.irepository.package+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`irp`],
      },
      "application/vnd.is-xpr": { source: `iana`, extensions: [`xpr`] },
      "application/vnd.isac.fcs": { source: `iana`, extensions: [`fcs`] },
      "application/vnd.iso11783-10+zip": { source: `iana`, compressible: !1 },
      "application/vnd.jam": { source: `iana`, extensions: [`jam`] },
      "application/vnd.japannet-directory-service": { source: `iana` },
      "application/vnd.japannet-jpnstore-wakeup": { source: `iana` },
      "application/vnd.japannet-payment-wakeup": { source: `iana` },
      "application/vnd.japannet-registration": { source: `iana` },
      "application/vnd.japannet-registration-wakeup": { source: `iana` },
      "application/vnd.japannet-setstore-wakeup": { source: `iana` },
      "application/vnd.japannet-verification": { source: `iana` },
      "application/vnd.japannet-verification-wakeup": { source: `iana` },
      "application/vnd.jcp.javame.midlet-rms": {
        source: `iana`,
        extensions: [`rms`],
      },
      "application/vnd.jisp": { source: `iana`, extensions: [`jisp`] },
      "application/vnd.joost.joda-archive": {
        source: `iana`,
        extensions: [`joda`],
      },
      "application/vnd.jsk.isdn-ngn": { source: `iana` },
      "application/vnd.kahootz": { source: `iana`, extensions: [`ktz`, `ktr`] },
      "application/vnd.kde.karbon": { source: `iana`, extensions: [`karbon`] },
      "application/vnd.kde.kchart": { source: `iana`, extensions: [`chrt`] },
      "application/vnd.kde.kformula": { source: `iana`, extensions: [`kfo`] },
      "application/vnd.kde.kivio": { source: `iana`, extensions: [`flw`] },
      "application/vnd.kde.kontour": { source: `iana`, extensions: [`kon`] },
      "application/vnd.kde.kpresenter": {
        source: `iana`,
        extensions: [`kpr`, `kpt`],
      },
      "application/vnd.kde.kspread": { source: `iana`, extensions: [`ksp`] },
      "application/vnd.kde.kword": {
        source: `iana`,
        extensions: [`kwd`, `kwt`],
      },
      "application/vnd.kdl": { source: `iana` },
      "application/vnd.kenameaapp": { source: `iana`, extensions: [`htke`] },
      "application/vnd.keyman.kmp+zip": { source: `iana`, compressible: !1 },
      "application/vnd.keyman.kmx": { source: `iana` },
      "application/vnd.kidspiration": { source: `iana`, extensions: [`kia`] },
      "application/vnd.kinar": { source: `iana`, extensions: [`kne`, `knp`] },
      "application/vnd.koan": {
        source: `iana`,
        extensions: [`skp`, `skd`, `skt`, `skm`],
      },
      "application/vnd.kodak-descriptor": {
        source: `iana`,
        extensions: [`sse`],
      },
      "application/vnd.las": { source: `iana` },
      "application/vnd.las.las+json": { source: `iana`, compressible: !0 },
      "application/vnd.las.las+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`lasxml`],
      },
      "application/vnd.laszip": { source: `iana` },
      "application/vnd.ldev.productlicensing": { source: `iana` },
      "application/vnd.leap+json": { source: `iana`, compressible: !0 },
      "application/vnd.liberty-request+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.llamagraphics.life-balance.desktop": {
        source: `iana`,
        extensions: [`lbd`],
      },
      "application/vnd.llamagraphics.life-balance.exchange+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`lbe`],
      },
      "application/vnd.logipipe.circuit+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.loom": { source: `iana` },
      "application/vnd.lotus-1-2-3": { source: `iana`, extensions: [`123`] },
      "application/vnd.lotus-approach": { source: `iana`, extensions: [`apr`] },
      "application/vnd.lotus-freelance": {
        source: `iana`,
        extensions: [`pre`],
      },
      "application/vnd.lotus-notes": { source: `iana`, extensions: [`nsf`] },
      "application/vnd.lotus-organizer": {
        source: `iana`,
        extensions: [`org`],
      },
      "application/vnd.lotus-screencam": {
        source: `iana`,
        extensions: [`scm`],
      },
      "application/vnd.lotus-wordpro": { source: `iana`, extensions: [`lwp`] },
      "application/vnd.macports.portpkg": {
        source: `iana`,
        extensions: [`portpkg`],
      },
      "application/vnd.mapbox-vector-tile": {
        source: `iana`,
        extensions: [`mvt`],
      },
      "application/vnd.marlin.drm.actiontoken+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.marlin.drm.conftoken+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.marlin.drm.license+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.marlin.drm.mdcf": { source: `iana` },
      "application/vnd.mason+json": { source: `iana`, compressible: !0 },
      "application/vnd.maxar.archive.3tz+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.maxmind.maxmind-db": { source: `iana` },
      "application/vnd.mcd": { source: `iana`, extensions: [`mcd`] },
      "application/vnd.mdl": { source: `iana` },
      "application/vnd.mdl-mbsdf": { source: `iana` },
      "application/vnd.medcalcdata": { source: `iana`, extensions: [`mc1`] },
      "application/vnd.mediastation.cdkey": {
        source: `iana`,
        extensions: [`cdkey`],
      },
      "application/vnd.medicalholodeck.recordxr": { source: `iana` },
      "application/vnd.meridian-slingshot": { source: `iana` },
      "application/vnd.mermaid": { source: `iana` },
      "application/vnd.mfer": { source: `iana`, extensions: [`mwf`] },
      "application/vnd.mfmp": { source: `iana`, extensions: [`mfm`] },
      "application/vnd.micro+json": { source: `iana`, compressible: !0 },
      "application/vnd.micrografx.flo": { source: `iana`, extensions: [`flo`] },
      "application/vnd.micrografx.igx": { source: `iana`, extensions: [`igx`] },
      "application/vnd.microsoft.portable-executable": { source: `iana` },
      "application/vnd.microsoft.windows.thumbnail-cache": { source: `iana` },
      "application/vnd.miele+json": { source: `iana`, compressible: !0 },
      "application/vnd.mif": { source: `iana`, extensions: [`mif`] },
      "application/vnd.minisoft-hp3000-save": { source: `iana` },
      "application/vnd.mitsubishi.misty-guard.trustweb": { source: `iana` },
      "application/vnd.mobius.daf": { source: `iana`, extensions: [`daf`] },
      "application/vnd.mobius.dis": { source: `iana`, extensions: [`dis`] },
      "application/vnd.mobius.mbk": { source: `iana`, extensions: [`mbk`] },
      "application/vnd.mobius.mqy": { source: `iana`, extensions: [`mqy`] },
      "application/vnd.mobius.msl": { source: `iana`, extensions: [`msl`] },
      "application/vnd.mobius.plc": { source: `iana`, extensions: [`plc`] },
      "application/vnd.mobius.txf": { source: `iana`, extensions: [`txf`] },
      "application/vnd.modl": { source: `iana` },
      "application/vnd.mophun.application": {
        source: `iana`,
        extensions: [`mpn`],
      },
      "application/vnd.mophun.certificate": {
        source: `iana`,
        extensions: [`mpc`],
      },
      "application/vnd.motorola.flexsuite": { source: `iana` },
      "application/vnd.motorola.flexsuite.adsi": { source: `iana` },
      "application/vnd.motorola.flexsuite.fis": { source: `iana` },
      "application/vnd.motorola.flexsuite.gotap": { source: `iana` },
      "application/vnd.motorola.flexsuite.kmr": { source: `iana` },
      "application/vnd.motorola.flexsuite.ttc": { source: `iana` },
      "application/vnd.motorola.flexsuite.wem": { source: `iana` },
      "application/vnd.motorola.iprm": { source: `iana` },
      "application/vnd.mozilla.xul+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xul`],
      },
      "application/vnd.ms-3mfdocument": { source: `iana` },
      "application/vnd.ms-artgalry": { source: `iana`, extensions: [`cil`] },
      "application/vnd.ms-asf": { source: `iana` },
      "application/vnd.ms-cab-compressed": {
        source: `iana`,
        extensions: [`cab`],
      },
      "application/vnd.ms-color.iccprofile": { source: `apache` },
      "application/vnd.ms-excel": {
        source: `iana`,
        compressible: !1,
        extensions: [`xls`, `xlm`, `xla`, `xlc`, `xlt`, `xlw`],
      },
      "application/vnd.ms-excel.addin.macroenabled.12": {
        source: `iana`,
        extensions: [`xlam`],
      },
      "application/vnd.ms-excel.sheet.binary.macroenabled.12": {
        source: `iana`,
        extensions: [`xlsb`],
      },
      "application/vnd.ms-excel.sheet.macroenabled.12": {
        source: `iana`,
        extensions: [`xlsm`],
      },
      "application/vnd.ms-excel.template.macroenabled.12": {
        source: `iana`,
        extensions: [`xltm`],
      },
      "application/vnd.ms-fontobject": {
        source: `iana`,
        compressible: !0,
        extensions: [`eot`],
      },
      "application/vnd.ms-htmlhelp": { source: `iana`, extensions: [`chm`] },
      "application/vnd.ms-ims": { source: `iana`, extensions: [`ims`] },
      "application/vnd.ms-lrm": { source: `iana`, extensions: [`lrm`] },
      "application/vnd.ms-office.activex+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ms-officetheme": {
        source: `iana`,
        extensions: [`thmx`],
      },
      "application/vnd.ms-opentype": { source: `apache`, compressible: !0 },
      "application/vnd.ms-outlook": { compressible: !1, extensions: [`msg`] },
      "application/vnd.ms-package.obfuscated-opentype": { source: `apache` },
      "application/vnd.ms-pki.seccat": {
        source: `apache`,
        extensions: [`cat`],
      },
      "application/vnd.ms-pki.stl": { source: `apache`, extensions: [`stl`] },
      "application/vnd.ms-playready.initiator+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ms-powerpoint": {
        source: `iana`,
        compressible: !1,
        extensions: [`ppt`, `pps`, `pot`],
      },
      "application/vnd.ms-powerpoint.addin.macroenabled.12": {
        source: `iana`,
        extensions: [`ppam`],
      },
      "application/vnd.ms-powerpoint.presentation.macroenabled.12": {
        source: `iana`,
        extensions: [`pptm`],
      },
      "application/vnd.ms-powerpoint.slide.macroenabled.12": {
        source: `iana`,
        extensions: [`sldm`],
      },
      "application/vnd.ms-powerpoint.slideshow.macroenabled.12": {
        source: `iana`,
        extensions: [`ppsm`],
      },
      "application/vnd.ms-powerpoint.template.macroenabled.12": {
        source: `iana`,
        extensions: [`potm`],
      },
      "application/vnd.ms-printdevicecapabilities+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ms-printing.printticket+xml": {
        source: `apache`,
        compressible: !0,
      },
      "application/vnd.ms-printschematicket+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.ms-project": {
        source: `iana`,
        extensions: [`mpp`, `mpt`],
      },
      "application/vnd.ms-tnef": { source: `iana` },
      "application/vnd.ms-visio.viewer": { extensions: [`vdx`] },
      "application/vnd.ms-windows.devicepairing": { source: `iana` },
      "application/vnd.ms-windows.nwprinting.oob": { source: `iana` },
      "application/vnd.ms-windows.printerpairing": { source: `iana` },
      "application/vnd.ms-windows.wsd.oob": { source: `iana` },
      "application/vnd.ms-wmdrm.lic-chlg-req": { source: `iana` },
      "application/vnd.ms-wmdrm.lic-resp": { source: `iana` },
      "application/vnd.ms-wmdrm.meter-chlg-req": { source: `iana` },
      "application/vnd.ms-wmdrm.meter-resp": { source: `iana` },
      "application/vnd.ms-word.document.macroenabled.12": {
        source: `iana`,
        extensions: [`docm`],
      },
      "application/vnd.ms-word.template.macroenabled.12": {
        source: `iana`,
        extensions: [`dotm`],
      },
      "application/vnd.ms-works": {
        source: `iana`,
        extensions: [`wps`, `wks`, `wcm`, `wdb`],
      },
      "application/vnd.ms-wpl": { source: `iana`, extensions: [`wpl`] },
      "application/vnd.ms-xpsdocument": {
        source: `iana`,
        compressible: !1,
        extensions: [`xps`],
      },
      "application/vnd.msa-disk-image": { source: `iana` },
      "application/vnd.mseq": { source: `iana`, extensions: [`mseq`] },
      "application/vnd.msgpack": { source: `iana` },
      "application/vnd.msign": { source: `iana` },
      "application/vnd.multiad.creator": { source: `iana` },
      "application/vnd.multiad.creator.cif": { source: `iana` },
      "application/vnd.music-niff": { source: `iana` },
      "application/vnd.musician": { source: `iana`, extensions: [`mus`] },
      "application/vnd.muvee.style": { source: `iana`, extensions: [`msty`] },
      "application/vnd.mynfc": { source: `iana`, extensions: [`taglet`] },
      "application/vnd.nacamar.ybrid+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.nato.bindingdataobject+cbor": { source: `iana` },
      "application/vnd.nato.bindingdataobject+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.nato.bindingdataobject+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`bdo`],
      },
      "application/vnd.nato.openxmlformats-package.iepd+zip": {
        source: `iana`,
        compressible: !1,
      },
      "application/vnd.ncd.control": { source: `iana` },
      "application/vnd.ncd.reference": { source: `iana` },
      "application/vnd.nearst.inv+json": { source: `iana`, compressible: !0 },
      "application/vnd.nebumind.line": { source: `iana` },
      "application/vnd.nervana": { source: `iana` },
      "application/vnd.netfpx": { source: `iana` },
      "application/vnd.neurolanguage.nlu": {
        source: `iana`,
        extensions: [`nlu`],
      },
      "application/vnd.nimn": { source: `iana` },
      "application/vnd.nintendo.nitro.rom": { source: `iana` },
      "application/vnd.nintendo.snes.rom": { source: `iana` },
      "application/vnd.nitf": { source: `iana`, extensions: [`ntf`, `nitf`] },
      "application/vnd.noblenet-directory": {
        source: `iana`,
        extensions: [`nnd`],
      },
      "application/vnd.noblenet-sealer": {
        source: `iana`,
        extensions: [`nns`],
      },
      "application/vnd.noblenet-web": { source: `iana`, extensions: [`nnw`] },
      "application/vnd.nokia.catalogs": { source: `iana` },
      "application/vnd.nokia.conml+wbxml": { source: `iana` },
      "application/vnd.nokia.conml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.iptv.config+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.nokia.isds-radio-presets": { source: `iana` },
      "application/vnd.nokia.landmark+wbxml": { source: `iana` },
      "application/vnd.nokia.landmark+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.nokia.landmarkcollection+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.nokia.n-gage.ac+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ac`],
      },
      "application/vnd.nokia.n-gage.data": {
        source: `iana`,
        extensions: [`ngdat`],
      },
      "application/vnd.nokia.n-gage.symbian.install": {
        source: `apache`,
        extensions: [`n-gage`],
      },
      "application/vnd.nokia.ncd": { source: `iana` },
      "application/vnd.nokia.pcd+wbxml": { source: `iana` },
      "application/vnd.nokia.pcd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.radio-preset": {
        source: `iana`,
        extensions: [`rpst`],
      },
      "application/vnd.nokia.radio-presets": {
        source: `iana`,
        extensions: [`rpss`],
      },
      "application/vnd.novadigm.edm": { source: `iana`, extensions: [`edm`] },
      "application/vnd.novadigm.edx": { source: `iana`, extensions: [`edx`] },
      "application/vnd.novadigm.ext": { source: `iana`, extensions: [`ext`] },
      "application/vnd.ntt-local.content-share": { source: `iana` },
      "application/vnd.ntt-local.file-transfer": { source: `iana` },
      "application/vnd.ntt-local.ogw_remote-access": { source: `iana` },
      "application/vnd.ntt-local.sip-ta_remote": { source: `iana` },
      "application/vnd.ntt-local.sip-ta_tcp_stream": { source: `iana` },
      "application/vnd.oai.workflows": { source: `iana` },
      "application/vnd.oai.workflows+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oai.workflows+yaml": { source: `iana` },
      "application/vnd.oasis.opendocument.base": { source: `iana` },
      "application/vnd.oasis.opendocument.chart": {
        source: `iana`,
        extensions: [`odc`],
      },
      "application/vnd.oasis.opendocument.chart-template": {
        source: `iana`,
        extensions: [`otc`],
      },
      "application/vnd.oasis.opendocument.database": {
        source: `apache`,
        extensions: [`odb`],
      },
      "application/vnd.oasis.opendocument.formula": {
        source: `iana`,
        extensions: [`odf`],
      },
      "application/vnd.oasis.opendocument.formula-template": {
        source: `iana`,
        extensions: [`odft`],
      },
      "application/vnd.oasis.opendocument.graphics": {
        source: `iana`,
        compressible: !1,
        extensions: [`odg`],
      },
      "application/vnd.oasis.opendocument.graphics-template": {
        source: `iana`,
        extensions: [`otg`],
      },
      "application/vnd.oasis.opendocument.image": {
        source: `iana`,
        extensions: [`odi`],
      },
      "application/vnd.oasis.opendocument.image-template": {
        source: `iana`,
        extensions: [`oti`],
      },
      "application/vnd.oasis.opendocument.presentation": {
        source: `iana`,
        compressible: !1,
        extensions: [`odp`],
      },
      "application/vnd.oasis.opendocument.presentation-template": {
        source: `iana`,
        extensions: [`otp`],
      },
      "application/vnd.oasis.opendocument.spreadsheet": {
        source: `iana`,
        compressible: !1,
        extensions: [`ods`],
      },
      "application/vnd.oasis.opendocument.spreadsheet-template": {
        source: `iana`,
        extensions: [`ots`],
      },
      "application/vnd.oasis.opendocument.text": {
        source: `iana`,
        compressible: !1,
        extensions: [`odt`],
      },
      "application/vnd.oasis.opendocument.text-master": {
        source: `iana`,
        extensions: [`odm`],
      },
      "application/vnd.oasis.opendocument.text-master-template": {
        source: `iana`,
      },
      "application/vnd.oasis.opendocument.text-template": {
        source: `iana`,
        extensions: [`ott`],
      },
      "application/vnd.oasis.opendocument.text-web": {
        source: `iana`,
        extensions: [`oth`],
      },
      "application/vnd.obn": { source: `iana` },
      "application/vnd.ocf+cbor": { source: `iana` },
      "application/vnd.oci.image.manifest.v1+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oftn.l10n+json": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.contentaccessdownload+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.contentaccessstreaming+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.cspg-hexbinary": { source: `iana` },
      "application/vnd.oipf.dae.svg+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.dae.xhtml+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.mippvcontrolmessage+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.pae.gem": { source: `iana` },
      "application/vnd.oipf.spdiscovery+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.spdlist+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.ueprofile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oipf.userprofile+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.olpc-sugar": { source: `iana`, extensions: [`xo`] },
      "application/vnd.oma-scws-config": { source: `iana` },
      "application/vnd.oma-scws-http-request": { source: `iana` },
      "application/vnd.oma-scws-http-response": { source: `iana` },
      "application/vnd.oma.bcast.associated-procedure-parameter+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.drm-trigger+xml": {
        source: `apache`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.imd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.bcast.ltkm": { source: `iana` },
      "application/vnd.oma.bcast.notification+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.provisioningtrigger": { source: `iana` },
      "application/vnd.oma.bcast.sgboot": { source: `iana` },
      "application/vnd.oma.bcast.sgdd+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.sgdu": { source: `iana` },
      "application/vnd.oma.bcast.simple-symbol-container": { source: `iana` },
      "application/vnd.oma.bcast.smartcard-trigger+xml": {
        source: `apache`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.sprov+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.stkm": { source: `iana` },
      "application/vnd.oma.cab-address-book+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.cab-feature-handler+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.cab-pcc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.cab-subs-invite+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.cab-user-prefs+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.dcd": { source: `iana` },
      "application/vnd.oma.dcdc": { source: `iana` },
      "application/vnd.oma.dd2+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`dd2`],
      },
      "application/vnd.oma.drm.risd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.group-usage-list+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.lwm2m+cbor": { source: `iana` },
      "application/vnd.oma.lwm2m+json": { source: `iana`, compressible: !0 },
      "application/vnd.oma.lwm2m+tlv": { source: `iana` },
      "application/vnd.oma.pal+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.detailed-progress-report+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.poc.final-report+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.poc.groups+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.poc.invocation-descriptor+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.poc.optimized-progress-report+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.push": { source: `iana` },
      "application/vnd.oma.scidm.messages+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.xcap-directory+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.omads-email+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/vnd.omads-file+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/vnd.omads-folder+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/vnd.omaloc-supl-init": { source: `iana` },
      "application/vnd.onepager": { source: `iana` },
      "application/vnd.onepagertamp": { source: `iana` },
      "application/vnd.onepagertamx": { source: `iana` },
      "application/vnd.onepagertat": { source: `iana` },
      "application/vnd.onepagertatp": { source: `iana` },
      "application/vnd.onepagertatx": { source: `iana` },
      "application/vnd.onvif.metadata": { source: `iana` },
      "application/vnd.openblox.game+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`obgx`],
      },
      "application/vnd.openblox.game-binary": { source: `iana` },
      "application/vnd.openeye.oeb": { source: `iana` },
      "application/vnd.openofficeorg.extension": {
        source: `apache`,
        extensions: [`oxt`],
      },
      "application/vnd.openstreetmap.data+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`osm`],
      },
      "application/vnd.opentimestamps.ots": { source: `iana` },
      "application/vnd.openvpi.dspx+json": { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.custom-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.customxmlproperties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawing+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.chart+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.chartshapes+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramcolors+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramdata+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramlayout+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramstyle+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.extended-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.commentauthors+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.comments+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.handoutmaster+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.notesmaster+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.notesslide+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.presentation":
        { source: `iana`, compressible: !1, extensions: [`pptx`] },
      "application/vnd.openxmlformats-officedocument.presentationml.presentation.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.presprops+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.slide": {
        source: `iana`,
        extensions: [`sldx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slide+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.slidelayout+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.slidemaster+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.slideshow":
        { source: `iana`, extensions: [`ppsx`] },
      "application/vnd.openxmlformats-officedocument.presentationml.slideshow.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.slideupdateinfo+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.tablestyles+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.tags+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.template": {
        source: `iana`,
        extensions: [`potx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.template.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.presentationml.viewprops+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.calcchain+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.chartsheet+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.comments+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.connections+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.dialogsheet+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.externallink+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcachedefinition+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcacherecords+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivottable+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.querytable+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionheaders+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionlog+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sharedstrings+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
        source: `iana`,
        compressible: !1,
        extensions: [`xlsx`],
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheetmetadata+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.table+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.tablesinglecells+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.template": {
        source: `iana`,
        extensions: [`xltx`],
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.template.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.usernames+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.volatiledependencies+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.theme+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.themeoverride+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.vmldrawing": {
        source: `iana`,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.comments+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document":
        { source: `iana`, compressible: !1, extensions: [`docx`] },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document.glossary+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.endnotes+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.fonttable+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.footer+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.footnotes+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.numbering+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.settings+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.styles+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.template":
        { source: `iana`, extensions: [`dotx`] },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.template.main+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.websettings+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-package.core-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-package.digital-signature-xmlsignature+xml":
        { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-package.relationships+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oracle.resource+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.orange.indata": { source: `iana` },
      "application/vnd.osa.netdeploy": { source: `iana` },
      "application/vnd.osgeo.mapguide.package": {
        source: `iana`,
        extensions: [`mgp`],
      },
      "application/vnd.osgi.bundle": { source: `iana` },
      "application/vnd.osgi.dp": { source: `iana`, extensions: [`dp`] },
      "application/vnd.osgi.subsystem": { source: `iana`, extensions: [`esa`] },
      "application/vnd.otps.ct-kip+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oxli.countgraph": { source: `iana` },
      "application/vnd.pagerduty+json": { source: `iana`, compressible: !0 },
      "application/vnd.palm": {
        source: `iana`,
        extensions: [`pdb`, `pqa`, `oprc`],
      },
      "application/vnd.panoply": { source: `iana` },
      "application/vnd.paos.xml": { source: `iana` },
      "application/vnd.patentdive": { source: `iana` },
      "application/vnd.patientecommsdoc": { source: `iana` },
      "application/vnd.pawaafile": { source: `iana`, extensions: [`paw`] },
      "application/vnd.pcos": { source: `iana` },
      "application/vnd.pg.format": { source: `iana`, extensions: [`str`] },
      "application/vnd.pg.osasli": { source: `iana`, extensions: [`ei6`] },
      "application/vnd.piaccess.application-licence": { source: `iana` },
      "application/vnd.picsel": { source: `iana`, extensions: [`efif`] },
      "application/vnd.pmi.widget": { source: `iana`, extensions: [`wg`] },
      "application/vnd.poc.group-advertisement+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.pocketlearn": { source: `iana`, extensions: [`plf`] },
      "application/vnd.powerbuilder6": { source: `iana`, extensions: [`pbd`] },
      "application/vnd.powerbuilder6-s": { source: `iana` },
      "application/vnd.powerbuilder7": { source: `iana` },
      "application/vnd.powerbuilder7-s": { source: `iana` },
      "application/vnd.powerbuilder75": { source: `iana` },
      "application/vnd.powerbuilder75-s": { source: `iana` },
      "application/vnd.preminet": { source: `iana` },
      "application/vnd.previewsystems.box": {
        source: `iana`,
        extensions: [`box`],
      },
      "application/vnd.procrate.brushset": { extensions: [`brushset`] },
      "application/vnd.procreate.brush": { extensions: [`brush`] },
      "application/vnd.procreate.dream": { extensions: [`drm`] },
      "application/vnd.proteus.magazine": {
        source: `iana`,
        extensions: [`mgz`],
      },
      "application/vnd.psfs": { source: `iana` },
      "application/vnd.pt.mundusmundi": { source: `iana` },
      "application/vnd.publishare-delta-tree": {
        source: `iana`,
        extensions: [`qps`],
      },
      "application/vnd.pvi.ptid1": { source: `iana`, extensions: [`ptid`] },
      "application/vnd.pwg-multiplexed": { source: `iana` },
      "application/vnd.pwg-xhtml-print+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xhtm`],
      },
      "application/vnd.qualcomm.brew-app-res": { source: `iana` },
      "application/vnd.quarantainenet": { source: `iana` },
      "application/vnd.quark.quarkxpress": {
        source: `iana`,
        extensions: [`qxd`, `qxt`, `qwd`, `qwt`, `qxl`, `qxb`],
      },
      "application/vnd.quobject-quoxdocument": { source: `iana` },
      "application/vnd.radisys.moml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-audit-conf+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-audit-conn+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-audit-dialog+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-audit-stream+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-conf+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-base+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-fax-detect+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-fax-sendrecv+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-group+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-speech+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.radisys.msml-dialog-transform+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.rainstor.data": { source: `iana` },
      "application/vnd.rapid": { source: `iana` },
      "application/vnd.rar": { source: `iana`, extensions: [`rar`] },
      "application/vnd.realvnc.bed": { source: `iana`, extensions: [`bed`] },
      "application/vnd.recordare.musicxml": {
        source: `iana`,
        extensions: [`mxl`],
      },
      "application/vnd.recordare.musicxml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`musicxml`],
      },
      "application/vnd.relpipe": { source: `iana` },
      "application/vnd.renlearn.rlprint": { source: `iana` },
      "application/vnd.resilient.logic": { source: `iana` },
      "application/vnd.restful+json": { source: `iana`, compressible: !0 },
      "application/vnd.rig.cryptonote": {
        source: `iana`,
        extensions: [`cryptonote`],
      },
      "application/vnd.rim.cod": { source: `apache`, extensions: [`cod`] },
      "application/vnd.rn-realmedia": { source: `apache`, extensions: [`rm`] },
      "application/vnd.rn-realmedia-vbr": {
        source: `apache`,
        extensions: [`rmvb`],
      },
      "application/vnd.route66.link66+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`link66`],
      },
      "application/vnd.rs-274x": { source: `iana` },
      "application/vnd.ruckus.download": { source: `iana` },
      "application/vnd.s3sms": { source: `iana` },
      "application/vnd.sailingtracker.track": {
        source: `iana`,
        extensions: [`st`],
      },
      "application/vnd.sar": { source: `iana` },
      "application/vnd.sbm.cid": { source: `iana` },
      "application/vnd.sbm.mid2": { source: `iana` },
      "application/vnd.scribus": { source: `iana` },
      "application/vnd.sealed.3df": { source: `iana` },
      "application/vnd.sealed.csf": { source: `iana` },
      "application/vnd.sealed.doc": { source: `iana` },
      "application/vnd.sealed.eml": { source: `iana` },
      "application/vnd.sealed.mht": { source: `iana` },
      "application/vnd.sealed.net": { source: `iana` },
      "application/vnd.sealed.ppt": { source: `iana` },
      "application/vnd.sealed.tiff": { source: `iana` },
      "application/vnd.sealed.xls": { source: `iana` },
      "application/vnd.sealedmedia.softseal.html": { source: `iana` },
      "application/vnd.sealedmedia.softseal.pdf": { source: `iana` },
      "application/vnd.seemail": { source: `iana`, extensions: [`see`] },
      "application/vnd.seis+json": { source: `iana`, compressible: !0 },
      "application/vnd.sema": { source: `iana`, extensions: [`sema`] },
      "application/vnd.semd": { source: `iana`, extensions: [`semd`] },
      "application/vnd.semf": { source: `iana`, extensions: [`semf`] },
      "application/vnd.shade-save-file": { source: `iana` },
      "application/vnd.shana.informed.formdata": {
        source: `iana`,
        extensions: [`ifm`],
      },
      "application/vnd.shana.informed.formtemplate": {
        source: `iana`,
        extensions: [`itp`],
      },
      "application/vnd.shana.informed.interchange": {
        source: `iana`,
        extensions: [`iif`],
      },
      "application/vnd.shana.informed.package": {
        source: `iana`,
        extensions: [`ipk`],
      },
      "application/vnd.shootproof+json": { source: `iana`, compressible: !0 },
      "application/vnd.shopkick+json": { source: `iana`, compressible: !0 },
      "application/vnd.shp": { source: `iana` },
      "application/vnd.shx": { source: `iana` },
      "application/vnd.sigrok.session": { source: `iana` },
      "application/vnd.simtech-mindmapper": {
        source: `iana`,
        extensions: [`twd`, `twds`],
      },
      "application/vnd.siren+json": { source: `iana`, compressible: !0 },
      "application/vnd.sketchometry": { source: `iana` },
      "application/vnd.smaf": { source: `iana`, extensions: [`mmf`] },
      "application/vnd.smart.notebook": { source: `iana` },
      "application/vnd.smart.teacher": {
        source: `iana`,
        extensions: [`teacher`],
      },
      "application/vnd.smintio.portals.archive": { source: `iana` },
      "application/vnd.snesdev-page-table": { source: `iana` },
      "application/vnd.software602.filler.form+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`fo`],
      },
      "application/vnd.software602.filler.form-xml-zip": { source: `iana` },
      "application/vnd.solent.sdkm+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sdkm`, `sdkd`],
      },
      "application/vnd.spotfire.dxp": { source: `iana`, extensions: [`dxp`] },
      "application/vnd.spotfire.sfs": { source: `iana`, extensions: [`sfs`] },
      "application/vnd.sqlite3": { source: `iana` },
      "application/vnd.sss-cod": { source: `iana` },
      "application/vnd.sss-dtf": { source: `iana` },
      "application/vnd.sss-ntf": { source: `iana` },
      "application/vnd.stardivision.calc": {
        source: `apache`,
        extensions: [`sdc`],
      },
      "application/vnd.stardivision.draw": {
        source: `apache`,
        extensions: [`sda`],
      },
      "application/vnd.stardivision.impress": {
        source: `apache`,
        extensions: [`sdd`],
      },
      "application/vnd.stardivision.math": {
        source: `apache`,
        extensions: [`smf`],
      },
      "application/vnd.stardivision.writer": {
        source: `apache`,
        extensions: [`sdw`, `vor`],
      },
      "application/vnd.stardivision.writer-global": {
        source: `apache`,
        extensions: [`sgl`],
      },
      "application/vnd.stepmania.package": {
        source: `iana`,
        extensions: [`smzip`],
      },
      "application/vnd.stepmania.stepchart": {
        source: `iana`,
        extensions: [`sm`],
      },
      "application/vnd.street-stream": { source: `iana` },
      "application/vnd.sun.wadl+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wadl`],
      },
      "application/vnd.sun.xml.calc": { source: `apache`, extensions: [`sxc`] },
      "application/vnd.sun.xml.calc.template": {
        source: `apache`,
        extensions: [`stc`],
      },
      "application/vnd.sun.xml.draw": { source: `apache`, extensions: [`sxd`] },
      "application/vnd.sun.xml.draw.template": {
        source: `apache`,
        extensions: [`std`],
      },
      "application/vnd.sun.xml.impress": {
        source: `apache`,
        extensions: [`sxi`],
      },
      "application/vnd.sun.xml.impress.template": {
        source: `apache`,
        extensions: [`sti`],
      },
      "application/vnd.sun.xml.math": { source: `apache`, extensions: [`sxm`] },
      "application/vnd.sun.xml.writer": {
        source: `apache`,
        extensions: [`sxw`],
      },
      "application/vnd.sun.xml.writer.global": {
        source: `apache`,
        extensions: [`sxg`],
      },
      "application/vnd.sun.xml.writer.template": {
        source: `apache`,
        extensions: [`stw`],
      },
      "application/vnd.sus-calendar": {
        source: `iana`,
        extensions: [`sus`, `susp`],
      },
      "application/vnd.svd": { source: `iana`, extensions: [`svd`] },
      "application/vnd.swiftview-ics": { source: `iana` },
      "application/vnd.sybyl.mol2": { source: `iana` },
      "application/vnd.sycle+xml": { source: `iana`, compressible: !0 },
      "application/vnd.syft+json": { source: `iana`, compressible: !0 },
      "application/vnd.symbian.install": {
        source: `apache`,
        extensions: [`sis`, `sisx`],
      },
      "application/vnd.syncml+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`xsm`],
      },
      "application/vnd.syncml.dm+wbxml": {
        source: `iana`,
        charset: `UTF-8`,
        extensions: [`bdm`],
      },
      "application/vnd.syncml.dm+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`xdm`],
      },
      "application/vnd.syncml.dm.notification": { source: `iana` },
      "application/vnd.syncml.dmddf+wbxml": { source: `iana` },
      "application/vnd.syncml.dmddf+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`ddf`],
      },
      "application/vnd.syncml.dmtnds+wbxml": { source: `iana` },
      "application/vnd.syncml.dmtnds+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/vnd.syncml.ds.notification": { source: `iana` },
      "application/vnd.tableschema+json": { source: `iana`, compressible: !0 },
      "application/vnd.tao.intent-module-archive": {
        source: `iana`,
        extensions: [`tao`],
      },
      "application/vnd.tcpdump.pcap": {
        source: `iana`,
        extensions: [`pcap`, `cap`, `dmp`],
      },
      "application/vnd.think-cell.ppttc+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.tmd.mediaflex.api+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.tml": { source: `iana` },
      "application/vnd.tmobile-livetv": { source: `iana`, extensions: [`tmo`] },
      "application/vnd.tri.onesource": { source: `iana` },
      "application/vnd.trid.tpt": { source: `iana`, extensions: [`tpt`] },
      "application/vnd.triscape.mxs": { source: `iana`, extensions: [`mxs`] },
      "application/vnd.trueapp": { source: `iana`, extensions: [`tra`] },
      "application/vnd.truedoc": { source: `iana` },
      "application/vnd.ubisoft.webplayer": { source: `iana` },
      "application/vnd.ufdl": { source: `iana`, extensions: [`ufd`, `ufdl`] },
      "application/vnd.uic.osdm+json": { source: `iana`, compressible: !0 },
      "application/vnd.uiq.theme": { source: `iana`, extensions: [`utz`] },
      "application/vnd.umajin": { source: `iana`, extensions: [`umj`] },
      "application/vnd.unity": { source: `iana`, extensions: [`unityweb`] },
      "application/vnd.uoml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`uoml`, `uo`],
      },
      "application/vnd.uplanet.alert": { source: `iana` },
      "application/vnd.uplanet.alert-wbxml": { source: `iana` },
      "application/vnd.uplanet.bearer-choice": { source: `iana` },
      "application/vnd.uplanet.bearer-choice-wbxml": { source: `iana` },
      "application/vnd.uplanet.cacheop": { source: `iana` },
      "application/vnd.uplanet.cacheop-wbxml": { source: `iana` },
      "application/vnd.uplanet.channel": { source: `iana` },
      "application/vnd.uplanet.channel-wbxml": { source: `iana` },
      "application/vnd.uplanet.list": { source: `iana` },
      "application/vnd.uplanet.list-wbxml": { source: `iana` },
      "application/vnd.uplanet.listcmd": { source: `iana` },
      "application/vnd.uplanet.listcmd-wbxml": { source: `iana` },
      "application/vnd.uplanet.signal": { source: `iana` },
      "application/vnd.uri-map": { source: `iana` },
      "application/vnd.valve.source.material": { source: `iana` },
      "application/vnd.vcx": { source: `iana`, extensions: [`vcx`] },
      "application/vnd.vd-study": { source: `iana` },
      "application/vnd.vectorworks": { source: `iana` },
      "application/vnd.vel+json": { source: `iana`, compressible: !0 },
      "application/vnd.veraison.tsm-report+cbor": { source: `iana` },
      "application/vnd.veraison.tsm-report+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.verimatrix.vcas": { source: `iana` },
      "application/vnd.veritone.aion+json": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.veryant.thin": { source: `iana` },
      "application/vnd.ves.encrypted": { source: `iana` },
      "application/vnd.vidsoft.vidconference": { source: `iana` },
      "application/vnd.visio": {
        source: `iana`,
        extensions: [`vsd`, `vst`, `vss`, `vsw`, `vsdx`, `vtx`],
      },
      "application/vnd.visionary": { source: `iana`, extensions: [`vis`] },
      "application/vnd.vividence.scriptfile": { source: `iana` },
      "application/vnd.vocalshaper.vsp4": { source: `iana` },
      "application/vnd.vsf": { source: `iana`, extensions: [`vsf`] },
      "application/vnd.wap.sic": { source: `iana` },
      "application/vnd.wap.slc": { source: `iana` },
      "application/vnd.wap.wbxml": {
        source: `iana`,
        charset: `UTF-8`,
        extensions: [`wbxml`],
      },
      "application/vnd.wap.wmlc": { source: `iana`, extensions: [`wmlc`] },
      "application/vnd.wap.wmlscriptc": {
        source: `iana`,
        extensions: [`wmlsc`],
      },
      "application/vnd.wasmflow.wafl": { source: `iana` },
      "application/vnd.webturbo": { source: `iana`, extensions: [`wtb`] },
      "application/vnd.wfa.dpp": { source: `iana` },
      "application/vnd.wfa.p2p": { source: `iana` },
      "application/vnd.wfa.wsc": { source: `iana` },
      "application/vnd.windows.devicepairing": { source: `iana` },
      "application/vnd.wmc": { source: `iana` },
      "application/vnd.wmf.bootstrap": { source: `iana` },
      "application/vnd.wolfram.mathematica": { source: `iana` },
      "application/vnd.wolfram.mathematica.package": { source: `iana` },
      "application/vnd.wolfram.player": { source: `iana`, extensions: [`nbp`] },
      "application/vnd.wordlift": { source: `iana` },
      "application/vnd.wordperfect": { source: `iana`, extensions: [`wpd`] },
      "application/vnd.wqd": { source: `iana`, extensions: [`wqd`] },
      "application/vnd.wrq-hp3000-labelled": { source: `iana` },
      "application/vnd.wt.stf": { source: `iana`, extensions: [`stf`] },
      "application/vnd.wv.csp+wbxml": { source: `iana` },
      "application/vnd.wv.csp+xml": { source: `iana`, compressible: !0 },
      "application/vnd.wv.ssp+xml": { source: `iana`, compressible: !0 },
      "application/vnd.xacml+json": { source: `iana`, compressible: !0 },
      "application/vnd.xara": { source: `iana`, extensions: [`xar`] },
      "application/vnd.xarin.cpj": { source: `iana` },
      "application/vnd.xecrets-encrypted": { source: `iana` },
      "application/vnd.xfdl": { source: `iana`, extensions: [`xfdl`] },
      "application/vnd.xfdl.webform": { source: `iana` },
      "application/vnd.xmi+xml": { source: `iana`, compressible: !0 },
      "application/vnd.xmpie.cpkg": { source: `iana` },
      "application/vnd.xmpie.dpkg": { source: `iana` },
      "application/vnd.xmpie.plan": { source: `iana` },
      "application/vnd.xmpie.ppkg": { source: `iana` },
      "application/vnd.xmpie.xlim": { source: `iana` },
      "application/vnd.yamaha.hv-dic": { source: `iana`, extensions: [`hvd`] },
      "application/vnd.yamaha.hv-script": {
        source: `iana`,
        extensions: [`hvs`],
      },
      "application/vnd.yamaha.hv-voice": {
        source: `iana`,
        extensions: [`hvp`],
      },
      "application/vnd.yamaha.openscoreformat": {
        source: `iana`,
        extensions: [`osf`],
      },
      "application/vnd.yamaha.openscoreformat.osfpvg+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`osfpvg`],
      },
      "application/vnd.yamaha.remote-setup": { source: `iana` },
      "application/vnd.yamaha.smaf-audio": {
        source: `iana`,
        extensions: [`saf`],
      },
      "application/vnd.yamaha.smaf-phrase": {
        source: `iana`,
        extensions: [`spf`],
      },
      "application/vnd.yamaha.through-ngn": { source: `iana` },
      "application/vnd.yamaha.tunnel-udpencap": { source: `iana` },
      "application/vnd.yaoweme": { source: `iana` },
      "application/vnd.yellowriver-custom-menu": {
        source: `iana`,
        extensions: [`cmp`],
      },
      "application/vnd.zul": { source: `iana`, extensions: [`zir`, `zirz`] },
      "application/vnd.zzazz.deck+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`zaz`],
      },
      "application/voicexml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`vxml`],
      },
      "application/voucher-cms+json": { source: `iana`, compressible: !0 },
      "application/voucher-jws+json": { source: `iana`, compressible: !0 },
      "application/vp": { source: `iana` },
      "application/vp+cose": { source: `iana` },
      "application/vp+jwt": { source: `iana` },
      "application/vq-rtcpxr": { source: `iana` },
      "application/wasm": {
        source: `iana`,
        compressible: !0,
        extensions: [`wasm`],
      },
      "application/watcherinfo+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wif`],
      },
      "application/webpush-options+json": { source: `iana`, compressible: !0 },
      "application/whoispp-query": { source: `iana` },
      "application/whoispp-response": { source: `iana` },
      "application/widget": { source: `iana`, extensions: [`wgt`] },
      "application/winhlp": { source: `apache`, extensions: [`hlp`] },
      "application/wita": { source: `iana` },
      "application/wordperfect5.1": { source: `iana` },
      "application/wsdl+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wsdl`],
      },
      "application/wspolicy+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wspolicy`],
      },
      "application/x-7z-compressed": {
        source: `apache`,
        compressible: !1,
        extensions: [`7z`],
      },
      "application/x-abiword": { source: `apache`, extensions: [`abw`] },
      "application/x-ace-compressed": { source: `apache`, extensions: [`ace`] },
      "application/x-amf": { source: `apache` },
      "application/x-apple-diskimage": {
        source: `apache`,
        extensions: [`dmg`],
      },
      "application/x-arj": { compressible: !1, extensions: [`arj`] },
      "application/x-authorware-bin": {
        source: `apache`,
        extensions: [`aab`, `x32`, `u32`, `vox`],
      },
      "application/x-authorware-map": { source: `apache`, extensions: [`aam`] },
      "application/x-authorware-seg": { source: `apache`, extensions: [`aas`] },
      "application/x-bcpio": { source: `apache`, extensions: [`bcpio`] },
      "application/x-bdoc": { compressible: !1, extensions: [`bdoc`] },
      "application/x-bittorrent": { source: `apache`, extensions: [`torrent`] },
      "application/x-blender": { extensions: [`blend`] },
      "application/x-blorb": { source: `apache`, extensions: [`blb`, `blorb`] },
      "application/x-bzip": {
        source: `apache`,
        compressible: !1,
        extensions: [`bz`],
      },
      "application/x-bzip2": {
        source: `apache`,
        compressible: !1,
        extensions: [`bz2`, `boz`],
      },
      "application/x-cbr": {
        source: `apache`,
        extensions: [`cbr`, `cba`, `cbt`, `cbz`, `cb7`],
      },
      "application/x-cdlink": { source: `apache`, extensions: [`vcd`] },
      "application/x-cfs-compressed": { source: `apache`, extensions: [`cfs`] },
      "application/x-chat": { source: `apache`, extensions: [`chat`] },
      "application/x-chess-pgn": { source: `apache`, extensions: [`pgn`] },
      "application/x-chrome-extension": { extensions: [`crx`] },
      "application/x-cocoa": { source: `nginx`, extensions: [`cco`] },
      "application/x-compress": { source: `apache` },
      "application/x-compressed": { extensions: [`rar`] },
      "application/x-conference": { source: `apache`, extensions: [`nsc`] },
      "application/x-cpio": { source: `apache`, extensions: [`cpio`] },
      "application/x-csh": { source: `apache`, extensions: [`csh`] },
      "application/x-deb": { compressible: !1 },
      "application/x-debian-package": {
        source: `apache`,
        extensions: [`deb`, `udeb`],
      },
      "application/x-dgc-compressed": { source: `apache`, extensions: [`dgc`] },
      "application/x-director": {
        source: `apache`,
        extensions: [
          `dir`,
          `dcr`,
          `dxr`,
          `cst`,
          `cct`,
          `cxt`,
          `w3d`,
          `fgd`,
          `swa`,
        ],
      },
      "application/x-doom": { source: `apache`, extensions: [`wad`] },
      "application/x-dtbncx+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`ncx`],
      },
      "application/x-dtbook+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`dtb`],
      },
      "application/x-dtbresource+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`res`],
      },
      "application/x-dvi": {
        source: `apache`,
        compressible: !1,
        extensions: [`dvi`],
      },
      "application/x-envoy": { source: `apache`, extensions: [`evy`] },
      "application/x-eva": { source: `apache`, extensions: [`eva`] },
      "application/x-font-bdf": { source: `apache`, extensions: [`bdf`] },
      "application/x-font-dos": { source: `apache` },
      "application/x-font-framemaker": { source: `apache` },
      "application/x-font-ghostscript": {
        source: `apache`,
        extensions: [`gsf`],
      },
      "application/x-font-libgrx": { source: `apache` },
      "application/x-font-linux-psf": { source: `apache`, extensions: [`psf`] },
      "application/x-font-pcf": { source: `apache`, extensions: [`pcf`] },
      "application/x-font-snf": { source: `apache`, extensions: [`snf`] },
      "application/x-font-speedo": { source: `apache` },
      "application/x-font-sunos-news": { source: `apache` },
      "application/x-font-type1": {
        source: `apache`,
        extensions: [`pfa`, `pfb`, `pfm`, `afm`],
      },
      "application/x-font-vfont": { source: `apache` },
      "application/x-freearc": { source: `apache`, extensions: [`arc`] },
      "application/x-futuresplash": { source: `apache`, extensions: [`spl`] },
      "application/x-gca-compressed": { source: `apache`, extensions: [`gca`] },
      "application/x-glulx": { source: `apache`, extensions: [`ulx`] },
      "application/x-gnumeric": { source: `apache`, extensions: [`gnumeric`] },
      "application/x-gramps-xml": { source: `apache`, extensions: [`gramps`] },
      "application/x-gtar": { source: `apache`, extensions: [`gtar`] },
      "application/x-gzip": { source: `apache` },
      "application/x-hdf": { source: `apache`, extensions: [`hdf`] },
      "application/x-httpd-php": { compressible: !0, extensions: [`php`] },
      "application/x-install-instructions": {
        source: `apache`,
        extensions: [`install`],
      },
      "application/x-ipynb+json": { compressible: !0, extensions: [`ipynb`] },
      "application/x-iso9660-image": { source: `apache`, extensions: [`iso`] },
      "application/x-iwork-keynote-sffkey": { extensions: [`key`] },
      "application/x-iwork-numbers-sffnumbers": { extensions: [`numbers`] },
      "application/x-iwork-pages-sffpages": { extensions: [`pages`] },
      "application/x-java-archive-diff": {
        source: `nginx`,
        extensions: [`jardiff`],
      },
      "application/x-java-jnlp-file": {
        source: `apache`,
        compressible: !1,
        extensions: [`jnlp`],
      },
      "application/x-javascript": { compressible: !0 },
      "application/x-keepass2": { extensions: [`kdbx`] },
      "application/x-latex": {
        source: `apache`,
        compressible: !1,
        extensions: [`latex`],
      },
      "application/x-lua-bytecode": { extensions: [`luac`] },
      "application/x-lzh-compressed": {
        source: `apache`,
        extensions: [`lzh`, `lha`],
      },
      "application/x-makeself": { source: `nginx`, extensions: [`run`] },
      "application/x-mie": { source: `apache`, extensions: [`mie`] },
      "application/x-mobipocket-ebook": {
        source: `apache`,
        extensions: [`prc`, `mobi`],
      },
      "application/x-mpegurl": { compressible: !1 },
      "application/x-ms-application": {
        source: `apache`,
        extensions: [`application`],
      },
      "application/x-ms-shortcut": { source: `apache`, extensions: [`lnk`] },
      "application/x-ms-wmd": { source: `apache`, extensions: [`wmd`] },
      "application/x-ms-wmz": { source: `apache`, extensions: [`wmz`] },
      "application/x-ms-xbap": { source: `apache`, extensions: [`xbap`] },
      "application/x-msaccess": { source: `apache`, extensions: [`mdb`] },
      "application/x-msbinder": { source: `apache`, extensions: [`obd`] },
      "application/x-mscardfile": { source: `apache`, extensions: [`crd`] },
      "application/x-msclip": { source: `apache`, extensions: [`clp`] },
      "application/x-msdos-program": { extensions: [`exe`] },
      "application/x-msdownload": {
        source: `apache`,
        extensions: [`exe`, `dll`, `com`, `bat`, `msi`],
      },
      "application/x-msmediaview": {
        source: `apache`,
        extensions: [`mvb`, `m13`, `m14`],
      },
      "application/x-msmetafile": {
        source: `apache`,
        extensions: [`wmf`, `wmz`, `emf`, `emz`],
      },
      "application/x-msmoney": { source: `apache`, extensions: [`mny`] },
      "application/x-mspublisher": { source: `apache`, extensions: [`pub`] },
      "application/x-msschedule": { source: `apache`, extensions: [`scd`] },
      "application/x-msterminal": { source: `apache`, extensions: [`trm`] },
      "application/x-mswrite": { source: `apache`, extensions: [`wri`] },
      "application/x-netcdf": { source: `apache`, extensions: [`nc`, `cdf`] },
      "application/x-ns-proxy-autoconfig": {
        compressible: !0,
        extensions: [`pac`],
      },
      "application/x-nzb": { source: `apache`, extensions: [`nzb`] },
      "application/x-perl": { source: `nginx`, extensions: [`pl`, `pm`] },
      "application/x-pilot": { source: `nginx`, extensions: [`prc`, `pdb`] },
      "application/x-pkcs12": {
        source: `apache`,
        compressible: !1,
        extensions: [`p12`, `pfx`],
      },
      "application/x-pkcs7-certificates": {
        source: `apache`,
        extensions: [`p7b`, `spc`],
      },
      "application/x-pkcs7-certreqresp": {
        source: `apache`,
        extensions: [`p7r`],
      },
      "application/x-pki-message": { source: `iana` },
      "application/x-rar-compressed": {
        source: `apache`,
        compressible: !1,
        extensions: [`rar`],
      },
      "application/x-redhat-package-manager": {
        source: `nginx`,
        extensions: [`rpm`],
      },
      "application/x-research-info-systems": {
        source: `apache`,
        extensions: [`ris`],
      },
      "application/x-sea": { source: `nginx`, extensions: [`sea`] },
      "application/x-sh": {
        source: `apache`,
        compressible: !0,
        extensions: [`sh`],
      },
      "application/x-shar": { source: `apache`, extensions: [`shar`] },
      "application/x-shockwave-flash": {
        source: `apache`,
        compressible: !1,
        extensions: [`swf`],
      },
      "application/x-silverlight-app": {
        source: `apache`,
        extensions: [`xap`],
      },
      "application/x-sql": { source: `apache`, extensions: [`sql`] },
      "application/x-stuffit": {
        source: `apache`,
        compressible: !1,
        extensions: [`sit`],
      },
      "application/x-stuffitx": { source: `apache`, extensions: [`sitx`] },
      "application/x-subrip": { source: `apache`, extensions: [`srt`] },
      "application/x-sv4cpio": { source: `apache`, extensions: [`sv4cpio`] },
      "application/x-sv4crc": { source: `apache`, extensions: [`sv4crc`] },
      "application/x-t3vm-image": { source: `apache`, extensions: [`t3`] },
      "application/x-tads": { source: `apache`, extensions: [`gam`] },
      "application/x-tar": {
        source: `apache`,
        compressible: !0,
        extensions: [`tar`],
      },
      "application/x-tcl": { source: `apache`, extensions: [`tcl`, `tk`] },
      "application/x-tex": { source: `apache`, extensions: [`tex`] },
      "application/x-tex-tfm": { source: `apache`, extensions: [`tfm`] },
      "application/x-texinfo": {
        source: `apache`,
        extensions: [`texinfo`, `texi`],
      },
      "application/x-tgif": { source: `apache`, extensions: [`obj`] },
      "application/x-ustar": { source: `apache`, extensions: [`ustar`] },
      "application/x-virtualbox-hdd": { compressible: !0, extensions: [`hdd`] },
      "application/x-virtualbox-ova": { compressible: !0, extensions: [`ova`] },
      "application/x-virtualbox-ovf": { compressible: !0, extensions: [`ovf`] },
      "application/x-virtualbox-vbox": {
        compressible: !0,
        extensions: [`vbox`],
      },
      "application/x-virtualbox-vbox-extpack": {
        compressible: !1,
        extensions: [`vbox-extpack`],
      },
      "application/x-virtualbox-vdi": { compressible: !0, extensions: [`vdi`] },
      "application/x-virtualbox-vhd": { compressible: !0, extensions: [`vhd`] },
      "application/x-virtualbox-vmdk": {
        compressible: !0,
        extensions: [`vmdk`],
      },
      "application/x-wais-source": { source: `apache`, extensions: [`src`] },
      "application/x-web-app-manifest+json": {
        compressible: !0,
        extensions: [`webapp`],
      },
      "application/x-www-form-urlencoded": { source: `iana`, compressible: !0 },
      "application/x-x509-ca-cert": {
        source: `iana`,
        extensions: [`der`, `crt`, `pem`],
      },
      "application/x-x509-ca-ra-cert": { source: `iana` },
      "application/x-x509-next-ca-cert": { source: `iana` },
      "application/x-xfig": { source: `apache`, extensions: [`fig`] },
      "application/x-xliff+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`xlf`],
      },
      "application/x-xpinstall": {
        source: `apache`,
        compressible: !1,
        extensions: [`xpi`],
      },
      "application/x-xz": { source: `apache`, extensions: [`xz`] },
      "application/x-zip-compressed": { extensions: [`zip`] },
      "application/x-zmachine": {
        source: `apache`,
        extensions: [`z1`, `z2`, `z3`, `z4`, `z5`, `z6`, `z7`, `z8`],
      },
      "application/x400-bp": { source: `iana` },
      "application/xacml+xml": { source: `iana`, compressible: !0 },
      "application/xaml+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`xaml`],
      },
      "application/xcap-att+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xav`],
      },
      "application/xcap-caps+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xca`],
      },
      "application/xcap-diff+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xdf`],
      },
      "application/xcap-el+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xel`],
      },
      "application/xcap-error+xml": { source: `iana`, compressible: !0 },
      "application/xcap-ns+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xns`],
      },
      "application/xcon-conference-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/xcon-conference-info-diff+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/xenc+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xenc`],
      },
      "application/xfdf": { source: `iana`, extensions: [`xfdf`] },
      "application/xhtml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xhtml`, `xht`],
      },
      "application/xhtml-voice+xml": { source: `apache`, compressible: !0 },
      "application/xliff+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xlf`],
      },
      "application/xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xml`, `xsl`, `xsd`, `rng`],
      },
      "application/xml-dtd": {
        source: `iana`,
        compressible: !0,
        extensions: [`dtd`],
      },
      "application/xml-external-parsed-entity": { source: `iana` },
      "application/xml-patch+xml": { source: `iana`, compressible: !0 },
      "application/xmpp+xml": { source: `iana`, compressible: !0 },
      "application/xop+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xop`],
      },
      "application/xproc+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`xpl`],
      },
      "application/xslt+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xsl`, `xslt`],
      },
      "application/xspf+xml": {
        source: `apache`,
        compressible: !0,
        extensions: [`xspf`],
      },
      "application/xv+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mxml`, `xhvml`, `xvml`, `xvm`],
      },
      "application/yaml": { source: `iana` },
      "application/yang": { source: `iana`, extensions: [`yang`] },
      "application/yang-data+cbor": { source: `iana` },
      "application/yang-data+json": { source: `iana`, compressible: !0 },
      "application/yang-data+xml": { source: `iana`, compressible: !0 },
      "application/yang-patch+json": { source: `iana`, compressible: !0 },
      "application/yang-patch+xml": { source: `iana`, compressible: !0 },
      "application/yang-sid+json": { source: `iana`, compressible: !0 },
      "application/yin+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`yin`],
      },
      "application/zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`zip`],
      },
      "application/zip+dotlottie": { extensions: [`lottie`] },
      "application/zlib": { source: `iana` },
      "application/zstd": { source: `iana` },
      "audio/1d-interleaved-parityfec": { source: `iana` },
      "audio/32kadpcm": { source: `iana` },
      "audio/3gpp": { source: `iana`, compressible: !1, extensions: [`3gpp`] },
      "audio/3gpp2": { source: `iana` },
      "audio/aac": { source: `iana`, extensions: [`adts`, `aac`] },
      "audio/ac3": { source: `iana` },
      "audio/adpcm": { source: `apache`, extensions: [`adp`] },
      "audio/amr": { source: `iana`, extensions: [`amr`] },
      "audio/amr-wb": { source: `iana` },
      "audio/amr-wb+": { source: `iana` },
      "audio/aptx": { source: `iana` },
      "audio/asc": { source: `iana` },
      "audio/atrac-advanced-lossless": { source: `iana` },
      "audio/atrac-x": { source: `iana` },
      "audio/atrac3": { source: `iana` },
      "audio/basic": {
        source: `iana`,
        compressible: !1,
        extensions: [`au`, `snd`],
      },
      "audio/bv16": { source: `iana` },
      "audio/bv32": { source: `iana` },
      "audio/clearmode": { source: `iana` },
      "audio/cn": { source: `iana` },
      "audio/dat12": { source: `iana` },
      "audio/dls": { source: `iana` },
      "audio/dsr-es201108": { source: `iana` },
      "audio/dsr-es202050": { source: `iana` },
      "audio/dsr-es202211": { source: `iana` },
      "audio/dsr-es202212": { source: `iana` },
      "audio/dv": { source: `iana` },
      "audio/dvi4": { source: `iana` },
      "audio/eac3": { source: `iana` },
      "audio/encaprtp": { source: `iana` },
      "audio/evrc": { source: `iana` },
      "audio/evrc-qcp": { source: `iana` },
      "audio/evrc0": { source: `iana` },
      "audio/evrc1": { source: `iana` },
      "audio/evrcb": { source: `iana` },
      "audio/evrcb0": { source: `iana` },
      "audio/evrcb1": { source: `iana` },
      "audio/evrcnw": { source: `iana` },
      "audio/evrcnw0": { source: `iana` },
      "audio/evrcnw1": { source: `iana` },
      "audio/evrcwb": { source: `iana` },
      "audio/evrcwb0": { source: `iana` },
      "audio/evrcwb1": { source: `iana` },
      "audio/evs": { source: `iana` },
      "audio/flac": { source: `iana` },
      "audio/flexfec": { source: `iana` },
      "audio/fwdred": { source: `iana` },
      "audio/g711-0": { source: `iana` },
      "audio/g719": { source: `iana` },
      "audio/g722": { source: `iana` },
      "audio/g7221": { source: `iana` },
      "audio/g723": { source: `iana` },
      "audio/g726-16": { source: `iana` },
      "audio/g726-24": { source: `iana` },
      "audio/g726-32": { source: `iana` },
      "audio/g726-40": { source: `iana` },
      "audio/g728": { source: `iana` },
      "audio/g729": { source: `iana` },
      "audio/g7291": { source: `iana` },
      "audio/g729d": { source: `iana` },
      "audio/g729e": { source: `iana` },
      "audio/gsm": { source: `iana` },
      "audio/gsm-efr": { source: `iana` },
      "audio/gsm-hr-08": { source: `iana` },
      "audio/ilbc": { source: `iana` },
      "audio/ip-mr_v2.5": { source: `iana` },
      "audio/isac": { source: `apache` },
      "audio/l16": { source: `iana` },
      "audio/l20": { source: `iana` },
      "audio/l24": { source: `iana`, compressible: !1 },
      "audio/l8": { source: `iana` },
      "audio/lpc": { source: `iana` },
      "audio/matroska": { source: `iana` },
      "audio/melp": { source: `iana` },
      "audio/melp1200": { source: `iana` },
      "audio/melp2400": { source: `iana` },
      "audio/melp600": { source: `iana` },
      "audio/mhas": { source: `iana` },
      "audio/midi": {
        source: `apache`,
        extensions: [`mid`, `midi`, `kar`, `rmi`],
      },
      "audio/midi-clip": { source: `iana` },
      "audio/mobile-xmf": { source: `iana`, extensions: [`mxmf`] },
      "audio/mp3": { compressible: !1, extensions: [`mp3`] },
      "audio/mp4": {
        source: `iana`,
        compressible: !1,
        extensions: [`m4a`, `mp4a`, `m4b`],
      },
      "audio/mp4a-latm": { source: `iana` },
      "audio/mpa": { source: `iana` },
      "audio/mpa-robust": { source: `iana` },
      "audio/mpeg": {
        source: `iana`,
        compressible: !1,
        extensions: [`mpga`, `mp2`, `mp2a`, `mp3`, `m2a`, `m3a`],
      },
      "audio/mpeg4-generic": { source: `iana` },
      "audio/musepack": { source: `apache` },
      "audio/ogg": {
        source: `iana`,
        compressible: !1,
        extensions: [`oga`, `ogg`, `spx`, `opus`],
      },
      "audio/opus": { source: `iana` },
      "audio/parityfec": { source: `iana` },
      "audio/pcma": { source: `iana` },
      "audio/pcma-wb": { source: `iana` },
      "audio/pcmu": { source: `iana` },
      "audio/pcmu-wb": { source: `iana` },
      "audio/prs.sid": { source: `iana` },
      "audio/qcelp": { source: `iana` },
      "audio/raptorfec": { source: `iana` },
      "audio/red": { source: `iana` },
      "audio/rtp-enc-aescm128": { source: `iana` },
      "audio/rtp-midi": { source: `iana` },
      "audio/rtploopback": { source: `iana` },
      "audio/rtx": { source: `iana` },
      "audio/s3m": { source: `apache`, extensions: [`s3m`] },
      "audio/scip": { source: `iana` },
      "audio/silk": { source: `apache`, extensions: [`sil`] },
      "audio/smv": { source: `iana` },
      "audio/smv-qcp": { source: `iana` },
      "audio/smv0": { source: `iana` },
      "audio/sofa": { source: `iana` },
      "audio/sp-midi": { source: `iana` },
      "audio/speex": { source: `iana` },
      "audio/t140c": { source: `iana` },
      "audio/t38": { source: `iana` },
      "audio/telephone-event": { source: `iana` },
      "audio/tetra_acelp": { source: `iana` },
      "audio/tetra_acelp_bb": { source: `iana` },
      "audio/tone": { source: `iana` },
      "audio/tsvcis": { source: `iana` },
      "audio/uemclip": { source: `iana` },
      "audio/ulpfec": { source: `iana` },
      "audio/usac": { source: `iana` },
      "audio/vdvi": { source: `iana` },
      "audio/vmr-wb": { source: `iana` },
      "audio/vnd.3gpp.iufp": { source: `iana` },
      "audio/vnd.4sb": { source: `iana` },
      "audio/vnd.audiokoz": { source: `iana` },
      "audio/vnd.celp": { source: `iana` },
      "audio/vnd.cisco.nse": { source: `iana` },
      "audio/vnd.cmles.radio-events": { source: `iana` },
      "audio/vnd.cns.anp1": { source: `iana` },
      "audio/vnd.cns.inf1": { source: `iana` },
      "audio/vnd.dece.audio": { source: `iana`, extensions: [`uva`, `uvva`] },
      "audio/vnd.digital-winds": { source: `iana`, extensions: [`eol`] },
      "audio/vnd.dlna.adts": { source: `iana` },
      "audio/vnd.dolby.heaac.1": { source: `iana` },
      "audio/vnd.dolby.heaac.2": { source: `iana` },
      "audio/vnd.dolby.mlp": { source: `iana` },
      "audio/vnd.dolby.mps": { source: `iana` },
      "audio/vnd.dolby.pl2": { source: `iana` },
      "audio/vnd.dolby.pl2x": { source: `iana` },
      "audio/vnd.dolby.pl2z": { source: `iana` },
      "audio/vnd.dolby.pulse.1": { source: `iana` },
      "audio/vnd.dra": { source: `iana`, extensions: [`dra`] },
      "audio/vnd.dts": { source: `iana`, extensions: [`dts`] },
      "audio/vnd.dts.hd": { source: `iana`, extensions: [`dtshd`] },
      "audio/vnd.dts.uhd": { source: `iana` },
      "audio/vnd.dvb.file": { source: `iana` },
      "audio/vnd.everad.plj": { source: `iana` },
      "audio/vnd.hns.audio": { source: `iana` },
      "audio/vnd.lucent.voice": { source: `iana`, extensions: [`lvp`] },
      "audio/vnd.ms-playready.media.pya": {
        source: `iana`,
        extensions: [`pya`],
      },
      "audio/vnd.nokia.mobile-xmf": { source: `iana` },
      "audio/vnd.nortel.vbk": { source: `iana` },
      "audio/vnd.nuera.ecelp4800": {
        source: `iana`,
        extensions: [`ecelp4800`],
      },
      "audio/vnd.nuera.ecelp7470": {
        source: `iana`,
        extensions: [`ecelp7470`],
      },
      "audio/vnd.nuera.ecelp9600": {
        source: `iana`,
        extensions: [`ecelp9600`],
      },
      "audio/vnd.octel.sbc": { source: `iana` },
      "audio/vnd.presonus.multitrack": { source: `iana` },
      "audio/vnd.qcelp": { source: `apache` },
      "audio/vnd.rhetorex.32kadpcm": { source: `iana` },
      "audio/vnd.rip": { source: `iana`, extensions: [`rip`] },
      "audio/vnd.rn-realaudio": { compressible: !1 },
      "audio/vnd.sealedmedia.softseal.mpeg": { source: `iana` },
      "audio/vnd.vmx.cvsd": { source: `iana` },
      "audio/vnd.wave": { compressible: !1 },
      "audio/vorbis": { source: `iana`, compressible: !1 },
      "audio/vorbis-config": { source: `iana` },
      "audio/wav": { compressible: !1, extensions: [`wav`] },
      "audio/wave": { compressible: !1, extensions: [`wav`] },
      "audio/webm": {
        source: `apache`,
        compressible: !1,
        extensions: [`weba`],
      },
      "audio/x-aac": {
        source: `apache`,
        compressible: !1,
        extensions: [`aac`],
      },
      "audio/x-aiff": { source: `apache`, extensions: [`aif`, `aiff`, `aifc`] },
      "audio/x-caf": {
        source: `apache`,
        compressible: !1,
        extensions: [`caf`],
      },
      "audio/x-flac": { source: `apache`, extensions: [`flac`] },
      "audio/x-m4a": { source: `nginx`, extensions: [`m4a`] },
      "audio/x-matroska": { source: `apache`, extensions: [`mka`] },
      "audio/x-mpegurl": { source: `apache`, extensions: [`m3u`] },
      "audio/x-ms-wax": { source: `apache`, extensions: [`wax`] },
      "audio/x-ms-wma": { source: `apache`, extensions: [`wma`] },
      "audio/x-pn-realaudio": { source: `apache`, extensions: [`ram`, `ra`] },
      "audio/x-pn-realaudio-plugin": { source: `apache`, extensions: [`rmp`] },
      "audio/x-realaudio": { source: `nginx`, extensions: [`ra`] },
      "audio/x-tta": { source: `apache` },
      "audio/x-wav": { source: `apache`, extensions: [`wav`] },
      "audio/xm": { source: `apache`, extensions: [`xm`] },
      "chemical/x-cdx": { source: `apache`, extensions: [`cdx`] },
      "chemical/x-cif": { source: `apache`, extensions: [`cif`] },
      "chemical/x-cmdf": { source: `apache`, extensions: [`cmdf`] },
      "chemical/x-cml": { source: `apache`, extensions: [`cml`] },
      "chemical/x-csml": { source: `apache`, extensions: [`csml`] },
      "chemical/x-pdb": { source: `apache` },
      "chemical/x-xyz": { source: `apache`, extensions: [`xyz`] },
      "font/collection": { source: `iana`, extensions: [`ttc`] },
      "font/otf": { source: `iana`, compressible: !0, extensions: [`otf`] },
      "font/sfnt": { source: `iana` },
      "font/ttf": { source: `iana`, compressible: !0, extensions: [`ttf`] },
      "font/woff": { source: `iana`, extensions: [`woff`] },
      "font/woff2": { source: `iana`, extensions: [`woff2`] },
      "image/aces": { source: `iana`, extensions: [`exr`] },
      "image/apng": { source: `iana`, compressible: !1, extensions: [`apng`] },
      "image/avci": { source: `iana`, extensions: [`avci`] },
      "image/avcs": { source: `iana`, extensions: [`avcs`] },
      "image/avif": { source: `iana`, compressible: !1, extensions: [`avif`] },
      "image/bmp": {
        source: `iana`,
        compressible: !0,
        extensions: [`bmp`, `dib`],
      },
      "image/cgm": { source: `iana`, extensions: [`cgm`] },
      "image/dicom-rle": { source: `iana`, extensions: [`drle`] },
      "image/dpx": { source: `iana`, extensions: [`dpx`] },
      "image/emf": { source: `iana`, extensions: [`emf`] },
      "image/fits": { source: `iana`, extensions: [`fits`] },
      "image/g3fax": { source: `iana`, extensions: [`g3`] },
      "image/gif": { source: `iana`, compressible: !1, extensions: [`gif`] },
      "image/heic": { source: `iana`, extensions: [`heic`] },
      "image/heic-sequence": { source: `iana`, extensions: [`heics`] },
      "image/heif": { source: `iana`, extensions: [`heif`] },
      "image/heif-sequence": { source: `iana`, extensions: [`heifs`] },
      "image/hej2k": { source: `iana`, extensions: [`hej2`] },
      "image/ief": { source: `iana`, extensions: [`ief`] },
      "image/j2c": { source: `iana` },
      "image/jaii": { source: `iana`, extensions: [`jaii`] },
      "image/jais": { source: `iana`, extensions: [`jais`] },
      "image/jls": { source: `iana`, extensions: [`jls`] },
      "image/jp2": {
        source: `iana`,
        compressible: !1,
        extensions: [`jp2`, `jpg2`],
      },
      "image/jpeg": {
        source: `iana`,
        compressible: !1,
        extensions: [`jpg`, `jpeg`, `jpe`],
      },
      "image/jph": { source: `iana`, extensions: [`jph`] },
      "image/jphc": { source: `iana`, extensions: [`jhc`] },
      "image/jpm": {
        source: `iana`,
        compressible: !1,
        extensions: [`jpm`, `jpgm`],
      },
      "image/jpx": {
        source: `iana`,
        compressible: !1,
        extensions: [`jpx`, `jpf`],
      },
      "image/jxl": { source: `iana`, extensions: [`jxl`] },
      "image/jxr": { source: `iana`, extensions: [`jxr`] },
      "image/jxra": { source: `iana`, extensions: [`jxra`] },
      "image/jxrs": { source: `iana`, extensions: [`jxrs`] },
      "image/jxs": { source: `iana`, extensions: [`jxs`] },
      "image/jxsc": { source: `iana`, extensions: [`jxsc`] },
      "image/jxsi": { source: `iana`, extensions: [`jxsi`] },
      "image/jxss": { source: `iana`, extensions: [`jxss`] },
      "image/ktx": { source: `iana`, extensions: [`ktx`] },
      "image/ktx2": { source: `iana`, extensions: [`ktx2`] },
      "image/naplps": { source: `iana` },
      "image/pjpeg": { compressible: !1, extensions: [`jfif`] },
      "image/png": { source: `iana`, compressible: !1, extensions: [`png`] },
      "image/prs.btif": { source: `iana`, extensions: [`btif`, `btf`] },
      "image/prs.pti": { source: `iana`, extensions: [`pti`] },
      "image/pwg-raster": { source: `iana` },
      "image/sgi": { source: `apache`, extensions: [`sgi`] },
      "image/svg+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`svg`, `svgz`],
      },
      "image/t38": { source: `iana`, extensions: [`t38`] },
      "image/tiff": {
        source: `iana`,
        compressible: !1,
        extensions: [`tif`, `tiff`],
      },
      "image/tiff-fx": { source: `iana`, extensions: [`tfx`] },
      "image/vnd.adobe.photoshop": {
        source: `iana`,
        compressible: !0,
        extensions: [`psd`],
      },
      "image/vnd.airzip.accelerator.azv": {
        source: `iana`,
        extensions: [`azv`],
      },
      "image/vnd.clip": { source: `iana` },
      "image/vnd.cns.inf2": { source: `iana` },
      "image/vnd.dece.graphic": {
        source: `iana`,
        extensions: [`uvi`, `uvvi`, `uvg`, `uvvg`],
      },
      "image/vnd.djvu": { source: `iana`, extensions: [`djvu`, `djv`] },
      "image/vnd.dvb.subtitle": { source: `iana`, extensions: [`sub`] },
      "image/vnd.dwg": { source: `iana`, extensions: [`dwg`] },
      "image/vnd.dxf": { source: `iana`, extensions: [`dxf`] },
      "image/vnd.fastbidsheet": { source: `iana`, extensions: [`fbs`] },
      "image/vnd.fpx": { source: `iana`, extensions: [`fpx`] },
      "image/vnd.fst": { source: `iana`, extensions: [`fst`] },
      "image/vnd.fujixerox.edmics-mmr": { source: `iana`, extensions: [`mmr`] },
      "image/vnd.fujixerox.edmics-rlc": { source: `iana`, extensions: [`rlc`] },
      "image/vnd.globalgraphics.pgb": { source: `iana` },
      "image/vnd.microsoft.icon": {
        source: `iana`,
        compressible: !0,
        extensions: [`ico`],
      },
      "image/vnd.mix": { source: `iana` },
      "image/vnd.mozilla.apng": { source: `iana` },
      "image/vnd.ms-dds": { compressible: !0, extensions: [`dds`] },
      "image/vnd.ms-modi": { source: `iana`, extensions: [`mdi`] },
      "image/vnd.ms-photo": { source: `apache`, extensions: [`wdp`] },
      "image/vnd.net-fpx": { source: `iana`, extensions: [`npx`] },
      "image/vnd.pco.b16": { source: `iana`, extensions: [`b16`] },
      "image/vnd.radiance": { source: `iana` },
      "image/vnd.sealed.png": { source: `iana` },
      "image/vnd.sealedmedia.softseal.gif": { source: `iana` },
      "image/vnd.sealedmedia.softseal.jpg": { source: `iana` },
      "image/vnd.svf": { source: `iana` },
      "image/vnd.tencent.tap": { source: `iana`, extensions: [`tap`] },
      "image/vnd.valve.source.texture": { source: `iana`, extensions: [`vtf`] },
      "image/vnd.wap.wbmp": { source: `iana`, extensions: [`wbmp`] },
      "image/vnd.xiff": { source: `iana`, extensions: [`xif`] },
      "image/vnd.zbrush.pcx": { source: `iana`, extensions: [`pcx`] },
      "image/webp": { source: `iana`, extensions: [`webp`] },
      "image/wmf": { source: `iana`, extensions: [`wmf`] },
      "image/x-3ds": { source: `apache`, extensions: [`3ds`] },
      "image/x-adobe-dng": { extensions: [`dng`] },
      "image/x-cmu-raster": { source: `apache`, extensions: [`ras`] },
      "image/x-cmx": { source: `apache`, extensions: [`cmx`] },
      "image/x-emf": { source: `iana` },
      "image/x-freehand": {
        source: `apache`,
        extensions: [`fh`, `fhc`, `fh4`, `fh5`, `fh7`],
      },
      "image/x-icon": {
        source: `apache`,
        compressible: !0,
        extensions: [`ico`],
      },
      "image/x-jng": { source: `nginx`, extensions: [`jng`] },
      "image/x-mrsid-image": { source: `apache`, extensions: [`sid`] },
      "image/x-ms-bmp": {
        source: `nginx`,
        compressible: !0,
        extensions: [`bmp`],
      },
      "image/x-pcx": { source: `apache`, extensions: [`pcx`] },
      "image/x-pict": { source: `apache`, extensions: [`pic`, `pct`] },
      "image/x-portable-anymap": { source: `apache`, extensions: [`pnm`] },
      "image/x-portable-bitmap": { source: `apache`, extensions: [`pbm`] },
      "image/x-portable-graymap": { source: `apache`, extensions: [`pgm`] },
      "image/x-portable-pixmap": { source: `apache`, extensions: [`ppm`] },
      "image/x-rgb": { source: `apache`, extensions: [`rgb`] },
      "image/x-tga": { source: `apache`, extensions: [`tga`] },
      "image/x-wmf": { source: `iana` },
      "image/x-xbitmap": { source: `apache`, extensions: [`xbm`] },
      "image/x-xcf": { compressible: !1 },
      "image/x-xpixmap": { source: `apache`, extensions: [`xpm`] },
      "image/x-xwindowdump": { source: `apache`, extensions: [`xwd`] },
      "message/bhttp": { source: `iana` },
      "message/cpim": { source: `iana` },
      "message/delivery-status": { source: `iana` },
      "message/disposition-notification": {
        source: `iana`,
        extensions: [`disposition-notification`],
      },
      "message/external-body": { source: `iana` },
      "message/feedback-report": { source: `iana` },
      "message/global": { source: `iana`, extensions: [`u8msg`] },
      "message/global-delivery-status": {
        source: `iana`,
        extensions: [`u8dsn`],
      },
      "message/global-disposition-notification": {
        source: `iana`,
        extensions: [`u8mdn`],
      },
      "message/global-headers": { source: `iana`, extensions: [`u8hdr`] },
      "message/http": { source: `iana`, compressible: !1 },
      "message/imdn+xml": { source: `iana`, compressible: !0 },
      "message/mls": { source: `iana` },
      "message/news": { source: `apache` },
      "message/ohttp-req": { source: `iana` },
      "message/ohttp-res": { source: `iana` },
      "message/partial": { source: `iana`, compressible: !1 },
      "message/rfc822": {
        source: `iana`,
        compressible: !0,
        extensions: [`eml`, `mime`, `mht`, `mhtml`],
      },
      "message/s-http": { source: `apache` },
      "message/sip": { source: `iana` },
      "message/sipfrag": { source: `iana` },
      "message/tracking-status": { source: `iana` },
      "message/vnd.si.simp": { source: `apache` },
      "message/vnd.wfa.wsc": { source: `iana`, extensions: [`wsc`] },
      "model/3mf": { source: `iana`, extensions: [`3mf`] },
      "model/e57": { source: `iana` },
      "model/gltf+json": {
        source: `iana`,
        compressible: !0,
        extensions: [`gltf`],
      },
      "model/gltf-binary": {
        source: `iana`,
        compressible: !0,
        extensions: [`glb`],
      },
      "model/iges": {
        source: `iana`,
        compressible: !1,
        extensions: [`igs`, `iges`],
      },
      "model/jt": { source: `iana`, extensions: [`jt`] },
      "model/mesh": {
        source: `iana`,
        compressible: !1,
        extensions: [`msh`, `mesh`, `silo`],
      },
      "model/mtl": { source: `iana`, extensions: [`mtl`] },
      "model/obj": { source: `iana`, extensions: [`obj`] },
      "model/prc": { source: `iana`, extensions: [`prc`] },
      "model/step": {
        source: `iana`,
        extensions: [`step`, `stp`, `stpnc`, `p21`, `210`],
      },
      "model/step+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`stpx`],
      },
      "model/step+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`stpz`],
      },
      "model/step-xml+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`stpxz`],
      },
      "model/stl": { source: `iana`, extensions: [`stl`] },
      "model/u3d": { source: `iana`, extensions: [`u3d`] },
      "model/vnd.bary": { source: `iana`, extensions: [`bary`] },
      "model/vnd.cld": { source: `iana`, extensions: [`cld`] },
      "model/vnd.collada+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`dae`],
      },
      "model/vnd.dwf": { source: `iana`, extensions: [`dwf`] },
      "model/vnd.flatland.3dml": { source: `iana` },
      "model/vnd.gdl": { source: `iana`, extensions: [`gdl`] },
      "model/vnd.gs-gdl": { source: `apache` },
      "model/vnd.gs.gdl": { source: `iana` },
      "model/vnd.gtw": { source: `iana`, extensions: [`gtw`] },
      "model/vnd.moml+xml": { source: `iana`, compressible: !0 },
      "model/vnd.mts": { source: `iana`, extensions: [`mts`] },
      "model/vnd.opengex": { source: `iana`, extensions: [`ogex`] },
      "model/vnd.parasolid.transmit.binary": {
        source: `iana`,
        extensions: [`x_b`],
      },
      "model/vnd.parasolid.transmit.text": {
        source: `iana`,
        extensions: [`x_t`],
      },
      "model/vnd.pytha.pyox": { source: `iana`, extensions: [`pyo`, `pyox`] },
      "model/vnd.rosette.annotated-data-model": { source: `iana` },
      "model/vnd.sap.vds": { source: `iana`, extensions: [`vds`] },
      "model/vnd.usda": { source: `iana`, extensions: [`usda`] },
      "model/vnd.usdz+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`usdz`],
      },
      "model/vnd.valve.source.compiled-map": {
        source: `iana`,
        extensions: [`bsp`],
      },
      "model/vnd.vtu": { source: `iana`, extensions: [`vtu`] },
      "model/vrml": {
        source: `iana`,
        compressible: !1,
        extensions: [`wrl`, `vrml`],
      },
      "model/x3d+binary": {
        source: `apache`,
        compressible: !1,
        extensions: [`x3db`, `x3dbz`],
      },
      "model/x3d+fastinfoset": { source: `iana`, extensions: [`x3db`] },
      "model/x3d+vrml": {
        source: `apache`,
        compressible: !1,
        extensions: [`x3dv`, `x3dvz`],
      },
      "model/x3d+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`x3d`, `x3dz`],
      },
      "model/x3d-vrml": { source: `iana`, extensions: [`x3dv`] },
      "multipart/alternative": { source: `iana`, compressible: !1 },
      "multipart/appledouble": { source: `iana` },
      "multipart/byteranges": { source: `iana` },
      "multipart/digest": { source: `iana` },
      "multipart/encrypted": { source: `iana`, compressible: !1 },
      "multipart/form-data": { source: `iana`, compressible: !1 },
      "multipart/header-set": { source: `iana` },
      "multipart/mixed": { source: `iana` },
      "multipart/multilingual": { source: `iana` },
      "multipart/parallel": { source: `iana` },
      "multipart/related": { source: `iana`, compressible: !1 },
      "multipart/report": { source: `iana` },
      "multipart/signed": { source: `iana`, compressible: !1 },
      "multipart/vnd.bint.med-plus": { source: `iana` },
      "multipart/voice-message": { source: `iana` },
      "multipart/x-mixed-replace": { source: `iana` },
      "text/1d-interleaved-parityfec": { source: `iana` },
      "text/cache-manifest": {
        source: `iana`,
        compressible: !0,
        extensions: [`appcache`, `manifest`],
      },
      "text/calendar": { source: `iana`, extensions: [`ics`, `ifb`] },
      "text/calender": { compressible: !0 },
      "text/cmd": { compressible: !0 },
      "text/coffeescript": { extensions: [`coffee`, `litcoffee`] },
      "text/cql": { source: `iana` },
      "text/cql-expression": { source: `iana` },
      "text/cql-identifier": { source: `iana` },
      "text/css": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`css`],
      },
      "text/csv": { source: `iana`, compressible: !0, extensions: [`csv`] },
      "text/csv-schema": { source: `iana` },
      "text/directory": { source: `iana` },
      "text/dns": { source: `iana` },
      "text/ecmascript": { source: `apache` },
      "text/encaprtp": { source: `iana` },
      "text/enriched": { source: `iana` },
      "text/fhirpath": { source: `iana` },
      "text/flexfec": { source: `iana` },
      "text/fwdred": { source: `iana` },
      "text/gff3": { source: `iana` },
      "text/grammar-ref-list": { source: `iana` },
      "text/hl7v2": { source: `iana` },
      "text/html": {
        source: `iana`,
        compressible: !0,
        extensions: [`html`, `htm`, `shtml`],
      },
      "text/jade": { extensions: [`jade`] },
      "text/javascript": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`js`, `mjs`],
      },
      "text/jcr-cnd": { source: `iana` },
      "text/jsx": { compressible: !0, extensions: [`jsx`] },
      "text/less": { compressible: !0, extensions: [`less`] },
      "text/markdown": {
        source: `iana`,
        compressible: !0,
        extensions: [`md`, `markdown`],
      },
      "text/mathml": { source: `nginx`, extensions: [`mml`] },
      "text/mdx": { compressible: !0, extensions: [`mdx`] },
      "text/mizar": { source: `iana` },
      "text/n3": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`n3`],
      },
      "text/parameters": { source: `iana`, charset: `UTF-8` },
      "text/parityfec": { source: `iana` },
      "text/plain": {
        source: `iana`,
        compressible: !0,
        extensions: [`txt`, `text`, `conf`, `def`, `list`, `log`, `in`, `ini`],
      },
      "text/provenance-notation": { source: `iana`, charset: `UTF-8` },
      "text/prs.fallenstein.rst": { source: `iana` },
      "text/prs.lines.tag": { source: `iana`, extensions: [`dsc`] },
      "text/prs.prop.logic": { source: `iana` },
      "text/prs.texi": { source: `iana` },
      "text/raptorfec": { source: `iana` },
      "text/red": { source: `iana` },
      "text/rfc822-headers": { source: `iana` },
      "text/richtext": {
        source: `iana`,
        compressible: !0,
        extensions: [`rtx`],
      },
      "text/rtf": { source: `iana`, compressible: !0, extensions: [`rtf`] },
      "text/rtp-enc-aescm128": { source: `iana` },
      "text/rtploopback": { source: `iana` },
      "text/rtx": { source: `iana` },
      "text/sgml": { source: `iana`, extensions: [`sgml`, `sgm`] },
      "text/shaclc": { source: `iana` },
      "text/shex": { source: `iana`, extensions: [`shex`] },
      "text/slim": { extensions: [`slim`, `slm`] },
      "text/spdx": { source: `iana`, extensions: [`spdx`] },
      "text/strings": { source: `iana` },
      "text/stylus": { extensions: [`stylus`, `styl`] },
      "text/t140": { source: `iana` },
      "text/tab-separated-values": {
        source: `iana`,
        compressible: !0,
        extensions: [`tsv`],
      },
      "text/troff": {
        source: `iana`,
        extensions: [`t`, `tr`, `roff`, `man`, `me`, `ms`],
      },
      "text/turtle": { source: `iana`, charset: `UTF-8`, extensions: [`ttl`] },
      "text/ulpfec": { source: `iana` },
      "text/uri-list": {
        source: `iana`,
        compressible: !0,
        extensions: [`uri`, `uris`, `urls`],
      },
      "text/vcard": { source: `iana`, compressible: !0, extensions: [`vcard`] },
      "text/vnd.a": { source: `iana` },
      "text/vnd.abc": { source: `iana` },
      "text/vnd.ascii-art": { source: `iana` },
      "text/vnd.curl": { source: `iana`, extensions: [`curl`] },
      "text/vnd.curl.dcurl": { source: `apache`, extensions: [`dcurl`] },
      "text/vnd.curl.mcurl": { source: `apache`, extensions: [`mcurl`] },
      "text/vnd.curl.scurl": { source: `apache`, extensions: [`scurl`] },
      "text/vnd.debian.copyright": { source: `iana`, charset: `UTF-8` },
      "text/vnd.dmclientscript": { source: `iana` },
      "text/vnd.dvb.subtitle": { source: `iana`, extensions: [`sub`] },
      "text/vnd.esmertec.theme-descriptor": {
        source: `iana`,
        charset: `UTF-8`,
      },
      "text/vnd.exchangeable": { source: `iana` },
      "text/vnd.familysearch.gedcom": { source: `iana`, extensions: [`ged`] },
      "text/vnd.ficlab.flt": { source: `iana` },
      "text/vnd.fly": { source: `iana`, extensions: [`fly`] },
      "text/vnd.fmi.flexstor": { source: `iana`, extensions: [`flx`] },
      "text/vnd.gml": { source: `iana` },
      "text/vnd.graphviz": { source: `iana`, extensions: [`gv`] },
      "text/vnd.hans": { source: `iana` },
      "text/vnd.hgl": { source: `iana` },
      "text/vnd.in3d.3dml": { source: `iana`, extensions: [`3dml`] },
      "text/vnd.in3d.spot": { source: `iana`, extensions: [`spot`] },
      "text/vnd.iptc.newsml": { source: `iana` },
      "text/vnd.iptc.nitf": { source: `iana` },
      "text/vnd.latex-z": { source: `iana` },
      "text/vnd.motorola.reflex": { source: `iana` },
      "text/vnd.ms-mediapackage": { source: `iana` },
      "text/vnd.net2phone.commcenter.command": { source: `iana` },
      "text/vnd.radisys.msml-basic-layout": { source: `iana` },
      "text/vnd.senx.warpscript": { source: `iana` },
      "text/vnd.si.uricatalogue": { source: `apache` },
      "text/vnd.sosi": { source: `iana` },
      "text/vnd.sun.j2me.app-descriptor": {
        source: `iana`,
        charset: `UTF-8`,
        extensions: [`jad`],
      },
      "text/vnd.trolltech.linguist": { source: `iana`, charset: `UTF-8` },
      "text/vnd.vcf": { source: `iana` },
      "text/vnd.wap.si": { source: `iana` },
      "text/vnd.wap.sl": { source: `iana` },
      "text/vnd.wap.wml": { source: `iana`, extensions: [`wml`] },
      "text/vnd.wap.wmlscript": { source: `iana`, extensions: [`wmls`] },
      "text/vnd.zoo.kcl": { source: `iana` },
      "text/vtt": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`vtt`],
      },
      "text/wgsl": { source: `iana`, extensions: [`wgsl`] },
      "text/x-asm": { source: `apache`, extensions: [`s`, `asm`] },
      "text/x-c": {
        source: `apache`,
        extensions: [`c`, `cc`, `cxx`, `cpp`, `h`, `hh`, `dic`],
      },
      "text/x-component": { source: `nginx`, extensions: [`htc`] },
      "text/x-fortran": {
        source: `apache`,
        extensions: [`f`, `for`, `f77`, `f90`],
      },
      "text/x-gwt-rpc": { compressible: !0 },
      "text/x-handlebars-template": { extensions: [`hbs`] },
      "text/x-java-source": { source: `apache`, extensions: [`java`] },
      "text/x-jquery-tmpl": { compressible: !0 },
      "text/x-lua": { extensions: [`lua`] },
      "text/x-markdown": { compressible: !0, extensions: [`mkd`] },
      "text/x-nfo": { source: `apache`, extensions: [`nfo`] },
      "text/x-opml": { source: `apache`, extensions: [`opml`] },
      "text/x-org": { compressible: !0, extensions: [`org`] },
      "text/x-pascal": { source: `apache`, extensions: [`p`, `pas`] },
      "text/x-processing": { compressible: !0, extensions: [`pde`] },
      "text/x-sass": { extensions: [`sass`] },
      "text/x-scss": { extensions: [`scss`] },
      "text/x-setext": { source: `apache`, extensions: [`etx`] },
      "text/x-sfv": { source: `apache`, extensions: [`sfv`] },
      "text/x-suse-ymp": { compressible: !0, extensions: [`ymp`] },
      "text/x-uuencode": { source: `apache`, extensions: [`uu`] },
      "text/x-vcalendar": { source: `apache`, extensions: [`vcs`] },
      "text/x-vcard": { source: `apache`, extensions: [`vcf`] },
      "text/xml": { source: `iana`, compressible: !0, extensions: [`xml`] },
      "text/xml-external-parsed-entity": { source: `iana` },
      "text/yaml": { compressible: !0, extensions: [`yaml`, `yml`] },
      "video/1d-interleaved-parityfec": { source: `iana` },
      "video/3gpp": { source: `iana`, extensions: [`3gp`, `3gpp`] },
      "video/3gpp-tt": { source: `iana` },
      "video/3gpp2": { source: `iana`, extensions: [`3g2`] },
      "video/av1": { source: `iana` },
      "video/bmpeg": { source: `iana` },
      "video/bt656": { source: `iana` },
      "video/celb": { source: `iana` },
      "video/dv": { source: `iana` },
      "video/encaprtp": { source: `iana` },
      "video/evc": { source: `iana` },
      "video/ffv1": { source: `iana` },
      "video/flexfec": { source: `iana` },
      "video/h261": { source: `iana`, extensions: [`h261`] },
      "video/h263": { source: `iana`, extensions: [`h263`] },
      "video/h263-1998": { source: `iana` },
      "video/h263-2000": { source: `iana` },
      "video/h264": { source: `iana`, extensions: [`h264`] },
      "video/h264-rcdo": { source: `iana` },
      "video/h264-svc": { source: `iana` },
      "video/h265": { source: `iana` },
      "video/h266": { source: `iana` },
      "video/iso.segment": { source: `iana`, extensions: [`m4s`] },
      "video/jpeg": { source: `iana`, extensions: [`jpgv`] },
      "video/jpeg2000": { source: `iana` },
      "video/jpm": { source: `apache`, extensions: [`jpm`, `jpgm`] },
      "video/jxsv": { source: `iana` },
      "video/lottie+json": { source: `iana`, compressible: !0 },
      "video/matroska": { source: `iana` },
      "video/matroska-3d": { source: `iana` },
      "video/mj2": { source: `iana`, extensions: [`mj2`, `mjp2`] },
      "video/mp1s": { source: `iana` },
      "video/mp2p": { source: `iana` },
      "video/mp2t": {
        source: `iana`,
        extensions: [`ts`, `m2t`, `m2ts`, `mts`],
      },
      "video/mp4": {
        source: `iana`,
        compressible: !1,
        extensions: [`mp4`, `mp4v`, `mpg4`],
      },
      "video/mp4v-es": { source: `iana` },
      "video/mpeg": {
        source: `iana`,
        compressible: !1,
        extensions: [`mpeg`, `mpg`, `mpe`, `m1v`, `m2v`],
      },
      "video/mpeg4-generic": { source: `iana` },
      "video/mpv": { source: `iana` },
      "video/nv": { source: `iana` },
      "video/ogg": { source: `iana`, compressible: !1, extensions: [`ogv`] },
      "video/parityfec": { source: `iana` },
      "video/pointer": { source: `iana` },
      "video/quicktime": {
        source: `iana`,
        compressible: !1,
        extensions: [`qt`, `mov`],
      },
      "video/raptorfec": { source: `iana` },
      "video/raw": { source: `iana` },
      "video/rtp-enc-aescm128": { source: `iana` },
      "video/rtploopback": { source: `iana` },
      "video/rtx": { source: `iana` },
      "video/scip": { source: `iana` },
      "video/smpte291": { source: `iana` },
      "video/smpte292m": { source: `iana` },
      "video/ulpfec": { source: `iana` },
      "video/vc1": { source: `iana` },
      "video/vc2": { source: `iana` },
      "video/vnd.cctv": { source: `iana` },
      "video/vnd.dece.hd": { source: `iana`, extensions: [`uvh`, `uvvh`] },
      "video/vnd.dece.mobile": { source: `iana`, extensions: [`uvm`, `uvvm`] },
      "video/vnd.dece.mp4": { source: `iana` },
      "video/vnd.dece.pd": { source: `iana`, extensions: [`uvp`, `uvvp`] },
      "video/vnd.dece.sd": { source: `iana`, extensions: [`uvs`, `uvvs`] },
      "video/vnd.dece.video": { source: `iana`, extensions: [`uvv`, `uvvv`] },
      "video/vnd.directv.mpeg": { source: `iana` },
      "video/vnd.directv.mpeg-tts": { source: `iana` },
      "video/vnd.dlna.mpeg-tts": { source: `iana` },
      "video/vnd.dvb.file": { source: `iana`, extensions: [`dvb`] },
      "video/vnd.fvt": { source: `iana`, extensions: [`fvt`] },
      "video/vnd.hns.video": { source: `iana` },
      "video/vnd.iptvforum.1dparityfec-1010": { source: `iana` },
      "video/vnd.iptvforum.1dparityfec-2005": { source: `iana` },
      "video/vnd.iptvforum.2dparityfec-1010": { source: `iana` },
      "video/vnd.iptvforum.2dparityfec-2005": { source: `iana` },
      "video/vnd.iptvforum.ttsavc": { source: `iana` },
      "video/vnd.iptvforum.ttsmpeg2": { source: `iana` },
      "video/vnd.motorola.video": { source: `iana` },
      "video/vnd.motorola.videop": { source: `iana` },
      "video/vnd.mpegurl": { source: `iana`, extensions: [`mxu`, `m4u`] },
      "video/vnd.ms-playready.media.pyv": {
        source: `iana`,
        extensions: [`pyv`],
      },
      "video/vnd.nokia.interleaved-multimedia": { source: `iana` },
      "video/vnd.nokia.mp4vr": { source: `iana` },
      "video/vnd.nokia.videovoip": { source: `iana` },
      "video/vnd.objectvideo": { source: `iana` },
      "video/vnd.planar": { source: `iana` },
      "video/vnd.radgamettools.bink": { source: `iana` },
      "video/vnd.radgamettools.smacker": { source: `apache` },
      "video/vnd.sealed.mpeg1": { source: `iana` },
      "video/vnd.sealed.mpeg4": { source: `iana` },
      "video/vnd.sealed.swf": { source: `iana` },
      "video/vnd.sealedmedia.softseal.mov": { source: `iana` },
      "video/vnd.uvvu.mp4": { source: `iana`, extensions: [`uvu`, `uvvu`] },
      "video/vnd.vivo": { source: `iana`, extensions: [`viv`] },
      "video/vnd.youtube.yt": { source: `iana` },
      "video/vp8": { source: `iana` },
      "video/vp9": { source: `iana` },
      "video/webm": {
        source: `apache`,
        compressible: !1,
        extensions: [`webm`],
      },
      "video/x-f4v": { source: `apache`, extensions: [`f4v`] },
      "video/x-fli": { source: `apache`, extensions: [`fli`] },
      "video/x-flv": {
        source: `apache`,
        compressible: !1,
        extensions: [`flv`],
      },
      "video/x-m4v": { source: `apache`, extensions: [`m4v`] },
      "video/x-matroska": {
        source: `apache`,
        compressible: !1,
        extensions: [`mkv`, `mk3d`, `mks`],
      },
      "video/x-mng": { source: `apache`, extensions: [`mng`] },
      "video/x-ms-asf": { source: `apache`, extensions: [`asf`, `asx`] },
      "video/x-ms-vob": { source: `apache`, extensions: [`vob`] },
      "video/x-ms-wm": { source: `apache`, extensions: [`wm`] },
      "video/x-ms-wmv": {
        source: `apache`,
        compressible: !1,
        extensions: [`wmv`],
      },
      "video/x-ms-wmx": { source: `apache`, extensions: [`wmx`] },
      "video/x-ms-wvx": { source: `apache`, extensions: [`wvx`] },
      "video/x-msvideo": { source: `apache`, extensions: [`avi`] },
      "video/x-sgi-movie": { source: `apache`, extensions: [`movie`] },
      "video/x-smv": { source: `apache`, extensions: [`smv`] },
      "x-conference/x-cooltalk": { source: `apache`, extensions: [`ice`] },
      "x-shader/x-fragment": { compressible: !0 },
      "x-shader/x-vertex": { compressible: !0 },
    };
  }),
  Kte = e.n((t, n) => {
    n.exports = (Gte(), e.a(Wte).default);
  }),
  qte = e.n((e, t) => {
    var n = Kte();
    t.exports = function () {
      var e = {};
      return (
        Object.keys(n).forEach(function (t) {
          var r = n[t];
          r.extensions &&
            r.extensions.length > 0 &&
            r.extensions.forEach(function (n) {
              e[n] = t;
            });
        }),
        e
      );
    };
  }),
  Jte = e.n((e, t) => {
    var n = Object.prototype.toString;
    t.exports = function (e) {
      var t;
      return (
        n.call(e) === `[object Object]` &&
        ((t = Object.getPrototypeOf(e)),
        t === null || t === Object.getPrototypeOf({}))
      );
    };
  }),
  Yte = e.n((e, t) => {
    var n = Jte();
    t.exports = function (e, t) {
      if (!n(e)) throw TypeError(`Expected a plain object`);
      ((t ||= {}), typeof t == `function` && (t = { compare: t }));
      var r = t.deep,
        i = [],
        a = [],
        o = function (e) {
          var s = i.indexOf(e);
          if (s !== -1) return a[s];
          var c = {},
            l = Object.keys(e).sort(t.compare);
          (i.push(e), a.push(c));
          for (var u = 0; u < l.length; u++) {
            var d = l[u],
              f = e[d];
            c[d] = r && n(f) ? o(f) : f;
          }
          return c;
        };
      return o(e);
    };
  }),
  Xte = e.n((e, t) => {
    var n = Yte();
    ((t.exports.desc = function (e) {
      return n(e, function (e, t) {
        return t.length - e.length;
      });
    }),
      (t.exports.asc = function (e) {
        return n(e, function (e, t) {
          return e.length - t.length;
        });
      }));
  }),
  Zte = e.n((e, t) => {
    var n = qte(),
      r = Xte();
    ((t.exports = (e) => {
      let t = r.desc(n()),
        i = Object.keys(t).filter((t) => e.endsWith(t));
      return i.length === 0 ? [] : i.map((e) => ({ ext: e, mime: t[e] }));
    }),
      (t.exports.mime = (e) => {
        let t = r.desc(n()),
          i = Object.keys(t).filter((n) => t[n] === e);
        return i.length === 0 ? [] : i.map((e) => ({ ext: e, mime: t[e] }));
      }));
  }),
  Qte = e.o(Zte(), 1),
  $te = class extends Error {},
  ene = (e, t) => {
    let n = Qte.default.mime(t);
    return n.length === 1 ? `${e}.${n[0].ext}` : e;
  };
function tne(e, t, n = () => {}) {
  let r = new Set(),
    i = 0,
    a = 0,
    o = 0,
    s = () => r.size,
    l = () => i / o;
  t = { showBadge: !0, showProgressBar: !0, ...t };
  let d = (f, p, m) => {
    (r.add(p), (o += p.getTotalBytes()));
    let h = c.BrowserWindow.fromWebContents(m);
    if (!h) throw Error(`Failed to get window from web contents.`);
    if (t.directory && !u.default.isAbsolute(t.directory))
      throw Error("The `directory` option must be an absolute path");
    let _ = t.directory ?? c.app.getPath(`downloads`),
      v;
    if (t.filename) v = u.default.join(_, t.filename);
    else {
      let e = p.getFilename(),
        n = u.default.extname(e) ? e : ene(e, p.getMimeType());
      v = t.overwrite ? u.default.join(_, n) : zte(u.default.join(_, n));
    }
    let y = t.errorMessage ?? `The download of {filename} was interrupted`;
    (t.saveAs
      ? p.setSaveDialogOptions({ defaultPath: v, ...t.dialogOptions })
      : p.setSavePath(v),
      p.on(`updated`, () => {
        i = a;
        for (let e of r) i += e.getReceivedBytes();
        if (
          (t.showBadge &&
            [`darwin`, `linux`].includes(g.default.platform) &&
            (c.app.badgeCount = s()),
          !h.isDestroyed() && t.showProgressBar && h.setProgressBar(l()),
          typeof t.onProgress == `function`)
        ) {
          let e = p.getReceivedBytes(),
            n = p.getTotalBytes();
          t.onProgress({
            percent: n ? e / n : 0,
            transferredBytes: e,
            totalBytes: n,
          });
        }
        typeof t.onTotalProgress == `function` &&
          t.onTotalProgress({
            percent: l(),
            transferredBytes: i,
            totalBytes: o,
          });
      }),
      p.on(`done`, (l, f) => {
        if (
          ((a += p.getTotalBytes()),
          r.delete(p),
          t.showBadge &&
            [`darwin`, `linux`].includes(g.default.platform) &&
            (c.app.badgeCount = s()),
          !h.isDestroyed() &&
            !s() &&
            (h.setProgressBar(-1), (i = 0), (a = 0), (o = 0)),
          t.unregisterWhenDone && e.removeListener(`will-download`, d),
          f === `cancelled`)
        )
          (typeof t.onCancel == `function` && t.onCancel(p), n(new $te()));
        else if (f === `interrupted`) {
          let e = Ute(y, { filename: u.default.basename(v) });
          n(Error(e));
        } else if (f === `completed`) {
          let e = p.getSavePath();
          (g.default.platform === `darwin` && c.app.dock.downloadFinished(e),
            t.openFolderWhenDone && c.shell.showItemInFolder(e),
            typeof t.onCompleted == `function` &&
              t.onCompleted({
                fileName: p.getFilename(),
                filename: p.getFilename(),
                path: e,
                fileSize: p.getReceivedBytes(),
                mimeType: p.getMimeType(),
                url: p.getURL(),
              }),
            n(null, p));
        }
      }),
      typeof t.onStarted == `function` && t.onStarted(p));
  };
  e.on(`will-download`, d);
}
async function S9(e, t, n) {
  return new Promise((r, i) => {
    ((n = { ...n, unregisterWhenDone: !0 }),
      tne(e.webContents.session, n, (e, t) => {
        e ? i(e) : r(t);
      }),
      e.webContents.downloadURL(t));
  });
}
if (typeof c.default == `string`)
  throw TypeError(`Not running in an Electron environment!`);
var { env: C9 } = process,
  nne = `ELECTRON_IS_DEV` in C9,
  rne = Number.parseInt(C9.ELECTRON_IS_DEV, 10) === 1,
  ine = nne ? rne : !c.default.app.isPackaged,
  w9 = (e) => e.webContents ?? (e.id && e),
  T9 =
    (e) =>
    (t = {}) => (t.transform && !t.click && (e.transform = t.transform), e),
  ane = (e) => {
    let t;
    return e
      .filter(
        (e) => e !== void 0 && e !== !1 && e.visible !== !1 && e.visible !== ``,
      )
      .filter((e, n, r) => {
        let i =
          e.type === `separator` &&
          (!t || n === r.length - 1 || r[n + 1].type === `separator`);
        return ((t = i ? t : e), !i);
      });
  },
  one = (e, t) => {
    let n = (n, r) => {
      if (
        typeof t.shouldShowMenu == `function` &&
        t.shouldShowMenu(n, r) === !1
      )
        return;
      let { editFlags: i } = r,
        a = r.selectionText.length > 0,
        o = !!r.linkURL,
        s = (e) => i[`can${e}`] && a,
        l = {
          separator: () => ({ type: `separator` }),
          learnSpelling: T9({
            id: `learnSpelling`,
            label: `&Learn Spelling`,
            visible: !!(r.isEditable && a && r.misspelledWord),
            click() {
              w9(e).session.addWordToSpellCheckerDictionary(r.misspelledWord);
            },
          }),
          lookUpSelection: T9({
            id: `lookUpSelection`,
            label: `Look Up “{selection}”`,
            visible: g.default.platform === `darwin` && a && !o,
            click() {
              g.default.platform === `darwin` &&
                w9(e).showDefinitionForSelection();
            },
          }),
          searchWithGoogle: T9({
            id: `searchWithGoogle`,
            label: `&Search with Google`,
            visible: a,
            click() {
              let e = new URL(`https://www.google.com/search`);
              (e.searchParams.set(`q`, r.selectionText),
                c.default.shell.openExternal(e.toString()));
            },
          }),
          cut: T9({
            id: `cut`,
            label: `Cu&t`,
            enabled: s(`Cut`),
            visible: r.isEditable,
            click(t) {
              let n = w9(e);
              !t.transform && n
                ? n.cut()
                : ((r.selectionText = t.transform
                    ? t.transform(r.selectionText)
                    : r.selectionText),
                  c.default.clipboard.writeText(r.selectionText));
            },
          }),
          copy: T9({
            id: `copy`,
            label: `&Copy`,
            enabled: s(`Copy`),
            visible: r.isEditable || a,
            click(t) {
              let n = w9(e);
              !t.transform && n
                ? n.copy()
                : ((r.selectionText = t.transform
                    ? t.transform(r.selectionText)
                    : r.selectionText),
                  c.default.clipboard.writeText(r.selectionText));
            },
          }),
          paste: T9({
            id: `paste`,
            label: `&Paste`,
            enabled: i.canPaste,
            visible: r.isEditable,
            click(t) {
              let n = w9(e);
              if (t.transform) {
                let e = c.default.clipboard.readText(r.selectionText);
                ((e = t.transform ? t.transform(e) : e), n.insertText(e));
              } else n.paste();
            },
          }),
          selectAll: T9({
            id: `selectAll`,
            label: `Select &All`,
            click() {
              w9(e).selectAll();
            },
          }),
          saveImage: T9({
            id: `saveImage`,
            label: `Save I&mage`,
            visible: r.mediaType === `image`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                S9(e, r.srcURL));
            },
          }),
          saveImageAs: T9({
            id: `saveImageAs`,
            label: `Sa&ve Image As…`,
            visible: r.mediaType === `image`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                S9(e, r.srcURL, { saveAs: !0 }));
            },
          }),
          saveVideo: T9({
            id: `saveVideo`,
            label: `Save Vide&o`,
            visible: r.mediaType === `video`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                S9(e, r.srcURL));
            },
          }),
          saveVideoAs: T9({
            id: `saveVideoAs`,
            label: `Save Video& As…`,
            visible: r.mediaType === `video`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                S9(e, r.srcURL, { saveAs: !0 }));
            },
          }),
          copyLink: T9({
            id: `copyLink`,
            label: `Copy Lin&k`,
            visible: r.linkURL.length > 0 && r.mediaType === `none`,
            click(e) {
              ((r.linkURL = e.transform ? e.transform(r.linkURL) : r.linkURL),
                c.default.clipboard.write({
                  bookmark: r.linkText,
                  text: r.linkURL,
                }));
            },
          }),
          saveLinkAs: T9({
            id: `saveLinkAs`,
            label: `Save Link As…`,
            visible: r.linkURL.length > 0 && r.mediaType === `none`,
            click(t) {
              ((r.linkURL = t.transform ? t.transform(r.linkURL) : r.linkURL),
                S9(e, r.linkURL, { saveAs: !0 }));
            },
          }),
          copyImage: T9({
            id: `copyImage`,
            label: `Cop&y Image`,
            visible: r.mediaType === `image`,
            click() {
              w9(e).copyImageAt(r.x, r.y);
            },
          }),
          copyImageAddress: T9({
            id: `copyImageAddress`,
            label: `C&opy Image Address`,
            visible: r.mediaType === `image`,
            click(e) {
              ((r.srcURL = e.transform ? e.transform(r.srcURL) : r.srcURL),
                c.default.clipboard.write({
                  bookmark: r.srcURL,
                  text: r.srcURL,
                }));
            },
          }),
          copyVideoAddress: T9({
            id: `copyVideoAddress`,
            label: `Copy Video Ad&dress`,
            visible: r.mediaType === `video`,
            click(e) {
              ((r.srcURL = e.transform ? e.transform(r.srcURL) : r.srcURL),
                c.default.clipboard.write({
                  bookmark: r.srcURL,
                  text: r.srcURL,
                }));
            },
          }),
          inspect: () => ({
            id: `inspect`,
            label: `I&nspect Element`,
            click() {
              (w9(e).inspectElement(r.x, r.y),
                w9(e).isDevToolsOpened() && w9(e).devToolsWebContents.focus());
            },
          }),
          services: () => ({
            id: `services`,
            label: `Services`,
            role: `services`,
            visible: g.default.platform === `darwin` && (r.isEditable || a),
          }),
        },
        u =
          typeof t.showInspectElement == `boolean` ? t.showInspectElement : ine,
        d =
          t.showSelectAll ||
          (t.showSelectAll !== !1 && g.default.platform !== `darwin`);
      function f(t) {
        return {
          id: `dictionarySuggestions`,
          label: t,
          visible: !!(r.isEditable && a && r.misspelledWord),
          click(t) {
            w9(e).replaceMisspelling(t.label);
          },
        };
      }
      let p = [];
      a && r.misspelledWord && r.dictionarySuggestions.length > 0
        ? (p = r.dictionarySuggestions.map((e) => f(e)))
        : p.push({
            id: `dictionarySuggestions`,
            label: `No Guesses Found`,
            visible: !!(a && r.misspelledWord),
            enabled: !1,
          });
      let m = [
        p.length > 0 && l.separator(),
        ...p,
        l.separator(),
        t.showLearnSpelling !== !1 && l.learnSpelling(),
        l.separator(),
        t.showLookUpSelection !== !1 && l.lookUpSelection(),
        l.separator(),
        t.showSearchWithGoogle !== !1 && l.searchWithGoogle(),
        l.separator(),
        l.cut(),
        l.copy(),
        l.paste(),
        d && l.selectAll(),
        l.separator(),
        t.showSaveImage && l.saveImage(),
        t.showSaveImageAs && l.saveImageAs(),
        t.showCopyImage !== !1 && l.copyImage(),
        t.showCopyImageAddress && l.copyImageAddress(),
        t.showSaveVideo && l.saveVideo(),
        t.showSaveVideoAs && l.saveVideoAs(),
        t.showCopyVideoAddress && l.copyVideoAddress(),
        l.separator(),
        t.showCopyLink !== !1 && l.copyLink(),
        t.showSaveLinkAs && l.saveLinkAs(),
        l.separator(),
        u && l.inspect(),
        t.showServices && l.services(),
        l.separator(),
      ];
      if ((t.menu && (m = t.menu(l, r, e, p, n)), t.prepend)) {
        let i = t.prepend(l, r, e, n);
        Array.isArray(i) && m.unshift(...i);
      }
      if (t.append) {
        let i = t.append(l, r, e, n);
        Array.isArray(i) && m.push(...i);
      }
      m = ane(m);
      for (let e of m)
        if (
          (t.labels && t.labels[e.id] && (e.label = t.labels[e.id]),
          typeof e.label == `string` && e.label.includes(`{selection}`))
        ) {
          let t =
            typeof r.selectionText == `string` ? r.selectionText.trim() : ``;
          e.label = e.label.replace(
            `{selection}`,
            Fte(t, 25).replaceAll(`&`, `&&`),
          );
        }
      if (m.length > 0) {
        let n = c.default.Menu.buildFromTemplate(m);
        (typeof t.onShow == `function` && n.on(`menu-will-show`, t.onShow),
          typeof t.onClose == `function` && n.on(`menu-will-close`, t.onClose),
          n.popup(e));
      }
    };
    return (
      w9(e).on(`context-menu`, n),
      () => {
        e?.isDestroyed?.() || w9(e).removeListener(`context-menu`, n);
      }
    );
  };
function sne(e = {}) {
  if (g.default.type === `renderer`)
    throw Error(`Cannot use electron-context-menu in the renderer process!`);
  let t = !1,
    n = [],
    r = (r) => {
      if (t) return;
      let i = one(r, e),
        a = () => {
          i();
        };
      (w9(r).once(`destroyed`, a), n.push(a));
    },
    i = () => {
      for (let e of n) e();
      ((n.length = 0), (t = !0));
    };
  if (e.window) {
    let t = e.window;
    if (w9(t) === void 0) {
      let e = () => {
        r(t);
      };
      return (
        (t.addEventListener ?? t.addListener)(`dom-ready`, e, { once: !0 }),
        n.push(() => {
          t.removeEventListener(`dom-ready`, e, { once: !0 });
        }),
        i
      );
    }
    return (r(t), i);
  }
  for (let e of c.default.BrowserWindow.getAllWindows()) r(e);
  let a = (e, t) => {
    r(t);
  };
  return (
    c.default.app.on(`browser-window-created`, a),
    n.push(() => {
      c.default.app.removeListener(`browser-window-created`, a);
    }),
    i
  );
}
var cne = class {
  windowsByOwnerWebContentsId = new Map();
  originConversationIdsByOwnerWebContentsId = new Map();
  constructor(e) {
    this.options = e;
  }
  forgetWebContents(e) {
    this.originConversationIdsByOwnerWebContentsId.delete(e);
    let t = this.windowsByOwnerWebContentsId.get(e);
    (this.windowsByOwnerWebContentsId.delete(e),
      t != null && (t.isDestroyed() || t.close()));
  }
  getOwnerWebContentsId(e) {
    for (let [t, n] of this.windowsByOwnerWebContentsId)
      if (!n.isDestroyed() && n.webContents.id === e) return t;
    return null;
  }
  setOriginConversation(e, t) {
    (this.originConversationIdsByOwnerWebContentsId.set(e.id, t),
      this.sendOriginConversation(e.id));
  }
  async show(e) {
    let t = this.windowsByOwnerWebContentsId.get(e.id);
    if (t != null && !t.isDestroyed()) {
      (this.sendOriginConversation(e.id),
        this.options.showWindow(t, { stealFocus: !0 }));
      return;
    }
    let r = await this.options.createWindow({
      title: `Debug`,
      width: 920,
      height: 840,
      appearance: `secondary`,
      initialRoute: n.da,
      lockTitle: !0,
    });
    (this.windowsByOwnerWebContentsId.set(e.id, r),
      r.setMenuBarVisibility(!1),
      r.center());
    let i = () => {
        this.windowsByOwnerWebContentsId.get(e.id) === r &&
          this.windowsByOwnerWebContentsId.delete(e.id);
      },
      a = () => {
        (i(),
          this.originConversationIdsByOwnerWebContentsId.delete(e.id),
          r.isDestroyed() || r.close());
      };
    (r.on(`closed`, () => {
      (i(), e.removeListener(`destroyed`, a));
    }),
      e.once(`destroyed`, a),
      this.sendOriginConversation(e.id),
      this.options.showWindow(r, { stealFocus: !0 }));
  }
  sendOriginConversation(e) {
    let t = this.windowsByOwnerWebContentsId.get(e);
    t == null ||
      t.isDestroyed() ||
      this.options.sendMessageToWindow(t, {
        type: `debug-window-origin-conversation-changed`,
        conversationId:
          this.originConversationIdsByOwnerWebContentsId.get(e) ?? null,
      });
  }
};
function E9(e, t) {
  if (!Zz(e)) return !1;
  try {
    return new URL(e).origin === t;
  } catch {
    return !1;
  }
}
function lne(e, t, n) {
  return n && une(e, t, n) ? !0 : dne(e, t);
}
function une(e, t, n) {
  let r = D9(e),
    i = D9(t);
  return r == null ||
    i == null ||
    !Zz(e) ||
    !Zz(t) ||
    r.origin !== n ||
    i.origin !== n
    ? !1
    : r.pathname === i.pathname;
}
function dne(e, t) {
  let n = D9(e),
    r = D9(t);
  return n == null ||
    r == null ||
    n.protocol !== `file:` ||
    r.protocol !== `file:`
    ? !1
    : n.pathname === r.pathname;
}
function D9(e) {
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
var O9 = r.a(`window-manager`),
  fne = `http://localhost:5175/`,
  pne =
    process.platform === `darwin`
      ? `Reveal in Finder`
      : process.platform === `win32`
        ? `Open in Explorer`
        : `Open in File Manager`;
function mne() {
  return `
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>${c.app.getName()}</title>
  </head>
  <body>
    <main>
      <h1>Something went wrong…</h1>
    </main>
  </body>
</html>
`.trim();
}
var k9 = `#00000000`,
  hne = `#000000`,
  gne = `#f9f9f9`,
  _ne = 3840,
  vne = 2160,
  yne = 320,
  bne = 140,
  xne = 400,
  Sne = 400,
  Cne = 46,
  wne = 14,
  Tne = 16,
  Ene = 36,
  Dne = `#1f1f1f`,
  One = `#ffffff`,
  kne = 5e3,
  Ane = 1e3;
function A9(e) {
  return { x: Tne, y: Math.round((Cne * e - wne) / 2) };
}
function j9(e = 1) {
  return {
    color: k9,
    symbolColor: c.nativeTheme.shouldUseDarkColors ? One : Dne,
    height: Math.round(Ene * e),
  };
}
var jne = class {
  browserCommentPopupWindowCreatedHandlers = new Set();
  readyWebContentsIds = new Set();
  pendingMessages = new Map();
  primaryWindows = new Set();
  primaryWindowLastActiveAtMs = new WeakMap();
  lastActivePrimaryWindow = null;
  windowAppearances = new Map();
  registeredWindowIds = new Set();
  registeredWebContentsIds = new Set();
  appServiceWindowIds = new Set();
  webContentsHostIds = new Map();
  opaqueWindowSurfaceModes = new Map();
  windowZooms = new Map();
  appShellShortcutStates = new Map();
  historySwipeNavigationStates = new WeakMap();
  appShellCommandKeymapState = n.qt();
  appShellCloseTabAccelerators = n.Qt({
    commandId: `closeTab`,
    keymapState: this.appShellCommandKeymapState,
    isMacOS: process.platform === `darwin`,
  });
  debugWindowManager;
  isAppQuitting = !1;
  clearPendingActiveSpaceRefocus = null;
  rendererRecoveryAttempts = new Set();
  constructor(e) {
    ((this.options = e),
      P3(),
      (this.debugWindowManager = this.options.allowDebugMenu
        ? new cne({
            createWindow: (e) => this.createWindow(e),
            sendMessageToWindow: (e, t) => this.sendMessageToWindow(e, t),
            showWindow: (e, t) => this.showWindow(e, t),
          })
        : null));
  }
  setOnMainProcessInspectorStateChanged(e) {
    this.options.onMainProcessInspectorStateChanged = e;
  }
  setQueueCodexDeepLinkUrl(e) {
    this.options.queueCodexDeepLinkUrl = e;
  }
  refreshAppShellCommandAccelerators() {
    ((this.appShellCommandKeymapState = n.qt()),
      (this.appShellCloseTabAccelerators = n.Qt({
        commandId: `closeTab`,
        keymapState: this.appShellCommandKeymapState,
        isMacOS: process.platform === `darwin`,
      })));
  }
  queueCodexDeepLinkUrl(e, t) {
    return this.options.queueCodexDeepLinkUrl?.(e, t) === !0;
  }
  async openLinkInPreferredTarget(e, t, n) {
    return IA({
      request: t,
      targetWebContents: e,
      settingsStore: this.options.settingsStore,
      queueCodexDeepLinkUrl: this.queueCodexDeepLinkUrl.bind(this),
      errorKind: n,
      errorReporter: this.options.errorReporter,
    });
  }
  markWebContentsReady(e) {
    (this.readyWebContentsIds.add(e.id), this.flushPendingMessages(e));
  }
  forgetWebContents(e) {
    (this.readyWebContentsIds.delete(e),
      this.pendingMessages.delete(e),
      this.appShellShortcutStates.delete(e),
      this.debugWindowManager?.forgetWebContents(e));
  }
  isWebContentsReady(e) {
    return this.readyWebContentsIds.has(e);
  }
  setAppShellShortcutState(e, t) {
    this.appShellShortcutStates.set(e, t);
  }
  setWindowZoom(e, t) {
    let n = c.BrowserWindow.fromWebContents(e),
      r = n && this.windowAppearances.get(n.id);
    n == null ||
      (r !== `primary` && r !== `quickChat`) ||
      (process.platform === `darwin`
        ? n.setWindowButtonPosition(A9(t))
        : (process.platform === `win32` || process.platform === `linux`) &&
          (this.windowZooms.set(n.id, t), n.setTitleBarOverlay(j9(t))));
  }
  setHistorySwipeNavigationState(e, t) {
    let n = c.BrowserWindow.fromWebContents(e);
    n == null ||
      this.windowAppearances.get(n.id) !== `primary` ||
      (this.historySwipeNavigationStates.set(n, t), dP(n, t));
  }
  getAppShellShortcutState(e) {
    return this.appShellShortcutStates.get(e) ?? null;
  }
  markAppQuitting() {
    this.isAppQuitting = !0;
    let e = this.getPrimaryWindow();
    e && this.persistPrimaryWindowBounds(e);
  }
  showPrimaryWindow(e) {
    let t = this.getPrimaryWindow();
    return this.showWindow(t, e);
  }
  showLastActivePrimaryWindow() {
    let e = this.lastActivePrimaryWindow;
    return e != null && !e.isDestroyed()
      ? this.showWindow(e)
      : ((this.lastActivePrimaryWindow = null),
        this.showWindow(this.getPrimaryWindow()));
  }
  isHotkeyWindowThread(e) {
    return this.windowAppearances.get(e.id) === `hotkeyWindowThread`;
  }
  showWindow(e, t) {
    if (!e) return !1;
    e.isMinimized() && e.restore();
    let n = t?.stealFocus === !0 && process.platform === `win32`;
    return (
      n && e.setAlwaysOnTop(!0),
      t?.focusComposer === !0 &&
        t.stealFocus === !0 &&
        process.platform === `darwin` &&
        (!e.isFocused() || e.isFullScreen()) &&
        this.refocusAfterActiveSpaceChange(e),
      e.show(),
      t?.stealFocus === !0 &&
        process.platform === `darwin` &&
        c.app.focus({ steal: !0 }),
      e.focus(),
      t?.focusComposer === !0 &&
        this.sendMessageToWindow(e, { type: `focus-composer` }),
      n && e.setAlwaysOnTop(!1),
      !0
    );
  }
  refocusAfterActiveSpaceChange(e) {
    this.clearPendingActiveSpaceRefocus?.();
    let t = null,
      n = null,
      r = !0,
      i = () => {
        r &&
          ((r = !1),
          n != null && clearTimeout(n),
          t?.(),
          (t = null),
          e.off(`closed`, i),
          this.clearPendingActiveSpaceRefocus === i &&
            (this.clearPendingActiveSpaceRefocus = null));
      },
      a = () => {
        if (!r || (i(), e.isDestroyed())) return;
        let t = e.webContents;
        t.isDestroyed() ||
          (c.app.focus({ steal: !0 }),
          e.focus(),
          t.focus(),
          this.sendMessageToWindow(e, { type: `focus-composer` }));
      };
    ((t = cP(`NSWorkspaceActiveSpaceDidChangeNotification`, a)),
      (n = t == null ? setTimeout(a, Ane) : setTimeout(i, kne)),
      e.on(`closed`, i),
      (this.clearPendingActiveSpaceRefocus = i));
  }
  sendMessageToWindow(e, t) {
    this.sendMessageToWebContents(e.webContents, t);
  }
  sendMessageToAllRegisteredWindows(e) {
    this.sendMessageToAllWindows(e);
  }
  sendMessageToAllWindows(e) {
    for (let t of c.BrowserWindow.getAllWindows())
      t.isDestroyed() ||
        (this.appServiceWindowIds.has(t.id) && this.sendMessageToWindow(t, e));
  }
  sendMessageToWebContents(e, t) {
    if (e.isDestroyed()) return;
    if (this.isWebContentsReady(e.id)) {
      e.send(B, t);
      return;
    }
    let n = this.pendingMessages.get(e.id) ?? [];
    (n.push(t), this.pendingMessages.set(e.id, n));
  }
  sendMessageToWebContentsId(e, t) {
    for (let n of c.BrowserWindow.getAllWindows())
      if (!(n.isDestroyed() || n.webContents.id !== e)) {
        this.sendMessageToWebContents(n.webContents, t);
        return;
      }
  }
  getPrimaryWindow() {
    let e = this.lastActivePrimaryWindow;
    if (e != null && !e.isDestroyed() && this.primaryWindows.has(e)) return e;
    let t = this.getPrimaryWindows(),
      n = t.at(-1) ?? null,
      r = -1;
    for (let e of t) {
      let t = this.primaryWindowLastActiveAtMs.get(e) ?? -1;
      t > r && ((n = e), (r = t));
    }
    return n
      ? ((this.lastActivePrimaryWindow = n), n)
      : ((this.lastActivePrimaryWindow = null), null);
  }
  getPrimaryWindows() {
    return this.getTrackedPrimaryWindows();
  }
  wasPrimaryWindowFocusedWithin(e, t) {
    if (!this.primaryWindows.has(e) || e.isDestroyed()) return !1;
    if (e.isFocused()) return !0;
    let n = this.primaryWindowLastActiveAtMs.get(e);
    return n != null && Date.now() - n <= t;
  }
  refreshWindowBackdrops() {
    for (let e of c.BrowserWindow.getAllWindows()) {
      if (e.isDestroyed() || !this.registeredWindowIds.has(e.id)) continue;
      let t = this.windowAppearances.get(e.id);
      t != null && this.applyWindowBackdrop(e, t, !0);
    }
  }
  hasRegisteredWebContents(e) {
    return this.registeredWebContentsIds.has(e.id);
  }
  isAppServiceWindow(e) {
    return this.appServiceWindowIds.has(e.id);
  }
  registerAuxiliaryWindow(e, t, n) {
    this.registerWindow(e, V, !1, t, n);
  }
  getHostIdForWebContents(e) {
    return this.webContentsHostIds.get(e.id) ?? null;
  }
  getRendererWindowLogFields(e) {
    let t = c.BrowserWindow.fromWebContents(e),
      n = t != null && !t.isDestroyed();
    return {
      rendererWebContentsId: e.id,
      rendererWindowId: n ? t.id : null,
      rendererWindowAppearance: n
        ? (this.windowAppearances.get(t.id) ?? null)
        : null,
      rendererWindowVisible: n ? t.isVisible() : null,
      rendererWindowFocused: n ? t.isFocused() : null,
    };
  }
  isTrustedIpcSender(e, t) {
    return e.id != null && !this.registeredWebContentsIds.has(e.id)
      ? !1
      : Nne({ sender: e, senderFrame: t, moduleDir: this.options.moduleDir });
  }
  isMainProcessInspectorEnabled() {
    return O.url() != null;
  }
  async toggleMainProcessInspector() {
    throw Error(`Feature unavailable.`);
  }
  openMcpAppSandboxDevtools(e, t) {
    F3(e, t, this.options.allowMcpAppSandboxDevtools === !0);
  }
  async createWindow(e = {}) {
    let {
        title: t,
        width: r = 1280,
        height: i = 820,
        appearance: o = `primary`,
        show: s = !0,
        initialRoute: l,
        hostId: d = V,
        parent: f,
        focusable: m,
        lockTitle: h = !1,
      } = e,
      g = Date.now(),
      _ = o === `primary` ? V : d,
      v = o === `primary`,
      y = v ? this.restorePrimaryWindowBounds() : null,
      b = y?.width ?? r,
      x = y?.height ?? i,
      S = y?.x,
      C = y?.y,
      w = y?.isMaximized === !0,
      T = Bne(o, this.getPrimaryMinimumSize()),
      E = this.shouldAlwaysUseOpaqueWindowSurface(o),
      D = z9({
        appearance: o,
        opaqueWindowSurfaceEnabled: E,
        platform: process.platform,
      }),
      O = zne(o),
      k = {
        preload: this.options.preloadPath,
        contextIsolation: !0,
        nodeIntegration: !1,
        spellcheck: !0,
        devTools: this.options.allowDevtools,
        ...(O ? { webviewTag: !0 } : {}),
      },
      { backgroundColor: A, backgroundMaterial: j } = L9({
        platform: process.platform,
        appearance: o,
        opaqueWindowSurfaceEnabled: E,
        prefersDarkColors: c.nativeTheme.shouldUseDarkColors,
      }),
      M = new c.BrowserWindow({
        width: b,
        height: x,
        ...(S === void 0 || C === void 0 ? {} : { x: S, y: C }),
        title: t ?? c.app.getName(),
        backgroundColor: A,
        show: s,
        parent: f,
        ...(m === void 0 ? {} : { focusable: m }),
        ...(process.platform === `win32` || process.platform === `linux`
          ? { autoHideMenuBar: !0 }
          : {}),
        backgroundMaterial: j ?? void 0,
        ...D,
        minWidth: T?.width,
        minHeight: T?.height,
        webPreferences: k,
      });
    this.applyWindowBackdrop(M, o, !0);
    let N = M.webContents,
      P = N.id;
    (O &&
      (z3(N, this.options.allowMcpAppSandboxDevtools === !0, {
        allowNonMcpAppSandboxWebviews: o === `primary`,
      }),
      o === `primary` &&
        sz({
          getAuthToken: this.options.getChatGptWebviewAuthToken,
          onBusinessCheckoutComplete: (e) => {
            N.isDestroyed() ||
              N.send(B, { type: `business-checkout-complete`, workspaceId: e });
          },
          onPersonalCheckoutComplete: (e) => {
            N.isDestroyed() ||
              N.send(B, { type: `personal-checkout-complete`, planType: e });
          },
          onCreditPurchaseCheckoutComplete: () => {
            N.isDestroyed() ||
              N.send(B, { type: `credit-purchase-checkout-complete` });
          },
          onReturnToCodex: () => {
            N.isDestroyed() || N.send(B, { type: `navigate-back` });
          },
          owner: N,
        })),
      M.webContents.once(`did-finish-load`, () => {
        (M.webContents.setZoomLevel(0),
          O9().info(`window main frame finished load`, {
            safe: {
              hostId: _,
              windowId: M.id,
              webContentsId: M.webContents.id,
              appearance: o,
              startupElapsedMs: Date.now() - g,
            },
          }));
      }),
      M.once(`ready-to-show`, () => {
        O9().info(`window ready-to-show`, {
          safe: {
            hostId: _,
            windowId: M.id,
            webContentsId: M.webContents.id,
            appearance: o,
            startupElapsedMs: Date.now() - g,
          },
        });
      }));
    let F = this.installApplicationMenuTitleBarOverlaySync(M, o),
      I = () => {
        this.applyWindowBackdrop(M, o, !1);
      },
      ee = () => {
        this.sendMessageToWindow(M, {
          type: `electron-window-position-changed`,
        });
      };
    (M.on(`move`, ee),
      process.platform === `darwin` &&
        (M.on(`move`, I),
        M.on(`resize`, I),
        o === `primary` &&
          M.on(`swipe`, (e, t) => {
            let n = this.historySwipeNavigationStates.get(M);
            t === `left` && n?.canGoBack === !0
              ? this.sendMessageToWindow(M, { type: `navigate-back` })
              : t === `right` &&
                n?.canGoForward === !0 &&
                this.sendMessageToWindow(M, { type: `navigate-forward` });
          })),
      (process.platform === `win32` || process.platform === `linux`) &&
        M.removeMenu(),
      M.on(`closed`, () => {
        (F?.(),
          M.removeListener(`move`, ee),
          process.platform === `darwin` &&
            (M.removeListener(`move`, I), M.removeListener(`resize`, I)));
      }),
      (h ||
        o === `primary` ||
        o === `hud` ||
        o === `hotkeyWindowHome` ||
        o === `hotkeyWindowThread`) &&
        M.on(`page-title-updated`, (e) => {
          (e.preventDefault(), M.isDestroyed() || M.setTitle(M.getTitle()));
        }),
      this.installWebContentsDiagnostics(M),
      this.registerWindow(M, _, v, o, `register`),
      v &&
        M.on(`close`, (e) => {
          this.persistPrimaryWindowBounds(M);
          let t = this.getPrimaryWindows().some((e) => e !== M);
          if (
            process.platform === `win32` &&
            !this.isAppQuitting &&
            this.options.canHideLastWindowToTray?.() === !0 &&
            !t
          ) {
            (e.preventDefault(), M.hide());
            return;
          }
          if (process.platform === `darwin` && !this.isAppQuitting && !t) {
            if (M.isFullScreen()) {
              (e.preventDefault(),
                M.once(`leave-full-screen`, () => {
                  M.isDestroyed() || M.hide();
                }),
                M.setFullScreen(!1));
              return;
            }
            (e.preventDefault(), M.hide());
          }
        }));
    let L = () => {
      this.sendMessageToWindow(M, {
        type: `window-fullscreen-changed`,
        isFullScreen: M.isFullScreen(),
      });
    };
    (M.on(`enter-full-screen`, L), M.on(`leave-full-screen`, L), L());
    let R = () => {
        (this.applyWindowBackdrop(M, o, !1),
          this.sendMessageToWindow(M, {
            type: `electron-window-focus-changed`,
            isFocused: M.isFocused(),
          }));
      },
      z = XI({
        allowDebugMenu: this.options.allowDebugMenu,
        getCloseTabAccelerators: () => this.appShellCloseTabAccelerators,
        getCommandKeymapState: () => this.appShellCommandKeymapState,
        getState: () => this.appShellShortcutStates.get(P) ?? null,
        sendAction: (e) => {
          this.sendMessageToWebContents(N, e);
        },
      });
    (M.on(`focus`, R),
      M.on(`blur`, R),
      M.on(`show`, R),
      M.on(`hide`, R),
      N.on(`before-input-event`, z),
      R(),
      w &&
        M.once(`ready-to-show`, () => {
          M.isDestroyed() || M.maximize();
        }));
    let te = N9();
    if (
      (M.webContents.setWindowOpenHandler(
        ({ disposition: e, url: t, frameName: r }) => {
          if (
            n.gc(r)?.windowId === n.hc.BROWSER_COMMENT_POPUP &&
            this.browserCommentPopupWindowCreatedHandlers.size > 0 &&
            t === `about:blank` &&
            o === `primary` &&
            P9(M.webContents.getURL(), this.options.moduleDir)
          )
            return {
              action: `allow`,
              outlivesOpener: !1,
              overrideBrowserWindowOptions:
                this.createBrowserCommentPopupWindowOptions(M),
            };
          if (te && E9(t, te)) return { action: `allow` };
          let i = e === `foreground-tab` || e === `background-tab`;
          return (
            Zz(t) &&
              this.openLinkInPreferredTarget(
                M.webContents,
                {
                  url: t,
                  ...(i
                    ? { disposition: `new-tab`, openTargetIntent: `alternate` }
                    : {}),
                  source: `manual`,
                  initiator: `window_open`,
                },
                `open-external-window`,
              ),
            { action: `deny` }
          );
        },
      ),
      M.webContents.on(`did-create-window`, (e, t) => {
        let r = n.gc(t.frameName);
        if (
          !(
            r?.windowId !== n.hc.BROWSER_COMMENT_POPUP ||
            t.url !== `about:blank` ||
            o !== `primary`
          )
        ) {
          (e.setMenuBarVisibility(!1),
            e.setVisibleOnAllWorkspaces(!1),
            process.platform === `win32` && e.removeMenu(),
            e.hide(),
            this.installNativeContextMenu(e));
          for (let t of this.browserCommentPopupWindowCreatedHandlers)
            t({
              browserTabId: r.browserTabId,
              conversationId: r.conversationId,
              hostId: this.getHostIdForWebContents(M.webContents) ?? _,
              owner: M.webContents,
              sessionId: r.sessionId,
              window: e,
            });
        }
      }),
      M.on(`closed`, () => {
        (this.appShellShortcutStates.delete(P),
          !N.isDestroyed() && N.removeListener(`before-input-event`, z));
      }),
      M.webContents.on(`will-navigate`, (e, t) => {
        lne(t, M.webContents.getURL(), te) ||
          (e.preventDefault(),
          Zz(t) &&
            this.openLinkInPreferredTarget(
              M.webContents,
              { url: t, source: `manual`, initiator: `window_open` },
              `open-external-navigation`,
            ));
      }),
      M.webContents.on(`will-frame-navigate`, (e) => {
        !e.isMainFrame && F9(e.url) && e.preventDefault();
      }),
      M.webContents.on(`will-redirect`, (e, t) => {
        let n = e.url ?? t;
        !e.isMainFrame && F9(n) && e.preventDefault();
      }),
      this.installNativeContextMenu(M),
      !c.app.isPackaged)
    ) {
      let e = new URL(M9());
      return (
        l && e.searchParams.set(`initialRoute`, l),
        O &&
          this.options.allowMcpAppSandboxDevtools === !0 &&
          e.searchParams.set(`mcpAppSandboxDevtools`, `1`),
        M.loadURL(e.toString()),
        M
      );
    }
    let ne = (0, u.join)(a.L(this.options.moduleDir), `index.html`);
    if ((0, p.existsSync)(ne))
      try {
        return (
          await M.loadURL(
            a.I(l, {
              mcpAppSandboxDevtools:
                O && this.options.allowMcpAppSandboxDevtools === !0,
            }),
          ),
          M
        );
      } catch (e) {
        O9().warning(`Failed to load bundle at`, {
          safe: {},
          sensitive: { indexPath: ne, error: e },
        });
      }
    let re = `data:text/html;charset=utf-8,${encodeURIComponent(mne())}`;
    return (await M.loadURL(re), M);
  }
  async createPrimaryWindow(e = {}) {
    return this.createWindow({ ...e, appearance: `primary` });
  }
  addBrowserCommentPopupWindowCreatedHandler(e) {
    return (
      this.browserCommentPopupWindowCreatedHandlers.add(e),
      () => {
        this.browserCommentPopupWindowCreatedHandlers.delete(e);
      }
    );
  }
  createBrowserCommentPopupWindowOptions(e) {
    let t = `browserCommentPopup`,
      r = this.shouldAlwaysUseOpaqueWindowSurface(t),
      { backgroundColor: i, backgroundMaterial: a } = L9({
        platform: process.platform,
        appearance: t,
        opaqueWindowSurfaceEnabled: r,
        prefersDarkColors: c.nativeTheme.shouldUseDarkColors,
      });
    return {
      title: ``,
      width: 294,
      height: n.yc,
      parent: e,
      show: !1,
      backgroundColor: i,
      backgroundMaterial: a ?? void 0,
      ...z9({
        appearance: t,
        opaqueWindowSurfaceEnabled: r,
        platform: process.platform,
      }),
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        spellcheck: !0,
        devTools: this.options.allowDevtools,
      },
    };
  }
  registerWindow(e, t, n, r, i) {
    let a = e.id,
      o = e.webContents.id;
    (this.windowAppearances.set(a, r),
      this.registeredWindowIds.add(a),
      this.registeredWebContentsIds.add(o),
      i === `register` && this.appServiceWindowIds.add(a),
      this.webContentsHostIds.set(o, t),
      n && this.trackPrimaryWindow(e),
      e.on(`closed`, () => {
        (this.forgetWebContents(o),
          this.windowAppearances.delete(a),
          this.registeredWindowIds.delete(a),
          this.registeredWebContentsIds.delete(o),
          this.appServiceWindowIds.delete(a),
          this.webContentsHostIds.delete(o),
          this.opaqueWindowSurfaceModes.delete(a),
          this.windowZooms.delete(a),
          n &&
            (this.primaryWindows.delete(e),
            this.lastActivePrimaryWindow === e &&
              (this.lastActivePrimaryWindow = null)));
      }),
      i === `register` && this.options.onWindowRegistered?.(e));
  }
  trackPrimaryWindow(e) {
    (this.primaryWindows.add(e),
      e.isFocused()
        ? ((this.lastActivePrimaryWindow = e),
          this.primaryWindowLastActiveAtMs.set(e, Date.now()))
        : (this.lastActivePrimaryWindow ??= e),
      e.on(`focus`, () => {
        ((this.lastActivePrimaryWindow = e),
          this.primaryWindowLastActiveAtMs.set(e, Date.now()));
      }),
      e.on(`blur`, () => {
        this.primaryWindowLastActiveAtMs.set(e, Date.now());
      }));
  }
  getTrackedPrimaryWindows() {
    let e = [];
    for (let t of this.primaryWindows) {
      if (t.isDestroyed()) {
        this.primaryWindows.delete(t);
        continue;
      }
      e.push(t);
    }
    return this.primaryWindows.size === 0
      ? ((this.lastActivePrimaryWindow = null), [])
      : (this.lastActivePrimaryWindow?.isDestroyed() &&
          (this.lastActivePrimaryWindow = null),
        e);
  }
  restorePrimaryWindowBounds() {
    let e = this.options.globalState.get(`electron-main-window-bounds`) ?? null;
    if (!e) return null;
    let t = this.clampPrimaryWindowBounds(e);
    return this.isWithinDisplayWorkArea(t) ? t : null;
  }
  getPrimaryMinimumSize() {
    return { width: 480, height: 600 };
  }
  syncPrimaryMinimumSize() {
    let e = this.getPrimaryWindows();
    if (e.length === 0) return;
    let t = this.getPrimaryMinimumSize();
    for (let n of e) {
      if (
        (n.setMinimumSize(t.width, t.height),
        n.isMaximized() || n.isFullScreen())
      )
        continue;
      let e = n.getNormalBounds(),
        r = Math.max(e.width, t.width),
        i = Math.max(e.height, t.height);
      (r === e.width && i === e.height) ||
        n.setBounds({ ...e, width: r, height: i });
    }
    let n = this.getPrimaryWindow();
    n && this.persistPrimaryWindowBounds(n);
  }
  persistPrimaryWindowBounds(e) {
    if (e.isDestroyed()) return;
    let t = e.getNormalBounds(),
      n = this.getPrimaryMinimumSize(),
      r = {
        x: t.x,
        y: t.y,
        width: Math.max(t.width, n.width),
        height: Math.max(t.height, n.height),
        isMaximized: e.isMaximized(),
      };
    this.options.globalState.set(`electron-main-window-bounds`, r);
  }
  clampPrimaryWindowBounds(e) {
    let t = this.getPrimaryMinimumSize();
    return {
      ...e,
      width: Math.max(e.width, t.width),
      height: Math.max(e.height, t.height),
    };
  }
  flushPendingMessages(e) {
    let t = this.pendingMessages.get(e.id);
    if (!t || t.length === 0) {
      this.pendingMessages.delete(e.id);
      return;
    }
    if ((this.pendingMessages.delete(e.id), !e.isDestroyed()))
      for (let n of t) e.send(B, n);
  }
  isWithinDisplayWorkArea(e) {
    let t = c.screen.getAllDisplays();
    if (t.length === 0) return !0;
    let n = { x: e.x, y: e.y, width: e.width, height: e.height };
    return t.some((e) => {
      let t = e.workArea;
      return (
        t.x < n.x + n.width &&
        t.x + t.width > n.x &&
        t.y < n.y + n.height &&
        t.y + t.height > n.y
      );
    });
  }
  installApplicationMenuTitleBarOverlaySync(e, t) {
    if (
      (process.platform !== `win32` && process.platform !== `linux`) ||
      (t !== `primary` && t !== `quickChat`)
    )
      return;
    let n = () => {
      e.isDestroyed() || e.setTitleBarOverlay(j9(this.windowZooms.get(e.id)));
    };
    return (
      c.nativeTheme.on(`updated`, n),
      n(),
      () => {
        c.nativeTheme.off(`updated`, n);
      }
    );
  }
  isOpaqueWindowsEnabled() {
    return (
      (Mne(
        this.options.settingsStore.getEffective(n.Gi.theme.key) ?? `system`,
      ) === `light`
        ? this.options.settingsStore.getEffective(n.Gi.lightChromeTheme.key)
        : this.options.settingsStore.getEffective(n.Gi.darkChromeTheme.key)
      )?.opaqueWindows === !0
    );
  }
  shouldUseOpaqueWindowSurface(e, t, n) {
    let r = c.screen.getDisplayMatching(t);
    return (
      this.shouldAlwaysUseOpaqueWindowSurface(e) ||
      Rne({ appearance: e, isFocused: n, platform: process.platform }) ||
      Ine({
        appearance: e,
        platform: process.platform,
        scaleFactor: r.scaleFactor,
        windowBounds: t,
      })
    );
  }
  shouldAlwaysUseOpaqueWindowSurface(e) {
    return (
      Lne({
        appearance: e,
        opaqueWindowsEnabled: this.isOpaqueWindowsEnabled(),
        platform: process.platform,
      }) ||
      (!sP() && !I9(e))
    );
  }
  applyWindowBackdrop(e, t, n) {
    let r = e.getBounds(),
      i = this.shouldUseOpaqueWindowSurface(t, r, e.isFocused());
    if (!n && this.opaqueWindowSurfaceModes.get(e.id) === i) return;
    let { backgroundColor: a, backgroundMaterial: o } = L9({
      platform: process.platform,
      appearance: t,
      opaqueWindowSurfaceEnabled: i,
      prefersDarkColors: c.nativeTheme.shouldUseDarkColors,
    });
    (e.setBackgroundColor(a),
      o != null && e.setBackgroundMaterial(o),
      process.platform === `darwin` && e.setVibrancy(Vne(t, i)),
      this.opaqueWindowSurfaceModes.set(e.id, i),
      this.sendMessageToWindow(e, {
        type: `electron-window-opaque-surface-changed`,
        opaqueWindowSurfaceEnabled: i,
      }));
  }
  installNativeContextMenu(e) {
    let t = sne({
      window: e,
      prepend: (e, t) => {
        if (t.mediaType !== `image`) return [];
        let n = a.R(t.srcURL);
        return n == null
          ? []
          : [
              {
                label: pne,
                click: () => {
                  c.shell.showItemInFolder(n);
                },
              },
            ];
      },
      showInspectElement: this.options.allowInspectElement,
    });
    e.on(`closed`, () => {
      t();
    });
  }
  installWebContentsDiagnostics(e) {
    let t = e.webContents,
      n = e.id,
      r = t.id,
      i = this.options.errorReporter;
    (t.on(`render-process-gone`, (t, a) => {
      a.reason === `clean-exit` ||
        this.isAppQuitting ||
        (a.reason !== `launch-failed` &&
          i.reportFatal(Error(`Renderer process gone (${a.reason})`), {
            groupingFingerprint: [`renderer-process-gone`, a.reason],
            tags: { errorType: `renderer-process-gone`, reason: a.reason },
            extra: { windowId: n, webContentsId: r, exitCode: a.exitCode },
          }),
        this.maybeRecoverFromRendererCrash(e, a.reason));
    }),
      t.on(`did-finish-load`, () => {
        this.rendererRecoveryAttempts.delete(r);
      }),
      t.on(`unresponsive`, () => {
        i.reportFatal(Error(`Renderer unresponsive`), {
          tags: { errorType: `renderer-unresponsive` },
          extra: { windowId: n, webContentsId: r, url: dI(t.getURL()) },
        });
      }),
      t.on(`did-fail-load`, (e, t, a, o, s, c, l) => {
        s &&
          t !== -3 &&
          i.reportFatal(Error(`Renderer did-fail-load`), {
            tags: { errorType: `renderer-did-fail-load` },
            extra: {
              windowId: n,
              webContentsId: r,
              errorCode: t,
              errorDescription: a,
              validatedURL: dI(o),
              frameProcessId: c,
              frameRoutingId: l,
            },
          });
      }));
  }
  maybeRecoverFromRendererCrash(e, t) {
    if (this.isAppQuitting || t === `clean-exit` || e.isDestroyed()) return;
    let n = e.webContents;
    n.isDestroyed() ||
      this.rendererRecoveryAttempts.has(n.id) ||
      (this.rendererRecoveryAttempts.add(n.id),
      setTimeout(() => {
        if (!e.isDestroyed() && !e.webContents.isDestroyed())
          try {
            e.webContents.reload();
          } catch (e) {
            O9().warning(`Failed to reload crashed window`, {
              safe: {},
              sensitive: { error: e },
            });
          }
      }, 500));
  }
};
function Mne(e) {
  return e === `system`
    ? c.nativeTheme.shouldUseDarkColors
      ? `dark`
      : `light`
    : e;
}
function M9() {
  return process.env.ELECTRON_RENDERER_URL || fne;
}
function Nne({ sender: e, senderFrame: t, moduleDir: n }) {
  return t != null && t !== e.mainFrame ? !1 : P9((t ?? e.mainFrame).url, n);
}
function N9() {
  let e = M9();
  return Zz(e) ? new URL(e).origin : null;
}
function Pne(e) {
  return (0, u.join)(e, `..`, `..`, `webview`, `index.html`);
}
function P9(e, t) {
  if (e === `about:blank` || Fne(e)) return !0;
  let n = N9();
  if (n && E9(e, n)) return !0;
  let r = (0, s.pathToFileURL)(Pne(t)).toString();
  return !!e.startsWith(r);
}
function Fne(e) {
  try {
    let t = new URL(e);
    return t.protocol === `app:` ? t.host === `-` || t.host === `` : !1;
  } catch {
    return !1;
  }
}
function F9(e) {
  try {
    return new URL(e).protocol === `app:`;
  } catch {
    return !1;
  }
}
function I9(e) {
  return (
    e === `avatarOverlay` ||
    e === `browserCommentPopup` ||
    e === `globalDictation` ||
    e === `hotkeyWindowHome` ||
    e === `hotkeyWindowThread`
  );
}
function Ine({ appearance: e, platform: t, scaleFactor: n, windowBounds: r }) {
  if (t !== `darwin` || I9(e)) return !1;
  let i = r.width * n,
    a = r.height * n;
  return Math.max(i, a) >= _ne && Math.min(i, a) >= vne;
}
function Lne({ appearance: e, opaqueWindowsEnabled: t, platform: n }) {
  return t && !I9(e) && (n === `darwin` || n === `win32`);
}
function Rne({ appearance: e, isFocused: t, platform: n }) {
  return !t && !I9(e) && n === `darwin`;
}
function zne(e) {
  return e === `primary` || e === `hotkeyWindowThread` || e === `hud`;
}
function Bne(e, t) {
  switch (e) {
    case `avatarOverlay`:
    case `browserCommentPopup`:
    case `globalDictation`:
      return null;
    case `primary`:
      return t;
    case `hotkeyWindowHome`:
      return { width: yne, height: bne };
    case `hotkeyWindowThread`:
      return { width: xne, height: Sne };
    case `hud`:
    case `quickChat`:
    case `secondary`:
      return { width: 400, height: 400 };
  }
}
function L9({
  platform: e,
  appearance: t,
  opaqueWindowSurfaceEnabled: n,
  prefersDarkColors: r,
}) {
  return n
    ? {
        backgroundColor: r ? hne : gne,
        backgroundMaterial: e === `win32` ? `none` : null,
      }
    : e === `win32` && !I9(t)
      ? { backgroundColor: k9, backgroundMaterial: `mica` }
      : { backgroundColor: k9, backgroundMaterial: null };
}
function Vne(e, t) {
  return t || I9(e) ? null : `menu`;
}
function R9({
  alwaysOnTop: e,
  hasShadow: t = !0,
  platform: n,
  resizable: r,
  thickFrame: i,
  transparent: a = !0,
}) {
  return {
    frame: !1,
    transparent: a,
    hasShadow: t,
    resizable: r,
    minimizable: !1,
    maximizable: !1,
    fullscreenable: !1,
    skipTaskbar: !0,
    ...(e ? { alwaysOnTop: !0 } : {}),
    ...(n === `win32`
      ? { accentColor: !1, roundedCorners: !1, thickFrame: i }
      : {}),
    ...(n === `darwin` ? { type: `panel` } : {}),
  };
}
function z9({
  appearance: e,
  opaqueWindowSurfaceEnabled: t,
  platform: n,
  windowZoom: r = 1,
}) {
  switch (e) {
    case `browserCommentPopup`:
      return R9({
        hasShadow: !1,
        platform: n,
        resizable: !1,
        thickFrame: !1,
        transparent: !0,
      });
    case `globalDictation`:
      return {
        ...R9({
          alwaysOnTop: !0,
          hasShadow: !1,
          platform: n,
          resizable: !1,
          thickFrame: !1,
          transparent: !0,
        }),
        ...(n === `darwin` ? { acceptFirstMouse: !0 } : {}),
      };
    case `avatarOverlay`:
      return {
        ...R9({ alwaysOnTop: !0, platform: n, resizable: !1, thickFrame: !1 }),
        ...(n === `darwin` ? { enableLargerThanScreen: !0 } : {}),
        hasShadow: !1,
      };
    case `hotkeyWindowHome`:
      return R9({ platform: n, resizable: !1, thickFrame: !1 });
    case `hotkeyWindowThread`:
      return R9({ platform: n, resizable: !0 });
    case `quickChat`:
    case `primary`:
      return n === `darwin`
        ? {
            titleBarStyle: `hiddenInset`,
            trafficLightPosition: A9(r),
            ...(e === `quickChat`
              ? { hasShadow: !0, resizable: !0, transparent: !0 }
              : {}),
            ...(t ? {} : { vibrancy: `menu` }),
          }
        : n === `win32` || n === `linux`
          ? {
              titleBarStyle: `hidden`,
              titleBarOverlay: j9(r),
              ...(e === `quickChat` ? { resizable: !0 } : {}),
            }
          : {
              titleBarStyle: `default`,
              ...(e === `quickChat` ? { resizable: !0 } : {}),
            };
    case `secondary`:
      return n === `darwin`
        ? t
          ? { titleBarStyle: `default` }
          : { vibrancy: `menu`, titleBarStyle: `default` }
        : { titleBarStyle: `default` };
    case `hud`:
      return n === `darwin`
        ? t
          ? {
              titleBarStyle: `hiddenInset`,
              minimizable: !1,
              maximizable: !1,
              fullscreenable: !1,
              alwaysOnTop: !0,
              trafficLightPosition: { x: 10, y: 10 },
            }
          : {
              vibrancy: `menu`,
              titleBarStyle: `hiddenInset`,
              minimizable: !1,
              maximizable: !1,
              fullscreenable: !1,
              alwaysOnTop: !0,
              trafficLightPosition: { x: 10, y: 10 },
            }
        : {
            titleBarStyle: `default`,
            minimizable: !1,
            maximizable: !1,
            fullscreenable: !1,
            alwaysOnTop: !0,
          };
  }
}
var Hne = 2e3;
function Une({
  appBrand: e,
  buildFlavor: t,
  allowDevtools: r,
  allowInspectElement: a,
  allowDebugMenu: o,
  errorReporter: s,
  globalState: l,
  settingsStore: d,
  desktopRoot: f,
  preloadPath: m,
  repoRoot: h,
  isMacOS: g,
  isWindows: _,
  isDevMode: v,
  canHideLastWindowToTray: y,
  isRemoteHostedPIPEnabled: b,
  onWindowRegistered: x,
  disposables: S,
}) {
  let C = (0, u.join)(h, `electron`, `src`, `icons`),
    w = (e) => {
      if (!c.app.isPackaged) return null;
      let t = (0, u.join)(process.resourcesPath, e);
      return (0, p.existsSync)(t) ? t : null;
    },
    T = (e) => {
      let t = (0, u.join)(C, e);
      return (0, p.existsSync)(t) ? t : null;
    },
    E = (e) => w(e) ?? T(e),
    D = () => {
      switch (t) {
        case i.a.Dev:
        case i.a.Nightly:
        case i.a.InternalAlpha:
        case i.a.PublicBeta:
        case i.a.Prod:
          return d.get(n.Gs.DOCK_ICON_PREFERENCE) ?? `app-default`;
        case i.a.Agent:
          return `app-default`;
      }
    },
    O = () => {
      switch (t) {
        case i.a.Dev:
          return T(`icon-dev-outline.png`);
        case i.a.Agent:
          return E(`icon-agent.png`);
        case i.a.Nightly:
        case i.a.InternalAlpha:
        case i.a.PublicBeta:
        case i.a.Prod:
          return E(`${C_(t, `darwin`, e)}.png`);
      }
    },
    k = w_(t),
    A = () =>
      c.nativeTheme.shouldUseDarkColorsForSystemIntegratedUI ? k.dark : k.light,
    j = (r) => {
      if (
        r === `app-default` &&
        t !== i.a.Dev &&
        (c.app.isPackaged || e === n.Vc.ChatGPT)
      ) {
        let e = c.app.dock;
        e != null && Reflect.apply(e.setIcon.bind(e), e, [null]);
        return;
      }
      let a = r === `codex-system` ? A() : null,
        o = (a == null ? null : E(a)) ?? O(),
        s =
          o == null
            ? c.nativeImage.createEmpty()
            : c.nativeImage.createFromPath(o);
      s.isEmpty() || c.app.dock?.setIcon(s);
    },
    M = () => {
      if (!g) return;
      let e = D();
      (j(e),
        kA({
          preference: e,
          resourceName: e === `codex-system` ? k.light : null,
        }).then((e) => {
          e && j(D());
        }));
    };
  if (g) {
    M();
    let e = () => {
      let e = D();
      e === `codex-system` && j(e);
    };
    (c.nativeTheme.on(`updated`, e),
      S.add(() => {
        c.nativeTheme.off(`updated`, e);
      }));
  }
  let N = null,
    P = new jne({
      desktopRoot: f,
      iconDirectoryName: du,
      globalState: l,
      settingsStore: d,
      moduleDir: __dirname,
      preloadPath: m,
      repoRoot: h,
      allowDevtools: r,
      allowMcpAppSandboxDevtools: v,
      allowDebugMenu: o,
      allowInspectElement: a,
      errorReporter: s,
      getChatGptWebviewAuthToken: (e) =>
        N?.getLocalAuthToken(e) ?? Promise.resolve(null),
      canHideLastWindowToTray: y,
      onWindowRegistered: (e) => {
        (N?.registerWindow(e), x?.(e));
      },
    }),
    F = new L5(P, l, Y8(), b),
    I = new hte({ windowManager: P, globalState: l, isDevMode: v }),
    ee = new vte(P),
    L = new H7({
      canForceGlobalDictationLock: v,
      forceGlobalDictationLock:
        v && l.get(n.Gs.GLOBAL_DICTATION_FORCE_LOCK_DEBUG_ENABLED) === !0,
      getVisibleNativePetWebContents: () => F.getVisibleWebContents(),
      windowManager: P,
      globalState: l,
    }),
    R = new B8(l, d, P, !0);
  S.add(() => {
    (R.dispose(), ee.dispose());
  });
  let z = (e) => {
      if (!_) return;
      let t = setTimeout(() => {
        e.isDestroyed() || e.isVisible() || (e.show(), e.focus());
      }, Hne);
      t.unref();
      let n = () => {
        clearTimeout(t);
      };
      (e.once(`show`, n), e.once(`closed`, n));
    },
    te = async ({ show: e, initialRoute: t }) => {
      if (N == null) return null;
      let n = await P.createPrimaryWindow({
        title: c.app.getName(),
        show: e,
        initialRoute: t === `/` ? void 0 : t,
      });
      return (e || z(n), n);
    };
  return {
    windowManager: P,
    avatarOverlayManager: F,
    hotkeyWindowLifecycleManager: I,
    globalDictationLifecycleManager: L,
    appshotHotkeysService: R,
    quickChatController: ee,
    updateDockIcon: M,
    setWindowContext: (e) => {
      N = e;
      for (let t of c.BrowserWindow.getAllWindows())
        t.isDestroyed() || !P.isAppServiceWindow(t) || e.registerWindow(t);
    },
    ensureWindow: async () => {
      let e = P.getPrimaryWindow();
      return e
        ? N == null
          ? null
          : (e.isMinimized() && e.restore(),
            e.show(),
            e.focus(),
            e.isDestroyed() ? null : e)
        : te({ show: !1 });
    },
    createFreshWindow: async (e = `/`) => te({ show: !0, initialRoute: e }),
    showLastActivePrimaryWindow: () => P.showLastActivePrimaryWindow(),
    markAppQuitting: () => {
      (P.markAppQuitting(), F.markAppQuitting(), ee.dispose());
    },
    isTrustedIpcSender: P.isTrustedIpcSender.bind(P),
    sendMessageToWindow: P.sendMessageToWindow.bind(P),
    sendMessageToAllRegisteredWindows:
      P.sendMessageToAllRegisteredWindows.bind(P),
    sendMessageToAllWindows: P.sendMessageToAllWindows.bind(P),
    getPrimaryWindow: P.getPrimaryWindow.bind(P),
    getContextForWebContents: (e) => (P.hasRegisteredWebContents(e) ? N : null),
  };
}
var Wne = 3e4,
  Gne = class {
    totalInvocations = 0;
    rollingWindowCounter = new n.oa({ windowMs: Wne });
    recordInvocation(e = Date.now()) {
      ((this.totalInvocations += 1), this.rollingWindowCounter.record(1, e));
    }
    getSnapshot(e = Date.now()) {
      let t = this.rollingWindowCounter.getSnapshot(e);
      return {
        totalInvocations: this.totalInvocations,
        invocationsLast30s: t.count,
      };
    }
  };
function Kne(e) {
  return typeof e == `object` && !!e;
}
function qne(e) {
  return (
    e === `worktree-cleanup-inputs` ||
    e === `computer-use-invalidate-service-pid` ||
    e === `computer-use-service-pid` ||
    e === `codex-home` ||
    e === `read-shortcut-link` ||
    e === `platform-family` ||
    e === `platform-os` ||
    e === `process-start` ||
    e === `process-write` ||
    e === `process-resize` ||
    e === `process-terminate` ||
    e === `fs-read-file` ||
    e === `fs-write-file` ||
    e === `fs-create-directory` ||
    e === `fs-get-metadata` ||
    e === `fs-read-directory` ||
    e === `fs-remove` ||
    e === `fs-copy` ||
    e === `fs-watch` ||
    e === `fs-unwatch` ||
    e === `worker-exit`
  );
}
function Jne(e) {
  return Kne(e)
    ? e.type === `worker-main-rpc-request` &&
        typeof e.workerId == `string` &&
        typeof e.requestId == `string` &&
        qne(e.method)
    : !1;
}
var Yne = class {
    worker = null;
    latestAuthUser = null;
    listeners = new Set();
    exitListeners = new Set();
    mainRpcHandler = null;
    exitedWorkers = new WeakSet();
    disposedWorkers = new WeakSet();
    constructor(e) {
      this.id = e;
    }
    dispose() {
      let e = this.worker;
      e &&
        ((this.worker = null),
        this.disposedWorkers.add(e),
        this.handleWorkerExit(e),
        e.terminate());
    }
    addListener(e) {
      return (
        this.listeners.add(e),
        () => {
          this.listeners.delete(e);
        }
      );
    }
    addExitListener(e) {
      return (
        this.exitListeners.add(e),
        () => {
          this.exitListeners.delete(e);
        }
      );
    }
    setMainRpcHandler(e) {
      this.mainRpcHandler = e;
    }
    postMessage(e) {
      this.ensureWorker().postMessage(e);
    }
    start() {
      this.ensureWorker();
    }
    setSentryUser(e, t, n, r) {
      this.latestAuthUser = {
        authMethod: e,
        userId: t,
        email: n,
        accountId: r,
      };
      let i = this.worker;
      i &&
        i.postMessage({
          type: `worker-sentry-user-update`,
          authMethod: e,
          userId: t,
          email: n,
          accountId: r,
        });
    }
    emitAppEvent(e) {
      let t = this.worker;
      t && t.postMessage({ type: `worker-app-event`, event: e });
    }
    ensureWorker() {
      if (this.worker) return this.worker;
      let e = r.i(`worker-manager`),
        t = (0, u.join)(__dirname, `worker.js`),
        o = n.h(i.a.resolve()),
        s = c.app.isPackaged ? c.app.getAppPath() : process.cwd(),
        l = {
          workerId: this.id,
          sentryInitOptions: a.O,
          maxLogLevel: o,
          sentryRewriteFramesRoot: s,
          spawnInsideWsl: n.li(),
        },
        d = new C.Worker(t, { name: this.id, workerData: l });
      return (
        d.on(`message`, (e) => {
          if (Jne(e)) {
            this.handleMainRpcRequest(d, e);
            return;
          }
          this.listeners.forEach((t) => {
            t(e);
          });
        }),
        d.on(`error`, (t) => {
          e.warning(`Worker error`, { safe: {}, sensitive: { error: t } });
        }),
        d.on(`exit`, (t) => {
          (this.worker === d && (this.worker = null),
            this.handleWorkerExit(d),
            t !== 0 &&
              !this.disposedWorkers.has(d) &&
              e.warning(`Worker exited`, { safe: { code: t }, sensitive: {} }));
        }),
        d.unref(),
        (this.worker = d),
        this.latestAuthUser &&
          d.postMessage({
            type: `worker-sentry-user-update`,
            authMethod: this.latestAuthUser.authMethod,
            userId: this.latestAuthUser.userId,
            email: this.latestAuthUser.email,
            accountId: this.latestAuthUser.accountId,
          }),
        d
      );
    }
    async handleMainRpcRequest(e, t) {
      let n = r.i(`worker-manager`);
      if (t.workerId !== this.id) {
        n.warning(`Received main RPC request for wrong worker`, {
          safe: {
            expectedWorkerId: this.id,
            workerId: t.workerId,
            method: t.method,
          },
        });
        return;
      }
      let i = this.mainRpcHandler;
      if (!i) {
        let n = {
          type: `worker-main-rpc-response`,
          workerId: this.id,
          requestId: t.requestId,
          method: t.method,
          result: {
            type: `error`,
            error: {
              message: `No main RPC handler configured for worker '${this.id}'`,
            },
          },
        };
        e.postMessage(n);
        return;
      }
      try {
        let n = await i(t, this.createMainRpcHandlerContext(e)),
          r = {
            type: `worker-main-rpc-response`,
            workerId: this.id,
            requestId: t.requestId,
            method: t.method,
            result: { type: `ok`, value: n },
          };
        e.postMessage(r);
      } catch (n) {
        let r =
            typeof n == `object` &&
            n &&
            `code` in n &&
            typeof n.code == `string`
              ? n.code
              : void 0,
          i = {
            type: `worker-main-rpc-response`,
            workerId: this.id,
            requestId: t.requestId,
            method: t.method,
            result: {
              type: `error`,
              error: {
                ...(r == null ? {} : { code: r }),
                message:
                  n instanceof Error
                    ? n.message
                    : `Failed to handle main RPC request: ${String(n)}`,
              },
            },
          };
        e.postMessage(i);
      }
    }
    createMainRpcHandlerContext(e) {
      return {
        emitEvent: (t) => {
          let n = {
            type: `worker-main-rpc-event`,
            workerId: this.id,
            method: t.method,
            params: t.params,
          };
          e.postMessage(n);
        },
      };
    }
    handleWorkerExit(e) {
      if (this.exitedWorkers.has(e)) return;
      (this.exitedWorkers.add(e),
        this.exitListeners.forEach((e) => {
          e();
        }));
      let t = this.mainRpcHandler;
      if (!t) return;
      let n = r.i(`worker-manager`);
      t(
        {
          type: `worker-main-rpc-request`,
          workerId: this.id,
          requestId: `worker-exit`,
          method: `worker-exit`,
          params: {},
        },
        this.createMainRpcHandlerContext(e),
      ).catch((e) => {
        n.warning(`Worker exit cleanup failed`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }
  },
  B9 = class {
    logger = r.i(`worker-bus-message-handler`);
    pending = new Map();
    pendingRequestFromHost = new Map();
    hostListeners = new Set();
    knownWebContents = new Set();
    workerManager;
    constructor(e, t = {}) {
      ((this.workerId = e),
        (this.options = t),
        (this.workerManager = new Yne(this.workerId)),
        this.workerManager.addListener((e) => {
          this.handleWorkerMessage(e);
        }));
    }
    async handleMessage(e, t) {
      if ((this.trackLifecycle(e), t.workerId !== this.workerId)) {
        this.logger.warning(`Received worker message for wrong worker`, {
          safe: {
            expectedWorkerId: this.workerId,
            workerId: t.workerId,
            type: t.type,
          },
        });
        return;
      }
      switch (t.type) {
        case `worker-request`: {
          let n = t;
          (this.pending.set(n.request.id, e),
            this.options.onWorkerRequestSent?.(),
            this.workerManager.postMessage(n));
          return;
        }
        case `worker-request-cancel`:
          this.pending.delete(t.id);
          break;
      }
      this.workerManager.postMessage(t);
    }
    dispose() {
      let e = Error(`Worker bus disposed for '${this.workerId}'`);
      for (let t of this.pendingRequestFromHost.values()) t.reject(e);
      (this.pendingRequestFromHost.clear(), this.workerManager.dispose());
    }
    requestFromHost(e) {
      if (e.signal?.aborted)
        return Promise.reject(new DOMException(`Aborted`, `AbortError`));
      let t = n.fi(`main-${(0, f.randomUUID)()}`),
        r = { id: t, method: e.method, params: e.params },
        i = { type: `worker-request`, workerId: this.workerId, request: r },
        a = new Promise((n, r) => {
          this.pendingRequestFromHost.set(t, {
            method: e.method,
            resolve: (e) => {
              n(e);
            },
            reject: r,
          });
        });
      try {
        (this.options.onWorkerRequestSent?.(),
          this.workerManager.postMessage(i));
      } catch (e) {
        return (
          this.pendingRequestFromHost.delete(t),
          Promise.reject(
            e instanceof Error
              ? e
              : Error(`Failed to post worker request: ${String(e)}`),
          )
        );
      }
      let o = e.signal;
      if (o) {
        let e = () => {
          o.removeEventListener(`abort`, e);
          let n = this.pendingRequestFromHost.get(t);
          n &&
            (this.pendingRequestFromHost.delete(t),
            this.workerManager.postMessage({
              type: `worker-request-cancel`,
              workerId: this.workerId,
              id: t,
            }),
            n.reject(new DOMException(`Aborted`, `AbortError`)));
        };
        o.addEventListener(`abort`, e);
        let n = () => {
          o.removeEventListener(`abort`, e);
        };
        a.then(n, n);
      }
      return a;
    }
    subscribeFromHost(e) {
      return (
        this.hostListeners.add(e),
        () => {
          this.hostListeners.delete(e);
        }
      );
    }
    subscribeWorkerExit(e) {
      return this.workerManager.addExitListener(e);
    }
    setSentryUser(e, t, n, r) {
      this.workerManager.setSentryUser(e, t, n, r);
    }
    emitAppEvent(e) {
      this.workerManager.emitAppEvent(e);
    }
    setMainRpcHandler(e) {
      this.workerManager.setMainRpcHandler(e);
    }
    startWorker() {
      this.workerManager.start();
    }
    trackLifecycle(e) {
      this.knownWebContents.has(e) ||
        (this.knownWebContents.add(e),
        e.once(`destroyed`, () => {
          (this.knownWebContents.delete(e), this.cleanupOrigin(e));
        }));
    }
    cleanupOrigin(e) {
      for (let [t, n] of this.pending.entries()) {
        if (n !== e) continue;
        this.pending.delete(t);
        let r = {
          type: `worker-request-cancel`,
          workerId: this.workerId,
          id: t,
        };
        this.workerManager.postMessage(r);
      }
    }
    handleWorkerMessage(e) {
      if (e.workerId !== this.workerId) {
        this.logger.warning(`Received worker message for wrong worker`, {
          safe: {
            expectedWorkerId: this.workerId,
            workerId: e.workerId,
            type: e.type,
          },
        });
        return;
      }
      if (e.type === `worker-response`) {
        let t = this.pendingRequestFromHost.get(e.response.id);
        if (t) {
          if (
            (this.pendingRequestFromHost.delete(e.response.id),
            t.method !== e.response.method)
          ) {
            t.reject(Error(`Mismatched worker response method`));
            return;
          }
          if (e.response.result.type === `ok`) {
            t.resolve(e.response.result.value);
            return;
          }
          t.reject(Error(e.response.result.error.message));
          return;
        }
        let n = this.pending.get(e.response.id);
        if (!n) return;
        (this.pending.delete(e.response.id), this.send(n, e));
        return;
      }
      (this.hostListeners.forEach((t) => {
        t(e);
      }),
        this.options.hostOnlyEventTypes?.has(e.event.type) ||
          this.broadcast(e));
    }
    send(e, t) {
      e.isDestroyed() || e.send(_u(this.workerId), t);
    }
    broadcast(e) {
      this.knownWebContents.forEach((t) => {
        this.send(t, e);
      });
    }
  };
function Xne({ disposables: e, isTrustedIpcEvent: t }) {
  let r = new n.Ur(),
    i = new n.$(r),
    a = new Gne(),
    o = [],
    s = new B9(`git`, {
      hostOnlyEventTypes: new Set([
        `create-worktree-path`,
        `create-worktree-setup-started`,
        `create-worktree-stream`,
      ]),
      onWorkerRequestSent: () => {
        a.recordInvocation();
      },
    }),
    l = new B9(`open-in`),
    u = new Map();
  l.setMainRpcHandler(async (e) => {
    switch (e.method) {
      case `read-shortcut-link`: {
        let t = u.get(e.params.path);
        t ??
          ((t = c.shell.readShortcutLink(e.params.path)),
          u.set(e.params.path, t));
        let { icon: n, target: r } = t;
        return { icon: n, target: r };
      }
      case `worker-exit`:
        return {};
    }
  });
  let d = new Set(),
    f = new Map(),
    p = new Map(),
    m = null,
    h = null;
  e.add(() => {
    f.forEach((e) => {
      e();
    });
  });
  let g = (e, t, n, r) => {
      ((m = { authMethod: e, userId: t, email: n, accountId: r }),
        o.forEach((i) => {
          i(e, t, n, r);
        }),
        f.forEach((i, a) => {
          a.setSentryUser(e, t, n, r);
        }),
        p.forEach((i) => {
          i.setSentryUser(e, t, n, r);
        }));
    },
    _ = (e) => s.requestFromHost(e),
    v = async (e) => {
      let t = new B9(`computer-use-capture`);
      x(t);
      let n = r.subscribe((e) => {
          t.emitAppEvent(e);
        }),
        i = !1,
        a = () => {
          i || ((i = !0), n.dispose(), f.delete(t), t.dispose());
        };
      f.set(t, a);
      try {
        t.startWorker();
        let n = await t.requestFromHost(e);
        return (n.result === `started` && S(e.params.requestId, e.signal), n);
      } finally {
        a();
      }
    },
    y = (t) => {
      (e.add(t),
        o.push((e, n, r, i) => {
          t.setSentryUser(e, n, r, i);
        }),
        e.add(
          r.subscribe((e) => {
            t.emitAppEvent(e);
          }),
        ));
    },
    b = (e, n) => {
      (y(n),
        c.ipcMain.handle(gu(e), async (e, r) => {
          t(e) && (await n.handleMessage(e.sender, r));
        }));
    };
  return {
    appEvent: r,
    gitManager: i,
    gitWorkerInvocationSampler: a,
    requestComputerUseCaptureWorker: v,
    requestGitWorker: _,
    requestOpenInWorker: (e) => l.requestFromHost(e),
    subscribeComputerUseCaptureWorkerEvent: (e, t) => {
      let n = (n) => {
        Zne(n, e) && t(n);
      };
      return (
        d.add(n),
        () => {
          d.delete(n);
        }
      );
    },
    subscribeGitWorkerEvent: (e, t) =>
      s.subscribeFromHost((n) => {
        n.type === `worker-event` && Qne(n.event, e) && t(n.event);
      }),
    updateWorkerSentryUser: g,
    registerWorkerBusMessageHandlers: () => {
      y(l);
      for (let e of n.di) {
        if (e === `git`) {
          b(e, s);
          continue;
        }
        b(e, new B9(e));
      }
    },
    setGitWorkerMainRpcHandler: (e) => {
      s.setMainRpcHandler(e);
    },
    setComputerUseCaptureWorkerMainRpcHandler: (e) => {
      ((h = e),
        f.forEach((t, n) => {
          n.setMainRpcHandler(e);
        }),
        p.forEach((t) => {
          t.setMainRpcHandler(e);
        }));
    },
  };
  function x(e) {
    (h != null && e.setMainRpcHandler(h),
      m != null &&
        e.setSentryUser(m.authMethod, m.userId, m.email, m.accountId));
  }
  function S(e, t) {
    if (p.has(e)) return;
    let n = new B9(`computer-use-capture`);
    (x(n), p.set(e, n));
    let i = r.subscribe((e) => {
        n.emitAppEvent(e);
      }),
      a = n.subscribeFromHost((t) => {
        t.type === `worker-event` &&
          (d.forEach((e) => {
            e(t.event);
          }),
          t.event.type === `update` &&
            t.event.requestId === e &&
            (t.event.update.type === `completed` ||
              t.event.update.type === `failed`) &&
            l());
      }),
      o = n.subscribeWorkerExit(() => {
        c || (u(`update_worker_exited`), l());
      }),
      s = () => {
        l();
      };
    t?.addEventListener(`abort`, s, { once: !0 });
    let c = !1,
      l = () => {
        c ||
          ((c = !0),
          a(),
          o(),
          t?.removeEventListener(`abort`, s),
          i.dispose(),
          p.delete(e),
          n.dispose());
      },
      u = (t) => {
        d.forEach((n) => {
          n({
            type: `update`,
            requestId: e,
            update: { type: `failed`, failureReason: t },
          });
        });
      };
    (n.startWorker(),
      n
        .requestFromHost({
          method: `watchUpdates`,
          params: { requestId: e },
          signal: t,
        })
        .catch(() => {
          c || (u(`update_watch_failed`), l());
        }));
  }
}
function Zne(e, t) {
  return e.type === t;
}
function Qne(e, t) {
  return e.type === t;
}
var $ne = 3,
  ere = 5.5,
  tre = 1,
  V9 = null,
  H9 = null,
  U9 = null,
  W9 = !1,
  nre = !1;
function rre() {
  return V9;
}
async function ire(e) {
  return process.platform !== `win32` && process.platform !== `darwin`
    ? null
    : ((W9 = !0),
      V9 ??
        H9 ??
        ((nre ||=
          (c.app.on(`before-quit`, () => {
            G9();
          }),
          !0)),
        (H9 = (async () => {
          let t = await are(e.buildFlavor, e.appBrand, e.repoRoot),
            n = new c.Tray(t.defaultIcon);
          if (!W9) return (n.destroy(), null);
          if (process.platform === `win32`) {
            let t = x_(e.buildFlavor, e.appBrand);
            if (t.dark !== t.light) {
              let t = () => {
                let t = K9(e.buildFlavor, e.appBrand, e.repoRoot);
                t != null && n.setImage(t);
              };
              (c.nativeTheme.on(`updated`, t),
                (U9 = () => {
                  c.nativeTheme.off(`updated`, t);
                }),
                t());
            }
          }
          return (
            n.setToolTip(c.app.getName()),
            (V9 = new p6(
              n,
              e.onTrayButtonClick,
              e.onTrayMenuOpenMainWindow,
              e.onTrayMenuOpenNewThread,
              e.onTrayMenuOpenRecentThread,
              e.getChronicleSidecarControlState,
              e.toggleChronicleSidecar,
              t,
              a.H(),
              c.app.getName(),
            )),
            V9
          );
        })().finally(() => {
          H9 = null;
        })),
        H9));
}
function G9() {
  ((W9 = !1), U9?.(), (U9 = null), V9?.destroy(), (V9 = null));
}
async function are(e, t, n) {
  if (process.platform === `darwin`) {
    let [e] = S_(t),
      r = [
        ...(c.app.isPackaged ? [(0, u.join)(process.resourcesPath, e)] : []),
        (0, u.join)(n, `electron`, `src`, `icons`, e),
      ];
    for (let e of r) {
      let t = c.nativeImage.createFromPath(e);
      if (!t.isEmpty())
        return (
          t.setTemplateImage(!0),
          { defaultIcon: t, chronicleRunningIcon: q9(t) }
        );
    }
    let i = await c.app.getFileIcon(process.execPath, { size: `normal` });
    return { defaultIcon: i, chronicleRunningIcon: q9(i) };
  }
  let r = K9(e, t, n);
  return r == null
    ? {
        defaultIcon: await c.app.getFileIcon(process.execPath, {
          size: `small`,
        }),
        chronicleRunningIcon: null,
      }
    : { defaultIcon: r, chronicleRunningIcon: null };
}
function K9(e, t, n) {
  let r = x_(e, t),
    i = c.nativeTheme.shouldUseDarkColorsForSystemIntegratedUI
      ? r.dark
      : r.light,
    a = [
      ...(c.app.isPackaged ? [(0, u.join)(process.resourcesPath, i)] : []),
      (0, u.join)(n, `electron`, `src`, `icons`, i),
    ];
  for (let e of a) {
    let t = c.nativeImage.createFromPath(e);
    if (!t.isEmpty()) return t;
  }
  return null;
}
function q9(e) {
  let t = e.getSize(),
    n = t.width,
    r = t.height;
  if (n <= 0 || r <= 0) return null;
  let i = c.nativeImage.createEmpty(),
    a = !1;
  for (let t of e.getScaleFactors()) {
    let o = ore(e, n, r, t);
    o != null && (i.addRepresentation(o), (a = !0));
  }
  return !a || i.isEmpty() ? null : (i.setTemplateImage(!0), i);
}
function ore(e, t, n, r) {
  let i = Buffer.from(e.toBitmap({ scaleFactor: r })),
    a = sre(i, t, n);
  if (a == null) return null;
  let { height: o, scaleFactor: s, width: c } = a,
    l = Math.min(c, o) / Math.min(t, n),
    u = $ne * l,
    d = ere * l,
    f = tre * l,
    p = c - u - f,
    m = o - u - f;
  for (let e = 0; e < o; e += 1)
    for (let t = 0; t < c; t += 1) {
      let n = Math.hypot(t + 0.5 - p, e + 0.5 - m),
        r = (e * c + t) * 4;
      (n <= d && (i[r + 3] = 0),
        n <= u &&
          ((i[r] = 0), (i[r + 1] = 0), (i[r + 2] = 0), (i[r + 3] = 255)));
    }
  return { buffer: i, width: c, height: o, scaleFactor: s };
}
function sre(e, t, n) {
  let r = e.length / 4;
  if (!Number.isInteger(r) || r <= 0) return null;
  if (r === t * n) return { width: t, height: n, scaleFactor: 1 };
  let i = t / n,
    a = Math.round(Math.sqrt(r / i)),
    o = Math.round(a * i);
  return o * a === r ? { width: o, height: a, scaleFactor: o / t } : null;
}
function cre({
  exitImmediately: e,
  exitImmediatelyForUpdateInstall: t,
  quitForUpdateInstall: n,
  quitState: r,
}) {
  if (e) {
    t();
    return;
  }
  (r.allowQuitTemporarilyForUpdateInstall(), n());
}
var lre = (0, d.promisify)(h.execFile),
  ure = `--open-project`,
  J9 = `HKCU\\Software\\Classes\\Directory\\shell\\OpenProjectInCodex`,
  dre = `${J9}\\command`;
function fre(e, t = a.W()) {
  return [
    { key: J9, value: `Open project in ${t}` },
    { key: J9, name: `Icon`, value: `"${e}",0` },
    { key: dre, value: `"${e}" ${ure} "%1"` },
  ];
}
async function pre({ key: e, name: t, value: n }) {
  await lre(
    `reg.exe`,
    [`add`, e, ...(t ? [`/v`, t] : [`/ve`]), `/d`, n, `/f`],
    { windowsHide: !0 },
  );
}
async function mre({ isWindows: e, isPackaged: t, executablePath: n }) {
  if (!(!e || !t))
    try {
      await Promise.all(fre(n).map(pre));
    } catch (e) {
      r.r().warning(`Failed to register Windows folder context menu`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
var hre = 2e3;
function gre({
  platform: e = process.platform,
  pid: t = process.pid,
  loadNativeAddon: n = a.D,
} = {}) {
  e === `win32` && Y9(n).killProcessDescendants(t);
}
function _re({
  delayMs: e = hre,
  loadNativeAddon: t = a.D,
  platform: n = process.platform,
} = {}) {
  n === `win32` && Y9(t).armTerminateCurrentProcess(e);
}
function Y9(e) {
  let t = e();
  if (t == null) throw Error(`Failed to load Windows updater addon.`);
  return t;
}
function vre(e) {
  return async (t) => {
    switch (t.method) {
      case `computer-use-invalidate-service-pid`:
        return (
          await e.invalidateManagedComputerUseService(
            t.params.processIdentifier,
          ),
          {}
        );
      case `computer-use-service-pid`:
        return e.ensureManagedComputerUseService();
      case `worker-exit`:
        return {};
    }
  };
}
function X9(e) {
  return JSON.stringify([e.hostId, e.watchId]);
}
function yre(e) {
  let t = new Map(),
    r = new Map(),
    i = (t) => {
      let n = e.getAppServerClientForHost(t);
      if (n == null) throw Error(`Execution host ${t.hostId} not found`);
      return n;
    },
    a = (e) => {
      let n = t.get(e);
      if (n == null) throw Error(`Process ${e} not found`);
      return n;
    },
    o = () => {
      let e = Array.from(r.values());
      r.clear();
      for (let t of e)
        t.session != null && t.session.dispose().catch(() => void 0);
    };
  return async (s, c) => {
    switch (s.method) {
      case `worker-exit`:
        return (o(), {});
      case `worktree-cleanup-inputs`:
        return e.getWorktreeCleanupInputsForHost(s.params);
      case `codex-home`:
        return i(s.params).codexHome();
      case `platform-family`:
        return i(s.params).platformFamily();
      case `platform-os`:
        return i(s.params).platformOs();
      case `process-start`: {
        let { hostId: e, processHandle: n, ...r } = s.params,
          a = i({ hostId: e })
            .spawn(r)
            .then((e) => ({ process: e, stdin: e.stdin.getWriter() }));
        t.set(n, a);
        try {
          let { process: e } = await a,
            [t] = await Promise.all([
              e.wait(),
              Z9(e.stdout, `stdout`, n, c),
              Z9(e.stderr, `stderr`, n, c),
            ]);
          return t;
        } finally {
          t.get(n) === a && t.delete(n);
        }
      }
      case `process-write`: {
        let { stdin: e } = await a(s.params.processHandle);
        return (
          s.params.delta != null && (await e.write(s.params.delta)),
          s.params.closeStdin && (await e.close()),
          {}
        );
      }
      case `process-resize`: {
        let { process: e } = await a(s.params.processHandle);
        return (await e.resize(s.params.size), {});
      }
      case `process-terminate`: {
        let { process: e } = await a(s.params.processHandle);
        return (e.kill(), {});
      }
      case `fs-read-file`:
        return n.Co(await i(s.params).readFile(s.params.path));
      case `fs-write-file`:
        return (
          await i(s.params).writeFile(s.params.path, s.params.contents),
          {}
        );
      case `fs-create-directory`:
        return (
          await i(s.params).createDirectory(s.params.path, {
            recursive: s.params.recursive,
          }),
          {}
        );
      case `fs-get-metadata`: {
        let e = await i(s.params).stat(s.params.path, {
          bypassCache: s.params.bypassCache,
          followSymlinks: s.params.followSymlinks,
        });
        return {
          birthtimeMs: e.birthtimeMs,
          ctimeMs: e.ctimeMs,
          ino: e.ino,
          isDirectory: e.isDirectory(),
          isFile: e.isFile(),
          isSymbolicLink: e.isSymbolicLink(),
          mtimeMs: e.mtimeMs,
          size: e.size,
        };
      }
      case `fs-read-directory`:
        return (await i(s.params).readDirectory(s.params.path)).map((e) => ({
          name: e.name,
          isDirectory: e.isDirectory(),
          isFile: e.isFile(),
          isSymbolicLink: e.isSymbolicLink(),
        }));
      case `fs-remove`:
        return (
          await i(s.params).remove(s.params.path, {
            recursive: s.params.recursive,
            force: s.params.force,
          }),
          {}
        );
      case `fs-copy`:
        return (
          await i(s.params).copy(
            s.params.sourcePath,
            s.params.destinationPath,
            { recursive: s.params.recursive },
          ),
          {}
        );
      case `fs-watch`: {
        let {
            hostId: e,
            watchId: t,
            path: n,
            recursive: a,
            renameEventHandling: o,
          } = s.params,
          l = X9({ hostId: e, watchId: t }),
          u = { session: null };
        r.set(l, u);
        let d;
        try {
          d = await i({ hostId: e }).startFileWatch({
            path: n,
            recursive: a,
            renameEventHandling: o,
            watchId: t,
            onChange: (n) => {
              r.get(l) === u &&
                c.emitEvent({
                  method: `fs-watch-changed`,
                  params: { hostId: e, watchId: t, ...n },
                });
            },
          });
        } catch (e) {
          throw (r.get(l) === u && r.delete(l), e);
        }
        return (
          (u.session = d),
          r.get(l) === u
            ? (d.closed.then((n) => {
                r.get(l) === u &&
                  (r.delete(l),
                  c.emitEvent({
                    method: `fs-watch-closed`,
                    params: {
                      hostId: e,
                      watchId: t,
                      reason: n.reason,
                      ...(n.reason === `watch-error` ? { error: n.error } : {}),
                    },
                  }));
              }),
              { coverage: d.coverage, path: d.path })
            : (d.dispose().catch(() => void 0),
              { coverage: d.coverage, path: d.path })
        );
      }
      case `fs-unwatch`: {
        let e = X9(s.params),
          t = r.get(e);
        return t == null
          ? {}
          : (r.delete(e), t.session != null && (await t.session.dispose()), {});
      }
    }
  };
}
async function Z9(e, t, n, r) {
  for await (let i of e)
    r.emitEvent({
      method: `process-output-delta`,
      params: { processHandle: n, stream: t, chunk: i },
    });
}
function bre(e) {
  let i = t.r(e.isMacOS),
    a = [],
    o = null,
    s = async (t) => {
      let n = [];
      for (let e of t) n.includes(e) || n.push(e);
      if (n.length === 0) return;
      let r = it(e.globalState),
        i = [];
      for (let t of n)
        r.includes(t) || ((r = [t, ...r]), lt(e.globalState, t), i.push(t));
      let a = n[n.length - 1];
      if (i.length > 0) {
        (ct(e.globalState, r),
          e.windowManager.sendMessageToAllWindows({
            type: `workspace-root-options-updated`,
          }));
        for (let t of i)
          e.windowManager.sendMessageToAllWindows({
            type: `workspace-root-option-added`,
            root: t,
          });
      }
      let o = rt(e.globalState);
      ((o.length !== 1 || o[0] !== a) &&
        (st(e.globalState, [a]),
        e.windowManager.sendMessageToAllWindows({
          type: `active-workspace-roots-updated`,
        })),
        e.windowManager.showPrimaryWindow({ stealFocus: !0 }),
        e.windowManager.sendMessageToAllWindows({
          type: `navigate-to-route`,
          path: `/`,
          state: { focusComposerNonce: Date.now() },
        }));
    },
    c = async (t) => {
      let n = Cu(t);
      if (n.length === 0) return;
      let i = await e.ensureWindow();
      if (i == null || i.isDestroyed()) return;
      e.windowManager.showPrimaryWindow({ stealFocus: !0 });
      let a = t.some((e) => u.extname(e).toLowerCase() === `.skill`),
        o = n.filter(({ path: e }) => u.extname(e).toLowerCase() !== `.skill`);
      (e.windowManager.sendMessageToWindow(i, {
        type: `new-projectless-task`,
        files: n,
        ...(a ? { prefillPromptKind: `install-skill` } : {}),
        ...(o.length > 0 ? { previewFiles: o } : {}),
      }),
        r.r().info(`Opened desktop files in projectless task`, {
          safe: { fileCount: n.length, fileExtensions: xre(n) },
        }));
    },
    l = async (e) => {
      let t = u.resolve(e);
      try {
        let e = await (0, m.stat)(t);
        return e.isDirectory()
          ? { kind: `directory`, path: t }
          : e.isFile() && xu(t)
            ? { kind: `file`, path: t }
            : null;
      } catch (e) {
        return (
          r.r().warning(`Failed to stat desktop open path`, {
            safe: {},
            sensitive: { error: e },
          }),
          null
        );
      }
    },
    d = async () => {
      if (a.length === 0) return;
      let e = a.splice(0, a.length),
        t = await Promise.all(e.map((e) => l(e))),
        n = [],
        r = [];
      for (let e of t)
        if (e != null) {
          if (e.kind === `directory`) {
            n.push(e.path);
            continue;
          }
          r.push(e.path);
        }
      (await s(n), await c(r), a.length > 0 && (await d()));
    },
    f = async () => {
      if (o) return o;
      o = d();
      try {
        await o;
      } finally {
        o = null;
      }
    },
    p = (t) => {
      (a.push(...t),
        f().catch((t) => {
          e.errorReporter.reportNonFatal(t instanceof Error ? t : n.Rs(t), {
            kind: `desktop-open-paths`,
          });
        }));
    },
    h = t.i(p);
  return (
    h.length > 0 && p(h),
    () => {
      (t.t(p), i());
    }
  );
}
function xre(e) {
  return Array.from(
    new Set(e.map((e) => u.extname(e.path).slice(1).toLowerCase())),
  ).sort();
}
function Q9(e) {
  return e.replace(/\\/g, `/`);
}
function Sre(e, t) {
  let r = n.it(gi(t)).replace(/\\/g, `/`),
    i = it(e).map(Q9),
    a = (e) => {
      let t = Q9(e);
      return i.includes(t) ? !1 : t === r || t.startsWith(`${r}/`);
    },
    o = rt(e),
    s = o.filter((e) => !a(e));
  s.length !== o.length && st(e, s);
  let c = it(e),
    l = c.filter((e) => !a(e));
  l.length !== c.length && ct(e, l);
}
var $9 = { enabled: !1, running: !1, state: `disabled` };
function Cre(e) {
  let t = (e instanceof Error ? e.message : String(e)).toLowerCase();
  return (
    t.includes(`sqlite state db`) ||
    t.includes(`state db backfill`) ||
    t.includes(`sqlite`) ||
    t.includes(`database`)
  );
}
async function wre() {
  let {
      startedAtMs: e,
      buildFlavor: o,
      desktopSentry: s,
      sparkleManager: l,
      setSparkleBridgeHandlers: d,
      setSecondInstanceArgsHandler: f,
    } = a.u(),
    p = n.h(o),
    m = i.a.shouldIncludeSparkle(o, process.platform, process.env),
    h = i.a.shouldIncludeUpdater(o, process.platform, process.env),
    g = i.a.allowDevtools(o),
    _ = o === i.a.Dev || o === i.a.Agent,
    v = i.a.allowDebugMenu(o),
    y = n.Nt((e, t, n) => {
      s.captureException(e, {
        fingerprint: n.groupingFingerprint,
        level: t === `fatal` ? `fatal` : `error`,
        tags: n.tags,
        extra: n.extra,
      });
    }),
    b = c.app.getVersion(),
    x = i.o(),
    S = (e) => {
      y.reportNonFatal(`Datadog log sink failure`, {
        kind: `datadog-log-sink-failure`,
        tags: { failureType: e.type, reason: e.reason },
        extra: { failure: e },
      });
    },
    C = new n.Bt({
      fetchImpl: (e, t) => c.net.fetch(e instanceof URL ? e.toString() : e, t),
      reportFailure: S,
    }),
    w = new n.zt({
      source: `codex-desktop`,
      env: o,
      codexAppSessionId: i.i,
      platform: process.platform,
      buildInfo: { version: b, buildNumber: a.k.value },
      reportFailure: S,
      transport: C,
    }),
    T = new n.Rt({
      appSessionId: i.i,
      appShell: x ? `owl` : `electron`,
      buildFlavor: o,
      buildInfo: { version: b, buildNumber: a.k.value },
      hostArchitecture: process.arch,
      hostOsName: n.Ra(process.platform) ?? null,
      hostPlatform: process.platform,
      hostSystemVersion:
        typeof process.getSystemVersion == `function`
          ? process.getSystemVersion()
          : null,
      source: `codex-desktop`,
      transport: C,
    });
  a.o(y, w, i.i, p);
  let E = Fu({ app: c.app }),
    D = Iu({ app: c.app }),
    O = r.o(`startup`),
    k = (t, n, r = {}) => {
      let i = Date.now();
      O.trace(t, {
        safe: { phaseElapsedMs: i - n, startupElapsedMs: i - e, ...r },
      });
    };
  k(`bootstrap handoff complete`, e, { appWhenReadyResolved: c.app.isReady() });
  let A = process.platform === `darwin`,
    j = process.platform === `win32`,
    M = !c.app.isPackaged,
    N = !1,
    P = new n.sr();
  (P.add(E),
    P.add(D),
    r.r().info(`Launching app`, {
      safe: {
        buildFlavor: o,
        nodeEnv: process.env.NODE_ENV,
        enableUpdater: h,
        enableSparkle: m,
        allowDevtools: g,
        allowInspectElement: _,
        allowDebugMenu: v,
        platform: process.platform,
        packaged: c.app.isPackaged,
        agentRunId: process.env.CODEX_ELECTRON_AGENT_RUN_ID?.trim() || null,
      },
    }));
  let F = Date.now();
  (await c.app.whenReady(),
    k(`main app.whenReady resolved`, F),
    M &&
      ((F = Date.now()), k(`react devtools extension loaded=${await Qd()}`, F)),
    (F = Date.now()),
    a.z(a.L(__dirname)),
    k(`registered app protocol`, F),
    (F = Date.now()),
    await Dy(),
    k(`shell environment hydrated`, F));
  let I = a.d({ appBrand: a.U(), moduleDir: __dirname });
  ((F = Date.now()),
    await gd(I.globalState, md),
    await hd(I.globalState),
    k(`local data migrations completed`, F),
    await Dp(I.codexHome),
    Ye(
      Xe(
        {
          openAIMcpFormElicitations:
            I.globalState.getStored(
              `electron-openai-mcp-form-elicitations-enabled`,
            ) ?? He.openAIMcpFormElicitations,
        },
        { buildFlavor: o },
      ),
    ),
    await a.V(I.repoRoot));
  let ee = () =>
      Ke().cuaPIP &&
      I.settingsStore.getEffective(n.Hi.alwaysHidePictureInPicture.key) !== !0,
    L = hp({ isEnabled: ee, isMacOS: A, preferenceStore: I.globalState });
  P.add(L.dispose);
  let R = Une({
    appBrand: a.U(),
    buildFlavor: o,
    allowDevtools: g,
    allowDebugMenu: v,
    allowInspectElement: _,
    errorReporter: y,
    globalState: I.globalState,
    settingsStore: I.settingsStore,
    desktopRoot: I.desktopRoot,
    preloadPath: I.preloadPath,
    repoRoot: I.repoRoot,
    isMacOS: A,
    isWindows: j,
    isDevMode: M,
    canHideLastWindowToTray: () => N,
    isRemoteHostedPIPEnabled: ee,
    onWindowRegistered: L.registerWindow,
    disposables: P,
  });
  (L.setWindowMessageSender((e) => {
    R.windowManager.sendMessageToAllWindows(e);
  }),
    L.setPetWakeRequestHandler(() => {
      R.avatarOverlayManager.wake().catch((e) => {
        r.r().warning(`Failed to wake pet for PiP placement request`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }));
  let z = ee(),
    te = () => {
      let e = ee();
      (L.reconcileEnabledState(),
        z !== e &&
          (r.r().info(`Remote hosted PiP availability changed`, {
            safe: { enabled: e },
          }),
          R.avatarOverlayManager.reconcileRemoteHostedPIPContentHost()),
        (z = e));
    };
  P.add(I.settingsStore.onDidChange(n.Hi.alwaysHidePictureInPicture.key, te));
  let ne = !1,
    re = () => (ne ? R.ensureWindow() : Promise.resolve(null)),
    ie = (e) => (ne ? R.createFreshWindow(e) : Promise.resolve(null)),
    ae = (e) => R.isTrustedIpcSender(e.sender, e.senderFrame ?? null);
  (H6({
    buildFlavor: o,
    getContextForWebContents: R.getContextForWebContents,
    isTrustedIpcEvent: ae,
    usesOwlAppShell: x,
  }),
    c.ipcMain.on(iu, (e) => {
      if (!ae(e)) {
        e.returnValue = `light`;
        return;
      }
      e.returnValue = c.nativeTheme.shouldUseDarkColors ? `dark` : `light`;
    }));
  let oe = () => {
    let e = c.nativeTheme.shouldUseDarkColors ? `dark` : `light`;
    R.windowManager.refreshWindowBackdrops();
    for (let t of c.BrowserWindow.getAllWindows())
      t.isDestroyed() || t.webContents.send(su, e);
  };
  (c.nativeTheme.on(`updated`, oe),
    P.add(() => {
      c.nativeTheme.off(`updated`, oe);
    }));
  let se = z6(),
    ce = null,
    le = (e) => {
      if (e?.quitImmediately === !1) {
        se.allowQuitTemporarilyForUpdateInstall();
        return;
      }
      (se.allowQuitTemporarilyForUpdateInstall(), c.app.quit());
    },
    ue = Xne({ disposables: P, isTrustedIpcEvent: ae }),
    de = [],
    fe = null,
    pe = null,
    me = (e) => !1,
    he = bp({
      ensureWindow: re,
      showPrimaryWindow: (e) => ne && R.windowManager.showPrimaryWindow(e),
    }),
    ge = n6({
      app: c.app,
      globalState: I.globalState,
      settingsStore: I.settingsStore,
      windowServices: R,
      repoRoot: I.repoRoot,
      errorReporter: y,
      desktopSentry: s,
      datadogLogger: w,
      performanceLogger: T,
      maxLogLevel: p,
      buildFlavor: o,
      sparkleManager: l,
      avatarOverlayManager: R.avatarOverlayManager,
      hotkeyWindowLifecycleManager: R.hotkeyWindowLifecycleManager,
      hasActiveComputerUseTurn: (e) => me(e),
      globalDictationHotkeyController:
        R.globalDictationLifecycleManager.getHotkeyController(),
      appshotHotkeysService: R.appshotHotkeysService,
      gitManager: ue.gitManager,
      requestComputerUseCaptureWorker: ue.requestComputerUseCaptureWorker,
      requestGitWorker: ue.requestGitWorker,
      requestOpenInWorker: ue.requestOpenInWorker,
      refreshApplicationMenu: () => {
        pe?.();
      },
      subscribeComputerUseCaptureWorkerEvent:
        ue.subscribeComputerUseCaptureWorkerEvent,
      subscribeGitWorkerEvent: ue.subscribeGitWorkerEvent,
      gitWorkerInvocationSampler: ue.gitWorkerInvocationSampler,
      updateWorkerSentryUser: ue.updateWorkerSentryUser,
      appEvent: ue.appEvent,
      onAppServerSqliteRecovery: he.onRecovery,
      onCompletedThreadsChanged: (e) => {
        ((de = e), fe?.setCompletedThreads(e));
      },
    });
  L.setThreadSuppressionPredicate(
    (e, t) =>
      ge
        .getElectronMessageHandlerForWindow(e)
        ?.getBrowserSidebarManager()
        .isBrowserSidebarPresentedForConversation(e, t) ?? !1,
  );
  let _e = ge.getWindowContext();
  d({
    onDownloadProgressChanged: () => {
      _e.broadcastAppUpdateState();
    },
    onDownloadedUpdateAppBrandChanged: () => {
      _e.broadcastAppUpdateState();
    },
    onInstallProgressChanged: () => {
      j && _e.broadcastAppUpdateState();
    },
    onUpdateReadyChanged: () => {
      _e.broadcastAppUpdateState();
    },
    onUpdateLifecycleStateChanged: () => {
      _e.broadcastAppUpdateState();
    },
    onRelaunchNoticeChanged: () => {
      _e.broadcastAppUpdateState();
    },
    onInstallUpdatesRequested: (e) => {
      le(e);
    },
    isTrustedIpcEvent: ae,
  });
  let ve = () => _e.appServerConnectionRegistry.getConnection(V),
    ye = j8({
      buildFlavor: o,
      globalState: I.globalState,
      errorReporter: y,
      sparkleManager: l,
      gitManager: ue.gitManager,
      getLocalAppServerClient: () => _e.getAppServerClient(),
      applyCodexAppConfig: async (e) => {
        try {
          await Ll(e, await _e.applyCodexAppConfigFromDisk(I.codexHome));
        } catch (t) {
          (r.r().warning(`Failed to apply Codex app config from deeplink`, {
            safe: {},
            sensitive: { error: t },
          }),
            await Ll(e, {
              status: `unexpected-error`,
              configPath: Il(I.codexHome),
              error: t,
            }));
        }
      },
      getLocalAppServerConnection: ve,
      windowServices: R,
      createFreshWindow: ie,
      ensureWindow: re,
      getElectronMessageHandlerForWindow: ge.getElectronMessageHandlerForWindow,
      allowDevtools: g,
      allowDebugMenu: v,
      enableSparkle: m,
      isMacOS: A,
      appVersion: b,
    });
  pe = ye.refreshApplicationMenu;
  let be = () => {
    ye.refreshApplicationMenu();
  };
  (ge.setOnBrowserSidebarStateChanged(() => {
    (ye.refreshApplicationMenu(), L.syncActiveThreadForFocusedWindow());
  }),
    c.app.on(`browser-window-focus`, be),
    P.add(() => {
      c.app.off(`browser-window-focus`, be);
    }),
    R.windowManager.setOnMainProcessInspectorStateChanged(() => {
      ye.refreshApplicationMenu();
    }),
    R.windowManager.setQueueCodexDeepLinkUrl(
      ye.deepLinks.queueCodexDeepLinkUrl,
    ),
    ue.setGitWorkerMainRpcHandler(
      yre({
        getAppServerClientForHost: ge.getAppServerClientForHost,
        getWorktreeCleanupInputsForHost: ge.getWorktreeCleanupInputsForHost,
      }),
    ),
    ue.setComputerUseCaptureWorkerMainRpcHandler(
      vre({
        ensureManagedComputerUseService: () => {
          if (ce == null)
            throw Error(`Managed Computer Use service is unavailable`);
          return ce.ensureServicePid();
        },
        invalidateManagedComputerUseService: (e) =>
          ce == null ? Promise.resolve() : ce.invalidateServicePid(e),
      }),
    ),
    ue.registerWorkerBusMessageHandlers(),
    (F = Date.now()),
    ye.deepLinks.registerProtocolClient(),
    t.n(Su(process.argv)),
    k(`registered deep link protocol`, F));
  let xe = (e) => {
      (e.isMinimized() && e.restore(), e.show(), e.focus());
    },
    Se = async () => {
      if (ne)
        try {
          R.hotkeyWindowLifecycleManager.hide();
          let e = R.getPrimaryWindow() ?? (await ie(`/`));
          if (e == null) return;
          xe(e);
        } catch (e) {
          y.reportNonFatal(
            e instanceof Error ? e : `Failed to open window on second instance`,
            { kind: `second-instance-open-window-failed` },
          );
        }
    };
  f((e) => {
    let n = t.n(Su(e));
    if (ye.deepLinks.queueProcessArgs(e)) {
      n && Se();
      return;
    }
    if (n) {
      Se();
      return;
    }
    Se();
  });
  let Ce = async (e, t) => {
    if (!ne) return;
    R.hotkeyWindowLifecycleManager.hide();
    let n = R.getPrimaryWindow(),
      r = n ?? (await ie(e));
    r != null &&
      (ge.desktopNotificationManager.dismissByNavigationPath(e),
      n != null && t.navigateExistingWindow && ye.navigateToRoute(r, e),
      xe(r));
  };
  A &&
    ((fe = new rd((e) => Ce(e, { navigateExistingWindow: !0 }), a.H())),
    fe.setCompletedThreads(de));
  let we = async () => {
      N = !0;
      try {
        await ire({
          appBrand: a.U(),
          buildFlavor: o,
          repoRoot: I.repoRoot,
          onTrayButtonClick: async () => {
            R.hotkeyWindowLifecycleManager.show() || (await re());
          },
          onTrayMenuOpenMainWindow: () =>
            Ce(`/`, { navigateExistingWindow: !1 }),
          onTrayMenuOpenNewThread: () =>
            Ce(`/`, { navigateExistingWindow: !0 }),
          onTrayMenuOpenRecentThread: (e) =>
            Ce(e, { navigateExistingWindow: !0 }),
          getChronicleSidecarControlState: () =>
            _e.appServerConnectionRegistry
              .getMaybeConnection(`local`)
              ?.getChronicleSidecarControlState() ?? $9,
          toggleChronicleSidecar: async () => {
            let e = _e.appServerConnectionRegistry.getMaybeConnection(V);
            return e == null
              ? $9
              : e.getChronicleSidecarControlState().running
                ? e.pauseChronicleSidecar()
                : e.resumeChronicleSidecar();
          },
        });
      } catch (e) {
        ((N = !1),
          y.reportNonFatal(e instanceof Error ? e : `Failed to set up tray`, {
            kind: `tray-setup-failed`,
            tags: { errorType: `tray-setup-failed` },
          }),
          re());
      }
    },
    Te = async (e) => {
      if (A) {
        if (((N = e), !e)) {
          G9();
          return;
        }
        await we();
      }
    };
  j && we();
  let Ee = er({
    codexCliPath: rr({ isPackaged: c.app.isPackaged, repoRoot: I.repoRoot })
      .codexCliPath,
    codexHome: I.codexHome,
    onAnalyticsEvent: (e) => {
      let t = R.getPrimaryWindow();
      t != null &&
        R.windowManager.sendMessageToWebContents(t.webContents, {
          type: `computer-use-analytics-event`,
          event: e,
        });
    },
  });
  ((me = Ee.hasActiveTurn),
    P.add(ho({ appServerConnection: ve(), isEnabled: ee })),
    P.add(
      ql({ appServerConnection: ve(), closeActiveTurn: Ee.closeActiveTurn }),
    ));
  let De = Al({
      appVersion: b,
      buildFlavor: o,
      codexHome: I.codexHome,
      getLocalAppServerConnection: ve,
      globalState: I.globalState,
      isPackaged: c.app.isPackaged,
      onInstallStateObserved: (e) => {
        let t = R.getPrimaryWindow();
        t != null &&
          R.windowManager.sendMessageToWebContents(t.webContents, {
            type: `bundled-plugin-install-state-observed`,
            observation: e,
          });
      },
      onSkillsMayHaveChanged: () => {
        R.sendMessageToAllRegisteredWindows({
          type: `ipc-broadcast`,
          method: `query-cache-invalidate`,
          sourceClientId: `desktop`,
          version: n.fc(`query-cache-invalidate`),
          params: { queryKey: [`skills`] },
        });
      },
      onReconcileComplete: () => {
        (R.sendMessageToAllRegisteredWindows({
          type: `ipc-broadcast`,
          method: `query-cache-invalidate`,
          sourceClientId: `desktop`,
          version: n.fc(`query-cache-invalidate`),
          params: { queryKey: [`plugins`] },
        }),
          Ee.refresh());
      },
      repoRoot: I.repoRoot,
      trashItem: (e) => c.shell.trashItem(e),
    }),
    Oe = () => {
      De.reconcileExternalPluginState(`focus`);
    };
  if (
    (c.app.on(`browser-window-focus`, Oe),
    P.add(() => {
      c.app.off(`browser-window-focus`, Oe);
    }),
    A)
  ) {
    try {
      await Wl({ sourceServiceAppPath: mee(), codexHome: I.codexHome });
    } catch (e) {
      r.r().warning(
        `Failed to sync canonical Computer Use service app during startup`,
        { safe: {}, sensitive: { error: e } },
      );
    }
    if (((ce = new Gl(Ul(I.codexHome))), P.add(ce), i.a.isInternal(o))) {
      delete process.env[tr];
      try {
        let e = await qd({
          services: new Map([
            [
              `computer-use`,
              async () => {
                if (ce == null)
                  throw Error(`Managed Computer Use service is unavailable`);
                return (await ce.ensureServicePid(), {});
              },
            ],
          ]),
        });
        ((process.env[tr] = e.pipePath),
          P.add(() => {
            (process.env.NODE_REPL_HOST_SERVICES_PIPE_PATH === e.pipePath &&
              delete process.env[tr],
              e.dispose());
          }));
      } catch (e) {
        (delete process.env[tr],
          r.r().warning(`Failed to start node_repl host services native pipe`, {
            safe: {},
            sensitive: { error: e },
          }));
      }
    }
  }
  let ke = yt({
    setBrowserUseNativePipeEnabled: (e) => {
      _e.getBrowserSessionRegistry().setBrowserUseNativePipeEnabled(e);
    },
  });
  P.add(() => {
    (ke.dispose(), Ee.dispose());
  });
  let Ae = (e, t) => {
    e.openAIMcpFormElicitations != null &&
      I.globalState.set(a.S, e.openAIMcpFormElicitations);
    let n = Xe(e);
    if (
      (ye.setDesktopFeatureAvailability(n),
      De.setDesktopFeatureAvailability(n, t),
      ke.setDesktopFeatureAvailability(n),
      Ee.setDesktopFeatureAvailability(n),
      te(),
      ce != null)
    ) {
      let e = Ke(),
        t = e.appshotsEnabled,
        n = e.computerUse && e.computerUseNodeRepl;
      ce.setEnabledFeatures({ appshotsEnabled: t, nodeReplEnabled: n }).catch(
        (e) => {
          r.r().warning(`Failed to reconcile managed Computer Use service`, {
            safe: { appshotsEnabled: t, nodeReplEnabled: n },
            sensitive: { error: e },
          });
        },
      );
    }
  };
  (V6({
    desktopSentry: s,
    hotkeyWindowLifecycleManager: R.hotkeyWindowLifecycleManager,
    globalDictationLifecycleManager: R.globalDictationLifecycleManager,
    codexHome: I.codexHome,
    globalState: I.globalState,
    nativeContextMenuIconSearchRoots: [
      (0, u.join)(I.repoRoot, `webview`, `public`),
      (0, u.join)(I.desktopRoot, `webview`),
    ],
    getContextForWebContents: R.getContextForWebContents,
    getPrimaryWindow: R.getPrimaryWindow,
    showPrimaryWindow: (e) => ne && R.windowManager.showPrimaryWindow(e),
    sendMessageToWindow: R.sendMessageToWindow,
    ensureWindow: re,
    createFreshWindow: ie,
    setDesktopFeatureAvailability: Ae,
    setAvatarOverlayFeedbackDiagnosticsEnabled: (e) => {
      R.avatarOverlayManager.setFeedbackDiagnosticsEnabled(e);
    },
    setAutomaticBackgroundDownloadsEnabled: (e) => {
      l.setAutomaticBackgroundDownloadsEnabled(e);
    },
    restoreAvatarOverlayOpenState: (e) =>
      R.avatarOverlayManager.restoreOpenState(e),
    getAvatarOverlayCompositionSurfaceHost: (e) =>
      R.avatarOverlayManager.getCompositionSurfaceHost(e),
    reloadBundledPlugins: () => De.reloadBundledPlugins(),
    setMacMenuBarEnabled: Te,
    navigateToRoute: ye.navigateToRoute,
    setRemoteHostedPIPActiveThreadID: L.setActiveThreadForWebContents,
    setRemoteHostedPIPHiddenThreadIDs: L.setHiddenThreadIDs,
    setRemoteHostedPIPHostLayout: L.setHostLayoutForWebContents,
    getDockMenuController: () => fe,
    getTrayController: rre,
    isTrustedIpcEvent: ae,
  }),
    new Vp(c.app, y, r.r()).register(),
    U6({
      isWindows: j,
      disableQuitConfirmationPrompt:
        process.env.CODEX_ELECTRON_DISABLE_QUIT_CONFIRMATION === `1`,
      quitState: se,
      windows: R,
      applicationMenuManager: ye.applicationMenuManager,
      ensureWindow: re,
      hotkeyWindowLifecycleManager: R.hotkeyWindowLifecycleManager,
      globalDictationLifecycleManager: R.globalDictationLifecycleManager,
      globalState: I.globalState,
      settingsStore: I.settingsStore,
      flushAndDisposeContext: ge.flushAndDisposeContext,
      disposables: P,
      appEvent: ue.appEvent,
    }),
    (F = Date.now()),
    Sre(I.globalState, I.settingsStore));
  try {
    await _e.applyCodexAppConfigFromDisk(I.codexHome);
  } catch (e) {
    r.r().warning(`Failed to apply Codex app config during startup`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  F = Date.now();
  let je = _e.appServerConnectionRegistry.getConnection(V),
    Me = () => {};
  if (
    !(await a.i({
      databasePath: a.t,
      onBackfillWait: async () => {
        try {
          Me = await I8({
            onQuit: () => {
              (se.markQuitApproved(), c.app.quit());
            },
          });
        } catch (e) {
          r.r().warning(`Failed to show SQLite backfill progress window`, {
            safe: {},
            sensitive: { error: e },
          });
        }
      },
      open: async () => {
        (await je.connect(), await he.enable());
      },
      shouldHandleError: Cre,
    }))
  )
    return;
  k(`local app-server sqlite initialized`, F);
  try {
    await I.settingsStore.initialize({
      readConfig: () => je.getUserSavedConfiguration(),
      batchWriteConfigValues: (e) =>
        je.sendAppServerRequest(`config/batchWrite`, e),
    });
  } catch (e) {
    r.r().warning(`Failed to initialize desktop settings`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  (L.ensureContentHostStarted(),
    (le = (e) => {
      if (e?.quitImmediately === !1) {
        se.allowQuitTemporarilyForUpdateInstall({
          allowWithoutPrompt: !0,
          skipDrainBeforeQuit: !0,
        });
        return;
      }
      cre({
        exitImmediately: j && c.app.isPackaged,
        exitImmediatelyForUpdateInstall: () => {
          Tre({
            runCleanupBeforeImmediateExit: () => {
              (G9(),
                B6({
                  hotkeyWindowLifecycleManager: R.hotkeyWindowLifecycleManager,
                  globalDictationLifecycleManager:
                    R.globalDictationLifecycleManager,
                  flushAndDisposeContext: ge.flushAndDisposeContext,
                  disposables: P,
                }));
            },
            markAppQuitting: R.markAppQuitting,
          });
        },
        quitForUpdateInstall: () => {
          c.app.quit();
        },
        quitState: se,
      });
    }));
  let Ne = qi({
      appVersion: b,
      appServerConnectionRegistry: _e.appServerConnectionRegistry,
      getDesktopFeatureAvailability: Ke,
      gitManager: ue.gitManager,
      globalState: I.globalState,
      threadProjectAssignments: new WP(I.globalState, R.windowManager),
      settingsStore: I.settingsStore,
      appServerClient: _e.getAppServerClient(),
      worktreeLifecycle: {
        requestCleanup: () => {
          _e.getMessageHandler().getWorktreeService().requestCleanup();
        },
        setOwnerThread: ({ conversationId: e, worktree: t }) =>
          _e
            .getMessageHandler()
            .getWorktreeService()
            .setOwnerThread({ conversationId: e, worktree: t }),
      },
      requestDynamicToolsForThreadStart: () =>
        _e.getMessageHandler().requestDynamicToolsForThreadStart(null, !0),
      isPackaged: c.app.isPackaged,
      repoRoot: I.repoRoot,
    }),
    Pe = mt({
      appEvent: ue.appEvent,
      appServerClient: _e.getAppServerClient(),
      appServerConnectionRegistry: _e.appServerConnectionRegistry,
      globalState: I.globalState,
      settingsStore: I.settingsStore,
    });
  (P.add(
    n.Yr((e) => {
      let t = R.getPrimaryWindow();
      t != null &&
        R.windowManager.sendMessageToWebContents(t.webContents, {
          type: `ephemeral-generation-token-usage`,
          usage: e,
        });
    }),
  ),
    _e.getMessageHandler().setAutomationSchedulerController(Ne),
    P.add(() => {
      (Ne.dispose(), Pe.dispose());
    }),
    k(`window context initialized`, F));
  let Fe = Ke(),
    Ie = Xe(Fe);
  (Je(Fe, Ie) && Ae(Ie),
    (F = Date.now()),
    await mre({
      isWindows: j,
      isPackaged: c.app.isPackaged,
      executablePath: process.execPath,
    }),
    k(`windows folder context menu registered`, F, { isWindows: j }),
    (F = Date.now()),
    ye.refreshApplicationMenu(),
    k(`application menu refreshed`, F),
    (F = Date.now()),
    await De.waitForPendingReconcile(),
    k(`bundled plugins reconcile checked`, F, { isMacOS: A }),
    (F = Date.now()));
  let Le = await R.ensureWindow();
  (Le &&
    (P.add(
      bre({
        ensureWindow: R.ensureWindow,
        errorReporter: y,
        globalState: I.globalState,
        isMacOS: A,
        windowManager: R.windowManager,
      }),
    ),
    xe(Le)),
    (ne = !0),
    Me(),
    k(`window ensured`, F, { windowVisible: Le?.isVisible() ?? !1 }),
    (F = Date.now()),
    await ye.deepLinks.flushPendingDeepLinks(),
    k(`pending deep links flushed`, F),
    k(`startup complete`, e));
}
function Tre({ markAppQuitting: e, runCleanupBeforeImmediateExit: t }) {
  let n;
  try {
    e();
    try {
      gre();
    } catch (e) {
      n ??= e;
    }
    try {
      _re();
    } catch (e) {
      n ??= e;
    }
    t();
    for (let e of c.BrowserWindow.getAllWindows())
      e.isDestroyed() || e.destroy();
    if (n != null)
      throw n instanceof Error
        ? n
        : Error(typeof n == `string` ? n : `Windows update cleanup failed.`);
  } catch (e) {
    r.r().warning(`Immediate update exit cleanup failed before process exit`, {
      safe: {},
      sensitive: { error: e },
    });
  } finally {
    c.app.exit(0);
  }
}
exports.runMainAppStartup = wre;
//# sourceMappingURL=main-BOrkpaTV.js.map
