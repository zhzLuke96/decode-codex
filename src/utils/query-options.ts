// Restored from ref/webview/assets/queryOptions-TXUhkfjF.js
// Identity helper preserving query option inference at call sites.

export function queryOptions<TOptions>(options: TOptions): TOptions {
  return options;
}
