// Restored from ref/webview/assets/app-initial~app-main~remote-conversation-page~hotkey-window-thread-page~automations-page~th~bnlvjk3w-ClqKjb2h.js
// Passthrough in the current build: the workspace root string is returned as-is (the host canonicalizes it).
export function normalizeWorkspaceRoot(path: string): string {
  return path;
}
