// Restored from ref/webview/assets/app-intl-signal-Bd_tJ6VJ.js
// app-intl-signal-Bd_tJ6VJ chunk restored from the Codex webview bundle.
import * as appScopeRuntime from "../boundaries/app-scope";
import { defineMessages } from "../vendor/react-intl";
export const appIntlSignal = appScopeRuntime._appScopeG(
  appScopeRuntime._appScopeT,
  defineMessages({
    locale: "en",
    messages: {},
  }),
);

export function initAppIntlSignalRuntime(): void {
  // Legacy chunks exposed a Rollup initializer; ESM imports initialize the
  // restored app-intl signal eagerly.
  void appIntlSignal;
}
