// Restored from ref/webview/assets/app-initial~app-main~onboarding-page-BUwCKIcU.js
// Returns whether the given platform identifier is the Electron desktop platform.
export function isElectronPlatform(platform: string): boolean {
  return platform === "electron";
}
