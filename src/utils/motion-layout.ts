// Restored from ref/webview/assets/app-initial~app-main~automations-page-Bc0ZtIBr.js
// Thin Framer Motion layout primitives used by the home page body.
import { motion } from "framer-motion";

export const motionLayoutDiv = motion.div;

export const motionTransitionConfig = {
  type: "spring",
  duration: 0.5,
  bounce: 0,
} as const;
