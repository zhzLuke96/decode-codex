// Restored from ref/webview/assets/local-environment-selection-Dz7Qei2P.js
// Persisted workspace selections for local environment config paths.

import { persistedAtom } from "../../utils/persisted-atom";
import type { LocalEnvironmentSelectionsByWorkspace } from "./types";
export const localEnvironmentSelectionsByWorkspaceAtom =
  persistedAtom<LocalEnvironmentSelectionsByWorkspace>(
    "local-env-selections-by-workspace",
    {},
  );
