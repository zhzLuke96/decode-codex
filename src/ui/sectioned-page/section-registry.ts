// Restored from ref/webview/assets/sectioned-page-BgyNiIIs.js
// Context used by section headers to register scroll targets.
import React from "react";
import type { SectionRegistryContextValue } from "./types";

export const SectionRegistryContext =
  React.createContext<SectionRegistryContextValue | null>(null);
